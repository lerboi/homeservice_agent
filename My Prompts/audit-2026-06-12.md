# Voco Full-System Audit — 2026-06-12  ·  Remediation tracker (updated 2026-06-19)

> Eight parallel deep passes over the working tree, the live Supabase DB
> (`exbzhmparzjlpkryeiso`, via MCP), and the `livekit-agent` repo. This file has
> since been turned into a **living remediation tracker**. Every finding is
> annotated with a status and a note describing what was done / what is left.

---

## ⚡ RESUME SUMMARY — read this first (updated 2026-06-19)

**You are mid-remediation of this audit. This section tells a fresh context exactly where things stand.**

### Deployment topology (critical context)
- **Two repos:**
  - Main Next.js app: `C:\Users\leheh\.Projects\homeservice_agent` → deploys to **Vercel** on push to `main`. Canonical prod host is **`https://www.voco.live`** (apex `voco.live` 307-redirects to `www`).
  - Voice agent: `C:\Users\leheh\.Projects\livekit-agent` (sibling dir) → deploys to **Railway** on push to its `main`. GitHub remote: `lerboi/livekit_agent`.
- **DB migrations are applied OUT-OF-BAND** via the Supabase MCP. There is **no migration registry** (`supabase_migrations.schema_migrations` does not exist; `list_migrations` returns `[]`). ⇒ To verify a migration is applied, **query the live catalog directly** (pg_proc / pg_constraint / information_schema), never trust `list_migrations`.
- **`.env.local` has `NEXT_PUBLIC_APP_URL=http://localhost:3000`** (prod is `www.voco.live`). ⇒ Never register Google Calendar watch channels from a local script without overriding the webhook address to the prod host (Google can't reach localhost).
- **`CRON_SECRET` is now SET in Vercel** (added 2026-06-18). Before that it was missing → every one of the 11 Vercel crons 500'd and never ran (this was the root cause of the calendar freeze + made the H4/H5 crons inert). Value lives in `.env.local`. All crons run now.

### Work completed in three waves
- **WAVE 1 (the original 2026-06-12 fix wave):** implemented uncommitted, then **committed + pushed by the user** (main commit `5a5814c "updated"`; livekit `8fbe053`). Plus migrations **068–071 applied live** via MCP. This closed the Blockers, Criticals, and most Highs.
- **WAVE 2 (2026-06-18, "fix the rest"):** main commits `45badbf → 6eef7b6`; livekit `2ea2400`, `3d02f91`. See per-item notes for the mapping. All builds green (`next build` / `py_compile`).
- **H6 calendar incident (2026-06-18):** 2 frozen Google calendars revived live; durable daily sweep + alerting shipped (`d52211a`). One-off ops scripts left in `scripts/`: `revive-google-calendars.mjs`, `reregister-google-watches.mjs` (run via `node --env-file=.env.local scripts/<file>`).
- **WAVE 3 (2026-06-19, "no-external-dependency sweep"):** every remaining no-external item was first re-verified against current code + live DB, then a full adversarial review ran over the diffs. Implemented **19** items — main commits `734bbee → 900af75`, livekit `9dea46e`. Closed **H2, M20c, LOW-8/9/11/13/14/15/16/18/20/21/22/23/24/25/29/31/32**. `next build` green; livekit 35 tests pass. Verification reclassified **M27 / LOW-5 / LOW-10** as actually needing migrations/Twilio config (moved to the external-step bucket) and found **LOW-19** is a non-defect. Committed to branch **`fix/audit-noexternal-2026-06-18`** in both repos — **NOT pushed** (no auto-deploy yet). One live DB change: the 56 legacy `stripe_webhook_events` rows were marked `processed=true` via MCP (part of H2).

### Scoreboard
| Severity | Total | ✅ Fixed | ⏳ Partial | ❌ Not done | 🚫 Won't-fix (accepted) |
|---|---|---|---|---|---|
| Blockers | 2 | 2 | – | – | – |
| Critical | 4 | 4 | – | – | – |
| High | 11 | 10 | – | 1 (H10) | – |
| Medium | 27 | 21 | – | 6 (M8,M13,M15,M16,M17,M27) | – |
| Low | 32 | 22 | – | 7 (LOW-1,2,5,10,12,26,30) | 3 (LOW-4,6,19) |

### 👉 What to do next (start here)
The no-external-dependency sweep is **DONE (WAVE 3)** and sits on branch **`fix/audit-noexternal-2026-06-18`** in both repos, **unpushed**. Merge + push to deploy (main → Vercel, livekit → Railway).

Everything remaining needs a decision or external step from the user:
- **DB migrations (user applies SQL via MCP):** **M27** (add `calls.last_heartbeat_at` + ideally `is_test_call`, livekit watchdog write, cron back-fill), **M13** (external-busy recheck in `book_appointment_atomic`), **M15** (manual-booking provider column), **LOW-5** (`DROP DEFAULT/NOT NULL` on `processed_at`), **LOW-10** (`sms_opt_outs` table + Twilio opt-out config), **LOW-12** (webhook dedup constraint), **LOW-26** (`book_appointment_atomic` exclusion handling), **LOW-30** (perf-advisor sweep), **LOW-1/LOW-2** (renumber dup 030 / migration registry).
- **M8** — OAuth `state` hardening (new `OAUTH_STATE_SECRET` Vercel env var + nonce/expiry + 4 callback ownership re-checks).
- **H10** — demo-call audio authoring (bespoke dialogue) + 3 ElevenLabs voice-preview clips.
- **M16** — zones wiring (cross-repo design change).
- **M17** — pre-session context-fetch backgrounding (risky live-call path; deferred intentionally).

Status legend: ✅ FIXED · ⏳ PARTIAL · ❌ NOT DONE · 🚫 WON'T FIX (accepted)

---

## BLOCKERS

### ✅ B1. `next build` fails: stale `src/app/dashboard/leads/` resurrects deleted components
Original: untracked `src/app/dashboard/leads/` imported deleted Phase-59 `Lead*` components, fetched `/api/leads`, subscribed to dropped `leads` table → build died.
**Status (WAVE 1):** Directory and `/api/leads` route are gone; no live `Lead*` imports (only stale comments) and the `leads` table is dropped live. Jobs feature replaced it. `next build` green. Verified.

### ✅ B2. Migration 068 not applied to live DB
Original: webhook inserts `processing_started_at` on every event → 500 for every Stripe event until 068 runs.
**Status:** 068 **applied live** — `stripe_webhook_events.processing_started_at`, `idx_subscriptions_one_current`, `oauth_refresh_locks` RLS, and all hot-path indexes confirmed present in the live catalog. (Note: migration registry still absent — separate observability gap, see LOW-2.)

---

## CRITICAL — security

### ✅ S1. 7 SECURITY DEFINER RPCs executable by `anon`/`authenticated`
**Status:** Migration **069 applied live**. All 7 (`book_appointment_atomic`, `assign_sg_number`, `set_primary_calendar`, `record_call_outcome`, `merge_customer`, `unmerge_customer`, `increment_rate_limit`) now have EXECUTE revoked from `anon`+`authenticated` (service_role only) and `search_path=public, pg_temp` pinned — verified via `has_function_privilege` on every overload.

### ✅ S2. `oauth_refresh_locks` wide open (RLS disabled + anon grants)
**Status:** RLS **enabled live**, zero policies, client roles can't bypass → default-deny. Lock RPCs revoked from anon/authenticated. Exploit path closed.

### ✅ S3. Onboarding checkout `ui_mode: 'embedded_page'` under Basil pin
**Status:** Code sends `ui_mode: 'embedded'` (Basil-valid) and `stripe.js` pins `2025-06-30.basil`. Pin+enum consistent. Committed/deployed. (Live runtime success was test-mode-verified 2026-06-12 per code comments.)

### ✅ S4. Voice pipeline: missing DEEPGRAM/ELEVEN/OPENAI keys kill calls silently
**Status:** `agent.py __main__` has a fail-fast preflight asserting `OPENAI_API_KEY`/`DEEPGRAM_API_KEY`/`ELEVEN_API_KEY` before boot. Committed (livekit), deployed. (Optional ElevenLabs `/v1/voices` probe NOT added — bad voice_id still fails at TTS time; out of scope.)

---

## HIGH

### ✅ H1. `past_due`/`unpaid` never enforced
**Status:** Shared Python gate `livekit-agent/src/lib/subscription_gate.py` blocks canceled/paused/incomplete always, and past_due after a 3-day grace; consumed by `agent.py` (disconnects) + `twilio_routes.py` (routes to AI, not owner-pickup). `proxy.js` dead gate removed; `BillingWarningBanner` persists past grace (no dismiss). Deployed. (Residual: stale `src/lib/subscription-gate.js` is dead code with zero callers — harmless, could be deleted.)

### ✅ H2. Stripe webhook can permanently lose events (false 200 in claim window)
**Status: FIXED (WAVE 3, `734bbee`).** In-request fix was already deployed. Added `/api/cron/reconcile-stripe-webhooks` (6-hourly, `30 */6 * * *`): re-fetches still-`processed=false` events from Stripe and replays them through the 5 handlers (now `export`ed from the webhook route, verified by build). Claims each row first (same stale-claim rule) so it can't race a live delivery; ages out >25d events (Stripe ~30d retention) and marks aged-out/`resource_missing` rows **terminal** so they can't starve the oldest-first batch; Sentry-alerts on every recovery. The 56 legacy pre-fix rows (test-mode QA, unrecoverable from Stripe) were one-time marked `processed=true` via MCP — live `unprocessed=0`. Known bound (documented in the cron): a >25d-stuck `invoice.paid` replay could zero the current cycle's usage — not worth touching the renewal-race-sensitive shared handler.

### ✅ H3. Annual-plan overage unbillable
**Status:** Both checkout routes set `billing_mode: { type: 'flexible' }` for annual; attach failure now Sentry-alerts (no silent swallow) in webhook + verify-checkout. Committed/deployed.

### ✅ H4. Failed Stripe meter events permanently lost
**Status:** Outbox table `stripe_meter_failures` (migration **071, live**); `post_call.py` upserts failures to it; `/api/cron/retry-meter-events` (every 6h) replays with idempotent `overage_<call_id>` identifier + Sentry alert at MAX_ATTEMPTS. Cron now actually runs (CRON_SECRET set).

### ✅ H5. Twilio numbers never released on churn
**Status:** `/api/cron/release-churned-numbers` (daily) releases SG inventory / Twilio US-CA numbers for is_current canceled tenants past a 30-day retention; `provisionPhoneNumber` is idempotent (friendlyName tag / inventory reuse). Committed via WAVE 1 + cron live now.

### ✅ H6. Calendar mirror freezes silently → double-bookings
**Status (this session):** Outlook 410-resync, monthly window re-anchor, Outlook 404-recreate were WAVE 1. **Subpart 4 (daily pull-sync sweep + staleness re-anchor + Sentry alerting) shipped `d52211a`** and verified live (`swept:2`). Root cause of the real-world freeze was **missing `CRON_SECRET` in Vercel** (now fixed). Two frozen Google calendars (Make It AI, voco) revived live 2026-06-18 (events resynced, watches re-registered through expiry, push restored). Blank-mirror-safe re-anchor (sync-first/purge-after) also addressed. See M14 for the lib-level stale purge.

### ✅ H7. Python refresh-lock loser re-refreshes → bricks Jobber
**Status:** `jobber.py`/`xero.py` give up on poll timeout (return None), never fall through to a second wire refresh; `error_state` only set on definitive 400/401. Committed (livekit), deployed.

### ✅ H8. Post-call 8s budget vs 5s triage starves notifications
**Status:** Layer-2 triage timeout 5.0→2.5s; explicit 3.0s Stripe meter timeout; record_outcome + owner notifications reordered BEFORE the slow slot calc; EMERGENCY forces sms+email. Committed (livekit), deployed.

### ✅ H9. Dashboard data-loss/UX highs (7 subparts)
**Status:** All 7 fixed (WAVE 1): pagination (useDocumentList total_count/loadMore), JobFlyout transcript+recordings, realtime INSERT refetch, reconnect handling on 8 channels, AbortController on all listed fetches, mobile FAB clears BottomTabBar, UnmergeBanner self-detects via `/api/admin/merges?active=1`. Deployed.

### ❌ H10. Public site: flagship demo audio broken + voice previews missing
**Status: NOT DONE.** `public/audio/demo-emergency.mp3` + `demo-routine.mp3` are still byte-identical 3s stubs (vs 24–30s transcripts in `AudioDemoSection.jsx`); `public/audio/voices/` still holds only 6 obsolete Gemini-era stubs — `professional.mp3`/`friendly.mp3`/`local_expert.mp3` don't exist so dashboard `VoicePickerSection` previews 404 (it now shows a graceful "unavailable" toast). **Next:** generate real demo-call audio (bespoke dialogue — needs authoring); the 3 voice-preview clips can be ElevenLabs-generated (key in `.env.local`, voice IDs `ELEVENLABS_VOICE_ID_AI`/`_CALLER`). Place in `public/audio/`.

### ✅ H11. `ai_voice` voice picker silent no-op
**Status:** Migration **070 applied live** — `tenants_ai_voice_check` allows `professional|friendly|local_expert`; agent `ELEVENLABS_VOICE_MAP` honors those labels; dashboard `VoicePickerSection` + `ai-voice-validation.js` write the same set. End-to-end consistent. (Note: `local_expert` intentionally reuses `professional`'s voice_id — only 2 ElevenLabs voices provisioned; persona differs via tone.)

---

## MEDIUM

**Billing/Stripe**
- ✅ **M1.** Canceled tenants got unlimited repeat 14-day trials. **FIXED (WAVE 2, `347293d`):** onboarding checkout grants no trial to tenants with any prior subscription (incl. canceled) and reuses `stripe_customer_id`.
- ✅ **M2.** `invoice.paid` usage-reset race acked 0-row UPDATE. **FIXED (WAVE 1):** captures affected rows, throws → Stripe retries when 0 matched.
- ✅ **M3.** LiveKit FastAPI read an arbitrary subscription history row. **FIXED:** `twilio_routes.py` filters `subscriptions.is_current=True`.
- ✅ **M4.** Webhook tenant resolution metadata-only; dashboard-created subs dropped. **FIXED (WAVE 2, `347293d`):** `handleSubscriptionEvent` falls back to resolving tenant via `stripe_customer_id`.
- ✅ **M5.** `verify-checkout` `syncSubscription` had 3 bugs the webhook fixed. **FIXED (WAVE 2, `347293d`):** orders by `subscription.created` (not nonexistent `subscription.updated`), Date.parse numeric compare, tenant_id-scoped current-row lookup, same-sub-id calls_used carry-forward.
- ✅ **M6.** Double-subscription guard check-then-act. **FIXED/MITIGATED (WAVE 2, `347293d`):** both checkout routes pass a `tenant+plan+interval` idempotencyKey so identical parallel-tab checkouts collapse to one session. (NOT a full distributed lock — two tabs with DIFFERENT plans could still create two subs; acceptable for the realistic double-click case. A bulletproof fix would need a pending-sentinel row guarded by `idx_subscriptions_one_current`.)

**Auth/API**
- ✅ **M7.** Google webhook trusted spoofable `X-Goog-Channel-Token` as tenant_id. **FIXED (WAVE 1):** header used for logging only; tenant resolved from DB via `watch_channel_id`; missing channel-id dropped.
- ❌ **M8.** OAuth `state` (all 4 providers) is a static non-expiring per-tenant HMAC **signed with the Supabase service-role key**; callbacks never re-verify the logged-in user owns the tenant. **NOT DONE.** **Next:** add an `OAUTH_STATE_SECRET` env var (set in Vercel) used by `signOAuthState`/`verifyOAuthState` (`src/app/api/google-calendar/auth/route.js`) with a random nonce + issued-at + short TTL; in each of the 3 callback routes (google-calendar, outlook-calendar, integrations/[provider]) call `createSupabaseServer().auth.getUser()` and confirm the user's tenant matches the state's tenantId before upserting credentials. Implement with a fallback to the service-role key if `OAUTH_STATE_SECRET` is unset so it can't hard-break.
- ✅ **M9.** Twilio webhook signature forgeable if token empty / `ALLOW_UNSIGNED` ungated. **FIXED:** `security.py` raises 503 on empty token (fail-closed); `ALLOW_UNSIGNED_WEBHOOKS` only bypasses when `PYTHON_ENV != production` (default production). Committed (livekit).

**Data/tokens**
- ✅ **M10.** OAuth tokens owner-browser-readable plaintext. **FIXED:** migration **069 live** revokes `accounting_credentials`/`calendar_credentials` (and the token columns) from `anon`+`authenticated`; service_role only. Verified via `has_column_privilege`.
- ✅ **M11.** Integration telemetry 100% dropped (enum missing + fanout never called). **FIXED:** enum values `integration_fetch`+`integration_fetch_fanout` live (071); per-provider emit wired (WAVE 1); **fanout emit added (WAVE 2, livekit `2ea2400`)** at the agent.py gather boundary (fire-and-forget with per-task timings).

**Scheduling**
- ✅ **M12.** All-day blocks vanish from busy set in west-of-UTC evenings (UTC-midnight prefilter). **FIXED (WAVE 2, main `b1f366a` + livekit `3d02f91`):** widened the `calendar_events`/`calendar_blocks` `end_time` prefilter by 24h in available-slots (JS) + `_availability_lib.fetch_scheduling_data` + `book_appointment` slot-taken recalc (PY).
- ❌ **M13.** `book_appointment` never re-checks external calendar busy at booking time (10-min slot tokens). **NOT DONE — needs a migration.** **Next:** add a `calendar_events`+`calendar_blocks` overlap check inside `book_appointment_atomic` (all-day-aware) via migration 072, OR re-fetch external busy and re-validate immediately before calling the RPC (app-side, no migration). RPC currently checks only `appointments` overlap.
- ✅ **M14.** Full/initial resync never purges stale mirror rows. **FIXED (WAVE 2, `6eef7b6`):** Google `syncCalendarEvents` full-resync deletes rows not refreshed this run (synced_at < runStart) + nulls dependent refs, after the upsert (blank-mirror-safe). Outlook 410 already purged; the daily cron re-anchor also purges.
- ❌ **M15.** Manual bookings never write `external_event_provider`; `calendar_blocks` has no provider column → provider-matched cancel can hit the wrong provider. **NOT DONE — needs a migration.** Cancel READER already prefers `external_event_provider` (WAVE 1). **Next:** (a) dashboard manual-booking POST (`/api/appointments` POST) must write `external_event_provider` after the eventId; (b) add a `provider` column to `calendar_blocks` (migration) + write it on push; (c) make calendar-blocks PATCH/DELETE resolve creds by that owning provider (is_primary as null-fallback).
- ❌ **M16.** Zones/travel-buffers are dead code in BOTH repos (flat 30-min buffer; zones UI is theatre). **NOT DONE — design change.** **Next:** resolve a candidate zone from the booking's postal code (via `service_zones.postal_codes`) and thread it through `candidate_zone_id`/`candidateZoneId` in slot calc AND `p_zone_id` in `book_appointment_atomic`, in both the voice agent (`book_appointment.py` hardcodes `zone_id=None`) and the dashboard manual-booking/available-slots paths.
- ❌ **M17.** Pre-session Xero/Jobber context fetch blocks the greeting up to 2.5s (dead air). **NOT DONE — DEFERRED (risky).** The gather is still awaited before `session.start` in `agent.py`. **Next (when ready):** fire the merged-context/caller-history fetch as a background task, start the session + deterministic greeting immediately, and inject the customer-context STATE block via a system-prompt/chat-ctx update once it resolves (completes during ~5-7s greeting playout). High-risk live-call path — test carefully (see memory notes on agent fragility / cascade failures).

**Dashboard/API**
- ✅ **M18.** Calls badge mixes windows + UTC today/month boundaries. **FIXED (WAVE 2, `2b28f60`):** `dashboard/stats` computes today/first-of-month in the tenant's timezone via new `src/lib/tenant-time.js`. (Window-mixing of all-time open inquiries + today's missed calls left as-is — defensible product semantics; possible per-call double-count not de-duped.)
- ✅ **M19.** `GET /api/invoices` side-effectful bulk overdue UPDATE on every render. **FIXED (WAVE 2, `2b28f60`):** GET is read-only; the sent→overdue flip moved to the daily `invoice-reminders` cron. (Full-table JS summary aggregate kept — bounded per tenant; minor.)
- ✅ **M20.** Realtime 'Unknown' rows / conflict-dismiss / AppointmentFlyout joins / admin-merges swallow / frozen summary. **(all subparts done)**
  - ✅ (a) realtime inserts refetch instead of rendering bare rows (WAVE 1).
  - ✅ (b) conflict-dismiss reflects via calendar_events realtime subscription (WAVE 1).
  - ✅ **(c) — M20c FIXED (WAVE 3, `7a689f8`):** AppointmentFlyout fetches `GET /api/appointments/[id]` on open (clearing prior detail first), and that endpoint now joins `jobs ( id, status, customers ( name ) )` + `jobber_visit_id`. Detail is merged over the list prop so the "Not in Jobber yet" pill still works; recording/transcript/job + Create-Invoice sections now render.
  - ✅ (d) admin merges page renders an error state instead of "No merges yet" on failure (WAVE 2, `2b28f60`).
  - ✅ (e) `useDocumentList` refreshes summary on every load (WAVE 2, `2b28f60`).
- ✅ **M21.** Jobber `region='US'` hardcode (both repos) + Xero webhook raw-phone cacheTag. **FIXED (WAVE 2, livekit `2ea2400` + main `f52533a`):** Jobber phone region derived from the caller's E.164 in both `jobber.py` and `jobber.js`; Xero webhook always fires the broad `xero-context-${tenant}` revalidateTag (per-phone raw tags rarely matched the E.164 read key).
- ✅ **M22.** Xero Python adapter logged customer names + phone fragments at INFO. **FIXED (WAVE 2, livekit `2ea2400`):** count-only at INFO, PII-free shapes at DEBUG, success log uses a sha256 phone_hash.

**Config/site**
- ✅ **M23.** No CSP. **FIXED (WAVE 2, `45badbf`):** `Content-Security-Policy-Report-Only` header in `next.config.js` (Stripe/Supabase/Sentry/LiveKit/Maps sources) with a Sentry `report-uri` derived from `SENTRY_DSN`. (Flip to enforcing CSP once Report-Only is clean.)
- ✅ **M24.** robots crawled /dashboard,/api,/auth; no OG images; no metadataBase. **FIXED (WAVE 2, `45badbf`):** robots disallow added; `metadataBase` in root layout; openGraph/twitter images via `/og` generator on home/pricing/about/contact.
- ✅ **M25.** `.env.example` deleted. **FIXED (WAVE 2, `45badbf`):** recovered the 161-line contract from git history (`5a5814c~1`).
- ✅ **M26.** Six unused heavy deps. **FIXED (WAVE 2, `45badbf`):** removed `@freshbooks/api`, `@splinetool/react-spline`, `@splinetool/runtime`, `intuit-oauth`, `node-quickbooks`, `docx` (74 pkgs gone, build green).
- ❌ **M27.** Usage under-billing: crash before post-call usage increment means a ≥15s call is never counted; orphaned-calls cron doesn't back-fill usage. **NOT DONE.** `increment_calls_used` is called only in `post_call.py` (post-call section 4). `cleanup-orphaned-calls` only marks rows failed. **Next:** either move usage increment to a crash-safe point, or have a reconciliation cron back-fill `increment_calls_used` + the Stripe meter for ≥15s calls never counted. **WAVE 3 verification:** confirmed REAL but **needs a migration** — crashed calls have NULL `duration_seconds` (a generated column off `end_timestamp`), so the 15s gate can't be applied retroactively; fix needs `calls.last_heartbeat_at` (+ ideally `is_test_call`) + a livekit watchdog write + cron back-fill (reuse the `stripe_meter_failures` outbox). Cross-repo + migration → **external bucket**.

---

## LOW

- ❌ **LOW-1.** Duplicate migration version `030` (two files both applied). NOT DONE — renumber one (behavior-neutral; blocks clean replay). Touches applied migrations; low priority.
- ❌ **LOW-2.** Empty migration registry / no drift detection. NOT DONE — backfill `supabase_migrations.schema_migrations` or adopt the Supabase CLI workflow.
- ✅ **LOW-3.** Stale `pytest.skip("webhook app not yet implemented")`. FIXED (dormant — module exists, tests run; only a cosmetic comment remains).
- 🚫 **LOW-4.** Stripe webhook returns 401 (not 400) on bad signature. WON'T FIX — audit deemed acceptable (Stripe ignores status, retries any non-2xx).
- ❌ **LOW-5.** `stripe_webhook_events.processed_at` defaults `now()` on insert (misleading). NOT DONE — **needs a migration (WAVE 3 verification):** the column is `NOT NULL DEFAULT now()`, so a code-only fix is insufficient — must `ALTER ... DROP DEFAULT, DROP NOT NULL` + set `processed_at` explicitly on success. Lowest value (nothing reads the column). → **external bucket**.
- 🚫 **LOW-6.** 10-min claim steal vs slow handlers. WON'T FIX — Vercel function timeout ≪ 10min, practically unreachable.
- ✅ **LOW-7.** Billing banners' dismiss buttons contradicted not-dismissible spec. FIXED (no dismiss buttons present).
- ✅ **LOW-8.** Billing page hardcoded `$`. **FIXED (WAVE 3, `734bbee`):** `formatMoney()` via `Intl.NumberFormat` honoring `inv.currency` (invoice amount only; plan price is static).
- ✅ **LOW-9.** `post_call.py` `print()` not logger. **FIXED (WAVE 3, livekit `9dea46e`):** all 22 converted (info for status, error w/ `exc_info` in except blocks); LiveKit CLI configures root at INFO so info lines emit on Railway.
- ❌ **LOW-10.** Recovery SMS has no app-side STOP/opt-out suppression. NOT DONE — **needs a migration + Twilio config (WAVE 3 verification):** requires an `sms_opt_outs` table + STOP capture in the incoming-sms webhook (livekit) + a recipient filter in `send-recovery-sms`. Biggest caveat: Twilio's built-in opt-out may intercept STOP so it never reaches the webhook (and 21610 is mislabeled in two files as "Message body is required"). → **external bucket**.
- ✅ **LOW-11.** Healthcheck liveness-only. **FIXED (WAVE 3, livekit `9dea46e`):** Dockerfile HEALTHCHECK now probes `/health/db` (readiness).
- ❌ **LOW-12.** Integration webhooks lack replay/event-id dedup. NOT DONE — persist + check a webhook event id (Jobber/Xero) with a unique constraint.
- ✅ **LOW-13.** Token cron sequential/unbounded. **FIXED (WAVE 3, `34fa881`):** batched `Promise.all` (CONCURRENCY=5) with per-row error isolation. The NULL-expiry half was verified a **no-op** (the gate is in `adapter.js`; xero/jobber always populate `expiry_date`) and intentionally skipped.
- ✅ **LOW-14.** `check_customer_account` treated a known caller as new on fetch-failure. **FIXED (WAVE 3, livekit `9dea46e`):** new `FETCH_UNAVAILABLE` sentinel (`src/lib/fetch_sentinel.py`) — Jobber/Xero error paths return it while **not-connected + genuine no-match stay `None`**; the merge/agent coerce it out (prompt/`_sources`/deps stay dict|None) and the tool serves a neutral "records temporarily unavailable" STATE. 6-file chain + tests (35 pass). Reviewed CLEAN.
- ✅ **LOW-15.** Jobber disconnect left orphaned mirror rows. **FIXED (WAVE 3, `34fa881`):** disconnect purges `calendar_events` where `provider='jobber'` (guarded to jobber; non-fatal).
- ✅ **LOW-16.** Footer newsletter no-op. **FIXED (WAVE 3, `900af75`):** removed the dead form (no subscriber infra exists; wiring it would need an external dep).
- ✅ **LOW-17.** Sitemap omits /privacy + /terms. FIXED (WAVE 2, `b1f366a`).
- ✅ **LOW-18.** Signed-in `/auth/signin?redirect` dropped plan params. **FIXED (WAVE 3, `900af75`):** `src/proxy.js` honors a `?redirect` **validated against the resolved origin** (rejects backslash/protocol-relative open-redirect bypasses + signin self-loop). The string-prefix guard a first pass used was caught as an open-redirect by the review and hardened.
- 🚫 **LOW-19.** ScrollLinePath z-index drift in CostOfSilenceBlock. **NOT A DEFECT (WAVE 3 verification):** the SVG is `pointer-events-none` and the wave is anchored to `#features` and drawn strictly below CostOfSilenceBlock, so there's no overlap or click interception. Documentation-vs-impl tidiness only — closed, no change.
- ✅ **LOW-20.** `html lang` could claim `es`. **FIXED (WAVE 3, `900af75`):** `layout.js` pins `lang="en"` (the es locale is latent — no switcher/content, `getLocale()` always returns en today). Revisit if real es content + a switcher land.
- ✅ **LOW-21.** Callbacks view had no search/date filters. **FIXED (WAVE 3, `7a689f8`):** client-side search + date-range added; status-pill counts stay derived from the unfiltered list. Reviewed CLEAN.
- ✅ **LOW-22.** 'Clear all' left the jobs search text. **FIXED (WAVE 3, `7a689f8`):** search input is controlled; Clear all cancels the pending debounce first so stale text can't reappear.
- ✅ **LOW-23.** Hardcoded "AI Receptionist is active". **FIXED (WAVE 3, `7a689f8`):** driven from real provisioning + subscription state (active / no-number / paused / loading / unavailable-on-error); `past_due` in grace stays active.
- ✅ **LOW-24.** Dark-mode leftovers. **FIXED (WAVE 3, `7a689f8`):** `dark:` variants for UsageTile (+ its `usage-threshold.js` mirror, edited identically), CalendarConnectionsCard emerald, call-routing amber.
- ✅ **LOW-25.** CommandPalette missing ARIA. **FIXED (WAVE 3, `7a689f8`):** `role=combobox`/`listbox`/`option` + `aria-activedescendant` (id template matches option ids exactly).
- ❌ **LOW-26.** `book_appointment_atomic` surfaces raw 500 instead of `slot_taken` for overlapping-but-different starts. NOT DONE — needs migration: wrap INSERT in `EXCEPTION WHEN exclusion_violation THEN RETURN ...slot_taken` and/or key the advisory lock per-tenant only.
- ✅ **LOW-27.** working-hours PUT accepted `slot_duration_mins:0` (infinite loop) + invalid tz. FIXED (WAVE 2, `b1f366a`): validates int 5..480 + IANA tz.
- ✅ **LOW-28.** Calendar GET drops range-spanning appointments. FIXED (WAVE 2, `b1f366a`): overlap test on appointments query.
- ✅ **LOW-29.** calendar-blocks PATCH broke all-day external events. **FIXED (WAVE 3, `34fa881`):** mirrors the POST route's all-day branching (Google date-only vs dateTime; Outlook `isAllDay` + midnight dateTime; exclusive end date).
- ❌ **LOW-30.** RLS initplan (66) / multiple-permissive-policy (535) / unindexed-FK (23) / unused-index (18) advisor hits. NOT DONE — needs a perf migration sweep: `(select auth.uid())` wrap, consolidate policies, add FK indexes, drop unused indexes.
- ✅ **LOW-31.** Dead per-tier `ctaHref` in `pricingData.js`. **FIXED (WAVE 3, `900af75`):** removed from the 3 standard tiers (ENTERPRISE_TIER.ctaHref kept — it's consumed).
- ✅ **LOW-32.** jest / `jest-environment-jsdom` major mismatch. **FIXED (WAVE 3, `900af75`):** `jest-environment-jsdom` aligned to `^29.7.0` + lockfile regenerated.

---

## What's solid (verified, no action)
- Tenant isolation strong across all API routes + the LiveKit agent; live data integrity clean.
- Stripe/Xero/Jobber/Outlook webhook signature verification correct; all crons CRON_SECRET-gated; no secret leakage to client.
- Next.js 16 compliance (proxy.js, cacheComponents, async APIs, flat ESLint).
- Slot-math core: JS↔Python calculators are line-equivalent ports over one atomic RPC + GiST backstop.
- i18n EN↔ES structural parity; pricing math + plan handoff verified.
- BusinessIntegrationsClient state machine sound; Xero intent-verify correct.

---

## Appendix — commit map (this remediation)
Main repo (`homeservice_agent`, → Vercel):
- `5a5814c` — WAVE 1 wave commit ("updated") + migrations 068–071 applied live via MCP
- `d52211a` — H6 daily calendar sweep + staleness re-anchor + alerting (+ ops scripts)
- `45badbf` — M23, M24, M25, M26
- `347293d` — M1, M4, M5, M6
- `2b28f60` — M18, M19, M20(d,e) + `src/lib/tenant-time.js`
- `f52533a` — M21 (JS: Jobber region, Xero webhook cacheTag)
- `b1f366a` — M12 (JS), LOW-27, LOW-28, LOW-17
- `6eef7b6` — M14 (Google full-resync stale purge)
- **WAVE 3** (branch `fix/audit-noexternal-2026-06-18`, **NOT pushed**):
  - `734bbee` — H2 (reconcile cron + handler exports + vercel.json), LOW-8
  - `7a689f8` — M20c, LOW-21, LOW-22, LOW-23, LOW-24, LOW-25
  - `34fa881` — LOW-29, LOW-13, LOW-15
  - `900af75` — LOW-18, LOW-20, LOW-16, LOW-31, LOW-32

livekit-agent (`livekit_agent`, → Railway):
- `8fbe053` — WAVE 1 (S4, H1, M3, H7, H8, M9, H11 agent map, H4 writer, per-provider telemetry)
- `2ea2400` — M11 (fanout emit), M21 (Jobber region), M22 (PII logs)
- `3d02f91` — M12 (all-day prefilter, both slot paths)
- **WAVE 3** (branch `fix/audit-noexternal-2026-06-18`, **NOT pushed**):
  - `9dea46e` — LOW-14 (FETCH_UNAVAILABLE sentinel chain + tests), LOW-9, LOW-11

Ops scripts (main repo `scripts/`, run `node --env-file=.env.local scripts/<file>`):
- `revive-google-calendars.mjs` — force full Google resync for all connected calendars
- `reregister-google-watches.mjs` — re-register Google watch channels (set `WATCH_WEBHOOK_BASE=https://www.voco.live`)
