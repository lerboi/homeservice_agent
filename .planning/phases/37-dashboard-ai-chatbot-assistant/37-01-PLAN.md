---
phase: 37-dashboard-ai-chatbot-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chatbot-knowledge/index.js
  - src/lib/chatbot-knowledge/getting-started.md
  - src/lib/chatbot-knowledge/leads.md
  - src/lib/chatbot-knowledge/calls.md
  - src/lib/chatbot-knowledge/calendar.md
  - src/lib/chatbot-knowledge/invoices.md
  - src/lib/chatbot-knowledge/estimates.md
  - src/lib/chatbot-knowledge/analytics.md
  - src/lib/chatbot-knowledge/billing.md
  - src/lib/chatbot-knowledge/settings.md
  - src/lib/chatbot-knowledge/integrations.md
  - src/app/api/chat/route.js
  - tests/unit/chatbot-knowledge.test.js
autonomous: true
requirements:
  - CHAT-03
  - CHAT-06

must_haves:
  truths:
    - "getRelevantKnowledge returns the route-matched doc when currentRoute matches ROUTE_DOC_MAP"
    - "getRelevantKnowledge returns a keyword-matched doc when message contains a mapped keyword"
    - "getRelevantKnowledge returns getting-started.md as fallback when no keyword match and route is /dashboard"
    - "POST /api/chat returns 401 when user is not authenticated"
    - "POST /api/chat returns a JSON reply from Groq when given a valid message"
    - "POST /api/chat returns 400 when message is empty"
    - "POST /api/chat returns 503 when GROQ_API_KEY is missing"
  artifacts:
    - path: "src/lib/chatbot-knowledge/index.js"
      provides: "getRelevantKnowledge function — keyword + route RAG retrieval"
      exports: ["getRelevantKnowledge"]
    - path: "src/app/api/chat/route.js"
      provides: "POST handler — tenant auth, RAG retrieval, Groq chat completion"
      exports: ["POST"]
    - path: "src/lib/chatbot-knowledge/leads.md"
      provides: "Knowledge doc for leads dashboard area"
    - path: "src/lib/chatbot-knowledge/calendar.md"
      provides: "Knowledge doc for calendar dashboard area"
    - path: "tests/unit/chatbot-knowledge.test.js"
      provides: "Unit tests for getRelevantKnowledge"
  key_links:
    - from: "src/app/api/chat/route.js"
      to: "src/lib/chatbot-knowledge/index.js"
      via: "import { getRelevantKnowledge }"
      pattern: "getRelevantKnowledge\\(message, currentRoute\\)"
    - from: "src/app/api/chat/route.js"
      to: "Groq API"
      via: "OpenAI client with baseURL https://api.groq.com/openai/v1"
      pattern: "chat\\.completions\\.create"
    - from: "src/app/api/chat/route.js"
      to: "src/lib/get-tenant-id.js"
      via: "import { getTenantId }"
      pattern: "getTenantId\\(\\)"
---

<objective>
Build the chatbot backend: a static markdown knowledge base covering all 10 dashboard areas, a keyword+route RAG retrieval function, and a Next.js API route that authenticates the user, retrieves relevant knowledge, and calls Groq (Llama) for chat completions.

Purpose: This is the "brain" of the chatbot. Without the knowledge base and API route, the UI components (Plan 02) have nothing to talk to.
Output: 10 knowledge markdown files, RAG retrieval module, POST /api/chat route, unit tests for retrieval logic.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dashboard-ai-chatbot-assistant/37-CONTEXT.md
@.planning/phases/37-dashboard-ai-chatbot-assistant/37-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From src/lib/triage/layer2-llm.js (Groq client pattern):
```javascript
import OpenAI from 'openai';

let _client = null;
function getClient() {
  if (!_client) {
    _client = new OpenAI({
      apiKey: process.env.GROQ_API_KEY,
      baseURL: 'https://api.groq.com/openai/v1',
    });
  }
  return _client;
}
// Model: 'meta-llama/llama-4-scout-17b-16e-instruct'
```

From src/lib/get-tenant-id.js (auth pattern):
```javascript
export async function getTenantId() {
  const serverSupabase = await createSupabaseServer();
  const { data: { user } } = await serverSupabase.auth.getUser();
  if (!user) return null;
  const { data: tenant } = await serverSupabase
    .from('tenants').select('id').eq('owner_id', user.id).maybeSingle();
  return tenant?.id || null;
}
```

From src/app/api/search/route.js (API route pattern):
```javascript
import { getTenantId } from '@/lib/get-tenant-id';
export async function POST(request) {
  const tenantId = await getTenantId();
  if (!tenantId) {
    return Response.json({ error: 'Unauthorized' }, { status: 401 });
  }
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Knowledge base markdown docs and RAG retrieval function with tests</name>
  <files>
    src/lib/chatbot-knowledge/index.js,
    src/lib/chatbot-knowledge/getting-started.md,
    src/lib/chatbot-knowledge/leads.md,
    src/lib/chatbot-knowledge/calls.md,
    src/lib/chatbot-knowledge/calendar.md,
    src/lib/chatbot-knowledge/invoices.md,
    src/lib/chatbot-knowledge/estimates.md,
    src/lib/chatbot-knowledge/analytics.md,
    src/lib/chatbot-knowledge/billing.md,
    src/lib/chatbot-knowledge/settings.md,
    src/lib/chatbot-knowledge/integrations.md,
    tests/unit/chatbot-knowledge.test.js
  </files>
  <read_first>
    src/lib/triage/layer2-llm.js,
    src/app/dashboard/more/page.js,
    src/app/dashboard/leads/page.js,
    src/app/dashboard/calls/page.js,
    src/app/dashboard/calendar/page.js,
    .planning/phases/37-dashboard-ai-chatbot-assistant/37-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: getRelevantKnowledge("how do I filter leads?", "/dashboard/leads") returns content containing "# Leads"
    - Test 2: getRelevantKnowledge("how do invoices work?", "/dashboard") returns content containing "# Invoices" (keyword match: "invoices")
    - Test 3: getRelevantKnowledge("hello", "/dashboard") returns content containing "# Getting Started" (fallback to route-matched doc, no keyword match)
    - Test 4: getRelevantKnowledge("tell me about billing", "/dashboard/leads") returns content containing both "# Leads" (route match) AND "# Billing" (keyword match)
    - Test 5: getRelevantKnowledge returns at most 2 doc sections (never more)
    - Test 6: getRelevantKnowledge("xyz gibberish", "/dashboard/nonexistent") returns getting-started.md as fallback
  </behavior>
  <action>
    **Step 1 — Write tests first** in `tests/unit/chatbot-knowledge.test.js`:
    - Import `getRelevantKnowledge` from `@/lib/chatbot-knowledge/index.js`
    - Jest needs `moduleNameMapper` for `@/` alias — the existing `jest.config.js` already has this
    - Mock `fs.readFileSync` is NOT needed — read actual files (they'll exist after this task creates them)
    - Write the 6 test cases from the behavior block above
    - Run tests — they MUST fail (RED phase) because the files don't exist yet

    **Step 2 — Create 10 knowledge markdown docs** in `src/lib/chatbot-knowledge/`:
    Each doc follows this structure (per RESEARCH.md):
    ```
    # [Area Name] — Voco Dashboard

    ## What this section does
    [2-3 sentence overview]

    ## Key features
    - [Feature + how to use it]

    ## How to navigate here
    Go to [Page Name](/dashboard/path)

    ## Common tasks
    ### How do I [task]?
    [Step-by-step answer]

    ## Related sections
    - [Related area](/dashboard/related-path)
    ```

    Keep each doc under 500 words. Focus on user-facing workflows, not implementation details. Content for each doc:

    1. **getting-started.md**: Dashboard overview, setup checklist (connect calendar, configure hours, make test call), home page cards (recent leads, upcoming appointments, call stats), tour button
    2. **leads.md**: Lead lifecycle (New > Contacted > Booked > Completed > Paid), Kanban board toggle, lead detail flyout (call recording, transcript, notes), filtering by status/date/search, creating invoices from leads
    3. **calls.md**: Call log with date grouping, expandable call cards, transcripts, recordings, triage labels (emergency/routine/high_ticket), call duration and outcome filters
    4. **calendar.md**: Weekly/monthly view, appointment details flyout, connecting Google Calendar or Outlook, working hours configuration (/dashboard/more/working-hours), travel buffers between appointments
    5. **invoices.md**: Creating invoices (from lead or standalone), line items with quantity/rate, tax rate, discounts, sending via email/SMS, PDF generation, payment status tracking (draft/sent/paid/overdue), invoice settings
    6. **estimates.md**: Creating estimates with good/better/best pricing tiers, converting estimate to invoice, sending estimates to customers, estimate statuses
    7. **analytics.md**: Revenue charts, lead conversion funnel (calls > leads > booked > completed), pipeline donut chart by status, date range filtering
    8. **billing.md**: Current plan details (Starter/Growth/Scale), usage meter (X of Y calls used), trial countdown, upgrading plan, Stripe Customer Portal for payment methods and invoices, subscription management
    9. **settings.md**: Services & Pricing (add/edit services, urgency tags, drag-reorder), Working Hours (weekly schedule), Service Zones & Travel (coverage areas, travel buffers), Notifications & Escalation (SMS/email alerts, emergency contacts), AI & Voice Settings (phone number, AI tone, test call)
    10. **integrations.md**: QuickBooks sync, Xero sync, Google Calendar connection, Outlook Calendar connection, managing connected integrations

    **Step 3 — Create RAG retrieval function** in `src/lib/chatbot-knowledge/index.js`:
    - Use `readFileSync` from `fs` (server-only module — NEVER import from client component)
    - Use `join`, `dirname` from `path` and `fileURLToPath` from `url` to resolve `__dirname`
    - Define `ROUTE_DOC_MAP` object mapping dashboard routes to doc filenames:
      ```
      '/dashboard': 'getting-started.md',
      '/dashboard/leads': 'leads.md',
      '/dashboard/calendar': 'calendar.md',
      '/dashboard/calls': 'calls.md',
      '/dashboard/invoices': 'invoices.md',
      '/dashboard/estimates': 'estimates.md',
      '/dashboard/more/analytics': 'analytics.md',
      '/dashboard/more/billing': 'billing.md',
      '/dashboard/more/services-pricing': 'settings.md',
      '/dashboard/more/working-hours': 'settings.md',
      '/dashboard/more/service-zones': 'settings.md',
      '/dashboard/more/notifications': 'settings.md',
      '/dashboard/more/ai-voice-settings': 'settings.md',
      '/dashboard/more/integrations': 'integrations.md',
      '/dashboard/more/invoice-settings': 'settings.md',
      ```
    - Define `KEYWORD_DOC_MAP` array of `{ keywords: string[], doc: string }`:
      ```
      { keywords: ['lead', 'leads', 'crm', 'customer', 'caller', 'kanban'], doc: 'leads.md' },
      { keywords: ['calendar', 'appointment', 'booking', 'schedule', 'slot'], doc: 'calendar.md' },
      { keywords: ['call', 'calls', 'transcript', 'recording', 'voicemail'], doc: 'calls.md' },
      { keywords: ['invoice', 'invoices', 'payment', 'bill', 'pdf', 'send invoice'], doc: 'invoices.md' },
      { keywords: ['estimate', 'estimates', 'quote'], doc: 'estimates.md' },
      { keywords: ['analytics', 'revenue', 'chart', 'stats', 'report'], doc: 'analytics.md' },
      { keywords: ['billing', 'subscription', 'plan', 'upgrade', 'usage', 'trial'], doc: 'billing.md' },
      { keywords: ['setting', 'settings', 'service', 'working hours', 'zone', 'notification', 'ai voice'], doc: 'settings.md' },
      { keywords: ['integration', 'quickbooks', 'xero', 'connect', 'sync'], doc: 'integrations.md' },
      ```
    - Export `async function getRelevantKnowledge(message, currentRoute)`:
      1. Add route-matched doc to `docs` Set (use `ROUTE_DOC_MAP[currentRoute]`, fallback to `getting-started.md` for unknown routes)
      2. Keyword-match: lowercase the message, iterate `KEYWORD_DOC_MAP`, if any keyword is found in the message AND the doc differs from the route-matched doc, add it and break (max 1 additional doc)
      3. Read matched docs via `readFileSync(join(__dirname, docFile), 'utf-8')` — wrap in try/catch, skip missing files silently
      4. Cap at 2 docs, join with `\n\n---\n\n` separator
      5. Return the joined string (empty string if no docs matched)

    **Step 4 — Run tests** — all 6 must pass (GREEN phase).
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && npm test -- --testPathPattern=chatbot-knowledge --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/chatbot-knowledge.test.js exists and contains at least 6 test cases (`it(` or `test(` count >= 6)
    - src/lib/chatbot-knowledge/index.js exports getRelevantKnowledge (grep: `export async function getRelevantKnowledge`)
    - src/lib/chatbot-knowledge/index.js contains ROUTE_DOC_MAP with at least 10 route entries
    - src/lib/chatbot-knowledge/index.js contains KEYWORD_DOC_MAP with at least 8 keyword group entries
    - 10 markdown files exist in src/lib/chatbot-knowledge/ (getting-started.md, leads.md, calls.md, calendar.md, invoices.md, estimates.md, analytics.md, billing.md, settings.md, integrations.md)
    - Each markdown file starts with `# ` heading and contains `## Common tasks` section
    - Each markdown file is under 500 words (wc -w < 500)
    - npm test -- --testPathPattern=chatbot-knowledge exits 0
  </acceptance_criteria>
  <done>RAG retrieval function correctly returns route-matched + keyword-matched docs (max 2), all 10 knowledge docs created, all 6 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Chat API route with Groq integration</name>
  <files>src/app/api/chat/route.js</files>
  <read_first>
    src/lib/triage/layer2-llm.js,
    src/app/api/search/route.js,
    src/lib/get-tenant-id.js,
    .planning/phases/37-dashboard-ai-chatbot-assistant/37-RESEARCH.md
  </read_first>
  <action>
    Create `src/app/api/chat/route.js` as a POST handler following the established API route pattern from `search/route.js` and the Groq client pattern from `layer2-llm.js`.

    **Groq client singleton** (top of file):
    ```javascript
    import OpenAI from 'openai';
    import { getTenantId } from '@/lib/get-tenant-id';
    import { getRelevantKnowledge } from '@/lib/chatbot-knowledge/index.js';

    let _groqClient = null;
    function getGroqClient() {
      if (!_groqClient) {
        _groqClient = new OpenAI({
          apiKey: process.env.GROQ_API_KEY,
          baseURL: 'https://api.groq.com/openai/v1',
        });
      }
      return _groqClient;
    }
    ```

    **POST handler logic:**
    1. Check `process.env.GROQ_API_KEY` — if missing, return `Response.json({ error: 'AI assistant is not configured. Please set GROQ_API_KEY.' }, { status: 503 })`
    2. Call `getTenantId()` — if null, return `Response.json({ error: 'Unauthorized' }, { status: 401 })`
    3. Parse request body: `const { message, currentRoute, history = [] } = await request.json()`
    4. Validate message: if `!message || message.trim().length === 0`, return `Response.json({ error: 'Message is required' }, { status: 400 })`
    5. Call `getRelevantKnowledge(message, currentRoute)` to get knowledge context
    6. Build system prompt (exact text below):
       ```
       You are Voco AI, an expert assistant for the Voco dashboard — a platform that helps home service contractors (plumbers, HVAC, electricians, handymen) manage calls, leads, bookings, and invoices.

       Your role is to help business owners understand and use the dashboard. You:
       - Answer questions about features and how they work
       - Explain dashboard sections and their purpose
       - Provide navigation links when the user needs to go somewhere
       - Give short, practical answers (2-4 sentences unless a step-by-step is needed)

       You do NOT:
       - Create, edit, or delete any data
       - Access the user's specific data or account information
       - Make promises about features that don't exist

       The user is currently on: ${currentRoute || '/dashboard'}

       When you want to suggest navigation, use this format exactly:
       [Go to Page Name](/dashboard/path)

       Place navigation links on their own line after your answer.

       ${knowledge ? `\nRelevant documentation:\n${knowledge}` : ''}
       ```
    7. Build messages array: system prompt + last 10 history entries (`.slice(-10)`) + user message
    8. Call Groq: `getGroqClient().chat.completions.create({ model: 'meta-llama/llama-4-scout-17b-16e-instruct', messages, max_tokens: 500, temperature: 0.3 })`
    9. Wrap the Groq call in try/catch — on error, log with `console.error('Groq API error:', err)` and return `Response.json({ reply: "Something went wrong on my end. Please try again in a moment." })`
    10. Return `Response.json({ reply: response.choices[0].message.content })`

    **Do NOT:**
    - Add `export const runtime = 'edge'` — this route must run on Node.js runtime for `fs` access in getRelevantKnowledge
    - Stream the response — non-streaming `Response.json` is correct for this use case per D-08
    - Send more than last 10 history messages to Groq
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && node -e "const m = require('./src/app/api/chat/route.js'); console.log(typeof m.POST)" 2>&1 || echo "Module check — may need ESM verification"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/chat/route.js exists and contains `export async function POST`
    - File imports getTenantId: grep shows `import { getTenantId } from '@/lib/get-tenant-id'`
    - File imports getRelevantKnowledge: grep shows `import { getRelevantKnowledge } from '@/lib/chatbot-knowledge/index.js'`
    - File imports OpenAI: grep shows `import OpenAI from 'openai'`
    - File contains Groq base URL: grep shows `https://api.groq.com/openai/v1`
    - File contains model name: grep shows `meta-llama/llama-4-scout-17b-16e-instruct`
    - File contains GROQ_API_KEY null check: grep shows `process.env.GROQ_API_KEY` with 503 response
    - File contains max_tokens: 500 and temperature: 0.3
    - File contains history slicing: grep shows `.slice(-10)`
    - File contains the system prompt text: grep shows `You are Voco AI`
    - File does NOT contain `export const runtime = 'edge'`
    - File contains try/catch around Groq call with error message `Something went wrong on my end`
  </acceptance_criteria>
  <done>POST /api/chat route created with tenant auth guard, GROQ_API_KEY null check (503), empty message validation (400), RAG knowledge injection, Groq chat completion call (Llama model, max 500 tokens, temp 0.3), history capped at 10 messages, error handling with user-friendly fallback message.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=chatbot-knowledge` — all 6 unit tests pass
2. `ls src/lib/chatbot-knowledge/*.md | wc -l` returns 10
3. `grep -c "export async function POST" src/app/api/chat/route.js` returns 1
4. `grep "getRelevantKnowledge" src/app/api/chat/route.js` shows import present
5. `npm run build` completes without errors related to chatbot files
</verification>

<success_criteria>
- 10 knowledge markdown files exist covering all dashboard areas (per D-06)
- RAG retrieval function matches docs by route AND keyword, caps at 2 docs
- API route authenticates via getTenantId(), calls Groq via OpenAI SDK at Groq base URL (per D-05)
- API route passes current route to getRelevantKnowledge for contextual answers (per D-07)
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-dashboard-ai-chatbot-assistant/37-01-SUMMARY.md`
</output>
