---
phase: 03-scheduling-and-calendar-sync
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/dashboard/calendar/page.js
  - src/components/dashboard/CalendarView.js
  - src/components/dashboard/AppointmentFlyout.js
  - src/components/dashboard/ConflictAlertBanner.js
  - src/components/dashboard/BookingStatusBadge.js
  - src/app/api/appointments/route.js
  - src/app/api/appointments/[id]/route.js
autonomous: true
requirements:
  - SCHED-06
must_haves:
  truths:
    - "Calendar page shows week and day views with appointment blocks"
    - "Appointment blocks are color-coded by urgency (red=emergency, blue=routine, amber=high_ticket)"
    - "Travel buffer blocks render between cross-zone appointments"
    - "External Google Calendar events render as purple dashed blocks"
    - "Clicking an appointment opens a detail flyout (Sheet)"
    - "Owner can cancel an appointment from the flyout"
    - "Conflict alert banner shows when external events overlap platform bookings"
    - "Today's Agenda sidebar shows today's appointments chronologically"
  artifacts:
    - path: "src/app/dashboard/calendar/page.js"
      provides: "Calendar dashboard page"
      contains: "Calendar"
    - path: "src/components/dashboard/CalendarView.js"
      provides: "Week/day calendar grid with appointment blocks"
      contains: "CalendarView"
    - path: "src/components/dashboard/AppointmentFlyout.js"
      provides: "Sheet-based appointment detail panel"
      contains: "AppointmentFlyout"
    - path: "src/components/dashboard/ConflictAlertBanner.js"
      provides: "Conflict alert banner"
      contains: "ConflictAlertBanner"
  key_links:
    - from: "src/app/dashboard/calendar/page.js"
      to: "src/components/dashboard/CalendarView.js"
      via: "import CalendarView"
      pattern: "CalendarView"
    - from: "src/components/dashboard/CalendarView.js"
      to: "/api/appointments"
      via: "fetch"
      pattern: "api/appointments"
    - from: "src/components/dashboard/AppointmentFlyout.js"
      to: "/api/appointments/[id]"
      via: "fetch for cancel"
      pattern: "api/appointments"
---

<objective>
Build the Calendar/Appointments dashboard page with week/day views, appointment blocks, travel buffer blocks, external calendar events, appointment detail flyout, conflict alerts, and today's agenda sidebar.

Purpose: Owners need to see their bookings, external calendar blocks, and travel buffers in a visual calendar. This is the primary scheduling surface.
Output: Calendar page, CalendarView component, AppointmentFlyout, ConflictAlertBanner, BookingStatusBadge, and appointments API.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scheduling-and-calendar-sync/03-CONTEXT.md
@.planning/phases/03-scheduling-and-calendar-sync/03-UI-SPEC.md

@src/app/dashboard/layout.js

<interfaces>
From supabase/migrations/003_scheduling.sql (Plan 01):
- appointments: id, tenant_id, call_id, start_time, end_time, service_address, caller_name, caller_phone, urgency, zone_id, status, booked_via, google_event_id, notes, created_at
- calendar_events: id, tenant_id, provider, external_id, title, start_time, end_time, is_all_day, appointment_id, conflict_dismissed, synced_at
- service_zones: id, tenant_id, name, postal_codes[]

From src/app/dashboard/layout.js:
```javascript
export default function DashboardLayout({ children }) {
  // max-w-4xl mx-auto, bg-slate-50, breadcrumb, bg-white card wrapper
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Appointments API routes</name>
  <files>
    src/app/api/appointments/route.js
    src/app/api/appointments/[id]/route.js
  </files>
  <read_first>
    - src/lib/supabase-server.js
    - supabase/migrations/003_scheduling.sql
  </read_first>
  <action>
    Create `src/app/api/appointments/route.js`:
    - GET: Auth check via createSupabaseServer. Query params: `start` (ISO date), `end` (ISO date), `view` ('week'|'day').
    - Fetch appointments for tenant within date range where status != 'cancelled'. Include zone info via join on service_zones.
    - Fetch calendar_events for tenant within same date range.
    - Compute travel buffers between consecutive appointments: for each pair of appointments on same day, check zones and insert a buffer block `{ type: 'travel_buffer', start_time, end_time, from_zone, to_zone }`.
    - Detect conflicts: calendar_events that overlap confirmed appointments where conflict_dismissed=false.
    - Return `{ appointments, externalEvents, travelBuffers, conflicts }`.

    Create `src/app/api/appointments/[id]/route.js`:
    - GET: Auth check. Fetch single appointment by ID with call data (recording_url, transcript). Return appointment detail.
    - PATCH: Auth check. Receive `{ status: 'cancelled' }`. Update appointment status. If google_event_id exists, delete from Google Calendar via `after()`. Return updated appointment.
    - Also support PATCH for `{ conflict_dismissed: true }` on calendar_events to dismiss conflicts.
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/appointments/route.js contains `export async function GET`
    - src/app/api/appointments/route.js contains query for `appointments`
    - src/app/api/appointments/route.js contains query for `calendar_events`
    - src/app/api/appointments/route.js returns `conflicts` in response
    - src/app/api/appointments/[id]/route.js contains `export async function GET` and `export async function PATCH`
    - src/app/api/appointments/[id]/route.js contains `cancelled`
    - All existing tests pass
  </acceptance_criteria>
  <done>Appointments API serves calendar data with appointments, external events, travel buffers, and conflicts. Cancel endpoint updates status and async-removes Google Calendar event.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar page with CalendarView, AppointmentFlyout, ConflictAlertBanner, BookingStatusBadge</name>
  <files>
    src/app/dashboard/calendar/page.js
    src/components/dashboard/CalendarView.js
    src/components/dashboard/AppointmentFlyout.js
    src/components/dashboard/ConflictAlertBanner.js
    src/components/dashboard/BookingStatusBadge.js
  </files>
  <read_first>
    - .planning/phases/03-scheduling-and-calendar-sync/03-UI-SPEC.md
    - src/app/dashboard/layout.js
    - src/app/dashboard/services/page.js
  </read_first>
  <action>
    **Create `src/app/dashboard/calendar/page.js`:**
    'use client' page. Layout: on desktop (>=1024px) use `max-w-6xl` wrapper (override layout's max-w-4xl by adding a className). Two-column flex layout: calendar left ~65%, Today's Agenda sidebar right ~35%. On mobile: single column, agenda below calendar.

    Page structure:
    - ConflictAlertBanner at top (conditional on conflicts.length > 0)
    - Calendar header: prev/next ChevronLeft/ChevronRight buttons, date label (week range or day), "Today" button (outline, disabled when viewing today), Week|Day toggle (two buttons, active=bg-slate-900 text-white, inactive=bg-white border)
    - CalendarView component
    - Today's Agenda sidebar

    Fetch data from GET /api/appointments?start={weekStart}&end={weekEnd}&view={view} on mount and navigation. State: currentDate, viewMode ('week'|'day').

    Today's Agenda: heading "TODAY'S AGENDA" (uppercase tracking-wider text-xs font-semibold text-slate-500). Filter today's appointments. Chronological cards with left border color by urgency, time label, caller name, job type. Empty state: CalendarOff icon + "No appointments today" text.

    **Create `src/components/dashboard/CalendarView.js`:**
    'use client' component.

    Week view: `grid grid-cols-[48px_repeat(7,1fr)]`. Time axis: 7 AM to 8 PM, 1-hour rows (48px height). Day column headers: abbrev + date number. Today's column: blue circle on date number (`bg-blue-600 text-white rounded-full w-8 h-8`).

    Appointment blocks: Absolutely positioned within day columns. `top` and `height` calculated from start_time/end_time relative to 7AM base. Color by urgency:
    - emergency: `bg-red-50 border-l-2 border-red-400`
    - routine: `bg-blue-50 border-l-2 border-blue-400`
    - high_ticket: `bg-amber-50 border-l-2 border-amber-400`

    Content: caller name (14px/600), job type (13px/400), address (13px/400 truncate). Urgency badge top-right corner (11px).

    Travel buffer blocks: `bg-slate-100 border border-dashed border-slate-300 rounded-md pointer-events-none`. Label "Travel buffer" 11px text-slate-400.

    External Google Calendar blocks: `bg-purple-50 border border-dashed border-purple-300 rounded-md`. Title 13px text-purple-600. "Google Calendar" caption 11px. pointer-events-none. All-day events: horizontal strip above first time row, max 2 visible + "+N more".

    Current time indicator: red line `border-red-500 border-t-2` with red dot on left axis. Only on today's column. Update every 60s via setInterval.

    Day view: Single wide column, same time axis. Appointment blocks show full detail (no truncation).

    Block hover: `shadow-md` elevation, cursor pointer.
    Block click: opens AppointmentFlyout.

    Navigation fade: Calendar body opacity transition (100ms out, 150ms in) on week/day change.

    Loading: 3 random Skeleton blocks (48-96px height) across the grid.
    Empty state: Calendar icon 40px text-slate-300, "No appointments this week" heading, "Appointments booked by your AI will appear here." body.

    Mobile (<768px): Force day view only. Hide week toggle.

    **Create `src/components/dashboard/AppointmentFlyout.js`:**
    'use client' component using shadcn Sheet (side="right" desktop, side="bottom" mobile).

    Content per UI-SPEC:
    - Header: urgency badge + caller name (20px/600) + job type (16px/400)
    - Appointment Details section: date+time formatted, address with MapPin icon, duration, booked_via label
    - Caller section: phone with Phone icon, call timestamp relative
    - Links section: "Listen to Recording" (ExternalLink, opens recording_url in new tab), "View Transcript" (FileText), "Open Lead" (hidden in Phase 3 — lead detail page not yet built)
    - Notes section (if any)
    - Conflict section (conditional): amber panel with AlertTriangle, conflicting event title+time, "Dismiss Conflict" button (PATCH /api/appointments/[id] with conflict_dismissed)
    - Footer: "Cancel Appointment" button (text-red-600). Opens AlertDialog: "Cancel this appointment?" with "Yes, Cancel Appointment" (destructive) and "Keep Appointment". On confirm: PATCH /api/appointments/[id] with status=cancelled. Sheet closes, block disappears (fade 150ms), Sonner "Appointment cancelled".

    **Create `src/components/dashboard/ConflictAlertBanner.js`:**
    'use client' component.

    Amber banner per UI-SPEC: AlertTriangle icon, count text ("{N} calendar conflict(s) need(s) your review"), body text, "Review Conflicts" button, X dismiss button. Dismiss: Framer Motion slide-up + fade (200ms). "Review Conflicts": scroll to conflicted block + open its flyout.

    **Create `src/components/dashboard/BookingStatusBadge.js`:**
    Server component. Reusable Badge for call history rows.

    Variants per UI-SPEC Surface 5a:
    - confirmed: `border-emerald-200 bg-emerald-50 text-emerald-700` label "Booked"
    - lead_with_slots: `border-blue-200 bg-blue-50 text-blue-700` label "Lead - slots suggested"
    - no_booking: `border-slate-200 bg-slate-50 text-slate-500` label "No booking"
    - failed: `border-red-200 bg-red-50 text-red-600` label "Booking failed"
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/calendar/page.js contains `'use client'`
    - src/app/dashboard/calendar/page.js contains `CalendarView`
    - src/app/dashboard/calendar/page.js contains `ConflictAlertBanner`
    - src/app/dashboard/calendar/page.js contains `api/appointments`
    - src/app/dashboard/calendar/page.js contains `Today` (today button text)
    - src/components/dashboard/CalendarView.js contains `bg-red-50` (emergency color)
    - src/components/dashboard/CalendarView.js contains `bg-blue-50` (routine color)
    - src/components/dashboard/CalendarView.js contains `bg-purple-50` (external events)
    - src/components/dashboard/CalendarView.js contains `Travel buffer`
    - src/components/dashboard/CalendarView.js contains `border-red-500` (current time indicator)
    - src/components/dashboard/CalendarView.js contains `setInterval` (time indicator update)
    - src/components/dashboard/AppointmentFlyout.js contains `Sheet`
    - src/components/dashboard/AppointmentFlyout.js contains `Cancel Appointment`
    - src/components/dashboard/AppointmentFlyout.js contains `AlertDialog`
    - src/components/dashboard/ConflictAlertBanner.js contains `calendar conflict`
    - src/components/dashboard/ConflictAlertBanner.js contains `AlertTriangle`
    - src/components/dashboard/BookingStatusBadge.js contains `Booked`
    - src/components/dashboard/BookingStatusBadge.js contains `Lead`
    - All existing tests pass
  </acceptance_criteria>
  <done>Calendar page shows weekly/daily appointment views with urgency-colored blocks, travel buffers, external events, current time indicator, today's agenda sidebar, appointment detail flyout with cancel, and conflict alerts. BookingStatusBadge ready for call history integration.</done>
</task>

</tasks>

<verification>
- `node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests` passes
- Calendar page renders at /dashboard/calendar
- All 5 dashboard components exist with correct imports
- Appointments API returns appointments, external events, travel buffers, and conflicts
</verification>

<success_criteria>
- Owner sees appointments in a visual calendar with urgency color coding
- Travel buffers visible between cross-zone appointments
- External Google Calendar events shown as purple blocks
- Appointment detail flyout shows full context including recording/transcript links
- Owner can cancel appointments with confirmation dialog
- Conflict alerts surface when external events overlap platform bookings
- Routine calls display "Lead - slots suggested" booking status (SCHED-06)
- Calendar page responsive: day-only on mobile, week+day on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-and-calendar-sync/03-05-SUMMARY.md`
</output>
