---
phase: 60
plan: 03
type: execute
wave: 3
depends_on: [60-01, 60-02]
files_modified:
  - livekit_agent/src/messages/es.json
  - livekit_agent/src/prompt.py
  - .claude/skills/voice-call-architecture/SKILL.md
autonomous: false
requirements: []
addresses_decisions: [D-13, D-14, D-15]
cross_repo: true
tags: [voice-agent, spanish, i18n, skill-sync, documentation]

must_haves:
  truths:
    - "The Spanish-rendered system prompt (via `build_system_prompt(locale='es', ...)`) contains the same structural blocks and rule count as the English-rendered one — no rule dropped or diluted for Spanish."
    - "Each D-01..D-12 rule is present, recognizable, and idiomatically phrased in the Spanish render (not a literal transliteration)."
    - "The Spanish single-question opener reads naturally (e.g., '¿Cuál es la dirección donde necesita el servicio?' or equivalent) — user-reviewed before commit (D-14)."
    - "`voice-call-architecture/SKILL.md` reflects the new name-readback, single-question-address, and state+directive tool-return rules — content matches the code/prompt that actually shipped, not aspirational text."
    - "The user explicitly approves the Spanish draft (D-14 human checkpoint) before the Spanish commit lands in `livekit_agent/`."
  artifacts:
    - path: "livekit_agent/src/messages/es.json"
      provides: "Spanish mirrors of any prompt-surface agent.* strings edited in Plan 01 (or no-op if Plan 01 concluded en.json did not need edits — in which case this plan edits the Spanish prompt sections in prompt.py's Spanish render path)."
    - path: "livekit_agent/src/prompt.py"
      provides: "Spanish render paths (locale='es') include the same NAME USE / SERVICE ADDRESS / BEFORE BOOKING READBACK blocks as the English render, with idiomatic Spanish phrasing."
      contains: ["locale", "es"]
    - path: ".claude/skills/voice-call-architecture/SKILL.md"
      provides: "Updated documentation covering: (a) name-vocative suppression + single-readback-at-booking rule, (b) single-question address opener, (c) state+directive tool-return format across all 5 tools, (d) cross-reference to Phase 60 decisions D-01..D-16."
      contains: ["name vocative", "single-question", "STATE:", "DIRECTIVE:"]
  key_links:
    - from: "build_system_prompt(locale='es', ...)"
      to: "Spanish NAME USE + SERVICE ADDRESS + BEFORE BOOKING sections"
      via: "locale-conditional render path in prompt.py"
      pattern: "Cuál es la dirección|nombre|lectura"
    - from: ".claude/skills/voice-call-architecture/SKILL.md"
      to: "shipped behavior in livekit_agent/src/prompt.py and livekit_agent/src/tools/*.py"
      via: "documented rules must match code, not plan text"
      pattern: "name vocative|STATE:|DIRECTIVE:"
---

<objective>
Mirror the English prompt changes into the Spanish surface with idiomatic phrasing (D-13), gate the Spanish commit behind explicit user review (D-14), and update `.claude/skills/voice-call-architecture/SKILL.md` in `homeservice_agent` to reflect the shipped Phase 60 behavior (per CLAUDE.md "Keep skills in sync" rule).

Purpose: Spanish parity is a locked decision (D-13) — the same rules, no dilution. Machine-translation alone is insufficient because SG-context phrasings map differently into Latin American vs. Iberian Spanish; the user-review checkpoint (D-14) is the safety net. The skill-sync is explicitly required by CLAUDE.md and guards against Pitfall 7 (SKILL.md drifting from `prompt.py`).
Output: Spanish-language prompt sections present in the assembled `locale='es'` prompt; user-approved Spanish draft committed to `livekit_agent`; `SKILL.md` updated with Phase 60 rules in `homeservice_agent`.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/leroyngzz/Projects/homeservice_agent/CLAUDE.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-CONTEXT.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-VALIDATION.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-01-SUMMARY.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-02-SUMMARY.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md

<cross_repo_note>
Spanish changes land in `livekit_agent/` (separate repo). SKILL.md update lands in `homeservice_agent/` — that is this repo. Two repos, two commits minimum (more if the Spanish draft needs an iteration after user review).
</cross_repo_note>

<interfaces>
<!-- From prior plans + existing code -->

Spanish surface mechanics (per SKILL.md §4 end + RESEARCH.md Assumption A2):
- `livekit_agent/src/messages/es.json` mirrors `en.json` at `agent.*` / `notifications.*` key level.
- `build_system_prompt(locale, ...)` dispatches to locale-specific render paths inside each `_build_*` function (or uses locale-specific template strings from `messages/{locale}.json`).
- The Spanish render MUST produce the same structural blocks as the English render — same section count, same rules.

Idiomatic phrasing references (Claude drafts; user reviews per D-14):
- "What's the address where you need the service?" → "¿Cuál es la dirección donde necesita el servicio?" (formal usted) or "¿Cuál es la dirección donde necesitas el servicio?" (informal tú) — user picks register in review.
- "Do not address the caller by name" → "No se dirija al cliente por su nombre durante la llamada" (outcome-framed).
- "Read back the name and address in one utterance" → "Lea de nuevo el nombre y la dirección en una sola frase" or "Repita el nombre y la dirección completa en una sola intervención".
- "You can call me X" override recognition (D-03 in Spanish) — "puede llamarme X" / "me dicen X" / "me llamo X, pero dígame X" — outcome-framed rule; Gemini detects without enumeration (per RESEARCH.md Assumption A6, MEDIUM confidence; user review is the safety net).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft Spanish mirror of all D-01..D-12 prompt sections (es.json and/or prompt.py locale='es' path)</name>
  <files>livekit_agent/src/messages/es.json, livekit_agent/src/prompt.py</files>
  <read_first>
    - livekit_agent/src/messages/en.json (current state, post-Plan-01)
    - livekit_agent/src/messages/es.json (current state — identify the structural mirror; look for key-diffs vs en.json that might be pre-existing gaps)
    - livekit_agent/src/prompt.py (Plan 01 shipped version — inspect how the Spanish render path is structured; is it (a) pulled from `messages/es.json`, (b) inlined as Spanish literals in `prompt.py`, or (c) a mix?)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-01-SUMMARY.md (confirms exactly what shipped in English, and whether en.json was edited or only prompt.py)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md §Pitfall 4 (Spanish drift from English — same-shape requirement)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-VALIDATION.md Persona 7 (Spanish UAT script)

    **CROSS-REPO NOTE:** Both target files live in the separate `livekit_agent/` repo.
  </read_first>
  <action>
    Draft the Spanish mirror. This task does NOT commit to `main` — it produces a draft that the Task 2 human-review checkpoint gates. The task's commit is on a branch (or a staged but not-yet-pushed commit) named `phase-60-spanish-draft` so the user can review the diff cleanly.

    **Step 1 — Determine the Spanish render path.** Read `prompt.py` and `es.json`. Identify exactly where the locale='es' render picks up its section text. Three possibilities:
    - (a) `es.json` has keys like `agent.name_use`, `agent.service_address`, `agent.booking_readback` that are interpolated by `prompt.py`. → Edit `es.json`.
    - (b) `prompt.py` has inline Spanish literals in each `_build_*` function under a locale conditional. → Edit `prompt.py` under the locale='es' branches.
    - (c) Mix — part templates, part inline. → Edit both.

    Record the determination in the commit body so the reviewer knows where changes landed.

    **Step 2 — Same-shape structural check.** Before drafting text, confirm the Spanish-render structure has the same sections as English. If the Phase 30 Spanish render was shorter than the English render (RESEARCH.md Pitfall 4 warning sign), note the pre-existing gap in the commit body — Phase 60 mirrors the NEW blocks only, it does not retroactively fix Phase 30 structural gaps.

    **Step 3 — Draft the three new Spanish blocks** (outcome-framed, usted register preferred — user may downgrade to tú in review):

    **Spanish NAME USE DURING THE CALL block:**
    ```
    USO DEL NOMBRE DURANTE LA LLAMADA:
    - Capture el nombre del cliente al inicio y úselo en silencio para los registros. No se dirija
      al cliente por su nombre durante la llamada (no diga "Gracias, {nombre}" ni "Bien, {nombre}").
      La única excepción es la lectura de confirmación antes de la reserva, que es el único momento
      autoritativo para confirmar el nombre en voz alta.
    - Si el cliente le invita explícitamente a usar su nombre (por ejemplo, "puede llamarme X",
      "me dicen X", "dígame X"), puede usar su nombre de forma natural durante el resto de la
      llamada. Use su criterio — no busque una frase específica.
    - Si no se capturó ningún nombre (el cliente se negó o no se entendió), continúe sin nombre.
      Omita la parte del nombre en la lectura de confirmación. La reserva nunca se bloquea por
      falta de nombre.
    - No añada líneas de verificación adicionales para nombres deletreados o de baja confianza.
      La regla de CORRECCIONES existente se encarga de las correcciones de pronunciación durante
      la lectura.
    ```

    **Spanish SERVICE ADDRESS block:**
    ```
    DIRECCIÓN DEL SERVICIO:
    - Haga una pregunta natural: "¿Cuál es la dirección donde necesita el servicio?"
    - Extraiga lo que el cliente haya ofrecido — calle, código postal, unidad, bloque, nombre
      del edificio, etc.
    - Si falta alguna pieza que necesitaríamos para encontrar el lugar, haga exactamente una
      pregunta puntual sobre esa pieza faltante. Avance de una en una. Nunca ejecute un
      recorrido mecánico ni recite una lista de campos al cliente.
    - Capture lo suficiente para que podamos encontrar el lugar. No enumere nombres de campos
      en voz alta.
    ```

    **Spanish BEFORE BOOKING — READBACK block:**
    ```
    ANTES DE RESERVAR — LECTURA DE CONFIRMACIÓN (obligatoria):
    - Lea de nuevo el nombre del cliente (si se capturó) y la dirección completa del servicio en
      una sola intervención. Este es el único momento autoritativo para verificar ambos. Orden:
      primero el nombre, después la dirección (los nombres son más cortos, así que es más
      probable que el cliente corrija el nombre antes de pasar a la dirección).
    - Si el cliente corrige cualquier parte de la lectura, acepte la corrección (la corrección
      del cliente SIEMPRE es correcta — vea CORRECCIONES más arriba) y vuelva a leer la línea
      corregida completa antes de llamar a book_appointment. Si vuelve a corregir, repita el
      ciclo: acepte, relea la línea corregida completa, hasta que deje de corregir.
    - Si no se capturó ningún nombre, lea solo la dirección. No haga una pausa para pedir el
      nombre.
    - Llame a book_appointment solo después de que el cliente haya reconocido la lectura (el
      silencio o un "sí" / "correcto" explícito cuentan).
    ```

    **Step 4 — Apply to the identified surface** (from Step 1). For `es.json` keys: use Spanish literals. For inline `prompt.py` locale='es' branches: use the same outcome-framed Spanish text. Do NOT use placeholder keys — the text must be complete and idiomatic.

    **Step 5 — D-15 audit for Spanish.** Run the same grep checks as Plan 01 Task 1 Edit 4 against the Spanish render:
    - Confirm no VAD-redundant Spanish phrasings exist: `grep -nE "deje (que|de)|no (interrumpa|hable)|espere a que" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` — MUST return nothing (Spanish equivalents of "let them finish", "don't interrupt", "wait for them").
    - Confirm anti-hallucination rules have Spanish equivalents in the first 30 assembled lines.

    **Step 6 — Run pytest.** `pytest tests/ -x --tb=short` from `livekit_agent/`. Must be green — any Spanish JSON key issues (missing key, malformed) will surface here.

    **Step 7 — Stage the draft.** Do NOT push. Commit on a feature branch:
    ```
    git checkout -b phase-60-spanish-draft
    git add src/messages/es.json src/prompt.py
    git commit -m "draft(60): spanish mirror of prompt polish — PENDING USER REVIEW (D-13, D-14)"
    ```
    Commit body documents: (a) which surface was edited ((a)/(b)/(c) from Step 1), (b) register chosen (usted/tú), (c) any pre-existing Spanish structural gaps from Phase 30, (d) the three new blocks in full (as shipped in the file), (e) grep-audit results from Step 5.
  </action>
  <verify>
    <automated>cd livekit_agent && { grep -lE "USO DEL NOMBRE DURANTE LA LLAMADA|No se dirija al cliente por su nombre" src/prompt.py src/messages/es.json | wc -l | awk '$1 >= 1'; } && { grep -lF "Cuál es la dirección donde necesita el servicio" src/prompt.py src/messages/es.json | wc -l | awk '$1 >= 1'; } && { grep -lE "ANTES DE RESERVAR|LECTURA DE CONFIRMACIÓN|Lea de nuevo el nombre" src/prompt.py src/messages/es.json | wc -l | awk '$1 >= 1'; } && { grep -nE "deje (que|de)|no (interrumpa|hable)|espere a que" src/prompt.py src/messages/es.json && echo "FAIL_VAD_REDUNDANT" || echo "OK_NO_VAD_REDUNDANT"; } && pytest tests/ -x --tb=short && git log -1 --format=%s | grep -F "draft(60): spanish"</automated>
  </verify>
  <acceptance_criteria>
    - Spanish NAME USE block present: `grep -lF "No se dirija al cliente por su nombre" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns at least one file
    - Spanish SERVICE ADDRESS block present: `grep -lF "¿Cuál es la dirección donde necesita el servicio?" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns at least one file
    - Spanish BEFORE BOOKING READBACK block present: `grep -lF "Lea de nuevo el nombre" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns at least one file
    - Spanish CORRECTIONS cross-reference present: `grep -lF "corrección del cliente SIEMPRE es correcta" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns at least one file
    - Spanish outcome-framed minimum-capture phrasing: `grep -lF "Capture lo suficiente" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns at least one file
    - Spanish override-phrasing recognition is outcome-framed (not enumerated): `grep -cE "(puede llamarme|me dicen|dígame)" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` — if > 0, the examples must be in an "por ejemplo" / "por ejemplo" clause, not an exhaustive list (subjective; verified in Task 2 review)
    - No VAD-redundant Spanish phrases: `grep -nE "deje (que|de)|no (interrumpa|hable)|espere a que" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json` returns ZERO
    - Same-shape structural check: the Spanish render section count >= the English render section count for the three new blocks (manual-confirm — documented in commit body)
    - `pytest livekit_agent/tests/ -x --tb=short` passes (no JSON parse errors, no key-diff regressions)
    - Commit exists on branch `phase-60-spanish-draft` with subject starting `draft(60): spanish mirror`
    - Commit NOT yet on `main` (still on the feature branch — awaiting Task 2 review)
    - Commit body documents: edited surface (a/b/c), register (usted/tú), pre-existing gaps (if any), and Step 5 grep audit outputs
  </acceptance_criteria>
  <done>Spanish draft present in the identified surface(s); same-shape check passes; VAD-redundant audit clean; pytest green; commit staged on `phase-60-spanish-draft` branch awaiting user review in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review of Spanish draft — gate on approval before merge to main (D-14)</name>
  <what-built>
    Task 1 drafted Spanish mirrors of the three Phase 60 prompt blocks (NAME USE, SERVICE ADDRESS, BEFORE BOOKING READBACK) on a feature branch `phase-60-spanish-draft` in the `livekit_agent` repo. The draft is Claude-authored Spanish (usted register by default) that maps D-01..D-12 to idiomatic Spanish.
  </what-built>
  <how-to-verify>
    1. In `livekit_agent/`: `git diff main..phase-60-spanish-draft -- src/messages/es.json src/prompt.py` to see the full Spanish diff.
    2. Read each of the three Spanish blocks aloud (or have a Spanish-speaker read them) and check:
       - Are the blocks idiomatic (not literal transliterations)?
       - Is the register (usted vs. tú) appropriate for the tenant market? (Default: usted — formal; override to tú if the market is informal Latin American or youth-market.)
       - Are all D-01..D-12 rules present? (Reference: CONTEXT.md `<decisions>` block.)
       - Is the override-recognition phrasing in D-03's Spanish block natural? Would a Spanish speaker use "puede llamarme X" / "me dicen X" to invite name use? (RESEARCH.md Assumption A6 — MEDIUM confidence, this is the review surface.)
       - Does the single-question opener "¿Cuál es la dirección donde necesita el servicio?" sound like something an AI receptionist would naturally say, or does it sound stiff?
    3. Check that the Spanish block count matches the English block count — no rule silently dropped (RESEARCH.md Pitfall 4 warning sign).
    4. If approved: signal to merge the branch into main and push.
    5. If changes needed: describe the specific phrasing changes and loop back to Task 1 (not Task 2) for a re-draft. Each re-draft creates a new commit on the same branch.

    **Approval workflow:**
    - If approved, the executor runs (in `livekit_agent`):
      ```
      git checkout main
      git merge --no-ff phase-60-spanish-draft -m "feat(60): spanish mirror of prompt polish — user-approved (D-13, D-14)"
      git push
      git branch -d phase-60-spanish-draft
      ```
    - If rejected or changes needed, the executor stays on the feature branch and re-drafts.
  </how-to-verify>
  <resume-signal>Type "approved" (then the executor merges the draft branch to main and proceeds to Task 3) OR describe the specific phrasing changes needed (then the executor loops back to Task 1 to re-draft on the same branch). Acceptable resume signals: `approved`, `needs changes: <description>`, `rejected: <reason>`.</resume-signal>
  <files>livekit_agent/src/messages/es.json, livekit_agent/src/prompt.py (review only — no new writes in this task; executor may run `git merge --no-ff phase-60-spanish-draft` into main if the user signals `approved`)</files>
  <action>Present the Spanish diff to the user via `git diff main..phase-60-spanish-draft -- src/messages/es.json src/prompt.py` and pause for the resume-signal. If the user replies `approved`, the executor runs the merge command from the approval workflow above (in the `livekit_agent` repo) and proceeds to Task 3. If the user replies with requested changes, the executor loops back to Task 1 on the same branch (creating additional draft commits) and re-invokes this checkpoint until approval.</action>
  <verify>
    <automated>cd livekit_agent && { git rev-parse --verify phase-60-spanish-draft >/dev/null && echo "DRAFT_BRANCH_PRESENT"; } && { git log origin/main --oneline | grep -F "spanish mirror" && echo "APPROVED_AND_MERGED" || echo "AWAITING_APPROVAL"; }</automated>
  </verify>
  <done>User has explicitly signaled `approved` (or equivalent); the Spanish draft branch has been merged to main in `livekit_agent` with a commit message starting `feat(60): spanish mirror`; the feature branch is deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Update voice-call-architecture SKILL.md to reflect Phase 60 behavior (CLAUDE.md Keep-skills-in-sync rule)</name>
  <files>.claude/skills/voice-call-architecture/SKILL.md</files>
  <read_first>
    - .claude/skills/voice-call-architecture/SKILL.md (CURRENT STATE — read the full file; identify (a) the "Last updated" line at the top, (b) §4 prompt section ordering, (c) §5 tool return inventory)
    - livekit_agent/src/prompt.py (post-Plan-01, post-Task-2 state — SOURCE OF TRUTH for what to document)
    - livekit_agent/src/tools/book_appointment.py (post-Plan-02 — source of truth for tool returns)
    - livekit_agent/src/tools/capture_lead.py (post-Plan-02)
    - livekit_agent/src/tools/transfer_call.py (post-Plan-02)
    - livekit_agent/src/tools/check_availability.py (post-Plan-02)
    - livekit_agent/src/tools/check_caller_history.py (post-Plan-02)
    - CLAUDE.md (§Rules — "Keep skills in sync" is a hard requirement, not a soft preference)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md §Pitfall 7 (SKILL.md drift from prompt — the specific anti-pattern to avoid)
  </read_first>
  <action>
    Update `.claude/skills/voice-call-architecture/SKILL.md` in `homeservice_agent` to reflect the shipped Phase 60 behavior. Pull ALL updated text from the actual code files (source of truth), NOT from plan documents (to guard against RESEARCH.md Pitfall 7).

    **Step 1 — Append to the "Last updated" line at the top of SKILL.md.** Prepend a new Phase 60 entry. Keep prior entries intact; Phase 46 is currently the most recent. Format follows the existing pattern (parenthetical prose, comma-separated):

    ```
    2026-04-{DD} (**Phase 60: Voice prompt polish — name-once + single-question address
    intake**. `prompt.py` gains three new blocks: (1) NAME USE DURING THE CALL (no vocative
    mid-call; single readback at booking; caller-invited override; no-name path); (2) SERVICE
    ADDRESS (single-question opener "What's the address where you need the service?"; one
    targeted follow-up per missing piece; outcome-framed minimum capture); (3) BEFORE BOOKING
    — READBACK (mandatory name+address readback as the single authoritative verification
    moment, with accept-and-re-read correction loop referencing the existing CORRECTIONS rule).
    All 5 tool returns in `src/tools/*.py` (book_appointment, capture_lead, transfer_call,
    check_availability, check_caller_history) rewritten to strict STATE:<code>|DIRECTIVE:<imperative>
    format — machine-facing, not speakable English; generalizes the prior check_availability
    parrot-loop fix across every tool. `capture_lead` description reaches single-question-intake
    + readback parity with `book_appointment` (description-level only; signature parity deferred).
    Spanish `messages/es.json` + `prompt.py` locale='es' paths mirror all D-01..D-12 rules with
    idiomatic phrasing (user-reviewed per D-14). end_call.py unchanged (space-character return
    not a parrot-loop risk). agent.py VAD config unchanged (still LOW/LOW, 400ms/1000ms from
    backlog 999.2 fix). Tool signatures frozen — no new input surface.
    Previous: Phase 46: ...)
    ```

    The `{DD}` should be the actual day Plan 03 ships (use today's date in the executor's context). Preserve every pre-existing entry verbatim.

    **Step 2 — Update §4 (prompt section ordering).** Find the section that lists the prompt's ordered sections (per Phase 30 Plan 03 SUMMARY, this already exists). Add three entries for the new blocks, placed in their actual shipped positions (confirm by reading `prompt.py`). Use the exact section titles that shipped (NAME USE DURING THE CALL, SERVICE ADDRESS, BEFORE BOOKING — READBACK).

    **Step 3 — Update §5 (tool inventory / tool returns).** For each of the 5 tools, replace the old natural-English return-value examples with the shipped STATE+DIRECTIVE patterns. Copy the literal text from the tool files — do NOT paraphrase. For each tool, list every state code that appears in the file. Example format:

    ```
    ### book_appointment
    - State codes (D-16): `booking_succeeded`, `slot_taken`, `booking_invalid`, `booking_failed`
    - Return format: `"STATE:<code> [key=value ...] | DIRECTIVE:<imperative>"` — machine-facing,
      always ends with "Do not repeat this message text on-air."
    - Description reinforces CRITICAL PRECONDITION: readback-before-call (D-02).
    ```

    Cover book_appointment, capture_lead, transfer_call, check_availability, check_caller_history. Note `end_call` as unchanged.

    **Step 4 — Add a new subsection under §4 covering name-vocative suppression and single-question address intake.** This is a short paragraph (3-5 sentences) cross-referencing D-01..D-10 and noting that the readback is the single on-air name-confirmation moment. Include the specific English phrasing of the opener ("What's the address where you need the service?") so a future search for that string in SKILL.md returns a match.

    **Step 5 — Confirm the skill reads as source-of-truth to code.** Run these grep checks:
    ```
    grep -cF "name vocative" .claude/skills/voice-call-architecture/SKILL.md          # >= 1
    grep -cF "single-question" .claude/skills/voice-call-architecture/SKILL.md        # >= 1
    grep -cF "STATE:" .claude/skills/voice-call-architecture/SKILL.md                 # >= 5 (once per tool at minimum)
    grep -cF "DIRECTIVE:" .claude/skills/voice-call-architecture/SKILL.md             # >= 5
    grep -cF "What's the address where you need the service" .claude/skills/voice-call-architecture/SKILL.md   # >= 1
    grep -cF "BEFORE BOOKING — READBACK" .claude/skills/voice-call-architecture/SKILL.md                        # >= 1
    ```
    All must pass.

    **Step 6 — Commit** in `homeservice_agent` (NOT livekit_agent):
    ```
    git add .claude/skills/voice-call-architecture/SKILL.md
    git commit -m "docs(60): sync voice-call-architecture skill with Phase 60 prompt + tool-return changes"
    ```
    Commit body lists the four skill edits (new "Last updated" entry, §4 section-ordering update, §5 tool-return rewrites, new D-01..D-10 subsection) and includes Step 5 grep-check outputs.
  </action>
  <verify>
    <automated>grep -cF "name vocative" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "single-question" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "STATE:" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "DIRECTIVE:" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "What's the address where you need the service" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "BEFORE BOOKING" .claude/skills/voice-call-architecture/SKILL.md && grep -cF "Phase 60" .claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "Phase 60" .claude/skills/voice-call-architecture/SKILL.md` >= `1` (new "Last updated" entry present)
    - `grep -cF "name vocative" .claude/skills/voice-call-architecture/SKILL.md` >= `1`
    - `grep -cF "single-question" .claude/skills/voice-call-architecture/SKILL.md` >= `1`
    - `grep -cF "STATE:" .claude/skills/voice-call-architecture/SKILL.md` >= `5`
    - `grep -cF "DIRECTIVE:" .claude/skills/voice-call-architecture/SKILL.md` >= `5`
    - `grep -cF "What's the address where you need the service" .claude/skills/voice-call-architecture/SKILL.md` >= `1`
    - `grep -cF "BEFORE BOOKING" .claude/skills/voice-call-architecture/SKILL.md` >= `1`
    - `grep -cE "booking_succeeded|slot_taken|transfer_initiated|repeat_caller|slots_available" .claude/skills/voice-call-architecture/SKILL.md` >= `5` (state codes from every tool documented)
    - Prior `Last updated` entries preserved verbatim (Phase 46 block still present): `grep -cF "Phase 46: Priority (VIP) caller direct routing" .claude/skills/voice-call-architecture/SKILL.md` >= `1`
    - Commit landed in `homeservice_agent` with message starting `docs(60): sync voice-call-architecture skill`
    - Step 5 grep-check outputs recorded in commit body
    - No aspirational text: every STATE code and every DIRECTIVE phrasing in SKILL.md matches a string actually present in the corresponding `src/tools/*.py` file (spot-check: `grep -cF "STATE:booking_succeeded" .claude/skills/voice-call-architecture/SKILL.md` AND `grep -cF "STATE:booking_succeeded" livekit_agent/src/tools/book_appointment.py` — both >= 1)
  </acceptance_criteria>
  <done>SKILL.md `Last updated` section has a Phase 60 entry at the top; §4 prompt ordering reflects the three new blocks; §5 tool-return inventory shows STATE+DIRECTIVE patterns per tool with state codes copied from code; new D-01..D-10 subsection exists; Phase 46 entry preserved; one commit in `homeservice_agent`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spanish-speaking caller → Gemini model | Untrusted speech; posture unchanged from pre-Phase-60 (tool parameters frozen) |
| SKILL.md → future planner/executor context | Trusted authored doc; drift from code is the primary risk (Pitfall 7) |

## STRIDE Threat Register

Phase 60 Plan 03 is **security-neutral** per RESEARCH.md §Security Domain.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07 | Tampering | Spanish locale bypass (weaker rules for Spanish callers) | mitigate | D-13 + D-14: same-shape structural check ensures no rule is dropped; user review catches any dilution. Task 1 acceptance criteria include a grep check that all three Spanish blocks are present. |
| T-60-08 | Information Disclosure | SKILL.md drift from actual code (future planner makes wrong decisions based on stale doc) | mitigate | Task 3 Step 5 grep-sync checks enforce that every STATE code in SKILL.md maps to a real string in the corresponding tool file. Spot-check acceptance criterion confirms booking_succeeded appears in both SKILL.md and book_appointment.py. |

No BLOCKER threats. No HIGH severity. Rating: `security_review: informational`.
</threat_model>

<verification>
**Automated (run at plan completion):**
```
# Spanish blocks present
grep -lF "No se dirija al cliente por su nombre" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json
grep -lF "¿Cuál es la dirección donde necesita el servicio?" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json
grep -lF "Lea de nuevo el nombre" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json

# No VAD-redundant Spanish
grep -nE "deje (que|de)|no (interrumpa|hable)|espere a que" livekit_agent/src/prompt.py livekit_agent/src/messages/es.json   # must be empty

# Pytest green
cd livekit_agent && pytest tests/ -x --tb=short

# Skill-sync
grep -cF "Phase 60" .claude/skills/voice-call-architecture/SKILL.md
grep -cF "STATE:" .claude/skills/voice-call-architecture/SKILL.md
grep -cF "STATE:booking_succeeded" .claude/skills/voice-call-architecture/SKILL.md
grep -cF "STATE:booking_succeeded" livekit_agent/src/tools/book_appointment.py
```

**Manual (UAT Persona 7 — Spanish):**
- Spanish caller: "Hola, me llamo María García. Necesito un plomero en Calle Mayor 45, Madrid 28013."
- Expected: Spanish single-question intake; Spanish readback once; no vocative "Gracias, María" before readback; `book_appointment` fires after Spanish readback.
- Pass: zero English drift; same rule count observed as in English call.
</verification>

<success_criteria>
- Three Spanish prompt blocks present in `livekit_agent`
- User-approved (D-14) via Task 2 checkpoint
- Zero VAD-redundant Spanish phrases
- `SKILL.md` updated with Phase 60 "Last updated" entry, §4 ordering, §5 tool-return rewrites, new D-01..D-10 subsection
- Every STATE code in SKILL.md maps to a real string in the corresponding tool file (spot-check passes)
- Prior Phase 46 entry preserved verbatim in SKILL.md
- `pytest livekit_agent/tests/` green
- Two commits minimum: Spanish feat commit in `livekit_agent` (post-approval merge), SKILL.md docs commit in `homeservice_agent`
- UAT Persona 7 (Spanish) passes on live Railway deploy
</success_criteria>

<output>
After completion, create `.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-03-SUMMARY.md`. Record (a) the `livekit_agent` Spanish commit SHA, (b) the `homeservice_agent` SKILL.md commit SHA, (c) the register chosen (usted/tú), (d) whether any re-drafts were needed during Task 2 review, (e) any pre-existing Spanish structural gaps from Phase 30 that surfaced.

This is the final plan for Phase 60. After its completion plus the full UAT cycle (Personas 1-7) plus the 24-hour Sentry review window, the phase is ready for `/gsd-verify-work`.
</output>
