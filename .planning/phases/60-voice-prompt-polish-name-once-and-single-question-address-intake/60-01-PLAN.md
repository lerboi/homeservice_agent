---
phase: 60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livekit_agent/src/prompt.py
  - livekit_agent/src/messages/en.json
  - livekit_agent/docs/uat/phase-60-personas.md
  - livekit_agent/docs/uat/phase-60-sentry-playbook.md
autonomous: true
requirements: []
addresses_decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-15]
cross_repo: true
tags: [voice-agent, prompt-engineering, gemini, livekit]

must_haves:
  truths:
    - "AI never addresses the caller by name mid-call (no 'Thanks, {name}', 'Okay {name}', etc.) except during the booking readback or when the caller has explicitly invited name use."
    - "AI opens address intake with a single natural question ('What's the address where you need the service?' or very close) instead of a three-part walkthrough."
    - "If the caller volunteers only part of an address, the AI asks exactly one targeted follow-up for the specific missing piece — never a shopping list."
    - "Before `book_appointment` fires, the AI reads back the caller's name (if captured) and full service address in one utterance as the single authoritative verification moment."
    - "If the caller corrects any part of the readback, the AI accepts the correction and re-reads the corrected full line; it loops until the caller stops correcting."
    - "If no name was captured, the booking still completes — the readback contains only the address."
    - "Anti-hallucination rules (`OUTCOME WORDS — CRITICAL RULE`, `TOOL NARRATION — CRITICAL RULE`) remain in the top-of-prompt region; no VAD-redundant guidance ('wait for them to finish', 'don't interrupt') exists in the prompt."
    - "UAT persona scripts (1–7) and Sentry-regression playbook are documented in `livekit_agent/docs/uat/` before any UAT cycle begins."
    - "`livekit_agent/` repo is confirmed git-clonable/pushable from the executor environment; `livekit_agent/pyproject.toml` pytest config is confirmed."
  artifacts:
    - path: "livekit_agent/src/prompt.py"
      provides: "NAME USE DURING CALL block (D-01..D-05); SERVICE ADDRESS block (D-06..D-08) replacing three-part walkthrough; readback + corrections block in booking section (D-02, D-09, D-10)."
      contains: ["NAME USE DURING THE CALL", "SERVICE ADDRESS", "READBACK", "OUTCOME WORDS", "TOOL NARRATION"]
    - path: "livekit_agent/src/messages/en.json"
      provides: "Any agent.* templated strings referenced by the edited prompt sections, updated to match the new single-question + readback-once framing."
    - path: "livekit_agent/docs/uat/phase-60-personas.md"
      provides: "Seven scripted UAT personas covering D-01..D-14 (Persona 1..7 per VALIDATION.md)."
      contains: ["Persona 1", "Persona 2", "Persona 3", "Persona 4", "Persona 5", "Persona 6", "Persona 7"]
    - path: "livekit_agent/docs/uat/phase-60-sentry-playbook.md"
      provides: "Sentry query strings + what-to-look-for for `tool_call_cancelled` and parrot-loop regression signals."
      contains: ["tool_call_cancelled", "parrot"]
  key_links:
    - from: "_build_info_gathering_section (prompt.py)"
      to: "NAME USE DURING THE CALL block"
      via: "direct embedding in the info-gathering assembly"
      pattern: "NAME USE DURING THE CALL"
    - from: "_build_info_gathering_section (prompt.py)"
      to: "SERVICE ADDRESS single-question block"
      via: "direct embedding replacing the prior three-part walkthrough"
      pattern: "What's the address where you need the service"
    - from: "_build_booking_section (prompt.py)"
      to: "READBACK block referencing the CORRECTIONS rule"
      via: "embedded before the tool-call instruction"
      pattern: "read back.*name.*address|BEFORE BOOKING.*READBACK"
---

<objective>
Land the English-language prompt changes (Change A — name-once; Change B — single-question address intake; readback + corrections reinforcement), complete the D-15 light structural audit, and close all Wave 0 prerequisites so Plan 02 and Plan 03 have a validated platform to build on.

Purpose: The prompt is the **only** enforcement surface for these rules on the pinned Gemini 3.1 Flash Live stack (no `per_response_tool_choice`, no output filtering). Change A fixes the compounding mispronunciation problem on culturally-diverse names; Change B removes the mechanical three-part address walkthrough in favor of a single natural opener that handles SG-lead ("Jurong West, unit 6-12") and US-lead ("123 Main Street") cases identically.
Output: Updated `livekit_agent/src/prompt.py` with the three surgical section changes, corresponding `messages/en.json` string updates, and two new UAT-support docs.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/leroyngzz/Projects/homeservice_agent/CLAUDE.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/STATE.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/ROADMAP.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-CONTEXT.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-VALIDATION.md
@/Users/leroyngzz/Projects/homeservice_agent/.planning/phases/30-voice-agent-prompt-optimization/30-02-SUMMARY.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md

<cross_repo_note>
**All code changes in this plan land in `livekit_agent/` — a SEPARATE Railway-deployed git repo at `C:/Users/leheh/.Projects/livekit-agent/` (Windows path; adjust for local OS).** It is NOT under `homeservice_agent/`. The executor must `git clone` or `cd` into that repo to make changes and must push to its own remote. The only files this plan writes inside `homeservice_agent/` are the GSD planning artifacts.
</cross_repo_note>

<interfaces>
<!-- Key existing references the executor needs. Do not explore; use these directly. -->

From `livekit_agent/src/prompt.py` (Phase 30 structure, per SKILL.md §4):
- `build_system_prompt(locale, *, business_name, onboarding_complete, tone_preset, intake_questions, country, working_hours, tenant_timezone)` — central assembly
- `_build_identity_section` — D-15 audit target (persona still distinct)
- `_build_voice_behavior_section` — D-15 audit target (no VAD-redundant lines)
- `_build_corrections_section` — existing "CORRECTIONS — the caller's correction is ALWAYS correct" rule
- `_build_info_gathering_section` — approximate L295-337; current NAME + ADDRESS blocks
- `_build_booking_section` — BEFORE BOOKING / AFTER BOOKING blocks

From `livekit_agent/src/messages/en.json` (per SKILL.md §4 end):
- Two top-level keys: `agent.*` (recording_disclosure, language_clarification, capture_name, etc.) and `notifications.*` (booking_confirmation, recovery_sms_*)

From `livekit_agent/src/agent.py` (read-only — do NOT modify):
- VAD config: `AutomaticActivityDetection(start_of_speech_sensitivity=LOW, end_of_speech_sensitivity=LOW, prefix_padding_ms=400, silence_duration_ms=1000)` — this is why the prompt must have NO "let the caller finish" lines.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 prerequisites — confirm cross-repo access, inventory tool-return tests, document UAT personas and Sentry playbook</name>
  <files>livekit_agent/docs/uat/phase-60-personas.md, livekit_agent/docs/uat/phase-60-sentry-playbook.md</files>
  <read_first>
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-VALIDATION.md (Wave 0 Requirements section — defines personas 1..7)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-CONTEXT.md (Validation section — sketches personas 1..3)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md (Open Questions §6 — Sentry signals to document)
    - livekit_agent/pyproject.toml (confirm [tool.pytest.ini_options] present — this file is in the SEPARATE Railway repo)
  </read_first>
  <action>
    Complete all Wave 0 gaps from VALIDATION.md before any prompt edits ship.

    **Step 1 — Cross-repo access confirmation.** Confirm the executor can `git clone` and `git push` `livekit_agent/`. Run `git -C {livekit_agent_path} status` and `git -C {livekit_agent_path} remote -v` and capture both outputs in the task commit message. If access fails, stop and surface the blocker — the rest of the plan cannot proceed.

    **Step 2 — Pytest config discovery.** Inside `livekit_agent/`, read `pyproject.toml`. Confirm `[tool.pytest.ini_options]` or equivalent pytest config exists. If absent, flag as a prerequisite blocker. Record the `testpaths` value — the Task 2 regression command depends on it.

    **Step 3 — Tool-return test inventory.** From `livekit_agent/` repo root, run exactly:
    ```
    grep -rn "Booking confirmed\|slot is available\|lead captured\|address=\|Appointment confirmed\|transfer initiated" livekit_agent/tests/ || echo "NO_MATCHES"
    ```
    Capture the output in the commit message. If matches exist, note the file:line pairs in a comment at the top of Plan 02 (they will be updated alongside the tool-return rewrite in the same commit — see Plan 02 Task 1). If `NO_MATCHES`, explicitly state this in the commit body.

    **Step 4 — Write `livekit_agent/docs/uat/phase-60-personas.md`.** Seven personas, each section with: `## Persona N — {short-title}`, `**Addresses D-IDs:** ...`, `**Language:** en|es`, `**Script (what the tester says):**` (numbered caller turns), `**Expected AI behavior:**` (numbered assertions), `**Pass criteria:**` (bulleted). Use these exact personas:

    - **Persona 1 — Culturally diverse name + clear address (baseline).** Addresses D-01, D-02, D-06. Script: "Hi, my name is Jia En Tan. I need a plumber at 42 Lornie Road, Singapore 298107." Expected: AI captures name silently (no "Thanks, Jia En" / "Okay Jia En" anywhere before readback); opens address intake in a single natural question at some point if additional address work is needed; reads back name + address exactly once immediately before `book_appointment`. Pass: zero vocative name use before readback; readback is one utterance; `book_appointment` fires right after.
    - **Persona 2 — Casual one-breath address (SG-style lead).** Addresses D-06, D-07. Script: "Yeah it's Jurong West, block 6, unit 12-345." Expected: AI extracts street/block/unit/area from the single utterance; asks exactly one targeted follow-up for the postal code (or whichever field the validator needs); no three-part walkthrough. Pass: the follow-up is a single question, not a list.
    - **Persona 3 — Mid-readback correction (US-style lead).** Addresses D-05, D-09, D-10. Script: Name "Sam Johnson", address "123 Main Street, Austin, Texas 78701" — but during readback, tester corrects: "no, it's 125, not 123." Then on the next readback corrects: "and it's spelled with an 'h' — Johnson with an h." Expected: AI accepts each correction, re-reads the corrected full name + address both times, does not stop re-reading after one correction. Pass: at least two readback iterations occurred; each re-read contained the full line.
    - **Persona 4 — Caller invites name use (D-03 override).** Script: Early in the call: "You can call me Sam." Expected: AI may use the name naturally for the rest of the call (not every turn — outcome-framed); the readback still fires. Pass: subjective naturalness; at least one natural use of the name post-invitation; no vocative use before the invitation.
    - **Persona 5 — Caller refuses name (D-04).** Script: When asked for name, tester says: "I'd rather not give a name." Expected: AI proceeds without blocking; the readback contains only the address; `book_appointment` fires; DB row has `caller_name` null/empty. Pass: booking completes; Supabase `appointments` row reflects empty name.
    - **Persona 6 — Caller declines to book (D-11, D-12 decline path).** Script: Normal name + address intake, then tester says: "Actually, I just want someone to call me back later." Expected: AI still does the single-question intake; reads back name + address once before `capture_lead` fires. Pass: readback occurred on the decline path; `capture_lead` fires once, not after a vocative name re-use.
    - **Persona 7 — Spanish caller (D-13).** Script: In Spanish: "Hola, me llamo María García. Necesito un plomero en Calle Mayor 45, Madrid 28013." Expected: AI switches to Spanish on explicit request; single-question opener in Spanish; readback once in Spanish; all D-01..D-12 rules hold in Spanish. Pass: no English drift; readback in Spanish; same rule count as English.

    **Step 5 — Write `livekit_agent/docs/uat/phase-60-sentry-playbook.md`.** Document two Sentry queries the UAT reviewer runs 24 hours post-deploy:

    Query 1 — `tool_call_cancelled` baseline check:
    ```
    tags.component:voice-agent AND message:"tool_call_cancelled" AND timestamp:[now-24h TO now]
    ```
    What to look for: the baseline is zero-to-few per day (Phase 999.2 VAD tuning eliminated the main source). Phase 60 is a prompt-only change; any new spike means a VAD-redundant prompt line crept in.

    Query 2 — Parrot-loop signal (tool return spoken verbatim):
    ```
    tags.component:voice-agent AND (message:"Booking confirmed for" OR message:"slot is available" OR message:"earliest at" OR message:"lead captured") AND timestamp:[now-24h TO now]
    ```
    What to look for: the AI speaking one of the old natural-English return strings. Zero matches = D-16 held. Any match = a tool return regressed.

    Note in the doc that these are manual queries — Phase 60 does not ship a Sentry alert rule.

    **Step 6 — Commit.** One commit in `livekit_agent/` repo: `docs(60): add phase 60 UAT personas + Sentry playbook + wave 0 prereq confirmations`. Include the Step 1 + Step 2 + Step 3 outputs in the commit body (cross-repo access proof, pytest config confirmed, tool-return test inventory result).
  </action>
  <verify>
    <automated>test -f livekit_agent/docs/uat/phase-60-personas.md && test -f livekit_agent/docs/uat/phase-60-sentry-playbook.md && grep -c "^## Persona" livekit_agent/docs/uat/phase-60-personas.md && grep -c "tool_call_cancelled\|parrot" livekit_agent/docs/uat/phase-60-sentry-playbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `git -C livekit_agent status` succeeded — output captured in commit body
    - `git -C livekit_agent remote -v` succeeded — output captured in commit body
    - `livekit_agent/pyproject.toml` contains `[tool.pytest.ini_options]` (or equivalent) — confirmation + testpaths value recorded in commit body
    - `grep -rn "Booking confirmed\|slot is available\|lead captured\|address=\|Appointment confirmed\|transfer initiated" livekit_agent/tests/` output captured in commit body (even if `NO_MATCHES`)
    - `grep -c "^## Persona " livekit_agent/docs/uat/phase-60-personas.md` returns exactly `7`
    - `grep -c "Addresses D-" livekit_agent/docs/uat/phase-60-personas.md` returns at least `7` (one per persona)
    - `grep -Fx "**Addresses D-IDs:** D-13" livekit_agent/docs/uat/phase-60-personas.md` returns a match (Persona 7 Spanish)
    - `grep -c "tool_call_cancelled" livekit_agent/docs/uat/phase-60-sentry-playbook.md` >= 1
    - `grep -ci "parrot" livekit_agent/docs/uat/phase-60-sentry-playbook.md` >= 1
    - Commit landed in `livekit_agent` repo with message starting `docs(60):`
  </acceptance_criteria>
  <done>Cross-repo access confirmed; pytest config confirmed; tool-return test inventory captured; seven UAT personas documented covering D-01..D-14; Sentry playbook documents both regression queries; one commit in livekit_agent repo.</done>
</task>

<task type="auto">
  <name>Task 1: Edit prompt.py — name-vocative suppression (D-01..D-05) + single-question address intake (D-06..D-08) + booking readback with corrections (D-02, D-09, D-10)</name>
  <files>livekit_agent/src/prompt.py, livekit_agent/src/messages/en.json</files>
  <read_first>
    - livekit_agent/src/prompt.py (CURRENT STATE — read the ENTIRE file first; the approximate L295-337 range from CONTEXT.md may have drifted. Locate the sections by name, not by line number.)
    - livekit_agent/src/messages/en.json (read the full `agent.*` block — identify any capture_name / address-prompt strings that will be referenced by the new prompt sections)
    - livekit_agent/src/agent.py (READ-ONLY for context — confirm VAD config is LOW/LOW, 400ms/1000ms. Do NOT modify.)
    - .planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-RESEARCH.md §Code Examples (patterns 1–4) — use as illustrative reference, not verbatim text.
    - .planning/phases/30-voice-agent-prompt-optimization/30-02-SUMMARY.md (what Phase 30 shipped — the substrate sections to preserve)

    **CROSS-REPO NOTE:** `livekit_agent/src/prompt.py` and `livekit_agent/src/messages/en.json` live in the separate `livekit_agent` Railway repo. All edits must be made there and committed in that repo — NOT in `homeservice_agent/`.
  </read_first>
  <action>
    Make three surgical edits to `livekit_agent/src/prompt.py` and mirror any affected template strings in `livekit_agent/src/messages/en.json`. The D-15 light audit is covered in-flight as part of Edit 4 below. Do NOT rewrite Phase 30 sections — add or replace only the specific blocks described.

    **Edit 1 — Insert a NAME USE DURING THE CALL block inside `_build_info_gathering_section` (or a new tiny `_build_name_vocative_section` called from the info-gathering assembly).**

    Placement: immediately before or after the existing NAMES capture subsection (where Phase 30's "cultural awareness + capture early" rule lives). Exact replacement text (outcome-framed, per RESEARCH.md Pattern 2):

    ```
    NAME USE DURING THE CALL:
    - Capture the caller's name early and use it silently for records. Do not address the caller by
      name mid-call (no "Thanks, {name}" or "Okay {name}"). The sole exception is the booking
      readback below — that is the single authoritative moment to confirm the name on-air.
    - If the caller explicitly invites you to use their name (for example, they say "you can call
      me X" or "please call me X" or "I go by X"), you may use their name naturally for the rest
      of the call. Do not wait for a specific phrase — use judgment.
    - If no name was captured (caller declined or could not be understood), proceed without a name.
      Skip the name portion of the booking readback. Booking is never blocked by a missing name.
    - Do not add extra verification lines for spelled-out or low-confidence names. The existing
      CORRECTIONS rule handles mispronunciations during the readback.
    ```

    This block addresses D-01, D-02 (forward reference to readback), D-03, D-04, D-05.

    **Edit 2 — Replace the existing three-part address walkthrough in `_build_info_gathering_section` with a SERVICE ADDRESS block.** Remove whatever current text enumerates street / postal / unit as separate prescriptive steps. Replace with this exact block (outcome-framed, per Pattern 2):

    ```
    SERVICE ADDRESS:
    - Ask one natural question: "What's the address where you need the service?"
    - Extract whatever the caller volunteered — street, postal area, unit, block, building name, etc.
    - If a piece is missing that we would need to find the place, ask exactly one targeted
      follow-up for that specific missing piece. Loop one piece at a time. Never run a
      mechanical walkthrough or recite a list of fields to the caller.
    - Capture enough for us to find the place. Do not enumerate field names on-air.
    ```

    This addresses D-06, D-07, D-08. Do NOT add language like "wait for them to finish" — the VAD owns turn-taking (RESEARCH.md Pitfall 2).

    **Edit 3 — Update `_build_booking_section` to add a BEFORE BOOKING — READBACK block before the existing AFTER BOOKING block.** If an AFTER BOOKING block exists, leave it intact. Insert this block ahead of it (outcome-framed; references existing CORRECTIONS rule rather than redefining it):

    ```
    BEFORE BOOKING — READBACK (mandatory):
    - Read back the caller's name (if captured) and the full service address in one utterance.
      This is the single authoritative verification moment for both name and address. Order:
      name first, then address (names are shorter, so a caller is more likely to correct name
      before moving on to address).
    - If the caller corrects any part of the readback, accept the correction (the caller's
      correction is ALWAYS correct — see CORRECTIONS above) and re-read the corrected full line
      before calling book_appointment. If they correct again, loop: accept, re-read the full
      corrected line, until they stop correcting.
    - If no name was captured, read back only the address. Do not pause to ask for a name.
    - Call book_appointment only after the caller acknowledges the readback (silence or an
      explicit "yes" / "that's right" counts).
    ```

    This addresses D-02, D-09, D-10 and the no-name Pitfall 6.

    **Edit 4 — D-15 light structural audit (inline, in the same commit).** Do NOT rewrite. Perform three grep-based checks on the modified file and record results in the commit body:

    1. Run (from livekit_agent repo root):
       ```
       python -c "from src.prompt import build_system_prompt; p = build_system_prompt('en', business_name='Acme', onboarding_complete=True, tone_preset='professional', intake_questions=['Q1'], country='US', working_hours=None, tenant_timezone='America/Los_Angeles'); print(p[:2000])"
       ```
       Verify the first ~30 non-blank lines of the assembled prompt contain both `OUTCOME WORDS` and `TOOL NARRATION`. If they have drifted lower, STOP and ask the user — do not move them; this indicates a regression that needs triage.

    2. Run: `grep -nE "let (the|them) (caller )?finish|do(n't| not) (interrupt|talk over)|wait for (the|them) (caller )?to (finish|stop|be done)" livekit_agent/src/prompt.py` — MUST return nothing. If it returns matches, remove the offending lines (they are VAD-redundant per RESEARCH.md Pitfall 2).

    3. Confirm `_build_identity_section` still mentions the three configured personas or the `tone_preset` parameter — the persona-distinctness check. No edit required; just confirm it exists. Record the result.

    **Edit 5 — `messages/en.json` sync.** Read the current `agent.*` block. For any string that is referenced by the edited prompt sections (examples: `agent.capture_name`, `agent.capture_address_initial`, `agent.booking_readback`), either:
    - Delete the string if the rewritten prompt block no longer interpolates a template (the outcome-framed blocks above are self-contained — they do not need template strings), OR
    - Update the string to match the new single-question or readback phrasing.

    If NO `agent.*` strings were referenced by the replaced sections (because the Phase 30 prompt was fully inline), make NO changes to `en.json`. Record which option was taken in the commit body.

    **Commit structure:** one commit in `livekit_agent`: `feat(60): prompt polish — name-once + single-question address + readback corrections`. Commit body lists the four edits, the D-IDs addressed (D-01..D-10, D-15), and the grep audit outputs.
  </action>
  <verify>
    <automated>cd livekit_agent && grep -c "NAME USE DURING THE CALL" src/prompt.py && grep -c "What's the address where you need the service" src/prompt.py && grep -c "BEFORE BOOKING.*READBACK\|read back.*name.*address" src/prompt.py && { grep -nE "let (the|them) (caller )?finish|do(n't| not) (interrupt|talk over)|wait for (the|them) (caller )?to (finish|stop|be done)" src/prompt.py && echo "FAIL_VAD_REDUNDANT" || echo "OK_NO_VAD_REDUNDANT"; } && pytest tests/ -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NAME USE DURING THE CALL" livekit_agent/src/prompt.py` returns exactly `1`
    - `grep -c "Do not address the caller by name" livekit_agent/src/prompt.py` returns exactly `1`
    - `grep -cE "SERVICE ADDRESS" livekit_agent/src/prompt.py` returns at least `1` (the new block)
    - `grep -cF "What's the address where you need the service" livekit_agent/src/prompt.py` returns exactly `1`
    - `grep -cE "BEFORE BOOKING.*READBACK|read back the caller's name" livekit_agent/src/prompt.py` >= `1`
    - `grep -cF "capture enough for us to find the place" livekit_agent/src/prompt.py` >= `1`
    - `grep -cF "the caller's correction is ALWAYS correct" livekit_agent/src/prompt.py` >= `1` (reference to existing CORRECTIONS rule preserved)
    - `grep -nE "let (the|them) (caller )?finish|do(n't| not) (interrupt|talk over)|wait for (the|them) (caller )?to (finish|stop|be done)" livekit_agent/src/prompt.py` returns ZERO lines (D-15 VAD-redundant check)
    - `grep -cE "three-part walkthrough|mechanical walkthrough" livekit_agent/src/prompt.py` — the word "mechanical" appears AS AN INSTRUCTION NOT TO DO IT (match on "Never run a mechanical walkthrough"); no prescriptive three-field enumeration remains for the address ask
    - Assembled-prompt smoke check: the Python one-liner from Edit 4 Step 1 succeeds and its output's first 30 non-blank lines contain both `OUTCOME WORDS` and `TOOL NARRATION`
    - `pytest livekit_agent/tests/ -x --tb=short` passes (no test regression from edits)
    - Commit landed in `livekit_agent` with message starting `feat(60):`
    - Commit body documents D-15 grep audit outputs (VAD-redundant check = zero, anti-hallucination check = present in first 30 lines, persona-distinctness check = confirmed)
    - Commit body states whether `en.json` was edited and which strings, OR explicitly states no `en.json` edit was needed
  </acceptance_criteria>
  <done>`prompt.py` contains the NAME USE block (D-01..D-05), the single-question SERVICE ADDRESS block (D-06..D-08), and the BEFORE BOOKING READBACK block (D-02, D-09, D-10). D-15 audit ran clean (anti-hallucination rules still top-of-prompt; zero VAD-redundant lines). `messages/en.json` mirrors any affected templates. Existing pytest suite green. One commit in `livekit_agent` repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller audio → Gemini model | Untrusted speech input reaches the LLM via server VAD; no new surface in Phase 60 (tool parameters unchanged) |
| prompt.py → Gemini system instructions | Trusted authored text (git-versioned); no runtime interpolation of caller content into the system prompt |

## STRIDE Threat Register

Phase 60 is **security-neutral** per RESEARCH.md §Security Domain. The ASVS V5 (Input Validation) posture is unchanged — tool parameter surfaces are frozen per CONTEXT.md Phase Boundary. V7 (Error Handling & Logging) analysis is covered by Plan 02 (D-16 tool-return rewrite, which is net-positive for V7).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | Caller name spoken repeatedly across the call (pre-Phase-60 behavior) | mitigate | D-01 removes mid-call vocative; readback at booking is the single on-air mention. This is a MODEST PII-exposure reduction (one-utterance-per-call vs N-utterances), not a new mitigation. |
| T-60-02 | Tampering | Prompt injection via caller-spoken content | accept | Pre-existing posture unchanged — caller speech is captured as tool parameters, never interpolated into the system prompt. Phase 60 adds no interpolation points. |
| T-60-03 | Spoofing | Unverified name used across entire call | mitigate | D-01 + D-02: name is used silently and verified once at booking; the readback is the verification moment for the caller to confirm/correct. |

No BLOCKER threats. No HIGH severity. Rating: `security_review: informational`.
</threat_model>

<verification>
**Automated (run at plan completion):**
```
cd livekit_agent
grep -c "NAME USE DURING THE CALL" src/prompt.py
grep -c "What's the address where you need the service" src/prompt.py
grep -cE "BEFORE BOOKING|read back the caller's name" src/prompt.py
grep -nE "let (the|them) (caller )?finish|do(n't| not) (interrupt|talk over)|wait for (the|them) (caller )?to (finish|stop|be done)" src/prompt.py   # must be empty
pytest tests/ -x --tb=short
test -f docs/uat/phase-60-personas.md
test -f docs/uat/phase-60-sentry-playbook.md
grep -c "^## Persona " docs/uat/phase-60-personas.md   # must be 7
```

**Manual (UAT personas — run after Railway deploy, covered in phase-gate):**
- Persona 1 (D-01, D-02, D-06) — pass
- Persona 2 (D-06, D-07) — pass
- Persona 3 (D-05, D-09, D-10) — pass
- Persona 4 (D-03) — pass
- Persona 5 (D-04) — pass
</verification>

<success_criteria>
- Prompt contains NAME USE DURING THE CALL, SERVICE ADDRESS, and BEFORE BOOKING READBACK blocks
- Anti-hallucination rules (`OUTCOME WORDS`, `TOOL NARRATION`) still appear in the first 30 lines of assembled prompt
- Zero VAD-redundant phrases in `prompt.py`
- `pytest` green
- UAT personas 1..7 documented; Sentry playbook documented
- Cross-repo access confirmed; pytest config confirmed
- Two commits in `livekit_agent` repo (Task 0 docs commit, Task 1 feat commit)
</success_criteria>

<output>
After completion, create `.planning/phases/60-voice-prompt-polish-name-once-and-single-question-address-intake/60-01-SUMMARY.md`. Note the two commits in the `livekit_agent` repo (full short-SHAs) since they are not visible from `git log` in `homeservice_agent`.
</output>
