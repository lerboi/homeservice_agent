---
phase: 47-landing-objection-busting-repositioning-and-landing-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/accordion.jsx
  - tests/unit/landing-sections.test.js
  - tests/unit/public-chat-api.test.js
  - public/audio/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "shadcn Accordion primitive is importable from @/components/ui/accordion"
    - "Smoke test file exists that can assert content presence in new landing components"
    - "Public chat API smoke test confirms anonymous POST returns {reply}"
    - "OBJ-02 inline player has an approved audio source path"
  artifacts:
    - path: "src/components/ui/accordion.jsx"
      provides: "Radix Accordion primitive for FAQSection"
      contains: "AccordionTrigger"
    - path: "tests/unit/landing-sections.test.js"
      provides: "Jest smoke tests for content presence across Phase 47 landing sections"
      contains: "describe"
    - path: "tests/unit/public-chat-api.test.js"
      provides: "Jest unit test confirming /api/public-chat anonymous POST shape"
      contains: "public-chat"
  key_links:
    - from: "FAQSection.jsx (future, Plan 04)"
      to: "src/components/ui/accordion.jsx"
      via: "import from @/components/ui/accordion"
      pattern: "@/components/ui/accordion"
---

<objective>
Wave 0 foundation: install the missing shadcn Accordion component, scaffold the Jest smoke test files Phase 47 plans will use, confirm the pre-recorded audio source for the OBJ-02 inline player.

Purpose: RESEARCH.md Pitfall 1 confirms `src/components/ui/accordion.jsx` is missing despite CONTEXT.md D-09 claiming "already installed". FAQSection (Plan 04) imports from this file — it must exist before Plan 04 executes. Smoke tests scaffolded here give all downstream plans a Nyquist `<automated>` target.

Output: accordion primitive file, smoke test scaffold (empty describe blocks), public-chat anonymous POST test, audio sample path documented.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-CONTEXT.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md

<interfaces>
<!-- shadcn Accordion Radix API (will be installed by this plan) -->
<!-- Expected exports: Accordion, AccordionItem, AccordionTrigger, AccordionContent -->

Existing public chat API contract (already deployed, DO NOT modify):
- POST /api/public-chat
- Body: { message: string, currentRoute?: string, history?: Array<{role, content}> }
- Response 200: { reply: string }
- Response 429 / 400 / 503: { error: string }
- Implementation: src/app/api/public-chat/route.js (already exists)

Existing audio assets (public/audio/):
- demo-intro.mp3 (hero demo intro snippet — can be reused per Open Question #1 fallback)
- demo-mid.mp3
- demo-outro.mp3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Accordion component</name>
  <files>src/components/ui/accordion.jsx</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/components.json (confirms new-york preset, neutral, radix)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Pitfall 1: Accordion Not Installed section)
    - Verify absence: ls src/components/ui/ should NOT contain accordion.jsx
  </read_first>
  <action>
    Run: `npx shadcn@latest add accordion`

    This will:
    - Create `src/components/ui/accordion.jsx` with Radix Accordion primitives (Accordion, AccordionItem, AccordionTrigger, AccordionContent).
    - Match the new-york / neutral / radix preset already in components.json.
    - Pull in `@radix-ui/react-accordion` as a dependency if not already installed.

    If the CLI prompts for a framework/style, accept defaults (they match components.json). If the file already exists and CLI asks to overwrite, SKIP — do not overwrite. The goal is presence, not refresh.

    After install, verify the file exports `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent` by reading the file head.

    Do NOT modify any existing shadcn file (button, input, card, etc.). Accordion is the only component this phase needs.
  </read_first_reason>Confirms the preset the CLI will use and documents the missing-component finding from research.</read_first_reason>
  <action_note>CLI command, not manual code authoring. Preset matches components.json automatically.</action_note>
  <verify>
    <automated>test -f src/components/ui/accordion.jsx &amp;&amp; grep -l "AccordionTrigger" src/components/ui/accordion.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/accordion.jsx` exists
    - File contains exported symbols `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent` (grep each)
    - `grep "@radix-ui/react-accordion" package.json` returns a match (dependency added)
    - `npm run build` does not error on accordion imports (sanity)
  </acceptance_criteria>
  <done>
    `src/components/ui/accordion.jsx` is importable from `@/components/ui/accordion` and exports the 4 Radix Accordion primitives FAQSection will consume in Plan 04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold landing-sections smoke test file</name>
  <files>tests/unit/landing-sections.test.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Validation Architecture section — Phase Requirements Test Map)
    - C:/Users/leheh/.Projects/homeservice_agent/package.json (confirm jest test script shape)
    - ls tests/unit/ to confirm directory exists (if not, Task creates it)
  </read_first>
  <action>
    Create `tests/unit/landing-sections.test.js` as a Jest smoke-test scaffold. Each downstream plan will populate the `describe` blocks as it creates its section files. Wave 0 writes EMPTY placeholder `describe` blocks with `it.todo` entries so `npm test` passes cleanly today but the test IDs exist for Plan 02/03/04 to fill in.

    Exact file contents:

    ```js
    /**
     * Phase 47 — Landing sections smoke tests.
     * Populated progressively by plans 02, 03, 04 as component files are created.
     * Wave 0 provides the scaffold so `npm test` does not fail on missing file.
     */

    describe('Phase 47 — AfterTheCallStrip (REPOS-03)', () => {
      it.todo('renders at least 4 after-the-call workflow items');
      it.todo('does not use id="features" or id="testimonials" on its section element');
    });

    describe('Phase 47 — IdentitySection (OBJ-06)', () => {
      it.todo('renders complement-framing copy (no replacement language)');
    });

    describe('Phase 47 — PracticalObjectionsGrid (OBJ-02/03/04/05/08/09)', () => {
      it.todo('renders the $260,400 cost-of-inaction stat (OBJ-03)');
      it.todo('renders the 85% blind-test stat chip (OBJ-02)');
      it.todo('renders the 3-step setup strip with "forward", "hours", "live" (OBJ-04)');
      it.todo('renders trust/escalation badge row (OBJ-05)');
      it.todo('renders before-vs-after workflow comparison (OBJ-08)');
      it.todo('renders 5 trade icons: plumbing, HVAC, electrical, handyman, roofing (OBJ-09)');
    });

    describe('Phase 47 — OwnerControlPullQuote (REPOS-04)', () => {
      it.todo('renders on dark #1C1412 background');
      it.todo('contains an owner-control pull-quote');
    });

    describe('Phase 47 — FAQSection (OBJ-01 + D-10)', () => {
      it.todo('renders exactly 7 FAQ questions from the locked D-06 list');
      it.todo('uses shadcn Accordion with type="single" collapsible');
      it.todo('renders chat widget with "Still wondering?" header');
      it.todo('chat widget posts to /api/public-chat');
    });

    describe('Phase 47 — Hero copy (REPOS-01)', () => {
      it.todo('HeroSection H1 does not contain replacement framing "Let Voco Handle Your"');
      it.todo('HeroSection retains RotatingText element');
    });

    describe('Phase 47 — FinalCTA copy (REPOS-02)', () => {
      it.todo('FinalCTA subtitle contains owner-control language ("your rules" or "your schedule")');
    });
    ```

    This file uses `it.todo` so Jest reports them as pending but the suite passes. Downstream plans convert `it.todo` to real `it()` assertions when they create components.

    Run `npm test -- tests/unit/landing-sections.test.js` to confirm Jest finds and executes it cleanly.
  </action>
  <verify>
    <automated>npm test -- tests/unit/landing-sections.test.js --passWithNoTests 2>&amp;1 | grep -E "(pass|todo|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/landing-sections.test.js` exists
    - `grep "it.todo" tests/unit/landing-sections.test.js` returns &gt;= 16 matches (one per planned assertion)
    - `grep "Phase 47 — FAQSection (OBJ-01 + D-10)" tests/unit/landing-sections.test.js` matches (exact describe)
    - `npm test -- tests/unit/landing-sections.test.js` exits 0 (suite runs, todos reported, no failures)
  </acceptance_criteria>
  <done>
    Smoke test scaffold exists; downstream Plans 02, 03, 04 have addressable `it.todo` slots to convert into real assertions when their components are created.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold public-chat API smoke test + document OBJ-02 audio source</name>
  <files>tests/unit/public-chat-api.test.js, public/audio/README.md</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/api/public-chat/route.js (confirm POST handler shape)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Open Questions #1 — audio sample fallback)
    - ls public/audio/ (confirm demo-intro.mp3 exists)
  </read_first>
  <action>
    PART A — Create `tests/unit/public-chat-api.test.js`. This is a lightweight assertion that the route file exists and exports a POST handler (the full happy-path runs against a live Groq key in E2E; unit tests must not hit the network).

    Exact contents:

    ```js
    /**
     * Phase 47 — /api/public-chat anonymous endpoint smoke test.
     * Confirms the route file exists and exports a POST function.
     * Full request/response integration is covered by live E2E, not this unit test.
     */

    describe('/api/public-chat', () => {
      it('exports a POST handler', async () => {
        const route = await import('../../src/app/api/public-chat/route.js');
        expect(typeof route.POST).toBe('function');
      });
    });
    ```

    PART B — Create `public/audio/README.md` to document the OBJ-02 inline player audio source decision.

    Exact contents:

    ```markdown
    # Landing Page Audio Assets

    ## Files

    - `demo-intro.mp3` — Hero demo intro line (caller opening). Also used by Phase 47 OBJ-02 inline mini-player in PracticalObjectionsGrid (AudioPlayerCard.jsx). Per 47-RESEARCH.md Open Question #1, this file is the approved fallback for the OBJ-02 player until a dedicated short AI voice sample is produced.
    - `demo-mid.mp3` — Hero demo mid section.
    - `demo-outro.mp3` — Hero demo closing section.

    ## Phase 47 — OBJ-02 Player

    The `AudioPlayerCard.jsx` component (Plan 03) uses `/audio/demo-intro.mp3` as its `src`. If a dedicated short sample (~8–12 seconds) is produced later, swap the path.

    The single-play coordination rule (UI-SPEC Pitfall 6): only one audio source per page plays at a time. AudioPlayerCard must pause HeroDemoBlock audio (and vice versa) via the `window.vocoAudioRef` singleton pattern introduced in Plan 03.
    ```

    Do NOT modify any existing mp3 file. Do NOT add new audio binaries in this plan.
  </action>
  <verify>
    <automated>npm test -- tests/unit/public-chat-api.test.js 2>&amp;1 | grep -E "(PASS|pass)" &amp;&amp; test -f public/audio/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/public-chat-api.test.js` exists
    - `npm test -- tests/unit/public-chat-api.test.js` exits 0 (the POST export assertion passes — confirms route file imports cleanly)
    - File `public/audio/README.md` exists and `grep "demo-intro.mp3" public/audio/README.md` matches
    - `grep "vocoAudioRef" public/audio/README.md` matches (Pitfall 6 coordination rule documented for Plan 03)
  </acceptance_criteria>
  <done>
    Public-chat route is test-verified importable; OBJ-02 audio source decision is documented so Plan 03 executor uses `/audio/demo-intro.mp3` without further discovery.
  </done>
</task>

</tasks>

<verification>
- `src/components/ui/accordion.jsx` exists with 4 Radix exports
- `tests/unit/landing-sections.test.js` exists with at least 16 `it.todo` entries across 7 describe blocks
- `tests/unit/public-chat-api.test.js` exists and passes
- `public/audio/README.md` documents `demo-intro.mp3` as OBJ-02 source
- `npm test` passes (no failing suites introduced)
</verification>

<success_criteria>
Downstream plans (02, 03, 04) can import `@/components/ui/accordion`, reference approved audio path `/audio/demo-intro.mp3`, and convert `it.todo` placeholders to real assertions without creating new test infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-01-SUMMARY.md` per summary template.
</output>
