---
phase: 47-landing-objection-busting-repositioning-and-landing-polish
plan: 05
type: execute
wave: 3
depends_on: [47-02, 47-03, 47-04]
files_modified:
  - src/app/(public)/page.js
  - src/app/components/landing/HeroSection.jsx
  - src/app/components/landing/FinalCTASection.jsx
  - .claude/skills/public-site-i18n/SKILL.md
  - tests/unit/landing-sections.test.js
autonomous: false
requirements: [REPOS-01, REPOS-02, POLISH-11, POLISH-12]
must_haves:
  truths:
    - "Visitor sees the landing page in the final Phase 47 order: Hero (new copy) → ScrollLinePath{HowItWorks, FeaturesCarousel, AfterTheCallStrip, SocialProof} → IdentitySection → PracticalObjectionsGrid → OwnerControlPullQuote → FAQSection → FinalCTA (new copy)"
    - "Hero H1 no longer says 'Let Voco Handle Your' (replacement framing) and retains RotatingText"
    - "FinalCTA subtitle contains owner-control language ('your rules' or 'your schedule')"
    - "ScrollLinePath copper SVG wave still renders correctly (AfterTheCallStrip inserted as 3rd child does not break geometry)"
    - "No horizontal overflow at 375px (POLISH-12)"
    - "All new sections animate fade-up on scroll; reduced-motion users see static sections (POLISH-11)"
    - "Chat widget works end-to-end against /api/public-chat (visitor can type, hit send, receive reply)"
    - "public-site-i18n SKILL.md lists the 6 new components"
  artifacts:
    - path: "src/app/(public)/page.js"
      provides: "Landing entry wiring all Phase 47 sections into ScrollLinePath + post-ScrollLinePath order"
      contains: "AfterTheCallStrip"
    - path: "src/app/components/landing/HeroSection.jsx"
      provides: "Hero with REPOS-01 complement framing; RotatingText preserved"
      contains: "RotatingText"
    - path: "src/app/components/landing/FinalCTASection.jsx"
      provides: "Final CTA with REPOS-02 owner-control subtitle"
      contains: "your rules"
    - path: ".claude/skills/public-site-i18n/SKILL.md"
      provides: "Updated skill reflecting Phase 47 component additions"
      contains: "AfterTheCallStrip"
  key_links:
    - from: "src/app/(public)/page.js"
      to: "All 6 new landing component files"
      via: "dynamic() imports with loading skeletons + static imports where appropriate"
      pattern: "AfterTheCallStrip|IdentitySection|PracticalObjectionsGrid|OwnerControlPullQuote|FAQSection"
    - from: "ScrollLinePath children"
      to: "Must be exactly 4 after this plan: HowItWorks, FeaturesCarousel, AfterTheCallStrip, SocialProof"
      via: "JSX children order inside <ScrollLinePath>"
      pattern: "ScrollLinePath"
---

<objective>
Wire all 6 new Phase 47 components into `page.js`, ship REPOS-01 (Hero copy) + REPOS-02 (FinalCTA copy) edits, update the `public-site-i18n` skill to reflect the new file map, and gate the entire phase behind a human visual verification checkpoint.

Purpose: Plans 02, 03, and 04 produced component files but did NOT wire them into `page.js` (to avoid a 3-way file-ownership conflict). This plan is the single consumer of `page.js` — it adds dynamic imports for all new sections, inserts AfterTheCallStrip as the 3rd ScrollLinePath child (Pitfall 2 + 5), and appends the other 4 new sections after `</ScrollLinePath>` in the D-02/D-03/D-04 order. Hero + FinalCTA copy are copy-only edits that live in the same "publish the page" moment. Then a human confirms the whole thing renders correctly.

Output: Fully wired landing page, two copy-only edits, skill update, passing smoke tests, green human checkpoint.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-CONTEXT.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-02-SUMMARY.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-03-SUMMARY.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-04-SUMMARY.md
@src/app/(public)/page.js
@src/app/components/landing/HeroSection.jsx
@src/app/components/landing/FinalCTASection.jsx
@src/app/components/landing/ScrollLinePath.jsx
@.claude/skills/public-site-i18n/SKILL.md

<interfaces>
<!-- New Phase 47 components available to import (from Plans 02/03/04) -->
From src/app/components/landing/AfterTheCallStrip.jsx:
  export function AfterTheCallStrip(): JSX.Element  // Server Component

From src/app/components/landing/IdentitySection.jsx:
  export function IdentitySection(): JSX.Element  // Server Component

From src/app/components/landing/PracticalObjectionsGrid.jsx:
  export function PracticalObjectionsGrid(): JSX.Element  // Server Component wrapping AudioPlayerCard client island

From src/app/components/landing/OwnerControlPullQuote.jsx:
  export function OwnerControlPullQuote(): JSX.Element  // Server Component

From src/app/components/landing/FAQSection.jsx:
  export function FAQSection(): JSX.Element  // 'use client' (Radix Accordion requirement)

<!-- Current page.js post-Phase-47 target order (from 47-RESEARCH.md Architecture Patterns) -->
<ScrollProgress />
<HeroSection />                         ← copy edit REPOS-01
<ScrollLinePath>
  <HowItWorksSection />
  <FeaturesCarousel />
  <AfterTheCallStrip />                 ← NEW (3rd child)
  <SocialProofSection />
</ScrollLinePath>
<IdentitySection />                     ← NEW
<PracticalObjectionsGrid />             ← NEW
<OwnerControlPullQuote />               ← NEW
<FAQSection />                          ← NEW
<FinalCTASection />                     ← copy edit REPOS-02

<!-- Current HeroSection.jsx H1 (MUST be replaced) -->
Line ~54-66 currently:
  <h1 className="text-[2.25rem] md:text-[2.5rem] lg:text-[3rem] font-semibold text-white leading-[1.2] tracking-tight break-words">
    Let Voco Handle Your
    <br />
    <span className="text-[2.75rem] md:text-[3rem] lg:text-[3.75rem]">
      <RotatingText texts={['Phone Calls', 'Bookings', 'Invoices', 'Paperwork']} ... />
    </span>
  </h1>
  <p className="text-base sm:text-lg md:text-xl text-white/60 mt-5 mb-4 max-w-xl leading-relaxed">
    Your AI receptionist answers every call, books jobs, and captures leads — 24/7, in 70+ languages.
  </p>

<!-- Current FinalCTASection subtitle (MUST be replaced) -->
Line ~24-26:
  <p className="text-lg text-[#A1A1AA] mb-10 max-w-md mx-auto leading-relaxed">
    Set up your AI receptionist in 5 minutes. No tech skills needed. No credit card required.
  </p>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REPOS-01 Hero copy + REPOS-02 FinalCTA copy</name>
  <files>src/app/components/landing/HeroSection.jsx, src/app/components/landing/FinalCTASection.jsx, tests/unit/landing-sections.test.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/HeroSection.jsx (lines 1-100 to see current H1 at ~54-66)
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/FinalCTASection.jsx (full file, ~33 lines)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-CONTEXT.md (D-19 + D-20 tone guardrails)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Hero H1 Copy Update section, FinalCTA Copy Update section — locked directions)
  </read_first>
  <behavior>
    - Test 1: HeroSection.jsx H1 does not contain the replacement-framing string "Let Voco Handle Your".
    - Test 2: HeroSection.jsx still imports and uses `RotatingText` (D-19 preservation).
    - Test 3: HeroSection.jsx subtitle contains complement-framing keyword ("when you can't" OR "in charge" OR "stay in control" OR "stay in charge").
    - Test 4: FinalCTASection.jsx subtitle contains owner-control language ("your rules" OR "your schedule").
  </behavior>
  <action>
    PART A — HeroSection.jsx (REPOS-01, copy-only):

    Open `src/app/components/landing/HeroSection.jsx`. Locate the H1 block (around lines 54-66). Replace the H1 text and the first subtitle `<p>` (the one currently saying "Your AI receptionist answers every call, books jobs, and captures leads — 24/7, in 70+ languages.").

    Keep: the `<h1>` element, its className, the `<br />`, the `<span>` with text sizes, the `<RotatingText ... />` component call with ALL props unchanged, the `<p>` container className on the second-paragraph subtitle. The ONLY changes are the text strings.

    New H1 content (replaces "Let Voco Handle Your"):
    ```
    Voco Answers So You Can Keep
    ```

    New subtitle content (replaces "Your AI receptionist answers every call, books jobs, and captures leads — 24/7, in 70+ languages."):
    ```
    Voco picks up when you're on the roof, in a crawlspace, or running on four hours of sleep. You stay in charge of every job — it just makes sure the next call doesn't hang up.
    ```

    Do NOT touch the "Enter your business name and hear it in action." helper text (`<p className="text-xs sm:text-sm text-white/35 mb-8">`). Do NOT touch HeroDemoBlock. Do NOT touch the mobile marquee. Do NOT change the RotatingText `texts` array.

    Result: H1 becomes "Voco Answers So You Can Keep [RotatingText]". The rotating words ['Phone Calls', 'Bookings', 'Invoices', 'Paperwork'] become objects the owner keeps control of — the new H1 parses as "Voco answers [calls] so you can keep your phone calls / bookings / invoices / paperwork" — flipping the REPOS-01 replacement framing into a complement framing while preserving the existing RotatingText element.

    PART B — FinalCTASection.jsx (REPOS-02, copy-only):

    Open `src/app/components/landing/FinalCTASection.jsx`. Replace the subtitle `<p>` text only (around line 24-26). Keep the className, the `<AuthAwareCTA>`, the H2 "Your next emergency call is tonight."

    Replace:
    ```
    Set up your AI receptionist in 5 minutes. No tech skills needed. No credit card required.
    ```

    With:
    ```
    Your rules. Your schedule. Your customers. Voco just makes sure you don't miss the next one. Live in 5 minutes — no credit card.
    ```

    PART C — Convert smoke-test `it.todo` entries in `tests/unit/landing-sections.test.js`:

    Find the `Phase 47 — Hero copy (REPOS-01)` describe block. Replace with:
    ```js
    describe('Phase 47 — Hero copy (REPOS-01)', () => {
      const src = () => require('fs').readFileSync('src/app/components/landing/HeroSection.jsx', 'utf8');
      it('H1 does not contain replacement framing "Let Voco Handle Your"', () => {
        expect(src()).not.toMatch(/Let Voco Handle Your/);
      });
      it('HeroSection retains RotatingText element', () => {
        const s = src();
        expect(s).toMatch(/RotatingText/);
        expect(s).toMatch(/texts=\{\['Phone Calls', 'Bookings', 'Invoices', 'Paperwork'\]\}/);
      });
      it('subtitle uses complement framing language', () => {
        const s = src();
        expect(s).toMatch(/in charge|stay in control|stay in charge|when you can.?t|when you'?re/i);
      });
    });
    ```

    Find the `Phase 47 — FinalCTA copy (REPOS-02)` describe block. Replace with:
    ```js
    describe('Phase 47 — FinalCTA copy (REPOS-02)', () => {
      it('FinalCTA subtitle contains owner-control language', () => {
        const src = require('fs').readFileSync('src/app/components/landing/FinalCTASection.jsx', 'utf8');
        expect(src).toMatch(/your rules|your schedule/i);
      });
    });
    ```
  </action>
  <verify>
    <automated>npm test -- tests/unit/landing-sections.test.js -t "Hero copy|FinalCTA copy" 2>&amp;1 | grep -E "(PASS|pass|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Let Voco Handle Your" src/app/components/landing/HeroSection.jsx` returns NO match
    - `grep "Voco Answers So You Can Keep" src/app/components/landing/HeroSection.jsx` matches (new H1)
    - `grep "RotatingText" src/app/components/landing/HeroSection.jsx` matches (element preserved per D-19)
    - `grep "Phone Calls.*Bookings.*Invoices.*Paperwork" src/app/components/landing/HeroSection.jsx` matches (texts array unchanged)
    - `grep -Ei "in charge|stay in control|when you'?re" src/app/components/landing/HeroSection.jsx` matches (complement framing)
    - `grep -Ei "your rules|your schedule" src/app/components/landing/FinalCTASection.jsx` matches (owner-control language)
    - `grep "Set up your AI receptionist in 5 minutes. No tech skills needed. No credit card required." src/app/components/landing/FinalCTASection.jsx` returns NO match (old copy gone)
    - All smoke tests for Hero copy + FinalCTA copy pass
  </acceptance_criteria>
  <done>
    REPOS-01 and REPOS-02 copy edits shipped. HeroSection preserves RotatingText per D-19; FinalCTASection subtitle lands on owner-control framing per D-20.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire all 6 new sections into page.js (correct ScrollLinePath + post-ScrollLinePath order)</name>
  <files>src/app/(public)/page.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/(public)/page.js (current 93 lines — full file)
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/ScrollLinePath.jsx (confirm children handling — it maps over React.Children)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md (Route Order in page.js — post-phase)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Pattern 1: Dynamic Import with Loading Skeleton — copy the pattern verbatim)
  </read_first>
  <action>
    Rewrite `src/app/(public)/page.js` to add 5 new dynamic imports (AfterTheCallStrip, IdentitySection, PracticalObjectionsGrid, OwnerControlPullQuote, FAQSection) and update the JSX tree to match the UI-SPEC post-phase order.

    Full new file contents:

    ```jsx
    import { HeroSection } from '@/app/components/landing/HeroSection';
    import { ScrollProgress } from '@/app/components/landing/ScrollProgress';
    import { ScrollLinePath } from '@/app/components/landing/ScrollLinePath';
    import dynamic from 'next/dynamic';

    // Above-the-fold: HeroSection is statically imported for best LCP.
    // Below-the-fold: dynamically imported with loading skeletons to reduce initial JS bundle
    // and prevent CLS via explicit height reservations.

    const HowItWorksSection = dynamic(
      () => import('@/app/components/landing/HowItWorksSection').then((m) => m.HowItWorksSection),
      {
        loading: () => (
          <section className="bg-white" aria-hidden="true">
            <div className="pt-24 pb-8 text-center px-6">
              <div className="h-4 w-24 bg-black/10 rounded mx-auto mb-3" />
              <div className="h-10 w-80 bg-black/10 rounded mx-auto" />
            </div>
            {[1, 2, 3, 4].map((i) => (
              <div key={i} className={`min-h-screen ${i % 2 === 0 ? 'bg-[#FAFAF9]' : 'bg-white'}`} />
            ))}
          </section>
        ),
      }
    );

    const FeaturesCarousel = dynamic(
      () => import('@/app/components/landing/FeaturesCarousel').then((m) => m.FeaturesCarousel),
      {
        loading: () => (
          <section className="bg-[#FAFAF9] py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-5xl mx-auto">
              <div className="text-center mb-16">
                <div className="h-4 w-32 bg-black/10 rounded mx-auto mb-3" />
                <div className="h-10 w-96 bg-black/10 rounded mx-auto" />
              </div>
              <div className="h-[480px] rounded-2xl bg-white border border-stone-200/60 shadow-sm" />
              <div className="mt-6 h-16 rounded-xl bg-white/60" />
            </div>
          </section>
        ),
      }
    );

    const AfterTheCallStrip = dynamic(
      () => import('@/app/components/landing/AfterTheCallStrip').then((m) => m.AfterTheCallStrip),
      {
        loading: () => (
          <section className="bg-white py-12 md:py-16 px-6" aria-hidden="true">
            <div className="max-w-5xl mx-auto">
              <div className="text-center mb-12">
                <div className="h-4 w-28 bg-black/10 rounded mx-auto mb-3" />
                <div className="h-8 w-96 bg-black/10 rounded mx-auto" />
              </div>
              <div className="grid grid-cols-1 md:grid-cols-5 gap-8 md:gap-6">
                {[1, 2, 3, 4, 5].map((i) => (
                  <div key={i} className="h-24 rounded bg-black/5" />
                ))}
              </div>
            </div>
          </section>
        ),
      }
    );

    const SocialProofSection = dynamic(
      () => import('@/app/components/landing/SocialProofSection').then((m) => m.SocialProofSection),
      {
        loading: () => (
          <section className="bg-[#F5F5F4] py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-5xl mx-auto">
              <div className="text-center mb-16">
                <div className="h-4 w-24 bg-black/10 rounded mx-auto mb-3" />
                <div className="h-10 w-64 bg-black/10 rounded mx-auto" />
              </div>
              <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
                {[1, 2, 3].map((i) => (
                  <div key={i} className="h-64 rounded-2xl bg-white border border-stone-200/60" />
                ))}
              </div>
            </div>
          </section>
        ),
      }
    );

    const IdentitySection = dynamic(
      () => import('@/app/components/landing/IdentitySection').then((m) => m.IdentitySection),
      {
        loading: () => (
          <section className="bg-white py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-[720px] mx-auto text-center">
              <div className="h-4 w-32 bg-black/10 rounded mx-auto mb-3" />
              <div className="h-8 w-80 bg-black/10 rounded mx-auto mb-8" />
              <div className="h-20 w-full bg-black/5 rounded" />
            </div>
          </section>
        ),
      }
    );

    const PracticalObjectionsGrid = dynamic(
      () => import('@/app/components/landing/PracticalObjectionsGrid').then((m) => m.PracticalObjectionsGrid),
      {
        loading: () => (
          <section className="bg-[#FAFAF9] py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-5xl mx-auto">
              <div className="text-center mb-12">
                <div className="h-4 w-28 bg-black/10 rounded mx-auto mb-3" />
                <div className="h-10 w-80 bg-black/10 rounded mx-auto" />
              </div>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                {[1, 2, 3, 4, 5, 6].map((i) => (
                  <div key={i} className="h-80 rounded-2xl bg-white border border-stone-200/60 shadow-sm" />
                ))}
              </div>
            </div>
          </section>
        ),
      }
    );

    const OwnerControlPullQuote = dynamic(
      () => import('@/app/components/landing/OwnerControlPullQuote').then((m) => m.OwnerControlPullQuote),
      {
        loading: () => (
          <section className="bg-[#1C1412] py-20 md:py-24 px-6" aria-hidden="true">
            <div className="max-w-2xl mx-auto text-center">
              <div className="h-8 w-96 bg-white/[0.06] rounded mx-auto mb-4" />
              <div className="h-4 w-32 bg-white/[0.04] rounded mx-auto" />
            </div>
          </section>
        ),
      }
    );

    const FAQSection = dynamic(
      () => import('@/app/components/landing/FAQSection').then((m) => m.FAQSection),
      {
        loading: () => (
          <section className="bg-white py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-5xl mx-auto">
              <div className="text-center mb-12">
                <div className="h-4 w-24 bg-black/10 rounded mx-auto mb-3" />
                <div className="h-10 w-80 bg-black/10 rounded mx-auto" />
              </div>
              <div className="grid grid-cols-1 lg:grid-cols-[3fr_2fr] gap-12">
                <div className="space-y-4">
                  {[1, 2, 3, 4, 5, 6, 7].map((i) => (
                    <div key={i} className="h-14 bg-black/5 rounded-lg" />
                  ))}
                </div>
                <div className="h-[400px] rounded-2xl bg-black/5" />
              </div>
            </div>
          </section>
        ),
      }
    );

    const FinalCTASection = dynamic(
      () => import('@/app/components/landing/FinalCTASection').then((m) => m.FinalCTASection),
      {
        loading: () => (
          <section className="bg-[#1C1412] py-20 md:py-28 px-6" aria-hidden="true">
            <div className="max-w-3xl mx-auto text-center">
              <div className="h-10 w-80 bg-white/[0.06] rounded mx-auto mb-4" />
              <div className="h-6 w-64 bg-white/[0.04] rounded mx-auto" />
            </div>
          </section>
        ),
      }
    );

    export default function HomePage() {
      return (
        <>
          <ScrollProgress />
          <HeroSection />
          <ScrollLinePath>
            <HowItWorksSection />
            <FeaturesCarousel />
            <AfterTheCallStrip />
            <SocialProofSection />
          </ScrollLinePath>
          <IdentitySection />
          <PracticalObjectionsGrid />
          <OwnerControlPullQuote />
          <FAQSection />
          <FinalCTASection />
        </>
      );
    }
    ```

    Wiring rules (locked by RESEARCH.md + UI-SPEC + CONTEXT.md D-02/D-03/D-04/D-05):
    - ScrollLinePath has EXACTLY 4 children in this order: HowItWorksSection, FeaturesCarousel, AfterTheCallStrip, SocialProofSection. Never more, never reorder.
    - After `</ScrollLinePath>`: IdentitySection → PracticalObjectionsGrid → OwnerControlPullQuote → FAQSection → FinalCTASection. Exactly this order.
    - Every new section uses `dynamic(... { loading: <skeleton> })` following Pattern 1 from RESEARCH.md (matches existing HowItWorks/FeaturesCarousel/SocialProof pattern).
    - Loading skeleton backgrounds match final section backgrounds (prevents CLS, matches background rhythm).
    - HeroSection and ScrollProgress stay statically imported for LCP.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -40 &amp;&amp; grep -c "AfterTheCallStrip\|IdentitySection\|PracticalObjectionsGrid\|OwnerControlPullQuote\|FAQSection" src/app/\(public\)/page.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AfterTheCallStrip" src/app/(public)/page.js` matches (import + dynamic + JSX)
    - `grep "IdentitySection" src/app/(public)/page.js` matches
    - `grep "PracticalObjectionsGrid" src/app/(public)/page.js` matches
    - `grep "OwnerControlPullQuote" src/app/(public)/page.js` matches
    - `grep "FAQSection" src/app/(public)/page.js` matches
    - Order verification: inside `export default function HomePage`, the JSX order is: HeroSection → (ScrollLinePath open) → HowItWorksSection → FeaturesCarousel → AfterTheCallStrip → SocialProofSection → (ScrollLinePath close) → IdentitySection → PracticalObjectionsGrid → OwnerControlPullQuote → FAQSection → FinalCTASection
    - `grep -A 8 "return (" src/app/\(public\)/page.js | grep -A 6 "ScrollLinePath>"` shows HowItWorks, FeaturesCarousel, AfterTheCallStrip, SocialProofSection between the two `ScrollLinePath` tags (exactly 4 children)
    - `npm run build` exits 0 (Next.js build succeeds — no missing imports, no TS errors if tsc is configured)
    - `npm test -- tests/unit/landing-sections.test.js` still passes (no regressions)
  </acceptance_criteria>
  <done>
    page.js wires all 6 new sections in the correct order. ScrollLinePath has exactly 4 children with AfterTheCallStrip inserted as the 3rd. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update public-site-i18n skill to reflect new components</name>
  <files>.claude/skills/public-site-i18n/SKILL.md</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/public-site-i18n/SKILL.md (current file — locate the "Landing page components" / file map section)
    - C:/Users/leheh/.Projects/homeservice_agent/CLAUDE.md ("Keep skills in sync" rule)
  </read_first>
  <action>
    Open `.claude/skills/public-site-i18n/SKILL.md`. Locate the section that lists landing page section components (typically under "Landing Section Components" or a similar heading — match the existing structure).

    Add the 6 new Phase 47 components to that list with 1-line descriptions:

    ```markdown
    - `AfterTheCallStrip.jsx` — Post-call workflow extension strip (5 items: CRM lead, SMS/email, calendar sync, recurring slot, analytics). 3rd child of ScrollLinePath. Server Component.
    - `IdentitySection.jsx` — OBJ-06 identity/change-aversion emotional section. Server Component.
    - `PracticalObjectionsGrid.jsx` — OBJ-02/03/04/05/08/09 consolidated 6-card grid. Server Component outer; embeds AudioPlayerCard client island for OBJ-02.
    - `AudioPlayerCard.jsx` — OBJ-02 inline mini-player. 'use client'. Coordinates via `window.__vocoPlayingAudio` singleton (pause-other rule).
    - `OwnerControlPullQuote.jsx` — REPOS-04 dark-section owner-control pull-quote. Server Component.
    - `FAQSection.jsx` — OBJ-01 + D-10 FAQ accordion (7 Q&A) + chat panel grid. 'use client' (Radix Accordion requirement); embeds FAQChatWidget.
    - `FAQChatWidget.jsx` — 'use client' chat island posting to /api/public-chat. History capped at 10 entries client-side.
    ```

    Add a brief note in the ScrollLinePath-related section (if present) or in the constraints section:

    ```markdown
    **ScrollLinePath children (post-Phase-47):** exactly 4 — HowItWorksSection, FeaturesCarousel, AfterTheCallStrip, SocialProofSection. AfterTheCallStrip must NOT use `id="features"` or `id="testimonials"` (ScrollLinePath anchor points) and must be a Server Component (renders synchronously before ScrollLinePath's 100ms/1000ms re-measure).
    ```

    Update any "last reviewed" date marker at the top of the skill if one exists (set to today's date — 2026-04-13). Do not rewrite sections unrelated to Phase 47.
  </action>
  <verify>
    <automated>grep -c "AfterTheCallStrip\|IdentitySection\|PracticalObjectionsGrid\|OwnerControlPullQuote\|FAQSection\|FAQChatWidget\|AudioPlayerCard" .claude/skills/public-site-i18n/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AfterTheCallStrip" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "IdentitySection" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "PracticalObjectionsGrid" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "OwnerControlPullQuote" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "FAQSection" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "FAQChatWidget" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "AudioPlayerCard" .claude/skills/public-site-i18n/SKILL.md` matches
    - `grep "ScrollLinePath" .claude/skills/public-site-i18n/SKILL.md` returns at least 1 match (constraint note present)
  </acceptance_criteria>
  <done>
    Skill file is in sync per CLAUDE.md rule — all new Phase 47 landing components are documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification of full Phase 47 landing page</name>
  <files>(none — interactive verification only)</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md (Section Layout Contracts + Route Order + Background rhythm table)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-CONTEXT.md (all D- decisions)
  </read_first>
  <action>
    **Checkpoint — human visual verification of the full Phase 47 landing page.**

    What was built (Plans 01–04 + Tasks 1–3 of this plan): 6 new landing components wired into page.js, Hero + FinalCTA copy updated, ScrollLinePath geometry preserved with AfterTheCallStrip as 3rd child, chat widget powered by existing /api/public-chat, skill file updated.

    **Steps for the human to perform:**

    1. Start the dev server: `npm run dev` (or confirm it is already running).
    2. Open `http://localhost:3000/` in Chrome/Safari.

    **Desktop visual flow (1440px viewport):**
    - [ ] Hero: H1 reads "Voco Answers So You Can Keep [rotating word]" — NOT "Let Voco Handle Your". RotatingText cycles through Phone Calls / Bookings / Invoices / Paperwork.
    - [ ] Hero subtitle reads "Voco picks up when you're on the roof..." with "stay in charge" in the copy.
    - [ ] Scroll down: HowItWorksSection, then FeaturesCarousel, then AfterTheCallStrip (5 icons in a row: UserPlus / MessageSquare / CalendarCheck / Repeat / BarChart3 — "CRM lead created", "SMS + email sent", etc.), then SocialProofSection. All four are inside the ScrollLinePath copper wave.
    - [ ] ScrollLinePath integrity: copper sine wave still renders; dots at Features and Testimonials anchors still align.
    - [ ] After SocialProof: IdentitySection ("Your business. Your voice. Your rules." centered, 3 paragraphs, white background).
    - [ ] PracticalObjectionsGrid (bg-[#FAFAF9]) — 6 cards in 2-col grid. Each card has hover effect (orange tint, -translate-y-1).
    - [ ] OBJ-02 card: click play button. Audio plays. If hero demo was playing, it pauses. Aria label flips to "Pause audio sample".
    - [ ] OBJ-03 card: "$260,400/year lost" visible; stat chip shows "Voco starts at $99/mo..."
    - [ ] OBJ-04 card: 3 numbered steps (1 Forward, 2 Hours, 3 Live) with arrows between on desktop.
    - [ ] OBJ-05 card: 3 checkmark lines about escalation / recording / rules.
    - [ ] OBJ-08 card: Before vs After comparison (2-col grid inside card).
    - [ ] OBJ-09 card: 5 trade icons + one-line italic capability note.
    - [ ] OwnerControlPullQuote (bg-[#1C1412] dark) — "You set the rules. Voco follows them." centered.
    - [ ] FAQSection (bg-white) — 2-column grid on desktop: 7-item accordion left, chat widget right. Accordion items expand/collapse; only one open at a time; animation is smooth.
    - [ ] Chat widget: 3 suggestion chips visible ("Does it really sound natural?" / "How long does setup take?" / "What does it cost?"). Type a question, hit send, receive a Groq-generated reply. Error gracefully if rate limit hit.
    - [ ] FinalCTASection subtitle reads "Your rules. Your schedule. Your customers..." — contains "Your rules" or "Your schedule".
    - [ ] Background rhythm: no two same-colored sections adjacent except the intentional dark-dark pair (OwnerControlPullQuote + FinalCTA).

    **Mobile flow (375px viewport — Chrome DevTools):**
    - [ ] No horizontal scroll at any point on the page.
    - [ ] AfterTheCallStrip: 5 items stack vertically (single column).
    - [ ] PracticalObjectionsGrid: 6 cards stack vertically (single column).
    - [ ] OBJ-04 setup strip: 3 steps stack vertically with no arrows.
    - [ ] FAQSection: accordion on top, chat widget below (single column).
    - [ ] All copy wraps cleanly; no text overflow.

    **Reduced-motion check** (macOS: System Settings → Accessibility → Display → Reduce motion; Windows: Settings → Accessibility → Visual effects → Animation effects OFF):
    - [ ] Sections do NOT fade up on scroll — they appear statically.
    - [ ] RotatingText in hero still works (motion library choice for that widget).
    - [ ] Accordion expand/collapse still animates (Radix manages this; may still play a brief transition — acceptable).

    **Build + tests:**
    - [ ] `npm run build` — no errors, no warnings about missing imports.
    - [ ] `npm test` — full Jest suite passes (Phase 47 smoke tests all green).

    **Console check:**
    - [ ] No React errors (hydration mismatch, key warnings).
    - [ ] No 404s in Network tab for `/audio/demo-intro.mp3` or component chunks.

    Resume signal: if all checks pass, reply **"approved"**. If any fail, list each failing item as `FAIL: <what broke>` so a gap-closure plan can address them.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — requires human reply 'approved' or 'FAIL: ...' items before phase proceeds."</automated>
  </verify>
  <acceptance_criteria>
    - Human has exercised all desktop visual flow items on `/` at 1440px
    - Human has exercised all mobile flow items at 375px DevTools viewport
    - Human has toggled OS reduced-motion and confirmed AnimatedSection compliance
    - `npm run build` exits 0 on the human's machine
    - `npm test` exits 0 on the human's machine
    - Human has replied with either "approved" OR a numbered list of `FAIL: <issue>` items
  </acceptance_criteria>
  <done>
    Human returned "approved". Phase 47 ships. If instead any `FAIL:` items were returned, Phase 47 requires gap-closure planning via `/gsd:plan-phase 47 --gaps` before shipping.
  </done>
</task>

</tasks>

<verification>
Phase 47 is complete when:
- `npm run build` succeeds
- `npm test` is green
- Human verification (Task 4) returns "approved"
- All 14 in-scope requirements (OBJ-01/02/03/04/05/06/08/09, REPOS-01/02/03/04, POLISH-11/12) are visibly satisfied on the rendered landing page
</verification>

<success_criteria>
Visitor loading `/` sees the full Phase 47 landing: complement-framed hero, copper-wave ScrollLinePath with 4 children (AfterTheCallStrip inserted correctly), identity section, practical objections grid with working audio, owner-control pull-quote, FAQ with chat widget, owner-control FinalCTA. Phase ships.
</success_criteria>

<output>
After completion, create `.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-05-SUMMARY.md` per summary template.
</output>
