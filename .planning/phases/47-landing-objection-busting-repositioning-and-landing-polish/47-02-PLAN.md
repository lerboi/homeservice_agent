---
phase: 47-landing-objection-busting-repositioning-and-landing-polish
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - src/app/components/landing/AfterTheCallStrip.jsx
  - src/app/components/landing/IdentitySection.jsx
  - src/app/components/landing/OwnerControlPullQuote.jsx
  - tests/unit/landing-sections.test.js
autonomous: true
requirements: [REPOS-03, OBJ-06, REPOS-04, POLISH-11, POLISH-12]
must_haves:
  truths:
    - "Visitor sees a 5-item 'what Voco keeps doing after the call' strip between FeaturesCarousel and SocialProofSection (REPOS-03)"
    - "Visitor sees an identity/change-aversion section with complement framing (OBJ-06)"
    - "Visitor sees an owner-control pull-quote on dark background between SocialProof and FAQ (REPOS-04)"
    - "All three new sections animate via AnimatedSection (useReducedMotion compliant)"
    - "All three sections stack to single column at 375px, use grid/row at md+"
  artifacts:
    - path: "src/app/components/landing/AfterTheCallStrip.jsx"
      provides: "Post-call workflow strip (REPOS-03); Server Component; AnimatedSection wrapper"
      contains: "export function AfterTheCallStrip"
    - path: "src/app/components/landing/IdentitySection.jsx"
      provides: "Identity/change-aversion emotional section (OBJ-06); Server Component"
      contains: "export function IdentitySection"
    - path: "src/app/components/landing/OwnerControlPullQuote.jsx"
      provides: "Dark pull-quote section (REPOS-04); Server Component"
      contains: "export function OwnerControlPullQuote"
  key_links:
    - from: "src/app/components/landing/AfterTheCallStrip.jsx"
      to: "src/app/components/landing/AnimatedSection.jsx"
      via: "import { AnimatedSection } from './AnimatedSection'"
      pattern: "from './AnimatedSection'"
    - from: "AfterTheCallStrip section element"
      to: "ScrollLinePath measurement anchors"
      via: "MUST NOT use id='features' or id='testimonials' (Pitfall 2)"
      pattern: "id=\"after-call\""
---

<objective>
Build three autonomous landing sections that compose side-by-side with no cross-file dependencies: `AfterTheCallStrip` (REPOS-03), `IdentitySection` (OBJ-06), `OwnerControlPullQuote` (REPOS-04).

Purpose: Phase 47 needs 6 new sections. This plan builds the 3 that are pure static composition — no interactive widgets, no audio, no Radix primitives. These are Server Components, fast to render, and isolate from Plan 03 (PracticalObjectionsGrid with audio) and Plan 04 (FAQSection with chat). All three wrap AnimatedSection (POLISH-11) and follow the 375px responsive contract (POLISH-12).

Output: Three new `.jsx` component files in `src/app/components/landing/`, plus converted smoke-test assertions in `tests/unit/landing-sections.test.js`.

These components are CREATED here but NOT YET wired into `page.js` — Plan 05 handles page.js wiring to prevent file-ownership conflicts with Plans 03 and 04.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-CONTEXT.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md
@.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md
@src/app/components/landing/SocialProofSection.jsx
@src/app/components/landing/AnimatedSection.jsx
@src/app/components/landing/FinalCTASection.jsx
@src/app/components/landing/ScrollLinePath.jsx

<interfaces>
<!-- AnimatedSection component exports (verified from codebase) -->
From src/app/components/landing/AnimatedSection.jsx:
```jsx
export function AnimatedSection({ children, delay?, direction? }) // fade-up wrapper, useReducedMotion built-in
export function AnimatedStagger({ children, className? }) // stagger container
export function AnimatedItem({ children }) // staggered child
```

<!-- ScrollLinePath constraint (CRITICAL - Pitfall 2, Pitfall 5) -->
AfterTheCallStrip will become the 3rd ScrollLinePath child (between FeaturesCarousel and SocialProofSection) in Plan 05.
- MUST NOT use id="features" or id="testimonials" on <section> element
- MUST be a Server Component (no 'use client') so it renders synchronously before ScrollLinePath's 100ms/1000ms re-measure
- MUST NOT use `relative` on the <section> element — only on inner `<div>` (z-[1])

<!-- Existing eyebrow + H2 pattern (copy verbatim from SocialProofSection) -->
<p className="text-[14px] font-semibold text-[#F97316] tracking-wide uppercase mb-3">{eyebrow}</p>
<h2 className="text-3xl md:text-[2.25rem] font-semibold text-[#0F172A] leading-tight tracking-tight">{heading}</h2>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AfterTheCallStrip (REPOS-03) — 5 post-call workflow items inside ScrollLinePath</name>
  <files>src/app/components/landing/AfterTheCallStrip.jsx, tests/unit/landing-sections.test.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/SocialProofSection.jsx (eyebrow + H2 pattern to mirror)
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/AnimatedSection.jsx (confirm exports)
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/ScrollLinePath.jsx (confirm #features / #testimonials anchors — do NOT use those IDs)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md (After-the-Call Strip section — exact spec)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Pitfall 2, Pitfall 5, Pitfall 8)
    - tests/unit/landing-sections.test.js (locate the `AfterTheCallStrip (REPOS-03)` describe block with `it.todo` entries from Plan 01)
  </read_first>
  <behavior>
    - Test 1: Component renders at least 4 workflow items (planner selected 5 per D-05).
    - Test 2: Section element does NOT carry id="features" or id="testimonials".
    - Test 3: Component exports the named function `AfterTheCallStrip`.
  </behavior>
  <action>
    Create `src/app/components/landing/AfterTheCallStrip.jsx` as a Server Component (NO 'use client').

    Full file contents:

    ```jsx
    import { AnimatedSection, AnimatedStagger, AnimatedItem } from './AnimatedSection';
    import { UserPlus, MessageSquare, CalendarCheck, Repeat, BarChart3 } from 'lucide-react';

    const ITEMS = [
      {
        icon: UserPlus,
        label: 'CRM lead created',
        desc: 'Caller details, job type, and urgency land in your pipeline the moment the call ends.',
      },
      {
        icon: MessageSquare,
        label: 'SMS + email sent',
        desc: 'You get notified instantly. The caller gets a confirmation text with their booking.',
      },
      {
        icon: CalendarCheck,
        label: 'Calendar synced',
        desc: 'Google or Outlook updated in under 60 seconds so your next job never collides.',
      },
      {
        icon: Repeat,
        label: 'Recurring slot reserved',
        desc: 'Maintenance contracts auto-spawn future appointments without you lifting a finger.',
      },
      {
        icon: BarChart3,
        label: 'Analytics updated',
        desc: 'Revenue, conversion, and missed-call metrics refresh live on your dashboard.',
      },
    ];

    export function AfterTheCallStrip() {
      return (
        <section id="after-call" className="bg-white py-12 md:py-16 px-6">
          <div className="relative z-[1] max-w-5xl mx-auto">
            <AnimatedSection>
              <div className="text-center mb-12">
                <p className="text-[14px] font-semibold text-[#F97316] tracking-wide uppercase mb-3">
                  Full-stack workflow
                </p>
                <h2 className="text-3xl md:text-[2.25rem] font-semibold text-[#0F172A] leading-tight tracking-tight">
                  What Voco keeps doing after the call ends
                </h2>
              </div>
            </AnimatedSection>
            <AnimatedStagger className="grid grid-cols-1 md:grid-cols-5 gap-8 md:gap-6">
              {ITEMS.map(({ icon: Icon, label, desc }) => (
                <AnimatedItem key={label}>
                  <div className="flex flex-col items-center text-center gap-3">
                    <Icon className="w-6 h-6 text-[#F97316]" strokeWidth={2} aria-hidden="true" />
                    <p className="text-[14px] font-semibold text-[#0F172A]">{label}</p>
                    <p className="text-[14px] text-[#475569] leading-relaxed">{desc}</p>
                  </div>
                </AnimatedItem>
              ))}
            </AnimatedStagger>
          </div>
        </section>
      );
    }
    ```

    Key rules (verbatim from UI-SPEC + RESEARCH.md):
    - `id="after-call"` — NEVER `id="features"` or `id="testimonials"` (Pitfall 2).
    - NO `'use client'` directive — Server Component so it is in the DOM before ScrollLinePath measures (Pitfall 5 / Pattern 7).
    - NO `relative` on `<section>`. Only on the inner `<div>` with `z-[1]` to stack above the SVG (Pitfall 5).
    - Icons: 24px (`w-6 h-6`), color `text-[#F97316]`.
    - Labels: 14px semibold, color `text-[#0F172A]`.
    - Descriptors: 14px weight 400, color `text-[#475569]`.
    - Background: `bg-white`.
    - Padding: `py-12 md:py-16 px-6` (matches UI-SPEC After-the-Call Strip spec).
    - Layout: `grid grid-cols-1 md:grid-cols-5 gap-8 md:gap-6` (stack mobile, 5-col md+).

    Then POPULATE the `Phase 47 — AfterTheCallStrip (REPOS-03)` describe block in `tests/unit/landing-sections.test.js`. Replace the `it.todo` lines with:

    ```js
    describe('Phase 47 — AfterTheCallStrip (REPOS-03)', () => {
      it('renders at least 4 after-the-call workflow items', async () => {
        const src = require('fs').readFileSync('src/app/components/landing/AfterTheCallStrip.jsx', 'utf8');
        const matches = src.match(/label:\s*'[^']+'/g) || [];
        expect(matches.length).toBeGreaterThanOrEqual(4);
      });
      it('does not use id="features" or id="testimonials" on its section element', () => {
        const src = require('fs').readFileSync('src/app/components/landing/AfterTheCallStrip.jsx', 'utf8');
        expect(src).not.toMatch(/id=["']features["']/);
        expect(src).not.toMatch(/id=["']testimonials["']/);
      });
    });
    ```

    Run `npm test -- tests/unit/landing-sections.test.js` and confirm both assertions pass.
  </action>
  <verify>
    <automated>npm test -- tests/unit/landing-sections.test.js -t "AfterTheCallStrip" 2>&amp;1 | grep -E "(PASS|pass|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/components/landing/AfterTheCallStrip.jsx` exists
    - `grep "export function AfterTheCallStrip" src/app/components/landing/AfterTheCallStrip.jsx` matches
    - `grep "'use client'" src/app/components/landing/AfterTheCallStrip.jsx` returns NO match (must be Server Component)
    - `grep -E "id=[\"']features[\"']" src/app/components/landing/AfterTheCallStrip.jsx` returns NO match
    - `grep -E "id=[\"']testimonials[\"']" src/app/components/landing/AfterTheCallStrip.jsx` returns NO match
    - `grep "id=\"after-call\"" src/app/components/landing/AfterTheCallStrip.jsx` matches
    - `grep -c "label:" src/app/components/landing/AfterTheCallStrip.jsx` returns &gt;= 5
    - Test assertions under "AfterTheCallStrip (REPOS-03)" describe block all pass (no `it.todo` remaining in that block)
  </acceptance_criteria>
  <done>
    REPOS-03 component exists as a Server Component with 5 post-call workflow items, safe to insert inside ScrollLinePath, uses neutral section id, AnimatedSection + AnimatedStagger wrapping. Smoke tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IdentitySection (OBJ-06) — emotional complement framing</name>
  <files>src/app/components/landing/IdentitySection.jsx, tests/unit/landing-sections.test.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md (OBJ-06 Identity Section section)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (Pitfall 4: Defensive Copy)
    - C:/Users/leheh/.Projects/homeservice_agent/My Prompts/PROBLEMS.md (identity/change-aversion objection — primary source for tone)
  </read_first>
  <behavior>
    - Test 1: Component exports named function `IdentitySection`.
    - Test 2: Copy does NOT contain replacement/defensive openers ("worried", "don't worry", "replace you", "replacing", "replaces you").
    - Test 3: Copy DOES contain complement framing (at least one of: "stays", "still you", "in charge", "control", "you're").
  </behavior>
  <action>
    Create `src/app/components/landing/IdentitySection.jsx` as a Server Component.

    Full file contents:

    ```jsx
    import { AnimatedSection } from './AnimatedSection';

    export function IdentitySection() {
      return (
        <section className="bg-white py-20 md:py-28 px-6">
          <div className="max-w-[720px] mx-auto text-center">
            <AnimatedSection>
              <p className="text-[14px] font-semibold text-[#F97316] tracking-wide uppercase mb-3">
                You're still the boss
              </p>
              <h2 className="text-3xl md:text-[2.25rem] font-semibold text-[#0F172A] leading-tight tracking-tight mb-8">
                Your business. Your voice. Your rules.
              </h2>
              <div className="flex flex-col gap-6 text-[15px] text-[#475569] leading-relaxed">
                <p>
                  Voco doesn't show up on your truck. It doesn't meet your customers. It doesn't know your neighborhood the way you do.
                </p>
                <p>
                  What it does is pick up the phone when you're on the roof, in a crawlspace, or finally asleep after a 14-hour day — and it answers the way you told it to.
                </p>
                <p>
                  Every job you earned is still yours. Voco just makes sure the next one doesn't hang up before you can answer.
                </p>
              </div>
            </AnimatedSection>
          </div>
        </section>
      );
    }
    ```

    Copy guardrails (D-19 / D-21 / Pitfall 4):
    - NO defensive framing: never mention "worry", "replace", "replacing", "afraid", "concerned".
    - Lead with what Voco is NOT (first paragraph) — disarms identity threat directly.
    - Reinforce owner identity throughout — "your", "you", "you're".
    - Complement language: "when you're [busy with real work]" — Voco fills gaps, doesn't take over.

    Layout rules (UI-SPEC OBJ-06 Identity Section):
    - `bg-white`, `py-20 md:py-28 px-6`
    - `max-w-[720px] mx-auto text-center`
    - Eyebrow pill + H2 + 2-3 paragraph stack
    - Body text: `text-[15px] text-[#475569] leading-relaxed`
    - NO optional supporting visual for this draft (planner discretion per UI-SPEC — keeping content-only is cleanest)

    Then POPULATE the `Phase 47 — IdentitySection (OBJ-06)` describe block in `tests/unit/landing-sections.test.js`. Replace with:

    ```js
    describe('Phase 47 — IdentitySection (OBJ-06)', () => {
      it('renders complement-framing copy (no replacement language)', () => {
        const src = require('fs').readFileSync('src/app/components/landing/IdentitySection.jsx', 'utf8');
        // Anti-patterns (defensive / replacement language)
        expect(src).not.toMatch(/worried/i);
        expect(src).not.toMatch(/don'?t worry/i);
        expect(src).not.toMatch(/replace(s|d|r|ment)?\s+you/i);
        expect(src).not.toMatch(/replacing you/i);
        // Positive framing required
        expect(src).toMatch(/your|you'?re|still/i);
      });
    });
    ```
  </action>
  <verify>
    <automated>npm test -- tests/unit/landing-sections.test.js -t "IdentitySection" 2>&amp;1 | grep -E "(PASS|pass|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/components/landing/IdentitySection.jsx` exists
    - `grep "export function IdentitySection" src/app/components/landing/IdentitySection.jsx` matches
    - `grep "'use client'" src/app/components/landing/IdentitySection.jsx` returns NO match
    - `grep -Ei "worried|don'?t worry|replace.+you|replacing you" src/app/components/landing/IdentitySection.jsx` returns NO match
    - `grep -Ei "your|you'?re|still" src/app/components/landing/IdentitySection.jsx` matches at least 3 times
    - `grep "bg-white" src/app/components/landing/IdentitySection.jsx` matches (background rhythm — bg-white between bg-[#F5F5F4] SocialProof and bg-[#FAFAF9] PracticalObjectionsGrid)
    - Smoke test assertion for IdentitySection passes
  </acceptance_criteria>
  <done>
    OBJ-06 section exists with complement-framing copy that passes the anti-defensive-language lint, uses warm-neutral white background between SocialProof and PracticalObjectionsGrid.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OwnerControlPullQuote (REPOS-04) — dark pull-quote section</name>
  <files>src/app/components/landing/OwnerControlPullQuote.jsx, tests/unit/landing-sections.test.js</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-UI-SPEC.md (REPOS-04 Owner-Control Pull-Quote section)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-RESEARCH.md (REPOS-04 Pull-Quote Section code example)
    - C:/Users/leheh/.Projects/homeservice_agent/src/app/components/landing/FinalCTASection.jsx (dark-section radial-glow pattern to mirror)
  </read_first>
  <behavior>
    - Test 1: Component exports named function `OwnerControlPullQuote`.
    - Test 2: Renders on dark `#1C1412` background.
    - Test 3: Contains at least one owner-control pull-quote.
  </behavior>
  <action>
    Create `src/app/components/landing/OwnerControlPullQuote.jsx` as a Server Component.

    Full file contents (uses the UI-SPEC locked copy "You set the rules. Voco follows them."):

    ```jsx
    import { AnimatedSection } from './AnimatedSection';

    export function OwnerControlPullQuote() {
      return (
        <section className="relative overflow-hidden py-20 md:py-24 px-6">
          {/* Dark warm base — matches FinalCTA mood but with less orange intensity */}
          <div className="absolute inset-0 bg-[#1C1412]" />
          <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(249,115,22,0.10),transparent_60%)]" />

          <div className="relative max-w-2xl mx-auto text-center">
            <AnimatedSection>
              <p className="text-[24px] md:text-[30px] font-semibold text-white leading-tight">
                &ldquo;You set the rules. Voco follows them.&rdquo;
              </p>
              <p className="text-[14px] text-white/50 italic mt-4">
                — The Voco product principle
              </p>
            </AnimatedSection>
          </div>
        </section>
      );
    }
    ```

    Spec rules (UI-SPEC REPOS-04):
    - Dark background `bg-[#1C1412]` with orange radial glow overlay.
    - Content max-width 640px (`max-w-2xl` = 672px — close enough to UI-SPEC 640; OR use `max-w-[640px]` for exact match).
    - Quote: 24px mobile / 30px md+ semibold white, leading-tight, centered.
    - Attribution line: 14px text-white/50 italic.
    - `py-20 md:py-24 px-6`.
    - Uses typographic quote marks (`&ldquo;` / `&rdquo;`) per UI-SPEC "Optional quotation marks as decoration".
    - AnimatedSection wrapper (POLISH-11).

    Change `max-w-2xl` to `max-w-[640px]` if the executor prefers exact UI-SPEC alignment — either is acceptable; UI-SPEC says max-width 640px.

    Then POPULATE the `Phase 47 — OwnerControlPullQuote (REPOS-04)` describe block in `tests/unit/landing-sections.test.js`:

    ```js
    describe('Phase 47 — OwnerControlPullQuote (REPOS-04)', () => {
      it('renders on dark #1C1412 background', () => {
        const src = require('fs').readFileSync('src/app/components/landing/OwnerControlPullQuote.jsx', 'utf8');
        expect(src).toMatch(/bg-\[#1C1412\]/);
      });
      it('contains an owner-control pull-quote', () => {
        const src = require('fs').readFileSync('src/app/components/landing/OwnerControlPullQuote.jsx', 'utf8');
        // Must mention both "rules" and "follows" per the UI-SPEC locked copy,
        // OR an equivalent owner-control/rules framing.
        expect(src).toMatch(/rules/i);
        expect(src).toMatch(/follows|follow/i);
      });
    });
    ```
  </action>
  <verify>
    <automated>npm test -- tests/unit/landing-sections.test.js -t "OwnerControlPullQuote" 2>&amp;1 | grep -E "(PASS|pass|✓)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/components/landing/OwnerControlPullQuote.jsx` exists
    - `grep "export function OwnerControlPullQuote" src/app/components/landing/OwnerControlPullQuote.jsx` matches
    - `grep "bg-\[#1C1412\]" src/app/components/landing/OwnerControlPullQuote.jsx` matches
    - `grep "radial-gradient" src/app/components/landing/OwnerControlPullQuote.jsx` matches (orange glow)
    - `grep -Ei "rules.*follow|follow.*rules" src/app/components/landing/OwnerControlPullQuote.jsx` matches (D-21 quote)
    - Smoke tests for REPOS-04 pass
  </acceptance_criteria>
  <done>
    REPOS-04 dark-section pull-quote component exists with D-21 locked copy ("You set the rules. Voco follows them."), matches UI-SPEC color/typography contract, uses AnimatedSection.
  </done>
</task>

</tasks>

<verification>
All three new components exist at the correct paths, all pass their smoke test assertions, none use `'use client'`, none violate the ScrollLinePath insertion rules (AfterTheCallStrip), none use defensive copy (IdentitySection), all wrap AnimatedSection (POLISH-11), all stack single-column at 375px + multi-column at md+ (POLISH-12).

Run final: `npm test -- tests/unit/landing-sections.test.js` — no failures, `AfterTheCallStrip`, `IdentitySection`, `OwnerControlPullQuote` describe blocks all pass.
</verification>

<success_criteria>
Three Server Component files exist, smoke-tested, and ready to be imported by `page.js` in Plan 05. No other plan is blocked by this plan (Plans 03 and 04 touch different files).
</success_criteria>

<output>
After completion, create `.planning/phases/47-landing-objection-busting-repositioning-and-landing-polish/47-02-SUMMARY.md` per summary template.
</output>
