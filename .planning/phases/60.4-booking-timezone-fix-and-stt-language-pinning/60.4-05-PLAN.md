---
phase: 60.4
plan: 05
type: execute
wave: 4
depends_on: [60.4-04]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md
autonomous: false
decisions_addressed: [D-C-02, D-C-03, D-C-04, D-X-03]
must_haves:
  truths:
    - "A live UAT call reaching natural end-of-call flow shows `end_call_invoked_at - last_text_token_at < 2000ms` in the `[goodbye_race]` breadcrumb"
    - "The farewell phrase is still fully played (no mid-word truncation regression from 60.3 Plan 03)"
    - "If UAT verdict is inconclusive (flaky / single call noise), D-C-04 permits downgrade to 'diagnosed only' — Stream C defer to Phase 60.5"
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md"
      provides: "§Stream C UAT evidence — [goodbye_race] breadcrumb delta + farewell playback verdict"
  key_links:
    - from: "agent farewell utterance"
      to: "end_call tool invocation"
      via: "prompt-revision-enforced latency ceiling"
      pattern: "end_call_invoked_at.*last_text_token_at"
---

<objective>
Stream C UAT — verify the Plan 04 prompt revision closes the 16s end_call stall.

Assert (D-C-03): `end_call_invoked_at - last_text_token_at < 2000ms` on a live UAT call reaching natural end-of-call flow, measured via the 60.3 Plan 01 `[goodbye_race]` breadcrumb (NO new instrumentation per D-C-01).

Also verify (D-C-04 safeguard): the farewell phrase is still fully played — no regression of the 60.3 #5096 mid-word-truncation fix. If Plan 04 accidentally loosened the two-step mechanics, the UAT will catch it.

Per D-C-04, if the UAT evidence is inconclusive (single flaky call, delta between 2-10s, or farewell regresses) → downgrade Stream C to "diagnosed only" and defer fix to Phase 60.5 rather than re-iterating inside 60.4.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (Checkpoint): Execute Stream C UAT and measure [goodbye_race] delta</name>
  <what-built>
    Plan 04 revised `_build_call_duration_section` CRITICAL RULE in both EN and ES to add a "within 1-2 seconds of your final farewell token, call end_call" latency ceiling, replacing the over-corrected "separate turn with no additional speech" trigger text. Two-step mechanics and 9/10-minute bounds preserved. Railway auto-deployed Plan 04 GREEN commit.
  </what-built>
  <how-to-verify>
    **Preconditions:** Confirm Railway deploy for Plan 04 GREEN commit = `SUCCESS`. If not, wait.

    1. Dial **+6587528516** (or any tenant Twilio number — Stream C applies across all tenants).
    2. Have a natural short conversation (10-30s is fine — any exchange that leads to a clean goodbye is sufficient; does not have to be a booking).
    3. When the caller wants to end (or the agent naturally reaches a close), let the agent say the farewell and end the call organically. Do NOT hang up manually before the agent calls end_call.
    4. Note the call-id (visible in Railway logs as `call-<id>`).

    **Post-call evidence collection:**

    A. **Pull the `[goodbye_race]` breadcrumb from Railway logs** for this call-id. Format is a single JSON `logger.info` line from 60.3 Plan 01. Extract:
       - `end_call_invoked_at` (ISO timestamp or ms)
       - `last_text_token_at` (ISO timestamp or ms)
       - `playback_finished_at` (may be present)
       - `text_done` / `audio_done` flags (may be present)
       - `disconnect_reason`, `close_reason`

    B. **Compute delta:** `end_call_invoked_at - last_text_token_at` in milliseconds.
       - Expected: **< 2000ms** (target per D-C-03)
       - 60.3 baseline: 16,177ms (Call B-1)
       - Success criterion: delta < 2000ms AND no mid-word truncation in the farewell

    C. **Verify farewell completeness** — listen to the call recording (if available) or inspect the transcript tail:
       - Did the caller hear the full farewell phrase ("Thank you for calling..." / "Gracias por llamar...")?
       - Or did it cut off mid-word (regression of 60.3 Plan 03 protection)?

    **Populate `60.4-HUMAN-UAT.md`** with a new section `## Stream C UAT (Plan 05)`:

    ```markdown
    ## Stream C UAT (Plan 05) — post-Plan-04 deploy

    **Railway deploy commit:** <Plan 04 GREEN SHA>
    **Deploy timestamp:** <ISO UTC>
    **UAT call-id:** <call-<id>>
    **Call duration:** <seconds>

    ### [goodbye_race] breadcrumb (from Railway logs)

    ```json
    <paste JSON breadcrumb verbatim>
    ```

    ### Measurements

    - `last_text_token_at`: <ISO>
    - `end_call_invoked_at`: <ISO>
    - **delta:** <N>ms
    - `playback_finished_at`: <ISO or absent>
    - `text_done` / `audio_done`: <true/false/absent>
    - `disconnect_reason`: <...>
    - `close_reason`: <...>

    ### Farewell playback check

    - Transcript tail: <paste last 2-3 turns>
    - Full farewell audible? <yes/no>
    - Mid-word truncation (60.3 regression)? <yes/no>

    ### Verdict (D-C-03 / D-C-04)

    - [ ] delta < 2000ms → ✅ GREEN
    - [ ] delta 2000-10000ms → ⚠️ IMPROVED (partial); decide: re-fix or accept
    - [ ] delta > 10000ms → ❌ RED; consider D-C-04 downgrade to "diagnosed only" + defer to Phase 60.5
    - [ ] farewell truncation present → ❌ REGRESSION; revert Plan 04 revision, escalate to Branch G (60.5)

    **Owner decision:** <advance to Plan 06 | revise Plan 04 | downgrade per D-C-04>
    ```

    **Commit:**
    ```
    cd C:/Users/leheh/.Projects/homeservice_agent
    git add .planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md
    git commit -m "docs(60.4-05): Stream C UAT — [goodbye_race] delta measurement"
    ```
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved` (delta < 2000ms AND farewell intact — advance to Plan 06)
    - `partial` (delta 2000-10000ms — accept as improvement per D-C-04, advance to Plan 06 with the caveat logged)
    - `downgrade` (delta > 10000ms or evidence inconclusive — mark Stream C as "diagnosed only" per D-C-04 and still advance to Plan 06 for phase close)
    - `regressed` (farewell truncation returned — Plan 04 must be reverted before Plan 06)
    - `<free-form description>`
  </resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.4-C-UAT-01 | Information Disclosure | `[goodbye_race]` breadcrumb carries transcript_tail | accept | 60.3 Plan 01 already redacts E.164 phones via `_PHONE_REDACT_RE` and uses phone SHA-256 — inherits safe-by-default posture |

Manual UAT checkpoint. No new attack surface.
</threat_model>

<verification>
- `60.4-HUMAN-UAT.md` contains §"Stream C UAT (Plan 05)" fully populated
- Measured delta recorded; verdict captured
- Either Plan 06 unblocked (approved/partial/downgrade) OR Plan 04 revision cycle triggered (regressed)
</verification>

<success_criteria>
- D-C-03 measured (delta < 2s = GREEN; 2-10s = partial accept; >10s = downgrade per D-C-04)
- Farewell completeness verified (no 60.3 regression)
- Evidence-based decision logged in UAT file
</success_criteria>

<output>
UAT section populated + committed. No separate SUMMARY.md — subsumed into Plan 06 phase rollup.
</output>
