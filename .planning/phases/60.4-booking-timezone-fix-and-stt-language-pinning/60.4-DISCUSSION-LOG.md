# Phase 60.4: Booking timezone fix + STT language pinning — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 60.4-booking-timezone-fix-and-stt-language-pinning
**Areas discussed:** Stream A approach (user-redirected to code-audit), Stream A naive-ISO handling, Stream A tenant_timezone fallback, Stream B language pinning, Stream C (16s end_call stall) scope

---

## Stream A — Approach (user-redirected before AskUserQuestion ran)

**Claude's original framing:** Three options — diagnose-first (instrument + UAT), fix-forward from code read, narrow-scope instrument on events.insert() only.

**User response:** "The booking timezone sometimes is correct, and sometimes is wrong (more often correct). Can you just look through the code and strengthen it to ensure its always correct?"

**Resulting approach:** Inline code audit during discuss (not instrumentation phase), focused hardening of every datetime boundary. Audit confirmed the mechanism: `google_calendar.py:101-106` builds event body without `timeZone`, and `_ensure_utc_iso` sanitizer only covers SMS paths — not DB persistence or calendar push. See CONTEXT.md D-A-01..06.

**Notes:** User's "intermittent, mostly correct" framing was itself the key diagnostic clue — 100%-wrong = systemic, intermittent = conditional path. Aligned with the existing `_to_utc_iso` code comment documenting "Gemini occasionally strips the '+00:00' offset."

---

## Stream A — Naive-ISO handling

| Option | Description | Selected |
|--------|-------------|----------|
| Coerce-and-log | Extend existing `_ensure_utc_iso` to every boundary; log WARN when tzinfo missing; never crashes bookings | ✓ |
| Fail-loud (raise) | `_assert_utc_iso` raises on naive input at every boundary; catches regressions in dev but fails live bookings | |
| Coerce + unit test | Same as coerce-and-log plus a regression test covering the naive path | |

**User's choice:** Coerce-and-log (Recommended)
**Notes:** "Fail-loud" noted in CONTEXT Deferred Ideas as a test-only assertion helper at PLAN-A-2 implementer's discretion. Unit test coverage is already locked in D-A-06 regardless of this choice.

---

## Stream A — Tenant timezone fallback

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `America/Chicago` | Status quo — legacy US-only default | |
| Fall back to UTC + WARN | Neutral default; misconfigured tenants visible fast; multi-tenant safe | ✓ |
| Refuse to book on null | Returns user-safe error; surfaces config bug to live caller | |
| Verify in plan phase | Defer decision until researcher checks how many tenants have null column | |

**User's choice:** Fall back to UTC + log warning (Recommended)
**Notes:** Captured as D-A-05. Applies to both fallback sites (`book_appointment.py:266` and `check_availability.py:142`). A parallel RESEARCH task will still spot-check how many prod tenants have a null `tenant_timezone` to understand blast radius — but the decision is independent of that count.

---

## Stream B — STT language pinning

| Option | Description | Selected |
|--------|-------------|----------|
| Per-tenant `default_locale` from DB | Read existing column, map to BCP-47, pass to `RealtimeModel` | |
| Hardcode 'en-US' now | Smallest fix; add follow-up for ES tenants | |
| Research first in planning | Confirm kwarg name + list-mode behavior on pinned plugin | ✓ |

**User's choice:** Research first in planning
**Notes:** Provisional lean toward per-tenant locale captured in D-B-01 (same column already used by Phase 60.3 for prompt locale selection). Research items explicitly enumerated: (1) exact kwarg name, (2) value format, (3) list-mode behavior, (4) RealtimeModel instantiation timing. Final decision locks in PLAN-B-1 after RESEARCH.

---

## Stream C — 16s end_call stall scope

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to Phase 60.5 | Keep 60.4 narrow; new diagnose-first phase for the stall | |
| Investigate only (notes doc) | Root-cause hypothesis written; no fix commitment | |
| Fold in as Stream C | One plan to diagnose + fix during same UAT cycle | ✓ |

**User's choice:** Fold in as Stream C
**Notes:** Stream C captured as D-C-01..04. Reuses the `[goodbye_race]` instrumentation from 60.3-01 — no new instrumentation work. Mirrors 60.3 Stream A shape exactly (instrument already deployed → one UAT → evidence-pick fix → second UAT). Candidate fix sites ranked by likelihood in D-C-02. D-C-04 gives permission to downgrade Stream C to "diagnosis only, fix deferred to 60.5" if UAT evidence is inconclusive, so Stream A / B ship timelines are protected.

---

## Claude's Discretion

- Exact wording of structured WARN log lines (D-A-04 boundary-sanitize warn, D-A-05 null-tenant-tz warn).
- Stream B value-format specifics (`en-US` vs `en` vs Google-specific) — decided by research outcome.
- Stream C fix site (prompt vs tool description vs SDK-level timer) — decided by UAT evidence.
- Unit-test file locations and structure in the livekit-agent repo — follow existing conventions.

## Deferred Ideas

- Fail-loud `_assert_utc_iso` variant — test-only helper candidate, not production.
- Tenant-timezone onboarding guardrail — belongs to onboarding wizard, not livekit-agent; backlog.
- Retroactive backfill of mis-scheduled Google Calendar events — forward-only fix; ops script candidate.
- Bilingual STT (list-mode language pinning) — Phase 63+ if ever needed.
- Gemini model upgrade for noise-resilient ASR — explicitly out of scope (SDK versions locked D-X-04).
- Post-goodbye force-disconnect timer (SDK-level) — Stream C fallback if prompt/tool fixes fail.
