---
phase: 60.4
plan: 06
type: execute
wave: 5
depends_on: [60.4-03, 60.4-05]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md
autonomous: true
decisions_addressed: [D-X-01, D-X-02, D-X-03, D-X-04]
must_haves:
  truths:
    - "voice-call-architecture SKILL.md carries a Phase 60.4 subsection documenting the timeZone event-body fix, _ensure_utc_iso WARN semantics, UTC fallback across 3 sites, language kwarg wiring, anti-hallucination prompt directive, and the Stream C latency-ceiling revision"
    - "CLAUDE.md's 'keep skills in sync' rule is satisfied (voice-call-architecture is the skill that covers these subsystems)"
    - "60.4-SUMMARY.md phase rollup exists with per-plan commit SHAs, test count delta, UAT verdicts, and follow-ups"
    - "ROADMAP.md Phase 60.4 entry is finalized (status → Complete with links to artifacts) and STATE.md reflects phase close"
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md"
      provides: "§ 'Phase 60.4 — Booking timezone fix + STT language pinning + end_call stall close' subsection"
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md"
      provides: "phase-rollup per GSD template"
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md"
      provides: "Phase 60.4 status close + plan links"
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md"
      provides: "decisions log entry for Phase 60.4 close + current position advance"
  key_links:
    - from: "SKILL.md Phase 60.4 subsection"
      to: "livekit-agent Stream A/B/C commits"
      via: "commit SHA references"
      pattern: "60.4"
---

<objective>
Phase close. Update `voice-call-architecture` SKILL.md per CLAUDE.md's "Keep skills in sync" rule — additive subsection documenting the three Stream A/B/C outcomes so the next developer reading the skill has the full picture. Write phase-rollup SUMMARY.md. Finalize ROADMAP.md and STATE.md.

Per 60.3 pattern (reference `60.3-SUMMARY.md` rollup format), the phase-close plan is autonomous — no more code changes, just documentation sync across the four required surfaces.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update voice-call-architecture SKILL.md with Phase 60.4 subsection</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>
  <read_first>
    C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (full file — locate the Phase 60.3 subsection for insertion point),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md (verdict to cite),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-01-SUMMARY.md (Plan 01 commit SHAs),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-02-SUMMARY.md (Plan 02 commit SHAs),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-04-SUMMARY.md (Plan 04 commit SHAs)
  </read_first>
  <action>
    Insert a new subsection **AFTER** the existing `§"Phase 60.3 — Goodbye cut-off diagnosis + prompt audit"` subsection. Do NOT modify the Phase 60.3 subsection or earlier content. Additive only.

    **New subsection:**

    ```markdown
    ## Phase 60.4 — Booking timezone fix + STT language pinning + end_call stall close

    Three reliability-bug streams closed, all in `livekit-agent/` (cross-repo, git commit `--no-verify`, Railway auto-deploy).

    ### Stream A — Google Calendar event timezone (customer-facing regression)

    **Problem:** SG tenants booking 3 PM SG slots intermittently landed at 11 PM SG in Google Calendar (+8h shift matching UTC+8). Root cause: `events.insert()` body set `start.dateTime` only (no `start.timeZone`), and Gemini occasionally strips the UTC offset when re-emitting `slot_start` — leaving Google to interpret a naive `dateTime` in the calendar's default TZ.

    **Fix (Plan 01 commits `<RED SHA>` + `<GREEN SHA>`):**
    - `src/lib/google_calendar.py`: event body now sets `start.timeZone` + `end.timeZone` to `tenant_timezone`, with `dateTime` converted to naive local ISO via new `_to_naive_local_iso()` helper. **Pitfall 1 (60.4-RESEARCH):** do NOT pass BOTH offset-suffixed `dateTime` AND `timeZone` — Google double-offsets. The fix strips the offset via `ZoneInfo`-aware conversion.
    - `src/lib/calendar_push.py` + `src/tools/book_appointment.py`: `tenant_timezone` plumbed through the push path.
    - `_ensure_utc_iso` (book_appointment.py) now emits `[tz_coerce] naive ISO at boundary` WARN on naive input; never raises (D-A-04).
    - `America/Chicago` null-timezone fallback eliminated from ALL 3 sites: `book_appointment.py:266`, `check_availability.py:142`, **`post_call.py:612`** (third site not in CONTEXT — surfaced by 60.4 RESEARCH). All now fall back to `"UTC"` + `[tenant_config] null tenant_timezone tenant_id=...` WARN.
    - Outlook adapter (`outlook_calendar.py`) untouched — existing `{dateTime, timeZone}` pattern is Outlook-API-correct (different semantics than Google).

    ### Stream B — STT language pinning (defense-in-depth; native-audio blocker)

    **Problem:** Gemini Live transcripts intermittently contained German/Hindi hallucinations during silence even though the caller spoke only English.

    **Research finding (60.4-RESEARCH §"Stream B Native-Audio Blocker"):** `gemini-3.1-flash-live-preview` is a native-audio model; Google's official docs state `language_code` is explicitly NOT supported — native-audio models auto-detect input language. Upstream LiveKit community thread 410 confirms identical symptom on the same model class; classified as model-side, unsolvable at framework layer. Elimination is not achievable on the pinned stack.

    **Fix (Plan 02 commits `<RED SHA>` + `<GREEN SHA>`) — TWO surfaces (D-B-03 RE-OPENED):**
    - `src/agent.py`: added `_LOCALE_TO_BCP47 = {"en": "en-US", "es": "es-US"}` + `_locale_to_bcp47(locale)` helper. `RealtimeModel(...)` now takes `language=_locale_to_bcp47(locale)` — defense-in-depth, passed through to `SpeechConfig(language_code=...)`. Unknown locale → `en-US` fallback. `[60.4 Stream B] RealtimeModel language=... (locale=...)` info log per session (Railway visibility).
    - `src/prompt.py` `_build_language_section`: added `ANTI-HALLUCINATION — CRITICAL:` / `ANTI-ALUCINACIÓN — REGLA CRÍTICA:` paragraph in both locales. Directive: treat unintelligible audio as the default locale; never invent a foreign-language phrase to fill silence. 60.3 Plan 12 locale-parity preserved.

    **Acceptance (D-B-02 softened):** non-English token rate in deliberate-silence windows measurably reduced vs baseline, NOT eliminated. Framed as defense-in-depth, not a fix.

    ### Stream C — 16-second end_call stall (60.3 regression close)

    **Problem:** 60.3 Branch P CRITICAL RULE introduced `"THEN in a separate turn with no additional speech, call end_call"` — over-corrected. Cumulative UAT Call B-1 measured 16.177s gap from `last_text_token_at → end_call_invoked_at`. Model waits indefinitely for the under-specified "separate turn" trigger.

    **Fix (Plan 04 commits `<RED SHA>` + `<GREEN SHA>`):**
    - `src/prompt.py` `_build_call_duration_section`: CRITICAL RULE trigger text revised in both locales. EN: `"within 1-2 seconds of your final farewell token, call the end_call tool. Do not wait for the caller to say anything"`. ES: `"dentro de 1-2 segundos después de su última palabra de despedida, llame a la herramienta end_call. No espere a que el llamante diga nada"`. Two-step mechanics ("speak farewell → brief silence → end_call") and 9/10-minute bounds preserved, only the wait-trigger tightened.
    - Per D-C-01, no new instrumentation — 60.3 Plan 01 `[goodbye_race]` breadcrumb reused to measure delta.
    - Per D-C-04, if UAT evidence inconclusive, Stream C degrades to "diagnosed only" (deferred to Phase 60.5); NOT blocking for Streams A/B ship.

    ### Debugging playbook (appended rows)

    | Symptom | Likely cause | First check | Fix path |
    |---------|--------------|-------------|----------|
    | Google Calendar event at wrong time for non-US tenants | Missing `timeZone` field OR null `tenant_timezone` | Grep Railway for `[tenant_config] null tenant_timezone` or inspect event body shape | Plan 01 D-A-01 / D-A-05 |
    | `[tz_coerce]` WARN in Railway logs | Gemini emitted a naive ISO (offset stripped) | Inspect the log's boundary field — which tool/path? | Usually harmless — helper re-attaches UTC; investigate if spikes |
    | Transcript contains non-English tokens during silence | Gemini native-audio language auto-detection (unsolvable at SDK) | Railway log `[60.4 Stream B] RealtimeModel language=...` present? | Accept as known limitation OR evaluate model switch (Phase 60.5+) |
    | `[goodbye_race]` delta > 2000ms (end_call stall) | Prompt directive still under-specified | Check `_build_call_duration_section` — revision in place? | If regressed → Plan 04 revert + Branch G escalation |

    ### Invariant locks added this phase

    - Event body `start.timeZone == end.timeZone == tenant_timezone`, `dateTime` is naive (no `+` / `Z` suffix) — pinned by `tests/test_google_calendar_tz.py::test_event_body_dateTime_is_naive_when_timezone_set`.
    - No production `or "America/Chicago"` fallback strings remain — pinned by grep in Plan 01 acceptance; 3 sites each have explicit `"UTC"` fallback + WARN, pinned by `tests/test_tenant_timezone_fallback.py`.
    - `_ensure_utc_iso` WARN-and-continue behavior pinned by `tests/test_ensure_utc_iso.py::test_ensure_utc_iso_naive_warns_and_coerces`.
    - Locale → BCP-47 map scope restricted to `{en, es}` — pinned by Plan 02 acceptance (no `zh-CN`/`ms-MY`/etc).
    - EN + ES `_build_call_duration_section` latency ceiling pinned by `tests/test_prompt_call_duration.py::test_*_critical_rule_has_latency_ceiling`; legacy `"separate turn with no additional speech"` / `"turno separado sin ninguna palabra adicional"` pinned absent by inverted assertions.
    ```

    Update the top-of-file `Last updated:` line to `2026-04-22 (Phase 60.4 — booking TZ hardening + STT language defense-in-depth + end_call stall close).`

    Commit:
    ```
    cd C:/Users/leheh/.Projects/homeservice_agent
    git add .claude/skills/voice-call-architecture/SKILL.md
    git commit -m "docs(60.4): update voice-call-architecture skill with Phase 60.4 subsection"
    ```
  </action>
  <verify>
    <automated>grep -c "Phase 60.4" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 60.4 — Booking timezone fix" SKILL.md` returns ≥ 1
    - `grep -c "Phase 60.3" SKILL.md` unchanged from pre-task value (60.3 subsection preserved, not rewritten)
    - `grep -c "Pitfall 1" SKILL.md` returns ≥ 1 (double-offset pitfall documented)
    - `grep -c "post_call.py:612" SKILL.md` returns ≥ 1 (third fallback site documented)
    - `grep -c "_LOCALE_TO_BCP47\|language=en-US\|language=\"en-US\"" SKILL.md` returns ≥ 1
    - `grep -c "within 1-2 seconds\|dentro de 1-2 segundos" SKILL.md` returns ≥ 2
    - `grep -c "ANTI-HALLUCINATION\|ANTI-ALUCINACIÓN" SKILL.md` returns ≥ 2
    - `grep "Last updated" SKILL.md | head -1` contains `2026-04-22` AND `Phase 60.4`
    - Commit prefix `docs(60.4): update voice-call-architecture skill`
  </acceptance_criteria>
  <done>SKILL.md Phase 60.4 subsection inserted; commit pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Write 60.4-SUMMARY.md phase rollup + update ROADMAP.md + STATE.md</name>
  <files>
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md,
    C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md,
    C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md
  </files>
  <read_first>
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md (template to emulate — frontmatter, D-X-02 success criteria table, per-plan commit table, lessons, follow-ups),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-01-SUMMARY.md (Plan 01 outputs),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-02-SUMMARY.md (Plan 02 outputs),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-04-SUMMARY.md (Plan 04 outputs),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md (UAT verdicts — Plan 03 Streams A+B, Plan 05 Stream C),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md (Phase 60.4 entry to finalize),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md (add P60.4 decision log entry + advance current position)
  </read_first>
  <action>
    **A. Create `60.4-SUMMARY.md`** with the frontmatter + sections matching 60.3-SUMMARY.md's template:

    - Frontmatter: phase, parent_phase (60.3), kind (phase-rollup), status (complete / complete-with-followups), completed (2026-04-23 or actual), plans list
    - Opening paragraph: "What shipped" — one paragraph each for Streams A / B / C with final verdicts from the UAT file
    - **Success Criteria table** — one row per decision D-A-01 through D-X-04 with outcome (✅ / ⚠️ / ❌), evidence link
    - **Plan-by-plan commits table** — 6 rows (Plans 01-06), each with repo, commit SHAs, notes
    - **Key artifacts** — links to CONTEXT, RESEARCH, VALIDATION, HUMAN-UAT, 01-SUMMARY through 04-SUMMARY
    - **Test count delta** — pre-60.4 baseline 205 passed; post-60.4 total (compute from Plan 01 / Plan 02 / Plan 04 SUMMARY deltas)
    - **Lessons captured** — numbered list (at least 4 lessons; reference the actual surprises from execution — e.g. "research caught a third `America/Chicago` fallback site CONTEXT missed", "native-audio blocker forced D-B-03 re-open with prompt surface", "D-C-04 downgrade option proved valuable / unneeded")
    - **Follow-ups (non-blocking)** — prioritized list; include at minimum: (a) backfill tenants.tenant_timezone for any tenants surfaced by the WARN log after 60.4 ships; (b) Phase 60.5 candidate if Stream C UAT downgraded; (c) model switch to `gemini-live-2.5-flash` (non-native-audio) evaluation if Stream B language pin had zero observable effect; (d) `_LOCALE_TO_BCP47` expansion to additional locales once multilingual tenants onboard
    - **Self-Check** — artifact existence verified, commit SHAs present in git history

    **B. Update `.planning/ROADMAP.md`:**
    - Locate the Phase 60.4 entry
    - Mark status: `**Status:** Complete` (or `Complete with followups` if Plan 05 downgraded)
    - Append a `**Completed:** 2026-04-23` (or actual date) line
    - Add links to `.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md`
    - Mark all 6 plans as `[x]` completed

    **C. Update `.planning/STATE.md`:**
    - Append a new decision-log entry to `### Decisions (v6.0 planning)`:
      ```
      - [v6.0 P60.4-CLOSE]: Phase 60.4 shipped. Stream A (Google Calendar timeZone field + naive-local-ISO + UTC fallback across 3 sites + _ensure_utc_iso WARN) closes the intermittent +8h SG booking regression — verified by Plan 03 UAT (3 PM SG → 15:00 SGT event). Stream B shipped BOTH surfaces per D-B-03 RE-OPENED: `language="en-US"` / `"es-US"` kwarg on RealtimeModel (defense-in-depth; native-audio blocker means not an elimination) + ANTI-HALLUCINATION/ANTI-ALUCINACIÓN directive added to `_build_language_section` in both locales. Stream C revised Branch P CRITICAL RULE to bound the end_call wait to `within 1-2 seconds of final farewell token` (EN) / `dentro de 1-2 segundos` (ES) — <delta result from UAT>. Test count delta: <computed>. Third `America/Chicago` fallback site surfaced by 60.4 RESEARCH (post_call.py:612) — NOT in CONTEXT; all 3 sites now UTC + WARN. SKILL.md voice-call-architecture updated with additive Phase 60.4 subsection. Anchor for next phase: <from Plan 05 UAT verdict — e.g., Phase 60.5 if downgrade; or next backlog item if all green>.
      ```
    - Update `stopped_at:` to `Phase 60.4 complete`
    - Update `last_updated:` to current ISO timestamp
    - Update `last_activity:` to current date
    - Increment `completed_phases` / adjust `completed_plans` if tracker used

    Commit:
    ```
    cd C:/Users/leheh/.Projects/homeservice_agent
    git add .planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md .planning/ROADMAP.md .planning/STATE.md
    git commit -m "docs(60.4): phase close — SUMMARY + ROADMAP + STATE"
    ```
  </action>
  <verify>
    <automated>test -f C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-SUMMARY.md &amp;&amp; grep -c "Phase 60.4" C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md &amp;&amp; grep -c "P60.4-CLOSE" C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `60.4-SUMMARY.md` exists with frontmatter (`phase`, `kind: phase-rollup`, `status`, `plans` list)
    - SUMMARY.md contains §"Success Criteria" table with outcome column (✅ / ⚠️ / ❌) for every D-A, D-B, D-C, D-X decision ID
    - SUMMARY.md contains §"Plan-by-plan commits" with 6 rows
    - SUMMARY.md contains §"Lessons captured" with ≥ 4 numbered items
    - SUMMARY.md contains §"Follow-ups (non-blocking)" with ≥ 4 items including at minimum: tenant_timezone backfill, Phase 60.5 candidacy if applicable, model-switch evaluation, locale map expansion
    - `grep -c "Phase 60.4" C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md` returns ≥ 1
    - ROADMAP.md Phase 60.4 entry status line reads `Complete` or `Complete with followups`
    - `grep -c "P60.4-CLOSE" C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md` returns ≥ 1
    - STATE.md `stopped_at` and `last_updated` fields reflect phase close
    - Commit prefix `docs(60.4): phase close`
  </acceptance_criteria>
  <done>SUMMARY, ROADMAP, STATE all updated and committed. Phase 60.4 closed.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.4-DOC-01 | Information Disclosure | SUMMARY / SKILL.md carry commit SHAs + call-ids from UAT | accept | Call-ids are internal; no PII (phone-SHA256 + E.164 redaction from 60.3 Plan 01 applies) |

Documentation-only plan. No runtime attack surface.
</threat_model>

<verification>
- SKILL.md Phase 60.4 subsection present + additive (60.3 subsection preserved)
- 60.4-SUMMARY.md populated per template
- ROADMAP.md Phase 60.4 status marked complete
- STATE.md decision log updated
- All 3 commits pushed
</verification>

<success_criteria>
- CLAUDE.md "Keep skills in sync" rule satisfied
- Phase archive complete (CONTEXT, RESEARCH, VALIDATION, 6× PLANs, per-plan SUMMARYs, HUMAN-UAT, phase SUMMARY)
- Future developers reading voice-call-architecture skill have the full Phase 60.4 picture
- STATE.md ready for next phase pickup
</success_criteria>

<output>
No standalone output file beyond the artifacts listed (SUMMARY/ROADMAP/STATE/SKILL). Phase 60.4 closed.
</output>
