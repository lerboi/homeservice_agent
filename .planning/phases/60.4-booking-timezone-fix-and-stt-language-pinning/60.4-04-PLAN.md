---
phase: 60.4
plan: 04
type: execute
wave: 3
depends_on: [60.4-03]
files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py
  - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_call_duration.py
autonomous: true
decisions_addressed: [D-C-01, D-C-02, D-X-01, D-X-04]
must_haves:
  truths:
    - "The Branch P CRITICAL RULE in _build_call_duration_section is revised so the model invokes end_call within 1-2 seconds of its final farewell token (not 16 seconds)"
    - "Revision preserves the 60.3 Plan 03 primary goal (no mid-word truncation) — two-step mechanics intact, just with a concrete latency ceiling"
    - "Spanish branch updated in parity with English (USTED register per 60.3 Plan 05 convention)"
    - "Existing 60.3 Plan 03 and Plan 05 test invariants preserved (no regression on truncation-protection tests)"
  artifacts:
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/prompt.py"
      provides: "_build_call_duration_section with revised CRITICAL RULE — latency ceiling + preserved two-step"
      contains: "within 1"
    - path: "C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_call_duration.py"
      provides: "extended tests pinning the new latency directive in both locales"
  key_links:
    - from: "model farewell utterance"
      to: "end_call tool invocation"
      via: "prompt CRITICAL RULE"
      pattern: "within 1|within 2|without delay|no additional speech"
---

<objective>
Stream C prompt-nudge fix. Close the 16-second `last_text_token_at → end_call_invoked_at` gap surfaced in the 60.3 cumulative UAT (Call B-1 measured 16.177s).

Root cause (per RESEARCH §"Stream C Gemini Tool-Call Latency" + 60.3-SUMMARY Follow-up #1): Branch P's current CRITICAL RULE text `"THEN in a separate turn with no additional speech, call end_call"` over-corrected — the model interprets "separate turn" as "wait for a trigger that never comes". Model-side native-audio tool-call latency is known-variable per multiple community sources; no SDK-level fix exists. The evidence-picked fix per RESEARCH §"Stream C fix-site ranking" is the **prompt nudge**: replace the under-specified "separate turn" trigger text with an explicit **"within 1-2 seconds of your final farewell token, call end_call"** directive.

Per D-C-01, NO new instrumentation — the `[goodbye_race]` breadcrumb from 60.3 Plan 01 already captures the metric (`end_call_invoked_at - last_text_token_at`). Per D-C-02 we do one UAT in Plan 05 to verify the fix.

**Critical:** preserve the two-step mechanics that closed #5096-class mid-word truncation in 60.3 Plan 03. The revision tightens the WAIT duration from "separate turn" (unbounded) to "1-2 seconds" (bounded) — mechanics unchanged, latency ceiling added.

Output: prompt.py locale-paired revision shipped to livekit-agent main; extended tests pinning the new latency directive and preserving prior invariants.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md

<interfaces>
Current `_build_call_duration_section` (src/prompt.py:1094-1146) — Branch P text that over-corrected:

English (line 1123-1127):
```
ENDING THE CALL — CRITICAL RULE:
Your farewell must be FULLY spoken and heard by the caller before the line
disconnects. Completing the goodbye is a two-step commitment: (1) speak the
entire goodbye phrase, (2) let a brief silence follow, (3) THEN in a
separate turn with no additional speech, call end_call.
```

Spanish equivalent (line 1095-1121) uses USTED register and mirrors the three-step structure; same over-correction applies.

60.3 Plan 03 commit `ebaa556` promoted this section to position 5 in `build_system_prompt` (top-attention band). Position is correct; only the wait-trigger text is being tightened.

60.3 Plan 05 commit `9f27a7a` added ES parity with USTED register + INCORRECTO/CORRECTO parallel example.

Existing tests in `tests/test_prompt_call_duration.py` (from 60.3 Plan 05):
- Pin "CRITICAL RULE" / "REGLA CRÍTICA" headers
- Pin "two-step" / farewell content
- Pin 9/10-minute duration bounds
- Pin position-5-or-earlier invariant
- Pin Voco brand
- Must NOT break — add new tests, don't modify existing assertions unless directly testing the trigger text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Write RED tests for revised Stream C prompt directive</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_call_duration.py</files>
  <read_first>
    C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_call_duration.py (full existing file — 60.3 Plan 05 tests; extend, do not rewrite),
    C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (lines 1094-1146 — current Branch P text),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md (Call B-1 16.177s measurement — the regression this plan fixes),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-RESEARCH.md (§"Stream C — the prompt-nudge candidate")
  </read_first>
  <behavior>
    **Extend** `tests/test_prompt_call_duration.py` with the following new tests (do NOT delete existing 60.3 Plan 05 assertions — they must continue to pass):

    - `test_en_critical_rule_has_latency_ceiling`: `_build_call_duration_section(t, locale="en")` output contains substring (case-insensitive, any of): `"within 1"` OR `"within 2"` OR `"within 1-2"` OR `"1-2 seconds"` OR `"without delay"` — pin that the revision adds a latency bound.
    - `test_en_critical_rule_no_longer_says_separate_turn_indefinite`: the exact legacy phrase `"separate turn with no additional speech"` must NOT appear verbatim (inverted assertion — this is the over-correction text being replaced). Wording like `"do not add additional words"` or `"in the same or immediately-next turn"` is allowed; the legacy literal must be gone.
    - `test_es_critical_rule_has_latency_ceiling`: `_build_call_duration_section(t, locale="es")` contains (case-insensitive, any of): `"1-2 segundos"` OR `"dentro de 1"` OR `"dentro de 2"` OR `"sin demora"` OR `"inmediatamente"`.
    - `test_es_critical_rule_no_longer_says_turno_separado_sin_palabra`: the exact legacy ES phrase `"turno separado sin ninguna palabra adicional"` must NOT appear verbatim.
    - `test_both_locales_still_have_two_step_mechanics_preserved`: both EN and ES outputs contain the two-step commitment language (EN: `"two-step"` or numbered `"(1)"` `"(2)"`; ES: `"dos pasos"` or numbered `"(1)"` `"(2)"`). Preserves 60.3 Plan 03's mid-word-truncation protection — the latency ceiling ADDS to the mechanics, not REPLACES them.
    - `test_both_locales_still_contain_failure_mode_example`: EN still contains `"WRONG"` + `"RIGHT"` (or `"Failure mode"` / `"Correct path"`); ES still contains `"INCORRECTO"` + `"CORRECTO"`.
    - `test_call_duration_bounds_preserved`: both outputs still contain `"9 minute"` and `"10 minute"` (EN) / `"9 minutos"` and `"10 minutos"` (ES).

    Run:
    ```
    cd C:/Users/leheh/.Projects/livekit-agent
    pytest tests/test_prompt_call_duration.py -x
    ```
    New tests must FAIL (specifically the latency-ceiling + no-longer-says-legacy-text tests); existing tests must still pass.

    Commit:
    ```
    git commit --no-verify -m "test(60.4-04): RED — Stream C prompt latency ceiling + preserve mid-word invariants"
    ```
  </behavior>
  <action>Extend the existing test file per <behavior>. Do NOT rewrite existing assertions.</action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent &amp;&amp; pytest tests/test_prompt_call_duration.py -x 2>&amp;1 | grep -E "FAILED|PASSED" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_en_critical_rule_has_latency_ceiling\|def test_es_critical_rule_has_latency_ceiling\|def test_en_critical_rule_no_longer_says_separate_turn\|def test_es_critical_rule_no_longer_says_turno_separado\|def test_both_locales_still_have_two_step_mechanics_preserved\|def test_both_locales_still_contain_failure_mode_example\|def test_call_duration_bounds_preserved" tests/test_prompt_call_duration.py` returns ≥ 7
    - At least 4 new tests currently FAIL (the latency-ceiling + no-longer-legacy ones on both locales); preservation tests still PASS
    - All 60.3 Plan 05 tests (from file's existing content) still PASS
    - Commit with prefix `test(60.4-04): RED`
  </acceptance_criteria>
  <done>Extended test file committed; mix of RED (new) + GREEN (existing) as expected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Revise _build_call_duration_section CRITICAL RULE with latency ceiling — both locales</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/prompt.py</files>
  <read_first>
    C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (lines 1094-1146 — full builder),
    C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_call_duration.py (Task 1 tests — implementation must satisfy),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-RESEARCH.md (§"Stream C — the prompt-nudge candidate" — candidate revision text)
  </read_first>
  <action>
    Revise `_build_call_duration_section` (lines 1094-1146). Keep the CRITICAL RULE header, two-step/(1)(2)(3) mechanics, failure-mode example, and 9/10-minute bounds INTACT. Change ONLY the trigger text for end_call.

    **English branch (replace the existing CRITICAL RULE paragraph, preserving surrounding structure):**

    ```
    ENDING THE CALL — CRITICAL RULE:
    Your farewell must be FULLY spoken and heard by the caller before the line
    disconnects. Completing the goodbye is a two-step commitment: (1) speak the
    entire goodbye phrase, (2) let a brief silence follow (about 1 second, no
    further words), (3) within 1-2 seconds of your final farewell token, call
    the end_call tool. Do not wait for the caller to say anything — end_call
    is the correct next action after your farewell.
    ```

    **Spanish branch (replace the equivalent ES CRITICAL RULE paragraph):**

    ```
    TERMINAR LA LLAMADA — REGLA CRÍTICA:
    Su despedida debe pronunciarse COMPLETAMENTE y ser escuchada por el
    llamante antes de que la línea se desconecte. Completar la despedida
    es un compromiso de dos pasos: (1) pronuncie la frase de despedida
    completa, (2) deje que siga un breve silencio (aproximadamente 1 segundo,
    sin palabras adicionales), (3) dentro de 1-2 segundos después de su
    última palabra de despedida, llame a la herramienta end_call. No espere
    a que el llamante diga nada — end_call es la siguiente acción correcta
    después de su despedida.
    ```

    **Preserve unchanged:**
    - The WRONG/INCORRECTO failure-mode example block
    - The RIGHT/CORRECTO correct-path example block — but update the example's narration line to match the revised directive. Replace:
      - EN RIGHT path: `"[SILENCE — at least one full beat, do not speak]"` and `"[call end_call tool with no additional speech]"` — leave the example essentially as-is (it shows mechanics); tests do NOT require changing it.
      - ES RIGHT path: same — leave as-is.
    - The `CALL DURATION BOUNDS:` / `LÍMITES DE DURACIÓN DE LA LLAMADA:` block with 9/10-minute limits

    **Rationale inline comment (add above the function):**
    ```python
    # Phase 60.4 Stream C (D-C-02 prompt-nudge): Branch P (60.3 Plan 03) over-
    # corrected by saying "THEN in a separate turn with no additional speech,
    # call end_call" — the model interpreted "separate turn" as waiting for a
    # trigger that never came, causing a 16s end_call stall measured in
    # 60.3 cumulative UAT Call B-1 (16.177s). Revision: keep the two-step
    # mechanics that stopped the #5096 mid-word truncation, add a concrete
    # "within 1-2 seconds" latency ceiling so the model self-promotes to tool
    # invocation instead of waiting indefinitely.
    ```

    Run:
    ```
    cd C:/Users/leheh/.Projects/livekit-agent
    pytest tests/test_prompt_call_duration.py -x
    pytest -x  # full suite — no regressions
    git commit --no-verify -m "fix(60.4-04): GREEN — Stream C prompt latency ceiling (Branch P revision, two-step preserved)"
    git push
    ```
    Railway auto-deploys. UAT is Plan 05.
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent &amp;&amp; pytest tests/test_prompt_call_duration.py -x &amp;&amp; pytest -x 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "within 1-2 seconds" src/prompt.py` returns ≥ 1 match
    - `grep -n "dentro de 1-2 segundos" src/prompt.py` returns ≥ 1 match
    - `grep -c "separate turn with no additional speech" src/prompt.py` returns 0 (legacy EN text gone)
    - `grep -c "turno separado sin ninguna palabra adicional" src/prompt.py` returns 0 (legacy ES text gone)
    - `grep -n "two-step commitment" src/prompt.py` still returns ≥ 1 match (mechanics preserved)
    - `grep -n "dos pasos" src/prompt.py` still returns ≥ 1 match (ES mechanics preserved)
    - `grep -n "CRITICAL RULE\|REGLA CRÍTICA" src/prompt.py` still returns ≥ 2 matches (headers preserved in both locales)
    - `grep -n "9 minutes\|9 minutos\|10 minutes\|10 minutos" src/prompt.py` still returns ≥ 4 matches (duration bounds preserved)
    - `pytest tests/test_prompt_call_duration.py -x` exits 0 (all tests GREEN — new + existing 60.3 Plan 05 tests)
    - `pytest -x` full suite passes
    - Commit prefix `fix(60.4-04): GREEN — Stream C`
    - Inline comment `Phase 60.4 Stream C` present above the function
  </acceptance_criteria>
  <done>Revised prompt shipped; tests GREEN; full suite GREEN; Railway redeployed. Plan 05 UAT unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system prompt → Gemini Live model | Prompt text concatenated at session start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.4-C-01 | Denial of Service | Model invokes end_call prematurely before caller's last utterance | mitigate | Two-step mechanics preserved; latency ceiling is AFTER the "speak, silence" steps — caller's farewell is still fully played |
| T-60.4-C-02 | Tampering | Prompt injection from caller audio tricks model into premature end_call | accept | Gemini Live's tool-call arbitration is not prompt-injectable from audio input; same threat model as 60.3 |

Prompt-only change; no new attack surface. LOW threat level.
</threat_model>

<verification>
- Extended test file has new latency-ceiling assertions and preserved invariants; all green after Task 2
- Full livekit-agent suite passes (≥ Plans 01+02 baseline + Plan 04 delta)
- grep confirms: revised directive present, legacy "separate turn" text gone, two-step mechanics and duration bounds preserved in both locales
- Railway auto-deploys the revised prompt
</verification>

<success_criteria>
- Stream C prompt-nudge shipped (D-C-02 evidence-picked fix #1 per RESEARCH Table)
- 60.3 Plan 03 mid-word-truncation protection preserved (two-step commitment intact)
- Plan 05 UAT can test whether the 16s stall is now < 2s
</success_criteria>

<output>
Create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-04-SUMMARY.md` with RED + GREEN commit SHAs, grep counts, test count delta, rationale for any deviation from the exact wording above.
</output>
