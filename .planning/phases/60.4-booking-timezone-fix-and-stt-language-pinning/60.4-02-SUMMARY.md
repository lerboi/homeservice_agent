---
phase: 60.4-booking-timezone-fix-and-stt-language-pinning
plan: 02
subsystem: voice-call-architecture
tags: [livekit-agent, gemini-live, realtime-model, bcp47, prompt-engineering]

requires:
  - phase: 60.3
    provides: "_build_language_section ES branch (Plan 12 locale parity closure)"
provides:
  - "RealtimeModel language kwarg wired from tenants.default_locale via _LOCALE_TO_BCP47 map (en→en-US, es→es-US, fallback en-US)"
  - "Anti-hallucination directive in _build_language_section (EN + ES parity) telling Gemini to treat unintelligible audio as the locale language"
  - "Railway log line `[60.4 Stream B] RealtimeModel language=...` per session for ops visibility"
affects: [60.4-03]

tech-stack:
  added: []
  patterns:
    - "BCP-47 locale-map helper with safe fallback for SDK kwargs"
    - "Paired SDK+prompt defense-in-depth for Gemini native-audio locale pinning"

key-files:
  created:
    - "livekit-agent/tests/test_agent_stt_language.py"
    - "livekit-agent/tests/test_prompt_language.py"
  modified:
    - "livekit-agent/src/agent.py"
    - "livekit-agent/src/prompt.py"

key-decisions:
  - "Extracted `_locale_to_bcp47()` as a module-level helper so tests import and assert directly (instead of mocking RealtimeModel)"
  - "Scope limited to en/es only (CONTEXT discipline); zh/ms/ta/vi fall back to en-US"

patterns-established:
  - "tenant.default_locale (DB) → _locale_to_bcp47 → RealtimeModel(language=...) + log"
  - "ANTI-HALLUCINATION section appended to every locale branch of _build_language_section"

requirements-completed: [D-B-01, D-B-02, D-B-03, D-X-01, D-X-04]

duration: ~15min
completed: 2026-04-23
---

# Plan 60.4-02: Stream B — STT Language Pinning Summary

**Pinned Gemini Live's input language via the plugin `language=` kwarg AND hardened _build_language_section with an anti-hallucination directive (EN + ES parity) to stop non-English tokens from appearing during silence.**

## Performance

- **Started:** 2026-04-23
- **Completed:** 2026-04-23
- **Tasks:** 2 (RED tests + GREEN implementation)
- **Files modified:** 2 source + 2 test files created

## Accomplishments

- `_LOCALE_TO_BCP47` map + `_locale_to_bcp47()` helper in `src/agent.py`
- `RealtimeModel(..., language=_locale_to_bcp47(locale), ...)` wired at L379
- Railway observability log at L370: `[60.4 Stream B] RealtimeModel language=en-US (locale=en)`
- EN branch of `_build_language_section` now carries the `ANTI-HALLUCINATION — CRITICAL` block
- ES branch carries mirrored `ANTI-ALUCINACIÓN — REGLA CRÍTICA` block (USTED register, 60.3 Plan 12 convention preserved)
- 13 new tests pass; full suite 228 passing (1 pre-existing VIP failure unchanged)

## Task Commits

1. **Task 1 (RED)**: 2 test files — RED commit (livekit-agent main)
2. **Task 2 (GREEN)**: `1df5223` — `fix(60.4): GREEN — Stream B language kwarg + anti-hallucination prompt directive (D-B-01, D-B-03)`

## Files Created/Modified

- `src/agent.py` — added `_LOCALE_TO_BCP47` map, `_locale_to_bcp47()` helper, Railway info log, `language=` kwarg on RealtimeModel
- `src/prompt.py` — extended `_build_language_section` with anti-hallucination blocks in both EN and ES branches
- `tests/test_agent_stt_language.py` — 9 tests (2 specific locales + 7-param fallback)
- `tests/test_prompt_language.py` — 4 tests (EN directive, ES directive, parity ≤30%, 60.3 defaults preserved)

## Decisions Made

- `_locale_to_bcp47` accepts `str | None` and returns the fallback for empty/unknown input — simplifies the agent.py call site.
- Anti-hallucination directive wording: "treat it as English that was unclear / trátelo como español poco claro" — symmetric phrasing makes future localization mechanical.

## Deviations from Plan

None — plan executed as written. Locale scope restricted to en/es per plan.

## Issues Encountered

None.

## Next Phase Readiness

- Stream A (Plan 01) and Stream B (Plan 02) both deployed to Railway as of commit `1df5223`.
- Plan 03 combined UAT can now test both fixes in a single call cycle (D-X-03 bundled-deploy pattern).
- Railway log line `[60.4 Stream B] RealtimeModel language=...` establishes the D-B-02 ops baseline.

---
*Phase: 60.4-booking-timezone-fix-and-stt-language-pinning*
*Completed: 2026-04-23*
