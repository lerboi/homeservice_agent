---
phase: 60.4-booking-timezone-fix-and-stt-language-pinning
plan: 01
subsystem: voice-call-architecture
tags: [livekit-agent, google-calendar, timezone, zoneinfo, pytest]

requires:
  - phase: 60.3
    provides: "_ensure_utc_iso helper at tool boundary + tenant.tenant_timezone plumbing in slot calculator"
provides:
  - "Google Calendar event body carries timeZone field + naive local ISO dateTime (Pitfall 1 fix)"
  - "All 4 tenant_timezone fallback sites switched from America/Chicago to UTC + structured WARN"
  - "_ensure_utc_iso emits [tz_coerce] WARN on naive input (never raises)"
  - "Unit test suite pinning event body shape + fallback behavior"
affects: [60.4-02, 60.4-03]

tech-stack:
  added: []
  patterns:
    - "Naive-local-ISO + explicit timeZone field for Google Calendar events (avoids double-offset)"
    - "[tenant_config] structured WARN on NULL tenant_timezone at every call-path boundary"

key-files:
  created:
    - "livekit-agent/tests/test_google_calendar_tz.py"
    - "livekit-agent/tests/test_tenant_timezone_fallback.py"
    - "livekit-agent/tests/test_ensure_utc_iso.py"
  modified:
    - "livekit-agent/src/lib/google_calendar.py"
    - "livekit-agent/src/lib/calendar_push.py"
    - "livekit-agent/src/tools/book_appointment.py"
    - "livekit-agent/src/tools/check_availability.py"
    - "livekit-agent/src/tools/check_caller_history.py"
    - "livekit-agent/src/post_call.py"

key-decisions:
  - "Added _to_naive_local_iso helper in google_calendar.py (not shared utils) to keep the Pitfall 1 context co-located with the one caller"
  - "Also fixed src/tools/check_caller_history.py:56 (4th site) to meet the grep acceptance criterion scoped to src/tools/"
  - "Added `logger = logging.getLogger(__name__)` to post_call.py (file had no logger defined; WARN would have NameError'd without it)"

patterns-established:
  - "Google Calendar event body shape: {start/end: {dateTime: naive_local_iso, timeZone: tenant_tz}}"
  - "NULL tenant_timezone fallback: UTC + logger.warning('[tenant_config] null tenant_timezone tenant_id=%s ...')"

requirements-completed: [D-A-01, D-A-02, D-A-03, D-A-04, D-A-05, D-A-06, D-X-01, D-X-04]

duration: ~30min
completed: 2026-04-23
---

# Plan 60.4-01: Stream A — Booking Timezone Hardening Summary

**Eliminated the +8h booking offset by plumbing tenant_timezone through book_appointment → calendar_push → google_calendar, setting timeZone field on events.insert() with naive-local-ISO dateTime, and replacing all America/Chicago silent fallbacks with UTC + structured WARN.**

## Performance

- **Started:** 2026-04-23
- **Completed:** 2026-04-23
- **Tasks:** 2 (RED tests + GREEN implementation)
- **Files modified:** 6 source + 3 test files created

## Accomplishments

- `push_booking_to_calendar()` now accepts `tenant_timezone` (3-layer plumbing: book_appointment → calendar_push → google_calendar)
- `_to_naive_local_iso()` helper converts offset-suffixed UTC ISO → naive local ISO for the tenant's TZ
- Google event body: `start.timeZone` + `end.timeZone` set to tenant_timezone; `dateTime` is naive local (NOT offset-suffixed — Pitfall 1)
- 4 fallback sites eliminated: `book_appointment.py:266`, `check_availability.py:142`, `post_call.py:612`, `check_caller_history.py:56` — all now fall back to UTC with `[tenant_config] null tenant_timezone` WARN
- `_ensure_utc_iso` emits `[tz_coerce] naive ISO at boundary` WARN on naive input (D-A-04 coerce-not-raise)
- 10 new unit tests (all pass); full suite still at 215 passing (1 pre-existing VIP-lead failure unrelated to this plan)

## Task Commits

1. **Task 1 (RED)**: 3 test files — `9c89c26` (test: 60.4 RED)
2. **Task 2 (GREEN)**: 6 source files — `c2482f8` (fix: 60.4 GREEN)

Both pushed to `livekit_agent` main; Railway auto-deploys.

## Files Created/Modified

- `src/lib/google_calendar.py` — added `_to_naive_local_iso()` + `tenant_timezone` param on `push_booking_to_calendar()` + timeZone on event body
- `src/lib/calendar_push.py` — forwarded `tenant_timezone` to `push_google`
- `src/tools/book_appointment.py` — passes `tenant_timezone` at caller site; UTC fallback at L266; `_ensure_utc_iso` WARN
- `src/tools/check_availability.py` — UTC fallback + WARN (L142)
- `src/tools/check_caller_history.py` — UTC fallback + WARN (L56; 4th site flagged in deviations)
- `src/post_call.py` — added missing logger import; UTC fallback + WARN (L612)
- `tests/test_google_calendar_tz.py` — 5 tests (incl. 3-param parametrize for SG/UTC/Chicago naive-local conversion)
- `tests/test_tenant_timezone_fallback.py` — 3 tests (one per site), assert UTC fallback + WARN substring
- `tests/test_ensure_utc_iso.py` — 2 tests (naive → WARN; offset-aware → passthrough)

## Decisions Made

- Used `logger.warning` at the `_ensure_utc_iso` naive branch rather than raising, per D-A-04 (never break a live call mid-flight).
- Kept `_to_naive_local_iso` in `google_calendar.py` (co-located with its sole caller).
- Tests for fallback sites use source-level string assertions (grep-style) rather than invoking the runtime tool (which requires a full supabase/LiveKit fixture harness); trade-off documented — cheaper + faithful to the structural fix.

## Deviations from Plan

### Auto-fixed Issues

**1. [Extra scope — acceptance criterion coverage] 4th fallback site in `check_caller_history.py:56`**
- **Found during:** Task 2 grep sweep for `or "America/Chicago"` in `src/tools/`
- **Issue:** Plan listed 3 fallback sites but `grep "or \"America/Chicago\"" src/tools/` returned a 4th (`check_caller_history.py:56`). The acceptance criterion "returns 0" would have failed.
- **Fix:** Applied the same UTC + `[tenant_config]` WARN pattern to site #4.
- **Files modified:** `src/tools/check_caller_history.py`
- **Committed in:** `c2482f8` (GREEN commit)

**2. [Missing import] `post_call.py` had no logger**
- **Found during:** Task 2 after writing the UTC fallback at L612
- **Issue:** `post_call.py` used no logger anywhere (grep returned 0 matches pre-edit). My WARN call would have raised `NameError: name 'logger' is not defined` at runtime.
- **Fix:** Added `import logging` and `logger = logging.getLogger(__name__)` at the top of the file.
- **Committed in:** `c2482f8` (GREEN commit)

---

**Total deviations:** 2 auto-fixed (1 extra site, 1 missing import)
**Impact on plan:** Both auto-fixes essential for correctness. No scope creep beyond structural TZ hardening. The 4th site was covered by the plan's "verify no other production fallbacks" instruction.

## Issues Encountered

- Pre-existing test failure: `tests/webhook/test_routes.py::test_incoming_call_vip_lead` fails on baseline too (confirmed via `git stash`). Unrelated to this plan. Memory note `project_vip_caller_routing.md` flags VIP routing as a future concern; the failing test is from that area.

## Grep Verification

- `grep -n "def _to_naive_local_iso" src/lib/google_calendar.py` → 1 match (L20)
- `grep -c '"timeZone":' src/lib/google_calendar.py` → 2 (start + end)
- `grep -rn 'or "America/Chicago"' src/tools/ src/post_call.py` → 0 matches ✓
- `grep -c "\[tenant_config\] null tenant_timezone" src/tools/book_appointment.py src/tools/check_availability.py src/post_call.py` → 1 per file (3 total) ✓
- `grep -n "\[tz_coerce\] naive ISO at boundary" src/tools/book_appointment.py` → 1 match (L74) ✓

## Test Suite Delta

- Before: 205 passed / 1 failed (VIP pre-existing) / 0 deselected
- After: 215 passed / 1 failed (same VIP pre-existing)
- Delta: +10 new tests, all GREEN; no regressions introduced.

## Next Phase Readiness

- Railway is redeploying `c2482f8`. Ready for Plan 02 (Stream B STT language pinning), then Plan 03 combined UAT per D-X-03 bundled-deploy pattern.
- SG tenant next call should see calendar event at caller's stated wall-clock time.

---
*Phase: 60.4-booking-timezone-fix-and-stt-language-pinning*
*Completed: 2026-04-23*
