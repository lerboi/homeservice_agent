---
phase: 60.4
plan: 03
type: execute
wave: 2
depends_on: [60.4-01, 60.4-02]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md
autonomous: false
decisions_addressed: [D-A-06, D-B-02, D-X-03]
must_haves:
  truths:
    - "A live UAT call on SG tenant +6587528516 booking a 3 PM SG slot results in a Google Calendar event whose UI-visible start time is 15:00 SGT (not 23:00 SGT, not 07:00 local)"
    - "The same UAT call includes a deliberate 10-15s silence window mid-call; post-call transcript shows measurably reduced non-English token rate vs the 2026-04-21 baseline UAT (which had DE/HI tokens)"
    - "Railway logs for this session show the Plan 02 `[60.4 Stream B] RealtimeModel language=en-US (locale=en)` line"
    - "UAT evidence captured in 60.4-HUMAN-UAT.md with call-id, timestamps, Google Calendar event screenshot/link, transcript excerpts, and owner verdict per stream"
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md"
      provides: "UAT evidence log — Stream A (3 PM SG booking) + Stream B (silence transcript)"
  key_links:
    - from: "Railway Streams A+B deployment"
      to: "live Google Calendar event"
      via: "UAT call on +6587528516"
      pattern: "15:00\\|3:00 PM\\|3 PM"
---

<objective>
Combined UAT for Streams A + B (D-X-03 bundled-deploy pattern). Validate that:

1. **Stream A (D-A-06):** A 3 PM SG booking lands at 3 PM SG in Google Calendar — not 11 PM SG. This is the core regression observed on 2026-04-21 and the primary customer-facing fix.

2. **Stream B (D-B-02 softened acceptance):** The non-English token rate in a deliberate silence window is measurably reduced compared to the 2026-04-21 baseline (which had German/Hindi hallucinations). 100% elimination is NOT required — Gemini native-audio ignores `language_code` per Google docs (RESEARCH §Stream B Native-Audio Blocker). Defense-in-depth acceptance.

3. **Bonus:** Railway logs for the UAT session contain the `[60.4 Stream B] RealtimeModel language=en-US (locale=en)` line (verifies kwarg wiring end-to-end, not just in unit tests).

Purpose: end-to-end validation of Plans 01 + 02 before Stream C work starts. Evidence captured in HUMAN-UAT.md.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-UAT preparation — scaffold 60.4-HUMAN-UAT.md + confirm Railway deploy succeeded</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md</files>
  <read_first>
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md (UAT log format; emulate structure),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-01-PLAN.md (Plan 01 acceptance — Stream A expected behavior),
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-02-PLAN.md (Plan 02 acceptance — Stream B expected behavior)
  </read_first>
  <action>
    Create `60.4-HUMAN-UAT.md` scaffolded with empty sections ready for the checkpoint task to populate:

    ```markdown
    # Phase 60.4 — Human UAT

    **Purpose:** Bundled Streams A + B UAT (Plan 03) and Stream C UAT (Plan 05).
    Decision refs: D-A-06, D-B-02, D-C-02, D-C-03, D-X-03.

    ---

    ## Stream A + B UAT #1 (Plan 03) — bundled, post-Plans-01/02 deploy

    **Railway deploy commit:** <paste Plan 02 GREEN commit SHA>
    **Railway deploy timestamp:** <ISO UTC>
    **Tenant:** +6587528516 (SG, Asia/Singapore = UTC+8)
    **UAT call-id:** <paste call-<id> from Railway logs>
    **Call start (UTC):** <ISO>
    **Call duration:** <seconds>
    **Caller flow:**
      1. Dial +6587528516
      2. Request a 3 PM SG slot (today or tomorrow, Asia/Singapore)
      3. Deliberately go silent for 10-15 seconds after address readback (mic hot)
      4. Speak again to confirm booking
      5. Natural end-of-call

    ### Stream A (D-A-06) — evidence

    - **Google Calendar event ID:** <paste from Railway `Calendar event created: <id>` log>
    - **Event start (UI visible time):** <paste>
    - **Expected:** 15:00 SGT (3 PM SG) = 07:00 UTC
    - **Actual:** <paste>
    - **Verdict:** ✅ GREEN / ❌ RED / ⚠️ FLAKY

    ### Stream B (D-B-02 softened) — evidence

    - **Silence window start (UTC):** <ISO — caller fell silent>
    - **Silence window end (UTC):** <ISO — caller spoke again>
    - **Transcript excerpt during silence window:**
      ```
      <paste from post-call transcript>
      ```
    - **Non-English token count in silence window:** <N>
    - **Baseline (2026-04-21 UAT, pre-fix):** German/Hindi tokens observed; rough count <M>
    - **Delta:** <N - M tokens, or "qualitative: no non-English tokens this call">
    - **Railway log — language kwarg visible:**
      ```
      <paste `[60.4 Stream B] RealtimeModel language=en-US (locale=en)` log line, or note ABSENT>
      ```
    - **Verdict:** ✅ GREEN (measurable reduction) / ⚠️ NO CHANGE (expected per native-audio blocker) / ❌ REGRESSED

    ### Overall Plan 03 verdict

    - [ ] Stream A GREEN
    - [ ] Stream B GREEN or at-least-neutral (no regression)
    - [ ] Railway log confirms `language` kwarg wiring
    - [ ] Decision: advance to Plan 04 (Stream C fix) / revise Plans 01/02 / defer

    ---

    ## Stream C UAT #1 (Plan 05) — to be populated after Plan 04 ships

    <empty for now>
    ```

    Also confirm Railway deployment completed by asking the user to paste the Plan 02 GREEN commit SHA from livekit-agent main and confirm Railway status is `SUCCESS` for that commit.
  </action>
  <verify>
    <automated>test -f C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md &amp;&amp; echo EXISTS</automated>
  </verify>
  <acceptance_criteria>
    - File `60.4-HUMAN-UAT.md` exists
    - File contains section headers: `## Stream A + B UAT #1 (Plan 03)`, `### Stream A (D-A-06)`, `### Stream B (D-B-02 softened)`, `### Overall Plan 03 verdict`
    - File contains placeholder for Stream C (Plan 05) UAT section
    - File references `+6587528516` as the UAT tenant
    - File references decision IDs D-A-06, D-B-02, D-C-02, D-C-03, D-X-03
  </acceptance_criteria>
  <done>UAT scaffold ready; checkpoint task unblocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (Checkpoint): Execute bundled Streams A + B UAT call and capture evidence</name>
  <what-built>
    Streams A + B shipped across Plans 01 + 02:
    - Google Calendar `events.insert()` body now sets `timeZone: tenant_timezone` with naive local `dateTime` (RESEARCH Pitfall 1 safe pattern)
    - `tenant_timezone` plumbed through `book_appointment → calendar_push → google_calendar`
    - All 3 null-timezone fallback sites now return `"UTC"` + WARN (instead of `"America/Chicago"` silently)
    - `_ensure_utc_iso` now emits `[tz_coerce] naive ISO at boundary` WARN on naive input (never raises)
    - `RealtimeModel` now constructed with `language="en-US"` / `"es-US"` from `_LOCALE_TO_BCP47` map
    - `_build_language_section` carries an ANTI-HALLUCINATION / ANTI-ALUCINACIÓN directive in both EN + ES
    - Railway auto-deployed; `[60.4 Stream B] RealtimeModel language=...` info log emits per session
  </what-built>
  <how-to-verify>
    **Preconditions:** Confirm Railway deployment for Plan 02's GREEN commit is `SUCCESS`. If not, wait for deploy before calling.

    1. Dial **+6587528516** from any phone.
    2. When the agent answers and asks how it can help, request a service appointment today or tomorrow at **3 PM SG** (15:00 Asia/Singapore).
    3. Provide a name, phone readback, and service address when prompted.
    4. **At the address-readback step**, deliberately stay silent for **10-15 seconds** with the mic hot (do NOT mute). Let the agent wait.
    5. After the silence, speak again (e.g. `"Yes, that address is correct"`) and confirm the booking.
    6. Allow the natural end-of-call flow (goodbye → end_call).

    **Post-call evidence collection:**

    A. **Stream A — Google Calendar event time check:**
       - Open the tenant's Google Calendar UI.
       - Find the newly-created event for the booking.
       - Confirm the event start time displays as **15:00 (3:00 PM) in Asia/Singapore**, not 23:00 / 11:00 PM.
       - Note the event ID from Railway logs (`[agent] Calendar event created: <id>`).

    B. **Stream B — transcript language token check:**
       - Pull the Railway post-call log or query `calls.transcript_structured` for this call-id.
       - Locate the 10-15s silence window (by timestamps).
       - Count non-English tokens/words in that window.
       - Compare to the 2026-04-21 baseline call (which had German `"Ich liebe dich"` / Hindi `"क्या हाल है"` hallucinations — see 60.4-CONTEXT § Stream B motivation).
       - Accepted outcome: **measurably reduced** — even zero tokens this call = success; a lower but non-zero rate = also success (D-B-02 softened).

    C. **Bonus — Railway log grep:**
       - Search Railway logs for this call-id for the line `[60.4 Stream B] RealtimeModel language=en-US (locale=en)`.
       - If present → Plan 02 kwarg wiring verified end-to-end.
       - If absent → Plan 02 regression; do NOT advance to Plan 04.

    **Populate `60.4-HUMAN-UAT.md`** with:
    - Railway deploy commit SHA + timestamp
    - UAT call-id, start time, duration
    - Google Calendar event ID + UI-visible start time
    - Transcript excerpt from silence window
    - Non-English token count + baseline comparison
    - Railway log line (paste or "ABSENT")
    - Per-stream verdict (GREEN / RED / FLAKY)
    - Overall Plan 03 verdict

    **Commit** the populated UAT file:
    ```
    cd C:/Users/leheh/.Projects/homeservice_agent
    git add .planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HUMAN-UAT.md
    git commit -m "docs(60.4-03): Stream A+B UAT #1 evidence"
    ```

    If Stream A verdict is ❌ RED (event landed at wrong time) → STOP; revise Plan 01 and redeploy before Plan 04 planning.
    If Stream B verdict is ❌ REGRESSED (non-English tokens MORE frequent than baseline) → STOP; revise Plan 02.
    If Stream A ✅ and Stream B ✅ or ⚠️ neutral → advance to Plan 04.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved` (both streams pass per acceptance; advance to Plan 04)
    - `A failed` (Stream A event landed at wrong time; revise Plan 01)
    - `B regressed` (Stream B transcript got worse; revise Plan 02)
    - `flaky` (one-off issue; re-run UAT)
    - `<free-form description of surprises>`
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live phone caller → Twilio → LiveKit → Gemini | Full production call path exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60.4-UAT-01 | Information Disclosure | Caller PII (phone, address) surfaced in UAT evidence | mitigate | 60.4-HUMAN-UAT.md uses the same phone-redaction convention as 60.3-HUMAN-UAT.md; owner reviews before commit |

No new attack surface. Manual UAT checkpoint.
</threat_model>

<verification>
- 60.4-HUMAN-UAT.md exists and is populated after the UAT call
- Stream A verdict recorded with Google Calendar event ID + UI-visible time
- Stream B verdict recorded with transcript excerpt + token count vs baseline
- Railway log line captured (kwarg wiring proof)
- Plan 03 commit pushed
</verification>

<success_criteria>
- Stream A ✅ — 3 PM SG booking lands at 15:00 SGT in Google Calendar (the primary customer-facing regression closed)
- Stream B ✅ or ⚠️ — transcript language is at least no worse than baseline; measurable reduction is ideal; neutral is acceptable per D-B-02 softened acceptance
- Evidence committed to 60.4-HUMAN-UAT.md
- Decision to advance/revise captured
</success_criteria>

<output>
Populated `60.4-HUMAN-UAT.md` with §"Stream A + B UAT #1 (Plan 03)" fully filled; committed to homeservice_agent main. No separate SUMMARY.md for this plan (evidence lives in the UAT file); Plan 03 rollup subsumed into final phase SUMMARY at Plan 06.
</output>
