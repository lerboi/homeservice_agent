---
phase: 63.2
parent_phase: 63.1 (current livekit-agent/main tip — post-Phase-64 revert at `61a2e6e fix(63.1-11)`)
status: ready-for-planning
created: 2026-05-05
source: conversational research (2026-05-05) — upstream PyPI release notes for 1.5.7 (2026-04-30) + Explore-agent surface audit of livekit-agent/src/ against the two 1.5.7 fixes; no /gsd:discuss-phase round run (decisions locked inline given the narrow patch surface).
---

# Phase 63.2: LiveKit SDK patch upgrade to 1.5.7 — Context

**Gathered:** 2026-05-05
**Status:** Ready for planning

<domain>
## Phase Boundary

Pure patch-version migration in `C:\Users\leheh\.Projects\livekit-agent\` (cross-repo, same workflow as Phases 60.2 / 60.3 / 60.4 / 63 / 63.1). Bump the four livekit-* pins from `==1.5.6` (Phase 63 baseline) to `==1.5.7` (released 2026-04-30). No feature additions. No prompt edits. No tool-signature changes. No homeservice_agent-repo code changes (planning artifacts only).

**1.5.7 release contents (per upstream PyPI changelog):**
1. `fix(gemini live): use parameters instead of parameters_json_schema for raw schema function tools`
2. `fix: realtime reply generation after interruption`
3. Plus OpenAI-side and unrelated TTS/STT fixes irrelevant to this stack.

**Audit finding (2026-05-05 Explore-agent sweep of livekit-agent/src/):**
- **Fix #1 does not apply.** All four `@function_tool(raw_schema=...)` call sites (`book_appointment.py:251`, `check_slot.py:70`, `check_day.py:53`, `next_available_days.py:43`) use the standard JSON Schema `parameters` key. Zero references to `parameters_json_schema` anywhere in `src/`. The fix takes effect transparently or not at all.
- **Fix #2 has no code dependency.** The agent has no custom interruption-recovery path. Only one interruption-related reference exists: a diagnostic `@session.on("agent_false_interruption")` log handler at `src/agent.py:582-586`. Reply-after-interruption is fully delegated to Gemini server VAD + the SDK's internal handling. The fix may marginally improve barge-in resume behavior but no edits are forced.

**Honest framing:** This is a **hygiene patch bump**, not a behavior fix. The two relevant fixes don't change observable behavior in this stack. The motivations to ship anyway are (a) staying current with upstream patch fixes for safety / bug-class reduction we don't yet know about, and (b) maintaining the ~30-min ship muscle-memory before the next material upgrade (1.5.8 / 1.6 — whichever lands the SegmentSynchronizer cutoff-race fix that's still owed).

**Definition of "done":**
1. `livekit-agent/pyproject.toml` pins all four packages at `==1.5.7`.
2. `src/agent.py` `RealtimeModel(...)` constructs successfully on boot — no `TypeError`, no `ValidationError`, no capability-mismatch boot crash. Zero `src/` edits forced.
3. Gemini 3.1 Flash Live (`gemini-3.1-flash-live-preview`) is the active model (verified in Railway log).
4. All 6 in-process tools (`check_availability` / 3 sub-tools, `book_appointment`, `capture_lead`, `check_caller_history`, `check_customer_account`, `transfer_call`, `end_call`) still register and can be invoked by Gemini.
5. Existing Python test suite passes (254 expected pass, 1 pre-existing tolerated VIP failure unchanged). Grep-guard `tests/test_no_generate_reply_in_src.py` stays GREEN.
6. One UAT call on the SG tenant `+14783755631 / +6587528516` successfully completes a booking end-to-end (`check_availability` → `book_appointment` → Google Calendar event).

**Out of scope — LOCKED:**
- No feature additions.
- No edits to `src/prompt.py`, `src/tools/*.py`, `src/agent.py`, or any homeservice_agent-repo code file.
- No SegmentSynchronizer cutoff-race fix attempt — still byte-identical at 1.5.7 per upstream notes; remains a separate future phase.
- No `server cancelled tool calls` warning investigation (Phase 63.1 follow-up; not addressed by 1.5.7).
- No multi-agent / handoff support work — upstream still flags this as "long-term fix being investigated" for Gemini 3.1.
- No canary-call harness (Phase 63 deferred).
- No rollback-of-Phase-64 cleanup work — that revert shipped as `livekit-agent` commit `d4a1ee1` on 2026-04-25 and is settled.

</domain>

<decisions>
## Implementation Decisions

### D-01: Target pins — all four at `==1.5.7`, released 2026-04-30.
Confirmed via PyPI lookup (2026-05-05). `livekit-agents`, `livekit-plugins-google`, `livekit-plugins-silero`, `livekit-plugins-turn-detector` all have a 1.5.7 wheel published on the same date. No newer release exists as of 2026-05-05.

Pins to update in `livekit-agent/pyproject.toml` (lines 9-12 today):
- `livekit-agents==1.5.6` → `livekit-agents==1.5.7`
- `livekit-plugins-google==1.5.6` → `livekit-plugins-google==1.5.7`
- `livekit-plugins-silero==1.5.6` → `livekit-plugins-silero==1.5.7`
- `livekit-plugins-turn-detector==1.5.6` → `livekit-plugins-turn-detector==1.5.7`

The 2-line comment block above the pins (lines 7-8) is also updated to reference 1.5.7 + the two upstream fixes + this phase folder.

### D-02: Execute on a feature branch — never directly on `livekit-agent/main`.
Branch name: `phase-63.2-livekit-1.5.7`. Cut from `livekit-agent/main` HEAD at execute time (post-Phase-64-revert tip — currently `61a2e6e fix(63.1-11)` per ROADMAP.md line 1444). Merge to main only after:
(a) local clean reinstall + `pytest` passes,
(b) one Railway preview deploy runs cleanly (agent boots, worker registers, no `TypeError` at `RealtimeModel(...)` construction),
(c) one UAT call succeeds end-to-end.

**Rationale:** Same as Phase 63 D-02. A boot-time breakage on `main` crash-loops the Railway service, taking the tenant's inbound number offline. Branch-first is non-negotiable even for a patch bump — patch releases occasionally break things upstream didn't notice.

### D-03: Pure version bump, no opportunistic refactors.
`pyproject.toml` change is the only code edit. **Zero `src/` edits authorized**, including "while we're in here" cleanups, prompt edits, telemetry additions, or speculative interruption-handling code that might "use" the 1.5.7 reply-after-interruption fix. Per audit: no edit is required.

### D-04: Preserve all Phase 60.4 + Phase 63 + Phase 63.1 commits on the upgrade branch.
Branch is cut from `livekit-agent/main` HEAD which already contains all of these. None should be reverted, rebased out, or refactored. Verification at merge time: `git log --oneline` on post-merge main shows the same set of commits as pre-merge `origin/main` plus the new patch-bump commit and the merge commit.

Specifically (the 9 D-08 SHAs from Phase 63.1 preservation audit, plus the Phase 64 revert commit):

| SHA | Source phase | Subject |
|-----|--------------|---------|
| `c2482f8` | 60.4-01 | Stream A TZ hardening |
| `1df5223` | 60.4-02 | Stream B STT language pinning |
| `b46851b` | 60.4 | Slot-cache prefetch |
| `5e48273` | 60.4 | slot_token structural handoff |
| `e580f14` | 60.4 | Strip hallucinated slot example |
| `68828d7` | 60.4 | Gemini 3 sampling alignment |
| `87d6883` | 60.4 | voice_resolved diagnostic log |
| `38352f2` | 63-01 | Pin bump to 1.5.6 |
| `9ce12d6` | 63-01 | Phase 63 merge commit |
| `bc4befd` | 63.1 | Phase 63.1 merge commit |
| `61a2e6e` | 63.1-11 | (post-Phase-64-revert main tip) |
| `d4a1ee1` | 64 | Phase 64 revert commit (preserves history above the revert) |

### D-05: Audit-driven zero-`src/`-edit prediction is the explicit thesis.
The Explore-agent surface audit (2026-05-05) confirmed:
- All `@function_tool` raw schemas use `parameters` (standard) not `parameters_json_schema` (deprecated path the 1.5.7 fix targets).
- No custom `on_user_started_speaking` / `handle_interruption` / interruption-recovery code in `src/`.
- `RealtimeModel(...)` kwargs at `src/agent.py:433-443` (`model`, `voice`, `language`, `instructions`, `realtime_input_config`, `thinking_config`) are all unchanged in 1.5.7 per upstream changelog.
- `AgentSession(llm=model, tts=greeting_tts)` at `src/agent.py:474` — no interruption-related kwargs.
- No Dockerfile or `livekit.toml` version pin to update — Dockerfile delegates to `pyproject.toml` via `pip install --no-cache-dir .`.

If any `src/` edit becomes forced during preflight (Task 2), STOP and escalate — that would be an Assumption-violation requiring Phase 63.2 to either grow scope or split into 63.2 + 63.2.1. The thesis is zero forced edits.

### D-06: Pre-existing tolerated test failure — unchanged.
`tests/webhook/test_routes.py::test_incoming_call_vip_lead` is expected to fail (tracked since Phase 60.3-01, see memory `project_vip_caller_routing.md`). 254 pass + 1 tolerated fail = green. Any other failure is a blocker.

### D-07: Grep-guard regression test — must stay GREEN.
`tests/test_no_generate_reply_in_src.py` (added in Phase 63.1) is the canary against silent-capability-drop bugs. If 1.5.7 introduces any new capability gate that affects this stack, the grep-guard's coverage class is the place a follow-up grep-guard would live. For 63.2 itself, no additional grep-guards are added — the 1.5.7 fixes don't introduce new capability boundaries.

### D-08: SegmentSynchronizer cutoff race + tool-call cancellation warnings — observational only.
Same posture as Phase 63 D-09 gate 8. These are NOT merge blockers for 63.2. They are observational: count occurrences during the UAT call and record the count in HUMAN-UAT.md for trend tracking. No fix is in scope.

### D-09: Resume signal — `merge` or `abort` from user.
Per Phase 63 D-09 / Phase 63.1 D-09 pattern. The plan halts at the Railway-preview-deployed checkpoint. User dials the tenant number, observes the booking flow, and posts:
- `merge` — Railway SUCCESS + UAT booking confirmed + calendar event created + zero `TypeError`/`ValidationError`/`AttributeError`.
- `abort` — any of the above failed; rollback by deleting the local + remote branch (no merge happened, nothing to revert on main).

### D-10: Rollback discipline — branch-only, no force-push, no main reset.
If `abort`: delete `phase-63.2-livekit-1.5.7` locally + on origin. Main is untouched (the branch was never merged). The four pins remain at 1.5.6 (Phase 63 baseline).

If `merge` was issued but a regression surfaces post-merge: file revert via a forward `git revert <merge-sha>` commit on a new branch + Railway preview + UAT, NOT a force-push to main. Same rollback shape Phase 64 used (commit `d4a1ee1`).

### D-11: Skill-doc sync — append-only.
After merge, append a Phase 63.2 subsection to `homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md`. Phase 63 / 63.1 / 64-revert prose is preserved byte-identical. Subsection records: pin bump 1.5.6 → 1.5.7, the two upstream fixes (and that neither applies to this stack), the audit-confirmed zero-`src/`-edits result, the date, the merge SHA.

</decisions>
