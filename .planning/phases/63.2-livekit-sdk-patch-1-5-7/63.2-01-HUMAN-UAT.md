# Phase 63.2 Plan 01 — HUMAN-UAT

**Plan:** `63.2-01` — LiveKit SDK patch upgrade to 1.5.7
**Scaffold created:** 2026-05-05 (CONTEXT/PLAN draft phase — pre-execution)
**Executor preflight populated:** 2026-05-05T11:15:25Z (Task 3)
**Status:** Awaiting Railway preview deploy + UAT call (user verdict)

---

## Branch + Commit Metadata

- **Branch:** `phase-63.2-livekit-1.5.7`
- **Branch pushed to origin:** YES — 2026-05-05T11:15:25Z
- **Remote URL:** https://github.com/lerboi/livekit_agent/tree/phase-63.2-livekit-1.5.7
- **Head commit SHA (branch tip):** `8850b4f` — `fix(63.2): bump livekit-* pins to 1.5.7 patch release`
- **Branch cut from:** `187d207` (`updated` — current livekit-agent/main HEAD at cut time, post-Phase-61.1 follow-up commits)

> Note: PLAN/CONTEXT cited the cut-from SHA as `61a2e6e` (the post-Phase-64-revert tip recorded at plan-authoring time on 2026-05-05). Between plan-authoring and execution, `livekit-agent/main` advanced through the Phase-61.1 follow-up commits (`58747fb`, `622d621`, `904843c`, `11d7a24`, `c0faf75`, `cf3827b`, `14553a6`, `19cfea6`, `187d207 updated`). All 12 preserved SHAs from D-04 still present in the lineage; cut-from is `187d207` per actual main tip.

### Phase 60.4 + 63 + 63.1 + 64-revert commits preserved on branch (D-04)

All 12 verified present via `git log --oneline <sha> -1`:

| SHA | Source | Subject | Verified? |
|-----|--------|---------|:---:|
| `c2482f8` | 60.4-01 | fix(60.4): GREEN — Stream A TZ hardening (timeZone field + UTC fallback + _ensure_utc_iso WARN) | ✅ |
| `1df5223` | 60.4-02 | fix(60.4): GREEN — Stream B language kwarg + anti-hallucination prompt directive (D-B-01, D-B-03) | ✅ |
| `b46851b` | 60.4 | fix: prefetch scheduling data at session init; cache in deps[_slot_cache] | ✅ |
| `5e48273` | 60.4 | fix: structural slot_token handoff between check_availability and book_appointment | ✅ |
| `e580f14` | 60.4 | fix: strip hallucinated slot_token example + add _last_offered_token fallback | ✅ |
| `68828d7` | 60.4 | fix: Gemini 3 sampling alignment + anti-hallucination prompt hoist | ✅ |
| `87d6883` | 60.4 | diag: log voice resolution at session init | ✅ |
| `38352f2` | 63-01 | fix(63): bump livekit-* pins to 1.5.6 mainline, drop A2A_ONLY_MODELS git pin | ✅ |
| `9ce12d6` | 63-01 | fix(63): merge 1.5.6 mainline upgrade | ✅ |
| `bc4befd` | 63.1 | merge(63.1): generate_reply regression fix | ✅ |
| `61a2e6e` | 63.1-11 | fix(63.1-11): raise VAD silence_duration_ms 1500 -> 2500ms | ✅ |
| `d4a1ee1` | 64 | revert: undo Phase 64 — restore Phase 63.1 Realtime model state | ✅ |

---

## Local Preflight (Task 2) — Executor Captured

### `pip show` (all four resolved from site-packages, NOT git-builds)

```
--- livekit-agents ---
Name: livekit-agents
Version: 1.5.7
Location: C:\Users\leheh\AppData\Roaming\Python\Python313\site-packages

--- livekit-plugins-google ---
Name: livekit-plugins-google
Version: 1.5.7
Location: C:\Users\leheh\AppData\Roaming\Python\Python313\site-packages

--- livekit-plugins-silero ---
Name: livekit-plugins-silero
Version: 1.5.7
Location: C:\Users\leheh\AppData\Roaming\Python\Python313\site-packages

--- livekit-plugins-turn-detector ---
Name: livekit-plugins-turn-detector
Version: 1.5.7
Location: C:\Users\leheh\AppData\Roaming\Python\Python313\site-packages
```

### Import sanity

- `from src.agent import entrypoint` → ✅ `entrypoint import OK`
- `from src.prompt import build_system_prompt` → ✅ `prompt import OK`
- `from livekit.plugins import google` + `from livekit.agents import function_tool, RunContext, AgentSession` → ✅ `livekit imports OK`
- `from google.genai import types` (RealtimeInputConfig + ThinkingConfig + AutomaticActivityDetection present) → ✅ `google-genai types OK`

### RealtimeModel boot smoke

```
'gemini-3.1-flash-live-preview' has limited mid-session update support. instructions, chat context, and tool updates will not be applied until the next session.
RealtimeModel construction OK on 1.5.7: RealtimeModel
```

The capability-hint console message (limited mid-session update support) is the same benign warning observed in Phase 63 preflight — confirms PR #5413 capability-based routing remains active on 1.5.7. **NOT** an error.

### Pytest

`python -m pytest tests/ --ignore=tests/test_check_availability_slot_cache.py --ignore=tests/test_slot_token_handoff.py`

- **Summary line:** `9 failed, 284 passed, 2 warnings in 20.85s`
- **Tolerated VIP failure** (`tests/webhook/test_routes.py::test_incoming_call_vip_lead`): present (D-06 — pre-existing per memory `project_vip_caller_routing.md`).

**Pre-existing baseline failures unchanged from 1.5.6 → 1.5.7** (verified by rolling back to 1.5.6 and re-running the same 5 test files — identical 9 failures). All 9 failures are NOT 1.5.7 regressions; they are state drift relative to the plan's "254 passed + 1 failed" expectation, which was authored against the post-60.4 codebase. The codebase has since advanced through Phase 61, Phase 61.1, and the Phase 64 revert.

The 9 baseline failures (out of scope per Rule 1 scope boundary):
1. `tests/test_prompt_booking.py::test_en_availability_contract`
2. `tests/test_prompt_booking.py::test_es_availability_contract`
3. `tests/test_prompt_booking.py::test_both_locales_onboarding_gated_full_protocol`
4. `tests/test_prompt_outcome_words.py::test_en_reserved_words_enumerated`
5. `tests/test_prompt_outcome_words.py::test_es_reserved_words_enumerated`
6. `tests/test_prompt_outcome_words.py::test_en_failure_mode_3pm_example`
7. `tests/test_prompt_tool_narration.py::test_es_mentions_tool_names`
8. `tests/test_tenant_timezone_fallback.py::test_check_availability_null_tz_falls_back_to_UTC_with_warn`
9. `tests/webhook/test_routes.py::test_incoming_call_vip_lead` (the one D-06 expected)

**2 collection-error files** (also pre-existing on 1.5.6 — verified):
- `tests/test_check_availability_slot_cache.py` — imports `src.tools.check_availability` (split into `check_slot/check_day/next_available_days` per `9f8a2c2 refactor(tools)`)
- `tests/test_slot_token_handoff.py` — same import path

These two files were never updated when the module was split; pre-existing, not 1.5.7-induced.

### Grep-guard (D-07)

`python -m pytest tests/test_no_generate_reply_in_src.py -v`

```
tests/test_no_generate_reply_in_src.py::test_no_active_generate_reply_calls_in_src PASSED [100%]
============================== 1 passed in 0.05s ==============================
```

✅ GREEN — Phase 63.1 grep-guard remains valid; 63.2 introduces zero new `generate_reply` call sites.

### Zero-`src/`-edit thesis (D-05)

```
$ git diff --stat origin/main..HEAD
 pyproject.toml | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)
$ git diff --name-only origin/main..HEAD
pyproject.toml
```

✅ Only `pyproject.toml` changed. Zero `src/` files modified. D-05 thesis confirmed.

### Zero `TypeError` / `ValidationError` / `AttributeError` anywhere in preflight output? **YES — confirmed clean.**

**Evidence files (executor):**
- `/tmp/63.2-01-pip-show.txt`
- `/tmp/63.2-01-pytest.txt`
- `/tmp/63.2-01-realtime-smoke.txt`

---

## Railway Preview Deploy — User-verified

- **Deploy triggered by:** `git push -u origin phase-63.2-livekit-1.5.7` at 2026-05-05T11:15:25Z
- **Deploy status:** _(fill SUCCESS / FAILED)_
- **Deploy URL:** _(fill from Railway dashboard)_
- **`registered worker` log line observed?** _(fill yes/no)_
- **Boot-time `TypeError`/`ValidationError`/`AttributeError` in deploy logs?** _(fill yes/no — must be no for merge)_

Dashboard location: Railway → `voco-livekit-agent` service → Deployments → phase-63.2-livekit-1.5.7

---

## UAT Call Evidence — User-verified

**Dial from personal phone:** +14783755631 (SG tenant +6587528516)

### Call metadata

- **Call ID (Twilio or Railway log):** _(fill)_
- **Duration (seconds):** _(fill)_
- **Caller number:** redacted
- **Callee number:** +14783755631
- **Outcome:** _(fill — booking confirmed / capture_lead / abort / other)_
- **Locale tested:** _(fill — en-US / es-419 / both)_

### Tool chain evidence

- **Agent spoke first (D-09 #2 from Phase 63.1)?** _(fill yes/no)_
- **`check_availability` fired?** _(fill yes/no — visible in tool_call_log_tail)_
- **`book_appointment` fired?** _(fill yes/no — booking confirmed by agent on call)_
- **Google Calendar event created in tenant calendar?** _(fill yes/no)_
- **Tool chain log tail excerpt (3-5 lines):** _(paste sanitized excerpt — redact PII)_

### Negative-signal counts (D-08 observational)

- **`_SegmentSynchronizerImpl.playback_finished text_done=False` warning count:** _(fill — expected non-zero, NOT a blocker)_
- **`server cancelled tool calls` warning count:** _(fill — expected non-zero, NOT a blocker)_
- **`generate_reply is not compatible` log line count:** _(fill — must be 0; Phase 63.1 grep-guard ensures this)_
- **Any new warning class not seen in Phase 63 / 63.1 UAT calls?** _(fill — describe if yes; could indicate a 1.5.7 regression)_

---

## Resume Signal

Post `merge` or `abort` to the executor.

`merge` is valid only if:
- Railway deploy SUCCESS
- `registered worker` observed
- Agent spoke first
- `check_availability` fired AND `book_appointment` fired
- Calendar event created
- Zero `TypeError`/`ValidationError`/`AttributeError` in logs

`abort` if any of the above failed. Rollback per D-10 (delete branch, no main reset).

**User verdict:** _(fill `merge` or `abort`)_
**Verdict timestamp:** _(fill)_
**Verdict notes:** _(fill — anything the executor needs to know for SUMMARY)_
