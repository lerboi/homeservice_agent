---
phase: 29-hero-section-interactive-demo
plan: 04
type: execute
wave: 3
depends_on:
  - 29-01
  - 29-03
files_modified:
  - src/app/components/landing/HeroSection.jsx
  - .claude/skills/public-site-i18n/SKILL.md
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05

must_haves:
  truths:
    - "Visitor sees a business name input in the hero section instead of CTA buttons"
    - "Input bar transitions to audio player after clicking Listen to Your Demo"
    - "Full demo audio plays with business name dynamically inserted"
    - "Hero title is shorter with responsive rotating text width"
    - "Audio loads and begins playing within 3 seconds of button click"
    - "public-site-i18n skill file reflects the new hero architecture"
  artifacts:
    - path: "src/app/components/landing/HeroSection.jsx"
      provides: "Complete hero with demo input/player wired in"
      contains: "HeroDemoInput"
    - path: ".claude/skills/public-site-i18n/SKILL.md"
      provides: "Updated skill file reflecting Phase 29 hero changes"
      contains: "HeroDemoInput"
  key_links:
    - from: "src/app/components/landing/HeroSection.jsx"
      to: "src/app/components/landing/HeroDemoInput.jsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*HeroDemoInput"
    - from: "src/app/components/landing/HeroDemoInput.jsx"
      to: "src/app/components/landing/HeroDemoPlayer.jsx"
      via: "state transition from input to player"
      pattern: "HeroDemoPlayer"
---

<objective>
Wire HeroDemoInput and HeroDemoPlayer into HeroSection, creating the complete interactive demo flow. Update the public-site-i18n skill file.

Purpose: This is the integration plan that connects all previous plans into a working end-to-end experience. The input/player are wired into HeroSection via dynamic import, a wrapper component manages the input->player transition, and the skill file is updated per CLAUDE.md requirements.

Output: Complete hero section with interactive demo, updated skill file.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-hero-section-interactive-demo/29-CONTEXT.md
@.planning/phases/29-hero-section-interactive-demo/29-UI-SPEC.md
@.planning/phases/29-hero-section-interactive-demo/29-RESEARCH.md
@.planning/phases/29-hero-section-interactive-demo/29-01-SUMMARY.md
@.planning/phases/29-hero-section-interactive-demo/29-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 — HeroSection has a placeholder div for demo input -->
From src/app/components/landing/HeroSection.jsx (after Plan 01):
```jsx
// HeroSection is a Server Component
// Contains: Spline 3D bg, gradient overlays, h1 with RotatingText, subtitle
// Has placeholder: <div className="mt-8 pointer-events-auto"> for demo input
export function HeroSection()
```

<!-- From Plan 03 — Component contracts -->
From src/app/components/landing/HeroDemoInput.jsx:
```jsx
'use client';
export function HeroDemoInput({ onAudioReady })
// onAudioReady receives { audioBuffers: ArrayBuffer[] }
```

From src/app/components/landing/HeroDemoPlayer.jsx:
```jsx
'use client';
export function HeroDemoPlayer({ audioBuffers })
// audioBuffers: ArrayBuffer[] in order [intro, name, mid, outro]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire HeroDemoInput and HeroDemoPlayer into HeroSection with transition</name>
  <files>src/app/components/landing/HeroSection.jsx</files>
  <read_first>
    - src/app/components/landing/HeroSection.jsx (current state after Plan 01 — has placeholder div)
    - src/app/components/landing/HeroDemoInput.jsx (component contract — onAudioReady prop)
    - src/app/components/landing/HeroDemoPlayer.jsx (component contract — audioBuffers prop)
    - .planning/phases/29-hero-section-interactive-demo/29-UI-SPEC.md (Interaction States: state machine, Animation Contract: transitions)
    - .planning/phases/29-hero-section-interactive-demo/29-CONTEXT.md (D-12, D-13 — player replaces input in-place)
  </read_first>
  <action>
    Create a wrapper client component `HeroDemoBlock` that manages the input-to-player transition, then wire it into HeroSection via `next/dynamic`.

    **Option A (preferred): Inline the wrapper in a new file.**

    Create `src/app/components/landing/HeroDemoBlock.jsx`:
    ```jsx
    'use client';
    import { useState } from 'react';
    import { HeroDemoInput } from './HeroDemoInput';
    import dynamic from 'next/dynamic';

    // Lazy-load player (uses Web Audio API — no SSR)
    const HeroDemoPlayer = dynamic(
      () => import('./HeroDemoPlayer').then(m => m.HeroDemoPlayer),
      { ssr: false }
    );

    export function HeroDemoBlock() {
      const [audioBuffers, setAudioBuffers] = useState(null);

      function handleAudioReady({ audioBuffers: buffers }) {
        setAudioBuffers(buffers);
      }

      return (
        <div className="relative">
          {!audioBuffers ? (
            <div className="animate-in fade-in duration-200">
              <HeroDemoInput onAudioReady={handleAudioReady} />
            </div>
          ) : (
            <div className="animate-in fade-in slide-in-from-bottom-2 duration-200">
              <HeroDemoPlayer audioBuffers={audioBuffers} />
            </div>
          )}
        </div>
      );
    }
    ```

    The transition is handled by Tailwind's `animate-in` utilities: when `audioBuffers` becomes non-null, React unmounts HeroDemoInput and mounts HeroDemoPlayer with a fade-in + slide-up animation. This satisfies D-12 (input transitions to player) and D-13 (same position, same footprint).

    **Then update HeroSection.jsx:**

    1. Add dynamic import at the top (after existing dynamic imports):
       ```javascript
       const HeroDemoBlock = dynamic(
         () => import('./HeroDemoBlock').then(m => m.HeroDemoBlock),
         { ssr: false }
       );
       ```

    2. Replace the placeholder `<div className="mt-8 pointer-events-auto">` with:
       ```jsx
       <div className="mt-8 pointer-events-auto">
         <HeroDemoBlock />
       </div>
       ```

    3. Remove the `Button` import if it was re-added, and ensure no unused imports remain.

    **Files modified:** `src/app/components/landing/HeroSection.jsx` and new file `src/app/components/landing/HeroDemoBlock.jsx`.

    NOTE: Update the `files_modified` in frontmatter mentally — `HeroDemoBlock.jsx` is a new file created by this task.
  </action>
  <verify>
    <automated>cd "C:/Users/leheh/.Projects/homeservice_agent" && node -e "const fs=require('fs'); const hero=fs.readFileSync('src/app/components/landing/HeroSection.jsx','utf8'); const block=fs.readFileSync('src/app/components/landing/HeroDemoBlock.jsx','utf8'); const heroChecks=['HeroDemoBlock','dynamic']; const blockChecks=['use client','HeroDemoInput','HeroDemoPlayer','audioBuffers','onAudioReady','handleAudioReady','ssr: false']; const hm=heroChecks.filter(s=>!hero.includes(s)); const bm=blockChecks.filter(s=>!block.includes(s)); if(hm.length||bm.length){console.error('Hero MISSING:',hm,'Block MISSING:',bm);process.exit(1)}else{console.log('Wiring complete')}"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/components/landing/HeroDemoBlock.jsx` exists with `'use client'`
    - HeroDemoBlock manages `audioBuffers` state, renders HeroDemoInput when null, HeroDemoPlayer when populated
    - HeroDemoPlayer loaded via `dynamic()` with `ssr: false`
    - HeroSection.jsx imports HeroDemoBlock via `dynamic()`
    - HeroDemoBlock rendered inside the `mt-8 pointer-events-auto` div
    - Transition uses `animate-in fade-in` for input exit and player entrance
    - No unused imports in HeroSection.jsx
  </acceptance_criteria>
  <done>HeroSection renders the complete interactive demo: business name input that transitions to audio player on submit.</done>
</task>

<task type="auto">
  <name>Task 2: Update public-site-i18n skill file</name>
  <files>.claude/skills/public-site-i18n/SKILL.md</files>
  <read_first>
    - .claude/skills/public-site-i18n/SKILL.md (current skill file — need to understand structure and what to update)
    - src/app/components/landing/HeroSection.jsx (final state after Task 1)
    - src/app/components/landing/HeroDemoBlock.jsx (new component)
    - src/app/components/landing/HeroDemoInput.jsx (new component)
    - src/app/components/landing/HeroDemoPlayer.jsx (new component)
    - src/app/api/demo-voice/route.js (new API route)
  </read_first>
  <action>
    Update `.claude/skills/public-site-i18n/SKILL.md` to document the Phase 29 hero section changes. Add/update the following sections:

    1. **Update "Last updated" line** to: `Phase 29 — hero section interactive demo: business name input, ElevenLabs TTS demo player, RotatingText dynamic width`

    2. **Update Architecture Overview table** to add new files:
       - `HeroDemoBlock` | `src/app/components/landing/HeroDemoBlock.jsx` | Client wrapper managing input-to-player state transition
       - `HeroDemoInput` | `src/app/components/landing/HeroDemoInput.jsx` | Client component: business name input + loading state + auth-aware skip link
       - `HeroDemoPlayer` | `src/app/components/landing/HeroDemoPlayer.jsx` | Client component: waveform player with Web Audio API playback + post-play CTA
       - `Demo Voice API` | `src/app/api/demo-voice/route.js` | POST: ElevenLabs TTS for dynamic business name segment
       - `Static Audio` | `public/audio/demo-{intro,mid,outro}.mp3` | Pre-rendered ElevenLabs demo conversation segments

    3. **Update the Architecture Overview flow** to include the demo path:
       ```
       Hero Demo Flow:
         HeroSection (Server) -> HeroDemoBlock (Client, dynamic ssr:false)
           -> HeroDemoInput: user enters name -> POST /api/demo-voice + fetch static MP3s
           -> HeroDemoPlayer: Web Audio API stitches [intro, name, mid, outro] -> plays with waveform
       ```

    4. **Update File Map** to add all new files with their roles.

    5. **Update HeroSection description** in any existing section referencing it:
       - Old: references AuthAwareCTA, Watch Demo button, eyebrow pill, social proof
       - New: references HeroDemoBlock, input bar, audio player, shorter title with RotatingText dynamic width

    6. **Add RotatingText width change note:** "RotatingText now uses `useRef` + `getBoundingClientRect()` + `useLayoutEffect` for dynamic width measurement instead of invisible sizer span. Width transitions with `transition-[width] duration-200`."

    7. **Add env vars section** (or update existing):
       - `ELEVENLABS_API_KEY` — ElevenLabs TTS API key (server-side only)
       - `ELEVENLABS_VOICE_ID_AI` — Voice ID for AI receptionist
       - `ELEVENLABS_VOICE_ID_CALLER` — Voice ID for caller (pre-render only)

    Keep all existing content that is still accurate. Only update/add what changed.
  </action>
  <verify>
    <automated>cd "C:/Users/leheh/.Projects/homeservice_agent" && node -e "const fs=require('fs'); const c=fs.readFileSync('.claude/skills/public-site-i18n/SKILL.md','utf8'); const checks=['HeroDemoBlock','HeroDemoInput','HeroDemoPlayer','demo-voice','ELEVENLABS','Phase 29','RotatingText','getBoundingClientRect','Web Audio']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.error('MISSING:',missing);process.exit(1)}else{console.log('Skill file updated')}"</automated>
  </verify>
  <acceptance_criteria>
    - Skill file mentions Phase 29 in "Last updated" line
    - All 5 new files documented: HeroDemoBlock, HeroDemoInput, HeroDemoPlayer, demo-voice route, static audio
    - Hero demo flow documented in architecture section
    - RotatingText dynamic width change documented
    - ElevenLabs env vars listed
    - No references to removed elements (AuthAwareCTA in hero, Watch Demo button, eyebrow pill, social proof)
  </acceptance_criteria>
  <done>public-site-i18n skill file accurately reflects the Phase 29 hero architecture with all new components, API route, and RotatingText changes documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete hero demo</name>
  <what-built>
    Complete hero section interactive demo: business name input that generates a personalized AI receptionist call via ElevenLabs TTS, played back through a waveform audio player. RotatingText now animates width between words.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000

    **Hero copy verification:**
    - Title reads "Every Missed Call Is a Job Your [Competitor/Rival/Neighbor] Just Booked"
    - Rotating words cycle smoothly with width transitioning between words
    - Subtitle reads "Enter your business name and hear your AI receptionist answer in 30 seconds."
    - No eyebrow pill, no social proof line, no "Watch Demo" button

    **Input verification:**
    - Business name input visible in hero section
    - "Listen to Your Demo" button is dimmed/disabled with no text entered
    - Type 1 character — button still disabled
    - Type 2+ characters — button becomes orange and clickable
    - Below input: "Skip the demo — Start your free trial" link visible

    **Demo playback verification:**
    - Click "Listen to Your Demo" with a business name entered (e.g., "Smith Plumbing")
    - Button shows spinner + "Generating..." while loading
    - Input bar transitions to audio player (same position, no jump)
    - Audio plays automatically — you hear:
      1. Caller: "Hey, I'd like to get my AC serviced before summer hits."
      2. AI: "Thanks for calling Smith Plumbing! This is your AI receptionist..."
      3. Conversation continues with booking
      4. AI: "You're all set — Thursday at 2 PM..."
    - Waveform bars progress from dim to orange as audio plays
    - Elapsed time counter updates in M:SS format
    - Play/pause button works

    **Post-play verification:**
    - After audio ends, "Start Your Free Trial" button appears below player
    - Player stays visible with replay option
    - Clicking the CTA navigates to /onboarding

    **Mobile verification (resize to <640px):**
    - Input and button stack vertically
    - Waveform has fewer bars
    - Everything remains usable
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. HeroSection renders HeroDemoBlock which manages input->player transition
3. Full audio pipeline works: input name -> generate TTS -> stitch -> play
4. Skill file updated with all Phase 29 changes
5. Visual verification confirms all decisions D-01 through D-19
</verification>

<success_criteria>
- Complete end-to-end demo works: visitor enters business name, hears personalized AI receptionist call
- Audio loads and plays within 3 seconds of clicking "Listen to Your Demo" (DEMO-05)
- Hero section shows new copy with no old elements (DEMO-01, DEMO-04)
- Input transitions smoothly to player in-place (DEMO-02)
- Audio includes dynamic business name (DEMO-03)
- Skill file is in sync with codebase per CLAUDE.md requirement
</success_criteria>

<output>
After completion, create `.planning/phases/29-hero-section-interactive-demo/29-04-SUMMARY.md`
</output>
