---
phase: 48.1
plan: "03"
subsystem: public-landing
tags: [hero, carousel, faq, metadata, rebrand, copy]
dependency_graph:
  requires: [48.1-01]
  provides: [revenue-pain-hero, 4-pillar-carousel, faq-audio-demo-link, voco-ai-metadata]
  affects: [48.1-04]
tech_stack:
  added: []
  patterns: [hardcoded-jsx-copy, lucide-icons, next-metadata-export]
key_files:
  modified:
    - src/app/components/landing/HeroSection.jsx
    - src/app/components/landing/FeaturesCarousel.jsx
    - src/app/components/landing/FAQSection.jsx
    - src/app/layout.js
  created: []
decisions:
  - "Rewrote FeaturesCarousel.jsx completely (Write tool) rather than applying partial edits — prior intermediate edits left a dead placeholder function; a clean rewrite was safer and more readable"
  - "Updated carousel section heading copy from 'Everything your phone does — answered, booked, billed' (referenced 8 features) to 'Four ways Voco earns its keep' to match the trimmed 4-pillar count"
metrics:
  duration_minutes: 22
  completed_date: "2026-04-15T14:37:11Z"
  tasks_completed: 4
  tasks_total: 4
  files_modified: 4
  files_created: 0
---

# Phase 48.1 Plan 03: Copy Rewrite + Carousel Trim + Metadata Rebrand Summary

**One-liner:** Revenue-pain hero with #audio-demo CTAs, 4-pillar FeaturesCarousel with Singlish/code-switching, FAQSection #audio-demo link fix + 2 new entries, Voco AI root metadata.

---

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite HeroSection copy and CTAs | `be0be39` | HeroSection.jsx |
| 2 | Trim FeaturesCarousel to 4 pillars | `dfc3706` | FeaturesCarousel.jsx |
| 3 | FAQSection — 2 new entries + #audio-demo link | `816316e` | FAQSection.jsx |
| 4 | Root layout metadata to Voco AI | `9f74d08` | layout.js |

---

## What Was Built

### Task 1 — HeroSection rewrite (SC-02, SC-07)

- H1 changed from `"Voco Answers So You Can Keep [RotatingText]"` to declarative `"Stop losing $1,000+ every time you miss a call."` (D-22, exact)
- Sub-headline changed to `"Voco AI answers, triages, and books every call — in under 1 ring."` — the single in-body use of "Voco AI" per D-27 (D-23, exact)
- Replaced `HeroDemoBlock` (input-to-player) with a 2-CTA layout:
  - Primary: `"Hear Voco in action"` anchored to `#audio-demo` (orange filled, 44px height)
  - Secondary: `"Start 5-minute setup →"` linking to `/auth/signin` (link-style)
- Added micro-proof line below CTAs: `"Answers in English, Spanish, Mandarin, Malay, and 70+ more. 5-minute setup. No installs."` (D-24, exact)
- Removed `RotatingText` import and render entirely
- Removed `HeroDemoBlock` import entirely (component file deletion is Plan 04's responsibility)
- Preserved: Spline 3D scene, `bg-[#050505]` background, radial gradient overlay, grid texture, floating orb, mobile warm glow, AnimatedSection wrapper, mobile integration logo marquee (Google/Microsoft/Stripe/Twilio SVGs)

### Task 2 — FeaturesCarousel trimmed 9 → 4 pillars (SC-01)

Replaced entire FEATURES array with exactly 4 entries in narrative order (coverage → conversion → comprehension → safety net):

| Pillar | Title | Visual | Key copy |
|--------|-------|--------|----------|
| 1 | 24/7 AI Answering | AnsweringVisual (preserved) | "Every inbound call answered in under 1 ring" |
| 2 | Real-Time Calendar Booking | BookingVisual (preserved) | "books the caller into a confirmed slot before they hang up. Travel buffers included." |
| 3 | Speaks Your Trade | TradeVisual (new) | "Singlish and mid-call code-switching" + "70+ more" |
| 4 | Automated Lead Recovery | LeadRecoveryVisual (preserved) | "SMS with available slots. You wake up to booked jobs" |

Cut features (all absent from file): Custom Pickup Rules, 70+ Languages (as standalone pillar), Lead Capture & CRM, Post-Call SMS & Notifications, Invoicing & Estimates, Call Analytics & Dashboard.

Removed unused visual components: `PickupRulesVisual`, `LanguageVisual`, `LeadVisual`, `SMSVisual`, `InvoiceVisual`, `AnalyticsVisual`.

Removed unused Lucide imports: `Globe`, `PhoneForwarded`, `UserCheck`, `Receipt`, `BarChart3`, `Star`.
Added new Lucide imports: `Wrench`, `Zap`, `Thermometer` (for TradeVisual).

`id="features"` on outer `<section>` preserved — required by ScrollLinePath DOM query.

Carousel navigation logic, infinite-scroll tripling (TOTAL_SETS=3), pill indicators, progress bar, keyboard/arrow buttons, and reduced-motion handling all untouched.

### Task 3 — FAQSection updates (SC-01 supporting, SC-06 supporting)

- Fixed `href="#hero"` → `href="#audio-demo"` in the "Hear it yourself" link on FAQ q1 (robotic question)
- Added q8: language coverage — English, Spanish, Mandarin, Malay, Tagalog, Vietnamese, 70+, Singlish fluency, code-switching, auto-detection
- Added q9: high-ticket/complex jobs — site-visit estimate, lead card before callback, roadmap note for call samples
- All 7 original FAQs preserved unchanged
- shadcn Accordion structure, className strings, AnimatedSection wrapper all untouched

### Task 4 — Root metadata rebrand (SC-07)

Replaced minimal `{ title: 'Voco' }` with full metadata export:

```js
export const metadata = {
  title: 'Voco AI — AI Receptionist for Home Service Businesses',
  description: 'Stop losing revenue to missed calls...',
  openGraph: { title, description, type: 'website', siteName: 'Voco AI' },
  twitter: { card: 'summary_large_image', title, description },
};
```

`RootLayout` function body, ThemeProvider, NextIntlClientProvider, Inter font loader, Spline prefetch link — all unchanged.

---

## Verification Results

```
HeroSection: H1 OK, primary CTA #audio-demo OK, no RotatingText OK, no HeroDemoBlock OK
FeaturesCarousel: 4 pillars OK, id=features OK, Singlish OK, code-switching OK, 70+ OK, cut features absent OK
FAQSection: #audio-demo OK, no "#hero" OK, language FAQ OK, high-ticket FAQ OK, 9 total entries OK
layout.js: Voco AI OK (7 occurrences), openGraph OK, twitter OK, Stop losing revenue OK, RootLayout OK
```

Diff spillover check: `git diff --name-only be0be39^..9f74d08` returns exactly `HeroSection.jsx`, `FeaturesCarousel.jsx`, `FAQSection.jsx`, `layout.js` — no other files touched by this plan's commits.

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Partial edit left dead placeholder function in FeaturesCarousel.jsx**
- **Found during:** Task 2 mid-execution
- **Issue:** An intermediate Edit operation created a `_LanguageVisualRemoved_placeholder()` function — a dead stub that would have caused lint warnings and confused future readers
- **Fix:** Rewrote the entire FeaturesCarousel.jsx using the Write tool with a clean, authoritative version containing only the 4 needed visuals and the new FEATURES array
- **Files modified:** `src/app/components/landing/FeaturesCarousel.jsx`
- **Commit:** `dfc3706`

**2. [Rule 1 - Bug] Carousel section heading referenced old 8-feature count**
- **Found during:** Task 2
- **Issue:** Sub-heading read "Eight features that run the inbound side of your business" — factually wrong after trimming to 4
- **Fix:** Updated heading to "Four ways Voco earns its keep." with matching sub "Every inbound call handled — from the first ring to the confirmed booking."
- **Files modified:** `src/app/components/landing/FeaturesCarousel.jsx`
- **Commit:** `dfc3706`

---

## Known Stubs

None — all 4 files contain only hardcoded marketing copy. No data sources, no dynamic rendering, no placeholder text that prevents the plan's goals from being achieved.

---

## Threat Surface Scan

No new security-relevant surface introduced. All changes are:
- Hardcoded string literals in JSX components (no user input, no dynamic interpolation)
- Next.js `metadata` export with hardcoded strings (T-48.1-03-01: mitigated per plan threat model)
- No new network endpoints, no auth paths, no file access patterns, no schema changes

---

## Self-Check

### Files exist
- [x] `src/app/components/landing/HeroSection.jsx` — FOUND
- [x] `src/app/components/landing/FeaturesCarousel.jsx` — FOUND
- [x] `src/app/components/landing/FAQSection.jsx` — FOUND
- [x] `src/app/layout.js` — FOUND

### Commits exist
- [x] `be0be39` — HeroSection rewrite
- [x] `dfc3706` — FeaturesCarousel trim
- [x] `816316e` — FAQSection updates
- [x] `9f74d08` — layout.js metadata

## Self-Check: PASSED
