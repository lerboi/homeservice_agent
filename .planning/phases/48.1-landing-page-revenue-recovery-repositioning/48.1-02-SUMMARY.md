---
phase: 48.1-landing-page-revenue-recovery-repositioning
plan: "02"
subsystem: ui
tags: [react, nextjs, landing-page, audio, jsx, tailwind, lucide, framer-motion]

# Dependency graph
requires:
  - phase: 48.1-01
    provides: stub audio files (demo-emergency.mp3, demo-routine.mp3), pricing page calculator anchor, landing-sections test inventory

provides:
  - AudioDemoSection component (2-tab tabbed player + synchronized transcript, SC-04)
  - IntegrationsStrip component (6 logos, Live/Coming soon chips, SC-05)
  - CostOfSilenceBlock component ($260,400/year stat + /pricing#calculator deep-link, SC-03)
  - YouStayInControlSection component (identity intro + 3 WHO/WHEN/WHAT control cards + dark pull quote, SC-06)

affects:
  - 48.1-04 (page.js wiring — imports all 4 components from this plan)
  - 48.1-VALIDATION (landing-sections test groups now pass)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "__vocoPlayingAudio singleton for multi-player audio coordination (extended from AudioPlayerCard pattern)"
    - "CARD_CLS + STAT_CHIP_CLS shared style constants (from PracticalObjectionsGrid, reused verbatim)"
    - "ScrollLinePath stacking rule: outer <section> NO relative, inner <div> relative z-[1]"
    - "React fragment for multi-section named exports (YouStayInControlSection wraps 2 <section> elements)"

key-files:
  created:
    - src/app/components/landing/AudioDemoSection.jsx
    - src/app/components/landing/IntegrationsStrip.jsx
    - src/app/components/landing/CostOfSilenceBlock.jsx
    - src/app/components/landing/YouStayInControlSection.jsx
  modified: []

key-decisions:
  - "Text wordmarks used for integration logos (inline <span> with brand colors) — SVG sourcing deferred to a downstream design pass per CONTEXT.md Claude's Discretion"
  - "YouStayInControlSection uses React fragment to export both the white-bg identity+cards section and the dark-bg pull quote section as one named component"
  - "AudioDemoSection useEffect re-runs on activeTab change so transcript active-line index tracking reattaches event listeners to the new audio state"
  - "YouStayInControlSection placed OUTSIDE ScrollLinePath per UI-SPEC override of D-04 (CLS boundary at dark pull quote bg)"

patterns-established:
  - "Audio coordination singleton: window.__vocoPlayingAudio pauses previous player before new play() — prevents multi-stream overlap"
  - "Tab-switch full reset: pause + currentTime=0 + state resets before switching activeTab — prevents Pitfall 3 (stale transcript sync)"
  - "preload=metadata only on all audio elements — LCP safety (no eager loading)"
  - "ScrollLinePath inner div requires relative z-[1] to remain above the copper wave SVG"

requirements-completed: [P48.1-SC3, P48.1-SC4, P48.1-SC5, P48.1-SC6]

# Metrics
duration: 35min
completed: 2026-04-15
---

# Phase 48.1 Plan 02: Landing Narrative Sections Summary

**Four revenue-recovery narrative components built: AudioDemoSection (tabbed call demo with synchronized transcript), IntegrationsStrip (6 logos Live/Coming-soon), CostOfSilenceBlock ($260,400/year stat), and YouStayInControlSection (identity + 3 WHO/WHEN/WHAT cards + dark pull quote)**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-04-15T14:00:00Z
- **Completed:** 2026-04-15T14:35:48Z
- **Tasks:** 3
- **Files modified:** 4 (all new)

## Accomplishments

- AudioDemoSection: 2-tab pill UI (Emergency/Routine), 24-bar waveform visualizer, synchronized transcript with scrollIntoView, `__vocoPlayingAudio` singleton coordination, `preload="metadata"` for LCP safety, `aria-live` on transcript panel, error fallback message — SC-04 artifact complete
- IntegrationsStrip: 6 integration tiles (Google Calendar + Outlook as Live with green chips; Jobber + Housecall Pro + ServiceTitan + Zapier as Coming soon with grayscale + muted chips) — SC-05 artifact complete
- CostOfSilenceBlock: Static $260,400/year stat with orange underline accent, `/pricing#calculator` deep-link CTA — SC-03 artifact complete
- YouStayInControlSection: Absorbs IdentitySection 3-paragraph intro verbatim (D-20), 3 WHO/WHEN/WHAT cards with CARD_CLS pattern + italic STAT_CHIP_CLS chips (D-15), closing dark pull quote `"You set the rules. Voco follows them."` on `bg-[#1C1412]` — SC-06 artifact complete; D-14 forbidden vocabulary entirely absent

## Task Commits

Each task was committed atomically:

1. **Task 1: AudioDemoSection** - `f9595fd` (feat)
2. **Task 2: IntegrationsStrip + CostOfSilenceBlock** - `6f8a16f` (feat)
3. **Task 3: YouStayInControlSection** - `de98b33` (feat)

## Files Created/Modified

- `src/app/components/landing/AudioDemoSection.jsx` — Tabbed audio demo with 2 tracks, 24-bar waveform, synchronized transcript, `__vocoPlayingAudio` coordination
- `src/app/components/landing/IntegrationsStrip.jsx` — 6-logo integration trust strip with Live/Coming-soon chip treatment
- `src/app/components/landing/CostOfSilenceBlock.jsx` — $260,400/year stat block with /pricing#calculator deep-link
- `src/app/components/landing/YouStayInControlSection.jsx` — Consolidated identity + WHO/WHEN/WHAT cards + dark pull quote

## Decisions Made

- Text wordmarks used for integration logos (inline `<span>` elements with official brand hex colors) — sourcing full SVG paths was out of session scope per CONTEXT.md Claude's Discretion. A downstream design pass can swap these for proper SVGs without touching the component interface.
- `YouStayInControlSection` uses a React fragment (`<>...</>`) so Plan 04 can render it as a single import while it still produces two distinct `<section>` elements (white bg for identity+cards, dark `#1C1412` bg for pull quote).
- `AudioDemoSection` re-attaches audio event listeners in a `useEffect` that depends on `activeTab` — ensures `onTime` handler references the correct track's transcript array on each tab switch.
- `YouStayInControlSection` placed outside `ScrollLinePath` per UI-SPEC override of D-04. The closing dark pull quote creates a visual boundary that would clip the copper sine wave.

## Deviations from Plan

None — plan executed exactly as written. All D-14 through D-21 decisions honored. All STAT_CHIP_CLS chip texts are exact per D-15. Verbatim IdentitySection paragraphs present per D-20. Forbidden vocabulary absent from all card body copy.

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| Audio transcript copy (Emergency/Routine dialogue lines) | `src/app/components/landing/AudioDemoSection.jsx` | 9–30 | Scripted placeholder — plan explicitly states "adjust to match real recording when it ships" (D-09). Component renders and tests pass with this copy. |
| Integration logos as text wordmarks | `src/app/components/landing/IntegrationsStrip.jsx` | 62–84 | Official SVG paths deferred to design pass per CONTEXT.md Claude's Discretion. Wordmarks are recognizable and branded. |

These stubs do NOT prevent this plan's goal — SC-03 through SC-06 are all achieved. Stub audio content is replaced when real recordings ship (Plan 01 placed the audio files; production recordings swap them in place).

## Threat Flags

None — all 4 components are client-side JSX only. No new network endpoints, no auth paths, no file system access, no schema changes. T-48.1-02-01 (audio DoS) mitigated via `preload="metadata"` and `__vocoPlayingAudio` singleton.

## Issues Encountered

None. All tasks completed cleanly on first attempt.

## Next Phase Readiness

- All 4 SC-03 through SC-06 artifacts ready for Plan 04 to wire into `src/app/(public)/page.js`
- Plan 04 must: dynamically import all 4 components with `ssr: false` (AudioDemoSection uses audio API); place IntegrationsStrip + CostOfSilenceBlock inside `<ScrollLinePath>` wrapper; YouStayInControlSection outside the wrapper
- Plan 03 (running in parallel) produces HeroSection — no conflicts (files-modified sets are disjoint)
- `npm test -- --testPathPattern=landing` test groups added in Plan 01 Task 1 should now pass for all 4 components in this plan

---
*Phase: 48.1-landing-page-revenue-recovery-repositioning*
*Completed: 2026-04-15*
