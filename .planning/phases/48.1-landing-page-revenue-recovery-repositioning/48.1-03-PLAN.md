---
phase: 48.1
plan: 03
type: execute
wave: 1
depends_on: [48.1-01]
files_modified:
  - src/app/components/landing/HeroSection.jsx
  - src/app/components/landing/FeaturesCarousel.jsx
  - src/app/components/landing/FAQSection.jsx
  - src/app/layout.js
autonomous: true
requirements:
  - P48.1-SC1
  - P48.1-SC2
  - P48.1-SC4
  - P48.1-SC7
  - P48.1-SC8
threat_model:
  boundaries:
    - client-only component copy edits + Next.js root layout metadata
  threats:
    - id: T-48.1-03-01
      category: Spoofing
      component: Root layout metadata (Voco AI)
      disposition: mitigate
      severity: low
      mitigation: "Metadata strings are hardcoded in layout.js; no dynamic interpolation from external source"

must_haves:
  truths:
    - "Hero H1 reads 'Stop losing $1,000+ every time you miss a call.' with no RotatingText animation"
    - "Hero primary CTA label is 'Hear Voco in action' and anchors to #audio-demo; secondary CTA label is 'Start 5-minute setup →' linking to /auth/signin"
    - "FeaturesCarousel renders exactly 4 pillars in this order: 24/7 AI Answering, Real-Time Calendar Booking, Speaks Your Trade, Automated Lead Recovery — Pillar 3 body copy mentions Singlish and code-switching"
    - "FAQSection includes a language-coverage entry (Singlish + 70+ languages + code-switching); internal link from 'Hear it yourself' points to #audio-demo (not #hero)"
    - "Root metadata uses 'Voco AI' in title, openGraph.title, and twitter.title"
  artifacts:
    - path: src/app/components/landing/HeroSection.jsx
      provides: "Revenue-pain hero copy + new CTA pair"
      contains: "Stop losing"
    - path: src/app/components/landing/FeaturesCarousel.jsx
      provides: "4-pillar carousel (24/7 / Real-Time Booking / Speaks Your Trade / Lead Recovery)"
      contains: "Speaks Your Trade"
    - path: src/app/components/landing/FAQSection.jsx
      provides: "Updated FAQ with language-coverage entry + #audio-demo internal link fix"
      contains: "#audio-demo"
    - path: src/app/layout.js
      provides: "Voco AI metadata (title, description, openGraph, twitter)"
      contains: "Voco AI"
  key_links:
    - from: "HeroSection primary CTA"
      to: "#audio-demo (AudioDemoSection from Plan 02)"
      via: "anchor href"
      pattern: "href=\"#audio-demo\""
    - from: "FeaturesCarousel section element"
      to: "ScrollLinePath (Plan 04)"
      via: "id=\"features\" DOM query anchor"
      pattern: "id=\"features\""
    - from: "FAQSection 'Hear it yourself' link"
      to: "#audio-demo"
      via: "anchor href"
      pattern: "#audio-demo"
---

<objective>
Modify 4 existing files to carry the revenue-recovery hero message, trim the carousel to 4 pillars, update FAQ, and rebrand root metadata as "Voco AI" (per D-27 naming scope). Disjoint from Plan 02 — runs in parallel.

Purpose: Deliver SC-01 (section count reduction foothold: cutting HeroDemoBlock here frees hero for the new CTA pattern), SC-02 (hero dollar-pain copy + audio-demo CTA), SC-07 ("Voco AI" rebrand in metadata + hero only — everything else stays "Voco"), SC-08 performance contract respected (no new dependencies, no new heavy imports, no eager audio preloads).

Output: HeroSection rewritten copy and CTAs; FeaturesCarousel trimmed to 4 pillars; FAQSection updated (2 entries added, #hero → #audio-demo); layout.js metadata export updated.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-RESEARCH.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-UI-SPEC.md

<interfaces>
Current HeroSection.jsx structure (from RESEARCH verification):
- Imports RotatingText (being removed)
- Renders a HeroDemoBlock (being removed in Plan 04 — this plan removes the import)
- Has H1 "Voco Answers So You Can Keep..." and sub "Voco picks up when you're on the roof..."
- Preserve: Spline 3D scene, bg-[#050505], radial-gradient orange overlay, grid texture, AnimatedSection wrapper, mobile marquee with Google/Microsoft inline SVGs

Target HeroSection after this plan:
- H1: "Stop losing $1,000+ every time you miss a call." (plain text, no RotatingText)
- Sub: "Voco AI answers, triages, and books every call — in under 1 ring."
- Micro-proof (below CTAs): "Answers in English, Spanish, Mandarin, Malay, and 70+ more. 5-minute setup. No installs."
- Primary CTA button: "Hear Voco in action" with href="#audio-demo"
- Secondary CTA: "Start 5-minute setup →" with href="/auth/signin"
- Remove: RotatingText import and render, HeroDemoBlock import (Plan 04 deletes the component file)

FeaturesCarousel.jsx FEATURES array (lines 404–479 per RESEARCH §9):
- Currently has 9 entries
- Replace with exactly 4: 24/7 AI Answering (existing), Real-Time Calendar Booking (existing), Speaks Your Trade (NEW — absorbs OBJ-09), Automated Lead Recovery (existing)
- Pillar 3 "Speaks Your Trade" body copy MUST mention Singlish + code-switching + 70+ languages (D-33)

FAQSection.jsx:
- Current line 13 (per RESEARCH §4): "Hear it yourself" link targets #hero — must change to #audio-demo
- Add 2 entries: language coverage (full list + Singlish + code-switching), high-ticket scenario preview
- Keep existing 7 FAQs

Root layout.js (src/app/layout.js):
- Current: minimal metadata with just title: 'Voco'
- Target: title + description + openGraph + twitter all naming "Voco AI"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite HeroSection copy and CTAs (SC-02, SC-07)</name>
  <files>src/app/components/landing/HeroSection.jsx</files>
  <read_first>
    - src/app/components/landing/HeroSection.jsx (full current file — identify exact lines for H1, sub, CTA block, RotatingText import/render, HeroDemoBlock import/render; preserve Spline scene, radial overlay, grid texture, mobile marquee)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-UI-SPEC.md §"Section 1: Hero" (exact visual treatment, typography classes, CTA button styling)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md D-22 through D-26 (all hero copy locked verbatim)
  </read_first>
  <action>
    Modify `src/app/components/landing/HeroSection.jsx` with these exact surgical edits. Do NOT restructure the component beyond what's listed here.

    Edit 1 — Remove RotatingText import. Find the RotatingText import line (either `import RotatingText from './RotatingText';` or the named-import variant) and DELETE it entirely.

    Edit 2 — Remove HeroDemoBlock import. Find the HeroDemoBlock import line and DELETE it entirely. The HeroDemoBlock component file itself is deleted in Plan 04.

    Edit 3 — Replace H1 with exact copy (D-22). Locate the existing H1 and replace its full text content with the exact string: `Stop losing $1,000+ every time you miss a call.` If the current H1 uses a `<RotatingText>` wrapper, REMOVE the wrapper entirely — the new H1 is a plain declarative statement, no animation. Apply classes per UI-SPEC: `text-[2.25rem] md:text-[2.5rem] lg:text-[3rem] font-semibold text-white leading-tight`.

    Edit 4 — Replace sub-headline with exact copy (D-23). Locate the sub-headline element immediately below H1 and replace its text content with the exact string: `Voco AI answers, triages, and books every call — in under 1 ring.` Apply classes: `text-base sm:text-lg md:text-xl text-white/60`. This is the ONE and ONLY place on the landing page where "Voco AI" appears in body-visible text per D-27.

    Edit 5 — Replace the hero CTA area. REMOVE the HeroDemoBlock render (or whatever input-driven demo element is currently in the hero CTA area). In its place, render this exact JSX block:

    ```jsx
    <div className="flex flex-col sm:flex-row items-center justify-center gap-4 mt-8">
      <a
        href="#audio-demo"
        className="inline-flex items-center justify-center px-6 h-11 rounded-lg bg-[#F97316] text-white text-[15px] font-semibold hover:bg-[#EA580C] transition-colors"
      >
        Hear Voco in action
      </a>
      <a
        href="/auth/signin"
        className="inline-flex items-center justify-center px-4 h-11 text-[14px] font-semibold text-white/80 hover:text-white transition-colors"
      >
        Start 5-minute setup →
      </a>
    </div>
    <p className="text-xs sm:text-sm text-white/35 text-center mt-6">
      Answers in English, Spanish, Mandarin, Malay, and 70+ more. 5-minute setup. No installs.
    </p>
    ```

    Primary CTA is visually dominant (orange filled, 44px height via `h-11`); secondary CTA is link-style. Micro-proof line lives BELOW the CTAs.

    Edit 6 — Preserve the rest of HeroSection.jsx: Keep the Spline 3D scene render (desktop only, `hidden md:block`), keep the `bg-[#050505]` section background + radial gradient overlay + grid texture, keep the AnimatedSection wrapper, keep the existing mobile marquee with Google/Microsoft inline SVGs.

    Do NOT introduce useTranslations or i18n keys. Do NOT add analytics event handlers. Do NOT modify LandingNav.jsx (nav CTA text handled separately if at all). Do NOT re-add RotatingText anywhere. Do NOT touch HeroDemoBlock/Input/Player component files (Plan 04 deletes them).
  </action>
  <verify>
    <automated>grep -q "Stop losing" src/app/components/landing/HeroSection.jsx && grep -q "Voco AI answers, triages" src/app/components/landing/HeroSection.jsx && grep -q 'href="#audio-demo"' src/app/components/landing/HeroSection.jsx && grep -q "Hear Voco in action" src/app/components/landing/HeroSection.jsx && grep -q "Start 5-minute setup" src/app/components/landing/HeroSection.jsx && ! grep -q "RotatingText" src/app/components/landing/HeroSection.jsx && ! grep -q "HeroDemoBlock" src/app/components/landing/HeroSection.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Stop losing" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (exact H1 with $1,000+ and miss a call)
    - `grep "Voco AI answers, triages, and books every call" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (exact sub)
    - `grep "Answers in English, Spanish, Mandarin, Malay, and 70+ more" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (exact micro-proof)
    - `grep 'href="#audio-demo"' src/app/components/landing/HeroSection.jsx` returns ≥ 1 (primary CTA anchor)
    - `grep "Hear Voco in action" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (primary CTA label)
    - `grep 'href="/auth/signin"' src/app/components/landing/HeroSection.jsx` returns ≥ 1 (secondary CTA target)
    - `grep "Start 5-minute setup" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (secondary CTA label)
    - `grep -i "RotatingText" src/app/components/landing/HeroSection.jsx` returns 0 matches
    - `grep "HeroDemoBlock" src/app/components/landing/HeroSection.jsx` returns 0 matches
    - `grep "bg-\[#050505\]" src/app/components/landing/HeroSection.jsx` returns ≥ 1 (dark bg preserved)
    - Tests in Plan 01 Task 1 group "HeroSection 48.1" now pass
  </acceptance_criteria>
  <done>
    Hero carries revenue-pain H1, Voco-AI sub, 2-CTA pair, micro-proof line. RotatingText and HeroDemoBlock references fully removed. Spline scene, bg, overlays, and AnimatedSection all preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trim FeaturesCarousel to 4 pillars with Speaks Your Trade absorbing OBJ-09 (SC-01)</name>
  <files>src/app/components/landing/FeaturesCarousel.jsx</files>
  <read_first>
    - src/app/components/landing/FeaturesCarousel.jsx (full file — identify FEATURES array at lines 404–479 per RESEARCH §9, all visual component functions, all Lucide imports, the `id="features"` on outer section element)
    - src/app/components/landing/PracticalObjectionsGrid.jsx (OBJ-09 "Speaks Your Trade" body copy — absorb the tone and claim into the new Pillar 3 body)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-UI-SPEC.md §"Section 5: FeaturesCarousel-4"
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md D-31, D-32, D-33
  </read_first>
  <action>
    Modify `src/app/components/landing/FeaturesCarousel.jsx` with these surgical edits. This is a **data-array trim**, NOT a structural rewrite. The carousel navigation, infinite scroll, keyboard behavior, pill indicators, and reduced-motion handling are all array-length-driven — do NOT touch them.

    Edit 1 — Replace the FEATURES array (lines 404–479) with exactly 4 entries in this exact order:

    ```jsx
    const FEATURES = [
      {
        id: '24-7-answering',
        title: '24/7 AI Answering',
        body: 'Every inbound call answered in under 1 ring — day, night, weekends, holidays. No voicemail, no lost leads.',
        icon: Phone,
        Visual: AnsweringVisual,
      },
      {
        id: 'real-time-booking',
        title: 'Real-Time Calendar Booking',
        body: 'Voco reads your Google or Outlook calendar live and books the caller into a confirmed slot before they hang up. Travel buffers included.',
        icon: Calendar,
        Visual: BookingVisual,
      },
      {
        id: 'speaks-your-trade',
        title: 'Speaks Your Trade',
        body: 'Plumbing, HVAC, electrical, handyman — Voco knows the vocabulary. Also fluent in English, Spanish, Mandarin, Malay, and 70+ more, including Singlish and mid-call code-switching.',
        icon: Wrench,
        Visual: TradeVisual,
      },
      {
        id: 'lead-recovery',
        title: 'Automated Lead Recovery',
        body: 'If a caller hangs up before booking, Voco sends an SMS with available slots. You wake up to booked jobs, not missed opportunities.',
        icon: MessageCircle,
        Visual: LeadRecoveryVisual,
      },
    ];
    ```

    Use the existing 24/7, Calendar Booking, and Lead Recovery entries as a base — copy their existing Visual component references (`AnsweringVisual`, `BookingVisual`, `LeadRecoveryVisual` — or whatever names they use in the current file). Only update their body copy if needed to match the text above. If the existing icon names differ, keep the existing icon references and just update body copy + titles.

    Pillar 3 "Speaks Your Trade" body MUST contain all three: the word "Singlish", the phrase "code-switching", and "70+". The exact copy above satisfies this — do NOT remove any of those words during polish.

    Edit 2 — Add a `TradeVisual` visual component. If OBJ-09 in PracticalObjectionsGrid.jsx has a visual treatment worth reusing, inline it here. Otherwise create a minimal visual component in FeaturesCarousel.jsx:

    ```jsx
    function TradeVisual() {
      return (
        <div className="flex items-center justify-center gap-3">
          <Wrench className="w-8 h-8 text-[#F97316]" />
          <Zap className="w-8 h-8 text-[#F97316]" />
          <Thermometer className="w-8 h-8 text-[#F97316]" />
        </div>
      );
    }
    ```

    Add Lucide imports for `Wrench`, `Zap`, `Thermometer` if not already present. (`Wrench` is likely already imported if OBJ-09 uses it in PracticalObjectionsGrid, but since this is a separate file import, add it here.)

    Edit 3 — Delete unused micro-visual function components. Remove these function declarations from FeaturesCarousel.jsx if they are no longer referenced after the array trim: `PickupRulesVisual`, `LanguageVisual`, `LeadVisual` (if distinct from LeadRecoveryVisual), `SMSVisual`, `InvoiceVisual`, `AnalyticsVisual`. Only delete components with ZERO references.

    Edit 4 — Delete unused Lucide icon imports. Remove `Globe`, `PhoneForwarded`, `UserCheck`, `PhoneMissed`, `Receipt`, `BarChart3`, `MessageSquare` from the Lucide import line IF they are no longer referenced anywhere in the file. Keep `Phone`, `Calendar`, `Wrench`, `MessageCircle` (and the new `Zap`, `Thermometer` from Edit 2). Only delete imports with zero remaining references.

    Edit 5 — Preserve `id="features"` on the outer section element. RESEARCH §"ScrollLinePath ID Requirements" is explicit — ScrollLinePath queries `document.getElementById('features')` to anchor the copper wave. The outer `<section>` element MUST keep `id="features"`. Do NOT rename or remove it.

    Do NOT change carousel navigation logic. Do NOT add more than 4 entries. Do NOT remove Singlish/code-switching/70+ from Pillar 3 body. Do NOT add i18n keys. Do NOT touch PracticalObjectionsGrid.jsx (Plan 04 deletes it).
  </action>
  <verify>
    <automated>grep -E "24/7 AI Answering|Real-Time Calendar Booking|Speaks Your Trade|Automated Lead Recovery" src/app/components/landing/FeaturesCarousel.jsx | wc -l | awk '{if ($1 >= 4) print "OK"; else print "FAIL"}' && grep -q 'id="features"' src/app/components/landing/FeaturesCarousel.jsx && grep -q "Singlish" src/app/components/landing/FeaturesCarousel.jsx && ! grep -q "Custom Pickup Rules" src/app/components/landing/FeaturesCarousel.jsx && ! grep -q "Call Analytics" src/app/components/landing/FeaturesCarousel.jsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 pillar titles present: `grep "24/7 AI Answering" src/app/components/landing/FeaturesCarousel.jsx` returns ≥ 1; same for "Real-Time Calendar Booking", "Speaks Your Trade", "Automated Lead Recovery"
    - `grep -c "title:" src/app/components/landing/FeaturesCarousel.jsx` returns ≤ 8 (accounting for possible nested `title:` usage elsewhere; expected primary FEATURES array has exactly 4 title entries — if the repo uses `title:` elsewhere that's fine, main signal is absence of cut features)
    - `grep 'id="features"' src/app/components/landing/FeaturesCarousel.jsx` returns ≥ 1 (preserved)
    - Pillar 3 body coverage: `grep "Singlish" src/app/components/landing/FeaturesCarousel.jsx` returns ≥ 1; `grep "code-switching" src/app/components/landing/FeaturesCarousel.jsx` returns ≥ 1; `grep "70+" src/app/components/landing/FeaturesCarousel.jsx` returns ≥ 1
    - **Cut features absent (all must return 0):** `grep "Custom Pickup Rules" src/app/components/landing/FeaturesCarousel.jsx` returns 0; same for "Lead Capture & CRM", "Post-Call SMS", "Invoicing & Estimates", "Call Analytics", "70+ Languages" (as a standalone title, NOT as body copy text "70+ more")
    - Tests in Plan 01 Task 1 group "FeaturesCarousel-4 48.1" now pass
  </acceptance_criteria>
  <done>
    Carousel trims to 4 pillars in exact narrative order (coverage → conversion → comprehension → safety net). Pillar 3 body covers Singlish + code-switching + 70+ per D-33. `id="features"` preserved for ScrollLinePath. Unused visuals and icons cleaned up (tree-shaken).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update FAQSection with 2 new entries + fix #hero → #audio-demo link (SC-01 supporting, SC-06 supporting)</name>
  <files>src/app/components/landing/FAQSection.jsx</files>
  <read_first>
    - src/app/components/landing/FAQSection.jsx (full file — identify FAQS array, existing 7 entries, the "Hear it yourself" link at line 13 per RESEARCH §4 which currently targets #hero)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-UI-SPEC.md §"Section 7: FAQ"
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md D-33 (language coverage survives in FAQ), deferred section (high-ticket preview FAQ hint)
  </read_first>
  <action>
    Modify `src/app/components/landing/FAQSection.jsx` with these surgical edits.

    Edit 1 — Change internal link `#hero` → `#audio-demo`. Find any `href="#hero"` (RESEARCH §4 identifies this at line 13 in the "Hear it yourself" language) and change it to `href="#audio-demo"`. If the link text is something like "Hear it yourself" it should now point to the Plan 02 AudioDemoSection anchor.

    Edit 2 — Add new FAQ entry: Language coverage. Add this object to the FAQS array (append or insert — pick a natural position in the list, e.g. near any other language/region question):

    ```js
    {
      question: 'What languages does Voco support?',
      answer: 'Voco answers calls in English, Spanish, Mandarin, Malay, Tagalog, Vietnamese, and 70+ more. It handles Singlish fluently and can code-switch mid-call — so a caller who starts in English and switches to Hokkien stays understood. No setup required; language is auto-detected per call.',
    },
    ```

    Edit 3 — Add new FAQ entry: High-ticket scenario preview (previewing the deferred 3rd audio tab per CONTEXT.md deferred section):

    ```js
    {
      question: 'What about complex or high-ticket jobs — like a full system install quote?',
      answer: 'Voco captures the caller\'s needs, gathers key details (property size, existing system, timeline, budget range), and books a site-visit estimate with your best-qualified technician. You get a complete lead card before the callback. High-ticket call samples are on the roadmap for a follow-up update.',
    },
    ```

    Edit 4 — Keep all 7 existing FAQ entries. Do NOT delete or rewrite them. If any existing entry's copy is stale (refers to removed sections), leave a comment in the commit message noting it for follow-up but do NOT modify the copy in this task. Scope is the 2 additions + the #audio-demo link fix only.

    Edit 5 — Preserve shadcn Accordion structure, existing className strings, and any animation wrappers. Do NOT change the rendering shell.

    Do NOT add i18n keys. Do NOT delete existing FAQs. Do NOT touch FAQChatWidget.jsx.
  </action>
  <verify>
    <automated>grep -q "#audio-demo" src/app/components/landing/FAQSection.jsx && ! grep -q '"#hero"' src/app/components/landing/FAQSection.jsx && grep -q "Singlish" src/app/components/landing/FAQSection.jsx && grep -q "high-ticket\|High-ticket\|site-visit estimate" src/app/components/landing/FAQSection.jsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '#audio-demo' src/app/components/landing/FAQSection.jsx` returns ≥ 1 match (new anchor target)
    - `grep '"#hero"' src/app/components/landing/FAQSection.jsx` returns 0 matches (old anchor removed)
    - Language-coverage FAQ present: `grep "What languages does Voco support" src/app/components/landing/FAQSection.jsx` returns ≥ 1; `grep "Singlish" src/app/components/landing/FAQSection.jsx` returns ≥ 1; `grep "code-switch" src/app/components/landing/FAQSection.jsx` returns ≥ 1
    - High-ticket FAQ present: `grep -E "high-ticket|High-ticket|site-visit estimate" src/app/components/landing/FAQSection.jsx` returns ≥ 1
    - Existing FAQs preserved (heuristic): `grep -c "question:" src/app/components/landing/FAQSection.jsx` returns ≥ 9 (7 original + 2 new); and at minimum `grep -c "question:" src/app/components/landing/FAQSection.jsx` returns strictly more than pre-edit count (verified by diff +2 question entries)
    - Tests in Plan 01 Task 1 group "FAQSection 48.1" now pass
  </acceptance_criteria>
  <done>
    FAQ list gains language-coverage and high-ticket-preview entries. The "Hear it yourself" link now targets the Plan 02 AudioDemoSection correctly. Existing 7 FAQs preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update root layout metadata to Voco AI (SC-07)</name>
  <files>src/app/layout.js</files>
  <read_first>
    - src/app/layout.js (full file — identify current `export const metadata = { ... }` block; currently minimal with just title: 'Voco')
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md D-27 (Voco AI scope)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-RESEARCH.md §3 "Voco AI Naming: Full Scope Audit"
  </read_first>
  <action>
    Modify `src/app/layout.js` — update ONLY the `metadata` export. Do NOT change the `RootLayout` function, ThemeProvider placement, font loader, next-intl provider, or any other wiring.

    Replace the existing `export const metadata = { ... }` block with exactly this:

    ```js
    export const metadata = {
      title: 'Voco AI — AI Receptionist for Home Service Businesses',
      description: 'Stop losing revenue to missed calls. Voco AI answers, triages, and books every call in under 1 ring — for plumbers, HVAC, electricians, and handymen.',
      openGraph: {
        title: 'Voco AI — AI Receptionist for Home Service Businesses',
        description: 'Stop losing revenue to missed calls. Voco AI answers, triages, and books every call in under 1 ring.',
        type: 'website',
        siteName: 'Voco AI',
      },
      twitter: {
        card: 'summary_large_image',
        title: 'Voco AI — AI Receptionist for Home Service Businesses',
        description: 'Stop losing revenue to missed calls. Voco AI answers, triages, and books every call in under 1 ring.',
      },
    };
    ```

    Do NOT:
    - Do NOT change `RootLayout` function signature or return body
    - Do NOT add `<title>` tags in JSX (Next.js App Router uses the metadata export; hand-rolling `<title>` causes double titles)
    - Do NOT add icons, manifest, themeColor, or any other metadata fields outside what's listed (keep scope tight; follow-up phases can add OG images)
    - Do NOT introduce i18n for metadata (out of scope; RESEARCH §5)
    - Do NOT rename the project internally from "Voco" to "Voco AI" — D-27 restricts this to metadata + hero only
  </action>
  <verify>
    <automated>grep -q "Voco AI" src/app/layout.js && grep -q "openGraph" src/app/layout.js && grep -q "twitter" src/app/layout.js && grep -q "Stop losing revenue to missed calls" src/app/layout.js && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "title: 'Voco AI" src/app/layout.js` returns ≥ 1 (title uses Voco AI)
    - `grep "openGraph" src/app/layout.js` returns ≥ 1 (OG block present)
    - `grep "twitter" src/app/layout.js` returns ≥ 1 (Twitter card block present)
    - `grep "Stop losing revenue to missed calls" src/app/layout.js` returns ≥ 1 (description copy)
    - Count of "Voco AI" occurrences: `grep -c "Voco AI" src/app/layout.js` returns ≥ 3 (title + openGraph.title + twitter.title — plus potentially siteName)
    - RootLayout function still exists: `grep -E "RootLayout|export default function" src/app/layout.js` returns ≥ 1
    - Tests in Plan 01 Task 1 group "metadata 48.1" now pass
  </acceptance_criteria>
  <done>
    Root metadata names "Voco AI" in title, openGraph.title, twitter.title per D-27. RootLayout function body untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Client-only component copy edits + static Next.js metadata. No server logic, no auth surface, no user input, no persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.1-03-01 | Spoofing | Root layout metadata (Voco AI branding) | mitigate | Hardcoded string literals in layout.js; no dynamic interpolation from external source |
| T-48.1-03-02 | Information Disclosure | FAQSection answers | accept | All FAQ copy is marketing-safe, no internal/sensitive info |
| T-48.1-03-03 | Tampering | FeaturesCarousel FEATURES array | accept | Hardcoded array; no user-controllable input reaches it |
</threat_model>

<verification>
- 4 files modified: HeroSection.jsx, FeaturesCarousel.jsx, FAQSection.jsx, layout.js
- No new files created (Plan 02 owns creations)
- No files deleted (Plan 04 owns deletions)
- `npm test -- --testPathPattern=landing` now passes the "HeroSection 48.1", "FeaturesCarousel-4 48.1", "FAQSection 48.1", and "metadata 48.1" groups
- `git diff --name-only` returns only the 4 files in files_modified — no spillover into api/, dashboard/, or migrations
- Pillar 3 body copy contains: Singlish, code-switching, 70+
- HeroSection has no RotatingText and no HeroDemoBlock references
</verification>

<success_criteria>
- SC-01 supported: FeaturesCarousel trims 9 → 4 pillars, cutting 5 features
- SC-02 delivered: Hero H1 names dollar-value pain; primary CTA is "Hear Voco in action" anchoring to #audio-demo
- SC-07 delivered: Metadata title/og:title/twitter:title all use "Voco AI"; hero H1 sub uses "Voco AI" (single in-body use); all other landing copy stays "Voco"
- SC-08 protected: No new dependencies, no heavy imports, no audio eager-loads
- Plan 02 unaffected (files-modified sets are disjoint)
- Plan 04 can proceed: hero and carousel are ready for page.js rewiring
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-03-SUMMARY.md`
</output>
