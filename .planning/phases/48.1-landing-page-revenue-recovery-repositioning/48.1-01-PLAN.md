---
phase: 48.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/landing-sections.test.js
  - src/app/(public)/pricing/page.js
  - public/audio/demo-emergency.mp3
  - public/audio/demo-routine.mp3
autonomous: true
requirements:
  - P48.1-SC3
  - P48.1-SC4
  - P48.1-SC9
threat_model:
  boundaries:
    - none (prerequisite scaffolding only — no new runtime surface)
  threats:
    - id: T-48.1-01-01
      category: Tampering
      component: tests/unit/landing-sections.test.js
      disposition: accept
      severity: low
      rationale: "Test file updates cannot introduce runtime vulnerabilities; only failure to update causes false-red CI"

must_haves:
  truths:
    - "`npm test` exits 0 against the updated assertion set (matches new section inventory)"
    - "`/pricing#calculator` anchor resolves to the ROICalculator section"
    - "Stub audio files `demo-emergency.mp3` and `demo-routine.mp3` exist in /public/audio/ so dev renders without 404"
  artifacts:
    - path: tests/unit/landing-sections.test.js
      provides: "Updated assertions for Phase 48.1 section inventory (7 sections, no Beyond/Social/HowItWorks, new AudioDemo/Integrations/CostOfSilence/YouStayInControl)"
      contains: "audio-demo"
    - path: src/app/(public)/pricing/page.js
      provides: "id=\"calculator\" anchor on ROICalculator section"
      contains: "id=\"calculator\""
    - path: public/audio/demo-emergency.mp3
      provides: "Placeholder audio (copy of demo-intro.mp3) until real recording ships"
    - path: public/audio/demo-routine.mp3
      provides: "Placeholder audio (copy of demo-intro.mp3) until real recording ships"
  key_links:
    - from: "CostOfSilenceBlock (Plan 02)"
      to: "/pricing#calculator"
      via: "anchor href"
      pattern: "id=\"calculator\""
    - from: "AudioDemoSection (Plan 02)"
      to: "public/audio/demo-emergency.mp3"
      via: "<audio src>"
      pattern: "demo-emergency\\.mp3"
---

<objective>
Wave 0 prerequisites that unblock every other plan in Phase 48.1.

Purpose: The Phase 47 test file (`tests/unit/landing-sections.test.js`) will fail the moment any Phase 48.1 component change lands — it asserts BeyondReceptionistSection exists, asserts FeaturesCarousel has 9 features, asserts RotatingText renders in hero, and asserts an exact regex of page.js whitespace. All these break. We MUST update it first (per RESEARCH §7 and Pitfall 4/7). Additionally, the landing page's "Calculate yours →" deep-link to `/pricing#calculator` (D-05) requires a one-line `id` addition on the pricing page, and AudioDemoSection development requires placeholder mp3 files so dev renders without 404.

Output: Updated test file, one-line pricing anchor edit, two stub audio files.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-CONTEXT.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-RESEARCH.md
@.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-UI-SPEC.md

<interfaces>
<!-- Current ROICalculator wrapper at src/app/(public)/pricing/page.js (exact text to find):
  <section className="relative bg-[#EDEAE7] py-16">
     <div className="absolute inset-x-0 bottom-0 h-24 bg-gradient-to-b from-[#EDEAE7] to-[#F5F5F4] pointer-events-none" />
     <div className="relative max-w-6xl mx-auto px-6">
       <ROICalculator />
     </div>
   </section>

Target:  <section id="calculator" className="relative bg-[#EDEAE7] py-16">
-->

<!-- tests/unit/landing-sections.test.js is a Jest node-env test that reads landing files as strings
     and regex-matches against their source. It does NOT render React. Pattern-match assertions only. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update landing-sections.test.js for Phase 48.1 section inventory</name>
  <files>tests/unit/landing-sections.test.js</files>
  <read_first>
    - tests/unit/landing-sections.test.js (current Phase 47 assertions that must be replaced)
    - src/app/(public)/page.js (current page.js structure — new assertions will check post-rewrite structure, not current)
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-RESEARCH.md §7 "Existing Test Suite" for the full new-assertion list
  </read_first>
  <action>
    Rewrite `tests/unit/landing-sections.test.js` so it asserts Phase 48.1's post-rewrite structure, not Phase 47's. Tests read source files as strings and regex-match (existing pattern — preserve it). Apply the following assertion set verbatim:

    **DELETE these Phase 47 assertions entirely:**
    - Any assertion that `BeyondReceptionistSection` exists or renders (component is being deleted)
    - Any assertion about `PracticalObjectionsGrid` rendering 6 cards (component is being deleted)
    - Any assertion on `FeaturesCarousel` asserting 9 features, or asserting titles "Custom Pickup Rules", "70+ Languages", "Lead Capture & CRM", "Post-Call SMS & Notifications", "Invoicing & Estimates", "Call Analytics & Dashboard"
    - Any assertion on `HeroSection` asserting `RotatingText` import or render
    - Any assertion on `HeroSection` asserting subtitle matches /in charge|stay in control|stay in charge|when you can't|when you're/
    - The regex-match test that asserts `<HowItWorksSection[^>]*\/>\s*<BeyondReceptionistSection[^>]*\/>\s*<FeaturesCarousel[^>]*\/>/m` (exact whitespace regex per Pitfall 7)
    - Any assertion that `SocialProofSection` is imported or rendered
    - Any assertion that `IdentitySection` or `OwnerControlPullQuote` is imported

    **ADD these Phase 48.1 assertions (all are source-string regex matches, same pattern as existing tests):**

    Group: `HeroSection 48.1`
    - `src/app/components/landing/HeroSection.jsx` source matches `/Stop losing \$1,000\+ every time you miss a call\./`
    - Source matches `/Voco AI answers, triages, and books every call/`
    - Source matches `/Answers in English, Spanish, Mandarin, Malay/`
    - Source contains `href="#audio-demo"` (primary CTA anchor)
    - Source does NOT contain `import.*RotatingText` (regex `/import[^;]*RotatingText/` must not match)

    Group: `AudioDemoSection 48.1`
    - `src/app/components/landing/AudioDemoSection.jsx` file exists (use `fs.existsSync`)
    - Source matches `/id="audio-demo"/`
    - Source matches `/__vocoPlayingAudio/` (coordinates with AudioPlayerCard pause singleton)
    - Source contains both `demo-emergency` and `demo-routine` string references

    Group: `IntegrationsStrip 48.1`
    - `src/app/components/landing/IntegrationsStrip.jsx` file exists
    - Source contains "Google Calendar" AND "Outlook"
    - Source contains "Jobber" AND "Housecall Pro" AND "ServiceTitan" AND "Zapier"
    - Source matches `/Coming soon/i`
    - Source matches `/Live/` (for the live-integration chip label)

    Group: `CostOfSilenceBlock 48.1`
    - `src/app/components/landing/CostOfSilenceBlock.jsx` file exists
    - Source matches `/\$260,400/`
    - Source contains `/pricing#calculator` (href target)
    - Source matches `/Calculate yours/`

    Group: `YouStayInControlSection 48.1`
    - `src/app/components/landing/YouStayInControlSection.jsx` file exists
    - Source matches `/You Stay in Control/`
    - Source contains the exact pull quote string `You set the rules. Voco follows them.`
    - Source contains `bg-[#1C1412]` (the dark pull quote background)
    - Source uses `CARD_CLS` (the shared card class name — either imported or defined)

    Group: `FeaturesCarousel-4 48.1`
    - Source contains all 4 pillar titles: "24/7 AI Answering", "Real-Time Calendar Booking", "Speaks Your Trade", "Automated Lead Recovery"
    - Source does NOT contain "Custom Pickup Rules", "Lead Capture & CRM", "Post-Call SMS", "Invoicing & Estimates", "Call Analytics"
    - Source still contains `id="features"` (preserved for ScrollLinePath)

    Group: `FAQSection 48.1`
    - Source references `#audio-demo` (internal link from "Hear it yourself" should target the new audio section)
    - Source does NOT contain `#hero` as the audio anchor (must be updated from the current `#hero` target)

    Group: `page.js 48.1 structure`
    - `src/app/(public)/page.js` source does NOT import `HowItWorksSection`, `BeyondReceptionistSection`, `SocialProofSection`, `IdentitySection`, `OwnerControlPullQuote`, `HeroDemoBlock`, `PracticalObjectionsGrid`
    - Source imports (statically or via `dynamic`) `AudioDemoSection`, `IntegrationsStrip`, `CostOfSilenceBlock`, `YouStayInControlSection`
    - Source contains the fragment `<ScrollLinePath>` wrapping exactly 3 children component tags (IntegrationsStrip, CostOfSilenceBlock, FeaturesCarousel)
    - Regex check for component ordering: the index of `AudioDemoSection` tag < index of `<ScrollLinePath>` < index of `YouStayInControlSection` tag < index of `FAQSection` tag

    Group: `metadata 48.1 (Voco AI rebrand)`
    - `src/app/layout.js` source matches `/title:\s*['"]Voco AI/`
    - Source matches `/openGraph/` AND source matches `/twitter/` (metadata exports these fields)

    Group: `negative scope guard 48.1 (hard constraint per SC #9)`
    - No new file under `src/app/api/` (use a directory-ls equivalent — read package.json + git diff would be outside Jest scope; instead, assert no new import of `createClient` or `@/lib/supabase` from any landing component by grepping all *.jsx under `src/app/components/landing/`)
    - No new migration file under `supabase/migrations/` tied to this phase (assert `supabase/migrations/` directory count is read, baseline preserved — use a hardcoded baseline count captured at test-file-write time, e.g., 50 migrations; if this is brittle, skip and rely on plan-level scope discipline)

    Preserve the existing `describe`/`it` skeleton and `fs.readFileSync` helper pattern. Do NOT install new libs (no @testing-library/react, no jsdom env).

    If any existing assertion is reusable unchanged (e.g., one that checks `FinalCTASection` renders — that component is untouched), keep it.
  </action>
  <verify>
    <automated>node --check tests/unit/landing-sections.test.js && echo 'TEST FILE SYNTAX OK' && (npm test -- --testPathPattern=landing 2>&1 | tail -40; echo 'EXPECTED: tests for Plan 02 components (AudioDemoSection, IntegrationsStrip, CostOfSilenceBlock, YouStayInControlSection) WILL FAIL because those components do not exist yet at Wave 0. Task passes if (a) `node --check` reports the test file is syntactically valid AND (b) Jest harness loads the file without test-runner errors. Per-component file-existence assertions failing is the expected RED state for Wave 0 — they go GREEN as Plan 02 ships.')</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/landing-sections.test.js` file length > 150 lines (source-string regex suite for all 7 new assertion groups)
    - `grep -c "BeyondReceptionistSection" tests/unit/landing-sections.test.js` returns 0 (or all matches are inside negative assertions — assert-NOT form)
    - `grep -c "RotatingText" tests/unit/landing-sections.test.js` returns 0 (or only in negative assertions)
    - `grep "audio-demo" tests/unit/landing-sections.test.js` returns ≥ 1 match
    - `grep "260,400" tests/unit/landing-sections.test.js` returns ≥ 1 match
    - `grep "You set the rules. Voco follows them." tests/unit/landing-sections.test.js` returns ≥ 1 match
    - `grep "Housecall Pro" tests/unit/landing-sections.test.js` returns ≥ 1 match
    - Running `npm test -- --testPathPattern=landing` will FAIL at this point because the new components don't exist yet — this is expected and correct for Wave 0. Tests in the "file exists" group are expected to fail until Plan 02 ships. Document in the commit that RED state is correct for Wave 0.
    - Running `npm test -- --testPathPattern=landing` exits with ONLY the expected "file not found" / "asserts on nonexistent component" failures. There must be no syntax errors or test-harness errors (Jest must successfully load the file).
  </acceptance_criteria>
  <done>
    Test file rewritten. Jest loads it cleanly. Expected failures are those waiting on components from Plan 02/03 (new file existence). No legacy Phase 47 assertions remain.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add id="calculator" anchor to pricing page ROICalculator section</name>
  <files>src/app/(public)/pricing/page.js</files>
  <read_first>
    - src/app/(public)/pricing/page.js (locate the ROICalculator wrapper — current exact markup given in the interfaces block above)
  </read_first>
  <action>
    Find the line `<section className="relative bg-[#EDEAE7] py-16">` that wraps `<ROICalculator />` in `src/app/(public)/pricing/page.js` and change it to `<section id="calculator" className="relative bg-[#EDEAE7] py-16">`. This is the ONLY change to this file.

    Do NOT touch `ROICalculator.jsx` itself (per D-06, ROICalculator on /pricing stays untouched).
    Do NOT add any other anchors, props, or structural changes.
    Do NOT rename or restructure the existing section wrapper classes.
  </action>
  <verify>
    <automated>grep -c 'id="calculator"' src/app/\(public\)/pricing/page.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'id="calculator"' src/app/\(public\)/pricing/page.js` returns exactly 1 match
    - `grep 'relative bg-\[#EDEAE7\] py-16' src/app/\(public\)/pricing/page.js` still returns exactly 1 match (existing class preserved)
    - `git diff src/app/\(public\)/pricing/page.js` shows exactly 1 insertion (the `id="calculator"` attribute) and 1 deletion (the `<section className=` line without id) — net +1 line or same-line edit
    - `src/app/(public)/pricing/ROICalculator.jsx` is NOT modified (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>
    `/pricing#calculator` deep-link target exists. ROICalculator component file unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Place stub audio files for AudioDemoSection development</name>
  <files>public/audio/demo-emergency.mp3, public/audio/demo-routine.mp3</files>
  <read_first>
    - .planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-RESEARCH.md §"Environment Availability" (human dependency note — stubs are placeholder until real recordings ship)
  </read_first>
  <action>
    Copy the existing `public/audio/demo-intro.mp3` file to TWO new paths so AudioDemoSection (built in Plan 02) renders without 404 during development:
    1. `public/audio/demo-emergency.mp3` — stub placeholder for the Emergency tab
    2. `public/audio/demo-routine.mp3` — stub placeholder for the Routine tab

    Exact bash command (run from repo root):
    ```
    cp public/audio/demo-intro.mp3 public/audio/demo-emergency.mp3
    cp public/audio/demo-intro.mp3 public/audio/demo-routine.mp3
    ```

    Do NOT delete or modify `demo-intro.mp3`, `demo-mid.mp3`, or `demo-outro.mp3` (they are used by AudioPlayerCard for the existing HeroDemoBlock which is being removed in Plan 04 — but keep them in place; a human may reference them).

    Add a note in the commit message: "STUBS ONLY — real recordings via staging Gemini agent must replace these before `/gsd-verify-work` (phase gate per RESEARCH §Environment Availability)."
  </action>
  <verify>
    <automated>test -f public/audio/demo-emergency.mp3 && test -f public/audio/demo-routine.mp3 && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/audio/demo-emergency.mp3` succeeds (exit 0)
    - `test -f public/audio/demo-routine.mp3` succeeds (exit 0)
    - `stat -f%z public/audio/demo-emergency.mp3` returns the same byte count as `stat -f%z public/audio/demo-intro.mp3` (confirms stub is a true copy)
    - `stat -f%z public/audio/demo-routine.mp3` returns the same byte count as `stat -f%z public/audio/demo-intro.mp3`
    - `test -f public/audio/demo-intro.mp3` still succeeds (not deleted)
  </acceptance_criteria>
  <done>
    Two stub mp3 files in place. AudioDemoSection will render without network 404 in dev. Human must record real audio before phase verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan contains no runtime code changes that cross trust boundaries. All tasks are scaffolding (test file, anchor attribute, static asset placeholder).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.1-01-01 | Tampering | tests/unit/landing-sections.test.js | accept | Test file cannot introduce runtime vulnerabilities; worst-case failure is false-red CI, fixable by revision |
| T-48.1-01-02 | Information Disclosure | public/audio/demo-*.mp3 | accept | Audio is marketing content, intentionally public; stubs are copies of an already-public file |
| T-48.1-01-03 | Elevation of Privilege | src/app/(public)/pricing/page.js anchor | accept | HTML `id` attribute cannot elevate privilege; no auth surface touched |
</threat_model>

<verification>
- `npm test -- --testPathPattern=landing` loads and executes the updated test file without Jest harness errors (expected assertion failures for components-not-yet-built are acceptable)
- `grep 'id="calculator"' src/app/\(public\)/pricing/page.js` returns 1 match
- Two new audio stub files exist at `public/audio/demo-{emergency,routine}.mp3`
- `git diff --stat` shows exactly 4 files touched (1 test, 1 pricing page, 2 audio binaries)
</verification>

<success_criteria>
- ROADMAP SC #3 path unblocked: `/pricing#calculator` deep-link target exists.
- ROADMAP SC #4 path unblocked: AudioDemoSection can render stub audio without network failure.
- ROADMAP SC #9 respected: no dashboard, backend, API, or migration changes. Pricing-page edit is the ONE whitelisted exception (RESEARCH §6, CONTEXT.md code_context).
- Wave 1 plans can proceed: test file is updated, anchor exists, stubs exist.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-landing-page-revenue-recovery-repositioning/48.1-01-SUMMARY.md`
</output>
