---
phase: 49
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/app/dashboard/layout.js
  - src/app/dashboard/ImpersonationBanner.js
  - src/app/dashboard/TrialCountdownBanner.js
  - src/app/dashboard/BillingWarningBanner.js
  - src/components/dashboard/BottomTabBar.jsx
  - src/components/dashboard/OfflineBanner.jsx
  - src/components/dashboard/ConflictAlertBanner.js
autonomous: true
requirements:
  - DARK-08
must_haves:
  truths:
    - "User toggles to dark mode and the dashboard /main content wrapper/ background is dark (not warm-neutral light)"
    - "Sidebar (DashboardSidebar) remains navy in both modes — intentionally not migrated"
    - "ImpersonationBanner reads as urgent amber on light surface AND amber-on-dark in dark mode"
    - "TrialCountdownBanner reads as informational blue in both modes"
    - "BillingWarningBanner reads as destructive red in both modes"
    - "BottomTabBar (mobile) flips white → card with theme"
  artifacts:
    - path: "src/app/dashboard/layout.js"
      provides: "Main wrapper migrated bg-[#F5F5F4] → bg-background (light = same warm-neutral, dark = near-black)"
      contains: "bg-background"
    - path: "src/app/dashboard/ImpersonationBanner.js"
      provides: "Amber banner with dark variants per UI-SPEC banner pattern"
      contains: "dark:bg-amber-950/40"
    - path: "src/app/dashboard/TrialCountdownBanner.js"
      provides: "Blue banner with dark variants"
      contains: "dark:bg-blue-950/40"
    - path: "src/app/dashboard/BillingWarningBanner.js"
      provides: "Red destructive banner with dark variants"
      contains: "dark:bg-red-950/40"
    - path: "src/components/dashboard/BottomTabBar.jsx"
      provides: "Mobile bottom tab bar bg-white → bg-card border-stone-200 → border-border"
      contains: "bg-card"
  key_links:
    - from: "src/app/dashboard/layout.js"
      to: "globals.css :root --background"
      via: "Tailwind utility bg-background"
      pattern: "bg-background"
    - from: "src/app/dashboard/ImpersonationBanner.js"
      to: "Tailwind dark: variant"
      via: "@custom-variant dark resolved via .dark class on <html>"
      pattern: "dark:bg-amber-950"
---

<objective>
Migrate the dashboard layout shell — main content wrapper, mobile bottom tab bar, and the three system banners (impersonation, trial countdown, billing warning) plus two secondary banners (offline, conflict alert) — to dark-mode-aware tokens following UI-SPEC §System Banners and §Layout Shell category rules.

Purpose: Make the visible dashboard chrome (everything that wraps content) respond to the theme. This plan is the highest-visibility surface — when the user clicks the toggle, this is what they see flip first. Sidebar is intentionally excluded (stays navy per STATE.md decision).

Output: Layout shell + 5 banner files migrated. No design-tokens.js or globals.css edits (those are Plan 01's territory — this plan only consumes them).
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-01-SUMMARY.md
@src/app/dashboard/layout.js
@src/app/dashboard/ImpersonationBanner.js
@src/app/dashboard/TrialCountdownBanner.js
@src/app/dashboard/BillingWarningBanner.js
@src/components/dashboard/BottomTabBar.jsx

<interfaces>
Banner pattern (UI-SPEC §System Banners — verbatim, do not improvise):
  bg-{color}-50  → dark:bg-{color}-950/40
  text-{color}-900  → dark:text-{color}-200
  border-{color}-200 (or 300) → dark:border-{color}-800/60
  hover:bg-{color}-100 → dark:hover:bg-{color}-900/40

  ImpersonationBanner color = amber
  TrialCountdownBanner color = blue
  BillingWarningBanner color = red (destructive)
  ConflictAlertBanner color = amber (warning)
  OfflineBanner color = stone (neutral) — use bg-muted/text-muted-foreground/border-border per UI-SPEC neutral row

Layout shell (UI-SPEC §Layout Shell):
  dashboard/layout.js: bg-[#F5F5F4] → bg-background  (resolves to #F5F5F4 via --warm-surface alias OR shadcn's --background — Plan 01 declared --warm-surface but the existing --background already resolves to oklch(1 0 0) light / oklch(0.145 0 0) dark; UI-SPEC line 162 says use bg-background)
  Top bar glass: handled by design-tokens.js glass.topBar in Plan 01 — consumers of this token automatically theme. Verify dashboard/layout.js does not redeclare top-bar classes locally.
  BottomTabBar.jsx: bg-white → bg-card; border-stone-200 → border-border; text-stone-* (active tab indicator etc.) review case-by-case.

Disallowed hex in this plan's files (audit will flag):
  #C2410C, #9A3412, #F5F5F4, #0F172A, #475569

DashboardSidebar.jsx is OUT OF SCOPE for this plan (excluded from hex audit; Plan 01 owned the toggle button addition; sidebar styling is intentional permanent navy).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate dashboard/layout.js + BottomTabBar + secondary banners (Offline, ConflictAlert)</name>
  <files>src/app/dashboard/layout.js, src/components/dashboard/BottomTabBar.jsx, src/components/dashboard/OfflineBanner.jsx, src/components/dashboard/ConflictAlertBanner.js</files>
  <read_first>
    - src/app/dashboard/layout.js (full file — find every #F5F5F4, white, stone literal)
    - src/components/dashboard/BottomTabBar.jsx (full file)
    - src/components/dashboard/OfflineBanner.jsx (full file)
    - src/components/dashboard/ConflictAlertBanner.js (full file)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Layout Shell (lines 159–168), §System Banners (lines 169–180)
    - src/lib/design-tokens.js (post-Plan-01 — verify glass.topBar already migrated; consumers should NOT re-implement)
  </read_first>
  <behavior>
    - dashboard/layout.js wrapper uses `bg-background` instead of `bg-[#F5F5F4]`. Top bar glass (if defined inline) uses `bg-card/80 backdrop-blur-md border-b border-border` per UI-SPEC.
    - BottomTabBar mobile chrome flips white → card; border flips stone → border-border; active tab indicator uses `var(--brand-accent)` (no literal hex).
    - OfflineBanner uses neutral pattern: `bg-muted text-muted-foreground border-border`.
    - ConflictAlertBanner uses amber warning pattern (same as ImpersonationBanner — Task 2 reference).
  </behavior>
  <action>
    1. **src/app/dashboard/layout.js:**
       - Find `bg-[#F5F5F4]` on the main wrapper. Replace with `bg-background`.
       - Find any inline top-bar glass classes (`bg-white/80 backdrop-blur-md border-b border-stone-200/60`). If present locally, replace with `bg-card/80 backdrop-blur-md border-b border-border`. Prefer importing `glass.topBar` from `@/lib/design-tokens` if not already.
       - Find any other literal hex from the disallowed list (#C2410C, #9A3412, #0F172A, #475569). Convert: `#C2410C` → `var(--brand-accent)` (use `bg-[var(--brand-accent)]` arbitrary syntax); `#0F172A` → `text-foreground`; `#475569` → `text-muted-foreground`; `#F5F5F4` → `bg-background`.
       - Do NOT touch the `<TooltipProvider>` wrapping (line 47 area) — it is unrelated.
    2. **src/components/dashboard/BottomTabBar.jsx:**
       - Replace any `bg-white` on the tab bar container with `bg-card`.
       - Replace `border-stone-200` (or similar) with `border-border`.
       - Active tab indicator: if literal `text-[#C2410C]` or `border-[#C2410C]` present, replace with `text-[var(--brand-accent)]` / `border-[var(--brand-accent)]`.
       - Inactive tab text: `text-stone-500` or `text-[#475569]` → `text-muted-foreground`.
       - If any hover state uses `hover:bg-stone-100`, change to `hover:bg-accent`.
    3. **src/components/dashboard/OfflineBanner.jsx:**
       - Convert to neutral pattern: `bg-muted text-muted-foreground border-border` for the banner shell.
       - If the banner uses a destructive accent, downgrade to neutral (offline ≠ destructive — UI-SPEC neutral row). If existing copy treats it as destructive, change shell to red pattern: `bg-red-50 dark:bg-red-950/40 text-red-900 dark:text-red-200 border-red-200 dark:border-red-800/60`. Pick whichever matches the existing visual intent — prefer neutral.
    4. **src/components/dashboard/ConflictAlertBanner.js:**
       - Apply amber warning banner pattern verbatim:
         - `bg-amber-50` → `bg-amber-50 dark:bg-amber-950/40`
         - `text-amber-900` (or 800) → `text-amber-900 dark:text-amber-200`
         - `border-amber-300` (or 200) → `border-amber-300 dark:border-amber-800/60`
         - `hover:bg-amber-100` → `hover:bg-amber-100 dark:hover:bg-amber-900/40`
       - Convert any hex literals in the file using the same map as step 1.
    5. After edits, run hex-audit grep against just these 4 files — must return 0 disallowed hex.
    6. Run `npm run build` — must succeed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const files=['src/app/dashboard/layout.js','src/components/dashboard/BottomTabBar.jsx','src/components/dashboard/OfflineBanner.jsx','src/components/dashboard/ConflictAlertBanner.js'];const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;files.forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0])});console.log('layout/banners ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/app/dashboard/layout.js src/components/dashboard/BottomTabBar.jsx src/components/dashboard/OfflineBanner.jsx src/components/dashboard/ConflictAlertBanner.js` returns 0 matches.
    - `grep -c "bg-background" src/app/dashboard/layout.js` returns >= 1.
    - `grep -c "bg-card" src/components/dashboard/BottomTabBar.jsx` returns >= 1.
    - `grep -c "border-border" src/components/dashboard/BottomTabBar.jsx` returns >= 1.
    - ConflictAlertBanner contains `dark:bg-amber-950/40` AND `dark:text-amber-200` AND `dark:border-amber-800/60`.
    - OfflineBanner uses either `bg-muted` (neutral) OR `dark:bg-red-950/40` (destructive) — one must be present.
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>Layout wrapper, bottom tab bar, and 2 secondary banners migrated. Hex audit clean for these 4 files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the 3 system banners (Impersonation, TrialCountdown, BillingWarning)</name>
  <files>src/app/dashboard/ImpersonationBanner.js, src/app/dashboard/TrialCountdownBanner.js, src/app/dashboard/BillingWarningBanner.js</files>
  <read_first>
    - src/app/dashboard/ImpersonationBanner.js (full file — identify amber tones used)
    - src/app/dashboard/TrialCountdownBanner.js (full file — identify blue tones used)
    - src/app/dashboard/BillingWarningBanner.js (full file — identify red/amber tones used; some destructive contexts)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §System Banners (lines 169–180), §Status Badges and Urgency Pills (for color shift pattern)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Banner migration code example (lines 491–504)
  </read_first>
  <behavior>
    - ImpersonationBanner: amber light → amber-950/40 dark + amber-200 text dark + amber-800/60 border dark.
    - TrialCountdownBanner: blue light → blue-950/40 dark + blue-200 text dark + blue-800/60 border dark.
    - BillingWarningBanner: red light → red-950/40 dark + red-200 text dark + red-800/60 border dark. (If file has both warning and error states with different color families, apply pattern per family.)
    - All three banners remain visually urgent (high contrast, visible on dark surface).
    - No literal hex from disallowed list remains.
  </behavior>
  <action>
    1. **src/app/dashboard/ImpersonationBanner.js** — apply the AMBER pattern verbatim:
       Find every Tailwind amber class on the banner shell:
       - `bg-amber-50` → `bg-amber-50 dark:bg-amber-950/40`
       - `text-amber-900` (or `text-amber-800`) → `{existing} dark:text-amber-200`
       - `border-amber-200` (or `border-amber-300`) → `{existing} dark:border-amber-800/60`
       - `hover:bg-amber-100` → `{existing} dark:hover:bg-amber-900/40`
       - Any text icon color following the same shift: `text-amber-700` → `dark:text-amber-300`
       Convert any disallowed hex via the standard map (`#0F172A` → `text-foreground`, etc.).
    2. **src/app/dashboard/TrialCountdownBanner.js** — apply BLUE pattern:
       - `bg-blue-50` → `bg-blue-50 dark:bg-blue-950/40`
       - `text-blue-900` → `{existing} dark:text-blue-200`
       - `border-blue-200` → `{existing} dark:border-blue-800/60`
       - `hover:bg-blue-100` → `{existing} dark:hover:bg-blue-900/40`
       - Icon variants: `text-blue-700` → `dark:text-blue-300`
       - If the banner uses a CTA button styled with `bg-blue-600`, leave the active-button style alone (button has white text on saturated blue — readable in both modes); only adjust the surrounding banner shell.
    3. **src/app/dashboard/BillingWarningBanner.js** — may have multiple severity tiers (warning vs error). Apply per family:
       - For `amber-*` classes: same as ImpersonationBanner.
       - For `red-*` classes: red pattern — `bg-red-50` → `dark:bg-red-950/40`, `text-red-900` → `dark:text-red-200`, `border-red-200` → `dark:border-red-800/60`, `hover:bg-red-100` → `dark:hover:bg-red-900/40`, `text-red-700` → `dark:text-red-300`.
       - Convert any disallowed hex.
       - If the banner contains a "Resolve" or "Upgrade" CTA, leave button color alone (CTAs use design-tokens.js `btn.primary` which is already migrated, OR are intentionally white-on-red — check before changing).
    4. Hex audit on the 3 files — must be clean.
    5. `npm run build` must succeed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;['src/app/dashboard/ImpersonationBanner.js','src/app/dashboard/TrialCountdownBanner.js','src/app/dashboard/BillingWarningBanner.js'].forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0]);if(!/dark:/.test(c))throw new Error(p+' has no dark: variants — banner must be theme-aware')});console.log('banners ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dark:bg-amber-950/40" src/app/dashboard/ImpersonationBanner.js` returns >= 1.
    - `grep -c "dark:text-amber-200" src/app/dashboard/ImpersonationBanner.js` returns >= 1.
    - `grep -c "dark:border-amber-800/60" src/app/dashboard/ImpersonationBanner.js` returns >= 1.
    - `grep -c "dark:bg-blue-950/40" src/app/dashboard/TrialCountdownBanner.js` returns >= 1.
    - `grep -c "dark:text-blue-200" src/app/dashboard/TrialCountdownBanner.js` returns >= 1.
    - `grep -c "dark:border-blue-800/60" src/app/dashboard/TrialCountdownBanner.js` returns >= 1.
    - `grep -c "dark:bg-red-950/40\|dark:bg-amber-950/40" src/app/dashboard/BillingWarningBanner.js` returns >= 1.
    - `grep -c "dark:text-red-200\|dark:text-amber-200" src/app/dashboard/BillingWarningBanner.js` returns >= 1.
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/app/dashboard/{ImpersonationBanner,TrialCountdownBanner,BillingWarningBanner}.js` returns nothing.
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>All three system banners theme correctly. Light mode appearance is unchanged from baseline. Dark mode shows urgent-but-readable amber/blue/red on dark surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure CSS-class migration; no data flow, no auth, no input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Information Disclosure | ImpersonationBanner readability in dark mode | mitigate | Banner must remain visually urgent in both modes — UI-SPEC §System Banners color contrast pattern enforces amber-200 on amber-950/40 (~7:1 ratio per UI-SPEC §Color contrast validation table). Verified by acceptance grep for the exact class strings. |
| T-49-02-02 | Information Disclosure | BillingWarningBanner readability | mitigate | Same as T-49-02-01 with red family. UI-SPEC contrast table validates red-200 on red-950/40. |

**Confirmation of absence:** No new auth flows, no PII handling, no new endpoints, no XSS/CSRF surfaces.
</threat_model>

<verification>
- Hex audit grep returns 0 matches across this plan's 7 files.
- `npm run build` succeeds.
- After Plans 02/03/04/05 all merge, `npx jest tests/unit/dark-mode-hex-audit.test.js` runs green for the union of all migrated files.
- Manual checklist: "Layout Shell — Main bg / Bottom Tab Bar" PASS in both modes; all three system banners PASS in both modes.
</verification>

<success_criteria>
1. Dashboard main content bg flips between warm-neutral and near-black on theme toggle (DARK-08).
2. BottomTabBar (mobile) flips white → card (DARK-08).
3. All 3 system banners + 2 secondary banners theme correctly with the verbatim UI-SPEC banner pattern (DARK-08).
4. No disallowed hex remains in any of the 7 files this plan touches.
5. Sidebar (DashboardSidebar.jsx) is NOT modified by this plan — stays navy permanently per STATE.md decision.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-02-SUMMARY.md` documenting:
- Per-file diff summary (light classes + dark variants added)
- Any banners that had unusual structure (e.g., multi-tier severity in BillingWarningBanner) and how the migration handled each tier
- Confirmation no design-tokens.js or globals.css edits were made (Plan 01 territory)
- Manual checklist rows signed: "Layout Shell — Main bg / Bottom Tab Bar / Bottom tab indicator", "System Banners — Impersonation / TrialCountdown / BillingWarning / OfflineBanner / ConflictAlertBanner"
</output>
