---
phase: 49
plan: 05
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - "src/components/dashboard/**/*.{js,jsx}"
  - "src/app/dashboard/**/page.{js,jsx}"
  - "src/app/dashboard/error.js"
  - "src/app/dashboard/loading.js"
autonomous: false
requirements:
  - DARK-03
  - POLISH-08
must_haves:
  truths:
    - "After this plan runs, every dashboard page (calls/calendar/leads/estimates/invoices/services/settings/analytics/more/* + dashboard home) renders readable in dark mode"
    - "Every dashboard component file (excluding the 14 explicitly owned by Plans 02-04 and the 3 Phase 50 exclusions) has 0 disallowed hex"
    - "tests/unit/dark-mode-hex-audit.test.js (Wave 0 from Plan 01) passes after this plan completes"
    - "All headings consolidated to text-foreground; all body text consolidated to text-muted-foreground (POLISH-08)"
  artifacts:
    - path: "src/app/dashboard/page.js"
      provides: "Dashboard home migrated"
      contains: "text-foreground"
    - path: "src/app/dashboard/error.js"
      provides: "Dashboard error boundary themed"
      contains: "bg-card"
    - path: "src/app/dashboard/loading.js"
      provides: "Dashboard loading state themed"
      contains: "bg-muted"
    - path: "tests/unit/dark-mode-hex-audit.test.js"
      provides: "After this plan, runs GREEN (zero disallowed hex in dashboard trees)"
      contains: "no disallowed hex"
  key_links:
    - from: "src/app/dashboard/**/page.js"
      to: "src/lib/design-tokens.js"
      via: "import { card, glass, heading, body, focus, selected, btn } from '@/lib/design-tokens'"
      pattern: "from '@/lib/design-tokens'"
    - from: "src/components/dashboard/**/*.{js,jsx}"
      to: "globals.css :root and .dark CSS variables"
      via: "Tailwind utilities (bg-card, text-foreground, text-muted-foreground, border-border, bg-muted, hover:bg-accent) + var(--brand-accent) for brand-orange usages"
      pattern: "bg-card|text-foreground|text-muted-foreground|var\\(--brand-accent\\)"
---

<objective>
Sweep the remaining dashboard page and component files (everything NOT owned by Plans 02-04) to the same token migration. This is the bulk paint pass — ~58 component files and 28 page files containing ~500 disallowed hex occurrences. After this plan, the Wave 0 hex-audit test (RED-by-design from Plan 01) flips GREEN.

Purpose: DARK-03 is the broadest requirement — ALL dashboard component files must replace hardcoded hex with dark-mode-aware tokens. Plans 02-04 covered the high-risk surfaces (layout shell, banners, flyouts, badges); this plan eliminates the long tail.

Output: Every dashboard component and page file (with explicit exclusions) is hex-free per the standard 5-hex disallowed list. POLISH-08 typography consolidation is complete across the dashboard tree.

**Note:** This is the largest plan (3 tasks, broadly scoped). Acceptance is gated by the Wave 0 grep test, which makes per-file enumeration unnecessary — the test enforces zero drift across the entire tree.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-01-SUMMARY.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-02-SUMMARY.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-03-SUMMARY.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-04-SUMMARY.md
@src/lib/design-tokens.js
@tests/unit/dark-mode-hex-audit.test.js

<interfaces>
Standard token migration map (apply uniformly):
  bg-[#F5F5F4]   → bg-background  OR  bg-muted (depends on context: page-level wrapper = bg-background; inner panel = bg-muted)
  bg-[#FAFAF9]   → bg-card        (elevated surface)
  bg-white       → bg-card         (only when it's a component surface, NOT decorative chips)
  bg-[#0F172A]   → KEEP IF SIDEBAR-ADJACENT navy panel; otherwise text-foreground role
  text-[#0F172A] → text-foreground (POLISH-08)
  text-[#475569] → text-muted-foreground (POLISH-08)
  text-[#C2410C] → text-[var(--brand-accent)]
  bg-[#C2410C]   → bg-[var(--brand-accent)]  (with text-[var(--brand-accent-fg)] for buttons)
  bg-[#9A3412]   → bg-[var(--brand-accent-hover)]  (active/pressed state of brand button)
  border-[#C2410C] → border-[var(--brand-accent)]
  border-stone-200 → border-border
  hover:bg-stone-100 → hover:bg-accent
  bg-stone-50 / bg-stone-100 (panel bg) → bg-muted

Buttons: prefer importing `import { btn } from '@/lib/design-tokens'` and using `className={btn.primary}` for primary CTAs rather than re-implementing.

Selection / focus state (when present, e.g., active card / selected day): Apply UI-SPEC §Selection mapping — `border-[var(--brand-accent)] bg-[var(--selected-fill)]`.

Status pill / badge categorical migration: If a file inlines categorical colors (bg-green-50, etc.), apply UI-SPEC §Status Badges category mapping — same as Plan 04.

Files OWNED by other plans (DO NOT TOUCH in this plan):
  - Plan 01: src/app/globals.css, src/app/layout.js, src/components/theme-provider.jsx, src/lib/design-tokens.js, src/components/dashboard/DashboardSidebar.jsx
  - Plan 02: src/app/dashboard/layout.js, src/app/dashboard/{Impersonation,TrialCountdown,BillingWarning}Banner.js, src/components/dashboard/{BottomTabBar,OfflineBanner,ConflictAlertBanner}.{jsx,js}
  - Plan 03: src/components/dashboard/{LeadFlyout,AppointmentFlyout,QuickBookSheet,ChatbotSheet,ExternalEventSheet,TimeBlockSheet,RecordPaymentDialog,RecurringSetupDialog}.{jsx,js}
  - Plan 04: src/components/dashboard/{LeadStatusPills,BookingStatusBadge,EstimateStatusBadge,InvoiceStatusBadge,RecurringBadge,LeadFilterBar}.{jsx,js}

Files PERMANENTLY excluded (Phase 50 scope — DO NOT MIGRATE here):
  - src/components/dashboard/AnalyticsCharts.jsx (41 hex — Recharts inline SVG, needs useTheme())
  - src/components/dashboard/CalendarView.js (23 hex — dynamic class concatenation, needs useTheme())

Files INTENTIONALLY navy (preserve as-is — exclusion confirmed in tests/unit/dark-mode-hex-audit.test.js):
  - src/components/dashboard/DashboardSidebar.jsx (sidebar bg-[#0F172A] is permanent)

Pitfall 6 reminder: Replacing `text-[#0F172A]` blanket-globally would break sidebar text or text-on-orange buttons. Read each line's context. Most replacements ARE safe (they're heading text on light-card surfaces) but verify before replacing.

Files this plan MUST cover (the rest of the tree):
  Components (~58 files): all of src/components/dashboard/*.{js,jsx} EXCLUDING the lists above.
  Pages (~28 files): all of src/app/dashboard/**/page.{js,jsx} + error.js + loading.js (the layout.js and 3 banners are Plan 02's).

  Notable high-hex pages from RESEARCH.md hex audit (~250 hex across 24 pages — primary targets):
  - src/app/dashboard/page.js (dashboard home — Phase 48 work)
  - src/app/dashboard/leads/page.js
  - src/app/dashboard/calls/page.js
  - src/app/dashboard/calendar/page.js
  - src/app/dashboard/analytics/page.js
  - src/app/dashboard/estimates/page.js + new/page.js + [id]/page.js
  - src/app/dashboard/invoices/page.js + new/page.js + batch-review/page.js + [id]/page.js
  - src/app/dashboard/services/page.js
  - src/app/dashboard/settings/page.js
  - src/app/dashboard/more/page.js + 13 more/* sub-pages

  Notable high-hex components (~371 hex across 59 components — primary targets):
  - DailyOpsHub.jsx, RecentActivityFeed.jsx, SetupChecklist.jsx, SetupChecklistLauncher.jsx
  - LeadCard.jsx, CallsTile.jsx, HotLeadsTile.jsx, TodayAppointmentsTile.jsx, UsageTile.jsx, UsageRingGauge.js
  - InvoiceEditor.jsx, EstimateSummaryCards.jsx, InvoiceSummaryCards.jsx, LineItemRow.jsx, TierEditor.jsx
  - SettingsAISection.jsx, SettingsCalendarSection.jsx, SettingsHoursSection.jsx, NotificationPreferences.jsx, EscalationChainSection.js
  - VoicePickerSection.jsx, WorkingHoursEditor.js, ZoneManager.js, CalendarSyncCard.js
  - DashboardTour.jsx, CommandPalette.jsx, HelpDiscoverabilityCard.jsx
  - ChatMessage.jsx, ChatNavLink.jsx, ChatProvider.jsx, ChecklistItem.jsx, MoreBackButton.jsx, ContactCard.js, CustomerTimeline.jsx, DocumentListShell.jsx, EmptyStateAnalytics.jsx, EmptyStateCalendar.jsx, EmptyStateLeads.jsx, InvoiceSyncIndicator.jsx, PaymentLog.jsx, ReminderToggle.jsx, RevenueInput.jsx, SetupCompleteBar.jsx, SortableServiceRow.js, TranscriptViewer.jsx, TypingIndicator.jsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Component sweep — src/components/dashboard/*.{js,jsx} (excluding owned files)</name>
  <files>src/components/dashboard/*.{js,jsx}</files>
  <read_first>
    - src/lib/design-tokens.js (post-Plan-01 — verify exports available for import)
    - tests/unit/dark-mode-hex-audit.test.js (the gate this task must satisfy)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Component Category Treatments (lines 156–217 — covers all categories you'll encounter)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Pitfall 6 (typography sweep risk), §Pitfall 7 (token drift)
    - All summaries from Plans 02-04 (49-02-SUMMARY.md, 49-03-SUMMARY.md, 49-04-SUMMARY.md) to confirm exact owned-file boundaries
    - Each file BEFORE editing it (per Pitfall 6 — context-aware replacement, not blind sed)
  </read_first>
  <behavior>
    - Every component file in src/components/dashboard/ that is NOT in the owned-files exclusion list AND NOT in the Phase-50 exclusion list AND NOT DashboardSidebar.jsx contains 0 disallowed hex (the standard 5).
    - Status pill / badge inline categorical colors use UI-SPEC dark variants (Plan 04 pattern).
    - Selected/active states use var(--brand-accent) + var(--selected-fill).
    - CTA buttons import btn from design-tokens or use var(--brand-accent) inline.
    - Headings use text-foreground; body uses text-muted-foreground (POLISH-08).
  </behavior>
  <action>
    1. **Build a worklist:** List all `src/components/dashboard/*.{js,jsx}` files. Subtract:
       - Plan 02 ownership: BottomTabBar.jsx, OfflineBanner.jsx, ConflictAlertBanner.js
       - Plan 03 ownership: LeadFlyout.jsx, AppointmentFlyout.js, QuickBookSheet.js, ChatbotSheet.jsx, ExternalEventSheet.js, TimeBlockSheet.js, RecordPaymentDialog.jsx, RecurringSetupDialog.jsx
       - Plan 04 ownership: LeadStatusPills.jsx, BookingStatusBadge.js, EstimateStatusBadge.jsx, InvoiceStatusBadge.jsx, RecurringBadge.jsx, LeadFilterBar.jsx
       - Phase 50 exclusion: AnalyticsCharts.jsx, CalendarView.js
       - Permanent navy: DashboardSidebar.jsx (already migrated for toggle in Plan 01; intentional navy + orange literals stay)
       - Non-component file: usage-threshold.js (pure helper, no JSX/className — verify it has no hex; if it does, skip it as it's pure logic).

    2. **For each file in the worklist** (~50 files), perform context-aware migration:
       a. Read the file in full.
       b. For each occurrence of a disallowed hex (`#C2410C`, `#9A3412`, `#F5F5F4`, `#0F172A`, `#475569`):
          - Identify the role from surrounding markup (heading? body text? button bg? card surface? selected border?).
          - Apply the matching map from <interfaces>.
       c. For each inline categorical color (bg-{green|amber|blue|red|orange}-{50|700} etc.) on a status/badge-shaped element: apply UI-SPEC §Status Badges category dark variants.
       d. For each occurrence of `bg-white` on a component-surface (not decorative chip): replace with `bg-card` or `bg-muted` (card if elevated; muted if inline).
       e. Buttons: if the component re-implements the orange CTA inline, replace with `import { btn } from '@/lib/design-tokens'` + `className={btn.primary}`. (Plan 01 already migrated btn.primary to var() form.)
       f. Selected states: use `border-[var(--brand-accent)] bg-[var(--selected-fill)]` per UI-SPEC §Selection.
       g. Focus rings: prefer `import { focus } from '@/lib/design-tokens'` + `className={focus.ring}` for any focus-visible target.

    3. **Group files by archetype to maintain consistency:**
       - **Tile cards** (CallsTile, HotLeadsTile, TodayAppointmentsTile, UsageTile, UsageRingGauge): card.base from design-tokens (bg-card border-border) for outer wrapper; heading + body tokens for text; inline status hint colors via UI-SPEC categorical.
       - **List rows** (LeadCard, ChecklistItem, ContactCard, LineItemRow, SortableServiceRow): bg-card hover:bg-accent; text-foreground heading + text-muted-foreground body; selected → border-[var(--brand-accent)] bg-[var(--selected-fill)].
       - **Editors / forms** (InvoiceEditor, EstimateSummaryCards, InvoiceSummaryCards, TierEditor, WorkingHoursEditor, ZoneManager, RevenueInput): form labels → text-foreground; helper text → text-muted-foreground; section dividers → border-border; section bg → bg-card or bg-muted.
       - **Settings sections** (SettingsAISection, SettingsCalendarSection, SettingsHoursSection, NotificationPreferences, EscalationChainSection, CalendarSyncCard, VoicePickerSection): same as Editors archetype.
       - **Content cards** (RecentActivityFeed, DailyOpsHub, SetupChecklist, SetupChecklistLauncher, SetupCompleteBar, HelpDiscoverabilityCard, EmptyState*): card.base wrapper; heading + body tokens; CTAs use btn.primary.
       - **Chat surfaces** (ChatMessage, ChatNavLink, ChatProvider, TypingIndicator, TranscriptViewer): bg-muted for AI bubbles, bg-primary text-primary-foreground for user, text-foreground for content; ChatProvider has no className (logic only — no migration needed unless it has hex).
       - **Overlays / popovers** (CommandPalette, DashboardTour): use shadcn primitives' theme — local className overrides → bg-popover text-popover-foreground border-border for popover surfaces.
       - **Misc** (MoreBackButton, CustomerTimeline, DocumentListShell, InvoiceSyncIndicator, PaymentLog, ReminderToggle): apply standard map.

    4. **After every ~10 files migrated**, run: `npx jest tests/unit/dark-mode-hex-audit.test.js --bail` and inspect which files still fail. Iterate until all components green (pages still RED until Task 2).

    5. `npm run build` must succeed at the end.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const path=require('path');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;const exclude=new Set(['BottomTabBar.jsx','OfflineBanner.jsx','ConflictAlertBanner.js','LeadFlyout.jsx','AppointmentFlyout.js','QuickBookSheet.js','ChatbotSheet.jsx','ExternalEventSheet.js','TimeBlockSheet.js','RecordPaymentDialog.jsx','RecurringSetupDialog.jsx','LeadStatusPills.jsx','BookingStatusBadge.js','EstimateStatusBadge.jsx','InvoiceStatusBadge.jsx','RecurringBadge.jsx','LeadFilterBar.jsx','AnalyticsCharts.jsx','CalendarView.js','DashboardSidebar.jsx']);const dir='src/components/dashboard';const fails=[];for(const name of f.readdirSync(dir)){if(exclude.has(name))continue;const p=path.join(dir,name);if(!f.statSync(p).isFile())continue;if(!/\.(js|jsx)$/.test(name))continue;const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)fails.push(name+': '+m[0])}if(fails.length)throw new Error('Hex remains in '+fails.length+' files:\\n'+fails.join('\\n'));console.log('component sweep ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Bash command `node -e "..."` enumerating src/components/dashboard/*.{js,jsx} (minus exclusion list) finds 0 disallowed hex matches.
    - At least 30 of the migrated component files contain `text-foreground` OR `text-muted-foreground` (POLISH-08 consolidation visible across the tree).
    - At least 20 of the migrated component files contain `bg-card` OR `bg-muted` (surface migration visible).
    - At least 10 of the migrated component files contain `var(--brand-accent)` OR `import { btn` from '@/lib/design-tokens' (CTA tokens used).
    - `grep -l "var(--selected-fill)\|btn.primary\|btn\\.primary" src/components/dashboard/*.{js,jsx} | wc -l` returns >= 5 (selected state and btn import patterns adopted).
    - `npm run build` exit 0.
    - The 19 excluded files (Plans 02-04 owned + Phase 50 + DashboardSidebar) remain UNCHANGED by this task — verify via `git diff --stat src/components/dashboard/{LeadFlyout,LeadStatusPills,DashboardSidebar,AnalyticsCharts,CalendarView,...}.{jsx,js}` shows 0 lines changed for those files.
  </acceptance_criteria>
  <done>All ~50 in-scope component files free of disallowed hex; Wave 0 hex-audit test passes for the components portion; build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Page sweep — src/app/dashboard/**/page.{js,jsx} + error.js + loading.js</name>
  <files>src/app/dashboard/page.js, src/app/dashboard/error.js, src/app/dashboard/loading.js, src/app/dashboard/leads/page.js, src/app/dashboard/calls/page.js, src/app/dashboard/calendar/page.js, src/app/dashboard/analytics/page.js, src/app/dashboard/estimates/page.js, src/app/dashboard/estimates/new/page.js, src/app/dashboard/estimates/[id]/page.js, src/app/dashboard/invoices/page.js, src/app/dashboard/invoices/new/page.js, src/app/dashboard/invoices/batch-review/page.js, src/app/dashboard/invoices/[id]/page.js, src/app/dashboard/services/page.js, src/app/dashboard/settings/page.js, src/app/dashboard/more/page.js, src/app/dashboard/more/escalation-contacts/page.js, src/app/dashboard/more/working-hours/page.js, src/app/dashboard/more/service-zones/page.js, src/app/dashboard/more/call-routing/page.js, src/app/dashboard/more/integrations/page.js, src/app/dashboard/more/ai-voice-settings/page.js, src/app/dashboard/more/invoice-settings/page.js, src/app/dashboard/more/account/page.js, src/app/dashboard/more/calendar-connections/page.js, src/app/dashboard/more/notifications/page.js, src/app/dashboard/more/services-pricing/page.js, src/app/dashboard/more/billing/page.js, src/app/dashboard/more/analytics/page.js</files>
  <read_first>
    - src/app/dashboard/page.js (dashboard home — Phase 48 work, likely 30+ hex)
    - tests/unit/dark-mode-hex-audit.test.js (the gate)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Component Category Treatments
    - Each page file BEFORE editing (Pitfall 6 — context-aware)
  </read_first>
  <behavior>
    - Every page file in src/app/dashboard/** has 0 disallowed hex.
    - error.js theming: bg-card center-aligned panel with destructive accent for the error icon.
    - loading.js theming: bg-muted skeleton placeholders.
    - Page-level wrappers use bg-background (NOT bg-card — the layout already provides background; pages are content-area).
    - Inner panels / cards within pages use bg-card or bg-muted per UI-SPEC.
  </behavior>
  <action>
    1. **List all in-scope page files** — every `page.js` / `page.jsx` under `src/app/dashboard/**` plus `error.js` and `loading.js` at the dashboard root. (`src/app/dashboard/layout.js` is Plan 02's territory — DO NOT touch here.)
    2. **For each page file**, apply the same context-aware migration as Task 1:
       - Page-level wrappers: usually inherit from layout's bg-background — if a page has its own outer `bg-[#F5F5F4]`, remove it (background is already bg-background from layout).
       - Section panels: `bg-white` / `bg-[#FAFAF9]` → `bg-card`. Inner panels → `bg-muted`.
       - Headings: `text-[#0F172A]` → `text-foreground` (or `import { heading }` from design-tokens + apply).
       - Body / labels: `text-[#475569]` → `text-muted-foreground`.
       - CTAs: `bg-[#C2410C]` button → `import { btn } from '@/lib/design-tokens'` + `className={btn.primary}`.
       - Borders: `border-stone-200` → `border-border`.
       - Hover: `hover:bg-stone-100` → `hover:bg-accent`.
       - Inline status pills/badges: defer to imported components from Plan 04 if possible; if inline, apply UI-SPEC categorical mapping.
    3. **error.js:**
       - Wrapper: `bg-card border border-border rounded-2xl p-8 max-w-md mx-auto`.
       - Icon: destructive accent `text-destructive` (resolves to red in light, lighter red in dark via shadcn defaults).
       - Heading + body: text-foreground / text-muted-foreground.
       - Retry button: `import { btn } from '@/lib/design-tokens'` + btn.primary.
    4. **loading.js:**
       - Skeleton placeholders: `bg-muted` (theme-aware) with `animate-pulse`. NOT `bg-stone-100` or `bg-[#F5F5F4]` literal.
    5. **Notable tricky pages:**
       - `src/app/dashboard/page.js` (dashboard home, Phase 48): Likely contains the SetupChecklistLauncher (Phase 48-05 pivot) and tile grid. Tile components are owned by Task 1; the page just composes them. Remove any `bg-[#F5F5F4]` outer wrappers (layout provides bg).
       - `src/app/dashboard/more/billing/page.js`: Has Stripe-themed elements. Convert pricing-tier cards using card.base + selected.card patterns from design-tokens.
       - `src/app/dashboard/calendar/page.js`: Renders CalendarView (Phase 50 exclusion). The page wrapper itself migrates here, but CalendarView's internal urgency colors are NOT touched.
       - `src/app/dashboard/analytics/page.js`: Renders AnalyticsCharts (Phase 50 exclusion). Same pattern — page wrapper migrates here, charts deferred.
    6. After every 5–10 pages migrated, run `npx jest tests/unit/dark-mode-hex-audit.test.js --bail` to track progress.
    7. `npm run build` must succeed at the end.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const path=require('path');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;function walk(d){const out=[];for(const n of f.readdirSync(d)){const p=path.join(d,n);const st=f.statSync(p);if(st.isDirectory())out.push(...walk(p));else if(/page\.(js|jsx)$/.test(n)||n==='error.js'||n==='loading.js')out.push(p)}return out}const files=walk('src/app/dashboard').filter(p=>!p.endsWith('layout.js')&amp;&amp;!p.endsWith('ImpersonationBanner.js')&amp;&amp;!p.endsWith('TrialCountdownBanner.js')&amp;&amp;!p.endsWith('BillingWarningBanner.js'));const fails=[];for(const p of files){const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)fails.push(p+': '+m[0])}if(fails.length)throw new Error('Hex remains in '+fails.length+' pages:\\n'+fails.join('\\n'));console.log('page sweep ok ('+files.length+' files)')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The bash node script enumerating all dashboard page files (excluding layout.js + 3 banners which are Plan 02's) finds 0 disallowed hex.
    - At least 20 of the migrated pages contain `text-foreground` OR `text-muted-foreground` (POLISH-08).
    - At least 15 of the migrated pages contain `bg-card` (panel surfaces migrated).
    - `error.js` contains `bg-card` AND `text-destructive`.
    - `loading.js` contains `bg-muted` AND `animate-pulse` (skeleton pattern).
    - `src/app/dashboard/page.js` (dashboard home) contains 0 disallowed hex.
    - `npm run build` exit 0.
    - layout.js + 3 banner files at src/app/dashboard/ ROOT (Plan 02 owned) are NOT modified by this task — verify via git diff.
  </acceptance_criteria>
  <done>All in-scope dashboard page files free of disallowed hex; error/loading themed; Wave 0 hex-audit fully GREEN after this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full dark-mode manual verification + hex-audit regression gate</name>
  <files>.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md</files>
  <action>Pause execution. Run the automated gate (npx jest tests/unit/dark-mode-*.test.js) and npm run build first to confirm Plans 02-05 left the tree in a green state. Then present the manual walkthrough in <how-to-verify> below to the user and wait for explicit "approved" or a description of failures. Do NOT modify source files in this task.</action>
  <verify><automated>npx jest tests/unit/dark-mode-toggle-logic.test.js tests/unit/dark-mode-infra.test.js tests/unit/dark-mode-hex-audit.test.js --bail &amp;&amp; npm run build 2>&amp;1 | tail -5</automated></verify>
  <done>All 3 dark-mode jest tests GREEN; npm run build succeeds; user typed "approved" or all manual checklist surfaces signed PASS.</done>
  <what-built>
    Wave 2 complete after Plans 02 + 03 + 04 + 05:
    - Layout shell + banners (Plan 02)
    - Flyouts + modals (Plan 03)
    - Status pills + badges (Plan 04)
    - Bulk component + page sweep (Plan 05)
    Together: ~83 files migrated, ~537 hex occurrences eliminated.
  </what-built>
  <how-to-verify>
    1. **Automated gate:** Run `npx jest tests/unit/dark-mode-*.test.js`. ALL three Wave 0 tests must be GREEN: `dark-mode-toggle-logic.test.js`, `dark-mode-infra.test.js`, `dark-mode-hex-audit.test.js`. If hex-audit fails, it lists the offending files — fix them before proceeding.
    2. **Build gate:** Run `npm run build`. Must succeed with no hydration warnings.
    3. **Manual dark-mode walkthrough** — using `npm run dev` and the live dashboard:
       a. Toggle to dark mode via sidebar button.
       b. Visit each route in the dashboard:
          - `/dashboard` (home — tiles, setup checklist, daily ops hub)
          - `/dashboard/leads` (list + LeadFlyout open)
          - `/dashboard/calls` (call list + transcript viewer)
          - `/dashboard/calendar` (CalendarView itself stays on Phase 50 colors — verify the chrome around it is dark; calendar interior may look "off" until Phase 50)
          - `/dashboard/estimates` + open one + create new
          - `/dashboard/invoices` + open one + create new + batch-review
          - `/dashboard/services`
          - `/dashboard/settings` (each section: AI, Hours, Calendar, Notifications, Escalation, Voice)
          - `/dashboard/analytics` (charts themselves stay light-mode colors — Phase 50 — verify the page chrome is dark)
          - `/dashboard/more` + every sub-page (escalation-contacts, working-hours, service-zones, call-routing, integrations, ai-voice-settings, invoice-settings, account, calendar-connections, notifications, services-pricing, billing, analytics)
       c. Open each flyout/sheet at least once: LeadFlyout, AppointmentFlyout, QuickBookSheet, ChatbotSheet (always-mounted), ExternalEventSheet, TimeBlockSheet, RecordPaymentDialog (from invoice/payment flow), RecurringSetupDialog (from appointment flow if accessible).
       d. Trigger banners if possible: ImpersonationBanner (admin tool), TrialCountdownBanner (trial state), BillingWarningBanner (billing state). At minimum, verify their files in code.
       e. View status pills in dark: leads list categorical, booking status badges, estimate/invoice status badges.
       f. Check 375px viewport (Chrome DevTools mobile preset) in dark mode — no horizontal scroll, BottomTabBar visible and themed.
       g. Hard-reload (Cmd+Shift+R) at `/dashboard` in dark mode 5 times — confirm no flash, no console hydration warnings.
    4. **Fill in `.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md`** for every remaining row not signed by Plans 01-04 checkpoints.
    5. **Phase 50 visual debt acknowledgment:** AnalyticsCharts and CalendarView interior urgency colors WILL look wrong in dark mode (lines and color blocks render as light-mode SVG inline styles). This is EXPECTED — do not flag as a regression. Phase 50 fixes it via `useTheme()` hook.

    **Failure conditions (block phase exit if any occur):**
    - Any flyout/sheet renders white background or invisible text in dark mode.
    - Any status pill category is indistinguishable from another in dark mode (Pitfall 3).
    - Any page wrapper renders light background in dark mode.
    - Console hydration warning appears on hard reload.
    - 150ms transition feels janky or stutters.
    - localStorage theme not persisting across tab close.
    - Hex-audit test fails (means a file was missed).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass. If any failure, describe which file/surface and the exact symptom — planner will create a follow-up gap-closure plan via `/gsd-plan-phase --gaps`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Bulk CSS-class migration; no data flow changes, no new endpoints, no auth modification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05-01 | Information Disclosure | Typography sweep breaks rich-text contexts (Pitfall 6) | mitigate | Action explicitly instructs context-aware replacement (read each file before editing). DashboardSidebar.jsx (sole high-risk "dark text on dark surface" file) is permanently excluded from the audit. btn.primary text-on-orange remains text-white via the design-tokens.js token (Plan 01). |
| T-49-05-02 | Tampering | Token drift — variables declared but consumers still hex-literal (Pitfall 7) | mitigate | Wave 0 hex-audit test (Plan 01) is the gate. Task 1 and Task 2 acceptance both depend on it returning 0 matches. Cannot ship Plan 05 without the gate green. |
| T-49-05-03 | Tampering | Modifying Plan 02-04-owned files unintentionally | mitigate | Task acceptance includes git-diff verification that excluded files have 0 lines changed. Worklist construction in Task 1 step 1 explicitly enumerates exclusions. |
| T-49-05-04 | Information Disclosure | Pages rendering content with insufficient contrast in dark | mitigate | UI-SPEC §Color Contrast Validation table verifies all token pairings ≥ AA. Pages use only the validated tokens (text-foreground, text-muted-foreground, bg-card, bg-muted, bg-background, var(--brand-accent), text-destructive). No new color introductions. |

**Confirmation of absence:** No new auth, no new endpoints, no PII, no input handling. Pure presentation migration.
</threat_model>

<verification>
- `npx jest tests/unit/dark-mode-*.test.js` — all 3 tests GREEN (toggle-logic, infra, hex-audit).
- `npm run build` succeeds with 0 hydration warnings.
- Manual checklist 100% signed PASS for every dashboard surface.
- Git diff confirms Plans 02-04 owned files unchanged by Plan 05.
</verification>

<success_criteria>
1. Wave 0 hex-audit test passes — 0 disallowed hex in src/components/dashboard/**/*.{js,jsx} (excluding 19 owned/Phase50/sidebar files) AND 0 in src/app/dashboard/**/page.{js,jsx} + error.js + loading.js (DARK-03).
2. POLISH-08 typography consolidation visible: at least 30 components and 20 pages use `text-foreground` or `text-muted-foreground`.
3. Every flyout/sheet/dialog opens in dark mode with readable content (DARK-06 final verification across the full set).
4. Every status pill / badge category is distinguishable in dark (DARK-07 final verification).
5. Layout shell + banners + bottom tab bar respond to theme (DARK-08 final verification).
6. 150ms body transition smooth on toggle; no flash on hard reload (DARK-09).
7. localStorage persistence verified across browser sessions (DARK-02).
8. Phase 50 visual debt (charts + calendar urgency) acknowledged but not blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-05-SUMMARY.md` documenting:
- Final migrated component count (target ~50) and final migrated page count (target ~28)
- Files that required unusual treatment (e.g., DashboardTour overlay context, CommandPalette popover, SetupChecklistLauncher Phase 48-05 overlay)
- Any disallowed-hex occurrences discovered to be intentional that needed exclusion-list updates (should be zero — Plan 01's exclusion list was authoritative)
- Confirmation that hex-audit Wave 0 test is GREEN
- Manual checklist sign-off summary (which surfaces PASS, any partial-PASS notes)
- Phase 50 hand-off note: list of files NOT migrated and why (AnalyticsCharts.jsx, CalendarView.js, plus any inline urgency colors found in pages that must wait for useTheme())
- Skill sync recommendation: dashboard-crm-system skill needs update (theme toggle + new token taxonomy + POLISH-08 consolidation); public-site-i18n skill needs note that landing remains permanently light-only despite ThemeProvider wrap.
</output>
