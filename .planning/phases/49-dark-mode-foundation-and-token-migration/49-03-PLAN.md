---
phase: 49
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/components/dashboard/LeadFlyout.jsx
  - src/components/dashboard/AppointmentFlyout.js
  - src/components/dashboard/QuickBookSheet.js
  - src/components/dashboard/ChatbotSheet.jsx
  - src/components/dashboard/ExternalEventSheet.js
  - src/components/dashboard/TimeBlockSheet.js
  - src/components/dashboard/RecordPaymentDialog.jsx
  - src/components/dashboard/RecurringSetupDialog.jsx
autonomous: true
requirements:
  - DARK-03
  - DARK-06
must_haves:
  truths:
    - "User opens LeadFlyout in dark mode and reads the body text on a dark card surface (no white background, no invisible text)"
    - "User opens AppointmentFlyout in dark mode and the header / body / close affordance all theme correctly"
    - "User opens QuickBookSheet form in dark mode and form labels + inputs are legible"
    - "User opens ChatbotSheet (always-mounted) in dark mode and message bubbles render with theme-aware tokens"
    - "Dialog modals (RecordPayment, RecurringSetup) flip background to bg-card and text to foreground tokens"
    - "Sheets used by calendar (ExternalEvent, TimeBlock) follow same flyout pattern"
  artifacts:
    - path: "src/components/dashboard/LeadFlyout.jsx"
      provides: "Lead detail flyout migrated 30 hex → bg-card / text-foreground / text-muted-foreground / border-border"
      contains: "bg-card"
    - path: "src/components/dashboard/AppointmentFlyout.js"
      provides: "Appointment detail flyout migrated 12 hex"
      contains: "bg-card"
    - path: "src/components/dashboard/QuickBookSheet.js"
      provides: "Quick-book sheet form migrated"
      contains: "text-foreground"
    - path: "src/components/dashboard/ChatbotSheet.jsx"
      provides: "AI chat sheet migrated"
      contains: "bg-card"
  key_links:
    - from: "src/components/dashboard/LeadFlyout.jsx"
      to: "globals.css :root --card / --foreground / --muted-foreground"
      via: "Tailwind utilities bg-card, text-foreground, text-muted-foreground"
      pattern: "bg-card"
    - from: "src/components/dashboard/ChatbotSheet.jsx"
      to: "next-themes / sonner Toaster"
      via: "always-mounted Sheet inherits .dark class via root layout ThemeProvider"
      pattern: "bg-card"
---

<objective>
Migrate the four DARK-06-named flyouts/modals (LeadFlyout 30 hex, AppointmentFlyout 12 hex, QuickBookSheet 3 hex, ChatbotSheet 2 hex) plus four adjacent dialog/sheet siblings (ExternalEventSheet, TimeBlockSheet, RecordPaymentDialog, RecurringSetupDialog) to the Flyouts and Modals category treatment from UI-SPEC.

Purpose: DARK-06 explicitly calls out flyouts/modals as the highest-risk readability surface. Hardcoded `bg-white` + `text-[#0F172A]` here would render white-on-dark unreadable inversions. This plan eliminates the risk for every overlay surface in the dashboard.

Output: 8 overlay component files migrated. Light mode visually unchanged (bg-card resolves to white in light); dark mode now renders bg-card as elevated near-black with foreground-AAA text.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-01-SUMMARY.md
@src/components/dashboard/LeadFlyout.jsx
@src/components/dashboard/AppointmentFlyout.js
@src/components/dashboard/QuickBookSheet.js
@src/components/dashboard/ChatbotSheet.jsx

<interfaces>
Flyouts and Modals migration map (UI-SPEC §Flyouts and Modals — verbatim):
  bg-white                  → bg-card
  bg-white border-b border-stone-200  → bg-card border-b border-border (header)
  text-[#475569] / text-stone-500    → text-muted-foreground   (body)
  text-[#0F172A] / text-stone-900    → text-foreground         (heading)
  hover:bg-stone-100        → hover:bg-accent                  (close button hover)
  bg-stone-50 / bg-stone-100 (inner panels)  → bg-muted

Selected/active states inside flyouts (UI-SPEC §Selection):
  border-[#C2410C]          → border-[var(--brand-accent)]
  bg-[#C2410C]/[0.04]       → bg-[var(--selected-fill)]
  bg-[#F5F5F4] hover:bg-stone-100  → bg-muted hover:bg-accent (idle card)

Inline buttons inside flyouts (CTAs):
  Use design-tokens.js btn.primary (already migrated in Plan 01) — replace any local re-implementation of the orange button with `import { btn } from '@/lib/design-tokens'` and apply `className={btn.primary}` (or compose). If the file already references btn.primary, no change needed.

Disallowed hex for this plan's audit (the standard 5):
  #C2410C, #9A3412, #F5F5F4, #0F172A, #475569

DARK-03 cross-cutting requirement: This plan also satisfies a portion of DARK-03 (component-file hex elimination) for the 8 overlay files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the 4 DARK-06 flagship flyouts (LeadFlyout, AppointmentFlyout, QuickBookSheet, ChatbotSheet)</name>
  <files>src/components/dashboard/LeadFlyout.jsx, src/components/dashboard/AppointmentFlyout.js, src/components/dashboard/QuickBookSheet.js, src/components/dashboard/ChatbotSheet.jsx</files>
  <read_first>
    - src/components/dashboard/LeadFlyout.jsx (full file — 30 hex occurrences; identify each one's role: header bg, body text, status pill, action button, divider, etc.)
    - src/components/dashboard/AppointmentFlyout.js (full file — 12 hex)
    - src/components/dashboard/QuickBookSheet.js (full file — 3 hex)
    - src/components/dashboard/ChatbotSheet.jsx (full file — 2 hex; this is always-mounted from Phase 37)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Flyouts and Modals (lines 197–208), §Selection and Focus States (lines 211–217)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Pitfall 6 (typography sweep risk — read context, do not blanket replace)
    - src/lib/design-tokens.js (post-Plan-01 — confirm btn / card / heading / body / focus / selected exports for use in flyout buttons)
  </read_first>
  <behavior>
    - LeadFlyout in light mode: visually identical to baseline (bg-card resolves to white).
    - LeadFlyout in dark mode: card surface flips to elevated near-black; heading text foreground (~AAA contrast); body text muted-foreground (~AA); selected lead card border stays orange (var(--brand-accent) = #FB923C in dark) with white/[0.04] tinted fill.
    - AppointmentFlyout, QuickBookSheet, ChatbotSheet follow the same pattern.
    - ChatbotSheet message bubbles: user message uses bg-primary text-primary-foreground (already shadcn-themed); AI response uses bg-muted text-foreground.
    - All 4 files have ZERO disallowed hex after migration.
  </behavior>
  <action>
    For each of the 4 files, perform a context-aware migration (NOT blind sed). Read every line containing a disallowed hex and apply the matching rule:

    1. **LeadFlyout.jsx (30 hex):**
       - Header section (`Sheet`, `SheetContent`, or wrapper div with `bg-white`): `bg-white` → `bg-card`. If border `border-stone-200` or `border-[#...]` is present, switch to `border-border`.
       - Heading (`text-[#0F172A]`): replace with `text-foreground`.
       - Body text (`text-[#475569]`): replace with `text-muted-foreground`.
       - Inner panels / cards (`bg-[#F5F5F4]` or `bg-stone-50`): replace with `bg-muted`.
       - Selected lead/related card border (`border-[#C2410C]`): replace with `border-[var(--brand-accent)]`.
       - Selected card fill (`bg-[#C2410C]/[0.04]` or similar opacity): replace with `bg-[var(--selected-fill)]`.
       - Active-CTA button (`bg-[#C2410C]`): if it's the primary action, prefer `import { btn } from '@/lib/design-tokens'` and use `className={btn.primary}`. If structurally inconvenient, use `bg-[var(--brand-accent)] text-[var(--brand-accent-fg)] hover:bg-[var(--brand-accent)]/90 active:bg-[var(--brand-accent-hover)]`.
       - Close button hover (`hover:bg-stone-100`): replace with `hover:bg-accent`.
       - Status pills inside the flyout: if they reference local categorical hex, leave Plan 04 to handle them (Plan 04 owns LeadStatusPills + badges). If they're inline literal categorical colors (e.g. `bg-green-50 text-green-700`), apply the UI-SPEC Status Badges pattern: add `dark:bg-{color}-950/40 dark:text-{color}-300 dark:border-{color}-800/60`.
       - Verify NO instance of `text-[#0F172A]` or `bg-[#FFFFFF]` survives.
    2. **AppointmentFlyout.js (12 hex):** same map as LeadFlyout. Pay attention to:
       - Date / time labels — these are usually text-stone-500 or text-[#475569]; consolidate to `text-muted-foreground`.
       - Status (e.g., "Booked", "Completed") badges inside: same as LeadFlyout — leave categorical migration to Plan 04 unless it's an inline literal.
       - Action buttons (Reschedule, Cancel) use destructive pattern from UI-SPEC if cancel button uses red.
    3. **QuickBookSheet.js (3 hex):** Form-heavy. Likely `text-[#0F172A]` on labels and `bg-[#F5F5F4]` on input wrappers. Apply:
       - Form labels → `text-foreground` (heading token role).
       - Helper text → `text-muted-foreground`.
       - Input wrappers — if they use `bg-[#F5F5F4]`, use `bg-muted`. Native `<input>` inherits — if the form uses shadcn's `<Input />`, it's already themed.
    4. **ChatbotSheet.jsx (2 hex):** Always-mounted Sheet. Likely the 2 hex are on the SheetContent wrapper and a divider. Apply:
       - Wrapper bg → `bg-card`.
       - Header → `bg-card border-b border-border`.
       - Message bubble for AI: `bg-muted text-foreground`.
       - Message bubble for user: leave as `bg-primary text-primary-foreground` (already shadcn-themed; or apply this if not yet).
       - Input area at bottom: `bg-card border-t border-border`.
    5. After all 4 files migrated, run hex-audit grep on just these 4 files — must be 0 matches. Run `npm run build` — must succeed.
    6. Do NOT modify LeadStatusPills.jsx / BookingStatusBadge.js / EstimateStatusBadge.jsx in this task — Plan 04 owns those. If they're imported and rendered inside flyouts, the imports are unchanged here; only the flyout's own className strings change.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;const files=['src/components/dashboard/LeadFlyout.jsx','src/components/dashboard/AppointmentFlyout.js','src/components/dashboard/QuickBookSheet.js','src/components/dashboard/ChatbotSheet.jsx'];files.forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0]);if(!/bg-card|text-foreground|text-muted-foreground/.test(c))throw new Error(p+' did not adopt theme tokens')});console.log('flyouts ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/components/dashboard/{LeadFlyout.jsx,AppointmentFlyout.js,QuickBookSheet.js,ChatbotSheet.jsx}` returns 0 matches.
    - Each of the 4 files contains at least one of: `bg-card`, `text-foreground`, `text-muted-foreground`.
    - `grep -c "bg-card" src/components/dashboard/LeadFlyout.jsx` returns >= 1.
    - `grep -c "bg-card" src/components/dashboard/ChatbotSheet.jsx` returns >= 1.
    - `grep -c "text-foreground" src/components/dashboard/LeadFlyout.jsx` returns >= 1.
    - `grep -c "text-muted-foreground" src/components/dashboard/LeadFlyout.jsx` returns >= 1.
    - `grep -c "var(--brand-accent)\|btn.primary" src/components/dashboard/LeadFlyout.jsx` returns >= 1 (CTA button migrated).
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>4 DARK-06 flyouts migrated. Hex audit clean. No status pill files modified (Plan 04 owns those).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate adjacent sheets and dialogs (ExternalEvent, TimeBlock, RecordPayment, RecurringSetup)</name>
  <files>src/components/dashboard/ExternalEventSheet.js, src/components/dashboard/TimeBlockSheet.js, src/components/dashboard/RecordPaymentDialog.jsx, src/components/dashboard/RecurringSetupDialog.jsx</files>
  <read_first>
    - src/components/dashboard/ExternalEventSheet.js (full file — likely small, calendar event detail)
    - src/components/dashboard/TimeBlockSheet.js (full file — calendar time block detail)
    - src/components/dashboard/RecordPaymentDialog.jsx (full file — payment recording dialog)
    - src/components/dashboard/RecurringSetupDialog.jsx (full file — recurring appointment setup)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Flyouts and Modals (same pattern as Task 1)
  </read_first>
  <behavior>
    - All 4 sheets/dialogs migrate with the same map as Task 1.
    - Light mode visually identical; dark mode renders correctly.
    - 0 disallowed hex remaining.
  </behavior>
  <action>
    Apply the same migration map as Task 1 to each of the 4 files:
    1. **ExternalEventSheet.js:** Sheet wrapper `bg-white` → `bg-card`; headings → `text-foreground`; body → `text-muted-foreground`; inner panels → `bg-muted`; borders → `border-border`. If event has a status indicator using categorical colors, apply UI-SPEC Status Badges dark variants inline.
    2. **TimeBlockSheet.js:** Same as ExternalEventSheet. Time-block urgency / category colors that ARE part of the calendar urgency system are EXCLUDED from this phase (Phase 50 — DARK-10). However, if they're plain UI scaffolding (not the urgency category color blocks), migrate them.
    3. **RecordPaymentDialog.jsx:** Dialog wrapper uses shadcn `<Dialog>` — DialogContent already renders bg-popover (themed). Local class overrides: form labels → `text-foreground`; helper text → `text-muted-foreground`; success/error inline messages keep categorical colors but add dark variants per status badges pattern.
    4. **RecurringSetupDialog.jsx:** Same as RecordPaymentDialog. Configuration sections / preview cards: `bg-stone-50` or `bg-[#F5F5F4]` → `bg-muted`. Active selection (e.g., selected day-of-week pill) uses `border-[var(--brand-accent)] bg-[var(--selected-fill)]` per UI-SPEC §Selection.
    5. Hex audit on these 4 files — must be clean.
    6. `npm run build` must succeed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;['src/components/dashboard/ExternalEventSheet.js','src/components/dashboard/TimeBlockSheet.js','src/components/dashboard/RecordPaymentDialog.jsx','src/components/dashboard/RecurringSetupDialog.jsx'].forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0])});console.log('sheets/dialogs ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/components/dashboard/{ExternalEventSheet.js,TimeBlockSheet.js,RecordPaymentDialog.jsx,RecurringSetupDialog.jsx}` returns 0 matches.
    - At least one of the 4 files contains `bg-card` OR `bg-muted` OR `text-foreground` OR `text-muted-foreground` (most should contain all).
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>4 secondary sheets/dialogs migrated. Hex audit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | CSS-class migration only; no data flow, no auth changes. ChatbotSheet still renders user-supplied chat content — but XSS surface is unchanged from prior phases (Phase 48 ChatProvider). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03-01 | Information Disclosure | ChatbotSheet message bubble contrast in dark | mitigate | UI-SPEC §Color Contrast Validation table verifies bg-muted + text-foreground = ~14.2:1 in dark (AAA). Use of shadcn tokens guarantees the contrast level. |
| T-49-03-02 | Information Disclosure | LeadFlyout selected-card visibility in dark | mitigate | D-11 explicitly addresses this: orange border alone provides selection signal; bg-[var(--selected-fill)] = white/[0.04] in dark. Visible at the border level. |
| T-49-03-03 | Tampering | None | accept | No new authenticated mutations introduced. |

**Confirmation of absence:** No new XSS surfaces (ChatbotSheet rendering pattern unchanged from Phase 37/48), no new authenticated endpoints, no PII added.
</threat_model>

<verification>
- Hex audit grep returns 0 matches across this plan's 8 files.
- `npm run build` succeeds.
- Manual checklist rows: "Flyouts — LeadFlyout / AppointmentFlyout / QuickBookSheet / ChatbotSheet / ExternalEventSheet / TimeBlockSheet" + "Dialogs — RecordPayment / RecurringSetup" — all PASS in both modes.
</verification>

<success_criteria>
1. Every flyout (4 named in DARK-06 + 2 calendar sheets) renders readable content in dark mode (DARK-06).
2. Every dialog (RecordPayment, RecurringSetup) renders readable content in dark mode (DARK-06).
3. No hardcoded white background or invisible text remains in any of the 8 overlay files (DARK-06 explicit acceptance).
4. Selected card states inside flyouts use orange border + var(--selected-fill) per D-11 (DARK-03).
5. CTA buttons inside flyouts use design-tokens.js btn.primary OR var(--brand-accent) directly (DARK-03).
6. No status pill files modified (LeadStatusPills, BookingStatusBadge, EstimateStatusBadge are Plan 04's territory).
</success_criteria>

<output>
After completion, create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-03-SUMMARY.md` documenting:
- Per-file count of hex occurrences before/after (verifies the research's 30+12+3+2 numbers)
- Decisions on inline status pills (left for Plan 04 vs migrated inline)
- CTA button approach: imported `btn.primary` token vs inline `var(--brand-accent)` — note which files used which
- Manual checklist rows signed for all 8 overlay surfaces in both modes
- Confirmation that shadcn `<Dialog>` and `<Sheet>` primitives were not modified (they auto-theme via shadcn variables — only local className overrides changed)
</output>
