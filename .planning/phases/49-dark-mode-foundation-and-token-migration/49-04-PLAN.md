---
phase: 49
plan: 04
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/components/dashboard/LeadStatusPills.jsx
  - src/components/dashboard/BookingStatusBadge.js
  - src/components/dashboard/EstimateStatusBadge.jsx
  - src/components/dashboard/InvoiceStatusBadge.jsx
  - src/components/dashboard/RecurringBadge.jsx
  - src/components/dashboard/LeadFilterBar.jsx
autonomous: true
requirements:
  - DARK-04
  - DARK-07
must_haves:
  truths:
    - "User views LeadStatusPills in dark mode and can distinguish all 5 pipeline categories at a glance (no inversion collapse)"
    - "BookingStatusBadge renders categorical color in both modes with category meaning preserved"
    - "EstimateStatusBadge renders categorical color in both modes"
    - "InvoiceStatusBadge renders categorical color in both modes (paid/draft/sent/overdue distinguishable)"
    - "RecurringBadge (visual marker for recurring appointments) renders correctly in both modes"
    - "LeadFilterBar pipeline-status filter chips theme correctly"
  artifacts:
    - path: "src/components/dashboard/LeadStatusPills.jsx"
      provides: "5-category pipeline status pills with brand + categorical dark variants per UI-SPEC §Status Badges"
      contains: "var(--brand-accent)"
    - path: "src/components/dashboard/BookingStatusBadge.js"
      provides: "Booking lifecycle badge with categorical mapping (success/warning/info/destructive/neutral)"
      contains: "dark:"
    - path: "src/components/dashboard/EstimateStatusBadge.jsx"
      provides: "Estimate state badge with categorical mapping"
      contains: "dark:"
    - path: "src/components/dashboard/InvoiceStatusBadge.jsx"
      provides: "Invoice state badge"
      contains: "dark:"
  key_links:
    - from: "src/components/dashboard/LeadStatusPills.jsx"
      to: "globals.css --brand-accent"
      via: "var(--brand-accent) reference for 'new' active state"
      pattern: "var\\(--brand-accent\\)"
    - from: "src/components/dashboard/BookingStatusBadge.js"
      to: "Tailwind dark: variant"
      via: "@custom-variant dark .where(.dark, .dark *) (fixed in Plan 01)"
      pattern: "dark:bg-"
---

<objective>
Migrate the categorical status pill / badge family. These are the highest-risk migration target for "Categorical Inversion Collapse" (RESEARCH.md Pitfall 3) — naive symmetric inversion makes booked vs lost vs paid look identical in dark mode. Apply the UI-SPEC §Status Badges category table verbatim across all 6 files.

Purpose: DARK-07 explicitly requires "categorical meaning preserved." This plan eliminates the per-component creativity risk by following one shared category mapping across every status surface.

Output: 6 status / badge / filter files migrated using the verbatim category mapping. No bespoke per-file dark color invention.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-01-SUMMARY.md
@src/components/dashboard/LeadStatusPills.jsx
@src/components/dashboard/BookingStatusBadge.js
@src/components/dashboard/EstimateStatusBadge.jsx
@src/components/dashboard/InvoiceStatusBadge.jsx
@src/components/dashboard/RecurringBadge.jsx
@src/components/dashboard/LeadFilterBar.jsx

<interfaces>
UI-SPEC §Status Badges and Urgency Pills (lines 184–194) — verbatim category table:

| Category    | Light                                          | Dark (use these EXACT class strings)              |
|-------------|------------------------------------------------|---------------------------------------------------|
| Success     | bg-green-50 text-green-700 border-green-200    | bg-green-950/40 text-green-300 border-green-800/60 |
| Warning     | bg-amber-50 text-amber-700 border-amber-200    | bg-amber-950/40 text-amber-300 border-amber-800/60 |
| Info        | bg-blue-50 text-blue-700 border-blue-200       | bg-blue-950/40 text-blue-300 border-blue-800/60   |
| Neutral     | bg-stone-100 text-stone-700 border-stone-200   | bg-muted text-muted-foreground border-border      |
| Destructive | bg-red-50 text-red-700 border-red-200          | bg-red-950/40 text-red-300 border-red-800/60      |
| Active/brand| bg-orange-50 text-orange-700 border-orange-200 | bg-orange-950/40 text-orange-300 border-orange-800/60 |

For ACTIVE/brand pills (the "new" pipeline status currently `bg-[#C2410C] text-white border-[#C2410C]`):
  Active state → bg-[var(--brand-accent)] text-[var(--brand-accent-fg)] border-[var(--brand-accent)]
  (No dark: prefix needed — the var() switches automatically via :root vs .dark.)

LeadStatusPills.jsx PRE-state (RESEARCH.md §Code Examples lines 510–516):
  PIPELINE_STATUSES = [
    { value: 'new',       activeClass: 'bg-[#C2410C] text-white border-[#C2410C]' },         // brand active
    { value: 'booked',    activeClass: 'bg-blue-600 text-white border-blue-600' },            // info-saturated
    { value: 'completed', activeClass: 'bg-stone-700 text-white border-stone-700' },          // neutral-saturated
    { value: 'paid',      activeClass: 'bg-[#166534] text-white border-[#166534]' },          // success-saturated (custom green hex)
    { value: 'lost',      activeClass: 'bg-red-600 text-white border-red-600' },              // destructive-saturated
  ]
  Idle: bg-[#F5F5F4] text-[#475569] border-stone-200 hover:bg-stone-100

LeadStatusPills.jsx POST-state (RESEARCH.md §Code Examples lines 518–528):
  PIPELINE_STATUSES = [
    { value: 'new',       activeClass: 'bg-[var(--brand-accent)] text-[var(--brand-accent-fg)] border-[var(--brand-accent)]' },
    { value: 'booked',    activeClass: 'bg-blue-600 dark:bg-blue-500 text-white border-blue-600 dark:border-blue-500' },
    { value: 'completed', activeClass: 'bg-stone-700 dark:bg-stone-600 text-white border-stone-700 dark:border-stone-600' },
    { value: 'paid',      activeClass: 'bg-[#166534] dark:bg-emerald-600 text-white border-[#166534] dark:border-emerald-600' },
    { value: 'lost',      activeClass: 'bg-red-600 dark:bg-red-500 text-white border-red-600 dark:border-red-500' },
  ]
  Idle: 'bg-card text-foreground border-border hover:bg-accent hover:border-accent-foreground/20'

NOTE: #166534 (a custom dark green for "paid") is NOT in the standard 5-hex disallowed list — but it IS a hardcoded hex. It survives the standard hex-audit (which only checks for #C2410C/#9A3412/#F5F5F4/#0F172A/#475569). UI-SPEC accepts it because "paid" has a saturated brand-green identity that doesn't fit the {color}-950/40 desaturated pattern. Document this exception in the SUMMARY.

Pitfall 3 reminder: NEVER use `dark:bg-{color}-900` for badges — dark-900 collapses categories. Always use `-950/40 + -300 + -800/60` for chip-style badges, OR `-500 / -600 saturated` for active-state pills (LeadStatusPills active is the saturated case; chips and inactive badges use the chip pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate LeadStatusPills.jsx + LeadFilterBar.jsx (the active-pipeline pills)</name>
  <files>src/components/dashboard/LeadStatusPills.jsx, src/components/dashboard/LeadFilterBar.jsx</files>
  <read_first>
    - src/components/dashboard/LeadStatusPills.jsx (full file — match against RESEARCH.md PRE-state above)
    - src/components/dashboard/LeadFilterBar.jsx (full file — uses LeadStatusPills?  Or has its own status filter chips? Identify which)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Code Examples lines 508–528 (exact PRE/POST), §Pitfall 3 (Categorical Inversion Collapse)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Status Badges (lines 184–194)
  </read_first>
  <behavior>
    - All 5 pipeline statuses distinguishable in dark mode (RESEARCH.md PRE-state idle uses `text-[#475569]` which is illegible on dark; POST uses `text-foreground`).
    - Active "new" pill uses var(--brand-accent) → orange in light, lighter orange #FB923C in dark; text uses var(--brand-accent-fg) → white in light, dark text in dark per D-10 contrast.
    - Idle (unselected) pill: `bg-card text-foreground border-border` — readable in both modes.
    - No literal hex remains EXCEPT `#166534` for "paid" active state (UI-SPEC accepted exception — categorical brand green).
    - LeadFilterBar status chips (if present) follow the same pattern.
  </behavior>
  <action>
    1. **src/components/dashboard/LeadStatusPills.jsx:**
       - Apply the POST-state mapping verbatim from RESEARCH.md §Code Examples lines 518–528 (transcribed in interfaces above).
       - Replace the PIPELINE_STATUSES array `activeClass` strings with the new var()-and-dark-variant strings.
       - Replace the IDLE_CLASS / inactive class string with `'bg-card text-foreground border-border hover:bg-accent hover:border-accent-foreground/20'` (or the closest equivalent matching the file's existing variable name).
       - Verify the standard 5 disallowed hex are gone from the file. `#166534` is the documented exception — leave it.
    2. **src/components/dashboard/LeadFilterBar.jsx:**
       - Identify if it renders status filter chips (likely yes — Phase 48 work added/refined it). For each chip:
         - Active state: if the chip uses `bg-[#C2410C]` or matches the LeadStatusPills active pattern, apply the same POST mapping (var(--brand-accent) for "all"/"new" filters, saturated-{color}-{500/600} dark variants for categorical).
         - Idle state: `bg-card text-foreground border-border hover:bg-accent`.
       - Replace any `text-[#475569]` filter-label text → `text-muted-foreground`.
       - Replace any `text-[#0F172A]` headers → `text-foreground`.
       - Replace any `bg-[#F5F5F4]` panel backgrounds → `bg-muted`.
    3. Hex audit (excluding `#166534` if it appears):
       ```
       grep -nE "#(C2410C|9A3412|F5F5F4|0F172A|475569)\b" src/components/dashboard/LeadStatusPills.jsx src/components/dashboard/LeadFilterBar.jsx
       ```
       Must return 0 matches.
    4. `npm run build` must succeed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;['src/components/dashboard/LeadStatusPills.jsx','src/components/dashboard/LeadFilterBar.jsx'].forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0])});const pills=f.readFileSync('src/components/dashboard/LeadStatusPills.jsx','utf8');if(!pills.includes('var(--brand-accent)'))throw new Error('LeadStatusPills missing var(--brand-accent)');if(!/dark:bg-(blue|stone|emerald|red)-(500|600)/.test(pills))throw new Error('LeadStatusPills missing dark: variants');console.log('pills ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/components/dashboard/{LeadStatusPills.jsx,LeadFilterBar.jsx}` returns 0 matches.
    - `grep -c "var(--brand-accent)" src/components/dashboard/LeadStatusPills.jsx` returns >= 1.
    - `grep -cE "dark:bg-(blue|stone|emerald|red)-(500|600)" src/components/dashboard/LeadStatusPills.jsx` returns >= 4 (one per non-brand pipeline status).
    - `grep -c "bg-card\|text-foreground\|text-muted-foreground\|border-border" src/components/dashboard/LeadStatusPills.jsx` returns >= 2 (idle state uses these).
    - `grep -c "bg-card\|text-foreground\|text-muted-foreground" src/components/dashboard/LeadFilterBar.jsx` returns >= 1.
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>Pipeline pills + filter bar migrated. Active "new" pill is theme-aware via var(); other 4 categorical pills use saturated dark variants that preserve identity. Idle state uses theme tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate BookingStatusBadge + EstimateStatusBadge + InvoiceStatusBadge + RecurringBadge</name>
  <files>src/components/dashboard/BookingStatusBadge.js, src/components/dashboard/EstimateStatusBadge.jsx, src/components/dashboard/InvoiceStatusBadge.jsx, src/components/dashboard/RecurringBadge.jsx</files>
  <read_first>
    - src/components/dashboard/BookingStatusBadge.js (full file — identify states: booked/pending/cancelled/completed)
    - src/components/dashboard/EstimateStatusBadge.jsx (full file — identify states: draft/sent/accepted/declined/expired)
    - src/components/dashboard/InvoiceStatusBadge.jsx (full file — identify states: draft/sent/paid/overdue/void)
    - src/components/dashboard/RecurringBadge.jsx (full file — single visual marker, likely info or accent variant)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Status Badges category table (lines 184–194)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Pitfall 3
  </read_first>
  <behavior>
    - Each badge maps every state to one of the 6 categories (success/warning/info/neutral/destructive/active) and applies the verbatim light + dark class strings from UI-SPEC.
    - States and category mappings (use these unless the existing component clearly intends differently — read context first):
      - Booking: booked→success, pending→warning, completed→neutral, cancelled→destructive
      - Estimate: draft→neutral, sent→info, accepted→success, declined→destructive, expired→warning
      - Invoice: draft→neutral, sent→info, paid→success, overdue→destructive, void→neutral
      - Recurring: active variant (orange/brand) — uses bg-orange-50/dark:bg-orange-950/40 + text-orange-700/dark:text-orange-300 + border-orange-200/dark:border-orange-800/60
    - 0 disallowed hex in any of the 4 files.
  </behavior>
  <action>
    For each of the 4 files:
    1. Read the current state-to-class mapping (often a switch/object literal mapping state strings to className strings).
    2. For each state, classify it to one of the 6 UI-SPEC categories using the table in step "behavior" above.
    3. Replace the className strings with the verbatim UI-SPEC category strings from the interfaces table:
       - Success: `bg-green-50 text-green-700 border-green-200 dark:bg-green-950/40 dark:text-green-300 dark:border-green-800/60`
       - Warning: `bg-amber-50 text-amber-700 border-amber-200 dark:bg-amber-950/40 dark:text-amber-300 dark:border-amber-800/60`
       - Info: `bg-blue-50 text-blue-700 border-blue-200 dark:bg-blue-950/40 dark:text-blue-300 dark:border-blue-800/60`
       - Neutral: `bg-stone-100 text-stone-700 border-stone-200 dark:bg-muted dark:text-muted-foreground dark:border-border`
       - Destructive: `bg-red-50 text-red-700 border-red-200 dark:bg-red-950/40 dark:text-red-300 dark:border-red-800/60`
       - Active/brand: `bg-orange-50 text-orange-700 border-orange-200 dark:bg-orange-950/40 dark:text-orange-300 dark:border-orange-800/60`
    4. Convert any disallowed hex remaining (e.g., text labels using `text-[#0F172A]`) using the standard map.
    5. Hex audit on the 4 files — 0 matches required.
    6. `npm run build` must succeed.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const re=/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i;['src/components/dashboard/BookingStatusBadge.js','src/components/dashboard/EstimateStatusBadge.jsx','src/components/dashboard/InvoiceStatusBadge.jsx','src/components/dashboard/RecurringBadge.jsx'].forEach(p=>{const c=f.readFileSync(p,'utf8');const m=c.match(re);if(m)throw new Error(p+' still has '+m[0]);if(!/dark:/.test(c))throw new Error(p+' has no dark: variants')});console.log('badges ok')" &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#(C2410C|9A3412|F5F5F4|0F172A|475569)" src/components/dashboard/{BookingStatusBadge.js,EstimateStatusBadge.jsx,InvoiceStatusBadge.jsx,RecurringBadge.jsx}` returns 0 matches.
    - Each of the 4 files contains at least one occurrence of `dark:bg-` (proves dark variants present).
    - `grep -cE "dark:(bg|text|border)-(green|amber|blue|red|orange)-(950/40|300|800/60)" src/components/dashboard/BookingStatusBadge.js` returns >= 3 (a typical badge has at least success + warning + destructive states).
    - `grep -cE "dark:(bg|text|border)-(green|amber|blue|red|orange)-(950/40|300|800/60)" src/components/dashboard/InvoiceStatusBadge.jsx` returns >= 3.
    - `npm run build` exit 0.
  </acceptance_criteria>
  <done>4 badge components migrated using verbatim UI-SPEC category mapping. Category meaning preserved across light/dark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure presentation migration. Status enum-to-className mapping is data unchanged from prior phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Information Disclosure | Categorical inversion collapse (Pitfall 3) | mitigate | UI-SPEC §Status Badges category table is applied verbatim, NOT improvised per file. The `-950/40 + -300 + -800/60` shift was specifically designed to avoid the dark-900 collapse. Verified by acceptance grep for these exact class strings. Per-file creativity is explicitly forbidden in the task <action> instructions. |
| T-49-04-02 | Tampering | Status enum→category mapping mistakes | mitigate | The mapping table in <action> step "behavior" enumerates each state explicitly. Executor reads file first to confirm the file's actual states match the assumed mapping; if they don't, use context to classify (e.g., "void" → neutral, not destructive). |

**Confirmation of absence:** No new auth, no PII, no new endpoints. Status badge text is read-only display of existing enum values.
</threat_model>

<verification>
- Hex audit grep returns 0 matches across this plan's 6 files.
- `npm run build` succeeds.
- Manual checklist: "Status pills — LeadStatusPills (5 categories), BookingStatusBadge, EstimateStatusBadge, InvoiceStatusBadge, RecurringBadge" all PASS in both modes; categorical distinguishability confirmed in dark.
</verification>

<success_criteria>
1. LeadStatusPills 5 pipeline categories distinguishable in dark mode at arm's length — Pitfall 3 not triggered (DARK-07).
2. All 4 status badges + 1 recurring marker theme correctly using the UI-SPEC category table verbatim (DARK-07).
3. LeadFilterBar status chip family migrated (DARK-03 partial).
4. No hex literal in standard disallowed set (DARK-04 enforcement; #166534 paid-state exception documented).
5. design-tokens.js NOT modified by this plan (Plan 01 owns it).
</success_criteria>

<output>
After completion, create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-04-SUMMARY.md` documenting:
- Final state-to-category mapping for each badge component (Booking/Estimate/Invoice/Recurring) — confirms classifications were validated against actual file contents
- Documentation of the `#166534` paid-state hex exception (UI-SPEC accepted; Phase 50 may revisit)
- Confirmation that LeadStatusPills POST-state matches RESEARCH.md §Code Examples verbatim
- Manual checklist rows signed: all 6 files, both modes, with notes confirming categorical distinguishability
</output>
