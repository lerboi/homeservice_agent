---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.js
  - src/components/theme-provider.jsx
  - src/lib/design-tokens.js
  - src/components/dashboard/DashboardSidebar.jsx
  - tests/unit/dark-mode-infra.test.js
  - tests/unit/dark-mode-hex-audit.test.js
  - tests/unit/dark-mode-toggle-logic.test.js
  - .planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md
autonomous: false
requirements:
  - DARK-01
  - DARK-02
  - DARK-04
  - DARK-08
  - DARK-09
  - POLISH-08
must_haves:
  truths:
    - "User hard-reloads /dashboard in dark mode with zero flash of light content"
    - "User clicks sidebar theme toggle (binary sun/moon) and body background crossfades over 150ms"
    - "User preference persists across browser sessions via next-themes localStorage key 'theme'"
    - "design-tokens.js exports reference var(--*) tokens, not literal hex"
    - "globals.css @custom-variant dark uses :where(.dark, .dark *) selector"
    - "Wave 0 Jest tests run green on an untouched repo state (grep audit marks infra as fixed)"
  artifacts:
    - path: "src/components/theme-provider.jsx"
      provides: "Client ThemeProvider wrapper re-exporting next-themes provider"
      contains: "ThemeProvider"
    - path: "src/app/layout.js"
      provides: "Root layout with suppressHydrationWarning on <html> and ThemeProvider wrapping NextIntlClientProvider"
      contains: "ThemeProvider"
    - path: "src/app/globals.css"
      provides: "New CSS variables (--brand-accent, --brand-accent-hover, --brand-accent-fg, --selected-fill, --warm-surface, --warm-surface-elevated) in :root and .dark; 150ms body transition; fixed @custom-variant"
      contains: "--brand-accent"
    - path: "src/lib/design-tokens.js"
      provides: "Token groups rewritten to reference var(--*) and text-foreground / text-muted-foreground"
      contains: "var(--brand-accent)"
    - path: "src/components/dashboard/DashboardSidebar.jsx"
      provides: "Binary sun/moon theme toggle button between Ask Voco AI and Log Out"
      contains: "setTheme"
    - path: "tests/unit/dark-mode-infra.test.js"
      provides: "Wave 0 grep assertions for layout, globals.css selector, 150ms rule, token declarations"
    - path: "tests/unit/dark-mode-hex-audit.test.js"
      provides: "Wave 0 grep assertion: zero disallowed hex in dashboard trees (with excludes for Phase 50 files + sidebar navy)"
    - path: "tests/unit/dark-mode-toggle-logic.test.js"
      provides: "Pure-function tests for toggle helpers (getNextTheme, getToggleLabel)"
    - path: ".planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md"
      provides: "Manual screenshot checklist (flash, 150ms feel, localStorage persistence, flyout readability, badge categorical distinguishability)"
  key_links:
    - from: "src/app/layout.js"
      to: "src/components/theme-provider.jsx"
      via: "import { ThemeProvider } from '@/components/theme-provider'"
      pattern: "ThemeProvider.*attribute=\"class\""
    - from: "src/components/dashboard/DashboardSidebar.jsx"
      to: "next-themes useTheme"
      via: "import { useTheme } from 'next-themes'"
      pattern: "useTheme\\(\\)"
    - from: "src/lib/design-tokens.js"
      to: "src/app/globals.css"
      via: "var(--brand-accent) reference resolved by :root and .dark blocks"
      pattern: "var\\(--brand-accent\\)"
---

<objective>
Wire all Phase 49 infrastructure in one atomic plan: ThemeProvider + suppressHydrationWarning, new semantic tokens in globals.css (brand accent + selected fill + warm surface + 150ms body transition + @custom-variant fix), design-tokens.js CSS-variable rewrite (POLISH-08 typography consolidation included), the sidebar theme toggle button, and all Wave 0 test scaffolds so the rest of the phase has feedback within 60 seconds per task.

Purpose: Establish the token layer that Plans 02-05 migrate consumers onto. Without the tokens existing, parallel migration would produce drift. Without the Wave 0 tests, the hex-audit grep in downstream plans has no authoritative fail gate.

Output: Functional theme toggle (manually verifiable), green Wave 0 Jest suite, all semantic tokens declared, design-tokens.js free of hex literals.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md
@.planning/phases/49-dark-mode-foundation-and-token-migration/49-VALIDATION.md
@src/app/globals.css
@src/app/layout.js
@src/lib/design-tokens.js
@src/components/dashboard/DashboardSidebar.jsx
@src/components/ui/sonner.jsx
@jest.config.js

<interfaces>
Existing shadcn CSS variables already declared in globals.css (:root lines 127–170, .dark lines 172–204):
  --background, --foreground, --card, --card-foreground, --popover, --popover-foreground,
  --primary, --primary-foreground, --secondary, --secondary-foreground, --muted, --muted-foreground,
  --accent, --accent-foreground, --destructive, --border, --input, --ring,
  --sidebar, --sidebar-foreground, --chart-1..5

Existing @theme inline mappings (globals.css lines 5–54) already expose as Tailwind utilities:
  bg-background, text-foreground, bg-card, text-card-foreground, bg-popover, bg-primary, bg-secondary,
  bg-muted, text-muted-foreground, bg-accent, bg-destructive, border-border

design-tokens.js current shape (PRE-migration — see RESEARCH.md §Code Examples for AFTER):
  export const colors = { brandOrange: '#C2410C', brandOrangeDark: '#9A3412', navy: '#0F172A', warmSurface: '#F5F5F4', bodyText: '#475569' }
  export const btn = { primary: 'bg-[#C2410C] hover:bg-[#C2410C]/90 active:bg-[#9A3412] ... text-white ...' }
  export const card = { base: 'bg-white rounded-2xl ...' }
  export const glass = { topBar: 'bg-white/80 backdrop-blur-md border-b border-stone-200/60' }
  export const heading = 'text-[#0F172A] tracking-tight'
  export const body = 'text-[#475569]'
  export const focus = { ring: 'focus:outline-none focus:ring-2 focus:ring-[#C2410C] focus:ring-offset-1' }
  export const selected = { card: 'border-[#C2410C] bg-[#C2410C]/[0.04]', cardIdle: 'border-stone-200 bg-[#F5F5F4] hover:bg-stone-100' }

DashboardSidebar.jsx footer cluster order (bottom → top):
  1. Log Out button (existing ~lines 96–105)
  2. Ask Voco AI (existing ~lines 85–94)
  Insert theme toggle BETWEEN them (above Log Out, below Ask Voco AI).
  Sidebar is always navy bg-[#0F172A] — this is INTENTIONAL and must not change.

next-themes API (verified 0.4.6):
  <ThemeProvider attribute="class" defaultTheme="system" enableSystem>  // DO NOT pass disableTransitionOnChange
  const { theme, resolvedTheme, setTheme } = useTheme()
  Binary toggle reads resolvedTheme (not theme — theme returns 'system' pre-click)
  localStorage key defaults to 'theme' — no storageKey prop needed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds and manual checklist</name>
  <files>tests/unit/dark-mode-infra.test.js, tests/unit/dark-mode-hex-audit.test.js, tests/unit/dark-mode-toggle-logic.test.js, .planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md, src/lib/theme-toggle-logic.js</files>
  <read_first>
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-VALIDATION.md (per-task verification map)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Validation Architecture (lines 565–617)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §Component Category Treatments, §Motion Spec
    - jest.config.js (confirms node-only env, testMatch **/tests/**/*.test.js)
    - tests/unit/* (existing patterns — any example of repo's grep-assertion test style)
  </read_first>
  <behavior>
    - Test dark-mode-infra.test.js PASSES when: src/app/layout.js contains 'suppressHydrationWarning'; contains "ThemeProvider"; globals.css contains '@custom-variant dark (&:where(.dark, .dark *))'; globals.css contains '150ms' inside a prefers-reduced-motion block; globals.css :root block contains '--brand-accent:' and .dark block contains '--brand-accent:'; layout.js must NOT contain 'disableTransitionOnChange'.
    - Test dark-mode-hex-audit.test.js PASSES when: zero occurrences of the 5 disallowed hex patterns (#C2410C, #9A3412, #F5F5F4, #0F172A, #475569) appear in src/app/dashboard/** OR src/components/dashboard/**, EXCLUDING src/components/dashboard/AnalyticsCharts.jsx (Phase 50), src/components/dashboard/CalendarView.js (Phase 50), and src/components/dashboard/DashboardSidebar.jsx (intentional navy bg + orange active indicator migrate to var(), but file still contains no disallowed hex after Task 3). Also fails if src/lib/design-tokens.js contains any text-[#...] or bg-[#...] literal.
    - Test dark-mode-toggle-logic.test.js PASSES when pure helpers from src/lib/theme-toggle-logic.js return: getNextTheme('light') === 'dark'; getNextTheme('dark') === 'light'; getToggleLabel('light') === 'Dark mode'; getToggleLabel('dark') === 'Light mode'; getToggleAriaLabel('light') === 'Switch to dark mode'; getToggleAriaLabel('dark') === 'Switch to light mode'.
    - All three tests RED when repo is in pre-migration state (expected — Tasks 2 and 3 turn them GREEN).
  </behavior>
  <action>
    1. Create `src/lib/theme-toggle-logic.js` (pure ESM module, NO React imports — node-env testable):
       ```js
       export function getNextTheme(current) {
         return current === 'dark' ? 'light' : 'dark';
       }
       export function getToggleLabel(current) {
         return current === 'dark' ? 'Light mode' : 'Dark mode';
       }
       export function getToggleAriaLabel(current) {
         return `Switch to ${getNextTheme(current)} mode`;
       }
       ```
    2. Create `tests/unit/dark-mode-toggle-logic.test.js` using standard Jest `describe/it/expect` — import from `@/lib/theme-toggle-logic` (moduleNameMapper in jest.config.js line 7 resolves `@/` to `src/`). Six assertions covering each helper in both states.
    3. Create `tests/unit/dark-mode-infra.test.js`. Use `fs.readFileSync` (node env — no RTL) against absolute paths derived via `path.resolve(__dirname, '../../src/...')`. Assertions:
       - `layout.js` matches `/suppressHydrationWarning/` AND `/ThemeProvider/` AND does NOT match `/disableTransitionOnChange/`.
       - `globals.css` matches `/@custom-variant dark \(&:where\(\.dark, \.dark \*\)\)/` AND `/prefers-reduced-motion[^}]*150ms/s` (multiline).
       - `globals.css` contains `--brand-accent:` at least twice (once :root, once .dark). A simple count via `str.match(/--brand-accent:/g).length >= 2` is sufficient.
    4. Create `tests/unit/dark-mode-hex-audit.test.js`:
       - Glob `src/app/dashboard/**/*.{js,jsx}` and `src/components/dashboard/**/*.{js,jsx}` using `fs.readdirSync` recursion (no glob dep — match Phase 48 pattern of static-file-read tests).
       - Exclude by basename: `AnalyticsCharts.jsx`, `CalendarView.js`, `DashboardSidebar.jsx`.
       - Forbidden patterns: regex union `/#(C2410C|9A3412|F5F5F4|0F172A|475569)\b/i`.
       - Separately read `src/lib/design-tokens.js` and assert it contains NEITHER `text-[#` NOR `bg-[#` substrings.
       - On failure, `expect.fail(...)` with an enumerated list of `{file, matches}` so the executor can see exactly which files still have hex. Use `collectedMatches.length === 0` as the assertion for clean failure output.
    5. Create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md` — table with columns `Surface | Light PASS/FAIL | Dark PASS/FAIL | Notes`. Rows: layout shell (sidebar navy preserved, top bar glass, main bg, bottom tab bar), all 3 banners (Impersonation, TrialCountdown, BillingWarning), all 4 flyouts (Lead, Appointment, QuickBook, Chatbot), LeadStatusPills (5 pipeline states each), BookingStatusBadge, EstimateStatusBadge, SetupChecklist, DashboardTour, CommandPalette; plus perception checks (150ms body fade feel, no flash on hard reload cmd+shift+R x5, localStorage persistence across tab close, cross-tab sync, axe DevTools AA on /dashboard/leads in dark, 375px viewport in both modes).
    6. Do NOT yet modify any infra file — Tasks 2 and 3 turn these tests green. Commit these RED.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');['tests/unit/dark-mode-infra.test.js','tests/unit/dark-mode-hex-audit.test.js','tests/unit/dark-mode-toggle-logic.test.js','src/lib/theme-toggle-logic.js','.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md'].forEach(p=>{if(!f.existsSync(p))throw new Error('missing '+p)});console.log('ok')" &amp;&amp; npx jest tests/unit/dark-mode-toggle-logic.test.js --bail</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/dark-mode-infra.test.js` exists and Jest collects it (runnable, may currently FAIL expectedly).
    - File `tests/unit/dark-mode-hex-audit.test.js` exists (runnable, may FAIL expectedly).
    - File `tests/unit/dark-mode-toggle-logic.test.js` exists and PASSES (pure helpers are already implemented in Task 1).
    - File `src/lib/theme-toggle-logic.js` exports `getNextTheme`, `getToggleLabel`, `getToggleAriaLabel`.
    - File `.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md` contains a table row for each surface listed in action step 5.
    - `grep -c "import.*react" tests/unit/dark-mode-*.test.js` returns 0 (node-env only — no React imports anywhere in Wave 0 tests).
  </acceptance_criteria>
  <done>Wave 0 scaffolding committed. `npx jest tests/unit/dark-mode-toggle-logic.test.js` is green. `npx jest tests/unit/dark-mode-infra.test.js` and `tests/unit/dark-mode-hex-audit.test.js` are expected RED until Tasks 2 and 3 land — this is intentional TDD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: globals.css + layout.js + theme-provider + design-tokens rewrite</name>
  <files>src/app/globals.css, src/app/layout.js, src/components/theme-provider.jsx, src/lib/design-tokens.js</files>
  <read_first>
    - src/app/globals.css (full file — verify line 3 selector, line 127+ :root, line 172+ .dark, existing @layer base body rule)
    - src/app/layout.js (full file — current provider nesting; confirm NextIntlClientProvider is inner)
    - src/lib/design-tokens.js (full file — all 7 token groups currently hardcoded)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §New Semantic Tokens (lines 120–135), §Motion Spec, §Typography
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Code Examples (lines 399–489 — concrete before/after for globals.css and design-tokens.js), §Pitfall 2 (disableTransitionOnChange), §Pitfall 5 (@custom-variant selector)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md (D-04, D-05, D-10, D-11, D-13, D-14)
  </read_first>
  <behavior>
    - After this task: `tests/unit/dark-mode-infra.test.js` PASSES.
    - After this task: design-tokens.js portion of `tests/unit/dark-mode-hex-audit.test.js` PASSES (the design-tokens.js substring check). The dashboard-tree portion remains RED until Plans 02-05 land.
    - ThemeProvider wrapping produces no React hydration warning in console on hard reload (verified manually — item on checklist).
  </behavior>
  <action>
    1. **src/app/globals.css — 4 edits, make them atomic:**
       a. Line 3: replace `@custom-variant dark (&:is(.dark *));` with `@custom-variant dark (&:where(.dark, .dark *));` (Pitfall 5 — includes the `.dark` element itself, not just descendants).
       b. Inside `:root` block (after existing tokens, before the closing `}`), add:
          ```css
          --brand-accent: #C2410C;
          --brand-accent-hover: #B53B0A;
          --brand-accent-fg: #FFFFFF;
          --selected-fill: rgba(194, 65, 12, 0.04);
          --warm-surface: #F5F5F4;
          --warm-surface-elevated: #FAFAF9;
          --body-text: var(--muted-foreground);
          --heading-text: var(--foreground);
          ```
       c. Inside `.dark` block (after existing tokens, before the closing `}`), add:
          ```css
          --brand-accent: #FB923C;
          --brand-accent-hover: #F97316;
          --brand-accent-fg: #0F172A;
          --selected-fill: rgba(255, 255, 255, 0.04);
          --warm-surface: oklch(0.145 0 0);
          --warm-surface-elevated: oklch(0.205 0 0);
          --body-text: var(--muted-foreground);
          --heading-text: var(--foreground);
          ```
       d. Inside the existing `@layer base` block (the one that currently has `body { @apply bg-background text-foreground; ... }`), add:
          ```css
          @media (prefers-reduced-motion: no-preference) {
            body {
              transition:
                background-color 150ms ease,
                color 150ms ease;
            }
          }
          ```
    2. **Create src/components/theme-provider.jsx** (NEW file):
       ```jsx
       'use client';
       import { ThemeProvider as NextThemesProvider } from 'next-themes';
       export function ThemeProvider({ children, ...props }) {
         return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
       }
       ```
    3. **src/app/layout.js — edit root layout:**
       - Add `import { ThemeProvider } from '@/components/theme-provider';`
       - Add `suppressHydrationWarning` attribute to the `<html lang={locale} ...>` element.
       - Wrap `<NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>` with `<ThemeProvider attribute="class" defaultTheme="system" enableSystem>...</ThemeProvider>`.
       - **CRITICAL: Do NOT pass `disableTransitionOnChange`.** (Pitfall 2 — would defeat the 150ms body transition.)
       - Do NOT move `suppressHydrationWarning` to `<body>` — scoped to `<html>` only per D-04 and the planner threat model.
    4. **src/lib/design-tokens.js — full rewrite to CSS-var references** (per RESEARCH.md §Code Examples AFTER block):
       - `colors` object: change values to `var(--brand-accent)`, `var(--brand-accent-hover)`, `var(--sidebar)` (keep `navy` name mapped to navy), `var(--warm-surface)`, `var(--body-text)`.
       - `btn.primary`: `'bg-[var(--brand-accent)] hover:bg-[var(--brand-accent)]/90 active:bg-[var(--brand-accent-hover)] active:scale-95 text-[var(--brand-accent-fg)] shadow-[0_1px_2px_rgba(0,0,0,0.06)] transition-all duration-150'`
       - `card.base`: `'bg-card rounded-2xl shadow-[0_1px_2px_rgba(0,0,0,0.04)] border border-border'`
       - `glass.topBar`: `'bg-card/80 backdrop-blur-md border-b border-border'`
       - `heading`: `'text-foreground tracking-tight'` (POLISH-08)
       - `body`: `'text-muted-foreground'` (POLISH-08)
       - `focus.ring`: `'focus:outline-none focus:ring-2 focus:ring-[var(--brand-accent)] focus:ring-offset-1'`
       - `selected.card`: `'border-[var(--brand-accent)] bg-[var(--selected-fill)]'`
       - `selected.cardIdle`: `'border-border bg-muted hover:bg-accent'`
       - If there is a `gridTexture` export or any other tokens referencing hex, convert to `var(--*)` or remove (grep the file — no `text-[#` or `bg-[#` may remain).
    5. Run `npx jest tests/unit/dark-mode-infra.test.js tests/unit/dark-mode-toggle-logic.test.js` locally — both must be green before committing.
  </action>
  <verify>
    <automated>npx jest tests/unit/dark-mode-infra.test.js tests/unit/dark-mode-toggle-logic.test.js --bail &amp;&amp; node -e "const c=require('fs').readFileSync('src/lib/design-tokens.js','utf8');if(/text-\[#|bg-\[#/.test(c))throw new Error('design-tokens.js still has hex literals');console.log('tokens ok')" &amp;&amp; node -e "const c=require('fs').readFileSync('src/app/layout.js','utf8');if(c.includes('disableTransitionOnChange'))throw new Error('layout.js must not pass disableTransitionOnChange');if(!c.includes('suppressHydrationWarning'))throw new Error('missing suppressHydrationWarning');console.log('layout ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@custom-variant dark (&:where(.dark, .dark \*))" src/app/globals.css` returns 1.
    - `grep -c "\-\-brand-accent:" src/app/globals.css` returns >= 2 (once in :root, once in .dark).
    - `grep -c "prefers-reduced-motion" src/app/globals.css` returns >= 1 AND the same block contains `150ms`.
    - `grep -c "ThemeProvider" src/app/layout.js` returns >= 2 (import + usage).
    - `grep -c "suppressHydrationWarning" src/app/layout.js` returns 1 (scoped to `<html>` only).
    - `grep -c "disableTransitionOnChange" src/app/layout.js` returns 0.
    - `grep -E "text-\[#|bg-\[#" src/lib/design-tokens.js` returns nothing (no matches).
    - `grep -c "text-foreground" src/lib/design-tokens.js` returns >= 1 AND `grep -c "text-muted-foreground" src/lib/design-tokens.js` returns >= 1 (POLISH-08 consolidation).
    - File `src/components/theme-provider.jsx` exists and contains `'use client'` directive.
    - `npx jest tests/unit/dark-mode-infra.test.js` passes.
    - `npm run build` succeeds with no hydration warning logged (inspect stdout for `Warning:` strings).
  </acceptance_criteria>
  <done>Infra tests green. design-tokens.js cleared of hex. Layout wrapped with ThemeProvider. globals.css has new tokens + fixed variant + 150ms transition rule. `npm run build` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sidebar theme toggle button (binary sun/moon)</name>
  <files>src/components/dashboard/DashboardSidebar.jsx</files>
  <read_first>
    - src/components/dashboard/DashboardSidebar.jsx (full file — identify footer cluster lines; understand exact classname pattern of Ask Voco AI + Log Out buttons for geometric parity)
    - src/lib/theme-toggle-logic.js (pure helpers from Task 1 — import these for label/aria logic)
    - src/components/ui/tooltip.jsx (existing shadcn Tooltip primitive — already in use elsewhere)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-UI-SPEC.md §The Single New UI Element — Theme Toggle Button (lines 221–302)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-RESEARCH.md §Pattern 2 (lines 198–256 — mounted guard pattern)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-CONTEXT.md D-02, D-03 (binary toggle, placement above Log Out)
  </read_first>
  <behavior>
    - Toggle renders invisible placeholder (opacity-0, aria-hidden, tabIndex=-1) while mounted===false (hydration safety).
    - After mount, renders sun (resolvedTheme === 'dark') or moon (resolvedTheme === 'light') with matching label "Light mode" / "Dark mode".
    - Click calls setTheme(getNextTheme(resolvedTheme)) — does NOT emit 'system'.
    - Visual geometry identical to Ask Voco AI and Log Out sibling buttons (same px/py/rounded/text-white-60/hover).
    - Tooltip appears on hover with content "Switch to {target} mode", side="right".
    - Placed structurally between Ask Voco AI (above) and Log Out (below) in the sidebar footer cluster.
  </behavior>
  <action>
    1. Open `src/components/dashboard/DashboardSidebar.jsx`. Locate the footer cluster (the sibling block containing "Ask Voco AI" and "Log Out" buttons — per UI-SPEC line 226 approx lines 85–105 of the file).
    2. Add imports at the top of the file:
       ```jsx
       import { useTheme } from 'next-themes';
       import { useEffect, useState } from 'react';  // if not already present
       import { Sun, Moon } from 'lucide-react';
       import { Tooltip, TooltipContent, TooltipTrigger } from '@/components/ui/tooltip';
       import { getNextTheme, getToggleLabel, getToggleAriaLabel } from '@/lib/theme-toggle-logic';
       ```
    3. Define a component-local `ThemeToggleButton` inside the same file (not a separate file — keeps blast radius small, matches the sidebar's self-contained footer pattern):
       ```jsx
       function ThemeToggleButton() {
         const { resolvedTheme, setTheme } = useTheme();
         const [mounted, setMounted] = useState(false);
         useEffect(() => setMounted(true), []);

         if (!mounted) {
           return (
             <button
               className="flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium text-white/60 border-l-2 border-transparent ml-0 pl-[10px] w-full opacity-0"
               aria-hidden="true"
               tabIndex={-1}
             >
               <Moon className="h-4 w-4 shrink-0" />
               Dark mode
             </button>
           );
         }

         const current = resolvedTheme === 'dark' ? 'dark' : 'light';
         const Icon = current === 'dark' ? Sun : Moon;

         return (
           <Tooltip>
             <TooltipTrigger asChild>
               <button
                 type="button"
                 onClick={() => setTheme(getNextTheme(current))}
                 aria-label={getToggleAriaLabel(current)}
                 className="flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-colors text-white/60 hover:bg-white/[0.04] hover:text-white/80 border-l-2 border-transparent ml-0 pl-[10px] w-full focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--brand-accent)] focus-visible:ring-offset-2 focus-visible:ring-offset-[#0F172A]"
               >
                 <Icon className="h-4 w-4 shrink-0" />
                 {getToggleLabel(current)}
               </button>
             </TooltipTrigger>
             <TooltipContent side="right">{getToggleAriaLabel(current)}</TooltipContent>
           </Tooltip>
         );
       }
       ```
    4. Insert `<ThemeToggleButton />` in the JSX **between** the Ask Voco AI button (above it — sibling with Sparkles icon) and the Log Out button (below it). Visual order in the sidebar footer from top to bottom: `Ask Voco AI` → `Theme Toggle` → `Log Out`. Per D-03 "above the Log Out button and adjacent to Ask Voco AI."
    5. The `focus-visible:ring-offset-[#0F172A]` literal hex is an intentional exception because the sidebar stays permanently navy — the ring offset must match the fixed sidebar background, not a theme-variable. This is pre-authorized by STATE.md ("DashboardSidebar bg-[#0F172A] must NOT be migrated to semantic token"). The hex-audit test excludes DashboardSidebar.jsx entirely for this reason.
    6. Do NOT migrate the rest of DashboardSidebar.jsx's existing classNames in this task — the existing `border-[#C2410C]` active-nav indicator and `bg-[#C2410C]` logout confirm button are either (a) intentional sidebar-only styling or (b) covered when design-tokens.js flips consumers. If they persist as literal hex in the sidebar, they remain in the exclusion list (STATE.md decision). Acceptance allows DashboardSidebar.jsx to keep its intentional navy and orange literals.
    7. Run `npm run build` — must succeed.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/dashboard/DashboardSidebar.jsx','utf8');['useTheme','setTheme','Sun','Moon','ThemeToggleButton','getNextTheme','Tooltip'].forEach(s=>{if(!c.includes(s))throw new Error('missing '+s)});console.log('sidebar ok')" &amp;&amp; npx jest tests/unit/dark-mode-toggle-logic.test.js --bail &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTheme" src/components/dashboard/DashboardSidebar.jsx` returns >= 1.
    - `grep -c "setTheme" src/components/dashboard/DashboardSidebar.jsx` returns >= 1.
    - `grep -c "getNextTheme\|getToggleLabel\|getToggleAriaLabel" src/components/dashboard/DashboardSidebar.jsx` returns 3.
    - `grep -c "Sun\|Moon" src/components/dashboard/DashboardSidebar.jsx` returns >= 2.
    - `grep -c "ThemeToggleButton" src/components/dashboard/DashboardSidebar.jsx` returns >= 2 (definition + usage).
    - `grep -c "suppressHydrationWarning" src/components/dashboard/DashboardSidebar.jsx` returns 0 (this attribute belongs on `<html>` only).
    - Build succeeds (`npm run build` exit 0).
    - Toggle button visually appears between Ask Voco AI and Log Out (manual verification — checkpoint).
  </acceptance_criteria>
  <done>Sidebar renders toggle; clicking toggle swaps theme; placeholder prevents hydration mismatch; tooltip appears on hover; focus ring visible with Tab key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual dark-mode infrastructure verification</name>
  <files>.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md</files>
  <action>Pause execution and present the verification steps in <how-to-verify> below to the user. Wait for explicit "approved" or a list of issues. Do NOT modify any source files in this task — purely visual/perception verification of Tasks 1-3 work.</action>
  <verify><automated>echo "Manual verification — see how-to-verify block"</automated></verify>
  <done>User typed "approved" or all listed perception checks signed PASS in 49-MANUAL-CHECKLIST.md.</done>
  <what-built>
    - ThemeProvider wired in src/app/layout.js
    - Sidebar theme toggle button (Ask Voco AI → [toggle] → Log Out cluster)
    - globals.css has --brand-accent + @custom-variant fix + 150ms body transition
    - design-tokens.js all references var(--*) (POLISH-08 consolidated)
    - Wave 0 Jest suite green
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`.
    2. Navigate to `/dashboard`. Confirm sidebar renders normally with the new theme toggle button between "Ask Voco AI" (above) and "Log Out" (below).
    3. Click the toggle. Confirm the BODY (main content area, NOT the sidebar) crossfades from warm-neutral to near-black over ~150ms. Sidebar stays navy — this is correct.
    4. Toggle back. Confirm light-mode content is unchanged from before the phase (no visual regression on tokens that already resolved to white).
    5. Open DevTools Console. Hard-reload (Cmd+Shift+R) in dark mode 5 times. Confirm **no flash of light content** and **zero React hydration warnings** in console.
    6. Close tab. Reopen /dashboard. Confirm theme persists (last-selected mode still applied).
    7. Open DevTools > Application > Local Storage. Confirm key `theme` exists with value `light` or `dark`.
    8. Hover the toggle. Confirm tooltip appears to the right with "Switch to {target} mode".
    9. Tab to the toggle via keyboard. Confirm focus-visible ring appears (orange on navy sidebar).
    10. Fill in the PASS/FAIL cells in `.planning/phases/49-dark-mode-foundation-and-token-migration/49-MANUAL-CHECKLIST.md` for the "Layout Shell — Sidebar" row and "Perception — 150ms feel / hard-reload flash / localStorage / cross-tab" rows.

    **Known expected visual regressions (not failures):**
    - Main content may look mostly unchanged in dark mode because Plans 02-05 have not yet migrated consumers. This is expected — the infrastructure is what Plans 02-05 build on. The body background flipping and sidebar toggle working is the minimum acceptance for this plan.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plans 02-05, or describe any infra-level issues (flash, hydration warning, toggle not working, 150ms not smooth).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → localStorage | Theme preference ('light' or 'dark') written by next-themes script. User-scoped, non-sensitive. |
| Server → Client HTML | `suppressHydrationWarning` on `<html>` intentionally silences the server-vs-client className mismatch that next-themes causes (server renders no class; client script adds `dark`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | localStorage 'theme' value | accept | User-scoped only; value space is constrained to 'light' / 'dark' / 'system'; next-themes handles unknown values by falling back to defaultTheme. No security impact — worst case is incorrect theme on next load. |
| T-49-02 | Information Disclosure | `suppressHydrationWarning` on `<html>` | mitigate | Scoped to `<html>` ONLY (not `<body>`, not app-wide). Verified by acceptance_criteria grep in Task 2 (`grep -c "suppressHydrationWarning" src/app/layout.js` returns exactly 1). Other hydration mismatches remain loud in console. |
| T-49-03 | Denial of Service | Missing `disableTransitionOnChange` causing per-component transition stagger | accept | D-05 explicitly requires 150ms body-level transition instead. Risk is cosmetic only (brief multi-frame flash on some components). UI-SPEC §Motion Spec constrains transition to body-only; component-level `transition-colors` utilities are not introduced in this phase. |
| T-49-04 | Elevation of Privilege | None — no auth, no new endpoints, no PII, no RLS | accept | Pure UI phase. No security surface expanded. |
| T-49-05 | Repudiation | Theme preference is not audit-logged | accept | Non-sensitive UI state; no compliance requirement to log. |

**Confirmation of absence:** No new authenticated endpoints, no new PII handling, no new RLS policies, no XSS/CSRF vectors introduced. Landing page (public, unauthenticated) is explicitly out of scope (D-08) — no cross-origin data flow affected.
</threat_model>

<verification>
- `npx jest tests/unit/dark-mode-*.test.js` — toggle-logic test green (Task 1), infra test green after Task 2, hex-audit test remains RED (expected — Plans 02-05 turn it green).
- `npm run build` succeeds with zero hydration warnings in stdout.
- Manual checklist rows for "Layout Shell — Sidebar" and "Perception" are PASS (Task 4).
</verification>

<success_criteria>
1. User toggles theme in sidebar → body crossfades 150ms → preference persists to localStorage → survives hard reload with zero flash (DARK-01, DARK-02, DARK-09 infra).
2. `design-tokens.js` exports 0 hex literals; all references go through `var(--*)` or shadcn utility classes (DARK-04).
3. `heading` and `body` tokens in design-tokens.js resolve to `text-foreground` and `text-muted-foreground` (POLISH-08 infra complete).
4. `globals.css` declares `--brand-accent`, `--brand-accent-hover`, `--brand-accent-fg`, `--selected-fill`, `--warm-surface`, `--warm-surface-elevated`, `--body-text`, `--heading-text` in both `:root` and `.dark`.
5. `@custom-variant dark` uses the fixed `:where(.dark, .dark *)` selector (DARK-01 prerequisite).
6. `src/app/layout.js` uses `<ThemeProvider>` WITHOUT `disableTransitionOnChange`, with `suppressHydrationWarning` on `<html>` only.
7. Wave 0 test files exist and are runnable; hex-audit test is RED-by-design until Plans 02-05 complete (this is expected signal — do not suppress).
</success_criteria>

<output>
After completion, create `.planning/phases/49-dark-mode-foundation-and-token-migration/49-01-SUMMARY.md` documenting:
- Exact globals.css line ranges modified
- Final design-tokens.js diff (hex → var() replacements enumerated)
- Any decisions taken in "Claude's Discretion" zone (e.g., `--brand-accent` vs `--accent-orange` naming — document chosen convention)
- Confirmation that Pitfall 2 (disableTransitionOnChange) was NOT introduced
- Confirmation that D-09 shared-token-bleed was resolved via Option A (accept bleed — light-mode `bg-card` resolves to same white as before)
- Manual checklist section completed: "Layout Shell — Sidebar" and "Perception" rows signed PASS/FAIL with notes
- Wave 0 test status (toggle-logic green; infra green; hex-audit RED-by-design)
</output>
