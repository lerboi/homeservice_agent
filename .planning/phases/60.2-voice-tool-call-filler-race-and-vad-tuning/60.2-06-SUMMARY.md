---
phase: 60.2-voice-tool-call-filler-race-and-vad-tuning
plan: "06"
subsystem: voice-call-architecture

requires:
  - phase: 60.2-05
    provides: "Authoritative post-remediation state — Fix G shipped, Fix H reverted (cbe1bb9), Fix I self-resolved, Fix J declined"
provides:
  - "Updated voice-call-architecture skill reflecting final Phase 60.2 runtime state"
  - "Recorded Fix J accepted-limitation with #4486 + commit 3e51d8e citations in SKILL.md §1"
  - "Documented Fix H revert narrative in SKILL.md §5 (accepted limitation against AgentSession(llm=RealtimeModel) + session.say TTS requirement)"
  - "Bumped SKILL.md Last-updated paragraph to 2026-04-20 with Phase 60.2 entry covering Fix G, Fix H revert, Fix I self-resolution, Fix J decline, open 60.3 items"
affects: [60.3-goodbye-cutoff-diagnosis, 60.3-prompt-best-practices-audit]

tech-stack:
  added: []
  patterns:
    - "Skill-doc reflects the FINAL post-remediation runtime state — not the plan-intent state (Plan 05 surfaced that Fix H shipped but was a silent no-op; SKILL.md documents that truthfully rather than parroting the plan)"

key-files:
  created:
    - .planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-06-SUMMARY.md
    - .planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-SUMMARY.md
  modified:
    - .claude/skills/voice-call-architecture/SKILL.md

key-decisions:
  - "Follow objective, not plan literal — plan was authored pre-remediation and still described Fix H as shipped; objective overrides it with the final post-cbe1bb9 state"
  - "Keep the filler code strings (`Pre-tool filler`, `_filler_idx_`, `session.say`, `allow_interruptions`) present in SKILL.md but wrapped in a `Fix H reverted` narrative so future readers see the attempt AND the reason it was pulled — prevents a subsequent agent from re-landing the same broken pattern"
  - "Cite the Gemini-Live + RealtimeModel + TTS-required-for-say() constraint explicitly so any future reintroduction of session.say() audio has to acknowledge the TTS-attach + voice-mismatch tradeoff"
  - "Record Fix I as self-resolved (not deferred) per HUMAN-UAT.md verdict — post-call pipeline completed ~12s post-session-close without the 8s timeout firing on the post-Fix-G UAT call"

patterns-established:
  - "Skill updates at phase close document what SHIPPED, not what the plan intended — remediation inside a phase is first-class content"

requirements-completed: [CTX-60.2-FIX-J, CTX-60.2-FIX-G, CTX-60.2-FIX-H]

duration: ~20min
completed: 2026-04-20
---

# Phase 60.2 Plan 06 — Update voice-call-architecture SKILL.md Summary

**Rewrote the voice-call-architecture skill to reflect the FINAL post-remediation state of Phase 60.2: Fix G shipped, Fix H reverted (not shipped), Fix I self-resolved, Fix J declined with #4486 and commit 3e51d8e citations.**

## Performance

- **Duration:** ~20 min (read plan + authoritative UAT + summary; write 5 edits; create 2 summaries)
- **Started:** 2026-04-20
- **Completed:** 2026-04-20
- **Tasks:** 3 planned, executed as single atomic skill rewrite
- **Files modified:** 1 (SKILL.md in orchestrator repo)

## Accomplishments

- **SKILL.md §1 (Pin rationale):** appended a Fix J accepted-limitation paragraph citing livekit/agents#4486 (OPEN since Jan 2026) and commit `3e51d8e` (2026-04-11, the ambiguous `generate_reply()` compatibility change for `gemini-3.1-flash-live-preview`). Notes that Fix G is the mitigation that shipped and that Fix H was implemented and reverted — cross-references §5 for the reason.
- **SKILL.md §3 (Gemini Live Session / VAD Tuning):** updated the code block to show `silence_duration_ms=1500` with an inline `phase 60.2 Fix G` comment; updated the "Our config dampens server VAD" bullet to reflect 1500ms; added a "Phase 60.2 Fix G (shipped)" paragraph explaining the 1000→1500 change, the 2→0 cancellations-per-call UAT result, the barge-in tradeoff (deliberate speech now needs ~1.5s), and the upstream citations (#4441, #4486).
- **SKILL.md §5 (Tools):** added a new "Phase 60.2 — Pre-tool filler: Fix H reverted (accepted limitation)" subsection documenting:
  - What was attempted (4 scoped tools, `_FILLER_PHRASES` rotation via per-session `deps["_filler_idx_<tool>"]`, `await context.session.say(phrase, allow_interruptions=False)` with try/except, Plan 04 prompt change telling Gemini runtime plays filler).
  - Why it was reverted (`RuntimeError: trying to generate speech from text without a TTS model` on every filler attempt — `AgentSession(llm=RealtimeModel)` has no TTS; repair would require non-Gemini TTS introducing voice mismatch; commit `cbe1bb9` removed the code + restored pre-Plan-04 prompt + deleted `tests/test_filler_audio.py` + inverted `tests/test_prompt.py`).
  - The `allow_interruptions=False` silent-downgrade gotcha in livekit-agents 1.5.1 (`agent_activity.py:905-914`) for RealtimeModel sessions with server-side turn detection.
  - Accepted limitation for future reintroduction: either attach a TTS (voice mismatch tradeoff) or use a separate audio source (pre-rendered WAV via `rtc.AudioSource`).
  - Open items for Phase 60.3: goodbye cut-off diagnosis (different race class), Gemini 3.1 Flash Live + v1.5.1 + plugin-@`43d3734` prompt-best-practices audit.
  - Fix I self-resolved verdict.
- **SKILL.md Last-updated paragraph:** bumped from 2026-04-19 (Phase 60) to 2026-04-20 (Phase 60.2) with the full narrative: Fix G shipped metrics, Fix H implementation-and-revert story + commit `cbe1bb9`, Fix I self-resolution, Fix J decline, 60.3 open items, cross-links to `60.2-05-SUMMARY.md` and this `60.2-06-SUMMARY.md`. Previous-Phase-60 text preserved as "Previous:" continuation.

## Task Commits

Single atomic commit in the orchestrator repo on `main`:

1. **Task 1+2+3 (skill rewrite):** TBD — committed after this SUMMARY is written, alongside this file and the phase-level rollup.

## Files Created/Modified

**Orchestrator repo (`homeservice_agent`):**
- `.claude/skills/voice-call-architecture/SKILL.md` — 5 edits:
  - Last-updated paragraph bumped with Phase 60.2 entry (frontmatter line 10).
  - §1 pin rationale gained Fix J accepted-limitation paragraph.
  - §3 VAD code block updated to `silence_duration_ms=1500`.
  - §3 VAD Tuning subsection updated to describe 1500ms threshold and added Phase 60.2 Fix G narrative paragraph.
  - §5 Tools gained new "Phase 60.2 — Pre-tool filler: Fix H reverted" subsection before §6 Post-Call Pipeline.
- `.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-06-SUMMARY.md` — this file.
- `.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-SUMMARY.md` — phase-level rollup (see separate file).

## Decisions Made

- **Follow objective, not plan-literal.** The 60.2-06-PLAN.md was authored before Plan 05's remediation and still described Fix H as shipping. The objective explicitly overrode that: "do not describe Fix H as if it shipped, and do not describe the runtime-filler prompt guidance as if it's active." The skill rewrite honors the objective — Fix H is documented as attempted-and-reverted, prompt guidance is noted as restored to pre-Plan-04 content.
- **Keep the technical strings present.** The plan's acceptance criteria grep for `Pre-tool filler`, `_filler_idx_`, `session.say`, `allow_interruptions` — all remain present in SKILL.md but inside the "Fix H reverted" narrative. This serves future readers who might otherwise re-attempt the same pattern: they'll find the code shape AND immediately see why it doesn't work on `AgentSession(llm=RealtimeModel)`.
- **Cite the revert commit (`cbe1bb9`) in the Last-updated paragraph.** Gives a concrete sibling-repo reference for anyone tracing the history.
- **Record Fix I as self-resolved.** HUMAN-UAT.md Fix I Verdict was explicitly "RESOLVED" — no 8s post-call timeout observed on the post-Fix-G UAT call; post-call pipeline completed ~12s after session close. No code change needed.
- **Do not attempt a §5 cross-reference into the code example the plan wanted.** The plan's "code block showing the canonical pattern" was based on the pre-revert world; including it would mislead. Instead the §5 subsection describes the shape in prose and points at `cbe1bb9` for the diff.

## Deviations from Plan

**The plan's Task actions were written against a pre-remediation reality that no longer exists.** The objective (authored post-remediation) supersedes the Task action text — see "Decisions Made" above.

### Auto-fixed Issues

**1. [Rule 1 — Plan/reality drift] Plan described Fix H as shipping; reality is Fix H was reverted**
- **Found during:** Initial read of `60.2-06-PLAN.md` and cross-check against `60.2-05-SUMMARY.md` + `60.2-HUMAN-UAT.md`.
- **Issue:** The plan's Task 2 `<action>` block instructed inserting a canonical Fix H code example and a note that "filler audio is now handled automatically by the tool runtime" — both would be factually wrong post-`cbe1bb9`.
- **Fix:** Wrote the §5 subsection as "Phase 60.2 — Pre-tool filler: Fix H reverted (accepted limitation)" instead of "Pre-tool filler audio (Phase 60.2)". Kept the technical strings the plan acceptance-grep requires (`Pre-tool filler`, `_filler_idx_`, `session.say`, `allow_interruptions`, the out-of-scope tool list) but in a revert-narrative framing. Last-updated paragraph describes Fix H as implemented-and-reverted with the TTS-requirement citation.
- **Files modified:** `.claude/skills/voice-call-architecture/SKILL.md`.
- **Verification:** all plan acceptance grep anchors present (`#4486`, `3e51d8e`, `Pre-tool filler`, `_filler_idx_`, `session.say`, `allow_interruptions`, `silence_duration_ms` (×3), `Phase 60.2` (multi-occurrence), `2026-04-20`, `Fix I`, `self-resolved`, `check_caller_history|check_customer_account|end_call`, `Pin fix`, `60.2-SUMMARY`).

---

**Total deviations:** 1 auto-fixed (plan/reality drift)
**Impact on plan:** Skill now matches the shipped runtime state, not the intended one. Future agents reading SKILL.md will not attempt to reintroduce a `session.say()`-based filler without first confronting the TTS requirement.

## Issues Encountered

- None. The only tension was plan vs. reality, resolved by following the objective (which was written with full post-remediation knowledge).

## User Setup Required

None — this is a documentation-only change in the orchestrator repo.

## Next Phase Readiness

- Phase 60.2 is closed. The skill doc now accurately reflects the shipped state.
- Phase 60.3 scope is locked in via `60.2-05-SUMMARY.md` and cross-referenced here + in SKILL.md §5:
  - (a) goodbye cut-off diagnosis starting with `end_call` invocation-timing + last-text-token logging;
  - (b) Gemini 3.1 Flash Live + livekit-agents 1.5.1 + livekit-plugins-google @ 43d3734 prompt-best-practices audit of the full `build_system_prompt()`;
  - (c) optional: re-evaluate plugin upgrade (Fix J sibling) if upstream closes #4486 or a Railway preview can be stood up.

## Self-Check: PASSED

**Files modified:**
- FOUND: `C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` (modified)

**Acceptance-criteria grep anchors (from plan tasks 1-3):**
- `#4486` — FOUND (≥1) ✓ (3 matches)
- `3e51d8e` — FOUND (≥1) ✓ (2 matches)
- `Pre-tool filler` — FOUND (≥1) ✓ (1 match — subsection heading)
- `Phase 60.2` — FOUND (≥2) ✓ (multiple)
- `silence_duration_ms` — FOUND (≥2) ✓
- `Pin fix` — FOUND (≥1) ✓ (preserved from prior update)
- `_filler_idx_` — FOUND (≥1) ✓ (2 matches)
- `session.say` — FOUND (≥1) ✓ (5 matches)
- `allow_interruptions` — FOUND (≥1) ✓ (3 matches)
- `check_caller_history|check_customer_account|end_call` — FOUND (≥1) ✓ (out-of-scope tools mentioned in §5 Fix H reverted subsection)
- `2026-04-20` — FOUND (≥1) ✓ (2 matches)
- `Fix I` — FOUND (≥1) ✓ (1 match)
- `60.2-05-SUMMARY` — FOUND (≥1) ✓ (1 match in Last-updated)
- `self-resolved` — FOUND (≥1) ✓ (Fix I verdict recorded correctly)

**Objective fidelity (from spawn prompt):**
- Fix G shipped — documented as shipped with UAT metrics ✓
- Fix H reverted — documented as reverted, not shipped ✓
- Fix I self-resolved — documented with the ~12s post-call completion evidence ✓
- Fix J declined — documented with #4486 + 3e51d8e citations ✓
- No mention of runtime filler being active — revert narrative makes clear filler is now prompt-driven ✓
- No instruction that model should stop speaking its own filler — that prompt change was reverted; SKILL.md says model speaks its own filler again ✓
- Previous tables/sections claiming Fix H shipped — N/A (no such tables existed in prior SKILL.md) ✓
- Open 60.3 issues noted — goodbye cut-off + prompt audit documented ✓

---
*Phase: 60.2-voice-tool-call-filler-race-and-vad-tuning*
*Plan: 06*
*Completed: 2026-04-20*
