---
phase: 60.2-voice-tool-call-filler-race-and-vad-tuning
plan: 02
subsystem: voice-call-architecture
tags: [livekit, gemini, vad, server-vad, silence-duration, fix-g, wave-1]

# Dependency graph
requires:
  - phase: 60.1
    provides: "Existing RealtimeInputConfig block in src/agent.py (silence_duration_ms=1000 from backlog 999.2 fix)"
provides:
  - "Gemini server-VAD silence threshold raised 1000ms -> 1500ms in livekit-agent/src/agent.py"
  - "Refreshed VAD config comment referencing Phase 60.2 + upstream issues #4441 and #4486"
affects: [60.2-phase-verification, UAT-persona-1-rerun]

# Tech tracking
tech-stack:
  added: []  # no new deps
  patterns:
    - "Single-value VAD tuning pass-through to Gemini Live server (no plugin-side processing)"

key-files:
  created: []
  modified:
    - "C:/Users/leheh/.Projects/livekit-agent/src/agent.py"

key-decisions:
  - "Only silence_duration_ms changed; prefix_padding_ms=400 and LOW sensitivities remain locked per CONTEXT"
  - "Comment explicitly cites #4486 (Agent audio cuts off after one word) alongside pre-existing #4441 reference so future readers see the full upstream context"
  - "Did NOT assert allow_interruptions=False guarantees uninterruptibility in the comment (RESEARCH flagged 1.5.1 silently downgrades it to NOT_GIVEN)"

patterns-established:
  - "Cross-repo Fix G pattern: tiny numeric + comment edit lands in sibling livekit-agent repo via --no-verify commit; SUMMARY stays in orchestrator repo"

requirements-completed: [CTX-60.2-FIX-G]

# Metrics
duration: ~3min
completed: 2026-04-20
---

# Phase 60.2 Plan 02: Fix G — Raise Gemini Server-VAD silence_duration_ms Summary

**One-line numeric change in `livekit-agent/src/agent.py`: `silence_duration_ms=1000` -> `silence_duration_ms=1500`, with a refreshed six-line comment block citing Phase 60.2 and upstream livekit/agents issues #4441 + #4486.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-20T (this session)
- **Completed:** 2026-04-20
- **Tasks:** 1
- **Files modified:** 1 (in sibling repo)

## Accomplishments

- Gemini server-side VAD now waits 1500ms of caller silence (up from 1000ms) before cancelling in-flight tool calls.
- VAD comment block updated to reflect the new threshold and to reference `livekit/agents#4486` (which is the upstream issue tracking the "audio cuts off after one word" symptom seen on the 2026-04-19 UAT call) alongside the pre-existing `#4441` citation.
- All other VAD knobs (`prefix_padding_ms=400`, `start_of_speech_sensitivity=LOW`, `end_of_speech_sensitivity=LOW`) intentionally left untouched per CONTEXT "What NOT to do".
- Non-Wave-0 test suite remains fully green (99/99 passing) — no regressions introduced.

## Task Commits

Committed atomically in the sibling `livekit-agent` repo on `main` with `--no-verify` (Phase 60 cross-repo pattern):

1. **Task 1: Raise silence_duration_ms 1000->1500 and refresh comment** — `9ffc051` (fix)

## Files Created/Modified

- `C:/Users/leheh/.Projects/livekit-agent/src/agent.py` — 1 file changed, 8 insertions(+), 6 deletions(-).
  - Lines 210-215: 6-line comment block replaced with 8-line version citing Phase 60.2 + issues #4441 and #4486.
  - Line 221 -> 223: `silence_duration_ms=1000,` -> `silence_duration_ms=1500,  # phase 60.2 (was 1000 in phase 55 999.2 fix)`.

## Decisions Made

- **Single-knob change only:** CONTEXT locked `prefix_padding_ms` and both sensitivity fields. RESEARCH verified the pass-through to Gemini server VAD is direct (no plugin-side logic). Raising `silence_duration_ms` alone is sufficient to address the caller-side VAD spurious trigger class of cancellations.
- **Comment honesty:** Did not claim that `allow_interruptions=False` (used in Fix H) guarantees uninterruptible playback — RESEARCH §R1 documents that livekit-agents 1.5.1 silently downgrades it to `NOT_GIVEN` when using a `RealtimeModel` with server-side turn detection. Fix G's 1500ms threshold is the real un-interruption lever.
- **Tradeoff acknowledged in comment:** Explicitly states caller barge-in requires deliberate speech (>1.5s) — documents the known tradeoff for future readers.

## Deviations from Plan

None — plan executed exactly as written. Both edits (comment block + numeric value) applied verbatim from the `<action>` block.

## Issues Encountered

None. First-pass grep verification confirmed all 7 acceptance criteria pass:

- `grep "silence_duration_ms=1500"` -> 1 match (line 223) ✓
- `grep "silence_duration_ms=1000"` -> 0 matches ✓
- `grep -c "prefix_padding_ms=400"` -> 1 ✓ (unchanged)
- `grep "Phase 60.2"` -> found at line 212 inside VAD comment ✓
- `grep "#4486"` -> found at line 215 inside VAD comment ✓
- `grep -c "START_SENSITIVITY_LOW"` -> 1 ✓ (unchanged)
- Non-Wave-0 pytest suite: 99 passed (same pass profile as pre-edit baseline) ✓

The only failing test in the full suite (`tests/test_filler_audio.py::test_check_availability_plays_filler`) is an intentional Wave 0 RED stub from Plan 01 — NOT introduced by this plan. Documented in 60.2-01-SUMMARY as "RED-by-design" until Plan 03 (Fix H) lands.

## User Setup Required

None — no external services, env vars, or dashboard actions. Deployment happens automatically when Railway picks up the push (pending user push of the sibling repo).

## Next Phase Readiness

- **Plan 03 (Fix H tool filler, Wave 1)** — independent; Plan 03's Wave 0 tests target filler-audio code paths unrelated to VAD config, so Fix G and Fix H can land in either order.
- **Plan 04 (prompt narration update, Wave 1)** — independent; prompt edits don't interact with VAD config.
- **Phase-level UAT** — re-run Persona 1 call after Fix G + Fix H + Plan 04 prompt edit ship together. Expected observations per CONTEXT §"Success criteria":
  - `server cancelled tool calls` warnings drop 2/call -> 0-1/call (Fix G contribution)
  - No mid-sentence "[cut]" fragmentation around tool invocations (Fix H contribution, G assists)
  - `book_appointment: idempotent re-invocation` message gone on happy path
  - Post-call pipeline completes <8s (indirect benefit — Fix I deferred per RESEARCH)

## Self-Check: PASSED

**Files modified:**
- FOUND: C:/Users/leheh/.Projects/livekit-agent/src/agent.py (modified, committed)

**Commits exist (in sibling repo `livekit-agent` on `main`):**
- FOUND: 9ffc051 (Task 1 — fix(60.2): raise Gemini server-VAD silence_duration_ms 1000->1500 (Fix G))

**Acceptance criteria (Plan PLAN.md §acceptance_criteria):**
- silence_duration_ms=1500 present, =1000 absent ✓
- prefix_padding_ms=400 unchanged (count=1) ✓
- Phase 60.2 cited in comment ✓
- #4486 cited in comment ✓
- START_SENSITIVITY_LOW unchanged (count=1) ✓
- Non-Wave-0 pytest suite pass profile unchanged: 99 passed ✓

---
*Phase: 60.2-voice-tool-call-filler-race-and-vad-tuning*
*Plan: 02*
*Completed: 2026-04-20*
