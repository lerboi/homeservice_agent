---
phase: 60.2
plan: 06
type: execute
wave: 2
depends_on: [60.2-05]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
autonomous: true
requirements: [CTX-60.2-FIX-J]
requirements_addressed: [CTX-60.2-FIX-G, CTX-60.2-FIX-H, CTX-60.2-FIX-J]

must_haves:
  truths:
    - "SKILL.md §1 notes livekit/agents#4486 as an accepted upstream limitation and cites commit 3e51d8e as the reason the google plugin pin cannot advance"
    - "SKILL.md §5 (Tools) documents the new deterministic runtime-filler pattern with per-session deps counter"
    - "SKILL.md Last-updated line is bumped to 2026-04-20 with a 60.2 entry"
    - "If Fix I was deferred at UAT, SKILL.md records that decision as a known-limitation note"
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md"
      provides: "Updated skill reference aligned with 60.2 runtime state"
      contains: "#4486"
  key_links:
    - from: "SKILL.md §5 tool-invocation pattern"
      to: "src/tools/*.py in sibling livekit-agent repo"
      via: "documented 'first await is filler' convention + deps-scoped counter"
      pattern: "_filler_idx_|deterministic filler|runtime filler"
---

<objective>
Close Phase 60.2 by updating the `voice-call-architecture` skill to reflect the new runtime state. Document Fix J (accepted limitation with upstream citations), Fix G + Fix H (new VAD and filler behaviour), and Fix I (verdict from Plan 05 UAT).

Purpose: CLAUDE.md rule — "When making changes to any system covered by a skill, update the skill to reflect the new state."
Output: Edited SKILL.md; commit in homeservice_agent repo.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-HUMAN-UAT.md

<interfaces>
SKILL.md is ~34k tokens. The executor MUST NOT read the whole file — use grep to locate
headings, then Read ±40 lines around each target section.

Target anchors (confirmed to exist per CONTEXT references):
- §1 heading substring: "Why livekit-agents" (pin-justification section)
- §5 heading substring: "Tools" or "## 5" (tools reference)
- "Last updated" line at the bottom

RESEARCH §"Fix J Verdict" provides verbatim Fix J text; RESEARCH §"Architecture Patterns"
+ §R4 provide the filler-pattern shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Fix J accepted-limitation note to SKILL.md section 1</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-RESEARCH.md (section titled "Fix J Verdict")
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (section 1 only — run grep to find heading line, then Read offset plus or minus 40 lines)
  </read_first>

  <action>
    Locate section 1 with:
    `grep -n "Why livekit-agents" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md`

    Read the matching offset plus or minus 40 lines to see the section body and identify where it ends (next `## ` or `### ` heading).

    Append this paragraph immediately before the next heading that terminates section 1. Use the text verbatim — do NOT paraphrase:

    Phase 60.2 — Upgrade research (Fix J): accepted limitation. The `_SegmentSynchronizerImpl.playback_finished called before text/audio input is done` warnings at tool-call boundaries and session-close are a known upstream limitation (livekit/agents issue #4486, OPEN since Jan 2026, no fix available). Commit `3e51d8e` (2026-04-11) on the google plugin was investigated as a candidate upgrade but cannot advance the pin — it restructures `RealtimeCapabilities` and ambiguously changes `generate_reply()` compatibility for `gemini-3.1-flash-live-preview`, risking silent breakage of the greeting and intake-injection paths. Phase 60.2 Fix G (server-VAD `silence_duration_ms` raised 1000→1500ms) and Fix H (deterministic runtime filler via `context.session.say()`) mitigate the user-visible symptoms without requiring a plugin upgrade. Re-evaluate the upgrade when upstream closes #4486 OR when a Railway preview environment can be stood up to test a post-`3e51d8e` pin end-to-end.

    Wrap that paragraph as a bolded lead ("**Phase 60.2 — Upgrade research (Fix J): accepted limitation.**") followed by the rest of the sentences as plain prose so it matches the style of the existing section 1 "Pin fix" paragraph.

    Do NOT edit the pre-existing section 1 content. Do NOT remove the existing pin-justification text. APPEND ONLY.
  </action>

  <verify>
    <automated>grep -c "#4486" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "3e51d8e" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "Phase 60.2" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "#4486" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "3e51d8e" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "Phase 60.2" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "silence_duration_ms" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (the Fix J paragraph cites it)
    - Original section 1 "Pin fix:" paragraph still present: `grep -c "Pin fix" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
  </acceptance_criteria>

  <done>Section 1 of SKILL.md gains a Phase 60.2 Fix J note citing issue #4486 and commit 3e51d8e; existing section 1 content preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Document runtime filler pattern in SKILL.md section 5 (Tools)</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-RESEARCH.md (section "Architecture Patterns" subsection R4; section "Code Examples")
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_availability.py (post-Plan-03 edited file — the current authoritative pattern)
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (section 5 only — use `grep -n "## 5"` or `grep -n "Tools"` to locate; Read plus or minus 50 lines)
  </read_first>

  <action>
    Locate section 5 in SKILL.md:
    `grep -n "^## 5\|^## Tools\|### Tools" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md`

    Read plus or minus 50 lines around the heading to understand existing tool-pattern documentation (tool-factory shape, STATE+DIRECTIVE return format, etc.).

    Insert a new subsection at the end of section 5 (before the next top-level `##`). Title it "Pre-tool filler audio (Phase 60.2)" and include:

    - One paragraph explaining WHY the pattern exists (Gemini raced filler text against function_call at text-generation layer, truncating filler mid-syllable when tool response arrived; programmatic `session.say()` makes the filler TEXT deterministic).
    - One paragraph noting the scoped tools (check_availability, book_appointment, capture_lead, transfer_call) and the tools explicitly OUT of scope (check_caller_history silent-context, check_customer_account pre-session-data, end_call space-character return).
    - A code block showing the canonical pattern (copy from RESEARCH section "Code Examples" — the check_availability example).
    - A two-line note that `allow_interruptions=False` is silently downgraded to `NOT_GIVEN` by livekit-agents 1.5.1 when the LLM is a RealtimeModel with server-side turn detection (so the kwarg is intent-only; Fix G's 1500ms silence threshold is what actually reduces interruption).
    - A one-line note that the rotation counter is per-session via the `deps` dict (NOT `global _filler_idx`) because concurrent calls on one Railway worker share module state.

    Also, within section 5's existing VAD/tool-cancellation notes (look for `silence_duration_ms` or `server cancelled tool calls`), add a sentence: "Phase 60.2 raised `silence_duration_ms` from 1000ms to 1500ms — see `src/agent.py` and SKILL.md section 1 Fix J note." If no such existing subsection exists in section 5, add it as a new paragraph adjacent to the new filler subsection.

    Do NOT duplicate the Fix J paragraph from Task 1 here — just cross-reference it.
  </action>

  <verify>
    <automated>grep -c "Pre-tool filler" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "_filler_idx_" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "session.say" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Pre-tool filler" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "_filler_idx_" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "session.say" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "allow_interruptions" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "silence_duration_ms" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 2 (Fix J reference plus section 5 note)
    - `grep -c "check_caller_history\|check_customer_account\|end_call" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (out-of-scope tools mentioned)
  </acceptance_criteria>

  <done>Section 5 documents the Phase 60.2 runtime-filler pattern with code example, scope, counter location, and allow_interruptions caveat.</done>
</task>

<task type="auto">
  <name>Task 3: Bump Last-updated line and record Fix I verdict</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-HUMAN-UAT.md (Fix I Verdict section — determines how Fix I is recorded)
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (bottom of file only — use `grep -n "Last updated"` to locate)
  </read_first>

  <action>
    **Part A — Bump Last-updated line.** Locate with:
    `grep -n "Last updated" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md`

    Read the matching line plus or minus 5 lines. Update the line to reflect 2026-04-20 and reference Phase 60.2. Use format matching existing entries (look at the pattern already in place — typical shape is a bulleted list under a "## Last updated" heading).

    Add a new entry at the top of the Last-updated log:

    ```
    - 2026-04-20 (Phase 60.2): Tool-call filler race + VAD tuning. Fix G raised server-VAD silence_duration_ms 1000→1500ms. Fix H shipped deterministic runtime filler via context.session.say() across 4 scoped tools (check_availability, book_appointment, capture_lead, transfer_call) with per-session deps-scoped rotation counter. Fix J (upstream google plugin upgrade) investigated and declined — see section 1. See .planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-SUMMARY.md for full context.
    ```

    **Part B — Record Fix I verdict.** Read `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-HUMAN-UAT.md` — specifically the "Fix I Verdict" section which was filled in at Plan 05 Task 2.

    - If the verdict is **RESOLVED**: append this sentence to the 2026-04-20 entry: "Fix I (post-call 8s TIMEOUT) observed absent in UAT — self-resolved once cancellations dropped; no code change required."
    - If the verdict is **DEFERRED**: append this sentence instead: "Fix I (post-call 8s TIMEOUT) deferred to future phase — UAT observed timeout still fires on clean calls; requires parallelizing post-call pipeline stages or raising the shutdown_process_timeout + timeout in lockstep. Tracked as known limitation."

    Read the UAT doc before picking the branch — do NOT guess.
  </action>

  <verify>
    <automated>grep -c "2026-04-20" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "Fix I" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md ; grep -c "60.2-SUMMARY.md" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "2026-04-20" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1
    - `grep -c "Phase 60.2" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 2 (Fix J note from Task 1 plus new Last-updated entry)
    - `grep -c "Fix I" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (verdict recorded)
    - `grep -c "60.2-SUMMARY.md" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (cross-link to summary)
    - Exactly one of these strings present based on UAT verdict: either "self-resolved" OR "deferred to future phase" — `grep -cE "self-resolved|deferred to future phase" SKILL.md` returns at least 1
  </acceptance_criteria>

  <done>Last-updated log reflects 2026-04-20 Phase 60.2 entry with Fix G, Fix H, Fix J summary plus Fix I verdict matching the UAT doc; cross-link to 60.2-SUMMARY.md present.</done>
</task>

</tasks>

<verification>
From `C:/Users/leheh/.Projects/homeservice_agent`:
- `grep -n "#4486\|Pre-tool filler\|2026-04-20\|60.2-SUMMARY" .claude/skills/voice-call-architecture/SKILL.md` shows all four anchors present
- Changed file: only `.claude/skills/voice-call-architecture/SKILL.md`

Commit in homeservice_agent repo (normal commit — no `--no-verify` needed, this is the main repo):
`git commit -m "docs(60.2): update voice-call-architecture skill — filler pattern, VAD raise, Fix J accepted limitation"`
</verification>

<success_criteria>
- Section 1 includes Fix J accepted-limitation note with issue #4486 and commit 3e51d8e citations.
- Section 5 documents the new runtime-filler pattern with code example, scope list, and allow_interruptions caveat.
- Last-updated log bumped to 2026-04-20 with Phase 60.2 entry.
- Fix I verdict from UAT correctly recorded (self-resolved or deferred).
- All cross-repo paths use the Windows path convention (hyphen, not underscore).
</success_criteria>

<output>
After completion, create `C:\Users\leheh\.Projects\homeservice_agent\.planning\phases\60.2-voice-tool-call-filler-race-and-vad-tuning\60.2-06-SUMMARY.md`. Also create the phase-level rollup `C:\Users\leheh\.Projects\homeservice_agent\.planning\phases\60.2-voice-tool-call-filler-race-and-vad-tuning\60.2-SUMMARY.md` since this is the terminal plan.
</output>
