---
phase: 60.2-voice-tool-call-filler-race-and-vad-tuning
plan: "05"
subsystem: testing

requires:
  - phase: 60.2
    provides: Fix G (VAD silence_duration_ms 1500ms), Fix H (dropped — see below), Plan 04 prompt (reverted — see below)
provides:
  - UAT verdict for 60.2 fixes against live Persona 1 call (call-_+6587528516_945EJsSSXxWh, 226s)
  - Fix I verdict (RESOLVED — no 8s post-call timeout observed)
  - Fix H accepted-limitation finding (session.say incompatible with AgentSession(llm=RealtimeModel) in livekit-agents 1.5.1)
  - Inline remediation (revert Fix H + Plan 04 prompt) committed in sibling repo as cbe1bb9
affects: [60.3 — goodbye cut-off diagnosis, Gemini-Live prompt audit]

tech-stack:
  added: []
  patterns:
    - "RealtimeModel-only AgentSession cannot drive session.say() — do not attempt programmatic filler audio without attaching a TTS"
    - "Post-UAT inline remediation: revert broken code before writing SUMMARY rather than advancing with a known-broken fix"

key-files:
  created:
    - .planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-HUMAN-UAT.md
  modified:
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (Plan 04 revert)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_availability.py (Fix H removal)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/book_appointment.py (Fix H removal)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py (Fix H removal)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/transfer_call.py (Fix H removal)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py (inverted assertions)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_filler_audio.py (deleted)

key-decisions:
  - "Fix G alone is sufficient for the tool-call-cancellation race — all 7 UAT gates passed without Fix H's runtime filler ever firing"
  - "Drop Fix H rather than repair it — repairing would require attaching a TTS to AgentSession, introducing voice mismatch with Gemini's native voice"
  - "Goodbye cut-off is a separate race class (end-of-call turn, not tool-call turn) and needs diagnostic instrumentation before a prompt/code fix — deferred to Phase 60.3"
  - "Restore pre-Plan-04 prompt verbatim (do not conflate revert with prompt-philosophy rewrite) — Gemini-Live + v1.5.1 best-practice audit is a separate 60.3 scope"

patterns-established:
  - "UAT-surfaced structural defect → revert in place before writing SUMMARY — prevents a `passed` SUMMARY from cementing broken code"
  - "livekit-agents 1.5.1 session.say() requires TTS — not a usable filler mechanism on RealtimeModel-only sessions"

requirements-completed: []

duration: 60min
completed: 2026-04-20
---

# Phase 60.2 Plan 05 — UAT Regression + Live Persona 1 Call

**UAT against live Railway build with Fix G + Fix H + Plan 04 prompt. All 7 gates passed, but Railway logs revealed Fix H was a silent no-op; inline remediation reverted Fix H and Plan 04 prompt; Fix G retained as the phase's working fix.**

## Performance

- **Duration:** ~60 min (live call + log analysis + remediation + commit)
- **Started:** 2026-04-20 (see 60.2-HUMAN-UAT.md)
- **Completed:** 2026-04-20
- **Tasks:** 2/2
- **Files modified:** 7 in sibling repo + 2 in orchestrator repo

## Accomplishments

- Executed live Persona 1 UAT call (226s, emergency-booking flow).
- Collected verdicts for all 7 success-criteria gates — all PASS.
- Fix I verdict: RESOLVED (no 8s post-call timeout on this call).
- Surfaced the Fix H failure mode: `session.say()` cannot produce audio on a RealtimeModel-only `AgentSession` in livekit-agents 1.5.1 (3× `filler say() failed: trying to generate speech from text without a TTS model` warnings in Railway logs).
- Diagnosed the production contradiction: Plan 04's prompt told Gemini "runtime plays filler" while the runtime silently failed to do so; Gemini still spoke its own filler on this call thanks to the fallback tone guidance kept in the post-merge prompt.
- Reverted Fix H from the 4 scoped tools (removed `_FILLER_PHRASES`, deps-scoped rotation counter, try/except `await context.session.say(...)`).
- Restored `_build_tool_narration_section()` to pre-Plan-04 content; model speaks its own filler again.
- Deleted `tests/test_filler_audio.py`; inverted `tests/test_prompt.py` to guard against re-landing a broken Fix H.
- Full suite 107/107 → 101/101 passing post-revert.
- Documented the goodbye cut-off as a separate race for Phase 60.3 (not the same class as the tool-call race Fix G solves).

## Task Commits

1. **Task 1: Run full automated regression suite + create HUMAN-UAT template** — `05415c7` (orchestrator repo)
2. **Task 2: Manual UAT Persona 1 call + findings capture + inline remediation** — sibling repo `cbe1bb9` (revert Fix H + restore pre-Plan-04 prompt + test surgery) + orchestrator repo HUMAN-UAT findings + this SUMMARY

## Files Created/Modified

**Orchestrator repo (`homeservice_agent`):**
- `.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-HUMAN-UAT.md` — live UAT findings, Fix I verdict, remediation log
- `.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-05-SUMMARY.md` — this file

**Sibling repo (`livekit-agent`, commit `cbe1bb9`):**
- `src/prompt.py` — `_build_tool_narration_section()` restored to pre-Plan-04 text
- `src/tools/check_availability.py`, `src/tools/book_appointment.py`, `src/tools/capture_lead.py`, `src/tools/transfer_call.py` — Fix H removed
- `tests/test_prompt.py` — inverted assertions
- `tests/test_filler_audio.py` — deleted

## Decisions Made

- **Drop Fix H, not repair it.** Repairing would require attaching a TTS (Google, Cartesia, ElevenLabs) to the session — either creates a voice mismatch with Gemini's native voice for a 1–3s filler, or requires a matching-voice TTS that isn't available for Gemini Live's speaker identity. Cost/benefit is poor given Fix G alone passes the UAT gates.
- **Revert Plan 04's prompt change verbatim** rather than rewrite. User directive: don't conflate revert with prompt-philosophy rewrite. Gemini-Live + v1.5.1 + plugin-@43d3734 best-practice audit is tracked as Phase 60.3 scope.
- **Defer goodbye cut-off to 60.3.** It's a different race (end-of-call turn, not tool-call turn). The existing `prompt.py:562-567` goodbye-protection guidance and `end_call` tool description already contain the correct rule — needs diagnostic logging to determine why Gemini ignored it on this call before attempting a fix.

## Deviations from Plan

**Plan 05 did not specify a remediation step — it specified UAT only and a checkpoint return.**

Remediation was required because Plan 05's findings exposed a silently-failing Fix H that would have been cemented by an unmodified "passed" SUMMARY. Per user approval ("go with your recommendations"), the revert was executed inline rather than deferred to Phase 60.3.

### Auto-fixed Issues

**1. [Rule 1 — Blocking in production] Fix H runtime filler is a no-op**
- **Found during:** Task 2 Railway-log review after the UAT call.
- **Issue:** `context.session.say(phrase, allow_interruptions=False)` raises `trying to generate speech from text without a TTS model` on every call. The try/except swallows the error. Caller hears silence.
- **Fix:** Removed Fix H filler code from all 4 scoped tools; restored pre-Plan-04 `_build_tool_narration_section()` so Gemini speaks its own filler; inverted the prompt regression test; deleted the now-meaningless filler tests.
- **Files modified:** `src/prompt.py`, `src/tools/{check_availability,book_appointment,capture_lead,transfer_call}.py`, `tests/test_prompt.py`, `tests/test_filler_audio.py` (deleted).
- **Verification:** 101/101 pytest pass; Railway build deployed from commit `cbe1bb9`.
- **Committed in:** `cbe1bb9` (sibling repo).

---

**Total deviations:** 1 auto-fixed (1 blocking-in-production)
**Impact on plan:** Remediation kept the phase consistent — shipping a broken Fix H alongside a "filler now handled by runtime" prompt would have created a silent-audio regression for any caller whose own speech didn't cover the tool-execution window. Fix G (the actual working fix) remains unchanged.

## Issues Encountered

- Pre-UAT, the user's pull on the sibling repo brought upstream changes and dropped Plan 04's prompt rewrite during merge. That was caught and re-applied before the UAT call — but the UAT surfaced that Plan 04's premise (runtime plays filler) was wrong anyway, so both the re-apply and the original Plan 04 are rolled back by `cbe1bb9`.
- Caller reported the AI's goodbye cut off mid-sentence. Distinct from the tool-call race and not a Fix G/H regression — deferred to Phase 60.3 with instrumentation plan spelled out in HUMAN-UAT.md.

## User Setup Required

None — the Railway build containing the remediation (`cbe1bb9`) was pushed by the user after the inline revert.

## Next Phase Readiness

- Plan 06 (voice-call-architecture SKILL.md update) can now proceed with the **final** 60.2 state: Fix G shipped, Fix H reverted as accepted limitation (document the TTS-requirement citation against `AgentSession(llm=RealtimeModel)` in v1.5.1), Fix I resolved.
- Phase 60.3 scope locked in: (a) goodbye cut-off diagnosis starting with `end_call` invocation-timing + last-text-token logging, (b) Gemini 3.1 Flash Live + livekit-agents 1.5.1 + livekit-plugins-google @ 43d3734 prompt-best-practices audit of the full `build_system_prompt()`.

---
*Phase: 60.2-voice-tool-call-filler-race-and-vad-tuning*
*Completed: 2026-04-20*
