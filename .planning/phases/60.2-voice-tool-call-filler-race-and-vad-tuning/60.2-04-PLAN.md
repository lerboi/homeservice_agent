---
phase: 60.2
plan: 04
type: execute
wave: 1
depends_on: [60.2-01]
files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py
autonomous: true
requirements: [CTX-60.2-FIX-H]
requirements_addressed: [CTX-60.2-FIX-H]

must_haves:
  truths:
    - "_build_tool_narration_section() informs the model that the tool runtime plays a filler automatically"
    - "_build_tool_narration_section() instructs the model NOT to speak its own filler before check_availability, book_appointment, capture_lead, or transfer_call"
  artifacts:
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/prompt.py"
      provides: "Updated _build_tool_narration_section() that reflects runtime-handled filler"
      contains: "runtime"
  key_links:
    - from: "src/prompt.py _build_tool_narration_section()"
      to: "system_prompt assembled in src/prompt.py:516"
      via: "string composition in build_system_prompt (unchanged)"
      pattern: "_build_tool_narration_section\\(\\)"
---

<objective>
Update `src/prompt.py` `_build_tool_narration_section()` (line ~112) to reflect the fact that Fix H (Plan 03) now handles filler audio at the tool runtime. The model should be instructed NOT to speak its own filler before the four scoped tools — that would overlap the runtime filler and revive the race we just killed.

Purpose: Keep the prompt aligned with runtime reality; prevent double-filler overlap.
Output: One function body edit in `src/prompt.py`; the Wave 0 `test_tool_narration_mentions_runtime_filler` test flips GREEN.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-RESEARCH.md
@C:/Users/leheh/.Projects/livekit-agent/src/prompt.py
@C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py

<interfaces>
`_build_tool_narration_section()` lives at `src/prompt.py:112`. It's called at
`src/prompt.py:516` inside `build_system_prompt()`. The function returns a string
composed of directive text the AI reads as part of its system prompt.

Wave 0 test (Plan 01) asserts the returned string contains BOTH:
- the word "runtime" OR "automatically" (case-insensitive)
- a phrase matching "do not speak" / "don't speak" / "do not generate" (case-insensitive)

The final wording is Claude's discretion provided both substrings are present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update _build_tool_narration_section() for runtime-handled filler</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/prompt.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-CONTEXT.md (Fix H section — "Once Fix H is in place, update the prompt to reflect reality" subsection)
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (full `_build_tool_narration_section` function — line 112 onward)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py (assertion contract)
  </read_first>

  <behavior>
    - The returned string contains a sentence (or short paragraph) stating:
      - The tool runtime plays an acknowledgement phrase automatically when `check_availability`, `book_appointment`, `capture_lead`, or `transfer_call` is invoked
      - The model should NOT speak its own filler before those four tools — just invoke them
    - The existing filler prose examples MAY remain as fallback tone guidance (CONTEXT says they "can stay as fallback prose but their criticality drops").
  </behavior>

  <action>
    Open `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py` and locate `_build_tool_narration_section()` at line 112. Read the full function body to understand its current structure before editing.

    Then prepend (or integrate, depending on the function's existing structure) a new paragraph at the TOP of the returned string. The new paragraph MUST contain both required substrings:
    1. The word "runtime" or "automatically"
    2. The phrase "Do NOT speak your own filler" (capitalized as a directive, matching the assertion's "do not speak" match)

    Use this verbatim paragraph as the new content to insert (preserve the function's existing return-string concatenation style — if it uses a list joined with `\n`, add this as a new leading entry; if it uses an f-string, prepend):

    ```
    The tool runtime automatically plays a short acknowledgement phrase when you invoke
    check_availability, book_appointment, capture_lead, or transfer_call. Do NOT speak
    your own filler phrase before those four tools — just invoke the tool. The runtime
    filler covers the execution latency for you.
    ```

    **Preservation rules:**
    - Keep the existing filler prose examples (e.g. "Let me check that for you") further down in the section as fallback tone guidance. Their criticality drops, but deleting them would change agent voice style.
    - Do NOT change the function signature, return type, or call site at line 516.
    - Do NOT edit any other section-builder function in `src/prompt.py`.

    If the function currently returns a multi-line string via a list and `\n\n`.join, insert this paragraph as the first list entry. If it uses a triple-quoted string, prepend as a new leading paragraph with a blank line separator.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && python -m pytest tests/test_prompt.py -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "runtime" C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` increases by at least 1 compared to the file before this edit (new mention in narration section)
    - `grep -c "Do NOT speak" C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` returns at least 1
    - `grep -c "check_availability, book_appointment, capture_lead, or transfer_call" C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` returns at least 1
    - `grep -c "def _build_tool_narration_section" C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` still returns 1 (signature unchanged)
    - `grep -c "_build_tool_narration_section()" C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` still returns at least 1 (call site preserved)
    - `python -m pytest tests/test_prompt.py -x` exits 0 (Wave 0 test flips GREEN)
    - `python -m pytest tests/ -x -q` exits 0 (full suite green — no regressions)
  </acceptance_criteria>

  <done>_build_tool_narration_section() returns a string that informs the model of runtime-handled filler and forbids self-generated filler for the 4 scoped tools; Wave 0 prompt test green; full suite green.</done>
</task>

</tasks>

<verification>
Run from `C:\Users\leheh\.Projects\livekit-agent`:
- `python -m pytest tests/test_prompt.py -x -q` — GREEN
- `python -m pytest tests/ -x -q` — full suite green

Commit in livekit-agent repo with `--no-verify`:
`git commit --no-verify -m "docs(60.2): prompt notes runtime-handled filler; model must not emit own filler"`
</verification>

<success_criteria>
- Narration section informs the model that filler is runtime-handled.
- Narration section forbids self-generated filler for the 4 scoped tools.
- Wave 0 prompt test green.
- No regressions in the full Python test suite.
</success_criteria>

<output>
After completion, create `C:\Users\leheh\.Projects\homeservice_agent\.planning\phases\60.2-voice-tool-call-filler-race-and-vad-tuning\60.2-04-SUMMARY.md`
</output>
