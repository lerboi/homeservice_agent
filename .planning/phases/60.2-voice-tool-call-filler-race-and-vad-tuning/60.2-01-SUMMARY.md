---
phase: 60.2-voice-tool-call-filler-race-and-vad-tuning
plan: 01
subsystem: testing
tags: [pytest, pytest-asyncio, livekit-agents, fix-h, wave-0, scaffolding]

# Dependency graph
requires:
  - phase: 60.1
    provides: "Existing pytest suite + tool factory pattern (create_*_tool(deps)) in livekit-agent"
provides:
  - "Shared mock_run_context + deps_factory fixtures in tests/conftest.py"
  - "7 RED test stubs in tests/test_filler_audio.py pinning Fix H behavior contracts"
  - "1 RED test stub in tests/test_prompt.py reserving the narration update assertion"
  - "Wave 1 tasks (Plans 03 and 04) now have non-MISSING automated verify commands"
affects: [60.2-03-fix-h-tools, 60.2-04-prompt-update]

# Tech tracking
tech-stack:
  added: []  # no new deps — pytest-asyncio 0.23 already pinned
  patterns:
    - "Per-session deps-dict mocking for tool factories (MagicMock-free; plain dict is enough)"
    - "AsyncMock.await_args.args[0] assertions for session.say() verification"
    - "Wave 0 scaffold pattern: stubs are deliberately RED until the implementing plan lands"

key-files:
  created:
    - "C:/Users/leheh/.Projects/livekit-agent/tests/conftest.py"
    - "C:/Users/leheh/.Projects/livekit-agent/tests/test_filler_audio.py"
    - "C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py"
  modified: []

key-decisions:
  - "Wave 0 tests intentionally shipped RED — Plans 03/04 flip them GREEN per Nyquist rule"
  - "_minimal_deps() returns an empty dict; Plan 03 will extend per-tool as needed to reach the filler await without crashing earlier"
  - "Phrase sets in test_filler_audio.py were copied verbatim from CONTEXT.md 'Per-tool filler phrases' so Plan 03 implementation must match exactly"

patterns-established:
  - "Shared AsyncMock session.say fixture (mock_run_context) — reusable across all future tool tests"
  - "deps_factory returns a zero-arg factory so tests can simulate N independent sessions cheaply"

requirements-completed: [CTX-60.2-FIX-H]

# Metrics
duration: ~7min
completed: 2026-04-19
---

# Phase 60.2 Plan 01: Wave 0 Fix H Test Scaffolding Summary

**Seven RED pytest-asyncio stubs + shared mock_run_context/deps_factory fixtures in the sibling livekit-agent repo, pinning the Fix H filler-audio contract for Wave 1.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-19T20:00Z
- **Completed:** 2026-04-19T20:06Z
- **Tasks:** 3
- **Files created:** 3 (in sibling repo)

## Accomplishments

- Shared pytest fixtures (`mock_run_context`, `deps_factory`) landed in `tests/conftest.py` so every future Fix H test gets a drop-in AsyncMock `session.say`.
- `tests/test_filler_audio.py` scaffolds 7 RED assertions matching VALIDATION.md test IDs 1:1 (4 per-tool plays-filler + exception graceful-degradation + per-session-counter + rotation).
- `tests/test_prompt.py` reserves the single narration assertion Plan 04 must make GREEN.
- Baseline test suite remains healthy: **99 tests collect → 107 tests collect** after Wave 0, no regressions.
- Wave 1 plans (03 and 04) now satisfy the Nyquist rule — all their verify commands reference test IDs that actually exist.

## Task Commits

Each task committed atomically in the sibling `livekit-agent` repo on `main`:

1. **Task 1: Create conftest.py with mock_run_context + deps_factory** — `f65810f` (test)
2. **Task 2: Create test_filler_audio.py with 7 RED Fix H stubs** — `1cca025` (test)
3. **Task 3: Create test_prompt.py with narration assertion** — `0710ce7` (test)

_All three commits use `--no-verify` per the Phase 60 sibling-repo pattern documented in CONTEXT.md._

## Files Created/Modified

- `C:/Users/leheh/.Projects/livekit-agent/tests/conftest.py` — shared `mock_run_context` (SimpleNamespace with `.session.say = AsyncMock`) and `deps_factory` (returns zero-arg dict factory).
- `C:/Users/leheh/.Projects/livekit-agent/tests/test_filler_audio.py` — 7 pytest-asyncio tests covering: plays-filler per tool (4), exception-graceful-degradation (1), per-session counter isolation (1), 3-phrase rotation modulo (1). Phrase literals match CONTEXT.md verbatim.
- `C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py` — 1 test asserting `_build_tool_narration_section()` mentions runtime-handled filler AND instructs the model not to speak its own filler (Plan 04 will satisfy).

## Decisions Made

- **RED-by-design:** These stubs intentionally fail until Plans 03/04 ship. That is the Wave 0 contract — Plan 03's executor will observe the failures turn GREEN as real Fix H code lands, confirming each test ID corresponds to a real behavior change.
- **No tool-body stubbing in Wave 0:** `_minimal_deps()` returns `{}`. Plan 03 will extend it per-tool so each tool factory can reach the `await session.say()` line without earlier `KeyError`/`AttributeError` crashes. Keeping Wave 0 minimal avoids hardcoding tool-internal details that haven't been finalized.
- **Verbatim phrase sets:** Hard-coding the CONTEXT.md phrase strings into the test assertions creates a deliberate coupling — Plan 03 cannot silently deviate from the agreed filler phrases; any edit requires coordinated updates in both places.

## Deviations from Plan

None — plan executed exactly as written. The Wave 0 `<action>` blocks gave verbatim file contents; each Task's `<acceptance_criteria>` was met literally.

## Issues Encountered

None. All three `python -m pytest --collect-only` verification runs passed on first attempt. Total collection count went from 99 → 107 (7 filler + 1 prompt = +8 as expected).

## User Setup Required

None — no external services, env vars, or dashboard actions. Wave 0 is test-only scaffolding in the sibling repo.

## Next Phase Readiness

- **Plan 02 (Fix G VAD tuning, Wave 1, independent)** — unblocked; operates on `src/agent.py`, no dependency on this scaffolding.
- **Plan 03 (Fix H tool filler, Wave 1)** — unblocked; 7 RED tests are now addressable. Executor should extend `_minimal_deps()` in `test_filler_audio.py` once per tool to surface any deps-dict keys the tool body reads before the `await session.say()` line.
- **Plan 04 (prompt narration update, Wave 1)** — unblocked; 1 RED test reserved. Executor must ensure the updated `_build_tool_narration_section()` contains BOTH a "runtime" / "automatically" phrasing AND a "do not speak" / "don't speak" / "do not generate" instruction.

## Self-Check: PASSED

**Files exist:**
- FOUND: C:/Users/leheh/.Projects/livekit-agent/tests/conftest.py
- FOUND: C:/Users/leheh/.Projects/livekit-agent/tests/test_filler_audio.py
- FOUND: C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py

**Commits exist (in sibling repo `livekit-agent` on `main`):**
- FOUND: f65810f (Task 1 — conftest.py)
- FOUND: 1cca025 (Task 2 — test_filler_audio.py)
- FOUND: 0710ce7 (Task 3 — test_prompt.py)

**Acceptance criteria:**
- `python -m pytest tests/ --collect-only -q` → 107 tests collected (was 99; +8 = 7 filler + 1 prompt). No regressions.
- `python -m pytest tests/test_filler_audio.py --collect-only -q` → 7 tests collected. ✓
- `python -m pytest tests/test_prompt.py --collect-only -q` → 1 test collected. ✓
- All required substrings present in each file (verbatim phrase strings, fixture names, test function names) — verified during Write.

---
*Phase: 60.2-voice-tool-call-filler-race-and-vad-tuning*
*Plan: 01*
*Completed: 2026-04-19*
