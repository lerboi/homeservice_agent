---
phase: 61
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - livekit-agent/src/prompt.py
  - livekit-agent/src/tools/book_appointment.py
  - livekit-agent/src/tools/capture_lead.py
  - livekit-agent/tests/test_prompt_address_validation_rule.py
  - livekit-agent/tests/test_tool_descriptions_validation_precondition.py
  - .claude/skills/voice-call-architecture/SKILL.md
  - .claude/skills/auth-database-multitenancy/SKILL.md
  - .claude/skills/integrations-jobber-xero/SKILL.md
  - CLAUDE.md
autonomous: false
requirements:
  - D-E1
  - D-E3
  - D-G1
  - D-G2
  - D-G3

must_haves:
  truths:
    - "book_appointment + capture_lead tool descriptions contain the validation precondition language (D-E1) — outcome-framed, not usage hint"
    - "prompt.py contains a new CRITICAL RULE block (D-E3) hoisted into the top-attention zone alongside Phase 60 anti-hallucination rule"
    - "The new CRITICAL RULE prohibits the 6 verbatim phrases ('validated', 'verified', 'confirmed against Google', 'found your address', 'looked up your address', 'matches our records') unless preceding tool return contained 'verdict=validated' or 'verdict=validated_with_corrections'"
    - "Spanish mirror of the CRITICAL RULE lands in the same pass (Phase 60.3 D-B-03 locale-parity pattern)"
    - "voice-call-architecture SKILL.md updated with Phase 61 subsection covering google_maps integration + tool-return shapes + CRITICAL RULE"
    - "auth-database-multitenancy SKILL.md updated with migration 062 entry + new columns on appointments/inquiries + new gmaps_validate_events table + RPC overload note"
    - "integrations-jobber-xero SKILL.md cross-links the new D-D1 address_components shape (Phase 62 Jobber push will read these named keys)"
    - "CLAUDE.md migration count claim updated from 58 to 62"
    - "User has placed a UAT call AFTER Plan 03 deploy and verified the agent uses verdict-correct language (no 'validated' on unconfirmed; correct 'I have it as X — is that right?' on confirmed_with_changes)"
  artifacts:
    - path: "livekit-agent/src/prompt.py"
      provides: "_build_address_validation_section (new EN+ES locale-branched section per Phase 60.3 D-B-03 pattern)"
    - path: "livekit-agent/src/tools/book_appointment.py"
      provides: "Tool description rewritten with validation precondition (D-E1)"
    - path: "livekit-agent/src/tools/capture_lead.py"
      provides: "Tool description rewritten with validation precondition (D-E1, symmetric)"
    - path: "livekit-agent/tests/test_prompt_address_validation_rule.py"
      provides: "TDD assertions locking the EN+ES CRITICAL RULE presence + position + prohibited-phrase list"
    - path: ".claude/skills/voice-call-architecture/SKILL.md"
      provides: "Phase 61 subsection added"
    - path: ".claude/skills/auth-database-multitenancy/SKILL.md"
      provides: "Migration 062 entry + new columns + new table"
    - path: ".claude/skills/integrations-jobber-xero/SKILL.md"
      provides: "D-D1 cross-link"
  key_links:
    - from: "prompt.py _build_address_validation_section"
      to: "build_system_prompt()"
      via: "section called with locale; assembled into full prompt in top-attention position"
      pattern: "_build_address_validation_section.*locale"
    - from: "book_appointment.py tool description"
      to: "Gemini 3.1 Flash Live function declaration"
      via: "description field on the function spec dict"
      pattern: "address.*will be validated"
    - from: "CLAUDE.md migration count"
      to: "supabase/migrations/ on disk"
      via: "62 migrations after 062 ships"
      pattern: "62 migrations|62 DB migrations"
---

<objective>
Land the prompt-side rules that govern HOW the agent speaks the verdict-driven tool returns from Plan 03. Specifically:

1. Rewrite tool descriptions for `book_appointment` and `capture_lead` to encode the validation precondition (D-E1)
2. Add a new CRITICAL RULE block (D-E3) to `prompt.py` — hoisted into the top-attention zone alongside the Phase 60 anti-hallucination rule
3. Spanish mirror of the new rule (Phase 60.3 D-B-03 locale-parity pattern)
4. Update three SKILL.md files (voice-call-architecture, auth-database-multitenancy, integrations-jobber-xero) per CLAUDE.md skill-update mandate
5. Fix the stale "58 migrations" claim in CLAUDE.md → "62"
6. Pause for [BLOCKING] human UAT call after deploy — manual-only verification of D-B3 + D-E3 in production audio

This is a checkpoint plan — autonomous=false because the final UAT step requires the user to place real calls and report back.

Purpose: The full Phase 61 effect surfaces in this plan. After this lands, the LiveKit agent has every guard rail to speak the right thing in every verdict case AND the codebase documentation reflects the Phase 61 reality.

Output: 2 modified Python files (prompt.py + 2 tool files) + 1 new test file + 3 SKILL.md updates + 1 CLAUDE.md update + UAT signoff.
</objective>

<execution_context>
@/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-CONTEXT.md
@.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-RESEARCH.md
@.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-01-SUMMARY.md
@.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-02-SUMMARY.md
@.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-03-SUMMARY.md
@.claude/skills/voice-call-architecture/SKILL.md
@.claude/skills/auth-database-multitenancy/SKILL.md
@.claude/skills/integrations-jobber-xero/SKILL.md
@CLAUDE.md

<dual_repo_note>
Code files (prompt.py, book_appointment.py, capture_lead.py, test files) ship to BOTH:
- `livekit-agent/...` (mirror)
- `C:/Users/leheh/.Projects/livekit-agent/...` (sibling)

Skill + CLAUDE.md updates ship ONLY to the homeservice_agent repo (no sibling — they're documentation).
</dual_repo_note>

<interfaces>
<!-- Pinned existing prompt.py structure to extend -->

From C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (the Phase 60.3 modular section structure — locale-branched per D-B-03):
The build_system_prompt function assembles sections in a fixed order. Anti-hallucination CRITICAL RULES live in the TOP-ATTENTION zone (positions 2-6 per Phase 60.3 SUMMARY). The existing Phase 60 D-15 anti-hallucination block (caller-supplied facts) lives in `_build_corrections_section`. The new D-E3 rule is a SEPARATE rule about a SEPARATE truth-class ("validated") — it gets its OWN section.

Pattern: every locale-branched section has signature `_build_<name>_section(locale: str = "en")` and an `if locale == "es":` branch returning the Spanish mirror.

Existing tool descriptions to rewrite (current text — locate in book_appointment.py around lines 195-210 and capture_lead.py around lines 18-34):

book_appointment current description (approx — verify by full read):
> "Books an appointment for the caller. Use after check_availability has confirmed the slot is open. Provide slot_token (from check_availability), street_name, postal_code, optional unit_number, and caller_name. Returns BOOKED on success or SLOT_TAKEN if the slot was claimed by another call."

capture_lead current description (approx):
> "Captures an unbooked lead — the caller wants service but no booking can be completed (e.g., outside service area, no slots available). Records caller name, phone, job_type, address fields, and urgency. Returns LEAD_CAPTURED on success."

D-E3 prohibited phrase list (verbatim from CONTEXT.md):
- "validated"
- "verified"
- "confirmed against Google"
- "found your address"
- "looked up your address"
- "matches our records"

These phrases are ONLY allowed when the immediately preceding tool return contained `verdict=validated` or `verdict=validated_with_corrections`.

Spanish mirror prohibited phrases (planner pins these; outcome-framed; consistent with Phase 60.3 USTED-register pattern):
- "validado/validada"
- "verificado/verificada"
- "confirmado contra Google"
- "encontré su dirección"
- "consulté su dirección"
- "coincide con nuestros registros"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-E3 CRITICAL RULE to prompt.py (EN+ES) + tool description rewrites (D-E1) + TDD tests</name>
  <files>
    livekit-agent/src/prompt.py,
    livekit-agent/src/tools/book_appointment.py,
    livekit-agent/src/tools/capture_lead.py,
    livekit-agent/tests/test_prompt_address_validation_rule.py,
    livekit-agent/tests/test_tool_descriptions_validation_precondition.py
  </files>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (full — confirm current section assembly order; locate position 5 zone where call_duration sits per Phase 60.3 P05; D-E3 rule sits ALONGSIDE existing anti-hallucination block, NOT inside it)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_outcome_words.py (Phase 60.3 P09 — locale-parity test pattern; replicate this assertion structure)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_corrections.py (Phase 60.3 P08 — CRITICAL RULE test pattern; replicate)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/book_appointment.py (tool description block ~lines 195-235)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py (tool description block ~lines 18-34)
    - .planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-CONTEXT.md §D-E1 + D-E3 (the verbatim outcome-framed wording requirements)
    - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_livekit_prompt_philosophy.md (the user's prompt-engineering principles — outcome-based, SDK-matched, non-directive, Gemini 3.1 Flash Live-tuned)
  </read_first>
  <behavior>
    **Tests FIRST (RED):** create both test files describing the expected prompt + tool-description state. Tests must fail because prompt.py + tool descriptions are not yet edited.

    **`tests/test_prompt_address_validation_rule.py` — 8 tests minimum:**

    EN-locale tests (4):
    1. `test_en_address_validation_rule_present`: `build_system_prompt(..., locale="en")` contains the substring `ADDRESS VALIDATION — CRITICAL RULE` (header)
    2. `test_en_prohibited_phrases_enumerated`: assembled EN prompt contains all 6 prohibited phrases as quoted/listed items: `"validated"`, `"verified"`, `"confirmed against Google"`, `"found your address"`, `"looked up your address"`, `"matches our records"` — at minimum 5 of 6 substrings present (allowing prose flexibility)
    3. `test_en_unless_clause_present`: assembled EN prompt contains a substring matching `verdict=validated` AND `verdict=validated_with_corrections` (the unless-clause that allows the phrasing)
    4. `test_en_position_in_top_attention_zone`: the new section appears BEFORE `_build_tool_narration_section`'s output (top-attention zone — matches Phase 60.3 P09 outcome_words pattern)

    ES-locale tests (4):
    5. `test_es_address_validation_rule_present`: `build_system_prompt(..., locale="es")` contains a Spanish header (e.g. `VALIDACIÓN DE DIRECCIÓN — REGLA CRÍTICA`)
    6. `test_es_prohibited_phrases_enumerated`: at minimum 4 of 6 Spanish prohibited phrases (`validado`/`validada`, `verificado`/`verificada`, `confirmado contra Google`, `encontré su dirección`, `consulté su dirección`, `coincide con nuestros registros`) present
    7. `test_es_unless_clause_present`: ES prompt contains both `verdict=validated` AND `verdict=validated_with_corrections` (tool-return tokens are NOT translated — they're code identifiers)
    8. `test_es_position_in_top_attention_zone`: ES section appears before tool_narration ES output

    **`tests/test_tool_descriptions_validation_precondition.py` — 4 tests minimum:**

    1. `test_book_appointment_description_mentions_validation`: import the function spec from book_appointment.py; assert description field contains "validated" OR "validation" AND "address" (D-E1 outcome-framed precondition)
    2. `test_book_appointment_description_mentions_tool_return_branches`: description mentions that the tool return tells the agent whether the address was confirmed/corrected/unverified (D-E1 outcome-framed)
    3. `test_capture_lead_description_mentions_validation`: same as above for capture_lead
    4. `test_capture_lead_description_mentions_tool_return_branches`: same

    **GREEN phase — implementation:**

    --- STEP 1.1: Add `_build_address_validation_section` to prompt.py ---

    Insert the new section builder following the Phase 60.3 P09 pattern. Signature: `_build_address_validation_section(locale: str = "en") -> str`.

    EN body (outcome-framed; non-directive; references the tool-return token explicitly per memory `feedback_livekit_prompt_philosophy.md`):
    ```
    ADDRESS VALIDATION — CRITICAL RULE:

    The booking and lead tools validate the caller's address against an external service before
    committing. The tool return tells you what happened — read it carefully.

    When the tool return contains `verdict=validated` or `verdict=validated_with_corrections`,
    the address has been confirmed and you may speak the normalized form back to the caller as
    the final address.

    When the tool return contains `verdict=unvalidated` (any other case — unconfirmed, error,
    skipped, or unsupported region), the address has NOT been confirmed. You may only speak
    back what the caller themselves said.

    NEVER use any of these phrases unless the immediately preceding tool return contained
    `verdict=validated` or `verdict=validated_with_corrections`:
      - "validated"
      - "verified"
      - "confirmed against Google"
      - "found your address"
      - "looked up your address"
      - "matches our records"

    Saying any of these without the verdict to back them up is the worst failure mode in this
    section — the caller hangs up believing their address was checked when it was not. Silence
    or a neutral readback is always acceptable; false confirmation is not.
    ```

    ES body (USTED register, mirrors EN structure exactly):
    ```
    VALIDACIÓN DE DIRECCIÓN — REGLA CRÍTICA:

    Las herramientas de reserva y captura de cliente validan la dirección del llamante con un
    servicio externo antes de confirmar. El retorno de la herramienta le dice qué pasó — léalo
    con atención.

    Cuando el retorno contiene `verdict=validated` o `verdict=validated_with_corrections`, la
    dirección fue confirmada y puede repetir la forma normalizada al llamante como la dirección
    final.

    Cuando el retorno contiene `verdict=unvalidated` (cualquier otro caso — sin confirmar,
    error, omitida, o región no soportada), la dirección NO fue confirmada. Solo puede repetir
    lo que el llamante dijo.

    NUNCA use ninguna de estas frases a menos que el retorno inmediatamente anterior contenga
    `verdict=validated` o `verdict=validated_with_corrections`:
      - "validado" / "validada"
      - "verificado" / "verificada"
      - "confirmado contra Google"
      - "encontré su dirección"
      - "consulté su dirección"
      - "coincide con nuestros registros"

    Decir cualquiera de estas sin el verdict que las respalde es el peor modo de falla de esta
    sección — el llamante cuelga creyendo que su dirección fue verificada cuando no lo fue. El
    silencio o una repetición neutral siempre es aceptable; la confirmación falsa no lo es.
    ```

    --- STEP 1.2: Wire `_build_address_validation_section` into `build_system_prompt` ---

    In the section assembly order, place the new section in the TOP-ATTENTION zone — specifically AFTER `_build_corrections_section` and BEFORE `_build_tool_narration_section` (so it sits adjacent to the existing anti-hallucination CRITICAL RULE from D-15, both in the high-attention zone). This puts it at approximately position 4-5 alongside outcome_words and call_duration.

    Pass `locale` through:
    ```python
    sections = [
        # ... existing ordering preserved
        _build_corrections_section(locale),
        _build_address_validation_section(locale),  # NEW Phase 61
        _build_outcome_words_section(locale),
        _build_call_duration_section(t, locale=locale),
        _build_tool_narration_section(locale),
        # ... rest preserved
    ]
    ```
    (Verify exact existing order before inserting — DO NOT reorder existing sections.)

    --- STEP 1.3: Rewrite book_appointment tool description (D-E1) ---

    Locate the tool description string (likely in the `function_declaration` dict around lines 195-235). Replace with outcome-framed language:

    ```
    Books a confirmed appointment for the caller. Call this after check_availability has
    confirmed the slot is open. The address fields you provide will be validated against an
    external service before booking — the tool return will indicate whether the address was
    confirmed, corrected, or could not be verified, and will tell you what to speak back to the
    caller. Speak only what the return tells you. Required: slot_token (from check_availability),
    street_name, postal_code, caller_name. Optional: unit_number.
    ```

    Length budget: stay under 1024 chars (Pitfall A6). Current is ~620; new is ~570 — fits.

    --- STEP 1.4: Rewrite capture_lead tool description (D-E1, symmetric) ---

    ```
    Captures an unbooked lead when the caller wants service but no booking can be completed
    (outside service area, no slots available, etc.). The address fields you provide will be
    validated against an external service before recording — the tool return will indicate
    whether the address was confirmed, corrected, or could not be verified, and will tell you
    what to speak back to the caller. Speak only what the return tells you. Records caller name,
    phone, job type, address, and urgency.
    ```

    Length stays under 1024 chars.

    **Behavior summary:** RED phase ships 12+ failing tests across 2 new test files (8 prompt + 4 tool-description). GREEN phase ships `_build_address_validation_section(locale)` in prompt.py wired into the section assembly between corrections and outcome_words, plus rewritten outcome-framed tool descriptions on book_appointment + capture_lead — all turning the new tests GREEN while preserving every Phase 60.3 prompt test.

    Commit messages:
    - `test(61-04): RED — D-E3 prompt rule + D-E1 tool description tests`
    - `feat(61-04): GREEN — _build_address_validation_section EN+ES + tool description rewrites`
  </behavior>
  <action>
    See `<behavior>` block. RED → GREEN sequence: tests first, then prompt.py + tool-description edits to satisfy.

    Verify after GREEN that the existing Phase 60.3 tests for outcome_words / corrections / call_duration / tool_narration still pass (the new section sits between them but does not modify them).
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent &amp;&amp; pytest tests/test_prompt_address_validation_rule.py tests/test_tool_descriptions_validation_precondition.py -x &amp;&amp; pytest tests/test_prompt_*.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `livekit-agent/src/prompt.py` defines `_build_address_validation_section(locale: str = "en")`
    - `prompt.py` calls `_build_address_validation_section(locale)` from `build_system_prompt` between `_build_corrections_section` and `_build_outcome_words_section` (or another verifiable top-attention position)
    - `grep -q "ADDRESS VALIDATION — CRITICAL RULE" livekit-agent/src/prompt.py` succeeds
    - `grep -q "VALIDACIÓN DE DIRECCIÓN — REGLA CRÍTICA" livekit-agent/src/prompt.py` succeeds
    - `grep -c "verdict=validated" livekit-agent/src/prompt.py` returns >= 4 (EN + ES, both branches reference both verdict tokens)
    - `grep -c "looked up your address" livekit-agent/src/prompt.py` returns >= 1
    - `grep -c "encontré su dirección" livekit-agent/src/prompt.py` returns >= 1
    - `book_appointment.py` description string contains "validated" or "validation" AND "address"
    - `capture_lead.py` description string contains "validated" or "validation" AND "address"
    - `pytest tests/test_prompt_address_validation_rule.py -x` exits 0 (8+ tests GREEN)
    - `pytest tests/test_tool_descriptions_validation_precondition.py -x` exits 0 (4+ tests GREEN)
    - All Phase 60.3 prompt tests still GREEN: `pytest tests/test_prompt_*.py -x` exits 0 (covers corrections, outcome_words, call_duration, tool_narration, voice_behavior, info_gathering, booking, tail_sections, greeting_directive, customer_context, language, and the new address_validation_rule — robust to future renames)
    - Full suite: `pytest -x --deselect tests/webhook/test_routes.py::test_incoming_call_vip_lead` exits 0
    - Regression guard: `pytest tests/test_no_generate_reply_in_src.py -x` GREEN
    - Files in BOTH mirror and sibling, byte-identical
  </acceptance_criteria>
  <done>D-E1 + D-E3 shipped to prompt.py + tool descriptions; EN+ES locale parity preserved; full pytest suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Update three SKILL.md files + CLAUDE.md migration count</name>
  <files>
    .claude/skills/voice-call-architecture/SKILL.md,
    .claude/skills/auth-database-multitenancy/SKILL.md,
    .claude/skills/integrations-jobber-xero/SKILL.md,
    CLAUDE.md
  </files>
  <read_first>
    - .claude/skills/voice-call-architecture/SKILL.md (full — locate the "Last updated:" line at the top; locate the section structure for inserting a Phase 61 subsection in additive style)
    - .claude/skills/auth-database-multitenancy/SKILL.md (full — locate migration catalog section; identify how prior migrations are documented; add 062 entry)
    - .claude/skills/integrations-jobber-xero/SKILL.md (full — locate the Phase 56-related Python agent integration section; add D-D1 cross-link)
    - CLAUDE.md (find the "58 migrations" claim — likely in the auth-database-multitenancy table row; update to "62")
  </read_first>
  <action>
    --- STEP 2.1: Update `.claude/skills/voice-call-architecture/SKILL.md` ---

    Update the "Last updated:" line at top to:
    ```
    **Last updated**: 2026-04-25 (Phase 61 — Google Maps Address Validation API integrated as
    pre-check inside book_appointment + capture_lead; new ADDRESS VALIDATION CRITICAL RULE
    block in prompt.py top-attention zone EN+ES; D-E2 STATE+DIRECTIVE tool returns with
    verdict=validated|validated_with_corrections|unvalidated tokens; D-D3' service_address
    overwrite on confirmed/confirmed_with_changes; new src/integrations/google_maps.py
    follows xero/jobber pattern.)
    ```

    Add a new ADDITIVE subsection at the end of the document (do not modify existing content):

    ```markdown
    ## Phase 61 — Google Maps Address Validation Integration

    ### What changed
    - **New module:** `src/integrations/google_maps.py` — `validate_address_bounded()` is the
      public entry point. Per-call `httpx.AsyncClient` (matching xero.py/jobber.py pattern),
      1.5s timeout (D-C1), never raises, always returns Voco-shaped dict with `verdict` key.
    - **Pre-check in book_appointment.py:** validate runs BEFORE `atomic_book_slot` (D-B2 —
      external HTTP latency outside slot-lock contention window). On
      confirmed/confirmed_with_changes, `service_address` is overwritten with Google's
      `formatted_address` (D-D3'). Booking never blocks on Google.
    - **Pre-check in capture_lead.py:** symmetric validation pre-check (D-B4); same overwrite
      logic applied to inquiries.service_address.
    - **Tool returns (D-E2):** STATE+DIRECTIVE shape extended with verdict tokens —
      `BOOKED [verdict=validated]:`, `BOOKED [verdict=validated_with_corrections]:`,
      `BOOKED [verdict=unvalidated]:` (and LEAD CAPTURED equivalents).
    - **New CRITICAL RULE (D-E3):** `_build_address_validation_section(locale)` in prompt.py
      sits in top-attention zone alongside outcome_words / corrections / call_duration.
      Prohibits 6 verbatim phrases ("validated", "verified", "confirmed against Google",
      "found your address", "looked up your address", "matches our records") unless preceding
      tool return contained `verdict=validated` or `verdict=validated_with_corrections`.
      Spanish mirror present (D-B-03 locale-parity).
    - **Tool descriptions rewritten (D-E1):** book_appointment + capture_lead descriptions
      encode the validation precondition as outcome-framed prompt-surface language
      (Gemini 3.1 Flash Live reads tool descriptions during generation).
    - **Telemetry:** every validate writes one row to `gmaps_validate_events` (new sibling
      table — D-C2' overrides CONTEXT D-C2 because `usage_events` schema is call-billing-PK
      and cannot hold per-validate rows). Sentry only on verdict='error' (D-A3 + D-C3).

    ### Env var
    - `GOOGLE_MAPS_API_KEY` on Railway only (D-G1). Restricted to "Address Validation API"
      via Cloud Console API restrictions (NOT IP restrictions — D-G2; Railway egress IPs
      rotate). Module returns `verdict='skipped'` if env var missing — graceful degradation.

    ### Files
    | File | Role |
    |------|------|
    | `src/integrations/google_maps.py` | API client + verdict mapper + components mapper |
    | `src/lib/booking.py` | atomic_book_slot wrapper extended with 6 new kwargs |
    | `src/lib/write_outcome.py` | record_outcome wrapper extended with 6 new kwargs |
    | `src/tools/book_appointment.py` | Validation pre-check + D-E1 description + D-E2 returns |
    | `src/tools/capture_lead.py` | Symmetric pre-check + D-E1 description + D-E2 returns |
    | `src/prompt.py` | _build_address_validation_section EN+ES (D-E3) |

    ### Anti-hallucination invariants locked at test layer
    - `tests/test_prompt_address_validation_rule.py` — EN+ES presence + position + 6 prohibited phrases
    - `tests/test_tool_descriptions_validation_precondition.py` — D-E1 wording in both tool specs
    - `tests/test_book_appointment_validation.py` — D-D3' overwrite + D-E2 return shapes
    - `tests/test_capture_lead_validation.py` — D-B4 symmetry
    - `tests/test_no_generate_reply_in_src.py` — Phase 63.1 regression guard preserved
    ```

    --- STEP 2.2: Update `.claude/skills/auth-database-multitenancy/SKILL.md` ---

    Update the migration catalog section. Add an entry for migration 062. The format must match adjacent migration entries (look at the 060 / 061 entries for the exact shape).

    Suggested entry:
    ```markdown
    ### `062_phase61_address_validation.sql`
    **Phase 61 — Google Maps Address Validation + structured address storage.**

    - **appointments**: 6 new nullable columns — `formatted_address text`, `place_id text`,
      `latitude numeric(10,7)`, `longitude numeric(10,7)`, `address_components jsonb`,
      `address_validation_verdict text` with CHECK constraint enforcing 6-state enum
      (confirmed | confirmed_with_changes | unconfirmed | error | skipped | unsupported_region).
      Backward-compat columns preserved: service_address, postal_code, street_name.
    - **inquiries**: same 6 nullable columns + identical CHECK constraint (D-F1' override of
      CONTEXT D-F1 — Phase 59 dropped legacy `leads`; capture_lead now writes to inquiries via
      record_call_outcome RPC). Backward-compat: service_address preserved (postal_code and
      street_name don't exist on inquiries pre-Phase-61, so nothing to retain there).
    - **gmaps_validate_events**: new sibling table for per-validate telemetry (D-C2' override
      of CONTEXT D-C2 — usage_events schema `(call_id PK, tenant_id, created_at)` cannot hold
      per-validate rows). Columns: id uuid PK, tenant_id, call_id, verdict, latency_ms,
      cost_micro_cents, region_code, created_at. RLS enabled with SELECT-own policy
      (`tenants WHERE owner_id = auth.uid()`); INSERT only via service-role.
      Index: `(tenant_id, created_at DESC)`.
    - **book_appointment_atomic RPC**: signature extended from 11 args to 17 args (drop-loop
      pattern from 026 + CREATE OR REPLACE; new GRANT references the new 17-arg signature
      because Postgres treats different arities as different functions). New params all
      DEFAULT NULL — existing 11-arg callers unchanged.
    - **record_call_outcome RPC**: signature extended from 6 args to 12 args (same overload
      pattern). All new params DEFAULT NULL.
    - **Indexes added**: `idx_appointments_place_id WHERE place_id IS NOT NULL`,
      `idx_inquiries_place_id WHERE place_id IS NOT NULL`,
      `idx_gmaps_validate_events_tenant_created`.
    ```

    Update the migration COUNT or summary line at the top of the catalog (if there is one) from "61 migrations" → "62 migrations".

    --- STEP 2.3: Update `.claude/skills/integrations-jobber-xero/SKILL.md` ---

    Add a brief cross-link in the relevant section (likely the customer-context fetch / pre-session injection area):

    ```markdown
    ### Phase 61 cross-link — Voco-normalized address shape

    Phase 61 added a Voco-normalized `address_components` JSONB column to `appointments` and
    `inquiries` (D-D1 named-key shape):

    ```
    {
      "street_number": str | null,
      "route": str | null,
      "subpremise": str | null,
      "locality": str | null,
      "admin_area_level_1": str | null,
      "admin_area_level_2": str | null,
      "postal_code": str | null,
      "country": str | null,
      "country_code": str | null  // ISO short code from Google's postalAddress.regionCode
    }
    ```

    The Voco-normalized shape is **what Phase 62 Jobber write-side will read** when pushing a
    booked appointment into Jobber's Client.properties. Named-key access — zero translation.
    Mapper in `livekit-agent/src/integrations/google_maps.py::map_components` is the source of
    truth; mapper absorbs Google API surface changes (raw response is NOT stored).
    ```

    --- STEP 2.4: Update `CLAUDE.md` migration count ---

    Locate the "58 migrations" claim (in the auth-database-multitenancy table row, near the top of CLAUDE.md). Replace with "62" (or whatever the post-062 count is — verify with `ls supabase/migrations/*.sql | wc -l` after Plan 01 ships, which should yield 62).

    Specific text change from CLAUDE.md (per planning_context note):
    > "...all 58 DB migrations with every table definition..."
    becomes:
    > "...all 62 DB migrations with every table definition..."

    Commit message: `docs(61-04): update voice-call-architecture + auth-database-multitenancy + integrations-jobber-xero skills + CLAUDE.md migration count`
  </action>
  <verify>
    <automated>grep -q "Phase 61" .claude/skills/voice-call-architecture/SKILL.md &amp;&amp; grep -q "062_phase61_address_validation" .claude/skills/auth-database-multitenancy/SKILL.md &amp;&amp; grep -q "address_components" .claude/skills/integrations-jobber-xero/SKILL.md &amp;&amp; grep -q "62 DB migrations\|62 migrations" CLAUDE.md &amp;&amp; ! grep -q "58 DB migrations\|all 58 migrations" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `voice-call-architecture/SKILL.md` "Last updated:" line mentions Phase 61
    - `voice-call-architecture/SKILL.md` contains a new "## Phase 61 — Google Maps Address Validation Integration" subsection
    - `auth-database-multitenancy/SKILL.md` contains an entry for `062_phase61_address_validation.sql` listing the new columns on appointments + inquiries + gmaps_validate_events table + RPC overloads
    - `integrations-jobber-xero/SKILL.md` contains a Phase 61 cross-link section showing the D-D1 Voco-normalized address_components shape with the 9 named keys
    - `CLAUDE.md` no longer claims "58 DB migrations" — updated to "62" (or current count post-062)
    - All 3 SKILL.md files + CLAUDE.md committed to homeservice_agent (no sibling — these are docs)
  </acceptance_criteria>
  <done>3 skills + CLAUDE.md updated to reflect Phase 61 reality; documentation matches code state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human UAT — verify D-B3 verdict-driven readback + D-E3 anti-hallucination in production</name>
  <what-built>
    - Migration 062 applied to remote Supabase (Plan 01)
    - google_maps.py module deployed to Railway via livekit-agent (Plans 02 + 03)
    - book_appointment + capture_lead validation pre-check live (Plan 03)
    - prompt.py CRITICAL RULE deployed (Task 1)
    - SKILL.md + CLAUDE.md updated (Task 2)
    - GOOGLE_MAPS_API_KEY env var set on Railway (D-G3 user action — pre-requisite)

    The full Phase 61 user-facing behavior is now live: address validation, normalized readback, structured storage, anti-hallucination enforcement.
  </what-built>
  <how-to-verify>
    Place 4 test calls through your Voco number on Railway. Record the agent's spoken behavior on each:

    **Call 1 — confirmed (D-B3 confirmed branch):**
    1. Caller speaks a known-good full address ("Two Marina Boulevard, Singapore one-eight-nine-six-eight-nine" or "1600 Amphitheatre Parkway, Mountain View, California 94043" depending on tenant country)
    2. Agent should book and read back the normalized form with confidence (e.g., "Booked tomorrow at 3 PM at [normalized address] — anything else?")
    3. Verify in Supabase dashboard: `SELECT formatted_address, address_validation_verdict FROM appointments ORDER BY created_at DESC LIMIT 1` — should show `verdict=confirmed`, formatted_address populated

    **Call 2 — confirmed_with_changes (D-B3 confirmed_with_changes branch):**
    1. Caller speaks an address with a deliberate misspelling or missing component ("1600 Amphitheater Parkway, Mountain View" — note the misspelling) OR an SG HDB address without unit number
    2. Agent should read back the corrected form AND explicitly invite confirmation (e.g., "I have it as [normalized] — is that right?")
    3. Verify Supabase: verdict should be `confirmed_with_changes`

    **Call 3 — unconfirmed (D-B3 unconfirmed branch + D-E3 anti-hallucination):**
    1. Caller speaks gibberish or fictional address ("123 Made Up Street, Nowhere City")
    2. Agent should read back what the caller said (NOT a Google version) and MUST NOT use any of the 6 prohibited phrases ("validated", "verified", etc.)
    3. Verify Supabase: verdict=unconfirmed; formatted_address may be NULL or a best-guess; service_address should be the agent-joined caller string (NOT overwritten)

    **Call 4 — Spanish locale (D-E3 Spanish mirror):**
    1. Caller speaks Spanish from start ("Hola, necesito agendar una cita...")
    2. Agent should respond in Spanish AND the address-validation prompt rule should apply in Spanish (no `validado`/`verificado` claims unless verdict supports it)
    3. Spot-check Supabase row landed correctly

    **Sentry check (D-A3 + D-C3):**
    - After all 4 calls, verify Sentry has NO new events from `[phase61]` for verdict=unconfirmed (would be silent — only verdict=error pages)
    - Optionally: temporarily set GOOGLE_MAPS_API_KEY to garbage on Railway, place a 5th call, verify ONE Sentry event arrives with `component=google_maps_validate`, then restore the env var

    **Telemetry check:**
    - `SELECT verdict, latency_ms, region_code, created_at FROM gmaps_validate_events ORDER BY created_at DESC LIMIT 10` — should show one row per validate attempt across all 4 calls

    Report results in the resume signal: number of calls placed, verdict for each, any prohibited-phrase slips, any Sentry pages, any unexpected verdict mismatches.
  </how-to-verify>
  <files>(no source file changes — UAT-only checkpoint)</files>
  <action>Place the 4 UAT calls described in &lt;how-to-verify&gt; against the production Voco number on Railway, run the Supabase SELECT verification queries after each, and report results in the resume signal. This task is human-only — no automation possible (real audio + real Twilio + real Google Maps API).</action>
  <verify>
    <automated>echo "MANUAL UAT — see how-to-verify for 4-call protocol; verification = human transcript review + Supabase row inspection"</automated>
  </verify>
  <done>User reports "approved" after observing all 4 calls produce verdict-correct readback behavior, no prohibited-phrase regressions, gmaps_validate_events row per validate, and Sentry firing only on verdict=error.</done>
  <resume-signal>Type "approved" if all 4 calls behaved correctly, OR describe issues observed (with call IDs from the calls table) so we can iterate. If GOOGLE_MAPS_API_KEY is not yet set on Railway (D-G3 step 5 not done), say "blocked: env var pending" and complete D-G3 before re-running.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt source → Gemini 3.1 Flash Live | The new CRITICAL RULE crosses this boundary as static system_instructions; Gemini reads but cannot mutate |
| Tool description → Gemini function-declaration | Same boundary; descriptions inform Gemini's function-call decisions but cannot bypass server-side enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-20 | Tampering | Future PR removes the CRITICAL RULE without realizing the anti-hallucination dependency | mitigate | tests/test_prompt_address_validation_rule.py asserts presence + position + prohibited-phrase enumeration in EN+ES; CI fails on any drop |
| T-61-21 | Information Disclosure | The CRITICAL RULE itself contains no PII or secrets | accept | n/a — prompt strings are static documentation |
| T-61-22 | Tampering | SKILL.md drift across phases (skill claims one thing, code does another) | mitigate | The Phase 61 SKILL.md additions describe code state at commit; future Phases that touch the same surfaces must update again per CLAUDE.md skill-sync mandate |
| T-61-23 | Repudiation | UAT call results not captured in audit trail | mitigate | Resume-signal records UAT outcome verbally + Supabase row inspection; gmaps_validate_events provides per-validate audit trail; appointments.address_validation_verdict provides per-booking audit trail |
| T-61-24 | Spoofing | Anti-hallucination rule bypassed by Gemini despite prompt | accept | Phase 60.3 P09 outcome_words section is the precedent — same anti-hallucination structure; same rate of compliance expected. If prod data shows drift, escalate to a follow-up phase with reinforced rule (e.g., adding to outcome_words section's reserved-words enumeration) |
</threat_model>

<verification>
- prompt.py contains both EN and ES address validation CRITICAL RULE blocks
- 8+ TDD tests for the rule pass (EN + ES, presence + position + prohibited phrases + verdict-token)
- 4+ TDD tests for tool description precondition pass
- All Phase 60.3 prompt tests still pass (no regression)
- Three SKILL.md files + CLAUDE.md committed
- Human UAT signed off (4 calls, all verdicts behave per D-B3 branches; no prohibited-phrase regressions)
</verification>

<success_criteria>
- D-E3 CRITICAL RULE present in EN+ES, in top-attention zone
- D-E1 tool descriptions rewritten with validation precondition language
- 3 SKILL.md files reflect Phase 61 reality; CLAUDE.md migration count corrected
- UAT confirms D-B3 verdict-driven readback works in production
- UAT confirms D-E3 anti-hallucination prevents prohibited phrases on unconfirmed/error verdicts
- Sentry alerts only on verdict='error' (no false pages on unsupported_region or skipped)
- gmaps_validate_events has rows from real production calls
</success_criteria>

<output>
After completion, create `.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-04-SUMMARY.md` documenting:
- prompt.py addition (line count, position in section assembly)
- Tool description before-vs-after byte counts (length budget verification)
- 3 SKILL.md update summaries (which sections added/modified per file)
- CLAUDE.md diff (one line: migration count)
- UAT results: 4 calls placed, verdict per call, any anomalies, Supabase verification queries run
- Phase 61 closing signoff
</output>
