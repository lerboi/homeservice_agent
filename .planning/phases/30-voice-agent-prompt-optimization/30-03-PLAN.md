---
phase: 30-voice-agent-prompt-optimization
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - .claude/skills/voice-call-architecture/skill.md
autonomous: false
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06
must_haves:
  truths:
    - "Voice call architecture skill file documents all Phase 30 changes"
    - "User has verified prompt quality via code review"
  artifacts:
    - path: ".claude/skills/voice-call-architecture/skill.md"
      provides: "Updated architectural reference with Phase 30 additions"
      contains: "check_caller_history"
  key_links:
    - from: ".claude/skills/voice-call-architecture/skill.md"
      to: "C:/Users/Leroy/Desktop/Voco/Retell-ws-server/agent-prompt.js"
      via: "Documents prompt sections"
      pattern: "SLOT PREFERENCE DETECTION"
---

<objective>
Update the voice-call-architecture skill file to reflect all Phase 30 changes (new tool, new prompt sections, new dynamic variables, new migration), then have the user verify the prompt restructure quality.

Purpose: Keep the architectural reference in sync per CLAUDE.md rules, and verify prompt quality (D-04 cleanup is subjective).

Output: Updated skill.md, user sign-off on prompt quality.
</objective>

<execution_context>
@C:/Users/Leroy/Desktop/Voco/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Leroy/Desktop/Voco/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-voice-agent-prompt-optimization/30-CONTEXT.md
@.planning/phases/30-voice-agent-prompt-optimization/30-01-SUMMARY.md
@.planning/phases/30-voice-agent-prompt-optimization/30-02-SUMMARY.md
@.claude/skills/voice-call-architecture/skill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update voice-call-architecture skill file</name>
  <files>.claude/skills/voice-call-architecture/skill.md</files>
  <read_first>
    - .claude/skills/voice-call-architecture/skill.md
    - C:/Users/Leroy/Desktop/Voco/Retell-ws-server/agent-prompt.js (current state after Plan 02)
    - C:/Users/Leroy/Desktop/Voco/Retell-ws-server/server.js (current state after Plan 02)
    - src/app/api/webhooks/retell/route.js (current state after Plan 01)
    - src/lib/trade-templates.js (current state after Plan 01)
    - .planning/phases/30-voice-agent-prompt-optimization/30-01-SUMMARY.md
    - .planning/phases/30-voice-agent-prompt-optimization/30-02-SUMMARY.md
  </read_first>
  <action>
Update `.claude/skills/voice-call-architecture/skill.md` with the following additions. Read the current file first to find exact insertion points.

**1. Update "Last updated" line at top:**
Change to: `**Last updated**: 2026-03-27 (Phase 30: Voice Agent Prompt Optimization — smart slot preference detection, repeat caller awareness via check_caller_history tool, failed transfer callback booking, prompt cleanup, trade-specific intake questions, post-booking recap flow)`

**2. Architecture Overview diagram — update tool count:**
Change `↓  During call: AI uses 4 tools` to `↓  During call: AI uses 5 tools`
Add line: `↓    check_caller_history → webhook handler → read-only leads + appointments lookup`

**3. Tool Definitions section — add check_caller_history:**
Add after the `capture_lead` tool entry:

```
**`check_caller_history`** — Always available (NOT gated by onboarding_complete). No parameters — uses caller_number from the call record. Returns natural-language summary of caller's prior leads and upcoming appointments. AI invokes this after greeting, before first question. Read-only — no DB writes.
- Returns: "First-time caller. No prior history found." or "Returning caller. [appointments/leads summary]"
- Purpose: Enable repeat caller recognition (D-02) and prevent duplicate bookings
```

Update tool ordering line to: `**Tool ordering**: `transfer_call`, `capture_lead`, `check_caller_history`, `end_call`, then conditionally `check_availability` and `book_appointment`.`

**4. Agent System Prompt section — add new sections:**
After the existing `### Closing the Call` subsection, document the new sections:

```
### Repeat Caller Awareness (Phase 30, D-02)
- AI invokes `check_caller_history` after greeting, before first question
- First-time callers: proceed normally, do not mention
- Returning callers with appointment: "Welcome back! I see you have an appointment [date/time]..."
- Returning callers with leads only: "Welcome back, I have your information on file..."
- Both appointment + lead: mention appointment first, ask if new issue
- Uses gathered info to skip re-asking name/address

### Slot Preference Detection (Phase 30, D-01)
- AI detects time cues from natural conversation: morning/AM, afternoon, evening/after work, weekend, specific days
- Prioritizes matching slots from check_availability results
- No proactive "When do you prefer?" question — detect from language only
- If no matching slots: acknowledge preference and offer alternatives

### Trade-Specific Intake Questions (Phase 30, D-05)
- Injected via `intake_questions` dynamic variable (newline-separated string)
- Source: `TRADE_TEMPLATES.intakeQuestions` → `services.intake_questions` jsonb → `handleInbound` dynamic variable
- Asked naturally during conversation, not as checklist
- Skipped if caller already answered
- Examples: Plumber "Is the water still running?", HVAC "Heating or cooling?", Electrician "Any burning smells?"
```

**5. Booking Section — document enhanced flows:**
Add to the BOOKING-FIRST PROTOCOL documentation:

```
### Post-Booking Recap (Phase 30, D-06)
- After booking: recap date, time, AND address: "{business_name} will see you then. Is there anything else?"
- If yes: answer, then wrap up. If no: warm farewell + end_call.

### Post-Decline Flow (Phase 30, D-06)
- After second decline + capture_lead: "I've saved your information. Is there anything else you'd like to ask before I let you go?"
- If yes: answer, then end_call. If no: farewell + end_call.
```

**6. Transfer Section — document recovery flow:**
Add to CALL TRANSFER documentation:

```
### Transfer Recovery (Phase 30, D-03)
- When transfer_call returns "transfer_failed": offer callback booking
- Flow: "They're not available" → offer callback → check_availability → book_appointment (with callback note)
- If caller declines callback: capture_lead with callback-declined note
- When transfer returns "transfer_unavailable": capture_lead with callback request
```

**7. Prompt Structure Notes (D-04):**
Add to Key Design Decisions:

```
- **Prompt cleanup (Phase 30 D-04)**: Removed standalone RECORDING_NOTICE (redundant with OPENING LINE) and LANGUAGE_BARRIER_ESCALATION (redundant with LANGUAGE section). Replaced rigid "1-2 sentences" rule with nuanced conciseness: brief but never truncates booking confirmations, address recaps, or important details. Sections array uses `.filter(Boolean)` for conditional sections.
```

**8. File Map — add new migration:**
Add to the File Map table:
```
| `supabase/migrations/018_intake_questions.sql` | intake_questions jsonb column on services (Phase 30) |
```

**9. Database Tables — update services entry:**
Update the `services` row to: `Service catalog with urgency_tag for Layer 3 triage and intake_questions jsonb for trade-specific AI questioning`

**10. Event: call_inbound section — document new dynamic variables:**
Add to the dynamic_variables list: `trade_type`, `intake_questions` (newline-separated string from active services)

**11. Event: call_function_invoked — add check_caller_history handler:**
Add documentation block:

```
**`check_caller_history`**:
1. Resolve tenant and from_number from call record: `calls.select('tenant_id, from_number').eq('retell_call_id', call_id)`
2. Look up tenant timezone
3. Parallel query: `leads` (by tenant_id + from_number, most recent 3) + `appointments` (by tenant_id + caller_phone, upcoming non-cancelled 3)
4. Build natural-language summary with formatted dates via `formatSlotForSpeech()`
5. Return: "First-time caller" or "Returning caller" with appointment/lead details
6. Read-only — no database writes (D-02)
```
  </action>
  <verify>
    <automated>grep -q "check_caller_history" .claude/skills/voice-call-architecture/skill.md && grep -q "SLOT PREFERENCE DETECTION" .claude/skills/voice-call-architecture/skill.md && grep -q "TRANSFER RECOVERY" .claude/skills/voice-call-architecture/skill.md && grep -q "intake_questions" .claude/skills/voice-call-architecture/skill.md && grep -q "018_intake_questions" .claude/skills/voice-call-architecture/skill.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skill.md "Last updated" line contains "Phase 30"
    - skill.md contains `check_caller_history` in Tool Definitions section
    - skill.md contains documentation for all 6 decisions (D-01 through D-06)
    - skill.md contains `018_intake_questions.sql` in File Map
    - skill.md contains `trade_type` and `intake_questions` in call_inbound dynamic_variables documentation
    - skill.md contains `SLOT PREFERENCE DETECTION` section
    - skill.md contains `TRANSFER RECOVERY` section
    - skill.md contains `Trade-Specific Intake Questions` section
    - skill.md tool count updated to 5 in architecture diagram
  </acceptance_criteria>
  <done>Voice call architecture skill file fully updated to reflect all Phase 30 changes — new tool, new prompt sections, new dynamic variables, new migration, new design decisions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prompt quality and code review</name>
  <what-built>
Complete voice agent prompt optimization implementing all 6 decisions:
- D-01: Smart slot preference detection (time cues from natural language)
- D-02: Repeat caller awareness via check_caller_history tool
- D-03: Callback booking on failed transfer
- D-04: Prompt cleanup — removed redundancies, nuanced conciseness rule
- D-05: Trade-specific intake questions injected via dynamic variables
- D-06: Post-booking recap with address + post-decline farewell flow

Files changed across 2 repos:
- homeservice_agent: webhook route, trade templates, onboarding, migration, skill file
- Retell-ws-server: agent-prompt.js (prompt restructure), server.js (new tool + intake vars)
  </what-built>
  <how-to-verify>
1. Review the restructured prompt:
   - Open `C:/Users/Leroy/Desktop/Voco/Retell-ws-server/agent-prompt.js`
   - Verify no contradictions between sections
   - Verify RECORDING_NOTICE and LANGUAGE_BARRIER_ESCALATION standalone sections are gone
   - Verify the conciseness rule says "never truncate" not "1-2 sentences"

2. Review the check_caller_history tool chain:
   - `C:/Users/Leroy/Desktop/Voco/Retell-ws-server/server.js` — tool defined with no params
   - `src/app/api/webhooks/retell/route.js` — handler returns read-only caller history

3. Review trade template intake questions:
   - `src/lib/trade-templates.js` — intakeQuestions per trade
   - `src/app/api/onboarding/start/route.js` — populates intake_questions on services

4. Review migration:
   - `supabase/migrations/018_intake_questions.sql` — adds jsonb column

5. Deploy to test (when ready):
   - Deploy webhook changes to Vercel
   - Deploy WS server changes to Railway
   - Make a test call to verify end-to-end
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 30, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Skill file updated with all Phase 30 changes
- User has reviewed prompt for contradictions and quality
- All 6 decisions traceable in the implementation
</verification>

<success_criteria>
- Voice call architecture skill file is in sync with code changes
- User confirms prompt quality is acceptable
- Phase 30 ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/30-voice-agent-prompt-optimization/30-03-SUMMARY.md`
</output>
