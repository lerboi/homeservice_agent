---
phase: 10
slug: dashboard-guided-setup-and-first-run-experience
status: draft
shadcn_initialized: true
preset: new-york / neutral / cssVariables
created: 2026-03-23
revised: 2026-03-23
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (new-york style) |
| Preset | new-york, baseColor: neutral, cssVariables: true |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Inter (var(--font-inter), system-ui fallback) |

Source: `components.json`, `src/app/globals.css`

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge padding |
| sm | 8px | Compact element spacing, icon-to-label gap in checklist items |
| md | 16px | Default element spacing, card internal padding base |
| lg | 24px | Section padding (dashboard page `p-6` = 24px), checklist row padding |
| xl | 32px | Layout gaps between checklist and stats grid |
| 2xl | 48px | Major section breaks, settings page between section cards |
| 3xl | 64px | Page-level vertical spacing |

Exceptions:
- Nav link touch targets: `py-2 min-h-[44px] flex items-center` — 8px vertical padding (grid-aligned), 44px minimum height enforced via `min-h-[44px]`
- Checklist progress bar: 8px height (not on spacing scale, purely visual)
- Activity feed icon bubble: 32px (h-8 w-8) — matches existing RecentActivityFeed pattern

Source: existing dashboard layout (`p-6`, `space-y-8`, `gap-4`) — no changes to scale

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 (leading-snug for dense feed items) |
| Label | 14px (text-sm) | 600 (semibold), uppercase tracking-wider | 1.4 |
| Heading | 20px (text-xl) | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.15 (tabular-nums for stat widgets) |

Notes:
- Body at 14px weight 400 is the established dashboard pattern (activity feed descriptions, checklist item text)
- Label at 14px uppercase tracking-wider is the established stat widget label pattern (see `DashboardHomeStats`). Using 14px (not 12px) eliminates the fragile 2px size dependency — uppercase + tracking-wider provides sufficient visual distinction from body text without relying on a size difference.
- Heading at 20px semibold matches `text-xl font-semibold` used on "Recent Activity" and settings page h1
- Display at 28px semibold matches animated stat counter sizing (`text-[28px] font-semibold`)
- Maximum 2 weights in use: 400 (regular) and 600 (semibold)

Source: `src/components/dashboard/DashboardHomeStats.jsx`, `src/components/dashboard/RecentActivityFeed.jsx`

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F5F5F4` (warm-surface) | Dashboard page background (`bg-[#F5F5F4]`), content area fill |
| Secondary (30%) | `#FFFFFF` (white) | Cards, sidebar overlay, stat widgets, checklist banner card, settings section cards |
| Accent (10%) | `#C2410C` (brand-orange / Heritage Copper) | See reserved list below |
| Dark text | `#0F172A` | Headings, primary text, sidebar background |
| Muted text | `#475569` | Secondary text, labels, timestamps, empty state body copy |
| Sidebar | `#0F172A` | Sidebar background (established, do not alter) |
| Destructive | `oklch(0.577 0.245 27.325)` ≈ `#DC2626` | Destructive actions only (none in this phase) |

Accent (`#C2410C`) reserved for:
- Active sidebar nav left border indicator
- Checklist progress bar fill
- "Setup complete!" celebration bar background tint (10% opacity)
- Checklist item checkmark icon color (completed items)
- Brand logo gradient (existing — do not alter)
- Activity feed `lead_created` icon color (existing — do not alter)

Semantic supplementary colors (existing, re-used):
- Green `#166534` / `bg-green-50`: booking_created activity icon and checklist "all complete" success indicator
- Blue `text-blue-600` / `bg-blue-50`: status_changed activity icon (existing)
- Amber `text-amber-600` / `bg-amber-50`: notification_sent activity icon (existing)

Source: `src/app/globals.css` `:root` block, `src/components/dashboard/DashboardSidebar.jsx`, `src/components/dashboard/RecentActivityFeed.jsx`

---

## Component Inventory

### New Components (Phase 10)

| Component | Type | shadcn Base | Description |
|-----------|------|-------------|-------------|
| `SetupChecklist` | client | Card, Progress, Button | Full-width banner card with 6-item checklist, progress bar, and collapse behavior |
| `ChecklistItem` | client | none (custom) | Single checklist row: checkbox icon + label + arrow link (uncompleted) |
| `SetupCompleteBar` | client | none (custom) | Collapsed celebration bar shown when all 6 items complete; X to dismiss |
| `WelcomeBanner` | client | Card | Below-checklist welcome message shown only when stats are all-zero and no activity |
| `EmptyStateLeads` | server | none (custom) | Leads page first-run empty state: icon + description + "Make a Test Call" CTA |
| `EmptyStateCalendar` | server | none (custom) | Calendar page first-run empty state: icon + description + "Connect Calendar" CTA |
| `EmptyStateAnalytics` | server | none (custom) | Analytics page first-run empty state: icon + description + "Make a Test Call" CTA |
| `EmptyStateActivity` | client | none (custom) | Activity feed first-run empty state (upgrades existing "No recent activity" text) |
| `SettingsAISection` | client | Card | AI Receptionist settings card: phone number display + adapted TestCallPanel |
| `SettingsHoursSection` | client | Card | Working Hours settings card: wraps existing WorkingHoursEditor |
| `SettingsCalendarSection` | client | Card | Calendar Connections settings card: wraps existing CalendarSyncCard |

### Reused Components (no changes to API)

| Component | Source | Usage in Phase 10 |
|-----------|--------|-------------------|
| `TestCallPanel` | `src/components/onboarding/TestCallPanel.js` | Adapted for SettingsAISection — remove onboarding copy, keep state machine |
| `WorkingHoursEditor` | `src/components/dashboard/WorkingHoursEditor.js` | Embedded in SettingsHoursSection card |
| `CalendarSyncCard` | `src/components/dashboard/CalendarSyncCard.js` | Embedded in SettingsCalendarSection card |
| `AnimatedSection` | `src/app/components/landing/AnimatedSection.jsx` | Wrap SetupChecklist on dashboard home for entrance animation |
| `DashboardHomeStats` | `src/components/dashboard/DashboardHomeStats.jsx` | Existing — checklist inserts above it |
| `RecentActivityFeed` | `src/components/dashboard/RecentActivityFeed.jsx` | Upgrade empty state inline |
| `Progress` | shadcn | SetupChecklist progress bar |
| `Button` | shadcn | Empty state CTAs, settings action buttons |
| `Card` | shadcn | SetupChecklist wrapper, settings section wrappers |
| `Separator` | shadcn | Between settings sections |
| `Skeleton` | shadcn | Loading state for checklist while DB state is fetched |

---

## Layout Specifications

### SetupChecklist Banner (Dashboard Home)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [✓] Your setup checklist                              3 of 6 complete      │
│  ████████████░░░░░░░░░░░░░░  (progress bar, accent fill)                   │
│                                                                             │
│  ✓ Create account                            (muted, pre-checked)          │
│  ✓ Set up business profile                   (muted, pre-checked)          │
│  ✓ Configure services                        (muted, pre-checked)          │
│  ○ Connect Google Calendar          [Go to Settings →]                     │
│  ○ Configure working hours          [Go to Settings →]                     │
│  ○ Make a test call                 [Go to Settings →]                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

- Full-width card, `rounded-2xl`, border `border-stone-200/60`
- Padding: `p-6` (24px all sides)
- Progress bar: `h-2`, `rounded-full`, accent fill `bg-[#C2410C]`, track `bg-stone-200`
- Checked items: `text-[#475569]` with `CheckCircle2` lucide icon in accent color
- Unchecked items: `text-[#0F172A]` with `Circle` lucide icon in `text-stone-300`
- Arrow link: `text-sm text-[#C2410C] hover:underline` with `ArrowRight` icon
- Inserted above `DashboardHomeStats` inside the existing `div className="p-6 space-y-8"`
- Separated from stats by `space-y-8` gap (existing spacing)

### SetupCompleteBar (collapsed state)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ✓ Setup complete! Your AI receptionist is ready.                      [X] │
└─────────────────────────────────────────────────────────────────────────────┘
```

- Height: `py-3 px-6`
- Background: `bg-[#C2410C]/[0.06]` with `border border-[#C2410C]/20`
- Icon: `CheckCircle2` in `#C2410C`
- Text: 14px, weight 400, `text-[#0F172A]`
- X button: `text-[#475569] hover:text-[#0F172A]`, `aria-label="Dismiss setup checklist"`

### WelcomeBanner

- Shows only when: all stats are 0 AND activities array is empty AND checklist not all-complete
- Positioned: below SetupChecklist, above stats grid
- Style: no border, `bg-transparent`, centered text
- Copy: heading 20px semibold, body 14px muted

### Settings Page Layout (rebuilt from stub)

```
/dashboard/settings
├── Page heading: "Settings" (h1, text-xl, semibold)
├── Section 1: Your AI Receptionist (Card)
│   ├── Card header: title + phone number badge
│   └── Adapted TestCallPanel
├── Separator (Radix, bg-stone-100)
├── Section 2: Working Hours (Card)
│   └── WorkingHoursEditor
├── Separator
└── Section 3: Calendar Connections (Card)
    └── CalendarSyncCard
```

- Page wrapper: `p-6 space-y-6`
- Each section Card: `rounded-2xl border-stone-200/60 p-6`
- Between cards: `space-y-6` (24px) within the existing `space-y-8` rhythm

### Empty State Pattern (all pages)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│              [Lucide icon, h-10 w-10, text-stone-300]                      │
│                                                                             │
│              No leads yet                                                  │
│              When callers reach your AI, leads appear                      │
│              here with full call details.                                  │
│                                                                             │
│                        [ Make a Test Call ]                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

- Centered, `py-16` top/bottom padding
- Icon: `h-10 w-10 text-stone-300` (muted, not distracting)
- Heading: 16px semibold `text-[#0F172A]` — states the absence
- Body: 14px `text-[#475569]` — explains what WILL appear and how
- CTA Button: shadcn `Button` variant `default` (Primary — dark background per shadcn new-york)
- CTA links to the relevant action (not a dead-end)
- First-run state only — "No results" for filter-zero uses `<p className="text-sm text-[#475569]">No results match your filters.</p>` inline, no icon/CTA

---

## Interaction States

### Checklist Item States

| State | Visual |
|-------|--------|
| Pre-checked (locked) | `CheckCircle2` icon accent color, text `text-[#475569]`, no link |
| Unchecked (actionable) | `Circle` icon `text-stone-300`, text `text-[#0F172A]`, arrow link visible |
| Newly checked (just completed) | Brief scale animation (200ms ease-out) on checkbox icon, `prefers-reduced-motion` skips |

### Test Call Panel in Settings

- State machine: `ready → calling → in_progress → complete` (reuse existing)
- In `ready` state: shows AI phone number as `text-[#0F172A] font-mono text-sm` in a `bg-stone-50 rounded-lg px-4 py-2` pill
- In `calling/in_progress` state: Loader2 spinner + elapsed time counter
- In `complete` state: inline success — no CelebrationOverlay (that is onboarding-specific), instead show `CheckCircle2` icon with "Test call complete. Your AI is working!" in green `text-[#166534]`
- In `timeout` state: Alert component with copy per copywriting contract
- "Test My AI" button: shadcn `Button` variant `default`, full-width within the card section

### Progress Bar Update

- Progress bar width transitions with `transition-all duration-300 ease-out`
- Respects `prefers-reduced-motion` — no transition if reduced motion active

### Dismiss Animation (SetupCompleteBar)

- On X click: `opacity-0 scale-y-0 origin-top` over 200ms
- Respects `prefers-reduced-motion` — instant removal if reduced motion active

---

## Copywriting Contract

### Setup Checklist

| Element | Copy |
|---------|------|
| Checklist heading | "Your setup checklist" |
| Progress sub-label | "{N} of 6 complete" |
| Pre-checked item 1 | "Create account" |
| Pre-checked item 2 | "Set up business profile" |
| Pre-checked item 3 | "Configure services" |
| Unchecked item 4 | "Connect Google Calendar" |
| Unchecked item 5 | "Configure working hours" |
| Unchecked item 6 | "Make a test call" |
| Arrow link label | "Go to Settings" (aria-label: "Go to Settings — {item label}") |
| All-complete bar | "Setup complete! Your AI receptionist is ready." |
| Dismiss button aria | "Dismiss setup checklist" |

### Welcome Banner

| Element | Copy |
|---------|------|
| Heading | "Welcome to your dashboard" |
| Body | "Complete your setup to start receiving calls from real customers." |

### Empty States

| Page | Icon | Heading | Body | CTA Label |
|------|------|---------|------|-----------|
| Leads | `Users` | "No leads yet" | "When callers reach your AI, leads appear here with caller details, job type, and urgency." | "Make a Test Call" |
| Calendar | `Calendar` | "No appointments yet" | "When your AI books jobs, confirmed appointments appear here with job details and time slots." | "Connect Calendar" |
| Analytics | `BarChart3` | "No data yet" | "Analytics populate automatically as calls come in. Make a test call to see your first data point." | "Make a Test Call" |
| Activity (feed) | `Activity` | "No recent activity" | "Your AI's actions — new leads, bookings, and notifications — appear here as calls come in." | none (informational only) |

### Settings Page — AI Receptionist Section

| Element | Copy |
|---------|------|
| Section heading | "Your AI Receptionist" |
| Section description | "Your AI phone number is active. Test your receptionist anytime." |
| Phone number label | "AI Phone Number" |
| Test call button | "Test My AI" |
| Calling state label | "Calling your AI..." |
| In progress label | "Call in progress ({N}s)" |
| Success message | "Test call complete. Your AI is working!" |
| Timeout message | "The test call timed out. Check that your AI phone number is active and try again." |
| Error message | "Could not start the test call. Try again or contact support." |

### Filter-Zero States (not first-run)

| Page | Copy |
|------|------|
| Leads | "No leads match your filters." |
| Calendar | "No appointments in this range." |
| Analytics | "No data for the selected period." |

### Primary CTA for this phase

"Complete Setup" — the checklist progress arrow links use "Go to Settings" as the micro-CTA; the settings page test call uses "Test My AI" as the primary action CTA.

---

## Animation Contract

All animations must respect `prefers-reduced-motion: reduce`.

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| SetupChecklist entrance | AnimatedSection `direction="up"` fade+translate | 300ms | ease-out |
| Progress bar width change | `transition-all` | 300ms | ease-out |
| Newly-checked item | `scale(1) → scale(1.15) → scale(1)` on icon | 200ms | ease-out |
| SetupCompleteBar dismiss | `opacity-0 scale-y-0 origin-top` | 200ms | ease-out |
| Settings section entrance | AnimatedSection `direction="up"` | 300ms | ease-out |
| Empty state entrance | AnimatedSection `direction="up"` | 300ms | ease-out |

Source: existing `AnimatedSection` pattern, `globals.css` `draw-in` and `radial-pulse` precedents

---

## Accessibility Contract

- All checklist item arrow links: `aria-label="Go to Settings — {item label}"` (not just "Go to Settings")
- Checklist progress bar: `role="progressbar" aria-valuenow={N} aria-valuemin={0} aria-valuemax={6} aria-label="Setup progress"`
- Dismiss button: `aria-label="Dismiss setup checklist"`
- Empty state CTA buttons: descriptive aria-labels matching label text (e.g., `aria-label="Make a Test Call"`)
- "Test My AI" polling states: `aria-live="polite"` on the status message container
- Phone number in settings: `aria-label="Your AI phone number"` on the pill container
- All Lucide icons in informational positions: `aria-hidden="true"` with accompanying visible text
- Focus ring: inherited from shadcn `outline-ring/50` base layer — no overrides needed
- Touch targets: all clickable elements minimum 44px effective height via `min-h-[44px]` on nav items and `py-3` (12px) on checklist rows
- Color contrast: `#475569` on `#FFFFFF` = 5.07:1 (passes AA); `#0F172A` on `#FFFFFF` = 17.8:1; `#C2410C` on `#FFFFFF` = 4.6:1 (passes AA for large text and UI components)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (ui.shadcn.com) | Button, Card, Progress, Separator, Skeleton, Badge, Alert, Switch, Tooltip — all already installed | not required |
| third-party | none | not applicable |

No new shadcn components need to be added. All components required for Phase 10 are already installed in `src/components/ui/`.

---

## Implementation Notes for Executor

1. **Checklist persistence**: Store in Supabase `tenants` table as a `jsonb` column `setup_checklist_dismissed` (boolean) and derive item completion from existing DB columns:
   - "Create account" → `id` exists (always true post-auth)
   - "Set up business profile" → `business_name IS NOT NULL`
   - "Configure services" → count of services > 0
   - "Connect Google Calendar" → `calendar_connections` row exists with `is_active = true`
   - "Configure working hours" → `working_hours IS NOT NULL`
   - "Make a test call" → `onboarding_complete = true` (set by webhook on call completion)

2. **First-run vs filter-zero distinction**: Use a `hasEverHadData` boolean derived from whether the DB query returns 0 rows with no active filters applied. If filters are empty and count = 0 → first-run empty state. If filters are active → simple "no results" text.

3. **TestCallPanel adaptation**: Accept a `context` prop (`'onboarding' | 'settings'`). When `context='settings'`: hide CelebrationOverlay, replace "Go to Dashboard" button with inline success message, remove onboarding-specific heading copy.

4. **Settings page**: Convert from server component stub to `'use client'` component (needed for TestCallPanel polling state). WorkingHoursEditor and CalendarSyncCard already have their own client boundaries.

5. **Checklist position on dashboard home**: Insert `<SetupChecklist />` as first child inside `<div className="p-6 space-y-8">`, above the stats `<section>`. The `space-y-8` gap handles separation automatically.

---

## Revision Log

| Date | Changes |
|------|---------|
| 2026-03-23 | Initial draft |
| 2026-03-23 | Fix: replaced `py-2.5` (10px, not on 4-point grid) with `py-2 min-h-[44px] flex items-center` on nav touch targets. Removed 10px from all declarations. |
| 2026-03-23 | Fix: consolidated Label size from 12px to 14px — eliminates fragile 2px separation from Body; uppercase + tracking-wider treatment provides sufficient visual distinction. |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
