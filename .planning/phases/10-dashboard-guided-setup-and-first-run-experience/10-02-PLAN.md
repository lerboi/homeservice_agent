---
phase: 10-dashboard-guided-setup-and-first-run-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/onboarding/TestCallPanel.js
  - src/components/dashboard/SettingsAISection.jsx
  - src/components/dashboard/SettingsHoursSection.jsx
  - src/components/dashboard/SettingsCalendarSection.jsx
  - src/app/dashboard/settings/page.js
autonomous: true
requirements:
  - SETUP-03

must_haves:
  truths:
    - "Owner can trigger a test voice call from dashboard settings and hear their AI answer"
    - "Settings page shows 3 sections: AI Receptionist, Working Hours, Calendar Connections"
    - "TestCallPanel in settings context shows inline success message, not CelebrationOverlay"
    - "AI phone number is displayed in a styled pill on the settings page"
    - "WorkingHoursEditor and CalendarSyncCard render correctly in settings cards"
  artifacts:
    - path: "src/components/onboarding/TestCallPanel.js"
      provides: "context prop adaptation for settings vs onboarding behavior"
      contains: "context"
    - path: "src/app/dashboard/settings/page.js"
      provides: "Full settings page with 3 stacked card sections"
      min_lines: 40
    - path: "src/components/dashboard/SettingsAISection.jsx"
      provides: "AI Receptionist card with phone number display and adapted TestCallPanel"
      min_lines: 30
  key_links:
    - from: "src/components/dashboard/SettingsAISection.jsx"
      to: "src/components/onboarding/TestCallPanel.js"
      via: "import and render with context='settings'"
      pattern: "context.*settings"
    - from: "src/app/dashboard/settings/page.js"
      to: "src/components/dashboard/SettingsAISection.jsx"
      via: "import and render as first section"
      pattern: "import.*SettingsAISection"
---

<objective>
Rebuild the settings page from its current stub into a full 3-section settings hub, and adapt TestCallPanel to work in the settings context without onboarding-specific UI.

Purpose: Settings becomes the permanent home for AI testing, working hours configuration, and calendar connections — linked from the setup checklist.
Output: Working settings page with AI phone number display, test call capability, working hours editor, and calendar sync card.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-CONTEXT.md
@.planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-RESEARCH.md
@.planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-UI-SPEC.md

<interfaces>
<!-- Existing component APIs the executor must respect -->

From src/components/onboarding/TestCallPanel.js (CURRENT API — will be modified):
```javascript
export function TestCallPanel({ phoneNumber, onComplete, onGoToDashboard })
// States: ready, calling, in_progress, complete, timeout
// Uses: CelebrationOverlay (onboarding only), AnimatedSection
// Polls: /api/onboarding/test-call-status every 4s
// Triggers: POST /api/onboarding/test-call
```

From src/components/dashboard/WorkingHoursEditor.js (self-contained, no props needed):
```javascript
// Fetches own data from /api/working-hours
// Saves own changes internally
// Uses sonner for toast notifications
// 'use client' component
```

From src/components/dashboard/CalendarSyncCard.js (self-contained, no props needed):
```javascript
// Fetches own data from /api/calendar-sync/status
// Handles connect/disconnect internally
// 'use client' component
```

From src/lib/supabase.js:
```javascript
export const supabase = createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY);
```

shadcn components: Card, CardHeader, CardTitle, CardDescription, CardContent, Button, Separator, Skeleton, Alert, AlertDescription
Lucide icons: Phone, CheckCircle2, Loader2, Clock, Calendar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt TestCallPanel with context prop</name>
  <files>src/components/onboarding/TestCallPanel.js</files>
  <read_first>
    - src/components/onboarding/TestCallPanel.js (full current implementation — ALL states)
    - src/components/onboarding/CelebrationOverlay.jsx (understand what gets suppressed)
    - .planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-UI-SPEC.md (settings-context copy and behavior)
    - .planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-RESEARCH.md (Pattern 3: TestCallPanel Context Prop Adaptation)
  </read_first>
  <action>
Modify `TestCallPanel` to accept a new `context` prop (default `'onboarding'`):

**Signature change:**
```javascript
export function TestCallPanel({ phoneNumber, onComplete, onGoToDashboard, context = 'onboarding' })
```

**Changes to `ready` state (callState === 'ready'):**
When `context === 'settings'`:
- Remove the h1 heading ("Meet your AI receptionist") and subtitle
- Show only the button: `<Button onClick={handleCallMyAI} className="w-full min-h-[44px] bg-[#C2410C] hover:bg-[#C2410C]/90 active:bg-[#9A3412] text-white shadow-[0_1px_2px_0_rgba(0,0,0,0.3),inset_0_1px_0_0_rgba(255,255,255,0.1)] transition-all duration-150">Test My AI</Button>`
- Keep error Alert display below button
- Preserve existing `context === 'onboarding'` behavior (current code) as-is

**Changes to `calling` state:**
When `context === 'settings'`:
- Remove h1 heading, keep spinner + "Calling your AI..." text
- Use `text-sm` instead of `text-base` for label
- Add `aria-live="polite"` on the status container div

**Changes to `in_progress` state:**
When `context === 'settings'`:
- Remove h1 heading, keep phone icon + "Call in progress ({N}s)" text
- Use `text-sm` for labels
- Add `aria-live="polite"` on the status container div

**Changes to `complete` state:**
When `context === 'settings'`:
- Do NOT render `CelebrationOverlay`
- Do NOT render `AnimatedSection` wrapper
- Do NOT render "Go to Dashboard" button
- Instead render:
```jsx
<div className="flex items-center gap-2 text-[#166534]" aria-live="polite">
  <CheckCircle2 className="h-5 w-5" aria-hidden="true" />
  <span className="text-sm font-medium">Test call complete. Your AI is working!</span>
</div>
```
- Still call `onComplete()` callback if provided

**Changes to `timeout` state:**
When `context === 'settings'`:
- Remove h1 heading
- Show Alert with copy: "The test call timed out. Check that your AI phone number is active and try again."
- Show "Try Again" button only (no "Go to Dashboard" button)

**No changes to polling logic, elapsed timer, or handleCallMyAI function** — all that stays identical.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/onboarding/TestCallPanel.js contains `context = 'onboarding'`
    - src/components/onboarding/TestCallPanel.js contains `context === 'settings'`
    - src/components/onboarding/TestCallPanel.js contains `Test call complete. Your AI is working!`
    - src/components/onboarding/TestCallPanel.js contains `Test My AI`
    - src/components/onboarding/TestCallPanel.js contains `aria-live="polite"`
    - src/components/onboarding/TestCallPanel.js contains `Calling your AI`
    - src/components/onboarding/TestCallPanel.js contains `timed out`
    - Existing onboarding behavior preserved (CelebrationOverlay still rendered when context !== 'settings')
  </acceptance_criteria>
  <done>TestCallPanel renders compact inline UI when context='settings' and full onboarding UI when context='onboarding'; no regression in onboarding flow</done>
</task>

<task type="auto">
  <name>Task 2: Settings page rebuild with 3 section cards</name>
  <files>src/components/dashboard/SettingsAISection.jsx, src/components/dashboard/SettingsHoursSection.jsx, src/components/dashboard/SettingsCalendarSection.jsx, src/app/dashboard/settings/page.js</files>
  <read_first>
    - src/app/dashboard/settings/page.js (current stub to replace)
    - src/components/dashboard/WorkingHoursEditor.js (confirm self-contained, no props)
    - src/components/dashboard/CalendarSyncCard.js (confirm self-contained, no props)
    - src/components/onboarding/TestCallPanel.js (after Task 1 modifications — context prop)
    - src/lib/supabase-browser.js (client-side supabase for tenant data fetch)
    - .planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-UI-SPEC.md (Settings Page Layout section, AI Receptionist Section copy)
    - .planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-RESEARCH.md (Pattern 5: Settings Page Structure, Pitfall 2: phone number null handling)
  </read_first>
  <action>
**SettingsAISection.jsx** — `'use client'` component:
- Props: `{ phoneNumber, loading }` — phone number passed from parent
- Structure: shadcn `Card` with `className="rounded-2xl border-stone-200/60 p-6"` and `id="ai"` (for anchor link from checklist)
- Header: "Your AI Receptionist" as `text-base font-semibold text-[#0F172A]` + description "Your AI phone number is active. Test your receptionist anytime." as `text-sm text-[#475569] mt-1`
- Phone number display: If loading, show `Skeleton className="h-10 w-48"`. If phoneNumber is null/undefined, show `text-sm text-[#475569]` "Phone number not yet assigned". Otherwise show phone number in a pill: `<div className="bg-stone-50 rounded-lg px-4 py-2 mt-4 mb-4" aria-label="Your AI phone number"><span className="text-sm font-mono text-[#0F172A]">{phoneNumber}</span></div>`
- Below phone pill: render `<TestCallPanel phoneNumber={phoneNumber} context="settings" onComplete={() => {}} />` (no onGoToDashboard needed)
- If no phoneNumber and not loading, disable the test call section (don't render TestCallPanel)

**SettingsHoursSection.jsx** — `'use client'` component:
- No props needed (WorkingHoursEditor is self-contained)
- Structure: shadcn `Card` with `className="rounded-2xl border-stone-200/60 p-6"` and `id="hours"`
- Header: "Working Hours" as `text-base font-semibold text-[#0F172A]` + description "Set when your business accepts calls and appointments." as `text-sm text-[#475569] mt-1`
- Content: `<div className="mt-4"><WorkingHoursEditor /></div>`
- Import: `import WorkingHoursEditor from '@/components/dashboard/WorkingHoursEditor'`

**SettingsCalendarSection.jsx** — `'use client'` component:
- No props needed (CalendarSyncCard is self-contained)
- Structure: shadcn `Card` with `className="rounded-2xl border-stone-200/60 p-6"` and `id="calendar"`
- Header: "Calendar Connections" as `text-base font-semibold text-[#0F172A]` + description "Connect your calendar to sync appointments automatically." as `text-sm text-[#475569] mt-1`
- Content: `<div className="mt-4"><CalendarSyncCard /></div>`
- Import: `import CalendarSyncCard from '@/components/dashboard/CalendarSyncCard'`

**Settings page.js** — complete rewrite, `'use client'`:
1. Import: `useState`, `useEffect` from react; `Skeleton` from shadcn; `SettingsAISection`, `SettingsHoursSection`, `SettingsCalendarSection`; `supabase` from `@/lib/supabase-browser`
2. State: `phoneNumber` (null), `loading` (true)
3. `useEffect` on mount: fetch tenant phone number via `supabase.from('tenants').select('retell_phone_number').single()` — set phoneNumber from result, set loading false
4. `useEffect` for anchor scroll: on mount, read `window.location.hash`, if hash exists, `document.getElementById(hash.slice(1))?.scrollIntoView({ behavior: 'smooth' })` — wrap in `setTimeout(300)` to allow render
5. Layout: `<div className="p-6 space-y-6">` with h1 "Settings" (`text-xl font-semibold text-[#0F172A] mb-2`) then the 3 section components stacked with `space-y-6` gap
6. Render order: SettingsAISection (phoneNumber, loading) → SettingsHoursSection → SettingsCalendarSection
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/settings/page.js contains `'use client'`
    - src/app/dashboard/settings/page.js contains `import SettingsAISection`
    - src/app/dashboard/settings/page.js contains `import SettingsHoursSection`
    - src/app/dashboard/settings/page.js contains `import SettingsCalendarSection`
    - src/app/dashboard/settings/page.js contains `scrollIntoView`
    - src/app/dashboard/settings/page.js contains `retell_phone_number`
    - src/components/dashboard/SettingsAISection.jsx contains `id="ai"`
    - src/components/dashboard/SettingsAISection.jsx contains `TestCallPanel`
    - src/components/dashboard/SettingsAISection.jsx contains `context="settings"`
    - src/components/dashboard/SettingsAISection.jsx contains `Your AI Receptionist`
    - src/components/dashboard/SettingsHoursSection.jsx contains `id="hours"`
    - src/components/dashboard/SettingsHoursSection.jsx contains `WorkingHoursEditor`
    - src/components/dashboard/SettingsCalendarSection.jsx contains `id="calendar"`
    - src/components/dashboard/SettingsCalendarSection.jsx contains `CalendarSyncCard`
  </acceptance_criteria>
  <done>Settings page shows 3 stacked cards (AI Receptionist with test call, Working Hours, Calendar Connections); anchor links from checklist scroll to correct section; phone number displays or shows fallback</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- Settings page renders 3 sections with correct IDs (ai, hours, calendar)
- TestCallPanel in settings context shows "Test My AI" button and inline success
- WorkingHoursEditor and CalendarSyncCard render within their cards
- Anchor navigation from /dashboard/settings#ai scrolls to AI section
</verification>

<success_criteria>
- Settings page fully replaces stub with 3 functional sections
- Test call works from settings page using adapted TestCallPanel
- Phone number displayed in styled pill or shows "not yet assigned" fallback
- All section cards have anchor IDs matching checklist links
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-guided-setup-and-first-run-experience/10-02-SUMMARY.md`
</output>
