---
phase: 58
plan: 05
type: execute
wave: 2
depends_on: [58-02, 58-03, 58-04]
files_modified:
  - src/app/dashboard/jobs/page.js
  - src/app/dashboard/jobs/loading.js
  - src/app/dashboard/calls/page.js
  - src/app/dashboard/calls/loading.js
  - src/app/dashboard/calendar/page.js
  - src/app/dashboard/calendar/loading.js
  - src/app/dashboard/services/page.js
  - src/app/dashboard/services/loading.js
  - src/app/dashboard/settings/page.js
  - src/app/dashboard/settings/loading.js
  - src/app/dashboard/more/integrations/page.js
  - src/app/dashboard/more/integrations/loading.js
  - src/app/dashboard/more/billing/page.js
  - src/app/dashboard/more/billing/loading.js
  - src/components/dashboard/BusinessIntegrationsClient.jsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05]
user_setup: []
tags: [ui, polish, sweep, dashboard, skeletons, empty-states, focus-visible]

must_haves:
  truths:
    - "All 7 target pages render <EmptyState> when their primary data collection is empty (jobs, calls, calendar, services; integrations/settings/billing are non-list surfaces and skip empty state per UI-SPEC §8)"
    - "All 7 target pages have a loading.js file that renders layout-matching skeletons (CLS ≤ 0.1 target)"
    - "All 7 target pages render <ErrorState onRetry={refetch}> when their primary fetch fails"
    - "All interactive elements on the 7 pages use `focus-visible:` ring (either via design-tokens `focus.ring` import OR direct Tailwind `focus-visible:` class)"
    - "At least 3 primary async actions migrated to <AsyncButton> (Save settings, Connect/Disconnect integrations in BusinessIntegrationsClient, Reconnect); pending labels match UI-SPEC §10.5 defaults"
    - "No regressions on page happy-paths (navigation, data rendering, existing flows)"
    - "No hardcoded bg-white/bg-stone-*/text-black introduced (dark-mode parity preserved)"
  artifacts:
    - path: "src/app/dashboard/jobs/loading.js"
      provides: "List skeleton (≥5 rows ~72px each)"
      contains: "Skeleton"
    - path: "src/app/dashboard/calls/loading.js"
      provides: "List skeleton"
      contains: "Skeleton"
    - path: "src/app/dashboard/calendar/loading.js"
      provides: "Calendar grid skeleton (7 cols × 8 hour rows)"
      contains: "Skeleton"
    - path: "src/app/dashboard/services/loading.js"
      provides: "List skeleton"
      contains: "Skeleton"
    - path: "src/app/dashboard/settings/loading.js"
      provides: "Form skeleton (labels + input bars)"
      contains: "Skeleton"
    - path: "src/app/dashboard/more/integrations/loading.js"
      provides: "Cards grid skeleton"
      contains: "Skeleton"
    - path: "src/app/dashboard/more/billing/loading.js"
      provides: "Usage meter + invoice list skeleton"
      contains: "Skeleton"
    - path: "src/app/dashboard/jobs/page.js"
      provides: "Renders <EmptyState> when no jobs; <ErrorState onRetry> on fetch failure"
      contains: "EmptyState"
    - path: "src/app/dashboard/calls/page.js"
      provides: "Renders EmptyState (Phone icon, no calls copy per UI-SPEC §10.1) on empty; ErrorState on failure"
      contains: "EmptyState"
    - path: "src/app/dashboard/services/page.js"
      provides: "Renders EmptyState (Wrench icon) on empty; ErrorState on failure"
      contains: "EmptyState"
    - path: "src/components/dashboard/BusinessIntegrationsClient.jsx"
      provides: "Connect/Disconnect/Reconnect buttons migrated to <AsyncButton>; inline ErrorState on initialStatus fetch failure (guarded top-level)"
      contains: "AsyncButton"
  key_links:
    - from: "src/app/dashboard/jobs/page.js"
      to: "src/components/ui/empty-state.jsx"
      via: "import { EmptyState } from '@/components/ui/empty-state' (indirect via refactored EmptyStateLeads)"
      pattern: "EmptyState"
    - from: "src/app/dashboard/calls/page.js"
      to: "src/components/ui/error-state.jsx"
      via: "import { ErrorState } from '@/components/ui/error-state'"
      pattern: "ErrorState"
    - from: "src/components/dashboard/BusinessIntegrationsClient.jsx"
      to: "src/components/ui/async-button.jsx"
      via: "import { AsyncButton } from '@/components/ui/async-button'"
      pattern: "AsyncButton"
    - from: "Every consumer of @/lib/design-tokens focus.ring"
      to: "focus-visible: keyboard ring on interactive elements"
      via: "inherited token change from Plan 58-04"
      pattern: "focus-visible:"
---

<objective>
Apply the Phase 58 polish sweep across the 7 target dashboard pages. Wire primitives from Plan 58-04 (EmptyState, ErrorState, AsyncButton) into each page; add layout-matching `loading.js` skeletons (Next.js App Router convention); migrate hand-rolled `focus:ring` call sites to `focus-visible:` for pages that don't already import the design-tokens focus export; migrate Connect/Disconnect/Reconnect ad-hoc pending patterns in `BusinessIntegrationsClient.jsx` to `<AsyncButton>` as the canonical migration demonstration.

Purpose: Ship POLISH-01..05 across the full dashboard surface per the implemented page list (see INFO box below). This is the absorbed v5.0 Phase 51 work.

Output: 7 loading.js files (some new, some updated), 7 page.js files updated to render EmptyState + ErrorState, BusinessIntegrationsClient refactored to use AsyncButton at Connect/Disconnect/Reconnect sites.

---

> **INFO — Implemented page list supersedes CONTEXT.md D-16 stale text (W4 from plan-checker)**
>
> CONTEXT.md D-16 still lists `analytics` (route deleted in Phase 49) and duplicates `jobs`. The authoritative page list for this plan is the RESEARCH §E.1 refinement, which reflects the actual dashboard surface as of 2026-04-20:
>
> **7 pages:** `jobs`, `calls`, `calendar`, `services`, `settings`, `more/integrations`, `more/billing`.
>
> This list also matches 58-VALIDATION Wave 0 expectations and UI-SPEC §8's per-page matrix. If a future reader sees the D-16 wording and asks "what about analytics/leads?" — those routes no longer exist. The plan implements against the 7 pages above; CONTEXT.md D-16 text will be authored-corrected in a separate docs-only pass (not in this phase).

---

> **INFO — Dependency on 58-03 (B3 from plan-checker)**
>
> Both 58-03 and 58-05 modify `src/app/dashboard/more/integrations/page.js`. 58-03 lands first (adds `last_context_fetch_at` to the `.select(...)` string for the D-08 Jobber last-synced render). 58-05 (this plan) then wires ErrorState + creates loading.js for the same page WITHOUT touching the select string. Merge order preserved via `depends_on: [58-02, 58-03, 58-04]`. If the executor sees a local rebase conflict on this file, keep 58-03's `last_context_fetch_at` select AND 58-05's error/skeleton additions.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-CONTEXT.md
@.planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-UI-SPEC.md

<interfaces>
From Plan 58-04 (created primitives):
```jsx
import { EmptyState } from '@/components/ui/empty-state';
import { ErrorState } from '@/components/ui/error-state';
import { AsyncButton } from '@/components/ui/async-button';
```

From src/components/ui/skeleton.jsx (existing shadcn primitive):
```jsx
<Skeleton className="h-4 w-24" />  // animate-pulse rounded-md bg-accent
```

From 58-UI-SPEC §8 (page-by-page sweep matrix — locked):
| Page | Empty | Skeleton | Error+Retry | Focus | Async |
|------|-------|----------|-------------|-------|-------|
| /dashboard/jobs | wire existing EmptyStateLeads | ✅ new loading.js | ✅ | sweep | Mark done / Edit |
| /dashboard/calls | ✅ new Phone icon | ✅ | ✅ | sweep | Call back |
| /dashboard/calendar | wire EmptyStateCalendar | ✅ grid skeleton | ✅ | sweep | Mark complete |
| /dashboard/more/integrations | no empty state | ✅ cards grid | ✅ top-level | sweep | migrate Connect/Disconnect/Reconnect |
| /dashboard/services | ✅ Wrench icon | ✅ | ✅ | sweep | Save/Delete |
| /dashboard/settings | no empty state | ✅ form shape | ✅ | sweep | Save settings |
| /dashboard/more/billing | no empty state | ✅ meter+list | ✅ | sweep | Change plan / Portal |

From 58-UI-SPEC §10.1 (locked copy):
| Page | Headline | Description | CTA | Target |
| jobs | No jobs yet | "When callers reach your AI, jobs appear here with caller details, job type, and urgency." | Make a test call | /dashboard/more/ai-voice-settings |
| calls | No calls yet | "When callers reach your AI receptionist, they'll appear here with transcript and recording." | Make a test call | /dashboard/more/ai-voice-settings |
| calendar | No appointments yet | "Bookings from your AI receptionist will appear here. You can also add a time block to hold time off." | Add a time block | (TimeBlockSheet open) |
| services | No services yet | "Add the services you offer so callers can book the right job." | Add a service | (add-service flow) |

From 58-UI-SPEC §8 (skeleton shape guidance):
- Max depth per loading.js: 80 lines. If more, extract to src/components/dashboard/<Page>Skeleton.jsx.
- Use existing <Skeleton> primitive, do not hand-roll animate-pulse.
- Mirror shipped layout — prevent CLS ≤ 0.1 target.

From 58-UI-SPEC §10.5 (locked pendingLabel values):
- Save settings → "Saving…"
- Connect → "Connecting…"
- Disconnect → "Disconnecting…"
- Reconnect → "Reconnecting…"
- Add service → "Adding…"
- Add time block → "Adding…"
(Unicode `…` — single glyph, not three periods.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship 7 loading.js skeletons + empty-state / error-state wiring on the 7 pages</name>
  <files>src/app/dashboard/jobs/loading.js, src/app/dashboard/jobs/page.js, src/app/dashboard/calls/loading.js, src/app/dashboard/calls/page.js, src/app/dashboard/calendar/loading.js, src/app/dashboard/calendar/page.js, src/app/dashboard/services/loading.js, src/app/dashboard/services/page.js, src/app/dashboard/settings/loading.js, src/app/dashboard/settings/page.js, src/app/dashboard/more/integrations/loading.js, src/app/dashboard/more/integrations/page.js, src/app/dashboard/more/billing/loading.js, src/app/dashboard/more/billing/page.js</files>
  <read_first>
    - src/app/dashboard/jobs/page.js (current structure — identify data fetch site + empty-case handler)
    - src/app/dashboard/calls/page.js (current structure)
    - src/app/dashboard/calendar/page.js (current + look at how EmptyStateCalendar is invoked + TimeBlockSheet open handler)
    - src/app/dashboard/services/page.js (current + add-service flow)
    - src/app/dashboard/settings/page.js (current)
    - src/app/dashboard/more/integrations/page.js (current — integrations card surface; handled in Task 2 for AsyncButton migration and ErrorState top-level. NOTE: 58-03 already edited this file to add `last_context_fetch_at` to the `.select(...)` string — preserve that edit)
    - src/app/dashboard/more/billing/page.js (current)
    - src/app/dashboard/loading.js (existing precedent if present — copy skeleton pattern)
    - src/app/dashboard/jobs/loading.js (if already exists — extend; if not — create)
    - src/app/dashboard/calls/loading.js (same)
    - src/components/ui/skeleton.jsx (shadcn Skeleton primitive)
    - .planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-UI-SPEC.md §8 (per-page matrix) + §10.1 (locked copy) + §13 (what's NOT locked — executor discretion on skeleton row counts)
  </read_first>
  <action>
Approach: work page by page. For each page:
1. Add or update `loading.js` to render layout-matching Skeleton markup (≤80 lines — extract to `<PageSkeleton>` under `src/components/dashboard/` if exceeded).
2. In `page.js` (or the page's client component if the page is server-only), wire EmptyState for the empty data case and ErrorState for the fetch-failure case.
3. Keep existing happy-path behavior intact — no refactors beyond what's needed for the primitive wiring.

**Per-page specifications:**

**A. /dashboard/jobs** — loading.js renders 5-7 rows of ~72px height skeleton (name, subtitle, metadata, trailing icon). page.js already uses `EmptyStateLeads` (refactored in 58-04); verify it renders on `jobs.length === 0`. Wrap the fetch invocation in a try/catch or error-boundary pattern that exposes `<ErrorState onRetry={refetch} />` when the fetch throws. If the page uses React Query or SWR, the `error` state already exists — just render ErrorState. If the page is a server component that throws on fetch, add an error.js route segment in the same folder OR convert to a client boundary with ErrorState.

**B. /dashboard/calls** — loading.js list skeleton. page.js: import `{ EmptyState } from '@/components/ui/empty-state'`, `{ Phone } from 'lucide-react'`, render when `calls.length === 0`:

```jsx
<EmptyState
  icon={Phone}
  headline="No calls yet"
  description="When callers reach your AI receptionist, they'll appear here with transcript and recording."
  ctaLabel="Make a test call"
  ctaHref="/dashboard/more/ai-voice-settings"
/>
```

Wrap fetch with ErrorState (same pattern as jobs).

**C. /dashboard/calendar** — loading.js 7-column × 8-hour-row grid skeleton. page.js: EmptyStateCalendar already exists (refactored in 58-04); verify it renders for empty calendar state. Calendar CTA opens TimeBlockSheet — executor wires `ctaOnClick={() => setTimeBlockSheetOpen(true)}` if the refactored EmptyStateCalendar exposes a callback, OR the wrapper internally owns the state. ErrorState on fetch failure.

**D. /dashboard/services** — loading.js list skeleton. page.js: import EmptyState, `{ Wrench } from 'lucide-react'`, render on `services.length === 0`:

```jsx
<EmptyState
  icon={Wrench}
  headline="No services yet"
  description="Add the services you offer so callers can book the right job."
  ctaLabel="Add a service"
  ctaOnClick={() => openAddServiceDialog()}  // wire to existing add-service flow
/>
```

ErrorState on fetch failure.

**E. /dashboard/settings** — loading.js form-shaped skeleton (labels + input bars). page.js: no empty state (settings is a form, not a list). ErrorState on initial-load failure.

**F. /dashboard/more/integrations** — loading.js cards-grid skeleton. page.js: no empty state (cards render unconditionally). ErrorState top-level if `initialStatus` fetch fails — wrap the top-level render in a client boundary or error.js. (Connect/Disconnect/Reconnect AsyncButton migration happens in Task 2.)

**IMPORTANT — 58-03 precedes this plan on page.js:** 58-03 added `last_context_fetch_at` to the `.select(...)` string on this page. Do NOT remove that field during the ErrorState wiring. The select string must still contain `last_context_fetch_at` after 58-05's edits.

**G. /dashboard/more/billing** — loading.js usage-meter skeleton + invoice-list skeleton. page.js: no empty state (subscription always present post-trial-start). ErrorState on Stripe fetch failure.

**Skeleton shape example (jobs/loading.js, ~40 lines, use as template):**

```jsx
import { Skeleton } from '@/components/ui/skeleton';

export default function JobsLoading() {
  return (
    <div className="p-4 space-y-3">
      {Array.from({ length: 6 }).map((_, i) => (
        <div key={i} className="flex items-center gap-3 py-2">
          <Skeleton className="h-10 w-10 rounded-full" />
          <div className="flex-1 space-y-2">
            <Skeleton className="h-4 w-48" />
            <Skeleton className="h-3 w-32" />
          </div>
          <Skeleton className="h-8 w-16" />
        </div>
      ))}
    </div>
  );
}
```

**ErrorState wiring example** (client component with useEffect fetch):

```jsx
const [error, setError] = useState(null);
const refetch = useCallback(() => { setError(null); /* re-run fetch */ }, []);
if (error) return <ErrorState message={error.message} onRetry={refetch} />;
```

If the page uses server components with static rendering, the ErrorState may need to move into a child client component that owns the fetch (existing pattern in the dashboard — executor follows local conventions).

**Focus-visible sweep:** For any direct `focus:ring-2` / `focus:outline-none` / `focus:ring-[` literal string found in the page.js or its child components (ChecklistItem.jsx already migrated in this phase — wait, Plan 58-02 intentionally left it for Plan 58-05), update to `focus-visible:*`. Run `grep -rE "'focus:" src/app/dashboard/<page>` and `grep -rE "\"focus:" src/app/dashboard/<page>` to find the call sites. Rewrite each.

**NOT in scope:** Do NOT touch `:focus-within` selectors. Do NOT touch native form elements that legitimately need mouse-focus rings (input borders on text inputs). When in doubt — migrate; the UI-SPEC §5 rationale says focus-visible is preferred for keyboard-reveal semantics.

**Copy literal enforcement:** Every string in the EmptyState call sites above MUST match UI-SPEC §10.1 verbatim. Executor does not paraphrase. Unicode `…` for ellipsis (no `...`).
  </action>
  <verify>
    <automated>test -f src/app/dashboard/jobs/loading.js &amp;&amp; test -f src/app/dashboard/calls/loading.js &amp;&amp; test -f src/app/dashboard/calendar/loading.js &amp;&amp; test -f src/app/dashboard/services/loading.js &amp;&amp; test -f src/app/dashboard/settings/loading.js &amp;&amp; test -f src/app/dashboard/more/integrations/loading.js &amp;&amp; test -f src/app/dashboard/more/billing/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/jobs/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/calls/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/calendar/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/services/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/settings/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/more/integrations/loading.js &amp;&amp; grep -q "Skeleton" src/app/dashboard/more/billing/loading.js &amp;&amp; grep -q "No jobs yet" src/app/dashboard/jobs/page.js &amp;&amp; grep -q "No calls yet" src/app/dashboard/calls/page.js &amp;&amp; grep -q "No appointments yet" src/app/dashboard/calendar/page.js &amp;&amp; grep -q "No services yet" src/app/dashboard/services/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/jobs/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/calls/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/calendar/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/services/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/settings/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/more/integrations/page.js &amp;&amp; grep -q "ErrorState" src/app/dashboard/more/billing/page.js &amp;&amp; grep -q "AsyncButton" src/app/dashboard/settings/page.js &amp;&amp; grep -q "last_context_fetch_at" src/app/dashboard/more/integrations/page.js</automated>
  </verify>
  <acceptance_criteria>
    - All 7 loading.js files exist (jobs, calls, calendar, services, settings, more/integrations, more/billing)
    - Each loading.js imports and uses `<Skeleton>` primitive (no hand-rolled `animate-pulse`)
    - Each loading.js file has ≤ 80 lines (or the overflow lives in `src/components/dashboard/<Page>Skeleton.jsx`)
    - **W2 per-page content-specificity coverage:**
      - `/dashboard/jobs/page.js` contains "No jobs yet" literal + imports/renders `ErrorState`
      - `/dashboard/calls/page.js` contains literal "No calls yet" + "When callers reach your AI receptionist, they'll appear here with transcript and recording." + "Make a test call" + "/dashboard/more/ai-voice-settings" + imports/renders `EmptyState` AND `ErrorState`
      - `/dashboard/calendar/page.js` contains "No appointments yet" literal + imports/renders `ErrorState`
      - `/dashboard/services/page.js` contains literal "No services yet" + "Add the services you offer so callers can book the right job." + "Add a service" + imports/renders `EmptyState` AND `ErrorState`
      - `/dashboard/settings/page.js` imports/renders `ErrorState` AND `AsyncButton` (Save uses AsyncButton per UI-SPEC §8)
      - `/dashboard/more/integrations/page.js` imports/renders `ErrorState` AND still contains `last_context_fetch_at` in the `.select(...)` string (58-03 precedent preserved)
      - `/dashboard/more/billing/page.js` imports/renders `ErrorState`
    - `/dashboard/jobs/page.js` renders EmptyStateLeads (refactored wrapper) on empty + ErrorState on error
    - `/dashboard/calendar/page.js` renders EmptyStateCalendar (refactored wrapper) on empty + ErrorState on error
    - No page introduces `bg-white`, `bg-stone-*`, or `text-black` hardcoded classes (dark-mode regression guard)
    - No `focus:ring-2` or `focus:outline-none` remains in any of the 7 pages' direct source (all converted to `focus-visible:`) — run `grep -rE "focus:ring-2" src/app/dashboard` and ensure results are zero in the 7 page folders (shared components may still have `focus:` — those are OK for this plan; D-19 refined scope limits the sweep to design-tokens + direct consumers)
  </acceptance_criteria>
  <done>7 pages wired: skeletons, empty states, error + retry, AsyncButton on settings Save. Dashboard polish sweep shipped. Every page spot-checked by grep, not just 2 of 7 (W2 closure).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BusinessIntegrationsClient Connect/Disconnect/Reconnect to <AsyncButton> + sweep focus-visible on its interactive elements</name>
  <files>src/components/dashboard/BusinessIntegrationsClient.jsx</files>
  <read_first>
    - src/components/dashboard/BusinessIntegrationsClient.jsx (full file — focus on the 3 action call sites: Connect at ~271-299, Disconnect at ~283-299, Reconnect banner buttons)
    - src/components/ui/async-button.jsx (created in Plan 58-04)
    - .planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-UI-SPEC.md §4.3 (AsyncButton prop contract) + §10.5 (locked pending labels)
    - .planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-CONTEXT.md D-21 (POLISH-05 scope)
  </read_first>
  <action>
Step 1 — Import AsyncButton at the top of `BusinessIntegrationsClient.jsx`:

```jsx
import { AsyncButton } from '@/components/ui/async-button';
```

Step 2 — Find each ad-hoc pending-Button call site (current shape per research):

```jsx
<Button disabled={isConnecting}>
  {isConnecting ? (
    <><Loader2 className="h-4 w-4 mr-2 animate-spin" aria-hidden="true" /> Connecting…</>
  ) : (
    'Connect Jobber'
  )}
</Button>
```

Step 3 — Replace each with `<AsyncButton>`:

For Connect (Xero and Jobber each):
```jsx
<AsyncButton pending={isConnecting} pendingLabel="Connecting…" onClick={handleConnect}>
  {providerKey === 'xero' ? 'Connect Xero' : 'Connect Jobber'}
</AsyncButton>
```

For Disconnect:
```jsx
<AsyncButton pending={isDisconnecting} pendingLabel="Disconnecting…" variant="outline" onClick={handleDisconnect}>
  Disconnect
</AsyncButton>
```

For Reconnect (in the reconnect banner):
```jsx
<AsyncButton pending={isReconnecting} pendingLabel="Reconnecting…" onClick={handleReconnect}>
  {providerKey === 'xero' ? 'Reconnect Xero' : 'Reconnect Jobber'}
</AsyncButton>
```

Rules:
- Match the existing state variable names (`isConnecting`, `isDisconnecting`, `isReconnecting`, or whatever the file uses). Do NOT rename.
- Preserve any surrounding layout wrapper divs, aria-labels, or conditional rendering.
- Keep the variant mapping the same (Connect=default=copper primary; Disconnect=outline; Reconnect=default).
- If Jobber and Xero share one render function parameterized by `providerKey`, update that shared function once — do not duplicate.

Step 4 — Sweep `focus:ring*` literals in this file to `focus-visible:*`. Run grep on the file; for every match, replace `focus:` prefix with `focus-visible:`. Add `focus-visible:ring-offset-[var(--background)]` on sites where `focus:ring-offset-1` already existed (for dark-mode parity).

Step 5 — (Optional scope per D-16 "planner ordering") If the top of BusinessIntegrationsClient has a surface that could render on `initialStatus` being null/error, wrap it in `<ErrorState message="Unable to load integrations" onRetry={...} />` — but ONLY if the current code has such an error state already (or swallows it silently). If the current code assumes `initialStatus` is always populated (server-component guarantees it), leave as-is.

**Do not** alter the reconnect banner copy, the 4-state machine, or any Xero/Jobber-specific behavior. Only the primitive swap + focus-visible token sweep.
  </action>
  <verify>
    <automated>grep -q "import.*AsyncButton.*from.*'@/components/ui/async-button'" src/components/dashboard/BusinessIntegrationsClient.jsx &amp;&amp; grep -qE "pendingLabel=\"(Connecting|Disconnecting|Reconnecting)…\"" src/components/dashboard/BusinessIntegrationsClient.jsx &amp;&amp; grep -c "AsyncButton" src/components/dashboard/BusinessIntegrationsClient.jsx | awk '$1 &gt;= 3 {exit 0} {exit 1}' &amp;&amp; ! grep -E "^\s*focus:ring-2" src/components/dashboard/BusinessIntegrationsClient.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `import { AsyncButton } from '@/components/ui/async-button'` present
    - At least 3 `<AsyncButton>` usages (Connect, Disconnect, Reconnect — at minimum; Xero and Jobber shared render may yield more)
    - `pendingLabel="Connecting…"` (with Unicode ellipsis `…`) present
    - `pendingLabel="Disconnecting…"` present
    - `pendingLabel="Reconnecting…"` present
    - No bare `focus:ring-2` class string remaining in the file (all converted to `focus-visible:ring-2`)
    - Existing behavior preserved (no changes to handler names, state-variable names, 4-state machine rendering, reconnect banner copy)
    - File still exports the same component name (no rename)
  </acceptance_criteria>
  <done>BusinessIntegrationsClient async actions migrated; focus-visible swept on this file.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (no compile errors from primitive imports)
- Manual spot-check each of 7 pages at 375px + desktop: empty state renders, skeleton renders on slow-network throttling, ErrorState renders on blocked fetch, AsyncButton spinner renders during Connect on integrations page
- Keyboard walk: Tab through BusinessIntegrationsClient → Connect button → AsyncButton → each shows copper ring on focus; mouse-click does NOT show ring
- Dark mode spot-check: ring offset blends into page background (no light halo)
- No new hardcoded hex color classes introduced (`grep -rE "bg-(white|black|stone)"` in the 7 pages returns no new matches vs pre-plan state)
- 58-03's `last_context_fetch_at` select addition on more/integrations/page.js preserved through this plan's edits (grep guard in Task 1 verify)
</verification>

<success_criteria>
- POLISH-01: 4 list pages (jobs, calls, calendar, services) render EmptyState with locked UI-SPEC §10.1 copy
- POLISH-02: 7 pages have layout-matching loading.js skeletons
- POLISH-03: `focus:` → `focus-visible:` sweep complete on 7 pages + BusinessIntegrationsClient
- POLISH-04: 7 pages render ErrorState with Retry on fetch failure
- POLISH-05: BusinessIntegrationsClient uses AsyncButton with locked pendingLabel copy + Settings Save uses AsyncButton
- Zero regressions on existing happy-path flows
- Ready for Plan 58-07 UAT to walk the keyboard/dark-mode / empty-state / error-state scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/58-setup-checklist-final-wiring-skills-telemetry-uat-phase-51-polish-absorption/58-05-SUMMARY.md`
</output>
</content>
</invoke>