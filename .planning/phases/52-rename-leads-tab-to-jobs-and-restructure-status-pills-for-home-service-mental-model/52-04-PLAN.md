---
phase: 52
plan: 04
type: execute
wave: 2
depends_on: [52-02]
files_modified:
  - src/components/dashboard/LeadFlyout.jsx
  - src/components/dashboard/LeadFilterBar.jsx
  - src/components/dashboard/EmptyStateLeads.jsx
  - src/components/dashboard/HotLeadsTile.jsx
  - src/components/dashboard/DashboardSidebar.jsx
  - src/components/dashboard/BottomTabBar.jsx
  - src/components/dashboard/DashboardTour.jsx
  - src/components/dashboard/AppointmentFlyout.js
  - src/app/api/search/route.js
  - src/lib/notifications.js
  - src/app/dashboard/more/call-routing/page.js
  - src/app/dashboard/calls/page.js
  - src/app/dashboard/invoices/page.js
  - src/app/dashboard/invoices/batch-review/page.js
  - src/app/dashboard/invoices/[id]/page.js
  - src/app/dashboard/estimates/[id]/page.js
autonomous: true
requirements: [RENAME-01, RENAME-03]
tags: [dashboard, copy-reframe, navigation, internal-links]

must_haves:
  truths:
    - "Sidebar nav item displays 'Jobs' (not 'Leads') and links to /dashboard/jobs"
    - "BottomTabBar mobile tab displays 'Jobs' (not 'Leads') and links to /dashboard/jobs"
    - "LeadFlyout sheet titles, body copy, and toast messages say 'Job(s)' instead of 'Lead(s)'"
    - "LeadFlyout STATUS_LABELS map renders 'Scheduled' for booked status"
    - "LeadFilterBar aria-labels and sheet title say 'jobs' / 'Filter jobs'"
    - "EmptyStateLeads heading says 'No jobs yet' and body uses 'jobs' noun"
    - "HotLeadsTile title is 'New jobs', CTA is 'View all jobs', link href is /dashboard/jobs"
    - "Search API result type displays as 'Jobs' with href /dashboard/jobs"
    - "Notification email dashboard link points to /dashboard/jobs"
    - "Every internal-link reference to /dashboard/leads in src/ has been updated to /dashboard/jobs (zero hits in final grep)"
  artifacts:
    - path: "src/components/dashboard/DashboardSidebar.jsx"
      provides: "Sidebar nav item: label 'Jobs', href /dashboard/jobs"
      contains: "label: 'Jobs'"
    - path: "src/components/dashboard/BottomTabBar.jsx"
      provides: "Bottom tab item: label 'Jobs', href /dashboard/jobs"
      contains: "label: 'Jobs'"
    - path: "src/components/dashboard/LeadFlyout.jsx"
      provides: "Sheet titles, STATUS_LABELS (booked→Scheduled), toast strings reframed as 'Job'"
      contains: "Job Details"
    - path: "src/components/dashboard/HotLeadsTile.jsx"
      provides: "Title 'New jobs', CTA 'View all jobs', href /dashboard/jobs, count labels singular/plural"
      contains: "/dashboard/jobs"
    - path: "src/app/api/search/route.js"
      provides: "Search index type label 'Jobs' + href /dashboard/jobs"
      contains: "/dashboard/jobs"
    - path: "src/lib/notifications.js"
      provides: "Notification email dashboard link points at /dashboard/jobs"
      contains: "/dashboard/jobs"
  key_links:
    - from: "src/components/dashboard/DashboardSidebar.jsx + BottomTabBar.jsx"
      to: "src/app/dashboard/jobs/page.js (Plan 02 destination)"
      via: "nav href /dashboard/jobs renders the moved page"
      pattern: "/dashboard/jobs"
    - from: "src/components/dashboard/HotLeadsTile.jsx CTA + AppointmentFlyout linkedLead button + calls/page caller link + invoice/estimate 'View Lead' buttons"
      to: "src/app/dashboard/jobs/page.js (?open=ID, ?search=PHONE, ?status=completed)"
      via: "all internal hrefs use /dashboard/jobs base; querystring formats unchanged"
      pattern: "/dashboard/jobs"
    - from: "src/lib/notifications.js notification email"
      to: "src/app/dashboard/jobs (canonical URL)"
      via: "${NEXT_PUBLIC_APP_URL}/dashboard/jobs"
      pattern: "/dashboard/jobs"
---

<objective>
Reframe every remaining user-facing "Lead(s)" copy occurrence and every internal `/dashboard/leads` href reference across the codebase to "Job(s)" / `/dashboard/jobs`. This plan is the Wave 2 sweep: it depends on Plan 02 having moved the page to its new URL, and it eliminates every internal link that would otherwise hit the 308 redirect (which works, but is a slower extra hop and pollutes server logs).

Purpose: Completes RENAME-01 (nav labels + internal hrefs) and RENAME-03 (user-facing "lead(s)" copy reframe across all surfaces). The final-state guarantee is that `grep -r "/dashboard/leads" src/` returns zero hits. Status badges in LeadFlyout's internal `STATUS_LABELS` are also synced to "Scheduled" to match Plan 01.

Output: 16 files updated. After this plan, the only remaining `/dashboard/leads` reference in the entire repository is the source-side string in `next.config.js`'s redirect entry (added in Plan 02), which is the one allowed mention.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-01-SUMMARY.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-02-SUMMARY.md

<interfaces>
<!-- All known internal /dashboard/leads occurrences (verified by grep, 2026-04-16) -->

```
src/components/dashboard/HotLeadsTile.jsx                       — CTA href + count label + title
src/components/dashboard/DashboardTour.jsx:14                   — target: '[href="/dashboard/leads"]'
src/components/dashboard/DashboardSidebar.jsx                   — NAV_ITEMS entry (label 'Leads', href '/dashboard/leads')
src/components/dashboard/BottomTabBar.jsx                       — TABS entry (label 'Leads', href '/dashboard/leads')
src/components/dashboard/AppointmentFlyout.js:231               — router.push(`/dashboard/leads?open=${linkedLead.id}`)
src/app/dashboard/more/call-routing/page.js:343                 — router.push(`/dashboard/leads?open=${leadId}`)
src/app/dashboard/calls/page.js:304                             — href={`/dashboard/leads?search=${encodeURIComponent(call.from_number)}`}
src/app/dashboard/invoices/page.js:264                          — href="/dashboard/leads?status=completed"
src/app/dashboard/invoices/batch-review/page.js:134, 282        — router.push('/dashboard/leads')
src/app/dashboard/invoices/[id]/page.js:491                     — href={`/dashboard/leads?open=${invoice.lead_id}`}
src/app/dashboard/estimates/[id]/page.js:645                    — href={`/dashboard/leads?open=${estimate.lead_id}`}
src/app/api/search/route.js:65, 70                              — type label 'Leads', href `/dashboard/leads?open=${l.id}`
src/lib/notifications.js:319                                    — `${process.env.NEXT_PUBLIC_APP_URL}/dashboard/leads`
```

<!-- LeadFlyout internal STATUS_LABELS map (current — must change booked entry) -->
```javascript
// LeadFlyout.jsx
// const STATUS_LABELS = { new: 'New', booked: 'Booked', completed: 'Completed', paid: 'Paid', lost: 'Lost' };
// Required: booked: 'Scheduled' (mirrors LeadCard + LeadStatusPills change in Plan 01)
```

<!-- LeadFlyout literal user-facing strings to reframe (from UI-SPEC §"Component Mutations" #3) -->
```
'Loading lead details'        →  'Loading job details'
'Lead Details'                →  'Job Details'
"Couldn't load lead details." →  "Couldn't load job details."
'Lead marked as Lost' (×2)    →  'Job marked as Lost'
${lead.caller_name || 'Lead'} →  ${lead.caller_name || 'Job'}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reframe user-facing copy in 7 dashboard components — flyout, filter bar, empty state, hot tile, sidebar, bottom tab bar</name>
  <files>
    src/components/dashboard/LeadFlyout.jsx,
    src/components/dashboard/LeadFilterBar.jsx,
    src/components/dashboard/EmptyStateLeads.jsx,
    src/components/dashboard/HotLeadsTile.jsx,
    src/components/dashboard/DashboardSidebar.jsx,
    src/components/dashboard/BottomTabBar.jsx
  </files>
  <read_first>
    - All 6 component files in `<files>` (each in full)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md §"Component Mutations" #3, #4, #5, #6, #7, #8 AND §"Copywriting Contract" (canonical strings)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md (D-09 for scope, D-02 for "Scheduled" label, D-10 for variable-name preservation)
    - .planning/phases/49-dark-mode-foundation-and-token-migration/49-04-PLAN.md (preserve any dark-mode-aware classes — touch only copy strings)
  </read_first>
  <behavior>
    - LeadFlyout sheet titles, body copy, toast messages, and STATUS_LABELS map all use Job/Jobs terminology + 'Scheduled' label for booked
    - LeadFilterBar aria-labels and sheet title use jobs terminology
    - EmptyStateLeads heading and body use jobs terminology
    - HotLeadsTile title, CTA, count labels, error copy all use jobs terminology AND its CTA href is /dashboard/jobs
    - DashboardSidebar NAV_ITEMS entry: label 'Jobs', href '/dashboard/jobs'
    - BottomTabBar TABS entry: label 'Jobs', href '/dashboard/jobs'
    - File names, component export names, internal variable names, icon imports, and color/styling classes are unchanged across all 6 files (D-10)
  </behavior>
  <action>
    Apply each file's targeted edits per UI-SPEC §"Component Mutations". Each row below is one find-and-replace; do NOT use blind global replace because some files contain unrelated occurrences (e.g. `lead_id` JS identifiers, `eligibleLeads` variables — leave those alone).

    **A. src/components/dashboard/LeadFlyout.jsx — copy reframe (mutations #3 from UI-SPEC):**

    | Find (literal) | Replace with |
    |----------------|--------------|
    | `<SheetTitle className="sr-only">Loading lead details</SheetTitle>` | `<SheetTitle className="sr-only">Loading job details</SheetTitle>` |
    | `<SheetTitle>Lead Details</SheetTitle>` | `<SheetTitle>Job Details</SheetTitle>` |
    | `Couldn't load lead details.` | `Couldn't load job details.` |
    | `booked: 'Booked'` (in the local `STATUS_LABELS` map) | `booked: 'Scheduled'` |
    | `toast.success('Lead marked as Lost')` (BOTH occurrences — UI-SPEC notes lines ~344 and ~375) | `toast.success('Job marked as Lost')` |
    | `${lead.caller_name \|\| 'Lead'}` (inside the status-moved toast template literal) | `${lead.caller_name \|\| 'Job'}` |

    Do NOT rename `STATUS_OPTIONS`, `STATUS_LABELS`, `STATUS_COLORS`, the `LeadFlyout` export, or any local variable. Do NOT touch the lead-status select options' `value` keys (DB enum). Do NOT touch the field-save error message ("Status update failed. Try again — your changes weren't saved.") which is unchanged per UI-SPEC.

    **B. src/components/dashboard/LeadFilterBar.jsx — aria-labels and sheet title (mutation #4):**

    | Find | Replace |
    |------|---------|
    | `aria-label="Search leads"` | `aria-label="Search jobs"` |
    | `<SheetTitle>Filter leads</SheetTitle>` | `<SheetTitle>Filter jobs</SheetTitle>` |

    Do NOT touch the search input placeholder `Search name or phone...` (it does not say "lead" — UI-SPEC notes no edit required). Do NOT touch any filter dropdown logic.

    **C. src/components/dashboard/EmptyStateLeads.jsx — heading + body (mutation #5):**

    | Find | Replace |
    |------|---------|
    | `No leads yet` | `No jobs yet` |
    | `When callers reach your AI, leads appear here with caller details, job type, and urgency.` | `When callers reach your AI, jobs appear here with caller details, job type, and urgency.` |

    Do NOT change the CTA button label `Make a Test Call` (already correct). Do NOT rename the file (per RENAME-03).

    **D. src/components/dashboard/HotLeadsTile.jsx — title, CTA, counts, error copy, href (mutation #6):**

    | Find | Replace |
    |------|---------|
    | `Hot / new leads` (card title) | `New jobs` |
    | `No new leads right now.` | `No new jobs right now.` |
    | `View all leads` (CTA link text) | `View all jobs` |
    | `'/dashboard/leads'` (CTA link href) | `'/dashboard/jobs'` |
    | `'new lead' : 'new leads'` (singular/plural ternary) | `'new job' : 'new jobs'` |
    | `Couldn't load leads.` (error state heading) | `Couldn't load jobs.` |

    The empty-state body sentence `New inquiries from calls and forms land here first. Check back after your next call.` does not contain "lead" — leave unchanged. Do NOT rename the file or the `HotLeadsTile` export.

    **E. src/components/dashboard/DashboardSidebar.jsx — NAV_ITEMS entry (mutation #7):**

    Locate the entry on line ~18 with `href: '/dashboard/leads'` and `label: 'Leads'`. Change it to:
    ```javascript
    { href: '/dashboard/jobs', label: 'Jobs', icon: Users },
    ```
    Preserve the existing `icon` value (`Users` per UI-SPEC) and array position. Do NOT change any other NAV_ITEMS entry.

    **F. src/components/dashboard/BottomTabBar.jsx — TABS entry (mutation #8):**

    Locate the entry on line ~10 with `href: '/dashboard/leads'` and `label: 'Leads'`. Change it to:
    ```javascript
    { href: '/dashboard/jobs', label: 'Jobs', icon: Users },
    ```
    Preserve the existing icon value and array position. Do NOT change any other TABS entry.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cases=[['src/components/dashboard/LeadFlyout.jsx',[/<SheetTitle>Job Details<\/SheetTitle>/,/<SheetTitle className=\"sr-only\">Loading job details<\/SheetTitle>/,/Couldn't load job details\./,/booked:\s*'Scheduled'/,/toast\.success\('Job marked as Lost'\)/,/\$\{lead\.caller_name \|\| 'Job'\}/]],['src/components/dashboard/LeadFilterBar.jsx',[/aria-label=\"Search jobs\"/,/<SheetTitle>Filter jobs<\/SheetTitle>/]],['src/components/dashboard/EmptyStateLeads.jsx',[/No jobs yet/,/jobs appear here with caller details/]],['src/components/dashboard/HotLeadsTile.jsx',[/New jobs/,/No new jobs right now/,/View all jobs/,/'\/dashboard\/jobs'/,/'new job'\s*:\s*'new jobs'/,/Couldn't load jobs\./]],['src/components/dashboard/DashboardSidebar.jsx',[/href:\s*'\/dashboard\/jobs',\s*label:\s*'Jobs'/]],['src/components/dashboard/BottomTabBar.jsx',[/href:\s*'\/dashboard\/jobs',\s*label:\s*'Jobs'/]]];const stale=[['src/components/dashboard/LeadFlyout.jsx',[/Lead Details/,/Loading lead details/,/Lead marked as Lost/]],['src/components/dashboard/LeadFilterBar.jsx',[/Search leads/,/Filter leads/]],['src/components/dashboard/EmptyStateLeads.jsx',[/No leads yet/]],['src/components/dashboard/HotLeadsTile.jsx',[/'\/dashboard\/leads'/,/Hot \/ new leads/,/View all leads/]],['src/components/dashboard/DashboardSidebar.jsx',[/'\/dashboard\/leads'/]],['src/components/dashboard/BottomTabBar.jsx',[/'\/dashboard\/leads'/]]];let fail=false;for(const [f,rs] of cases){const s=fs.readFileSync(f,'utf8');for(const r of rs){if(!r.test(s)){console.error('FAIL: '+f+' missing pattern '+r);fail=true}}}for(const [f,rs] of stale){const s=fs.readFileSync(f,'utf8');for(const r of rs){if(r.test(s)){console.error('FAIL: '+f+' contains stale pattern '+r);fail=true}}}if(fail)process.exit(1);console.log('PASS: 6 files reframed correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - LeadFlyout.jsx contains literal `<SheetTitle>Job Details</SheetTitle>`, `Loading job details`, `Couldn't load job details.`, `booked: 'Scheduled'`, `toast.success('Job marked as Lost')`, and `|| 'Job'`
    - LeadFlyout.jsx no longer contains the literals `Lead Details`, `Loading lead details`, or `Lead marked as Lost`
    - LeadFilterBar.jsx contains `aria-label="Search jobs"` and `<SheetTitle>Filter jobs</SheetTitle>`; no longer contains `Search leads` or `Filter leads`
    - EmptyStateLeads.jsx heading is `No jobs yet`; body uses the word `jobs` (not `leads`)
    - HotLeadsTile.jsx contains `New jobs`, `View all jobs`, `'/dashboard/jobs'`, `'new job' : 'new jobs'`, and `Couldn't load jobs.`; no longer contains `'/dashboard/leads'` or `Hot / new leads`
    - DashboardSidebar.jsx NAV_ITEMS contains `{ href: '/dashboard/jobs', label: 'Jobs', icon: Users }`; no longer contains `'/dashboard/leads'`
    - BottomTabBar.jsx TABS contains `{ href: '/dashboard/jobs', label: 'Jobs', icon: Users }`; no longer contains `'/dashboard/leads'`
    - All 6 file names, component exports, and internal variable names are unchanged
  </acceptance_criteria>
  <done>
    - All "must be present" and "must NOT be present" assertions pass
    - All 6 files compile without lint errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Internal href + URL audit batch — update remaining /dashboard/leads to /dashboard/jobs across 10 files</name>
  <files>
    src/components/dashboard/DashboardTour.jsx,
    src/components/dashboard/AppointmentFlyout.js,
    src/app/dashboard/more/call-routing/page.js,
    src/app/dashboard/calls/page.js,
    src/app/dashboard/invoices/page.js,
    src/app/dashboard/invoices/batch-review/page.js,
    src/app/dashboard/invoices/[id]/page.js,
    src/app/dashboard/estimates/[id]/page.js,
    src/app/api/search/route.js,
    src/lib/notifications.js
  </files>
  <read_first>
    - Each of the 10 files in `<files>` — each at the line numbers identified in the `<interfaces>` block above (use the line numbers as starting points but verify the exact context before editing; line numbers may have shifted)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md (D-05, D-07, D-09)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md §"Files in Scope" (Internal link updates section)
  </read_first>
  <behavior>
    - Every `/dashboard/leads` substring across these 10 files becomes `/dashboard/jobs`
    - The Search API route's display label `'Leads'` and `type: 'leads'` field — the user-facing display label changes to `'Jobs'`; the internal `type` field key remains `'leads'` (D-10: internal symbol names stay)
    - The notification email link template uses `/dashboard/jobs` so new emails point at the canonical URL (legacy emails still work via the 308 in next.config.js)
    - DashboardTour CSS selector target updates from `[href="/dashboard/leads"]` to `[href="/dashboard/jobs"]` so the tour still finds the sidebar item
    - All API paths (`/api/leads`, `/api/leads/[id]`) anywhere in these files are PRESERVED unchanged (D-06)
    - Querystring formats (`?open=ID`, `?search=PHONE`, `?status=completed`) are preserved verbatim
  </behavior>
  <action>
    Apply per-file edits. The pattern is consistent: every literal `/dashboard/leads` becomes `/dashboard/jobs`. The Search API route additionally needs a display label update.

    **A. src/components/dashboard/DashboardTour.jsx (line ~14):**
    Change `target: '[href="/dashboard/leads"]'` to `target: '[href="/dashboard/jobs"]'`. Do NOT change the step title, body, or any other tour step.

    **B. src/components/dashboard/AppointmentFlyout.js (line ~231):**
    Change `router.push(\`/dashboard/leads?open=${linkedLead.id}\`)` to `router.push(\`/dashboard/jobs?open=${linkedLead.id}\`)`. Do NOT change the surrounding button text/icon (verify if it says "View Lead" — if so, also update to "View Job"; per CONTEXT.md D-09 user-facing copy on this surface is in scope. Search the file for the visible button label and reframe if found).

    **C. src/app/dashboard/more/call-routing/page.js (line ~343):**
    Change `router.push(\`/dashboard/leads?open=${leadId}\`)` to `router.push(\`/dashboard/jobs?open=${leadId}\`)`. Local variable name `leadId` stays per D-10.

    **D. src/app/dashboard/calls/page.js (line ~304):**
    Change `href={\`/dashboard/leads?search=${encodeURIComponent(call.from_number)}\`}` to `href={\`/dashboard/jobs?search=${encodeURIComponent(call.from_number)}\`}`. Do NOT change link text unless it says "View Lead" / "View Leads" — if so, also reframe to "Job"/"Jobs" per D-09.

    **E. src/app/dashboard/invoices/page.js (line ~264):**
    Change `href="/dashboard/leads?status=completed"` to `href="/dashboard/jobs?status=completed"`. Do NOT change link text unless it says "View Leads" — if so, reframe to "View Jobs".

    **F. src/app/dashboard/invoices/batch-review/page.js (lines ~134 and ~282):**
    Change BOTH occurrences `router.push('/dashboard/leads')` to `router.push('/dashboard/jobs')`. If the Cancel/Back button text in this page says "Back to Leads", reframe to "Back to Jobs".

    **G. src/app/dashboard/invoices/[id]/page.js (line ~491):**
    Change `href={\`/dashboard/leads?open=${invoice.lead_id}\`}` to `href={\`/dashboard/jobs?open=${invoice.lead_id}\`}`. The variable `invoice.lead_id` stays — that's a DB column name, untouched. Reframe link text "View Lead" → "View Job" if present.

    **H. src/app/dashboard/estimates/[id]/page.js (line ~645):**
    Change `href={\`/dashboard/leads?open=${estimate.lead_id}\`}` to `href={\`/dashboard/jobs?open=${estimate.lead_id}\`}`. `estimate.lead_id` stays unchanged. Reframe link text "View Lead" → "View Job" if present.

    **I. src/app/api/search/route.js (lines ~64-70):**
    Change the Leads result group:
    ```javascript
    type: 'leads',
    label: 'Leads',
    items: leadsRes.data.map((l) => ({
      ...
      href: `/dashboard/leads?open=${l.id}`,
    })),
    ```
    To:
    ```javascript
    type: 'leads',
    label: 'Jobs',
    items: leadsRes.data.map((l) => ({
      ...
      href: `/dashboard/jobs?open=${l.id}`,
    })),
    ```
    The internal `type: 'leads'` field key STAYS unchanged (D-10 — internal symbol). The user-facing `label: 'Leads'` becomes `'Jobs'`. The href changes. Do NOT change the Supabase query (`.from('leads')`) or any other backend call.

    **J. src/lib/notifications.js (line ~319):**
    Change `const dashboardLink = \`${process.env.NEXT_PUBLIC_APP_URL || 'https://localhost:3000'}/dashboard/leads\`;` to `const dashboardLink = \`${process.env.NEXT_PUBLIC_APP_URL || 'https://localhost:3000'}/dashboard/jobs\`;`. Do NOT change any email template text, recipient logic, lead/business name fields (which use the noun "lead" as a JS variable — `lead?.caller_name`, etc., per D-10 internal symbol preservation).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const{execSync}=require('child_process');const files=['src/components/dashboard/DashboardTour.jsx','src/components/dashboard/AppointmentFlyout.js','src/app/dashboard/more/call-routing/page.js','src/app/dashboard/calls/page.js','src/app/dashboard/invoices/page.js','src/app/dashboard/invoices/batch-review/page.js','src/app/dashboard/invoices/[id]/page.js','src/app/dashboard/estimates/[id]/page.js','src/app/api/search/route.js','src/lib/notifications.js'];let fail=false;for(const f of files){const s=fs.readFileSync(f,'utf8');if(s.includes('/dashboard/leads')){console.error('FAIL: '+f+' still contains /dashboard/leads');fail=true}}const search=fs.readFileSync('src/app/api/search/route.js','utf8');if(!/label:\s*'Jobs'/.test(search)){console.error('FAIL: search/route.js missing label: \\'Jobs\\'');fail=true}if(!/`\/dashboard\/jobs\\?open=/.test(search)){console.error('FAIL: search/route.js missing /dashboard/jobs href');fail=true}const notif=fs.readFileSync('src/lib/notifications.js','utf8');if(!/\/dashboard\/jobs/.test(notif)){console.error('FAIL: notifications.js missing /dashboard/jobs');fail=true}const tour=fs.readFileSync('src/components/dashboard/DashboardTour.jsx','utf8');if(!/\\[href=\"\/dashboard\/jobs\"\\]/.test(tour)){console.error('FAIL: DashboardTour selector not updated');fail=true}if(fail)process.exit(1);console.log('PASS: 10 files clean of /dashboard/leads + 4 spot checks')"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 10 files contains zero occurrences of `/dashboard/leads`
    - All instances now reference `/dashboard/jobs` with querystrings preserved verbatim (`?open=ID`, `?search=PHONE`, `?status=completed`)
    - src/app/api/search/route.js: user-facing `label: 'Jobs'` AND href template `\`/dashboard/jobs?open=${l.id}\`` AND internal `type: 'leads'` (preserved)
    - src/lib/notifications.js dashboard link template ends with `/dashboard/jobs`
    - src/components/dashboard/DashboardTour.jsx target selector is `[href="/dashboard/jobs"]`
    - No file's API call (`.from('leads')`, `/api/leads/...`, `lead_id` DB column references) was changed
    - Internal local variable names (`leadId`, `linkedLead.id`, `invoice.lead_id`, `estimate.lead_id`) are preserved
  </acceptance_criteria>
  <done>
    - All automated checks pass
    - Manual scan: any visible button text or link label that read "View Lead" / "View Leads" / "Back to Leads" has also been reframed to the Job/Jobs equivalent (per D-09 — copy reframe)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → notification email link | Owner clicks email link to dashboard. Destination URL is server-constructed from `NEXT_PUBLIC_APP_URL` env var + hardcoded path. No user input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | N/A | All 16 files in this plan | accept | Severity: N/A. Pure copy + URL string updates. No new auth, no new data fetch, no new user-input flow. The notification email link's host portion comes from `NEXT_PUBLIC_APP_URL` (already trusted env var) + hardcoded path suffix `/dashboard/jobs` (no user input). The Search API route still uses the same RLS-guarded Supabase query — only the response display label changes. No new XSS / SSRF / CSRF / open-redirect surface. |
| T-52-07 | Spoofing | DashboardTour selector | accept | The CSS selector `[href="/dashboard/jobs"]` is matched against DOM nodes the tour itself controls. No injection vector. |
</threat_model>

<verification>
- All 16 files modified by this plan compile and lint clean
- `grep -rn "/dashboard/leads" src/` returns ZERO hits across the entire src/ tree (this is the canonical phase-completion check from CONTEXT.md "code_context")
- `grep -rn "/dashboard/jobs" src/` returns hits in: DashboardSidebar.jsx, BottomTabBar.jsx, HotLeadsTile.jsx, DashboardTour.jsx, AppointmentFlyout.js, all 10 internal-link files, search/route.js, notifications.js, the moved jobs/page.js (router.replace from Plan 02), and the chatbot-knowledge files (Plan 03)
- LeadFlyout.jsx STATUS_LABELS map renders 'Scheduled' for booked (mirrors LeadStatusPills + LeadCard from Plan 01)
- All API routes (`/api/leads/*`) and DB column names (`lead_id`, `caller_name`) are unchanged
- All component file names (LeadFlyout.jsx, LeadCard.jsx, LeadStatusPills.jsx, LeadFilterBar.jsx, EmptyStateLeads.jsx, HotLeadsTile.jsx, AppointmentFlyout.js) are unchanged per RENAME-03
</verification>

<success_criteria>
- Zero `/dashboard/leads` references remain in src/ (final phase guarantee)
- Every user-facing "Lead(s)" string on the dashboard surface now reads "Job(s)"
- Sidebar + BottomTabBar nav labels read "Jobs" with hrefs to `/dashboard/jobs`
- LeadFlyout, LeadFilterBar, EmptyStateLeads, HotLeadsTile, search results, and notification email links all consistently use the new "Jobs" framing
- Internal symbols (file names, component exports, DB columns, API paths) preserved per D-10 and D-06
</success_criteria>

<output>
After completion, create `.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-04-SUMMARY.md`
</output>
</content>
</invoke>