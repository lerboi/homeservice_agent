---
phase: 52
plan: 05
type: execute
wave: 3
depends_on: [52-01, 52-02, 52-03, 52-04]
files_modified:
  - .claude/skills/dashboard-crm-system/SKILL.md
autonomous: false
requirements: [RENAME-01, RENAME-02, RENAME-03]
tags: [skills, documentation, verification, checkpoint]

must_haves:
  truths:
    - "dashboard-crm-system SKILL.md reflects the new nav label 'Jobs', URL /dashboard/jobs, and pill labels (New, Scheduled, Completed, Paid, Lost)"
    - "SKILL.md 'Last updated' date references Phase 52 with a one-line summary of the rename"
    - "Final repo-wide grep confirms zero /dashboard/leads hits in src/ (the only allowed mention is next.config.js redirect source)"
    - "User has visually verified the renamed surface in the browser (sidebar + page H1 + pill strip)"
  artifacts:
    - path: ".claude/skills/dashboard-crm-system/SKILL.md"
      provides: "Updated skill reference reflecting Phase 52 rename"
      contains: "/dashboard/jobs"
  key_links:
    - from: ".claude/skills/dashboard-crm-system/SKILL.md (skill index per CLAUDE.md rule)"
      to: "src/app/dashboard/jobs/page.js + LeadStatusPills.jsx + DashboardSidebar.jsx (the implementation)"
      via: "skill describes the renamed surface accurately so future Claude Code sessions don't drift back to 'Leads' nomenclature"
      pattern: "/dashboard/jobs"
---

<objective>
Update the `dashboard-crm-system` skill (per CLAUDE.md "Keep skills in sync" rule) to reflect Phase 52's rename, and run a final user-verification checkpoint that confirms the renamed surface looks and behaves correctly in the browser.

Purpose: Two responsibilities. (1) The skill is the architectural reference future Claude sessions read before touching dashboard code; if it still says "Leads" while the UI says "Jobs", the next phase will reintroduce stale nomenclature. CLAUDE.md mandates the post-change skill update. (2) The 22-file blast radius warrants a single human-verify checkpoint at the end so the user sees the new pill order, the visual gap before Lost, the sidebar label, and the redirect behaviour with their own eyes — automated greps catch missing strings but cannot judge "does this read naturally to a home-service owner."

Output: Updated SKILL.md with new nav label + URL + pill labels; user sign-off on the visual + functional outcome.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-01-SUMMARY.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-02-SUMMARY.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-03-SUMMARY.md
@.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-04-SUMMARY.md

<interfaces>
<!-- dashboard-crm-system skill structure (current — see SKILL.md for full file) -->
<!-- The skill's "Last updated" line lives at line 10 and lists prior-phase changes in chronological reverse order. -->
<!-- The Dashboard Page Structure block (line ~37+) lists `leads/page.js` as a child route — must update to `jobs/page.js`. -->
<!-- The File Map table (line ~70+) row for the leads page must update path + description. -->
<!-- The skill currently references "leads page" / "leads pipeline" / "Pipeline status pills" / status-pill terminology in multiple sections — those user-facing nomenclature references must reframe. -->

<!-- IMPORTANT: This skill is large (likely 100KB+). Edit ONLY the sections where the rename surfaces — do not load and rewrite the whole file. Use Edit tool with targeted find/replace patterns. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update dashboard-crm-system SKILL.md — reflect Phase 52 nav label, URL, and pill labels</name>
  <files>.claude/skills/dashboard-crm-system/SKILL.md</files>
  <read_first>
    - .claude/skills/dashboard-crm-system/SKILL.md (read in chunks — start with line 1 to ~250 to find the "Last updated" header, the Dashboard Page Structure block, and the File Map; then grep for "leads" / "/dashboard/leads" / "Leads" inside the file to locate every occurrence that needs reframing)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md (D-01 through D-13 — the canonical decisions to summarize in the skill's Last-updated note)
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md §"Copywriting Contract" (canonical labels)
    - CLAUDE.md (the project-instructions file — confirms the "Keep skills in sync" rule and the dashboard-crm-system skill's responsibility scope)
  </read_first>
  <behavior>
    - The "Last updated" line at the top of the skill is prepended (in reverse-chronological order) with a Phase 52 entry summarizing: nav rename Leads→Jobs, URL move /dashboard/leads → /dashboard/jobs (with 308 redirect), pill restructure (label Booked→Scheduled, Lost gap), and chatbot knowledge reframe
    - Every navigation/route reference in the skill that mentioned `/dashboard/leads` is updated to `/dashboard/jobs`
    - Every nav-label or section-name reference that said "Leads" (in the context of the sidebar/tab nomenclature or page H1) is updated to "Jobs"
    - References to status pill labels are updated to the new D-02 ordering (New, Scheduled, Completed, Paid, Lost) with a short note about the Lost gap
    - The Dashboard Page Structure ASCII tree updates the `leads/page.js` line to `jobs/page.js` with the new description
    - The File Map table row for the leads page updates its file path AND its Role description ("Jobs page: status pill strip...") to match the new framing
    - Internal terminology that still applies (DB column names: `leads.status`, API paths: `/api/leads/*`, component file names: `LeadCard.jsx`, `LeadFlyout.jsx`, `LeadStatusPills.jsx`, `LeadFilterBar.jsx`, `EmptyStateLeads.jsx`, `HotLeadsTile.jsx`) is PRESERVED as-is — these are technical identifiers per D-06 and D-10 and the skill must accurately describe them
  </behavior>
  <action>
    Edit `.claude/skills/dashboard-crm-system/SKILL.md` with the following targeted changes. Do NOT rewrite the whole file — use the Edit tool's find/replace per item.

    1. **Update the "Last updated" line at line 10** (or wherever that line currently lives — search for "Last updated" near the top). Prepend a Phase 52 summary so the line reads (paraphrased structure — adapt to the file's actual format):
       ```
       **Last updated**: 2026-04-16 (Phase 52 Leads → Jobs rename: dashboard sidebar + BottomTabBar nav label changed from "Leads" to "Jobs"; canonical URL renamed `/dashboard/leads` → `/dashboard/jobs` with 308 permanent redirect in next.config.js; status pill labels restructured to job-progression vernacular [New, Scheduled, Completed, Paid, Lost] with `ml-2` visual gap separating Lost from the active pipeline; LeadFlyout STATUS_LABELS, LeadCard STATUS_LABEL, LeadFilterBar aria-labels, EmptyStateLeads, HotLeadsTile copy + CTA href, search API result label, and notification email link all reframed to "Jobs"; chatbot knowledge corpus updated [/dashboard/jobs URLs + Jobs noun in 8 markdown files]; DB enum [`leads.status`: new, booked, completed, paid, lost], API routes [/api/leads/*], and component file names [LeadCard.jsx, LeadFlyout.jsx, LeadStatusPills.jsx, LeadFilterBar.jsx, EmptyStateLeads.jsx, HotLeadsTile.jsx] PRESERVED unchanged per D-06/D-10 minimal-blast-radius rule; Phase 49 categorical pill palette preserved verbatim. Previous: 2026-04-16 (Phase 49 dark mode + token migration ...)
       ```
       Keep the prior log entries intact after the new Phase 52 summary.

    2. **Find and replace ALL `/dashboard/leads` URL strings in the skill body** with `/dashboard/jobs`. Use the Edit tool to do these as separate find/replace calls — they may appear in: the Dashboard Page Structure ASCII tree, the File Map table, prose paragraphs describing the leads page, and the route-redirect note. Do NOT touch any reference to `/api/leads` (API routes unchanged) or the table name `leads` in DB-schema discussions.

    3. **Update the Dashboard Page Structure ASCII tree** (around line 37-65) — change the line:
       ```
       ├── leads/page.js              ← Status pill strip + filter bar + lead list + LeadFlyout
       ```
       To:
       ```
       ├── jobs/page.js               ← Status pill strip + filter bar + job list + LeadFlyout (file moved Phase 52)
       ```

    4. **Update the File Map table** (around line 70+) — the row for the leads page:
       ```
       | `src/app/dashboard/leads/page.js` | Leads page: status pill strip, filter bar, lead list, Realtime subscription |
       ```
       Becomes:
       ```
       | `src/app/dashboard/jobs/page.js` | Jobs page (formerly leads/page.js, moved Phase 52 with 308 redirect): status pill strip, filter bar, job list, Realtime subscription |
       ```

    5. **Update prose references to the user-facing surface** — any sentence that says "the leads page", "Leads tab", "Leads section", "leads pipeline" in a USER-FACING context (i.e. describing what owners see) reframes to the Jobs equivalent. Sentences that describe TECHNICAL implementation (e.g., "the `leads` table", "the `lead` object", "leads API endpoint", "lead lifecycle in lib/leads.js") STAY UNCHANGED — those are accurate technical descriptions.

    6. **Update the status-pill section** (search the file for "Pipeline status pills", "PIPELINE_STATUSES", or "status pill") to reflect new labels:
       - Old: pills described as `New, Booked, Completed, Paid, Lost`
       - New: pills described as `New, Scheduled, Completed, Paid, Lost` with a note: "The Lost pill renders with a `ml-2` margin to visually separate the terminal-negative state from the active pipeline. The DB enum values (`new`, `booked`, `completed`, `paid`, `lost`) are unchanged; only the display label for `booked` becomes 'Scheduled'."

    7. After all edits, run a self-check `grep -n "/dashboard/leads" .claude/skills/dashboard-crm-system/SKILL.md` — should return zero hits. Then `grep -n "/dashboard/jobs" .claude/skills/dashboard-crm-system/SKILL.md` — should return at least 3 hits (Dashboard Page Structure tree, File Map row, Last-updated summary).

    Do NOT rename the SKILL.md file. Do NOT alter sections unrelated to the rename (Architecture Overview's higher-level diagram, Realtime architecture, settings panels, etc.). Preserve all existing markdown formatting, table column widths, and section header levels.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.claude/skills/dashboard-crm-system/SKILL.md','utf8');const checks=[[/Phase 52 Leads → Jobs rename/,'Last-updated has Phase 52 summary'],[/jobs\/page\.js/,'Dashboard Page Structure has jobs/page.js'],[/src\/app\/dashboard\/jobs\/page\.js/,'File Map has jobs/page.js path'],[/New,\s*Scheduled,\s*Completed,\s*Paid,\s*Lost/,'Status pill list reflects new D-02 ordering'],[/\/dashboard\/leads/,'no stale /dashboard/leads URL in skill',true]];for(const [r,n,inverted] of checks){const m=r.test(s);if(inverted?m:!m){console.error('FAIL: '+n);process.exit(1)}}console.log('PASS: skill updated, 5 checks')"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md "Last updated" line includes a Phase 52 summary covering nav rename, URL move (with 308 redirect), pill restructure, and the preservation of DB/API/component-file-name identifiers
    - Zero occurrences of `/dashboard/leads` URL in SKILL.md (the API path `/api/leads` may still appear — that's correct and expected per D-06)
    - At least 3 occurrences of `/dashboard/jobs` in SKILL.md (Page Structure, File Map, Last-updated)
    - Dashboard Page Structure ASCII tree shows `jobs/page.js` (not `leads/page.js`)
    - File Map table row for the page references `src/app/dashboard/jobs/page.js`
    - Prose status-pill description reads `New, Scheduled, Completed, Paid, Lost` (the new D-02 ordering)
    - References to the `leads` DB table, `/api/leads/*` API routes, `lib/leads.js`, and component file names (`LeadCard.jsx`, `LeadFlyout.jsx`, etc.) are preserved unchanged — these are accurate technical descriptions
  </acceptance_criteria>
  <done>
    - All grep-based checks pass
    - SKILL.md still reads as a coherent architectural reference (no broken sentences from the targeted edits)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final repo-wide grep verification — confirm zero /dashboard/leads hits in src/ and exactly two in next.config.js</name>
  <files>(verification-only — no file modifications)</files>
  <read_first>
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-CONTEXT.md ("code_context" block — the canonical "grep -r /dashboard/leads src/ must return zero hits" guarantee)
  </read_first>
  <behavior>
    - `grep -rn "/dashboard/leads" src/` returns ZERO hits across the entire src/ tree
    - `grep -n "/dashboard/leads" next.config.js` returns exactly TWO hits (the source field of the exact-path redirect entry and the source field of the wildcard redirect entry — both legitimate)
    - `grep -rn "/dashboard/leads" .claude/skills/` returns ZERO hits
    - `grep -rn "/dashboard/jobs" src/` returns hits across the renamed surface (sanity — confirms the renames landed)
  </behavior>
  <action>
    Run the following verification commands (use the Grep tool with appropriate glob filters to keep the output readable):

    1. `Grep` tool with pattern `/dashboard/leads`, path `src/`, output_mode `count` → expect zero matching files. If any file shows a hit, the executor must investigate, fix the missed file, and re-run.

    2. `Grep` tool with pattern `/dashboard/leads`, path `next.config.js`, output_mode `content` → expect exactly two `source: '/dashboard/leads...'` lines (one exact, one wildcard) — both are the redirect entries created in Plan 02 and are the ONE allowed mention per CONTEXT.md.

    3. `Grep` tool with pattern `/dashboard/leads`, path `.claude/skills/`, output_mode `count` → expect zero hits (Plan 05 Task 1 cleared the skill).

    4. `Grep` tool with pattern `/dashboard/jobs`, path `src/`, output_mode `count` → expect hits across (at minimum): DashboardSidebar.jsx, BottomTabBar.jsx, HotLeadsTile.jsx, DashboardTour.jsx, AppointmentFlyout.js, jobs/page.js, more/call-routing/page.js, calls/page.js, invoices/page.js, invoices/batch-review/page.js, invoices/[id]/page.js, estimates/[id]/page.js, search/route.js, notifications.js, chatbot-knowledge/* — confirms Plans 02, 03, 04 all landed.

    5. Report the counts and any unexpected findings to the user as part of the checkpoint context for Task 3.

    If ANY of the four expected results does not match, this task FAILS — the executor must locate the missed file(s), apply the rename, and re-run the verification before proceeding to Task 3.
  </action>
  <verify>
    <automated>node -e "const{execSync}=require('child_process');try{const r=execSync('grep -rn \"/dashboard/leads\" src/ 2>/dev/null || true',{encoding:'utf8'});if(r.trim()){console.error('FAIL: /dashboard/leads hits in src/:\\n'+r);process.exit(1)}const r2=execSync('grep -n \"/dashboard/leads\" next.config.js 2>/dev/null || true',{encoding:'utf8'});const lines=r2.split('\\n').filter(Boolean);if(lines.length<2){console.error('FAIL: next.config.js redirect entries missing — got '+lines.length+' hits, expected 2');process.exit(1)}const r3=execSync('grep -rn \"/dashboard/leads\" .claude/skills/ 2>/dev/null || true',{encoding:'utf8'});if(r3.trim()){console.error('FAIL: /dashboard/leads hits in .claude/skills/:\\n'+r3);process.exit(1)}const r4=execSync('grep -rln \"/dashboard/jobs\" src/ 2>/dev/null || true',{encoding:'utf8'});const fileCount=r4.split('\\n').filter(Boolean).length;if(fileCount<10){console.error('FAIL: only '+fileCount+' files reference /dashboard/jobs in src/ — expected ≥10');process.exit(1)}console.log('PASS: 0 stale /dashboard/leads in src/, '+lines.length+' allowed mentions in next.config.js, 0 in skills, '+fileCount+' files reference /dashboard/jobs')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "/dashboard/leads" src/` returns zero hits
    - `grep -n "/dashboard/leads" next.config.js` returns exactly 2 hits (both inside redirect source fields)
    - `grep -r "/dashboard/leads" .claude/skills/` returns zero hits
    - `grep -rl "/dashboard/jobs" src/` returns at least 10 files (the renamed surface)
  </acceptance_criteria>
  <done>
    - All 4 grep results match expectations
    - Output captured and surfaced to the user as part of Task 3's checkpoint context
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the renamed Jobs surface</name>
  <files>(no file modifications — interactive checkpoint)</files>
  <read_first>
    - .planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-UI-SPEC.md §"Copywriting Contract" and §"Interaction Contract" (canonical strings to verify against)
    - The Task 2 verification output (counts of /dashboard/leads vs /dashboard/jobs) — surface to the user as supporting context
  </read_first>
  <action>
    This is a human-verify checkpoint task. Do not modify any file. Pause execution and present the user with the visual + functional verification protocol below. Wait for the user's "approved" signal (or issue report) before continuing.

    **What was built:** Phase 52 has reframed the dashboard's "Leads" surface to "Jobs":
    - Sidebar (desktop) and BottomTabBar (mobile) nav labels read "Jobs" (icon unchanged)
    - Canonical URL is `/dashboard/jobs`; old `/dashboard/leads` URLs (and any deep-links with querystrings) 308-redirect to the new path
    - Page H1 reads "Jobs"
    - Status pill strip reads left-to-right: New, Scheduled, Completed, Paid, [8px gap], Lost — Phase 49 categorical color palette preserved verbatim
    - LeadFlyout sheet title is "Job Details"; status dropdown shows "Scheduled" for the booked DB value; toast messages say "Job marked as Lost" / "{name} moved to {status}"
    - LeadCard status badges show "Scheduled" (was "Booked") and View aria-label says "View job from {name}"
    - HotLeadsTile on the dashboard home reads "New jobs" with a "View all jobs" CTA linking to /dashboard/jobs
    - EmptyStateLeads heading is "No jobs yet"
    - LeadFilterBar aria-labels say "Search jobs" / "Filter jobs"
    - Internal links from DashboardTour, AppointmentFlyout, calls/invoices/estimates pages, search results, and notification emails all point at /dashboard/jobs
    - Chatbot knowledge corpus updated to reference Jobs / /dashboard/jobs
    - dashboard-crm-system skill updated to reflect the new nomenclature and URL
    - DB enum (leads.status), API routes (/api/leads/*), component file names (LeadCard.jsx, LeadFlyout.jsx, etc.) PRESERVED unchanged

    **Verification protocol (present to user verbatim):** Start the dev server (`npm run dev`) if not already running, then verify each step in order:

    1. **Sidebar nav (desktop, viewport ≥768px):** Open http://localhost:3000/dashboard. Confirm the sidebar item that was previously "Leads" reads "Jobs" with the same Users icon. The href on hover should show /dashboard/jobs in the browser status bar.

    2. **Click the Jobs nav item.** The page should navigate to http://localhost:3000/dashboard/jobs (NOT /dashboard/leads). The page H1 should read "Jobs" with a count chip beside it.

    3. **Status pill strip:** Below the H1, confirm the pill strip reads left-to-right: New · Scheduled · Completed · Paid · [visible gap] · Lost. Confirm colors are unchanged from Phase 49 (New = brand accent, Scheduled = blue, Completed = stone, Paid = green, Lost = red on click). Gap before Lost is visually noticeable but small (8px / `ml-2`).

    4. **LeadCard:** If any leads exist, click into one. The card's status badge should display "Scheduled" if the lead is in `booked` state (not "Booked").

    5. **LeadFlyout:** With a lead open, confirm the sheet title says "Job Details". Open the status dropdown — confirm the option for the `booked` enum reads "Scheduled". Try moving a lead to Lost — the success toast should say "Job marked as Lost".

    6. **HotLeadsTile (dashboard home):** Navigate to http://localhost:3000/dashboard. The Hot Leads tile should now read "New jobs" with a "View all jobs" CTA. Click "View all jobs" — should land on /dashboard/jobs.

    7. **Empty state (only if a tenant has zero leads):** Skip if not applicable. Otherwise the empty state should read "No jobs yet."

    8. **Bottom tab bar (mobile, viewport <768px):** Resize the browser to ~375px or use device emulation. The bottom tab labelled with the Users icon should read "Jobs" and link to /dashboard/jobs.

    9. **Redirect verification:** In the URL bar, manually type http://localhost:3000/dashboard/leads and press Enter. The browser should land on /dashboard/jobs. Try the deep link http://localhost:3000/dashboard/leads?open=test123 — should land at /dashboard/jobs?open=test123.

    10. **Internal link smoke test:** Go to /dashboard/calls. Any "View" or caller link should point at /dashboard/jobs (check the browser status bar on hover).

    11. **Chatbot smoke test:** Open the Voco AI chatbot. Ask "Where do I see my callers?" or "How do I find leads?" — the chatbot should respond using "Jobs" terminology and any deep link should be /dashboard/jobs.

    Report any visual oddity (gap before Lost too tight or too wide, "Jobs" label feels misaligned, status dropdown still showing "Booked" anywhere, broken link landing on a 404 instead of redirect, etc.). Type "approved" if all 11 checks pass and the surface reads naturally to a home-service owner mental model.
  </action>
  <verify>
    <automated>echo "MANUAL: human-verify checkpoint — automated verification not applicable; user signal 'approved' is the verification"</automated>
  </verify>
  <acceptance_criteria>
    - User has executed all 11 verification steps in the running dev server
    - User has typed "approved" (or equivalent affirmation), OR has reported issues that must be resolved before proceeding
    - If issues reported: a follow-up gap-closure plan is created via /gsd-plan-phase --gaps and Task 3 is re-run after the gap closes
  </acceptance_criteria>
  <done>
    - User has signed off with "approved" signal
    - Phase 52 is shippable
  </done>
  <what-built>
    Phase 52 has reframed the dashboard's "Leads" surface to "Jobs":
    - Sidebar (desktop) and BottomTabBar (mobile) nav labels read "Jobs" (icon unchanged)
    - Canonical URL is `/dashboard/jobs`; old `/dashboard/leads` URLs 308-redirect to the new path
    - Page H1 reads "Jobs"
    - Status pill strip reads: New, Scheduled, Completed, Paid, [8px gap], Lost
    - LeadFlyout sheet title is "Job Details"; LeadCard badges show "Scheduled" for booked
    - HotLeadsTile reads "New jobs" / "View all jobs" with /dashboard/jobs href
    - EmptyStateLeads heading is "No jobs yet"
    - All internal hrefs and chatbot knowledge updated
    - dashboard-crm-system skill updated
    - DB enum, API routes, component file names PRESERVED unchanged
  </what-built>
  <how-to-verify>
    See the 11-step verification protocol embedded in `<action>` above. Start the dev server (`npm run dev`), then walk through each step.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan touches one skill markdown file (developer documentation, not code path) and runs read-only verifications. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | N/A | .claude/skills/dashboard-crm-system/SKILL.md | accept | Severity: N/A. Skill markdown is developer-facing reference documentation. Not loaded into the running app. No XSS / CSRF / data-flow surface. |
| T-52-09 | N/A | grep verification commands | accept | Severity: N/A. Read-only filesystem queries against local repo. No external network, no user input, no data mutation. |
</threat_model>

<verification>
- SKILL.md updated and passes the grep checks in Task 1
- Repo-wide grep verification (Task 2) confirms zero `/dashboard/leads` in src/ and .claude/skills/, exactly 2 in next.config.js (the redirect sources), and ≥10 files referencing `/dashboard/jobs` in src/
- User checkpoint (Task 3) signs off on visual + functional correctness across the 11 verification points
</verification>

<success_criteria>
- All 3 RENAME requirement IDs (RENAME-01, RENAME-02, RENAME-03) are visibly satisfied in the running dashboard
- Future Claude sessions reading the dashboard-crm-system skill will encounter accurate Jobs nomenclature, not stale Leads references
- The `dashboard-crm-system` skill update fulfills CLAUDE.md's "Keep skills in sync" mandate
- Phase 52 is shippable: no /dashboard/leads URLs in src/, no stale "Leads" copy on the renamed surface, the redirect handles legacy URLs, and the user has visually verified the result
</success_criteria>

<output>
After completion, create `.planning/phases/52-rename-leads-tab-to-jobs-and-restructure-status-pills-for-home-service-mental-model/52-05-SUMMARY.md`
</output>
</content>
</invoke>