---
phase: 60.3
slug: voice-agent-goodbye-cutoff-and-prompt-audit
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 60.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `60.3-RESEARCH.md` §"Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio 0.23 (`asyncio_mode = "auto"`) |
| **Config file** | `C:\Users\leheh\.Projects\livekit-agent\pyproject.toml` §`[tool.pytest.ini_options]` |
| **Quick run command** | `cd C:\Users\leheh\.Projects\livekit-agent && python -m pytest tests/test_goodbye_diag.py tests/test_prompt.py tests/test_prompt_customer_context.py -x -q` |
| **Full suite command** | `cd C:\Users\leheh\.Projects\livekit-agent && python -m pytest tests/ -x -q` |
| **Estimated runtime** | <5s quick · <30s full (60.2 baseline) |

---

## Sampling Rate

- **After every task commit:** Run quick command above (prompt + new goodbye-diag suite, targeted)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green AND Stream A UAT #2 evidence captured
- **Max feedback latency:** 30 seconds (full suite)

---

## Per-Task Verification Map

> Plan IDs are placeholder until planner finalizes wave assignment. Stream A is sequential (Plan 1 → Plan 2 → Plan 3); Stream B per-section patches are mostly sequential per audit ordering (D-B-04) but tail sections may parallelize.

| Task ID | Plan | Wave | Stream | Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|--------|----------|-----------|-------------------|-------------|--------|
| 60.3-01-* | 01 | 1 | A | `[goodbye_race]` log line emitted on every session close with valid JSON; `end_call_invoked_at` captured; `conversation_item_added` sets `last_text_token_at` on agent turns only; `_GoodbyeDiagHandler` captures `text_done`/`audio_done` from LogRecord `extra=`; flush is FIRST in `_on_close_async` (before post-call pipeline) | unit (mock) | `python -m pytest tests/test_goodbye_diag.py -x -q` | ❌ W0 | ⬜ pending |
| 60.3-02-* | 02 | 2 | A | Manual UAT #1: real Twilio call → `[goodbye_race]` log entry appears in Railway with all 6 timestamps populated and transcript_tail present | manual-only | owner places live call; capture in `60.3-HUMAN-UAT.md` | ❌ (manual by design) | ⬜ pending |
| 60.3-03-* | 03 | 3 | A | Fix-specific invariant (prompt clause OR end_call pre-guard) holds in unit test; UAT #2 confirms `text_done=true` OR warning absent | unit + manual UAT | added per Plan 3 (shape depends on chosen fix); owner UAT #2 | ❌ deferred to Plan 3 | ⬜ pending |
| 60.3-04-* | 04 | 4 | B | `60.3-PROMPT-AUDIT.md` exists with 16 sections × 7 dimensions scored; per-section finding tables produced | doc-deliverable | `test -f .planning/phases/60.3-.../60.3-PROMPT-AUDIT.md && grep -c "^## " <file>` ≥ 16 | ❌ W0 | ⬜ pending |
| 60.3-05+ | 05+ | 5+ | B | Per-section patch invariants: positive assertion ("section produces phrasing X") + negative assertion ("section does NOT produce anti-pattern Y", 60.2 inverted-assertion pattern); locale parity (`es != en`, non-empty, contains expected Spanish marker) | unit | `python -m pytest tests/test_prompt.py -x -q` (existing file) + per-plan new tests | ❌ W0 per plan | ⬜ pending |
| 60.3-final-UAT | last | last | B | Cumulative Stream B changes: no caller-experience regression; booking path still green; no new `_SegmentSynchronizerImpl` warnings at tool boundaries | manual-only | owner UAT call after final Stream B plan | ❌ (manual by design) | ⬜ pending |
| Regression | every | every | A+B | 101+/N suite green (101 baseline + N new tests added); 60.2 Plan 05 inverted assertions remain green | integration | full suite command above | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Sentinel Signals (Nyquist gate)

**Stream A — goodbye cutoff**
1. `[goodbye_race]` log entry present on **every** call after Plan 1 lands (baseline sampling per CONTEXT D-A-03).
2. `_SegmentSynchronizerImpl.playback_finished called before text/audio input is done` warning:
   - Pre-fix baseline: ≥1 occurrence per call with goodbye (matches 60.2 UAT)
   - Post-fix target: 0 occurrences on happy-path calls, OR occurrences with `text_done=true` (benign session-close per 60.2-RESEARCH.md)
3. `playback_finished_at - end_call_invoked_at` latency measured on post-fix UAT call.
4. Caller transcript: no mid-word cutoff of "Thank you for calling [X]" pattern.

**Stream B — prompt audit**
5. `tests/` suite: 101+/N passing (101 baseline + N new tests, all green).
6. `test_prompt.py` 60.2 Plan 05 inverted assertions remain green across all Stream B commits.
7. `60.3-PROMPT-AUDIT.md` exists with all 16 sections scored on 7 dimensions.
8. Per-section patch commits: one git commit per patch, each independently revertable.
9. Cumulative-change UAT: no regression in `server cancelled tool calls` count, booking path still green.

**Cross-stream**
10. `voice-call-architecture` SKILL.md updated at phase close (D-X-02 success criterion).
11. Re-evaluation note (D-A-03 instrumentation dial-back trigger) captured in `60.3-SUMMARY.md`.

---

## Wave 0 Requirements

- [ ] `tests/test_goodbye_diag.py` — NEW: 5–7 unit tests for Stream A instrumentation (hook points, log schema, flush ordering, handler cleanup)
- [ ] `tests/conftest.py` — extend existing fixtures: `mock_agent_session` with `output.audio.capture_frame = AsyncMock()`; `mock_diag_record` producing fresh `[{}]` list
- [ ] Per Stream B plan: test additions (one file or one function per patch; planner picks granularity — `tests/test_prompt_<section>.py` OR aggregated extension to `tests/test_prompt.py`)
- [ ] `60.3-PROMPT-AUDIT.md` — NEW: 16-section × 7-dimension scoring table + per-section findings (Plan 4 deliverable)
- [ ] `60.3-STREAM-A-ANALYSIS.md` — NEW: Plan 2 deliverable; 2–3 paragraph decision doc citing UAT #1 evidence and fix selection
- [ ] No new framework install — pytest 8 + pytest-asyncio already pinned

---

## Manual-Only Verifications

| Behavior | Stream | Why Manual | Test Instructions |
|----------|--------|------------|-------------------|
| Goodbye-cutoff race surfaces in production audio path | A | Voice race only reproduces over real LiveKit + Twilio + Gemini Live; mocks cannot reproduce `_SegmentSynchronizerImpl.playback_finished` timing | After Plan 1 deploy: owner places ≥1 Twilio call ending in `end_call`. Capture: Railway log entry for `[goodbye_race]`, audio recording (or owner's perceived caller experience), Sentry breadcrumb if event fires. Write evidence to `60.3-HUMAN-UAT.md`. |
| Goodbye fix verified post-deploy | A | Same — voice race signal | After Plan 3 deploy: owner places ≥1 Twilio call. Verify `text_done=true` in `[goodbye_race]` log OR warning absent; verify caller hears complete farewell. |
| Stream B cumulative prompt change does not regress caller experience | B | Caller perception (pacing, naturalness, anti-hallucination on time/address readback) is not unit-testable | After final Stream B plan deploy: owner places ≥2 calls — one new-caller booking, one repeat-caller. Verify booking confirmation reads back time/address correctly; no chat-LLM artifacts; barge-in handling intact. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (Stream A Plan 2 is manual-by-design — flagged in Manual-Only table)
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (full suite, 60.2 baseline)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
