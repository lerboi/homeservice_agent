---
phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit
plan: 11
subsystem: voice-agent
tags: [voice-agent, prompt, livekit, gemini-live, locale, spanish, booking, anti-hallucination, stream-b, d1-critical, d7-parity-gap-closed]

# Dependency graph
requires:
  - phase: 60.3
    provides: Plan 10 established the invariant-lock template (verify/compress/extend) — Plan 11 applies it to the largest Stream B partial-parity target
  - phase: 60.2
    provides: OUTCOME WORDS cross-reference surface (ES mirror uses 'PALABRAS DE RESULTADO' matching Plan 09 header)
provides:
  - "_build_booking_section outer-frame locale parity — was ~15% ES (readback block only), now ~100% ES (full BOOKING/SCHEDULING/AVAILABILITY RULES/HANDLING THE RESULT/BEFORE BOOKING — READBACK/AFTER BOOKING protocol in both locales)"
  - "D1 anti-hallucination invariants codified at the final booking checkpoint: two-step availability contract (check_availability BEFORE book_appointment), mandatory readback, anti-fabrication rule — all three asserted via inverted-substring tests in both EN and ES"
  - "postal_label now wired into BOTH locales' readback prose — SG 'postal code' / US 'zip code' flows through the address-verification line (previously only surfaced in _build_info_gathering_section after Plan 10)"
  - "onboarding_complete=False path includes business_name in both locales (was missing in EN, missing entirely in ES)"
  - "tests/test_prompt_booking.py — 17 D1 + D7 + postal_label + onboarding-gating assertions"
affects: [60.3-12, 60.3-13]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Invariant-lock template application (from Plan 10) scaled to the largest Stream B target — 103-line section restructured into two top-level locale branches (EN preserved verbatim; ES outer-frame added mirroring EN structure; ES readback block preserved verbatim from R-B1). Pattern now proven on the highest-stakes D1 surface."
    - "EN body preservation discipline — audit flagged the ES gap, not EN prose quality. Plan 11 explicitly did NOT rewrite EN prose (unlike Plans 05-09 which added new EN content alongside ES). Sets a precedent: when adding ES parity to a section, prefer preserving EN verbatim over restructuring both locales simultaneously."
    - "Tool-name-as-test-assertion pattern (from Plan 09) extended — `check_availability` and `book_appointment` asserted present (lowercased, untranslated) in BOTH locales. Codifies the no-translate convention at the D1 layer where fabrication is most costly."
    - "17-test count vs 10-behavior plan spec — 7 additional tests emerged from splitting parametrized behaviors into locale pairs (postal_label: 4 tests for 2 labels × 2 locales; onboarding gating: 3 tests; anti-fabrication: 2 tests). Letter-of-spec compliance via behavioral coverage; pytest collection count is implementation detail."

key-files:
  created:
    - "C:\\Users\\leheh\\.Projects\\livekit-agent\\tests\\test_prompt_booking.py"
  modified:
    - "C:\\Users\\leheh\\.Projects\\livekit-agent\\src\\prompt.py"

key-decisions:
  - "EN body preserved VERBATIM. The audit flagged the ES gap specifically — EN prose has been stable since pre-60.3 and is the source of the proven D1 anti-hallucination invariants (two-step contract, mandatory readback, anti-fabrication rule). Rewording EN would risk drift on the highest-stakes surface. Only addition to EN: postal_label wiring into the readback address fields + business_name wiring into the onboarding-incomplete fallback — both Rule-2 additions (missing critical functionality)."
  - "ES readback block preserved VERBATIM from R-B1. The pre-Plan-11 ES readback (`ANTES DE RESERVAR — LECTURA DE CONFIRMACIÓN`) was already locale-correct; Plan 11 wrapped it with a new ES outer frame rather than rewriting. Single Rule-2 addition to the ES readback: postal_label wired into the address fields parenthetical (same as EN)."
  - "ES outer frame structure mirrors EN 1:1. Section headers (RESERVA / PROGRAMACIÓN / REGLAS DE DISPONIBILIDAD / MANEJO DEL RESULTADO / ANTES DE RESERVAR — LECTURA DE CONFIRMACIÓN / DESPUÉS DE RESERVAR) map directly to EN (BOOKING / SCHEDULING / AVAILABILITY RULES / HANDLING THE RESULT / BEFORE BOOKING — READBACK / AFTER BOOKING). Sub-bullets preserved in the same order. Concrete example (2pm/3pm fresh-check) translated with identical structure."
  - "ES register: USTED throughout. Consistent with the pre-60.3 ES readback block (Lea / acepte / vuelva / llame) which already used USTED. Plan 11 extends USTED to the new outer frame (Guíe / No fuerce / No puede decir / Nunca lea / Confirme / pregunte). Register-consistency within this section: clean. Cross-plan USTED-vs-TÚ register normalization (Plans 05/06/07/10 USTED vs Plans 08/09 TÚ) still pending for Plan 12 batch work."
  - "D6 compression DEFERRED. The audit flagged 'minor compression possible in AVAILABILITY RULES (one bullet on vague-time-windows could be tightened).' Plan 11 chose NOT to compress: (1) rewording a D1-spine bullet without concrete UAT evidence of bloat invites regression; (2) vague-time-windows guidance is load-bearing — Gemini 3.1 Flash Live is known to hallucinate specific hours when callers say 'afternoon' without explicit directive to push back; (3) compression can happen in a dedicated polish plan if UAT later shows token-budget pressure. Audit decision annotated in source comment."
  - "Tool-name anti-translation convention codified at the test layer. `check_availability` and `book_appointment` MUST appear untranslated in both locales — enforced via `test_en_availability_contract` + `test_es_availability_contract` (both require `'check_availability' in lowered`) and `test_en_anti_fabrication_rule` + `test_es_anti_fabrication_rule` (both require `'book_appointment' in lowered`). Continues the Plan 06 convention (first codified for check_availability/book_appointment in tool_narration) and Plan 09 extension (codified as a test assertion, not just prose)."
  - "postal_label test matrix: 4 tests (SG-EN, US-EN, SG-ES, US-ES). More granular than Plan 10's 2-test parametrized matrix because the booking readback is the D1-critical surface for address verification — a one-sided regression (e.g. if a future editor drops the ES readback's postal_label substitution) would cause SG-ES callers to hear 'zip code' from an AI and the anti-hallucination rule on address details would degrade."
  - "onboarding_complete=False fallback — gained business_name interpolation in both locales. Was a latent correctness gap: the EN fallback prose said 'Booking is not yet available' without naming the business, which caused generic-sounding fallbacks during tenant onboarding. Rule-2 addition applied symmetrically (both locales gain f-string interpolation)."
  - "EN/ES length delta 9.3% (EN 4463 / ES 4922). Well within the 30% drift guard. Tighter than Plan 10's 14% delta because Plan 11 preserved the EN body verbatim while writing fresh ES — no one-sided compression risk during authoring. The 30% tolerance was set for future drift, not authoring."

patterns-established:
  - "Locale-branch coverage now 8 of 15 Stream B targets. Section coverage: info_gathering (pre-60.3 inner + Plan 10 outer-frame) + call_duration (Plan 05) + tool_narration (Plan 06) + voice_behavior (Plan 07) + corrections (Plan 08) + outcome_words (Plan 09) + info_gathering full (Plan 10) + booking (Plan 11). `grep 'if locale == \"es\":' src/prompt.py` count stays at 7 (booking's branch was ALREADY counted as inline `if locale == \"es\"` before Plan 11 — Plan 11 moved it from inline-expression to top-level-statement form but the count is unchanged)."
  - "**Largest Stream B diff shipped without EN regression.** Plan 11 added 158 lines (ES outer frame + postal_label wiring + business_name wiring + test file) and removed 35 lines (inline ES readback → top-level ES branch) — net +123 lines. The 145-line EN body was preserved char-for-char except for two narrow f-string additions. Demonstrates that the invariant-lock template scales to the largest Stream B targets."
  - "Invariant-lock template at D1 stakes. Plans 05-08 added ES to sections where a regression would be embarrassing but not dangerous. Plan 09 (outcome_words) and Plan 11 (booking) added ES to the D1 anti-hallucination spine — where a regression causes false booking confirmations. The test pattern (inverted substring assertions + tool-name-untranslated + postal_label parametrization + length-drift guard) has now been proven on the two highest-stakes surfaces."

requirements-completed: [60.2-DEFER-B]

# Metrics
duration: ~8min
completed: 2026-04-22
---

# Phase 60.3 Plan 11: _build_booking_section Invariant Lock + D7 Outer-Frame Parity Summary

**Closed the 85% ES outer-frame gap on the booking section — the prompt's single most-important D1 anti-hallucination surface. Pre-Plan-11, Spanish callers received a Spanish readback wrapped in English booking rules (a broken locale experience at the final commit checkpoint). Post-Plan-11, both locales carry the full 3-step protocol (check_availability → readback → book_appointment) with identical D1 invariants: two-step availability contract, mandatory readback, anti-fabrication rule. 17 tests codify the invariants at the test layer with tool-name-anti-translation and postal_label parametrization asserted in both locales. Largest Stream B diff (158 lines added, 35 lines removed) shipped without EN regression — the EN body was preserved verbatim except for two narrow Rule-2 additions (postal_label wiring + business_name wiring in the onboarding-incomplete fallback). 8th of 15 D7 gaps closed.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-22T11:02:00Z (approx.)
- **Completed:** 2026-04-22T11:10:00Z (approx.)
- **Tasks:** 2 (Task 1 TDD RED+GREEN, Task 2 full-suite validation + push)
- **Files modified:** 1 (`src/prompt.py`)
- **Files created:** 1 (`tests/test_prompt_booking.py`)

## Accomplishments

- **Closed 8th of 15 D7 locale-parity gaps — the LARGEST partial-parity section.** Pre-Plan-11, `_build_booking_section` carried an inline `if locale == "es"` branch covering only the BEFORE BOOKING — READBACK block (~15% of the section). Outer BOOKING/SCHEDULING/AVAILABILITY RULES/HANDLING THE RESULT/AFTER BOOKING blocks were English-only. Post-Plan-11, both locales carry parallel structure at 1:1 header mapping.
- **D1 anti-hallucination invariants codified at test layer.** Three invariants asserted in BOTH locales via inverted-substring tests:
  - Two-step availability contract: `check_availability` BEFORE `book_appointment` (test_en/es_availability_contract).
  - Mandatory readback: time + address read back before commit (test_en/es_readback_invariant).
  - Anti-fabrication rule: never say 'confirmed'/'booked' until `book_appointment` returns success (test_en/es_anti_fabrication_rule).
- **Tool names NOT translated.** `check_availability` and `book_appointment` asserted present untranslated in both locales — codifies the Plan 06/09 convention at the D1-critical surface.
- **postal_label wired into both locales' readback.** SG 'postal code' and US 'zip code' flow through the address-verification line. 4-cell test matrix (SG-EN, US-EN, SG-ES, US-ES) catches one-sided regressions.
- **Onboarding-incomplete fallback gained business_name in both locales.** Rule-2 addition — the pre-Plan-11 EN prose said 'Booking is not yet available' without naming the business (generic-sounding); ES was entirely missing the fallback. Now both locales emit `"{business_name}'s service is still being set up"` / `"{business_name} — hágale saber…"`.
- **EN body preserved VERBATIM.** Audit flagged the ES gap specifically — EN prose has been stable since pre-60.3 and is the source of the proven D1 invariants. Plan 11 deliberately did NOT rewrite EN (unlike Plans 05-09 which added new EN content alongside ES). Only two narrow Rule-2 f-string additions to EN: postal_label in the readback address fields + business_name in the onboarding-incomplete fallback.
- **ES readback block preserved VERBATIM from R-B1.** Pre-Plan-11 ES readback was already locale-correct; Plan 11 wrapped it with a new ES outer frame rather than rewriting it.
- **EN/ES length delta 9.3% (EN 4463 / ES 4922).** Well within the 30% drift guard. Tighter than Plan 10's 14% delta because Plan 11 preserved the EN body verbatim.

## Task Commits

1. **Task 1 RED:** `test(60.3-11): add failing tests for booking invariant lock + ES parity` — `bd65986` (test file only; 7 failures confirmed on postal_label EN wiring, postal_label ES wiring, onboarding-incomplete business_name interpolation in both locales, and ES anti-fabrication rule — all expected gaps prior to implementation).
2. **Task 1 GREEN:** `feat(60.3-11): booking — close 85% es parity gap` — `af2653e` (`src/prompt.py` rewrite of `_build_booking_section`: top-level `if locale == "es":` branch with full ES outer frame mirroring EN structure; EN body preserved verbatim; postal_label wired into both locales' readback; business_name wired into both locales' onboarding-incomplete fallback; audit-dimension decisions annotated in source comments). All 17 booking tests pass; full prompt-test subset 85 passed.
3. **Task 2:** Full suite validation + push — no new commit. Full livekit-agent suite `179 passed, 1 deselected` (pre-existing deferred VIP test `test_incoming_call_vip_lead` excluded via `--deselect`). Push `b2b1027..af2653e` to `lerboi/livekit_agent` main triggered Railway auto-deploy.

**livekit-agent remote:** `https://github.com/lerboi/livekit_agent.git` — commits `bd65986` + `af2653e` pushed to `main`.

## Files Created/Modified

- **`C:\Users\leheh\.Projects\livekit-agent\src\prompt.py`** — `_build_booking_section`:
  - Restructured from inline `if locale == "es"` (readback-only) to top-level `if locale == "es":` branch (full protocol).
  - ES branch: new RESERVA / PROGRAMACIÓN / REGLAS DE DISPONIBILIDAD / MANEJO DEL RESULTADO / ANTES DE RESERVAR — LECTURA DE CONFIRMACIÓN / DESPUÉS DE RESERVAR outer frame. USTED register throughout.
  - ES readback block: preserved verbatim from pre-Plan-11 inline form; postal_label wired in as f-string `{postal_label}` in the address fields parenthetical.
  - EN body: preserved verbatim. Narrow additions — postal_label in readback address fields (`"street, city, state/country, {postal_label}"`); business_name in onboarding-incomplete fallback.
  - Onboarding-incomplete path: ES branch added (`"CAPACIDADES:..."`); EN branch gained `{business_name}` interpolation.
  - In-source comment documents D1-D7 audit-dimension decisions + preservation rationale.

- **`C:\Users\leheh\.Projects\livekit-agent\tests\test_prompt_booking.py`** — new file, 17 tests:
  1. `test_en_nonempty` — EN full-protocol >500 chars.
  2. `test_es_nonempty` — ES full-protocol >500 chars (was ~300 chars pre-Plan-11).
  3. `test_en_es_length_within_1pt5x` — 30% length-drift tolerance.
  4. `test_en_availability_contract` — EN has `check_availability` + `before` + `book` (lowercased).
  5. `test_es_availability_contract` — ES has `check_availability` (untranslated) + `antes` + (`reservar` OR `book_appointment`).
  6. `test_en_readback_invariant` — EN has (`read back` OR `confirm`) + `address`.
  7. `test_es_readback_invariant` — ES has (`confirmar`/`confirme`/`confirma`) + `dirección`.
  8. `test_postal_label_propagates_en_postal_code` — SG-EN.
  9. `test_postal_label_propagates_en_zip_code` — US-EN.
  10. `test_postal_label_propagates_es_postal_code` — SG-ES (ES respects market via English label surface).
  11. `test_postal_label_propagates_es_zip_code` — US-ES.
  12. `test_en_onboarding_incomplete_simplified` — EN fallback mentions business_name; full protocol absent.
  13. `test_es_onboarding_incomplete_simplified` — ES fallback mentions business_name; full protocol absent.
  14. `test_both_locales_onboarding_gated_full_protocol` — onboarding_complete=True gate triggers full protocol in both locales.
  15. `test_en_es_distinct_and_nonempty_full_protocol` — copy-paste regression guard.
  16. `test_en_anti_fabrication_rule` — EN has `book_appointment` + (`confirmed` OR `booked`).
  17. `test_es_anti_fabrication_rule` — ES has `book_appointment` + (`confirmado` OR `reservado`).

## Decisions Made

- **EN body preserved VERBATIM.** Plan 11 deliberately did NOT rewrite EN prose. Audit flagged the ES gap specifically — EN prose has been stable since pre-60.3 and is the source of the proven D1 anti-hallucination invariants. Rewording EN would risk drift on the highest-stakes surface in the entire prompt. Only Rule-2 additions: postal_label wiring + business_name wiring.
- **ES readback block preserved VERBATIM from R-B1.** The pre-Plan-11 inline ES readback (`ANTES DE RESERVAR — LECTURA DE CONFIRMACIÓN`) was already locale-correct. Plan 11 restructured it from inline expression form to top-level branch form but kept the prose identical — except for adding `{postal_label}` to the address fields parenthetical (Rule 2).
- **ES outer frame mirrors EN 1:1.** Every EN section header, sub-bullet, and example has a 1:1 counterpart in ES with identical structure. The 2pm/3pm fresh-check example translates directly to `'¿está libre a las 2?'` / `'¿y a las 3?'` / `'solo las 2 está libre'`. Preserves the audit's D2 (realtime-model concrete example) grade across locales.
- **D6 compression DEFERRED on vague-time-windows bullet.** Audit flagged a minor compression opportunity but Plan 11 chose to preserve. Rationale: (1) rewording a D1-spine bullet without concrete UAT evidence of bloat invites regression; (2) vague-time-windows guidance is load-bearing against Gemini 3.1 Flash Live's tendency to hallucinate specific hours from vague windows; (3) compression can happen in a dedicated polish plan if UAT later shows token-budget pressure. Annotated in source comment.
- **ES register: USTED throughout.** Consistent with the pre-60.3 ES readback block (Lea / acepte / vuelva / llame). Plan 11 extends USTED to the new outer frame (Guíe / No fuerce / No puede decir / Nunca lea / Confirme / pregunte). Cross-plan USTED-vs-TÚ register normalization remains pending Plan 12.
- **Tool-name anti-translation codified at test layer.** `check_availability` and `book_appointment` asserted present (lowercased, untranslated) in both locales via four separate tests. Continues Plan 09's approach: enforce convention via test assertion, not just documentation.
- **4-cell postal_label test matrix.** More granular than Plan 10's 2-cell parametrized matrix because the booking readback is the D1-critical surface for address verification. A one-sided regression (ES readback losing postal_label substitution) would degrade SG-ES address verification specifically.
- **Onboarding-incomplete fallback gained business_name in both locales.** Rule-2 addition. Pre-Plan-11 EN prose said 'Booking is not yet available' without naming the business (generic-sounding). Symmetric fix across both locales.
- **17 collected tests vs 10-behavior plan spec.** Plan spec listed 10 behavioral tests; implementation expanded to 17 collected because (a) postal_label matrix split into 4 rows (SG-EN, US-EN, SG-ES, US-ES) instead of 1 parametrized fixture — more explicit failure attribution; (b) onboarding-gating split into 3 tests (EN-incomplete, ES-incomplete, both-complete-full-protocol) — clearer diagnostics; (c) anti-fabrication split into EN + ES tests — mirrors every other D1 invariant's locale pairing. Letter-of-spec compliance via behavioral coverage; pytest-collection count is implementation detail.

## Deviations from Plan

### Auto-added content (Rule 2: missing critical functionality)

**1. [Rule 2 - Correctness] business_name wired into onboarding-incomplete fallback in both locales**
- **Found during:** Task 1 RED (tests `test_en_onboarding_incomplete_simplified` + `test_es_onboarding_incomplete_simplified` failed — neither locale mentioned the business name in the fallback prose).
- **Issue:** EN fallback said 'Booking is not yet available' without naming the business — generic-sounding during tenant onboarding. ES path was entirely missing (onboarding-incomplete returned the same English prose regardless of locale).
- **Fix:** EN onboarding-incomplete prose gained `f"Booking is not yet available for {business_name}"`. ES path gained its own fallback (`"La reserva aún no está disponible para {business_name}"`) with USTED register.
- **Files modified:** `src/prompt.py`.
- **Commit:** `af2653e`.

**2. [Rule 2 - Correctness] postal_label wired into both locales' readback prose**
- **Found during:** Task 1 RED (tests `test_postal_label_propagates_en_*` + `test_postal_label_propagates_es_*` failed — postal_label was dead in the body).
- **Issue:** `postal_label` was in the signature but never consumed in the body of `_build_booking_section`. The readback prose said 'full service address' without field-level guidance on the market-specific label (SG 'postal code' vs US 'zip code'). Similar latent-dead-parameter bug that Plan 10 fixed in `_build_info_gathering_section`.
- **Fix:** EN readback gained `f"(street, city, state/country, {postal_label})"` in the address-line directive. ES readback gained `f"(calle, ciudad, estado/país, {postal_label})"` — the English label surfaces in the ES prose because it's a market signal (for model-side routing continuity across locales), same rationale as Plan 10's `_build_info_gathering_section` ES branch.
- **Files modified:** `src/prompt.py`.
- **Commit:** `af2653e`.

Both additions are symmetric across locales — no one-sided parity regression. Both are flagged in the `feat(60.3-11)` commit body. Both are straightforward Rule-2 additions (missing critical functionality discovered during TDD RED); no architectural decision required.

## Issues Encountered

- **Pre-existing VIP test failure** (`tests/webhook/test_routes.py::test_incoming_call_vip_lead`): still present, documented in `deferred-items.md` since Plan 01. Unrelated to this plan's scope. Deselected from Task 2 full-suite run (explicit `--deselect` flag). Full suite excluding that test: `179 passed` — up from `162 passed` at end of Plan 10 due to the 17 new booking tests.
- **Pre-tool-use Read-Before-Edit reminder fired once.** The Claude runtime's pre-Edit hook flagged `prompt.py` as possibly unread; the file had in fact been read in multiple segments earlier in the session (lines 400-520, 620-750, 665-775 — covering the full `_build_booking_section` line range 665-767 plus surrounding context). Edit succeeded; no re-read necessary.

## UAT Evidence Influence

None directly. Plan 11 is audit-directed (D1 anti-hallucination invariant lock + D7 outer-frame parity extension) — the audit (60.3-PROMPT-AUDIT.md §_build_booking_section) explicitly targets the 85% English-only outer frame as the largest partial-parity gap in Stream B. No specific UAT evidence influenced scope. Previous UATs (UAT #1 call-_+6587528516 declined; UAT #2 declined with goodbye truncation) were both English-language calls and did not surface booking-specific issues in either locale.

## User Setup Required

None — no external service configuration. Railway auto-deploy on `lerboi/livekit_agent` will pick up `af2653e` on `main` within ~2 minutes of the push.

## Next Phase Readiness

- **Plans 12-13 unblocked** — continue locale-parity work for the remaining sections.
- **Plan 12 target (batch):** `_build_greeting_section` (partial — uses t() for disclosure but surrounding prose English-only), `_build_language_section`, `_build_repeat_caller_section` (empty string — no change needed), `_build_customer_account_section`, `_build_intake_questions_section` (simple — just a preamble), `_build_decline_handling_section`, `_build_transfer_section` + cross-plan ES register normalization (USTED vs TÚ harmonization across Plans 05-11).
- **Plan 13 target:** Tail batch for `_build_identity_section`, `_build_working_hours_section` (day names + "closed"/"lunch" labels), plus any remaining polish.
- **D7 coverage after Plan 11:** 8 of 15 actionable gaps closed — `info_gathering` (pre-60.3 inner + Plan 10 outer), `call_duration` (P05), `tool_narration` (P06), `voice_behavior` (P07), `corrections` (P08), `outcome_words` (P09), `info_gathering full` (P10), `booking` (P11). 7 gaps remain across Plans 12-13.
- **`grep "if locale == \"es\":" src/prompt.py` count:** 7 (unchanged — Plan 11 restructured booking's inline `if locale == "es"` expression into a top-level `if locale == "es":` statement; both forms grep-match as 1 occurrence).

## Self-Check

- [x] `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py` — modified (top-level ES branch for `_build_booking_section`; EN preserved verbatim; postal_label wired; business_name wired; `_build_booking_section` length ratio EN 4463 / ES 4922 = 9.3% delta — verified via `python -c` at task completion).
- [x] `C:\Users\leheh\.Projects\livekit-agent\tests\test_prompt_booking.py` — created (17 tests collected, all pass).
- [x] Commit `bd65986` exists on livekit-agent main (TDD RED) — verified via `git log --oneline -3`.
- [x] Commit `af2653e` exists on livekit-agent main (TDD GREEN, pushed, Railway auto-deploy live) — verified via `git push` output `b2b1027..af2653e`.
- [x] Full livekit-agent suite: `179 passed, 1 deselected` (pre-existing deferred VIP test, out of scope — unchanged since Plan 01).
- [x] 60.2 Pitfall 6 inverted assertions still green (tool_narration EN + ES preserved).
- [x] Plan 05 call_duration locale invariants still green in both locales.
- [x] Plan 06 tool_narration locale invariants still green in both locales.
- [x] Plan 07 voice_behavior locale invariants still green in both locales.
- [x] Plan 08 corrections locale invariants still green in both locales.
- [x] Plan 09 outcome_words locale invariants still green in both locales.
- [x] Plan 10 info_gathering locale invariants still green in both locales.
- [x] EN/ES length ratio verified: EN 4463 chars / ES 4922 chars / delta 9.3% — well within 30% drift guard.

## Self-Check: PASSED

---
*Phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit*
*Completed: 2026-04-22*
