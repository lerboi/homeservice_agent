---
phase: 60.3
plan: 13
type: execute
wave: 13
depends_on: [12]
files_modified:
  - .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md
  - .claude/skills/voice-call-architecture/SKILL.md
  - .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - 60.2-DEFER-A
  - 60.2-DEFER-B
tags: [voice-agent, uat, skill-update, summary, stream-b, phase-close]

must_haves:
  truths:
    - "Owner has placed cumulative UAT call after all Stream B prompt changes"
    - "UAT evidence appended to 60.3-HUMAN-UAT.md as Stream B cumulative section with PASS/PARTIAL/FAIL verdict"
    - "voice-call-architecture SKILL.md is updated to reflect 60.3 end state: goodbye fix narrative, prompt audit cross-link, locale-parity note, instrumentation dial-back trigger"
    - "60.3-SUMMARY.md exists and captures all 8 D-X-02 success criteria outcomes with links to evidence"
    - "Instrumentation dial-back re-evaluation trigger (D-A-03) is captured as a non-blocking follow-up note in 60.3-SUMMARY.md"
    - "ROADMAP.md reflects Phase 60.3 as closed with plan count = 13"
  artifacts:
    - path: ".planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md"
      provides: "Stream B cumulative UAT section appended"
      contains: "Stream B — UAT (cumulative)"
    - path: ".claude/skills/voice-call-architecture/SKILL.md"
      provides: "60.3 goodbye-fix narrative + audit cross-link + locale-parity note"
      contains: "Phase 60.3"
    - path: ".planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md"
      provides: "Phase rollup covering both streams, 8 D-X-02 criteria, follow-ups"
      contains: "Phase 60.3"
  key_links:
    - from: "60.3-SUMMARY.md"
      to: "60.3-HUMAN-UAT.md + 60.3-PROMPT-AUDIT.md + 60.3-STREAM-A-ANALYSIS.md + all 13 plan summaries"
      via: "cross-reference links"
      pattern: "60.3-"
    - from: "SKILL.md"
      to: "Phase 60.3 deliverables"
      via: "additive update; does NOT churn existing §1 pin rationale, §3 Fix G, §5 Fix H revert"
      pattern: "Phase 60.3"
---

<objective>
Close Phase 60.3 with: (a) cumulative UAT call verifying no caller-experience regression from Stream B; (b) additive update to `voice-call-architecture` SKILL.md (per D-X-02 + CLAUDE.md skill-update protocol); (c) phase SUMMARY rollup covering both streams; (d) ROADMAP update.

Purpose: the final D-X-02 success criteria — SKILL.md updated, SUMMARY written, instrumentation dial-back trigger captured — require this wrap-up. Without it, the phase is technically incomplete even if all code changes landed.

Output: 4 markdown files updated or created; Phase 60.3 closed.
</objective>

<execution_context>
Hybrid plan: Task 1 is manual UAT (checkpoint:human-verify); Tasks 2-4 are autonomous writeups the executor completes after the UAT evidence is captured.

**SKILL.md update discipline (CLAUDE.md protocol):** read the full SKILL.md first, identify §1 pin rationale, §3 Fix G, §5 Fix H revert — these ALREADY exist and do NOT get rewritten. The 60.3 update is ADDITIVE: a new subsection narrating the goodbye fix (Branch P or Branch G per Plan 3), a cross-link to `60.3-PROMPT-AUDIT.md`, and a note on locale parity. Targeted insertion, not full-file rewrite.
</execution_context>

<context>
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-CONTEXT.md
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md
@.claude/skills/voice-call-architecture/SKILL.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cumulative Stream B UAT — owner places 2 calls (en + es)</name>
  <read_first>
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` (existing Stream A UAT #1 + UAT #2 sections)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` (what was changed)
    - Applies D-B-06 (UAT-driven verification), D-X-02 (final UAT success criterion).
  </read_first>
  <action>
    This is a checkpoint:human-verify task — the executor pauses and the owner performs the manual UAT described in <how-to-verify>. See that block for the full owner procedure. The executor does not place the call; the executor waits for the resume signal and then proceeds to the next task once the acceptance_criteria are satisfied.
  </action>
  <what-built>
    Stream B Plans 5-12 shipped locale-aware updates to 15 of 16 prompt sections (repeat_caller empty). All locale-aware sections have symmetric en + es prose. Stream A goodbye fix (Branch P or G) from Plan 3 remains deployed. This UAT confirms the cumulative prompt changes don't regress caller experience.
  </what-built>
  <how-to-verify>
    **Owner steps:**

    1. Confirm Railway deploy from Plan 12 Task 2 is live.

    2. **Place Call 1 — English persona, new caller, booking path:**
       - Caller: "Hi, I need a plumber for a leak under the sink."
       - Walk through: name → address → problem → time confirmation → read back → book_appointment → goodbye.
       - Observations:
         - Did agent speak natural EN throughout?
         - Did agent read back time + address before booking (D1 anti-hallucination)?
         - Any mid-word cutoff at goodbye (Stream A regression check)?
         - Any chat-LLM artifacts ("as an AI", "let me think")?

    3. **Place Call 2 — Spanish persona, repeat caller OR new caller:**
       - Caller starts in Spanish: "Hola, necesito un plomero para una fuga."
       - Observations:
         - Did agent respond in Spanish throughout (language detection + locale='es' prompt)?
         - Did agent read back cita + dirección before book_appointment (D1 anti-hallucination in ES)?
         - Any English fallback visible in the agent's responses (silent regression from Plan 12)?
         - Any mid-word cutoff at goodbye?

    4. Capture Railway logs for both calls. Check `[goodbye_race]` entries — both should show healthy timestamps (and for Branch G: playout_wait_ms populated).

    5. Append to `60.3-HUMAN-UAT.md`:
       ```markdown
       ## Stream B — UAT (cumulative verification)

       **Date:** YYYY-MM-DD

       ### Call B-1 — English booking path
       **Call SID:** call-_+...
       **Observations:**
       - Natural EN prose throughout: [yes/no]
       - Time + address readback before book_appointment: [yes/no]
       - Goodbye cutoff: [none / present — escalate]
       - Chat-LLM artifacts: [none / noted — ...]
       - `[goodbye_race]` log: text_done=[true/false], warning [absent/present]

       ### Call B-2 — Spanish language-detection path
       **Call SID:** call-_+...
       **Observations:**
       - Agent responded in Spanish throughout: [yes/no / partial]
       - Cita + dirección readback before book_appointment: [yes/no]
       - Any English fallback visible: [no / noted — section X]
       - Goodbye cutoff: [none / present]

       **Verdict:** [PASS / PARTIAL / FAIL]
       **Notes:** [free text]
       ```

    6. Type `continue` (PASS or acceptable PARTIAL) or `issue: <desc>` (FAIL — Stream B may need revert).
  </how-to-verify>
  <resume-signal>Type "continue" on PASS/PARTIAL. "issue: ..." on FAIL.</resume-signal>
  <acceptance_criteria>
    `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` contains `## Stream B — UAT (cumulative verification)` heading with Call B-1 and Call B-2 subsections and a final `Verdict:` line.
    Verified: `grep -q "Stream B — UAT (cumulative verification)" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md && grep -Eq "Verdict.*(PASS|PARTIAL|FAIL)" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update voice-call-architecture SKILL.md (additive)</name>
  <read_first>
    - `.claude/skills/voice-call-architecture/SKILL.md` (FULL file — required; understand §1, §3, §5 context before adding new content)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md` (goodbye fix branch chosen)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` (UAT #1, #2, cumulative evidence)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` (cross-link target)
    - Applies D-X-02 success criterion "voice-call-architecture SKILL.md updated to reflect final 60.3 state", CLAUDE.md skill-update protocol.
  </read_first>
  <action>
    **Add a new subsection** to `.claude/skills/voice-call-architecture/SKILL.md` — insertion point: end of the Phase-60-series narrative (after the 60.2 Fix G section). Title: `### Phase 60.3 — Goodbye cut-off diagnosis + prompt audit`.

    Content (~80–150 lines max — additive, do NOT rewrite §1 pin rationale or §3 Fix G):

    ```markdown
    ### Phase 60.3 — Goodbye cut-off diagnosis + prompt audit

    **Stream A — Goodbye cut-off fix:**

    - Diagnostic instrumentation shipped in Plan 1 (`src/agent.py` + `src/tools/end_call.py`): 6 timestamps (`end_call_invoked_at`, `last_text_token_at`, `last_audio_frame_at`, `playback_finished_at`, `participant_disconnect_at`, `session_close_at`) + `text_done`/`audio_done` flags + `transcript_tail` + `disconnect_reason`, emitted as a single `[goodbye_race]` log line + Sentry breadcrumb per call. Schema v1. Hooks: public livekit-agents 1.5.1 APIs only (`conversation_item_added`, `session.on("close")`, `ctx.room.on("participant_disconnected")`, `AudioOutput.capture_frame` wrapper post-`session.start()`, stdlib `logging.Handler` filtering the framework warning).
    - Fix shipped in Plan 3: **Branch [P|G]** — [short narrative, 1-2 sentences from the ANALYSIS + UAT #2 evidence]. [For Branch P: prompt-harden `_build_call_duration_section` to CRITICAL RULE with explicit failure-mode example; move to position 5 in assembled prompt. For Branch G: `await session.output.audio.wait_for_playout()` with 3s timeout in `end_call` tool body before scheduling `_delayed_disconnect`; 12s sleep unchanged.]
    - Upstream reference: livekit/agents#5096 (the authoritative bug for Gemini Realtime + EndCallTool). **#4486 is a DIFFERENT race class** — do not conflate.
    - Instrumentation sample rate dial-back (D-A-03): currently every-call. Re-evaluate after N≥10 post-fix clean UAT calls show no recurrence; then drop to race-detected-only (only emit when `_SegmentSynchronizerImpl.playback_finished` warning fires with `text_done=false`).
    - Privacy note: `caller_phone_sha256` (SHA-256 first-16-hex) is used; raw E.164 numbers are NEVER logged.

    **Stream B — Prompt audit + locale parity:**

    - `60.3-PROMPT-AUDIT.md` captures 16 sections × 7 dimensions scoring + per-section findings (see `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/`).
    - Plans 5-12 shipped per-section patches. D-B-03 mandate "full 16 sections × 2 locales" is satisfied: every `_build_*_section` that emits caller-facing prose now has an `if locale == "es":` branch mirroring the EN structure.
    - Exceptions (intentional, documented):
      - `_build_repeat_caller_section` returns `""` — no content, no parity work.
      - `_build_working_hours_section` dict KEYS (`"monday"` etc.) stay English for tenant config lookup compatibility; caller-facing prose (day labels in output sentences) is translated.
      - Tool names (`check_availability`, `book_appointment`, `capture_lead`, `end_call`, `transfer_call`) are code identifiers and never translated.
      - `TONE_LABELS` dict values (`"measured and formal"` etc.) not translated — embedded in identity_section prose.
      - `customer_context` STATE block from `format_customer_context_state` remains English (structured data; Jobber/Xero lookup compatibility).
    - New test files: `test_prompt_call_duration.py`, `test_prompt_tool_narration.py`, `test_prompt_voice_behavior.py`, `test_prompt_corrections.py`, `test_prompt_outcome_words.py`, `test_prompt_info_gathering.py`, `test_prompt_booking.py`, `test_prompt_tail_sections.py`. Existing `test_prompt.py` 60.2 inverted assertions preserved.
    - Token economy: [if Plan 4 audit measured before/after, include here; otherwise "section lengths roughly preserved; compression candidates documented in audit for future phase"].

    **Test count:** [N] tests at phase close (101 baseline + [delta]).

    **Plans 60.3-01 through 60.3-13** — see `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/`.
    ```

    **Do NOT:**
    - Rewrite §1 pin rationale (Fix J context — still accurate).
    - Modify §3 Fix G narrative.
    - Modify §5 Fix H revert narrative.
    - Delete any existing content.
    - Add a new top-level section — the 60.3 content is a subsection of the Phase 60-series narrative.
  </action>
  <verify>
    <automated>grep -q "Phase 60.3" .claude/skills/voice-call-architecture/SKILL.md && grep -q "60.3-PROMPT-AUDIT.md" .claude/skills/voice-call-architecture/SKILL.md && grep -q "livekit/agents#5096" .claude/skills/voice-call-architecture/SKILL.md && grep -c "Fix G" .claude/skills/voice-call-architecture/SKILL.md | awk '{exit !($1 > 0)}'</automated>
  </verify>
  <done>SKILL.md contains a new "Phase 60.3" subsection with Stream A + Stream B narratives, cross-link to the audit doc, upstream #5096 reference, and existing §§ unchanged (Fix G mentions preserved).</done>
</task>

<task type="auto">
  <name>Task 3: Write 60.3-SUMMARY.md</name>
  <read_first>
    - All 13 plan summaries (`60.3-01-SUMMARY.md` through `60.3-12-SUMMARY.md` plus this plan's own summary once generated)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` (Stream A UAT #1, #2, Stream B cumulative)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md`
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md`
    - `.planning/phases/60.2-voice-tool-call-filler-race-and-vad-tuning/60.2-SUMMARY.md` (reference format — match its rollup shape)
    - `.claude/skills/voice-call-architecture/SKILL.md` (your Task 2 updates)
    - Applies D-X-02 (all 8 success criteria), D-A-03 (dial-back trigger note).
  </read_first>
  <action>
    Create `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md` with this structure:

    ```markdown
    ---
    phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit
    parent_phase: 60.2
    kind: phase-rollup
    status: complete
    completed: YYYY-MM-DD
    plans:
      - "01: Stream A instrumentation — 6 hooks + [goodbye_race] log schema"
      - "02: Stream A UAT #1 + analysis — branch P/G selected"
      - "03: Stream A fix + UAT #2 — Branch [P|G] shipped"
      - "04: Stream B prompt audit doc"
      - "05: call_duration — locale parity + CRITICAL RULE"
      - "06: tool_narration — locale parity, 60.2 invariants preserved"
      - "07: voice_behavior — locale parity"
      - "08: corrections — locale parity, D1 critical"
      - "09: outcome_words — locale parity, highest anti-hallucination"
      - "10: info_gathering — invariants verified/locked"
      - "11: booking — 85% parity gap closed"
      - "12: tail sections batch — final 8 builders locale-parity"
      - "13: cumulative UAT + SKILL.md + SUMMARY"
    ---

    # Phase 60.3 — Voice Agent Goodbye Cut-off Diagnosis + Prompt Audit (Rollup)

    **What shipped:** [2-3 sentence summary — goodbye fix (Branch P or G) + full locale parity across 15 of 16 sections + prompt audit doc + diagnostic instrumentation].

    ## D-X-02 Success Criteria

    | # | Criterion | Outcome | Evidence |
    |---|-----------|---------|----------|
    | 1 | Diagnostic instrumentation deployed and emitting on every call | ✅ | 60.3-01-SUMMARY.md; UAT #1 evidence in 60.3-HUMAN-UAT.md |
    | 2 | Stream A UAT shows goodbye-race race hypothesis discriminated | ✅ | 60.3-STREAM-A-ANALYSIS.md |
    | 3 | Goodbye fix shipped (prompt or tool-level, picked from evidence) | ✅ | 60.3-03-SUMMARY.md (Branch [P|G]) |
    | 4 | Post-fix UAT confirms no recurrence of 'Thank you for calling [cut]' pattern | [✅ / ⚠️ PARTIAL / ❌] | 60.3-HUMAN-UAT.md §Stream A UAT #2 verdict |
    | 5 | 60.3-PROMPT-AUDIT.md captures full audit findings across 16 sections × 2 locales | ✅ | 60.3-PROMPT-AUDIT.md |
    | 6 | All Stream B per-section patches shipped, each as its own atomic commit | ✅ | Plans 05-12 SUMMARYs |
    | 7 | Final UAT confirms no regression from cumulative Stream B changes | [✅ / ⚠️ / ❌] | 60.3-HUMAN-UAT.md §Stream B cumulative verdict |
    | 8 | voice-call-architecture SKILL.md updated | ✅ | .claude/skills/voice-call-architecture/SKILL.md §Phase 60.3 |
    | 9 | Re-evaluation note for instrumentation dial-back captured | ✅ | This document §Follow-ups |

    ## Plan-by-plan commits

    | Plan | Repo | Commit | Notes |
    |---|---|---|---|
    | 01 — Instrumentation | livekit-agent | [SHA] | 6 hooks + flush + [goodbye_race] log schema |
    | 02 — UAT #1 + analysis | homeservice_agent | [SHA] | STREAM-A-ANALYSIS.md + HUMAN-UAT §UAT #1 |
    | 03 — Goodbye fix | livekit-agent | [SHA] | Branch [P|G] shipped; UAT #2 verdict [...] |
    | 04 — Prompt audit doc | homeservice_agent | [SHA] | 16 × 7 scored matrix |
    | 05 — call_duration | livekit-agent | [SHA] | EN+ES CRITICAL RULE |
    | 06 — tool_narration | livekit-agent | [SHA] | EN+ES, 60.2 invariants preserved |
    | 07 — voice_behavior | livekit-agent | [SHA] | EN+ES |
    | 08 — corrections | livekit-agent | [SHA] | EN+ES CRITICAL RULE |
    | 09 — outcome_words | livekit-agent | [SHA] | EN+ES highest anti-hallucination |
    | 10 — info_gathering | livekit-agent | [SHA] | Invariants locked |
    | 11 — booking | livekit-agent | [SHA] | 85% parity gap closed |
    | 12 — tail sections | livekit-agent | [SHA] | Final 8 builders |
    | 13 — SKILL + SUMMARY | homeservice_agent | [this SHA] | Phase close |

    ## Key artifacts

    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-CONTEXT.md` — decisions (D-A-*, D-B-*, D-X-*)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-RESEARCH.md` — R-A* hook points + R-B* section map + upstream #5096 verification
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-VALIDATION.md` — Nyquist gate sentinel signals
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md` — fix-selection decision doc
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` — 16×7 audit + per-section findings
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` — UAT #1 + UAT #2 + cumulative UAT evidence
    - `.claude/skills/voice-call-architecture/SKILL.md` — updated §Phase 60.3 subsection

    ## Test count delta

    - Pre-60.3 baseline: 101 tests (60.2 state).
    - Post-60.3: [N] tests.
    - Delta: +[N-101] across 8 new test files + existing file extensions.

    ## Lessons captured

    1. [Lesson from Stream A evidence — e.g., "UAT-driven fix selection saves prompt-iteration cycles" or "Public event emitters are sufficient — no monkey-patching needed"]
    2. [Lesson from Stream B — e.g., "Python-inline locale branches scale; JSON-extraction refactor deferred to a future phase"]
    3. [Lesson from 60.2 invariant preservation — "inverted assertions are load-bearing and outlive their triggering phase"]
    4. [Any privacy / telemetry lesson — e.g., "caller_phone_sha256 adds almost no code cost and is strictly better than log-scrubbing later"]

    ## Follow-ups (non-blocking)

    - **Instrumentation sample rate dial-back (D-A-03 trigger):** after N≥10 post-fix clean UAT calls, dial `[goodbye_race]` emission to race-detected-only (warning-triggered). Captured as a follow-up, not a new phase.
    - **Fix I (post-call 8s timeout) — passive monitoring continues.** No firing observed during 60.3 UAT. Promote to its own phase only if it fires on a clean call.
    - **Fix J (plugin upgrade past `43d3734`) — re-evaluate when:** livekit/agents#4486 closes OR #5096 closes with a fix OR Railway preview env available. None triggered by 60.3 work.
    - **Token-economy compression opportunities** identified in the audit but deferred — see `60.3-PROMPT-AUDIT.md` §Cross-Cutting Concerns.
    - **TONE_LABELS + customer_context STATE block locale extension** — not caller-facing prose; future phase if Hispanic market data shows value.

    ---
    *Phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit*
    *Completed: YYYY-MM-DD*
    ```

    Fill in bracketed values from actual commit SHAs, test counts, and UAT verdicts.
  </action>
  <verify>
    <automated>test -f .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md && grep -q "D-X-02 Success Criteria" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md && grep -q "Follow-ups" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md && grep -q "dial-back" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md</automated>
  </verify>
  <done>SUMMARY.md exists with all D-X-02 success criteria evaluated, commit table, lessons, and follow-ups including the dial-back trigger.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP + commit all docs</name>
  <read_first>
    - `.planning/ROADMAP.md` (locate Phase 60.3 entry)
    - your Task 2 + Task 3 outputs
  </read_first>
  <action>
    1. Update `.planning/ROADMAP.md` Phase 60.3 entry:
       - Mark status complete.
       - Set `**Plans:** 13 plans`.
       - Plan list with all 13 plans checked:
         ```
         Plans:
         - [x] 60.3-01-PLAN.md — Stream A instrumentation
         - [x] 60.3-02-PLAN.md — Stream A UAT #1 + analysis
         - [x] 60.3-03-PLAN.md — Stream A goodbye fix (Branch P/G)
         - [x] 60.3-04-PLAN.md — Prompt audit doc
         - [x] 60.3-05-PLAN.md — call_duration locale parity
         - [x] 60.3-06-PLAN.md — tool_narration locale parity
         - [x] 60.3-07-PLAN.md — voice_behavior locale parity
         - [x] 60.3-08-PLAN.md — corrections locale parity
         - [x] 60.3-09-PLAN.md — outcome_words locale parity
         - [x] 60.3-10-PLAN.md — info_gathering verify/lock
         - [x] 60.3-11-PLAN.md — booking full parity
         - [x] 60.3-12-PLAN.md — tail sections batch
         - [x] 60.3-13-PLAN.md — final UAT + SKILL.md + SUMMARY
         ```

    2. Commit:
       ```
       git add .planning/ROADMAP.md .claude/skills/voice-call-architecture/SKILL.md .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-SUMMARY.md .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md && git commit -m "docs(60.3): phase close — SKILL.md + SUMMARY + ROADMAP

       Phase 60.3 complete: goodbye cut-off fix (Branch [P|G]) + full
       prompt locale parity across 15 sections + diagnostic instrumentation
       + prompt audit doc. 13 plans shipped, D-X-02 success criteria all
       satisfied, cumulative UAT PASSED.

       Follow-ups (non-blocking) captured in 60.3-SUMMARY.md:
       - Instrumentation sample-rate dial-back trigger (D-A-03)
       - Fix I passive monitoring continues
       - Fix J plugin upgrade re-evaluation triggers

       Phase 60.3 Plan 13 of 13 — close."
       ```
  </action>
  <verify>
    <automated>grep -q "60.3-13-PLAN.md" .planning/ROADMAP.md && grep -q "13 plans" .planning/ROADMAP.md && git log -1 --format=%s | grep -q "phase close"</automated>
  </verify>
  <done>ROADMAP reflects Phase 60.3 complete with 13 plans. Final commit landed.</done>
</task>

</tasks>

<threat_model>None — documentation updates only.</threat_model>

<verification>
- All 4 documents updated/created.
- D-X-02 all 8 success criteria checked.
- Phase marked complete in ROADMAP.
</verification>

<success_criteria>
- Phase 60.3 closed.
- Cumulative UAT PASS or acceptable PARTIAL.
- SKILL.md reflects 60.3 state without churning existing §§.
- SUMMARY rolls up both streams + follow-ups.
</success_criteria>

<output>
Create `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-13-SUMMARY.md` per template noting plan 13 specifically (distinct from the phase-rollup 60.3-SUMMARY.md). 13-SUMMARY is a per-plan artifact; 60.3-SUMMARY is the phase-rollup.
</output>
