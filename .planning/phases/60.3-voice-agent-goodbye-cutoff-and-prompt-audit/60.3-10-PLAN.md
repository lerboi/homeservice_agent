---
phase: 60.3
plan: 10
type: execute
wave: 10
depends_on: [09]
files_modified:
  - C:\Users\leheh\.Projects\livekit-agent\src\prompt.py
  - C:\Users\leheh\.Projects\livekit-agent\tests\test_prompt_info_gathering.py
autonomous: true
requirements:
  - 60.2-DEFER-B
tags: [voice-agent, prompt, stream-b, info-gathering, locale-parity-maintenance]

must_haves:
  truths:
    - "Existing es branch in _build_info_gathering_section is maintained (this section already has locale parity per R-B1)"
    - "Phase 60 single-question framing locked — both locales retain one-question-at-a-time guidance (D-B-04 note: verify no drift from Phase 60)"
    - "Any D6 token compression applied in en is mirrored in es"
    - "Test suite stays green"
  artifacts:
    - path: "C:\\Users\\leheh\\.Projects\\livekit-agent\\src\\prompt.py"
      provides: "_build_info_gathering_section verified/compressed"
      contains: "if locale == \"es\""
    - path: "C:\\Users\\leheh\\.Projects\\livekit-agent\\tests\\test_prompt_info_gathering.py"
      provides: "locale-parity + single-question-framing assertions"
  key_links:
    - from: "60.3-PROMPT-AUDIT.md §_build_info_gathering_section"
      to: "this plan"
      via: "Target plan: Plan 10"
      pattern: "Target plan: Plan 10"
---

<objective>
Stream B patch for `_build_info_gathering_section` (src/prompt.py:304-394). Dimensions: **D6 token economy (section is 90 lines — audit may flag compression candidates), D7 locale parity already present — MAINTAIN**.

Unlike Plans 05-09, this section already has a full en-vs-es branch. The audit's job here is to verify Phase 60's single-question framing hasn't drifted and apply any D6 compression symmetrically to both branches.

Purpose: prevent locale drift; capture token-economy wins if audit flagged any.

Output: (potentially minimal) prose updates + new test file asserting the key invariants.
</objective>

<execution_context>Cross-repo. Wave 10 — serial.</execution_context>

<context>
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md
@C:\Users\leheh\.Projects\livekit-agent\src\prompt.py
@.planning/phases/60-voice-prompt-polish/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify invariants + apply audit-directed compression</name>
  <read_first>
    - `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py` lines 304-394 (full current section)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` §_build_info_gathering_section — if scoring is all ✓ or ~, this plan is an assert-and-commit; if ✗ on D6, apply compression
    - Applies D-B-02 dim 6, 7. Applies D-B-04 section #6.
  </read_first>
  <behavior>
    - test_en_exists_single_question_framing: EN output contains a single-question-at-a-time invariant — specifically the lowercased substring `"one"` AND `"at a time"` (Phase 60 lockdown).
    - test_es_exists_single_question_framing: ES output contains `"una"` AND (`"a la vez"` OR `"por vez"`).
    - test_en_address_intake_present: EN mentions address intake (contains `"address"` lowercased).
    - test_es_address_intake_present: ES mentions `"dirección"`.
    - test_en_phone_readback_present: EN contains a phone-readback invariant (lowercased `"phone number"` plus confirmation language `"read back"` OR `"confirm"`).
    - test_es_phone_readback_present: ES contains `"número de teléfono"` plus `"confirmar"` OR similar.
    - test_en_es_distinct_and_nonempty: both non-empty, distinct.
    - test_en_es_length_within_1pt5x: `abs(len(en) - len(es)) / max(len(en), len(es)) < 0.3` — catches drift where one locale gets trimmed but the other doesn't.
    - test_postal_label_parametrized_both_locales: call with `postal_label='postal code'` (SG) and `postal_label='zip code'` (US); assert EN correctly includes the passed label in both cases, and ES likewise substitutes in its own phrasing.
  </behavior>
  <action>
    **First, READ the current section in full** (lines 304-394) and identify:
    - Any phrase duplicated between en and es (candidates for compression).
    - Any Phase 60 single-question framing that could have drifted.
    - Any postal_label handling gaps.

    **If audit scored D6 as ✗:** identify specific sentences to compress. Apply the same compression to BOTH branches. Document the compression rationale in the commit message.

    **If audit scored D6 as ✓ or ~:** this plan becomes an assertion-only test-lock. Add `tests/test_prompt_info_gathering.py` with the 9 tests; if they all pass without source changes, the lock is in place — commit the test file only.

    **Do not remove or reframe:**
    - Any single-question-at-a-time guidance (Phase 60 lockdown).
    - The address / phone / issue triad.
    - The postal_label parametrization.

    **Create `tests/test_prompt_info_gathering.py`** with the 9 tests above. Tests must handle both `postal_label='zip code'` and `postal_label='postal code'` parametrically.
  </action>
  <verify>
    <automated>cd C:\Users\leheh\.Projects\livekit-agent && python -m pytest tests/test_prompt_info_gathering.py tests/test_prompt.py tests/test_prompt_customer_context.py tests/test_goodbye_diag.py tests/test_prompt_call_duration.py tests/test_prompt_tool_narration.py tests/test_prompt_voice_behavior.py tests/test_prompt_corrections.py tests/test_prompt_outcome_words.py -x -q</automated>
  </verify>
  <done>9 new tests pass; full suite green. If source was compressed, the compression applies symmetrically to en and es.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite + commit</name>
  <read_first>your changes</read_first>
  <action>
    ```
    cd C:\Users\leheh\.Projects\livekit-agent && python -m pytest tests/ -x -q
    cd C:\Users\leheh\.Projects\livekit-agent && git add src/prompt.py tests/test_prompt_info_gathering.py && git commit --no-verify -m "feat(60.3-10): info_gathering — verify invariants, lock with tests

    Addresses 60.3-PROMPT-AUDIT.md §_build_info_gathering_section
    (D6 token economy, D7 parity-maintenance).

    - Phase 60 single-question framing verified + asserted
    - en/es length within 30% — prevents drift
    - postal_label parametrization tested across SG and US

    9 new tests in tests/test_prompt_info_gathering.py.

    Phase 60.3 Stream B Plan 10 of 13."
    cd C:\Users\leheh\.Projects\livekit-agent && git push
    ```
  </action>
  <verify><automated>cd C:\Users\leheh\.Projects\livekit-agent && python -m pytest tests/ -x -q</automated></verify>
  <done>Suite green; commit pushed.</done>
</task>

</tasks>

<threat_model>None — prompt text only.</threat_model>
<verification>Phase 60 single-question framing preserved; no locale drift.</verification>
<success_criteria>D-B-04 section #6 complete.</success_criteria>
<output>Create `60.3-10-SUMMARY.md` per template.</output>
