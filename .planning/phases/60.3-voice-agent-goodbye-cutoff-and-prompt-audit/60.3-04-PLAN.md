---
phase: 60.3
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md
autonomous: true
requirements:
  - 60.2-DEFER-B
tags: [voice-agent, prompt, audit, stream-b, documentation]

must_haves:
  truths:
    - "60.3-PROMPT-AUDIT.md exists and is the single source of truth gating Plans 5-12"
    - "Every one of the 16 _build_*_section functions in src/prompt.py has its own ## heading in the audit"
    - "Every section is scored against all 7 audit dimensions (D-B-02)"
    - "Every section has a per-section findings block with a proposed-patch one-liner pointing to its patch plan (Plans 5-12)"
    - "Locale parity baseline explicitly documents en-vs-es prose gaps per section (R-B4)"
  artifacts:
    - path: ".planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md"
      provides: "16-section × 7-dimension scored audit + per-section findings"
      contains: "## _build_"
  key_links:
    - from: "60.3-PROMPT-AUDIT.md"
      to: "Plans 5-12 (per-section patches)"
      via: "proposed-patch one-liner per section names the target plan"
      pattern: "Proposed patch:.*Plan \\d+"
---

<objective>
Produce `60.3-PROMPT-AUDIT.md` — the findings document gating all Stream B per-section patch plans. No code changes in this plan.

Purpose: CONTEXT D-B-01 mandates findings-doc-first so each Stream B patch is atomic and reviewable against explicit acceptance criteria. Without this doc, per-section patches would embed their rationale inline and become hard to cross-reference.

Output: one markdown file with a 16×7 scoring matrix, per-section findings blocks, locale parity baseline, and a cross-cutting concerns section.
</objective>

<execution_context>
This is a **research-style documentation plan.** No code is modified in the livekit-agent repo. All work happens in this repo's planning directory.

The executor should treat `src/prompt.py` as READ-ONLY input and produce a structured markdown document. `60.3-RESEARCH.md` §R-B1, §R-B4, §R-B5 has already done most of the observational work — this plan assembles it into the authoritative audit artifact that Plans 5-12 reference.
</execution_context>

<context>
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-CONTEXT.md
@.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-RESEARCH.md
@C:\Users\leheh\.Projects\livekit-agent\src\prompt.py
@C:\Users\leheh\.Projects\livekit-agent\src\messages\en.json
@C:\Users\leheh\.Projects\livekit-agent\src\messages\es.json

<interfaces>
<!-- 16 section builders from src/prompt.py with verified line ranges (RESEARCH §R-B1). -->

| # | Function | Lines | Locale-aware? |
|---|----------|-------|----------------|
| 1 | `_build_identity_section(business_name, tone_label)` | 32-39 | No — static en |
| 2 | `_build_voice_behavior_section()` | 42-53 | No — static en |
| 3 | `_build_corrections_section()` | 56-74 | No — static en (CRITICAL RULE) |
| 4 | `_build_outcome_words_section()` | 77-109 | No — static en (CRITICAL RULE) |
| 5 | `_build_tool_narration_section()` | 112-150 | No — static en (post-60.2 state) |
| 6 | `_build_working_hours_section(working_hours, tenant_timezone)` | 153-216 | No — day labels English-only |
| 7 | `_build_greeting_section(locale, business_name, onboarding_complete, t)` | 219-245 | Partial — uses `t("agent.recording_disclosure")` |
| 8 | `_build_language_section(t)` | 248-263 | No — ironic; prose is English-only |
| 9 | `_build_repeat_caller_section(onboarding_complete)` | 266-269 | Returns empty string always |
| 10 | `_build_customer_account_section(customer_context)` | 272-301 | No — CRITICAL RULE in English |
| 11 | `_build_info_gathering_section(t, postal_label, locale)` | 304-394 | Yes — full `if locale == "es"` branch |
| 12 | `_build_intake_questions_section(intake_questions)` | 397-405 | No — preamble English-only |
| 13 | `_build_booking_section(business_name, onboarding_complete, postal_label, locale)` | 408-510 | Partial — `if locale == "es"` on readback only |
| 14 | `_build_decline_handling_section(business_name)` | 513-525 | No — static en; conditional on onboarding_complete |
| 15 | `_build_transfer_section(business_name)` | 528-541 | No — static en |
| 16 | `_build_call_duration_section(t)` | 544-556 (pre-Plan-3) OR restructured (post-Plan-3 Branch P) | No — takes t but unused |

<!-- The 7 audit dimensions (CONTEXT D-B-02, ranked). -->

1. **Anti-hallucination strength** — does the section prevent the model from speaking unverified facts?
2. **Realtime-model-specific phrasing** — does it avoid chat-LLM patterns (preamble, self-narration, markdown-heading reliance)?
3. **Section ordering** — does its position in the assembled prompt match its attentional priority?
4. **STATE+DIRECTIVE consistency** — does it phrase instructions as "state: X. directive: do Y" rather than "you should think about X"?
5. **VAD-redundant guidance** — does it redundantly prescribe pacing / barge-in behavior that the Fix G `silence_duration_ms=1500` VAD config already enforces?
6. **Token economy** — is every sentence pulling its weight? Any redundancy that could be compressed?
7. **Locale parity drift** — does a Spanish caller receive equivalent content? (en prose → es prose; both present in JSON messages OR Python branches)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 60.3-PROMPT-AUDIT.md</name>
  <read_first>
    - `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py` (full file, post-Plan-3 state — note Plan 3 Branch P may have restructured `_build_call_duration_section`)
    - `C:\Users\leheh\.Projects\livekit-agent\src\messages\en.json` (full file)
    - `C:\Users\leheh\.Projects\livekit-agent\src\messages\es.json` (full file)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-RESEARCH.md` §R-B1 (section map), §R-B4 (locale parity), §R-B5 (realtime-model anti-patterns)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md` (inform the call_duration_section finding)
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` (Stream A UAT evidence influences audit commentary on call_duration + tool_narration)
    - Applies D-B-01 (findings-doc shape), D-B-02 (7 dimensions), D-B-03 (full scope en+es), D-B-04 (section ordering for plan sequence).
  </read_first>
  <action>
    Create `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` with EXACTLY this structure:

    ```markdown
    ---
    phase: 60.3
    kind: prompt-audit
    status: complete
    created: YYYY-MM-DD
    gates: [60.3-05-PLAN, 60.3-06-PLAN, 60.3-07-PLAN, 60.3-08-PLAN, 60.3-09-PLAN, 60.3-10-PLAN, 60.3-11-PLAN, 60.3-12-PLAN]
    ---

    # Phase 60.3 — Prompt Audit

    Audit target: `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py` (post-Plan-3 state).
    Stack: Gemini 3.1 Flash Live + livekit-agents 1.5.1 + livekit-plugins-google @ 43d3734.
    Audit dimensions (CONTEXT D-B-02): 1 anti-hallucination • 2 realtime-model phrasing • 3 section ordering • 4 STATE+DIRECTIVE consistency • 5 VAD-redundant guidance • 6 token economy • 7 locale parity drift.

    Scoring scale per dimension: **✓** (clean, no action) / **~** (minor, document but no dedicated patch task) / **✗** (needs patch — named plan assigned).

    ## Scoring Matrix

    | # | Section | Lines | D1 anti-halluc | D2 realtime | D3 ordering | D4 state+directive | D5 VAD-redundant | D6 token | D7 locale | Target plan |
    |---|---------|-------|----------------|-------------|-------------|---------------------|-------------------|----------|------------|-------------|
    | 1 | identity | 32-39 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 2 | voice_behavior | 42-53 | ? | ? | ? | ? | ? | ? | ? | Plan 07 |
    | 3 | corrections | 56-74 | ? | ? | ? | ? | ? | ? | ? | Plan 08 |
    | 4 | outcome_words | 77-109 | ? | ? | ? | ? | ? | ? | ? | Plan 09 |
    | 5 | tool_narration | 112-150 | ? | ? | ? | ? | ? | ? | ? | Plan 06 |
    | 6 | working_hours | 153-216 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 7 | greeting | 219-245 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 8 | language | 248-263 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 9 | repeat_caller | 266-269 | n/a (empty) | n/a | n/a | n/a | n/a | n/a | n/a | none — empty |
    | 10 | customer_account | 272-301 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 11 | info_gathering | 304-394 | ? | ? | ? | ? | ? | ? | ? | Plan 10 |
    | 12 | intake_questions | 397-405 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 13 | booking | 408-510 | ? | ? | ? | ? | ? | ? | ? | Plan 11 |
    | 14 | decline_handling | 513-525 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 15 | transfer | 528-541 | ? | ? | ? | ? | ? | ? | ? | Plan 12 (batch) |
    | 16 | call_duration | 544-556 (or restructured) | ? | ? | ? | ? | ? | ? | ? | Plan 05 |

    Replace every `?` with ✓, ~, or ✗ by reading the actual section source and applying the dimension definition. Be specific: if a section scores ✗ on D7 because its English prose has no Spanish counterpart, say so in the per-section findings.

    ## Per-Section Findings

    ### ## _build_identity_section (lines 32-39)

    **Current behavior:** [1-sentence paraphrase of what the section currently says]

    **Dimension findings:**
    - **D1 Anti-hallucination:** [✓/~/✗ — explanation]
    - **D2 Realtime-model:** [...]
    - **D3 Section ordering:** [...]
    - **D4 STATE+DIRECTIVE:** [...]
    - **D5 VAD-redundant:** [...]
    - **D6 Token economy:** [...]
    - **D7 Locale parity:** [...]

    **Proposed patch:** [1-sentence. If batched in Plan 12: "Add `if locale == 'es':` branch with Spanish identity prose — Plan 12 batch."]

    **Target plan:** Plan 12 (batch)

    ---

    ### ## _build_voice_behavior_section (lines 42-53)

    [same structure]

    **Target plan:** Plan 07

    ---

    [... repeat for all 16 sections ...]

    ## Locale Parity Baseline

    **Messages JSON (en.json vs es.json):** [Per R-B4 — key-level parity complete. Reproduce the key list here with en vs es value diff for any divergent strings. If keys match 100%, say so.]

    **Python prose gaps (the real audit surface):** [Enumerate each section's Spanish-prose status as a table:

    | Section | English prose | Spanish prose | Gap class |
    |---------|---------------|---------------|-----------|
    | identity | inline | missing | full |
    | voice_behavior | inline | missing | full |
    | corrections | inline | missing | full |
    | ... | ... | ... | ... |
    | info_gathering | inline | inline full branch | none |
    | booking | inline | inline partial (readback only) | partial — outer frame en-only |
    | greeting | inline | t("agent.recording_disclosure") disclosure only | partial |
    ]

    **Strategic choice (per RESEARCH Open Question 3):** inline `if locale == "es"` branches vs extract-to-JSON. Recommended: **inline branches** for all Stream B patches (mirrors existing `_build_info_gathering_section` pattern, single source of truth, no JSON refactor cost). Document the rationale here.

    ## Cross-Cutting Concerns

    **CRITICAL RULE attention zone:** After Plan 3 Branch P (if executed), the prompt opens with identity → voice_behavior → corrections → outcome_words → call_duration → tool_narration → ... This puts 3 CRITICAL RULE sections (corrections, outcome_words, call_duration) + 1 quasi-CRITICAL (tool_narration) in the top 6 — healthy attention budget. If Branch G was executed instead, call_duration remains at position 16 — document this as a finding to address in Plan 05.

    **Negated instructions (D2 dim):** per RESEARCH R-B5, heavy `DO NOT` / `NEVER` usage may invert on realtime models. Audit identifies which negations are safely reframe-able (e.g., "DO NOT speak filler without calling a tool" → "ALWAYS call the tool in the same turn you speak filler") vs which must stay negated for caller-safety (anti-hallucination CRITICAL RULEs).

    **Token economy aggregate:** approximate current assembled prompt length: [count via `wc -w` or estimate by section-total word count]. Target for cumulative Stream B changes: [modest reduction, e.g., -10-15% word count from compression without losing anti-hallucination language].

    ## Plan Assignment Summary

    | Plan | Sections | Dominant dimensions addressed |
    |------|----------|-------------------------------|
    | 05 | call_duration | D1, D2, D3 (+D7 if Spanish parity added) |
    | 06 | tool_narration (audit-verify, minimal change) | D2, D5, D6 |
    | 07 | voice_behavior | D2, D4, D5, D7 |
    | 08 | corrections | D1, D2, D4, D7 |
    | 09 | outcome_words | D1, D2, D4, D7 |
    | 10 | info_gathering | D6 (compression), D7 (parity already present — maintain) |
    | 11 | booking | D1, D7 (partial parity → full) |
    | 12 | identity, working_hours, greeting, language, repeat_caller, customer_account, intake_questions, decline_handling, transfer | D7 primarily (locale parity), D6 where compression obvious |

    (Total: 16 sections mapped; repeat_caller excluded as it returns empty. 15 sections assigned across 8 plans.)

    ## Re-Audit Trigger

    If Plan 3 Branch P was executed and Plan 05's call_duration rewrite conflicts with Branch P's already-applied changes, re-audit _build_call_duration_section's final state before Plan 05's diff is written.
    ```

    **Execution discipline:**
    - READ every `_build_*_section` function in `src/prompt.py` verbatim before scoring — do not score from memory of RESEARCH.md.
    - For D7 (locale parity), diff `en.json` vs `es.json` directly; check each Python section for `if locale == "es"` branches.
    - When scoring, base each score on a concrete observation quoted from the section's source, not a feeling.
    - Keep per-section findings to ~100-200 words each — enough to inform Plan 5-12 authors, not enough to duplicate the patch diff.
  </action>
  <verify>
    <automated>test -f .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md && test $(grep -c "^### ## _build_" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md) -ge 16 && grep -q "## Locale Parity Baseline" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md && grep -q "## Plan Assignment Summary" .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md</automated>
  </verify>
  <done>`60.3-PROMPT-AUDIT.md` exists with all 16 `### ## _build_*` section headings (one per builder), a scoring matrix filled in (no `?` left), a Locale Parity Baseline section, a Cross-Cutting Concerns section, and a Plan Assignment Summary listing all 8 Stream B patch plans.</done>
</task>

<task type="auto">
  <name>Task 2: Commit audit doc</name>
  <read_first>
    - `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md` (your output)
  </read_first>
  <action>
    Commit in the homeservice_agent repo (NOT livekit-agent — audit is a planning artifact):
    ```
    git add .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-PROMPT-AUDIT.md && git commit -m "docs(60.3): prompt audit doc — 16 sections × 7 dimensions

    Gates Stream B per-section patches (Plans 5-12). Scoring matrix,
    per-section findings, locale parity baseline (R-B4), cross-cutting
    concerns. No source code changes.

    Phase 60.3 Stream B Plan 4 of 13."
    ```

    (Homeservice_agent commits use default pre-commit — no `--no-verify` here. If pre-commit fails, fix the lint issue and recommit — do not `--no-verify` docs.)
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "prompt audit doc"</automated>
  </verify>
  <done>Commit landed in homeservice_agent repo with audit doc included.</done>
</task>

</tasks>

<threat_model>
None — modifies a markdown planning doc only; no auth/input/crypto/source surface touched.
</threat_model>

<verification>
- `60.3-PROMPT-AUDIT.md` exists with 16 section headings.
- Scoring matrix complete (no `?`).
- Plans 5-12 can read their assigned sections' findings directly.
</verification>

<success_criteria>
- D-X-02 success criterion "`60.3-PROMPT-AUDIT.md` captures full audit findings across 16 sections × 2 locales" → satisfied.
- Stream B patch plans unblocked with explicit per-section guidance.
</success_criteria>

<output>
Create `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-04-SUMMARY.md` per template. Link to the audit doc. Summarize top 3 findings that will influence downstream patches.
</output>
