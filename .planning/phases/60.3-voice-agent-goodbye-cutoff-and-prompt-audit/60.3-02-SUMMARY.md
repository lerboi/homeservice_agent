---
phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit
plan: 02
subsystem: voice-agent-uat
tags: [uat, goodbye-race, stream-a, evidence-driven, fix-selection, branch-p]

# Dependency graph
requires:
  - phase: 60.3
    provides: "Plan 01 — [goodbye_race] instrumentation deployed to Railway (livekit-agent c4f0570); _GoodbyeDiagHandler emitting JSON diag records with 6 timestamp fields + text_done/audio_done + transcript_tail on every call close"

provides:
  - "60.3-HUMAN-UAT.md §'Stream A — UAT #1' — owner-captured live-call evidence: full [goodbye_race] JSON payload, parsed timestamps table, 3× _SegmentSynchronizerImpl warning capture, transcript showing caller heard 'Alright, I'll get all' cut mid-sentence"
  - "60.3-STREAM-A-ANALYSIS.md — evidence analysis + fix selection: **Selected fix: Branch P** (prompt-harden _build_call_duration_section) based on ambiguous-evidence discipline from Plan 2 line 285-294"
  - "Unambiguous Plan 3 execution directive: execute ONLY Branch P; mark Branch G tasks `skipped`"

affects: [60.3-03, 60.3-04, voice-call-architecture-skill]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Evidence-driven branch selection with ambiguity-resolution rule — when UAT matches multiple fix signatures, prefer the lower-risk (revertable) candidate first; use post-fix UAT to discriminate for follow-up iteration"
    - "Human-UAT + autonomous-analysis pairing — blocking checkpoint for real-call evidence capture, unblocked downstream autonomous write-up cites the evidence verbatim"

key-files:
  created:
    - ".planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md — Stream A UAT #1 evidence (owner-populated)"
    - ".planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md — fix selection doc with Branch P decision"
  modified: []

key-decisions:
  - "Selected fix: Branch P (prompt-hardening of _build_call_duration_section) — evidence ambiguous (matched #5096 signature AND Branch P end_call-before-last-text directional signal); applied Plan 2 ambiguity rule favoring lower-risk revertable change"
  - "Plan 3 will execute Branch P ONLY; Branch G tasks marked skipped. Post-Branch-P UAT #2 will discriminate — if truncation persists unchanged, promote Branch G for follow-up iteration within 60.3 evidence loop"
  - "Documented 3 non-blocking concerns for Plan 3 / phase-tail: (a) systemic 3× mid-call SegmentSynchronizer warnings not confined to goodbye turn — may need follow-up phase; (b) playback_finished_at stale-segment capture — instrumentation refinement note; (c) tool_call_log_tail empty despite end_call firing — by-design gap for future refinement"

patterns-established:
  - "Ambiguity-resolution rule: when UAT evidence matches two fix candidates, prefer the prompt-only (revertable) candidate over the runtime-behavior candidate; use the next UAT to discriminate"
  - "Stream A discipline enforced: no Plan 3 code changes without evidence-backed branch selection; selection doc cites concrete timestamp deltas from live call, not speculation"

requirements-completed:
  - 60.2-DEFER-A

# Metrics
duration: ~20min
completed: 2026-04-22
---

# Phase 60.3 Plan 02: Stream A UAT #1 + Fix Selection Summary

**Owner placed live Twilio UAT call against the Plan-01 Railway build, captured [goodbye_race] diagnostic payload showing textbook #5096 pipeline-race signature (text_done=false, audio_done=true) AND an 11ms end_call-before-last_text_token directional signal; evidence is ambiguous, so Branch P (prompt-only, fully revertable) selected per Plan 2 ambiguity-resolution rule.**

## Selected fix: Branch P

**Plan 3 directive:** Execute ONLY the Branch P tasks (`_build_call_duration_section` prompt-hardening at `C:\Users\leheh\.Projects\livekit-agent\src\prompt.py:544-556`). Mark Branch G tasks (`end_call.py` `wait_for_playout` pre-guard) as `skipped` in the commit message. Do NOT execute both. See [`60.3-STREAM-A-ANALYSIS.md`](./60.3-STREAM-A-ANALYSIS.md) §"Plan 3 execution directive" for full rationale.

## Performance

- **Duration:** ~20 min (execution span); Task 1 human-time-blocked by owner UAT call (174s call + log capture)
- **Started:** 2026-04-22T09:01:42Z (post-Plan-01 STATE.md stamp; Task 1 started when owner initiated UAT)
- **Completed:** 2026-04-22T09:20:03Z (Task 2 write-up)
- **Tasks:** 2 (Task 1: checkpoint:human-verify UAT; Task 2: auto analysis writeup)
- **Files modified:** 2 created (HUMAN-UAT + STREAM-A-ANALYSIS), 0 code changes

## Accomplishments

- **UAT #1 reproduced the bug:** call-_+6587528516_KwsBVWBZkKps (174s, declined booking) — caller heard `"Alright, I'll get all"` cut mid-sentence; would have continued `"...that saved down for you"`. Cut severity: last ~4-6 words of the final turn.
- **Full `[goodbye_race]` JSON payload captured** with all 6 timestamps + text_done/audio_done flags + transcript_tail + disconnect reason. Plan-01 instrumentation worked as designed.
- **Upstream #5096 signature confirmed:** `text_done=false, audio_done=true` at synchronizer's `playback_finished` — the exact upstream Gemini Live + EndCallTool race (RESEARCH §R-A9).
- **Three mid-call `_SegmentSynchronizerImpl` warnings captured at +152s, +202s, +222s** — correlated with earlier transcript truncations (`"Let me pull up the calendar for you real quick to"` and `"No problem at all. I'll get all that saved down"`). Indicates the pipeline-race is systemic across the call, not confined to the farewell turn. Captured as non-blocking follow-up concern.
- **Branch P selected per Plan 2 ambiguity-resolution rule** (lines 285-294): evidence matched both signatures, so prefer lower-risk revertable change first; post-Branch-P UAT #2 will discriminate.
- **Plan 3 unblocked** with an unambiguous single-branch directive — no executor speculation needed.

## Task Commits

Each task was committed atomically — Tasks 1 and 2 bundled per resume-instruction directive (Task 1 was human-completed with file written; Task 2's commit covers both files):

1. **Task 1: UAT #1 — owner places live Twilio call, captures Railway log** — (manual; file written by owner via the checkpoint flow). File content committed in the Task 2 bundle.
2. **Task 2: Analysis writeup — select Branch P or Branch G with evidence** — `9811ea2` (docs) — created `60.3-STREAM-A-ANALYSIS.md` with `**Selected fix: Branch P**` line and staged `60.3-HUMAN-UAT.md` alongside.

**Plan metadata:** (this SUMMARY commit) — will add `60.3-02-SUMMARY.md` + `STATE.md` + `ROADMAP.md` updates.

## Files Created/Modified

**homeservice_agent repo (C:\\Users\\leheh\\.Projects\\homeservice_agent):**
- `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md` — Stream A UAT #1 section with raw JSON, parsed timestamps table, caller-experience notes, and owner's initial characterization checking 2 of 4 signature boxes plus "ambiguous — needs analysis"
- `.planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md` — evidence summary citing concrete UAT timestamps (end_call vs last_text_token delta = -11ms; end_call vs playback_finished delta = +15239ms stale-segment; 3× SegmentSynchronizer warnings; text_done=false, audio_done=true); fix-candidate comparison; **Selected fix: Branch P** decision with ambiguity-rationale; Plan 3 execution directive; 3 non-blocking open-questions/concerns

**livekit-agent repo (C:\\Users\\leheh\\.Projects\\livekit-agent):** None — no code changes in Plan 02 (by design; Plan 02 is evidence capture + decision only).

## Decisions Made

- **Ambiguity → Branch P (per Plan 2 line 285-294):** The UAT evidence is genuinely ambiguous. The `text_done=false, audio_done=true` pattern is textbook #5096 (argues for Branch G). The 11ms `end_call_invoked_at < last_text_token_at` delta is directionally Branch P. The `playback_finished_at` captured on the diag record is from an earlier segment (+15.2s before end_call), not the goodbye turn, so it doesn't cleanly indicate the final-segment race either way. Transcript shows `"Alright, I'll get all"` as the aborted final turn — reads as model-layer truncation (Branch P) AND/OR audio-layer truncation of a longer intended text (Branch G). Plan 2's ambiguity rule explicitly mandates preferring Branch P in this exact case (lower-risk, revertable, no runtime-behavior change to `end_call`'s disconnect sequencing). Rule followed; rationale documented verbatim in STREAM-A-ANALYSIS.md `## Selected fix`.
- **Three non-blocking concerns documented, not promoted:** (a) systemic 3× mid-call SegmentSynchronizer warnings are larger-scope than the goodbye race — may need own phase after Branch P ships; (b) `playback_finished_at` stale-segment capture in the instrumentation is a refinement note, not a bug; (c) `tool_call_log_tail` being empty despite `end_call` firing is by-design instrumentation gap. All captured in STREAM-A-ANALYSIS.md §"Open questions / non-blocking concerns" so they aren't lost.

## Deviations from Plan

None — plan executed exactly as written. Task 1 was human-completed per checkpoint protocol; Task 2 followed the template in Plan 02 (lines 191-295) verbatim, applied the ambiguity-resolution discipline at line 285-294 as prescribed, and emitted the exact `**Selected fix: Branch P**` marker required by the automated verification (`grep -Eq "^\*\*Selected fix: Branch (P|G)\*\*$"`).

## Issues Encountered

None during Plan 02 execution. The UAT itself revealed the bug (by design — Plan 02 exists to reproduce and characterize it). Pre-existing pipeline-race class is the target of Plan 3 (Branch P) and any follow-up iteration if UAT #2 discriminates to Branch G.

## Known Stubs

None — both artifacts are complete decision documents. STREAM-A-ANALYSIS.md has concrete timestamp citations, not placeholders. Plan 3's executor has unambiguous direction.

## Next Phase Readiness

**Ready for Plan 3 (Branch P execution):** Executor should read:
1. `60.3-STREAM-A-ANALYSIS.md` first to confirm branch selection
2. `60.3-03-PLAN.md` for the Branch P task list
3. `src/prompt.py:544-556` for the current `_build_call_duration_section` (Branch P target)
4. `60.3-HUMAN-UAT.md` for the evidence context (optional; cited by ANALYSIS)

**Plan 3 execution rule:** Execute ONLY Branch P tasks. Mark Branch G tasks `skipped`. Do not dual-deploy.

**Post-Branch-P UAT #2 expectations:**
- If caller-perceived truncation resolves → Branch P confirmed; Plan 4+ continues with Stream B prompt audit
- If truncation persists at same severity (last 4-6 words cut) → evidence discriminates to Branch G; promote Branch G as a follow-up plan inside Phase 60.3 (not a new phase)
- If truncation reduces but doesn't fully resolve → mixed outcome; investigate if the 3× mid-call warnings also reduced (could indicate Branch P alone is insufficient across all truncation classes in the call)

**Blockers:** None. Plan 3 is executor-ready.

## Self-Check: PASSED

**File existence:**
- FOUND: .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-HUMAN-UAT.md
- FOUND: .planning/phases/60.3-voice-agent-goodbye-cutoff-and-prompt-audit/60.3-STREAM-A-ANALYSIS.md

**Commit existence (homeservice_agent repo):**
- FOUND: 9811ea2 (Task 2 — docs bundle: HUMAN-UAT + STREAM-A-ANALYSIS)

**Automated verification (per Plan 02 line 297):**
- `test -f .../60.3-STREAM-A-ANALYSIS.md` → PASS
- `grep -Eq "^\*\*Selected fix: Branch (P|G)\*\*$"` → PASS (matches `**Selected fix: Branch P**` exactly; exactly 1 occurrence)
- `grep -q "UAT evidence source"` → PASS (1 occurrence, linking to 60.3-HUMAN-UAT.md)

---
*Phase: 60.3-voice-agent-goodbye-cutoff-and-prompt-audit*
*Completed: 2026-04-22*
