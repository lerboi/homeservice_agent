---
phase: 02-onboarding-and-triage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.js
  - src/app/auth/signin/page.js
  - src/app/auth/callback/route.js
  - src/app/onboarding/page.js
  - src/app/onboarding/services/page.js
  - src/app/onboarding/layout.js
  - src/lib/supabase-server.js
  - messages/en.json
  - messages/es.json
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-06]
must_haves:
  truths:
    - "shadcn is initialized with New York style and Tailwind CSS configured"
    - "Google OAuth sign-in redirects to /auth/callback which exchanges code for session"
    - "Wizard Step 1 shows business name input and 3 tone preset cards"
    - "Wizard Step 2 shows 4 trade template cards and editable service list"
    - "Forward/back navigation works between steps without losing data"
    - "All UI copy uses next-intl translation keys, not hardcoded English"
  artifacts:
    - path: "src/app/auth/signin/page.js"
      provides: "Sign-in page with Google OAuth button"
      contains: "signInWithOAuth"
    - path: "src/app/auth/callback/route.js"
      provides: "OAuth callback handler exchanging code for session"
      contains: "exchangeCodeForSession"
    - path: "src/app/onboarding/page.js"
      provides: "Wizard Step 1: business name + tone preset"
      contains: "tone_preset"
    - path: "src/app/onboarding/services/page.js"
      provides: "Wizard Step 2: trade template + service list"
      contains: "TRADE_TEMPLATES"
  key_links:
    - from: "src/app/auth/signin/page.js"
      to: "src/lib/supabase-browser.js"
      via: "createClient() for OAuth"
      pattern: "supabase.auth.signInWithOAuth"
    - from: "src/app/auth/callback/route.js"
      to: "src/lib/supabase-server.js"
      via: "server-side session exchange"
      pattern: "exchangeCodeForSession"
    - from: "src/app/onboarding/services/page.js"
      to: "src/lib/trade-templates.js"
      via: "import TRADE_TEMPLATES for template cards"
      pattern: "TRADE_TEMPLATES"
---

<objective>
Initialize shadcn/Tailwind, build auth flow, and create wizard Steps 1-2.

Purpose: Get the onboarding UI skeleton in place — the owner can sign in, enter their business name, pick a tone preset, select a trade template, and see/edit their starter service list. This is the first half of the 3-step sprint.

Output: shadcn initialized, auth flow working, wizard Steps 1 and 2 with full UI per the UI-SPEC design contract.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-triage/02-CONTEXT.md
@.planning/phases/02-onboarding-and-triage/02-RESEARCH.md
@.planning/phases/02-onboarding-and-triage/02-UI-SPEC.md
@.planning/phases/02-onboarding-and-triage/02-VALIDATION.md

<interfaces>
From src/lib/supabase-browser.js:
```javascript
import { createBrowserClient } from '@supabase/ssr';
// or: import { createClient } from '@supabase/supabase-js';
// Used in client components for auth
```

From src/lib/supabase.js:
```javascript
export const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY);
// Server-side, service role — used in API routes
```

From src/app/layout.js:
```javascript
import { NextIntlClientProvider } from 'next-intl';
// Wraps app with i18n provider
```

From messages/en.json:
```json
{
  "agent": { ... },
  "notifications": { ... },
  "status": { ... }
}
// Needs "onboarding" and "services" namespaces added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn, Tailwind, auth flow, and onboarding layout</name>
  <files>
    src/app/globals.css
    src/app/layout.js
    src/app/auth/signin/page.js
    src/app/auth/callback/route.js
    src/app/onboarding/layout.js
    src/lib/supabase-server.js
  </files>
  <read_first>
    - src/app/layout.js
    - src/lib/supabase-browser.js
    - src/lib/supabase.js
    - package.json
    - .planning/phases/02-onboarding-and-triage/02-UI-SPEC.md
    - .planning/phases/02-onboarding-and-triage/02-RESEARCH.md
  </read_first>
  <action>
**Step 1: Initialize shadcn + Tailwind**

Run `npx shadcn@latest init` with New York style, neutral base color, default radius 0.5rem. This installs Tailwind CSS as a peer dependency and creates `components.json`, `tailwind.config.js`, and `src/app/globals.css`.

Then add required shadcn components:
```bash
npx shadcn@latest add button input card progress badge label alert sonner
```

**Step 2: Install Inter font**

In `src/app/layout.js`, add Inter font via `next/font/google`:
```javascript
import { Inter } from 'next/font/google';
const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
```

Apply to `<html>` tag: `className={inter.variable}`. Keep the existing `NextIntlClientProvider` wrapper intact.

Update `globals.css` to set `font-family: var(--font-inter), system-ui, sans-serif` on body.

**Step 3: Create Supabase server helper**

Create `src/lib/supabase-server.js` that creates a Supabase client for server components using cookies:
```javascript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';

export async function createSupabaseServer() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
    {
      cookies: {
        getAll() { return cookieStore.getAll(); },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            cookieStore.set(name, value, options);
          });
        },
      },
    }
  );
}
```

Note: This requires `@supabase/ssr` package — install it: `npm install @supabase/ssr`.

**Step 4: Auth sign-in page**

Create `src/app/auth/signin/page.js` as a client component (`'use client'`):
- "Continue with Google" button using `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin + '/auth/callback' } })`
- Email/password form as fallback (use `supabase.auth.signInWithPassword()` for login, `supabase.auth.signUp()` for register)
- Layout: centered card on `slate-50` background, max-w-sm
- Uses shadcn Button, Input, Label, Card components
- All strings from `useTranslations('auth')` — add auth namespace to messages

**Step 5: Auth callback route**

Create `src/app/auth/callback/route.js`:
```javascript
import { NextResponse } from 'next/server';
import { createSupabaseServer } from '@/lib/supabase-server';

export async function GET(request) {
  const { searchParams } = new URL(request.url);
  const code = searchParams.get('code');
  if (code) {
    const supabase = await createSupabaseServer();
    await supabase.auth.exchangeCodeForSession(code);
  }
  return NextResponse.redirect(new URL('/onboarding', request.url));
}
```

**Step 6: Onboarding layout**

Create `src/app/onboarding/layout.js`:
- Full-page `slate-50` background
- Centered wizard card: `max-w-lg mx-auto px-6 py-8 bg-white rounded-xl shadow-sm border border-slate-200`
- Mobile: `rounded-none shadow-none border-none`
- Logo placeholder (24px height) top-left, step counter top-right
- Progress bar (shadcn Progress component, 4px height) below header
- `aria-live="polite"` region on main content area
- Uses `usePathname()` to determine current step number (1=/onboarding, 2=/onboarding/services, 3=/onboarding/verify)

Add env vars to `.env.example`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists in project root (shadcn initialized)
    - `src/app/globals.css` contains Tailwind directives (`@tailwind` or `@import "tailwindcss"`)
    - `src/app/layout.js` contains `Inter` font import from `next/font/google`
    - `src/app/auth/signin/page.js` contains `signInWithOAuth`
    - `src/app/auth/callback/route.js` contains `exchangeCodeForSession`
    - `src/app/onboarding/layout.js` contains `max-w-lg` and `Progress`
    - `src/lib/supabase-server.js` contains `createServerClient`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>shadcn initialized, auth flow complete (sign-in + callback), onboarding layout shell with progress bar renders correctly</done>
</task>

<task type="auto">
  <name>Task 2: Wizard Step 1 (business name + tone) and Step 2 (trade template + services)</name>
  <files>
    src/app/onboarding/page.js
    src/app/onboarding/services/page.js
    src/app/api/onboarding/start/route.js
    messages/en.json
    messages/es.json
  </files>
  <read_first>
    - src/app/onboarding/layout.js
    - src/lib/supabase-browser.js
    - src/lib/supabase.js
    - src/lib/trade-templates.js
    - messages/en.json
    - messages/es.json
    - .planning/phases/02-onboarding-and-triage/02-UI-SPEC.md
    - .planning/phases/02-onboarding-and-triage/02-CONTEXT.md
  </read_first>
  <action>
**Step 1: Add onboarding and auth translation keys to messages**

Add to `messages/en.json` an `"onboarding"` namespace with all copy from the UI-SPEC Copywriting Contract:
```json
{
  "onboarding": {
    "step1_heading": "Set up your AI receptionist",
    "step1_subtext": "Start with your business name and how you'd like your AI to sound.",
    "step2_heading": "What do you do?",
    "step2_subtext": "Pick your trade to get a starter service list. You can edit it anytime.",
    "step3_heading": "Almost there",
    "step3_subtext": "Verify your mobile number and we'll assign your AI phone number.",
    "activation_heading": "Your AI is ready to answer calls",
    "activation_subtext": "Call {number} to test it, or hit the button below and we'll call you.",
    "cta_step1": "Continue to services",
    "cta_step2": "Continue to verify",
    "cta_step3_pre": "Send code",
    "cta_step3_otp": "Verify number",
    "cta_step3_provisioning": "Activating\u2026",
    "cta_activation": "Test your AI",
    "back": "Back",
    "step_counter": "Step {step} of 3",
    "business_name_label": "Business name",
    "business_name_placeholder": "e.g. Smith Plumbing",
    "tone_section_label": "How should your AI sound?",
    "tone_professional": "Professional",
    "tone_professional_desc": "Measured and formal. Builds trust with first-time callers.",
    "tone_friendly": "Friendly",
    "tone_friendly_desc": "Upbeat and warm. Feels approachable for residential customers.",
    "tone_local_expert": "Local Expert",
    "tone_local_expert_desc": "Relaxed and neighborly. Works well for established local businesses.",
    "trade_section_label": "Pick your trade",
    "services_section_label": "Your starter services",
    "services_section_helper": "These are pre-tagged based on your trade. You can add or remove services now, or edit tags anytime after setup.",
    "add_service": "Add service",
    "remove_service_aria": "Remove {serviceName}",
    "phone_label": "Your mobile number",
    "phone_helper": "We'll text you a verification code and use this for owner alerts.",
    "otp_label": "Verification code",
    "otp_resend": "Resend code",
    "error_business_name_empty": "Enter your business name to continue.",
    "error_no_trade": "Select a trade to get your starter service list.",
    "error_phone_invalid": "Enter a valid mobile number, including the country code.",
    "error_otp_incorrect": "That code doesn't match. Check your SMS and try again.",
    "error_otp_expired": "The code has expired. Tap 'Resend code' to get a new one.",
    "error_provisioning_failed": "We couldn't assign a phone number right now. Refresh and try again, or contact support.",
    "error_test_call_failed": "We couldn't place the test call. Dial {number} directly to test your AI.",
    "error_save_failed": "Changes couldn't be saved. Check your connection and try again."
  },
  "auth": {
    "signin_google": "Continue with Google",
    "signin_email": "Sign in with email",
    "email_label": "Email",
    "password_label": "Password",
    "signup_link": "Don't have an account? Sign up",
    "signin_link": "Already have an account? Sign in"
  }
}
```

Add equivalent keys to `messages/es.json` with Spanish translations. Keep all existing namespaces (agent, notifications, status) untouched.

**Step 2: Create Wizard Step 1 page (`src/app/onboarding/page.js`)**

Client component (`'use client'`). Layout per UI-SPEC:
- Heading: "Set up your AI receptionist" (Display / 28px / semibold, `text-3xl font-semibold`)
- Subtext below heading with 24px gap (`mb-6`)
- Business name input: shadcn Input with Label, placeholder "e.g. Smith Plumbing"
- Inline validation: if empty on submit, show error below field with `role="alert"` and red-600 text
- Tone preset section: label "How should your AI sound?" then 3 Card components
  - Each card: min-h-[80px], shows icon placeholder + preset name (semibold) + description (text-sm)
  - Selected card: `border-blue-600 bg-blue-50`, unselected: `border-slate-200 bg-slate-50`
  - Cards are keyboard-navigable (tabIndex, Enter/Space to select)
  - Default selection: `professional`
- Primary CTA: "Continue to services" button, full-width on mobile, `w-40` on desktop, `bg-blue-600 text-white`
- No Back button on Step 1
- On submit: POST to `/api/onboarding/start` with `{ business_name, tone_preset }`, then `router.push('/onboarding/services')`

**Step 3: Create API route `/api/onboarding/start/route.js`**

```javascript
import { supabase } from '@/lib/supabase';
import { createSupabaseServer } from '@/lib/supabase-server';

export async function POST(request) {
  const serverSupabase = await createSupabaseServer();
  const { data: { user } } = await serverSupabase.auth.getUser();
  if (!user) return Response.json({ error: 'Unauthorized' }, { status: 401 });

  const { business_name, tone_preset } = await request.json();

  // Upsert tenant row — create if new, update if returning
  const { data, error } = await supabase
    .from('tenants')
    .upsert({
      id: user.user_metadata?.tenant_id || undefined,
      business_name,
      tone_preset: tone_preset || 'professional',
    }, { onConflict: 'id' })
    .select('id')
    .single();

  if (error) return Response.json({ error: error.message }, { status: 500 });
  return Response.json({ tenant_id: data.id });
}
```

**Step 4: Create Wizard Step 2 page (`src/app/onboarding/services/page.js`)**

Client component. Layout per UI-SPEC:
- Heading: "What do you do?" (Display / 28px / semibold)
- Trade template cards: 4 cards in 2x2 grid (`grid grid-cols-2 gap-4`) for Plumber, HVAC, Electrician, General Handyman
  - Each card: trade label centered, icon placeholder
  - Selected: `border-blue-600 bg-blue-50`, unselected: `border-slate-200 bg-slate-50`
  - On select: populate service list from `TRADE_TEMPLATES[selected].services`
- Service list below trade cards:
  - Section label: "Your starter services"
  - Helper text: "These are pre-tagged based on your trade..."
  - Each service row: service name (text), urgency Badge (Emergency=red-100/red-700, Routine=slate-100/slate-600, High-Ticket=amber-100/amber-700), remove button (X icon, `aria-label="Remove {name}"`)
  - "Add service" button (secondary): opens inline input to type new service name, adds with 'routine' default tag
  - Limit: owner can add/remove/rename up to 3 additional services during sprint
- Validation: at least one trade must be selected to proceed (show error_no_trade)
- Back button: text-only "Back", slate-500, navigates to `/onboarding`
- CTA: "Continue to verify", saves trade_type and services to API, then `router.push('/onboarding/verify')`
- On submit: POST to `/api/onboarding/start` with `{ trade_type, services: [...] }` (extend the route to handle services array — save to services table)

Extend the API route `/api/onboarding/start/route.js` to also accept `trade_type` and `services` array. When services are provided, insert rows into the `services` table for the tenant using service role supabase client.

**Animation:** Wizard step transition: slide-in from right (forward), slide-in from left (back). Duration 200ms, ease-out. Use CSS transitions on a wrapper div.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json contains `"onboarding"` key with `"step1_heading": "Set up your AI receptionist"`
    - messages/es.json contains `"onboarding"` key with Spanish translations
    - src/app/onboarding/page.js contains `tone_preset` and `business_name`
    - src/app/onboarding/page.js contains `text-3xl` and `font-semibold` (Display heading)
    - src/app/onboarding/page.js contains `border-blue-600` (selected card style)
    - src/app/onboarding/services/page.js contains `TRADE_TEMPLATES`
    - src/app/onboarding/services/page.js contains urgency Badge colors: `red-100`, `amber-100`, `slate-100`
    - src/app/api/onboarding/start/route.js contains `auth.getUser()`
    - src/app/api/onboarding/start/route.js contains `from('tenants')`
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Wizard Steps 1 and 2 render with full UI per spec. Owner can enter business name, select tone, pick trade template, see/edit starter services. Data saves to Supabase. Navigation works between steps.</done>
</task>

</tasks>

<verification>
- `npm run build` — succeeds without errors
- `grep "signInWithOAuth" src/app/auth/signin/page.js` — present
- `grep "TRADE_TEMPLATES" src/app/onboarding/services/page.js` — present
- `grep "step1_heading" messages/en.json` — present
</verification>

<success_criteria>
- shadcn initialized with all required components
- Auth flow works (sign-in page, OAuth callback)
- Wizard Steps 1 and 2 render with correct layout, copy, and interaction states
- All UI copy sourced from next-intl translation keys
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-triage/02-03-SUMMARY.md`
</output>
