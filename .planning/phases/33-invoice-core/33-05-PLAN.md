---
phase: 33-invoice-core
plan: 05
type: execute
wave: 2
depends_on: [33-01, 33-03]
files_modified:
  - src/app/dashboard/invoices/new/page.js
  - src/components/dashboard/InvoiceEditor.jsx
  - src/components/dashboard/LineItemRow.jsx
autonomous: true
requirements: [D-02, D-04, D-06, D-07, D-12]
must_haves:
  truths:
    - "Owner can add typed line items (labor, materials, travel, flat-rate, discount) with correct field visibility per type"
    - "Materials line items show markup % field; other types do not"
    - "Each line item has a taxable toggle"
    - "Subtotal, tax (on taxable items only), and total auto-calculate as line items change"
    - "Navigating from a lead pre-fills customer data"
    - "Save as Draft creates a draft invoice; Send Invoice button exists in action bar (delivery wired in Plan 07)"
  artifacts:
    - path: "src/app/dashboard/invoices/new/page.js"
      provides: "Invoice creation page at /dashboard/invoices/new"
    - path: "src/components/dashboard/InvoiceEditor.jsx"
      provides: "Full invoice editor with header, line items, totals, notes"
    - path: "src/components/dashboard/LineItemRow.jsx"
      provides: "Single line item row with type-dependent field visibility"
  key_links:
    - from: "src/app/dashboard/invoices/new/page.js"
      to: "/api/invoices"
      via: "POST fetch on save"
      pattern: "fetch.*api/invoices.*POST"
    - from: "src/components/dashboard/InvoiceEditor.jsx"
      to: "src/lib/invoice-calculations.js"
      via: "import calculateLineTotal, calculateInvoiceTotals"
      pattern: "calculateLineTotal|calculateInvoiceTotals"
---

<objective>
Invoice Editor: Full-page editor for creating invoices with typed line items, auto-calculated totals, customer info fields, and lead pre-fill support.

Purpose: The invoice editor is how contractors create and edit invoices. It must handle 5 line item types with type-dependent field visibility, real-time total calculation, and lead data pre-filling.
Output: Working invoice creation page at /dashboard/invoices/new with line item editor and totals.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-invoice-core/33-CONTEXT.md
@.planning/phases/33-invoice-core/33-UI-SPEC.md
@.planning/phases/33-invoice-core/33-RESEARCH.md

<interfaces>
<!-- Invoice calculations from Plan 01 -->
From src/lib/invoice-calculations.js:
```javascript
export function calculateLineTotal(type, { quantity, unit_price, markup_pct })
// Returns: number (the calculated line total)

export function calculateInvoiceTotals(lineItems, taxRate)
// lineItems: [{ item_type, quantity, unit_price, markup_pct, taxable }]
// Returns: { subtotal: number, tax_amount: number, total: number }
```

<!-- POST /api/invoices body shape from Plan 03 -->
POST /api/invoices expects:
```json
{
  "lead_id": "uuid or null",
  "customer_name": "string",
  "customer_phone": "string",
  "customer_email": "string",
  "customer_address": "string",
  "job_type": "string",
  "issued_date": "YYYY-MM-DD",
  "due_date": "YYYY-MM-DD",
  "notes": "string",
  "payment_terms": "string",
  "line_items": [{
    "item_type": "labor|materials|travel|flat_rate|discount",
    "description": "string",
    "quantity": number,
    "unit_price": number,
    "markup_pct": number,
    "taxable": boolean,
    "sort_order": number
  }]
}
```

<!-- Invoice settings shape from Plan 02 -->
GET /api/invoice-settings returns:
```json
{
  "tax_rate": 0.0825,
  "payment_terms": "Net 30",
  "default_notes": "Thank you for your business!",
  "invoice_prefix": "INV"
}
```

<!-- Line item type field visibility from UI-SPEC -->
| Type | Description | Qty | Rate | Markup % | Taxable |
| labor | yes | yes (hours) | yes (hourly rate) | hidden | yes |
| materials | yes | yes | yes (unit cost) | yes | yes |
| travel | yes | hidden | yes (flat fee) | hidden | yes |
| flat_rate | yes | hidden | yes (amount) | hidden | yes |
| discount | yes | hidden | yes (amount, shown as negative) | hidden | hidden (always false) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LineItemRow component with type-dependent field visibility</name>
  <files>src/components/dashboard/LineItemRow.jsx</files>
  <read_first>
    - src/components/ui/input.jsx (Input component API)
    - src/components/ui/select.jsx (Select, SelectTrigger, SelectContent, SelectItem API)
    - src/components/ui/switch.jsx (Switch component API for taxable toggle)
    - .planning/phases/33-invoice-core/33-UI-SPEC.md (LineItemRow section — line 111, line 183)
    - src/lib/invoice-calculations.js (calculateLineTotal function signature — created in Plan 01)
  </read_first>
  <action>
Create `src/components/dashboard/LineItemRow.jsx` — a 'use client' component for a single line item row.

**Props:**
- `item`: `{ item_type, description, quantity, unit_price, markup_pct, taxable, sort_order }`
- `onChange(updatedItem)`: callback when any field changes
- `onRemove()`: callback to remove this line item
- `index`: row number for display

**Type select:** shadcn Select component with options:
- Labor, Materials, Travel/Trip Charge, Flat Rate, Discount
Values: 'labor', 'materials', 'travel', 'flat_rate', 'discount'

**Field visibility rules per item_type (per D-04):**

| Field | labor | materials | travel | flat_rate | discount |
|-------|-------|-----------|--------|-----------|----------|
| Description | Input | Input | Input | Input | Input |
| Quantity | Input (label: "Hours") | Input (label: "Qty") | hidden | hidden | hidden |
| Unit Price | Input (label: "Rate/hr") | Input (label: "Unit Cost") | Input (label: "Fee") | Input (label: "Amount") | Input (label: "Amount") |
| Markup % | hidden | Input (label: "Markup %") | hidden | hidden | hidden |
| Taxable | Switch | Switch | Switch | Switch | hidden (always false) |

**Calculated line total:** Display the calculated total at the end of the row using `calculateLineTotal(item.item_type, item)` from `@/lib/invoice-calculations`. Import this function. Show as `$XX.XX` right-aligned. For discount type, display in red text.

**Remove button:** Trash2 icon (lucide) at the far right. `onClick={onRemove}`.

**Layout:** Flex row on desktop, wrapping on mobile. Type select is ~120px wide. Description is flex-1. Number inputs are ~80px wide. Line total is ~100px wide right-aligned.

**When type changes:** Reset quantity to 1, markup_pct to 0, and taxable based on type (discount defaults to false, others default to true). Call onChange with the reset values.

**Number input handling:** Use `type="number"` with `step="0.01"` for unit_price and markup. Parse values with `parseFloat` on change. Default to 0 if NaN.
  </action>
  <verify>
    <automated>test -f src/components/dashboard/LineItemRow.jsx && grep -q "calculateLineTotal" src/components/dashboard/LineItemRow.jsx && grep -q "Trash2" src/components/dashboard/LineItemRow.jsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/LineItemRow.jsx exists as 'use client' component
    - Component imports `calculateLineTotal` from `@/lib/invoice-calculations`
    - Type select has 5 options: labor, materials, travel, flat_rate, discount
    - Markup % field is only rendered when item_type is 'materials'
    - Quantity field is hidden for travel, flat_rate, discount types
    - Taxable switch is hidden for discount type
    - Calculated line total is displayed using `calculateLineTotal`
    - Discount line total displays in red text
    - Trash2 remove button exists
    - Type change resets quantity, markup_pct, and taxable to type-appropriate defaults
  </acceptance_criteria>
  <done>LineItemRow renders type-dependent fields correctly for all 5 types, calculates and displays line total, handles remove action</done>
</task>

<task type="auto">
  <name>Task 2: Create InvoiceEditor and new invoice page with lead pre-fill</name>
  <files>src/components/dashboard/InvoiceEditor.jsx, src/app/dashboard/invoices/new/page.js</files>
  <read_first>
    - src/components/dashboard/LeadFlyout.jsx (lead data shape for pre-fill: customer_name, phone, address, job_type)
    - src/app/api/leads/[id]/route.js (lead GET response shape for pre-fill fetch)
    - .planning/phases/33-invoice-core/33-UI-SPEC.md (Invoice Editor section — lines 182-186, Action bar — line 186)
    - .planning/phases/33-invoice-core/33-CONTEXT.md (D-02 entry points, D-07 default terms)
  </read_first>
  <action>
**Create `src/components/dashboard/InvoiceEditor.jsx`:**

A 'use client' component that renders the full invoice editor form.

**Props:**
- `initialData`: optional object for pre-filling (from lead data or for editing)
- `settings`: invoice settings object (tax_rate, payment_terms, default_notes, etc.)
- `onSave(invoiceData)`: callback when "Save as Draft" is clicked
- `onSend(invoiceData)`: callback when "Send Invoice" is clicked
- `saving`: boolean for button loading state

**State:**
- `customerName`, `customerPhone`, `customerEmail`, `customerAddress`, `jobType` — string fields
- `issuedDate` — default to today (`new Date().toISOString().split('T')[0]`)
- `dueDate` — default to today + payment terms days. Parse "Net 30" → 30 days. Use `addDays` from date-fns.
- `notes` — pre-filled from `settings.default_notes` (per D-07)
- `paymentTerms` — pre-filled from `settings.payment_terms`
- `lineItems` — array of line item objects, starting with one empty labor item: `[{ item_type: 'labor', description: '', quantity: 1, unit_price: 0, markup_pct: 0, taxable: true, sort_order: 0 }]`

**Pre-fill from initialData:** If provided, set customer fields and jobType from initialData.

**Layout (top to bottom):**

1. **Customer info section** (Card):
   - Customer Name: Input (required)
   - Customer Email: Input type="email"
   - Customer Phone: Input type="tel"
   - Customer Address: Input (or textarea for multi-line)
   - Job Type: Input
   - Layout: 2-col grid on desktop, stacked on mobile

2. **Dates section** (inline with customer or separate row):
   - Issue Date: Input type="date", default today
   - Due Date: Input type="date", auto-calculated from payment terms
   - Payment Terms: Select (Net 15/30/45/60), changing this recalculates due date

3. **Line items section** (Card):
   - Header: "Line Items" label
   - Render each item in `lineItems` array as `<LineItemRow>` with index, onChange, onRemove
   - "Add Line Item" button at bottom (outline style, Plus icon from lucide). Adds a new item with defaults: `{ item_type: 'labor', description: '', quantity: 1, unit_price: 0, markup_pct: 0, taxable: true, sort_order: lineItems.length }`

4. **Totals section** (right-aligned below line items, inside the same Card or below it):
   - Calculate using `calculateInvoiceTotals(lineItems, settings.tax_rate)` from `@/lib/invoice-calculations`
   - Subtotal: label "Subtotal", value right-aligned
   - Tax: label "Tax ({taxRate * 100}%)", value right-aligned. Only show if tax_amount > 0.
   - Total Due: label "Total Due", value right-aligned, 20px semibold, stone-900
   - Use Separator between subtotal and total

5. **Notes section:**
   - Textarea for notes, pre-filled from `settings.default_notes`
   - Label: "Notes"

6. **Action bar:**
   - Desktop: Right-aligned button group at bottom
   - Mobile: Sticky bottom bar (fixed, z-40, bg-white, border-t, p-4)
   - Buttons:
     - "Save as Draft" — outline variant, calls `onSave` with assembled invoice data
     - "Send Invoice" — primary/accent (`bg-[#C2410C]`), calls `onSend` with assembled invoice data
   - Both buttons show spinner when `saving` is true

**Assembled invoice data shape** (matches POST /api/invoices body):
```javascript
{
  lead_id: initialData?.lead_id || null,
  customer_name: customerName,
  customer_phone: customerPhone,
  customer_email: customerEmail,
  customer_address: customerAddress,
  job_type: jobType,
  issued_date: issuedDate,
  due_date: dueDate,
  notes,
  payment_terms: paymentTerms,
  line_items: lineItems.map((item, i) => ({ ...item, sort_order: i }))
}
```

**Create `src/app/dashboard/invoices/new/page.js`:**

A 'use client' page that:
1. Reads `lead_id` from URL search params: `useSearchParams().get('lead_id')`.
2. On mount, fetches invoice settings: `GET /api/invoice-settings`.
3. If `lead_id` is present, also fetches lead data: `GET /api/leads/${lead_id}`. Extract `customer_name` (from lead's `caller_name` field), `customer_phone` (from `caller_phone`), `customer_address` (from `service_address`), `job_type` (from `service_type`). Pass as `initialData` to InvoiceEditor per D-02.
4. Renders `<InvoiceEditor>` with settings and initialData.
5. `onSave` handler: POST to `/api/invoices`, navigate to `/dashboard/invoices/${created.id}` on success. Toast: "Invoice saved as draft".
6. `onSend` handler: POST to `/api/invoices` first (to create it), then POST to `/api/invoices/${created.id}/send` (delivery handled in Plan 07 — for now just save and navigate). Toast: "Invoice created".
7. Page header: Back arrow (ArrowLeft from lucide) linking to `/dashboard/invoices`, title "New Invoice".
  </action>
  <verify>
    <automated>test -f src/components/dashboard/InvoiceEditor.jsx && test -f src/app/dashboard/invoices/new/page.js && grep -q "LineItemRow" src/components/dashboard/InvoiceEditor.jsx && grep -q "calculateInvoiceTotals" src/components/dashboard/InvoiceEditor.jsx && grep -q "lead_id" src/app/dashboard/invoices/new/page.js && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/InvoiceEditor.jsx exists as 'use client' component
    - InvoiceEditor imports and renders LineItemRow for each line item
    - InvoiceEditor imports `calculateInvoiceTotals` from `@/lib/invoice-calculations`
    - InvoiceEditor has customer info fields: name, email, phone, address, job type
    - InvoiceEditor has date fields: issued_date (default today), due_date (auto-calculated from payment terms)
    - "Add Line Item" button exists and adds a new line item with labor defaults
    - Totals section shows Subtotal, Tax line (when > 0), and Total Due
    - Notes textarea is pre-filled from `settings.default_notes` per D-07
    - Action bar has "Save as Draft" and "Send Invoice" buttons
    - src/app/dashboard/invoices/new/page.js reads `lead_id` from search params per D-02
    - New page fetches lead data when lead_id is present and passes as initialData
    - New page fetches invoice settings on mount
    - New page POSTs to `/api/invoices` on save
  </acceptance_criteria>
  <done>Invoice editor renders with typed line items, auto-calculated totals, customer info, notes pre-fill, and lead data pre-fill. New invoice page handles save-as-draft and initial send flow.</done>
</task>

</tasks>

<verification>
- Invoice editor renders with all sections (customer, dates, line items, totals, notes, actions)
- Adding/removing line items updates totals in real time
- Changing line item type shows/hides correct fields
- Materials shows markup %, others don't
- Lead pre-fill works when navigating with ?lead_id=
- `npm test -- --passWithNoTests` — existing tests still pass
</verification>

<success_criteria>
- Line item type selector shows all 5 types with correct field visibility per type
- Totals auto-calculate as line items are added/modified/removed
- Tax is calculated only on taxable items using settings.tax_rate
- Customer data pre-fills from lead when lead_id is in URL
- Default notes and payment terms come from invoice settings
- Save as Draft creates the invoice via API and navigates to detail page
- Mobile layout has sticky bottom action bar
</success_criteria>

<output>
After completion, create `.planning/phases/33-invoice-core/33-05-SUMMARY.md`
</output>
