---
phase: 33-invoice-core
plan: 04
type: execute
wave: 2
depends_on: [33-01, 33-03]
files_modified:
  - src/app/dashboard/invoices/page.js
  - src/components/dashboard/InvoiceSummaryCards.jsx
  - src/components/dashboard/InvoiceStatusBadge.jsx
autonomous: true
requirements: [D-13, D-15]
must_haves:
  truths:
    - "Owner sees three summary cards: Total Outstanding, Overdue Amount, Paid This Month"
    - "Owner can filter invoices by status (All / Draft / Sent / Overdue / Paid)"
    - "Invoice table shows invoice #, customer, job type, amount, dates, status badge"
    - "Clicking an invoice row navigates to the detail page"
    - "Empty state shows when no invoices exist"
  artifacts:
    - path: "src/app/dashboard/invoices/page.js"
      provides: "Full Invoices tab page with summary cards, filter tabs, table"
    - path: "src/components/dashboard/InvoiceSummaryCards.jsx"
      provides: "Three summary metric cards"
    - path: "src/components/dashboard/InvoiceStatusBadge.jsx"
      provides: "Reusable status badge component for invoice statuses"
  key_links:
    - from: "src/app/dashboard/invoices/page.js"
      to: "/api/invoices"
      via: "fetch on mount and on status filter change"
      pattern: "fetch.*api/invoices"
    - from: "src/app/dashboard/invoices/page.js"
      to: "/dashboard/invoices/[id]"
      via: "router.push on row click"
      pattern: "router.push.*invoices"
---

<objective>
Invoice List UI: Full Invoices tab page with summary cards, status filter tabs with counts, sortable invoice table, status badges, loading states, and empty states.

Purpose: This is the primary view contractors will see daily — a quick glance at outstanding/overdue/paid amounts and a filterable list of all invoices.
Output: Working Invoices tab that fetches from the API and renders invoice data.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-invoice-core/33-CONTEXT.md
@.planning/phases/33-invoice-core/33-UI-SPEC.md
@.planning/phases/33-invoice-core/33-RESEARCH.md

<interfaces>
<!-- API response shape from Plan 03 -->
GET /api/invoices returns:
```json
{
  "invoices": [{ "id", "invoice_number", "customer_name", "job_type", "total", "issued_date", "due_date", "status", "lead_id" }],
  "summary": { "total_outstanding": number, "overdue_amount": number, "paid_this_month": number },
  "status_counts": { "draft": n, "sent": n, "paid": n, "overdue": n, "void": n }
}
```
GET /api/invoices?status=draft — filters by status

<!-- Status badge colors from UI-SPEC -->
| Status | Background | Text | Border |
| Draft | stone-100 | stone-600 | stone-300 |
| Sent | blue-50 | blue-700 | blue-200 |
| Paid | emerald-50 | emerald-700 | emerald-200 |
| Overdue | red-50 | red-700 | red-200 |
| Void | stone-100 | stone-400 | stone-200 |

<!-- Summary card accent colors from UI-SPEC -->
| Total Outstanding | #C2410C (brandOrange) |
| Overdue Amount | #DC2626 (red-600) |
| Paid This Month | #059669 (emerald-600) |

<!-- Design tokens -->
card.base, heading (20px semibold), body (14px regular), Display (28px semibold)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InvoiceStatusBadge and InvoiceSummaryCards components</name>
  <files>src/components/dashboard/InvoiceStatusBadge.jsx, src/components/dashboard/InvoiceSummaryCards.jsx</files>
  <read_first>
    - src/app/dashboard/more/billing/page.js (status badge config pattern at lines 196-208, card layout patterns)
    - src/components/ui/badge.jsx (Badge component API)
    - src/lib/design-tokens.js (card.base, colors)
    - .planning/phases/33-invoice-core/33-UI-SPEC.md (Status Badge Colors and Summary Card Accent Colors)
  </read_first>
  <action>
**Create `src/components/dashboard/InvoiceStatusBadge.jsx`:**

A 'use client' component that renders a Badge with status-specific styling per D-13 and UI-SPEC.

```javascript
'use client';
import { Badge } from '@/components/ui/badge';

const STATUS_CONFIG = {
  draft:   { label: 'Draft',   className: 'bg-stone-100 text-stone-600 border-stone-300' },
  sent:    { label: 'Sent',    className: 'bg-blue-50 text-blue-700 border-blue-200' },
  paid:    { label: 'Paid',    className: 'bg-emerald-50 text-emerald-700 border-emerald-200' },
  overdue: { label: 'Overdue', className: 'bg-red-50 text-red-700 border-red-200' },
  void:    { label: 'Void',    className: 'bg-stone-100 text-stone-400 border-stone-200' },
};

export default function InvoiceStatusBadge({ status }) {
  const config = STATUS_CONFIG[status] || STATUS_CONFIG.draft;
  return <Badge variant="outline" className={config.className}>{config.label}</Badge>;
}
```

Export `STATUS_CONFIG` as a named export too so other components can reference it.

**Create `src/components/dashboard/InvoiceSummaryCards.jsx`:**

A 'use client' component accepting `summary` prop with `{ total_outstanding, overdue_amount, paid_this_month }`.

Three cards in a responsive grid:
- Desktop: 3-col grid (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`)
- Mobile: stacked

Each card structure (use shadcn Card component):
```
[Icon]  [Label]
[Amount in Display typography (28px semibold)]
```

Card 1 — Total Outstanding:
- Icon: `DollarSign` from lucide, color `text-[#C2410C]`
- Label: "Total Outstanding" (14px medium, stone-500)
- Value: formatted as `$X,XXX.XX` with `text-[#C2410C]` color, 28px semibold font

Card 2 — Overdue Amount:
- Icon: `AlertCircle` from lucide, color `text-red-600`
- Label: "Overdue Amount" (14px medium, stone-500)
- Value: formatted as `$X,XXX.XX` with `text-red-600` color, 28px semibold font

Card 3 — Paid This Month:
- Icon: `CheckCircle2` from lucide, color `text-emerald-600`
- Label: "Paid This Month" (14px medium, stone-500)
- Value: formatted as `$X,XXX.XX` with `text-emerald-600` color, 28px semibold font

Use `Number(value).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })` for formatting.

Card background: white (`bg-white`), rounded-lg, border, p-4 (16px padding per UI-SPEC md spacing).

Accept a `loading` prop — when true, render 3 Skeleton cards (Skeleton height 80px).
  </action>
  <verify>
    <automated>test -f src/components/dashboard/InvoiceStatusBadge.jsx && test -f src/components/dashboard/InvoiceSummaryCards.jsx && grep -q "STATUS_CONFIG" src/components/dashboard/InvoiceStatusBadge.jsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - InvoiceStatusBadge.jsx exports a default component and a named `STATUS_CONFIG` export
    - STATUS_CONFIG has entries for all 5 statuses: draft, sent, paid, overdue, void
    - Each status config has `label` and `className` with colors matching UI-SPEC exactly
    - InvoiceSummaryCards.jsx renders 3 Card components in a responsive grid
    - Card values use 28px semibold font (text-[28px] font-semibold or equivalent)
    - Card accent colors: brandOrange for Outstanding, red-600 for Overdue, emerald-600 for Paid
    - InvoiceSummaryCards.jsx handles `loading` prop with Skeleton fallbacks
  </acceptance_criteria>
  <done>Status badge component supports all 5 invoice statuses with correct colors. Summary cards display Outstanding/Overdue/Paid metrics with accent colors and loading states.</done>
</task>

<task type="auto">
  <name>Task 2: Build full Invoices tab page with filter tabs, table, and empty states</name>
  <files>src/app/dashboard/invoices/page.js</files>
  <read_first>
    - src/app/dashboard/invoices/page.js (current placeholder from Plan 03 — will be replaced)
    - src/app/dashboard/more/billing/page.js (reference for table layout, skeleton loading, data fetching pattern)
    - src/components/ui/table.jsx (Table component API: Table, TableHeader, TableBody, TableRow, TableCell, TableHead)
    - src/components/ui/skeleton.jsx (Skeleton component for loading states)
    - .planning/phases/33-invoice-core/33-UI-SPEC.md (Invoice List Page section — lines 175-179, Loading/Error states — lines 266-272)
  </read_first>
  <action>
Replace the placeholder `src/app/dashboard/invoices/page.js` with the full Invoices tab page.

'use client' component with the following structure:

**State:**
- `invoices` array, `summary` object, `statusCounts` object
- `loading` boolean (true initially)
- `error` string or null
- `activeStatus` string ('all' by default)

**Data fetching:**
On mount and when `activeStatus` changes, call:
```javascript
const url = activeStatus === 'all' ? '/api/invoices' : `/api/invoices?status=${activeStatus}`;
const res = await fetch(url);
const data = await res.json();
```
Set `invoices`, `summary`, `statusCounts` from response. Summary is only fetched once (not re-fetched on filter change — it represents overall metrics).

**Page layout (top to bottom):**

1. **Header row:** "Invoices" (20px semibold heading) on left. "Create Invoice" button (brandOrange `bg-[#C2410C] hover:bg-[#9A3412] text-white`) on right with Plus icon from lucide. Button navigates to `/dashboard/invoices/new`. On mobile, this becomes a floating action button (fixed bottom-right, rounded-full, 56px, z-50).

2. **Summary cards:** `<InvoiceSummaryCards summary={summary} loading={loading} />`

3. **Status filter tabs:** Horizontal row of tabs with counts. Tab items:
   - All ({total count})
   - Draft ({statusCounts.draft || 0})
   - Sent ({statusCounts.sent || 0})
   - Overdue ({statusCounts.overdue || 0})
   - Paid ({statusCounts.paid || 0})

   Active tab: text-[#C2410C] with border-b-2 border-[#C2410C] underline (brandOrange, 2px per UI-SPEC).
   Inactive tab: text-stone-500 hover:text-stone-700.
   Tab bar has border-b border-stone-200 full width.

4. **Invoice table (desktop):** shadcn Table component.
   - Columns: Invoice # | Customer | Job Type | Amount | Issued | Due | Status
   - Each row is clickable (cursor-pointer, hover:bg-stone-50). On click: `router.push(\`/dashboard/invoices/${invoice.id}\`)`.
   - Invoice #: `invoice_number` text (14px medium, stone-900)
   - Customer: `customer_name` (14px regular, stone-700)
   - Job Type: `job_type` (14px regular, stone-500)
   - Amount: `$${total.toLocaleString('en-US', { minimumFractionDigits: 2 })}` (14px medium, stone-900)
   - Issued: `format(new Date(issued_date), 'MMM d, yyyy')` using date-fns (14px regular, stone-500)
   - Due: same format (14px regular, stone-500)
   - Status: `<InvoiceStatusBadge status={status} />`

5. **Invoice cards (mobile, < 640px):** Instead of table, show card layout. Each card shows: invoice_number + customer_name on top line, amount on right, status badge below, due date small. Use `hidden sm:table` on the Table and `sm:hidden` on the card list to toggle.

6. **Loading state:** 3 Skeleton cards (summary) + 5 Skeleton table rows (same height as real rows, alternating widths for visual variety).

7. **Empty state (no invoices at all):**
   - Centered: FileText icon (lucide, 48px, stone-300)
   - Heading: "No invoices yet" (20px semibold, stone-900)
   - Body: "Create your first invoice from a completed job or start a new one. Your customers will receive a professional invoice with your business branding." (14px regular, stone-500, max-w-md, text-center)
   - CTA: "Create Your First Invoice" button (brandOrange)

8. **Empty state (filtered, no results):**
   - Heading: "No {status} invoices" (capitalize status)
   - Body: "You don't have any invoices with this status."

9. **Error state:** "Couldn't load invoices. Check your connection and try again." with retry button.

Import `useRouter` from `next/navigation` for row click navigation. Import `format` from `date-fns`.
  </action>
  <verify>
    <automated>grep -q "InvoiceSummaryCards" src/app/dashboard/invoices/page.js && grep -q "InvoiceStatusBadge" src/app/dashboard/invoices/page.js && grep -q "Create Invoice" src/app/dashboard/invoices/page.js && grep -q "No invoices yet" src/app/dashboard/invoices/page.js && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/invoices/page.js is 'use client' component
    - Page imports and renders InvoiceSummaryCards and InvoiceStatusBadge
    - Page fetches from `/api/invoices` on mount
    - Status filter tabs exist for All, Draft, Sent, Overdue, Paid with counts
    - Active tab has brandOrange underline (`border-[#C2410C]`)
    - Table has 7 columns: Invoice #, Customer, Job Type, Amount, Issued, Due, Status
    - Table rows are clickable with `router.push` to `/dashboard/invoices/${id}`
    - Mobile card layout exists with `sm:hidden` class
    - Empty state contains "No invoices yet" heading and "Create Your First Invoice" CTA
    - Filtered empty state contains "No {status} invoices"
    - Loading state renders Skeleton components
    - "Create Invoice" button links to `/dashboard/invoices/new`
  </acceptance_criteria>
  <done>Full Invoices tab page with summary cards, status filter tabs with counts, responsive table/card layout, row click navigation, loading states, empty states, and Create Invoice button</done>
</task>

</tasks>

<verification>
- Invoice list page renders with summary cards, filter tabs, and table
- Clicking a status tab filters the list
- Empty state shows correctly when no invoices exist
- Loading skeletons appear while data is being fetched
- `npm test -- --passWithNoTests` — existing tests still pass
</verification>

<success_criteria>
- Summary cards show Total Outstanding, Overdue Amount, Paid This Month with correct accent colors
- Status filter tabs show counts and highlight active tab with brandOrange underline
- Invoice table shows all required columns with correct formatting
- Table rows navigate to invoice detail on click
- Mobile layout uses cards instead of table
- Empty states match UI-SPEC copywriting exactly
- Loading states use Skeleton components
</success_criteria>

<output>
After completion, create `.planning/phases/33-invoice-core/33-04-SUMMARY.md`
</output>
