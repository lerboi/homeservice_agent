---
phase: 61.3-phase-61-production-regressions-round-3-cascade-recovery-slo
plan: 02
subsystem: testing
tags: [pytest, livekit-agent, cascade-recovery, invariants, substring-grep, gemini-3.1-flash-live]

# Dependency graph
requires:
  - phase: 61.3-01
    provides: cascade-recovery data-plumbing prerequisites
  - phase: 61.2
    provides: substring-grep invariant test pattern (tests/test_tool_mute_invariants.py)
provides:
  - Wave 0 invariant test scaffold guarding cascade-recovery mechanism (D-03/D-05/D-06/D-08)
  - 6 substring-grep tests over _availability_lib.py source text
  - Pre-Plan-03 RED state captured (4 of 6 tests fail; 2 pass incidentally)
affects: [61.3-03, 61.3-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Substring-grep invariant tests (no mocks, no fixtures, no SDK imports — pathlib reads only)"

key-files:
  created:
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_cascade_recovery_invariants.py
  modified: []

key-decisions:
  - "D-10: Plan 02 implements the test scaffold ahead of Plan 03 (Wave 0 from VALIDATION.md) so the recovery implementation has a binding spec to build against."
  - "Tests are intentionally RED at end of plan; turning GREEN is Plan 03's acceptance signal."

patterns-established:
  - "Wave-0 test-first scaffold: write the substring guards before the feature lands; RED is the expected end state for the test-only plan."

requirements-completed: []

# Metrics
duration: ~12min
completed: 2026-05-07
---

# Phase 61.3 Plan 02: Cascade-Recovery Invariant Tests Summary

**6 substring-grep invariant tests guarding the Plan 03 cascade-recovery mechanism — pure structural guards over `_availability_lib.py` source text, RED until Plan 03 lands `_attempt_tool_result_replay`.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-07T07:49Z
- **Completed:** 2026-05-07T08:01Z
- **Tasks:** 1
- **Files modified:** 1 (created)

## Accomplishments

- Created `tests/test_cascade_recovery_invariants.py` in the sibling `livekit-agent` repo with 6 test functions mirroring the `test_tool_mute_invariants.py` shape exactly.
- Verified the file has zero SDK imports, zero `unittest`/mocks, and only `pathlib.Path` — pure structural guards as D-10 mandates.
- Confirmed AST parses, exactly 6 `def test_*` functions, all 6 names match the VALIDATION.md per-task verification map row-for-row.
- Confirmed 61.2 invariant suite (`test_tool_mute_invariants.py`) still GREEN — 5/5 pass — no regression on prevention-side guards.
- Captured the expected RED state for the new suite so Plan 03 has a binding turn-green signal.

## Task Commits

Each task was committed atomically (in the `livekit-agent` sibling repo):

1. **Task 1: Create tests/test_cascade_recovery_invariants.py with 6 substring-grep tests** — `livekit-agent@ed57de4` (test)

The corresponding SUMMARY.md commit lands in the `homeservice_agent` worktree as the metadata commit (orchestrator-managed).

_Note: per parallel-execution mode, all commits use `--no-verify`; the orchestrator runs hooks once after all wave agents complete._

## Files Created/Modified

- `C:/Users/leheh/.Projects/livekit-agent/tests/test_cascade_recovery_invariants.py` — 6 substring-grep tests guarding D-03/D-05/D-06/D-08 (96 lines including docstring, helpers, and tests)

## Test Status at End of Plan (RED-by-design)

Running `python -m pytest tests/test_cascade_recovery_invariants.py --tb=short -q` from the `livekit-agent` repo yields **`4 failed, 2 passed`** — the suite is RED as VALIDATION.md Wave 0 requires.

| Test | Status | Reason |
|------|--------|--------|
| `test_replay_path_in_fallback` | ❌ RED | `await _attempt_tool_result_replay` not yet present (helper undefined). Plan 03 will land it. |
| `test_replay_uses_update_chat_ctx` | ❌ RED | `update_chat_ctx` + `FunctionCallOutput` not yet imported/called from `_availability_lib.py`. Plan 03 will land it. |
| `test_replay_not_generate_reply` | ✅ green | `generate_reply` was already absent from `_availability_lib.py`. Stays green when Plan 03 lands (negative invariant — guards against accidental introduction). |
| `test_replay_before_set_audio_enabled` | ✅ green (incidentally) | Both substrings exist as text — `_attempt_tool_result_replay` appears in a pre-existing comment at line 132 ("consumed by _attempt_tool_result_replay") and `set_audio_enabled(True)` exists at line 169. The ordering check passes because the comment precedes the unmute call in source order. The test will continue to pass (correctly) when Plan 03 lands the actual `await` invocation between those two lines. |
| `test_stall_recovery_counters_present` | ❌ RED | `stalled_generation_recoveries` / `stalled_generation_replay_failed` keys not yet emitted from `_availability_lib.py`. Plan 03 will land them. |
| `test_stall_counters_conditional_emit` | ❌ RED | `.get("stalled_generation_recoveries", 0)` / `.get("stalled_generation_replay_failed", 0)` patterns not yet present. Plan 03 will land them. |

**Plan 03 acceptance signal:** all 6 tests turn GREEN.

**Note on the 2 incidentally-passing tests:** the plan's acceptance criteria explicitly anticipated this — "at least `test_replay_path_in_fallback` fails" and "`test_replay_not_generate_reply` may pass at this stage — that's fine; the suite as a whole is RED until Plan 03." Confirmed: suite-level state is RED with 4 failures.

**Regression check (61.2 suite must stay green):**
```
tests/test_tool_mute_invariants.py .....                                 [100%]
============================== 5 passed in 0.04s ==============================
```

## Decisions Made

None - plan executed exactly as written. The exact test content was specified verbatim in the plan's `<action>` block; followed it character-for-character.

## Deviations from Plan

None - plan executed exactly as written.

The only out-of-band action was the mandatory worktree-base reset at start: this worktree's HEAD was `3c48708` (an older state without Phase 61.3 plan files), not based on the expected `2bbfaf4`. Per `<worktree_branch_check>` and the `feedback_gsd_worktree_stale_base_windows` memory, ran `git reset --hard 2bbfaf4` to bring the worktree onto the correct base before any work began. This is the documented Windows GSD parallel-executor workaround, not a plan deviation.

## Issues Encountered

- `pytest` was not on the bash PATH (only `python -m pytest` works). Switched verification commands accordingly. No code impact.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 has a binding test specification: it must implement `_attempt_tool_result_replay` (awaited from `_unmute_logic`'s TimeoutError fallback), call `update_chat_ctx` with a `FunctionCallOutput`, fire the replay BEFORE `set_audio_enabled(True)`, and emit `stalled_generation_recoveries` / `stalled_generation_replay_failed` counters via the `.get(key, 0) + 1` conditional-emit pattern.
- All 6 tests will turn GREEN when Plan 03 lands; that is Plan 03's automated acceptance signal.
- Plan 04 (D-12 SKILL closeout + live UAT) is unaffected by this plan.

## Self-Check: PASSED

- [x] `C:/Users/leheh/.Projects/livekit-agent/tests/test_cascade_recovery_invariants.py` exists (verified via Bash test -f).
- [x] Commit `ed57de4` exists in `livekit-agent` repo (verified via `git rev-parse --short HEAD`).
- [x] AST parses (`python -c "import ast; ast.parse(...)"` printed `AST OK`).
- [x] 6 `def test_*` functions present (Grep count = 6).
- [x] No `import livekit` (Grep count = 0).
- [x] No `from unittest` (Grep count = 0).
- [x] 61.2 invariant suite still GREEN (5/5).
- [x] New cascade suite RED (4 failed, 2 passed — overall RED, matches plan expectation).

---
*Phase: 61.3-phase-61-production-regressions-round-3-cascade-recovery-slo*
*Completed: 2026-05-07*
