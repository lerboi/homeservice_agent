---
phase: 61.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py
autonomous: true
requirements: []
addresses_decisions: [D-05, D-09]
repo: livekit-agent
must_haves:
  truths:
    - "Every STATE+DIRECTIVE return path in 6 data-fetch tools writes the return string to deps['_last_tool_state'] before returning"
    - "_on_tools_executed listener in mute_input_during_tool stores deps['_last_tool_call_id'] and deps['_last_tool_name'] from the FunctionToolsExecutedEvent"
    - "No regression to existing 61.2 invariants (mute calls, fallback timer, listener registration)"
  artifacts:
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py"
      provides: "Listener side-effect that captures last tool call_id and name"
      contains: '"_last_tool_call_id"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py"
      provides: "STATE-string capture into deps['_last_tool_state']"
      contains: '"_last_tool_state"'
  key_links:
    - from: "tool return paths"
      to: "deps['_last_tool_state']"
      via: "assignment immediately before each `return STATE...` statement"
    - from: "_on_tools_executed listener"
      to: "deps['_last_tool_call_id'] / deps['_last_tool_name']"
      via: "FunctionToolsExecutedEvent.function_calls[-1] read inside the existing listener"
---

<objective>
Add the data-capture plumbing required by Plan 03's cascade-recovery replay. Every data-fetch tool writes its STATE+DIRECTIVE return string to `deps["_last_tool_state"]` before returning, and the existing `_on_tools_executed` listener in `mute_input_during_tool` is extended with a side-effect that captures the `call_id` and `name` of the last function call from `FunctionToolsExecutedEvent`.

Purpose: `_tool_call_log` does NOT currently store the raw STATE string (RESEARCH § 3 verified). The replay helper in Plan 03 needs three deps keys to construct a valid `FunctionCallOutput`: `_last_tool_state` (the output text), `_last_tool_call_id` (the SDK-assigned id of the cancelled function call), `_last_tool_name` (the tool name). This plan creates that data without touching the recovery mechanism itself.

Output: 7 modified files; no behavior change to the call path; populates three new `deps` keys; preserves all existing 61.2 invariants.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-VALIDATION.md

<interfaces>
<!-- Pulled from RESEARCH.md § 3 (verified against installed SDK). -->
<!-- The _on_tools_executed listener is already attached at _availability_lib.py:132. -->
<!-- It receives a FunctionToolsExecutedEvent. -->

From `livekit/agents/llm/chat_context.py:343-366` (READ-ONLY — do NOT modify):
```python
class FunctionCall(BaseModel):
    id: str
    type: Literal["function_call"] = "function_call"
    name: str
    call_id: str           # The SDK-assigned id used by Gemini for FunctionResponse.id
    arguments: str
    created_at: float
```

From `livekit/agents/voice/events.py` — `FunctionToolsExecutedEvent`:
```python
# event.function_calls: list[FunctionCall]   # the calls that just executed
# event.function_call_outputs: list[FunctionCallOutput]   # their results (already sent to LLM)
```

The listener already exists at `_availability_lib.py:122-130`. This plan adds a side-effect to capture the last call's `call_id` and `name` into `deps`.

Existing tool STATE return shape (from `check_day.py:106-118`):
```python
if all_slots:
    log_tool_call(deps, {...})
    return (
        f"STATE:day_has_slots date_label={date_label} count={len(all_slots)}"
        " | DIRECTIVE:confirm the day is open; ask for a concrete hour; do not mention times."
    )
```

After this plan:
```python
if all_slots:
    log_tool_call(deps, {...})
    state = (
        f"STATE:day_has_slots date_label={date_label} count={len(all_slots)}"
        " | DIRECTIVE:confirm the day is open; ask for a concrete hour; do not mention times."
    )
    deps["_last_tool_state"] = state
    return state
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add _on_tools_executed side-effect to capture call_id + name in _availability_lib.py</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py</files>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py (full file — focus lines 122-130 where the listener lives)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md (§ 3 "Source of the last tool's STATE string and call_id" + § "Architecture Patterns" subsection "Tool STATE string capture")
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-CONTEXT.md (D-05 — replay path uses these deps keys)
  </read_first>
  <action>
    Modify `_on_tools_executed` in `_availability_lib.py` (currently lines 122-130) to add a side-effect that captures the last function call's `call_id` and `name`. The current body resets `saw_fresh_speaking[0] = False`; ADD capture below it. Replace the existing function with:

    ```python
    def _on_tools_executed(event):
        # Phase 61.2 Fix B: a fresh tool execution during the mute window
        # means we are inside a recovery generation step (Gemini retried after
        # a server cancel). Reset the listener so the unmute waits for the
        # NEW generation's clean speak/listen cycle, not the cancelled one.
        try:
            saw_fresh_speaking[0] = False
        except Exception:
            pass
        # Phase 61.3 D-05: capture last function-call call_id + name for the
        # cascade-recovery replay path (consumed by _attempt_tool_result_replay
        # added in Plan 03). FunctionToolsExecutedEvent.function_calls is a
        # list[FunctionCall]; use the most recent entry. Best-effort — null
        # guards in the replay helper handle missing keys.
        try:
            fcs = getattr(event, "function_calls", None) or []
            if fcs:
                last_fc = fcs[-1]
                deps["_last_tool_call_id"] = getattr(last_fc, "call_id", None)
                deps["_last_tool_name"] = getattr(last_fc, "name", None)
        except Exception:
            pass
    ```

    The listener parameter is renamed from `_event` to `event` (the underscore convention indicated unused; we now use it). Do NOT modify the listener REGISTRATION line at L132 — only the body. Do NOT modify the cleanup block at L144-152 — leave the existing `off`/`remove_listener` calls intact.

    Do NOT add a new `_last_tool_state` write here — that lives in Tasks 2-7 inside each tool file. This task only modifies the `_on_tools_executed` body.
  </action>
  <acceptance_criteria>
    - `grep -n '"_last_tool_call_id"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` returns at least one line inside the `_on_tools_executed` function body (line number between 120 and 145).
    - `grep -n '"_last_tool_name"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` returns at least one line in the same range.
    - `grep -n 'getattr(last_fc, "call_id"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` returns one line.
    - `grep -n 'session.on("function_tools_executed"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` STILL returns the existing registration (regression guard for 61.2 invariant `test_function_tools_executed_listener`).
    - `grep -c 'session.off("function_tools_executed"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` returns 1 (cleanup line preserved).
    - `python -c "import ast; ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py').read())"` exits 0 (syntax valid).
    - `pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x` passes all 5 existing 61.2 tests.
  </acceptance_criteria>
  <verify>
    <automated>pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x</automated>
  </verify>
  <done>Listener side-effect added; existing 61.2 invariants pass; AST parses; new deps keys are written but not yet consumed (Plan 03 consumes them).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture STATE string in deps in check_day.py (3 return paths)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py</files>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py (focus lines 76-118)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md (§ "Tool STATE string capture")
  </read_first>
  <action>
    For every `return` statement in `check_day.py` that produces a STATE+DIRECTIVE string (single-line OR multi-line tuple/parenthesized form), pull the literal into a local variable `state`, write `deps["_last_tool_state"] = state`, then `return state`.

    Specifically modify these return sites in `check_day.py`:

    1. Line 76 (in the wrapper `_safe_call` exception path):
       ```python
       return "STATE:lookup_failed | DIRECTIVE:apologize briefly; offer capture_lead; do not retry."
       ```
       Note: this return is in `_safe_call` which wraps `_impl`. `deps` may not be in scope here. CHECK by reading the surrounding function. If `deps` is in scope (it is — `_safe_call` accepts `deps` as its first parameter, verify in the file), apply the pattern. If not, leave this single-line return alone (the helper-fallback case would never trigger replay because the tool errored before producing useful state).

    2. Line 84: `return "STATE:lookup_failed reason=no_tenant | ..."`. `_impl(deps, date)` has `deps` in scope.
    3. Line 86: `return "STATE:missing_args | ..."`.
    4. Line 90: `return "STATE:lookup_failed reason=tenant | ..."`.
    5. Lines 94-97: the `STATE:past_date` parenthesized return.
    6. Line 101: `return "STATE:lookup_failed reason=scheduling_data | ..."`.
    7. Lines 108-111: the `STATE:day_has_slots` parenthesized return — THIS IS THE CRITICAL ONE for the AJ_5NcSoiaZGZTJ failure mode.
    8. Lines 115-118: the `STATE:day_empty` parenthesized return.

    Pattern for single-line:
    ```python
    state = "STATE:lookup_failed reason=no_tenant | DIRECTIVE:apologize briefly; offer capture_lead."
    deps["_last_tool_state"] = state
    return state
    ```

    Pattern for parenthesized (preserve original concatenation):
    ```python
    state = (
        f"STATE:day_has_slots date_label={date_label} count={len(all_slots)}"
        " | DIRECTIVE:confirm the day is open; ask for a concrete hour; do not mention times."
    )
    deps["_last_tool_state"] = state
    return state
    ```

    Do NOT change the STATE+DIRECTIVE TEXT. Only refactor the return shape. Do NOT remove or alter `log_tool_call` calls. The `_last_tool_state` write must come AFTER any `log_tool_call(...)` call that precedes the return so the existing log shape is preserved.

    For Site 1 (`_safe_call` line 76): inspect the function signature. If `_safe_call` is `def _safe_call(deps, ...)` then apply the pattern; if it's `def _safe_call(...)` without `deps`, skip Site 1 — note in the commit message that the wrapper-level error path is best-effort and not captured. The replay helper's null guard handles a missing `_last_tool_state`.
  </action>
  <acceptance_criteria>
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py` returns at least 7 (one per `_impl` return path; site 1 in `_safe_call` is conditional on `deps` being in scope).
    - `grep -c 'STATE:day_has_slots' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py` returns 1 (the critical return is preserved).
    - `grep -c 'STATE:day_empty' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py` returns 1.
    - The `STATE:day_has_slots` return is preceded by `deps["_last_tool_state"] = state` within the same `if all_slots:` block (verify by reading lines 106-115 after edit).
    - `python -c "import ast; ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py').read())"` exits 0.
    - `pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x` still passes (regression guard).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_day.py').read()); print('ok')"</automated>
  </verify>
  <done>All non-wrapper return paths in check_day.py write deps["_last_tool_state"] before returning; AST parses; STATE+DIRECTIVE text unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Capture STATE string in deps in check_slot.py + next_available_days.py</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py, C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py</files>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py (focus lines 99-220)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py (focus lines 62-95)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md (§ "Tool STATE string capture")
  </read_first>
  <action>
    Apply the same pattern as Task 2 — pull each STATE+DIRECTIVE return into a local `state` variable, write `deps["_last_tool_state"] = state`, then `return state`.

    Return sites in `check_slot.py` (verify by reading the file):
    1. Line 99: `return "STATE:lookup_failed | ..."` in `_safe_call` exception handler — apply only if `deps` is in scope.
    2. Line 107: `return "STATE:lookup_failed reason=no_tenant | ..."`.
    3. Line 111: `return "STATE:missing_args | ..."`.
    4. Line 115: `return "STATE:lookup_failed reason=tenant | ..."`.
    5. Lines 121-125: `STATE:past_date` parenthesized return.
    6. Line 128: `return "STATE:bad_time_format | ..."`.
    7. Lines 133-137: `STATE:past_time` parenthesized return.
    8. Line 140: `return "STATE:lookup_failed reason=scheduling_data | ..."`.
    9. Lines 169-192 (verify exact range): the slot-found `STATE:slot_*` parenthesized return — THIS IS A CRITICAL ONE for booking flows.
    10. Lines 195-216 (verify exact range): the slot-not-available parenthesized return.
    11. Lines 219-end (verify exact range): the alternates parenthesized return.

    Return sites in `next_available_days.py` (verify by reading the file):
    1. Line 62: `return "STATE:lookup_failed | ..."` in `_safe_call` exception handler — conditional on `deps` scope.
    2. Line 69: `return "STATE:lookup_failed reason=no_tenant | ..."`.
    3. Line 73: `return "STATE:lookup_failed reason=tenant | ..."`.
    4. Line 78: `return "STATE:lookup_failed reason=scheduling_data | ..."`.
    5. Lines 85-89 (parenthesized): `STATE:next_days_has_slots`.
    6. Lines 92-95 (parenthesized): `STATE:next_days_empty`.

    Same pattern as Task 2. Preserve all `log_tool_call(...)` calls. Do NOT change STATE+DIRECTIVE text. The `_last_tool_state` write must come AFTER `log_tool_call(...)` if present.
  </action>
  <acceptance_criteria>
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py` returns at least 9 (one per `_impl` return path; `_safe_call` site is conditional).
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py` returns at least 5 (one per `_impl` return path; `_safe_call` site is conditional).
    - `grep -c 'STATE:slot_' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py` matches the pre-edit count (no STATE strings dropped).
    - `python -c "import ast; ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py').read()); ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py').read()); print('ok')"` exits 0.
    - `pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_slot.py').read()); ast.parse(open(r'C:/Users/leheh/.Projects/livekit-agent/src/tools/next_available_days.py').read()); print('ok')"</automated>
  </verify>
  <done>Both files have deps["_last_tool_state"] before each non-wrapper return; AST parses; 61.2 invariants still pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Capture STATE string in deps in check_caller_history.py + check_customer_account.py + capture_lead.py</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py, C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py, C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py</files>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py (full file)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py (full file)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py (full file)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md (§ "Tool STATE string capture")
  </read_first>
  <action>
    Apply the same pattern as Tasks 2-3 across the three remaining data-fetch tools.

    Return sites in `check_caller_history.py` (5 sites — verify by reading file): lines 38, 56, 104, 154, 182. Apply pattern wherever `deps` is in scope (the `_impl(deps, ...)` body — all 5 return sites in `_impl` qualify).

    Return sites in `check_customer_account.py` (verify by reading): main return at line 95 (the assembled `state = "; ".join(state_parts)` already creates a local variable — just add `deps["_last_tool_state"] = state` before `return (...)` if the return wraps `state`, OR pull the parenthesized return into a new variable). Inspect the file to confirm the exact shape.

    Return sites in `capture_lead.py` (verify by reading): lines 61, 107, 148, 154, 162, 170. All in `_impl` body. Apply pattern.

    Same conventions: preserve STATE+DIRECTIVE text, preserve `log_tool_call(...)` ordering, do not touch `_safe_call` wrapper-level error returns unless `deps` is in scope.
  </action>
  <acceptance_criteria>
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py` returns at least 5.
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py` returns at least 1.
    - `grep -c 'deps\["_last_tool_state"\]' C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py` returns at least 6.
    - `python -c "import ast; [ast.parse(open(p).read()) for p in [r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py', r'C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py', r'C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py']]; print('ok')"` exits 0.
    - `pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x` passes (the test asserts `mute_input_during_tool` substring presence — make sure the call is preserved in each file).
    - `grep -c 'mute_input_during_tool' C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py` returns at least 2 (import + call) — same for the other two files.
  </acceptance_criteria>
  <verify>
    <automated>pytest C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py -x</automated>
  </verify>
  <done>All 6 data-fetch tools now write deps["_last_tool_state"] on every reachable return path; AST parses; 61.2 invariants pass; no STATE+DIRECTIVE text changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_impl` return → deps dict | In-process write to a per-call dict; no network surface |
| Listener event → deps dict | In-process event from livekit-agents SDK; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `deps["_last_tool_state"]` may contain PII (caller history names, phone-like strings) | mitigate | Same as 61.2: `_last_tool_state` lives only in the per-call `deps` dict (memory only); no logging, no Sentry, no DB write of this key. The replay path (Plan 03) sends it back to Gemini in the same channel the original tool already used — no new disclosure surface. Existing `_PHONE_REDACT_RE` in `agent.py:111` redacts E.164 from transcript_tail; this key is NOT included in transcript_tail. |
| T-01-02 | Tampering | Stale `_last_tool_call_id` from prior tool call mismatched with current `_last_tool_state` | accept | Best-effort posture per CONTEXT.md D-07. The stall-detection guard in Plan 03 (`last_frame_ms <= mute_set_at_ms`) scopes the replay window; mismatched call_id at most causes Gemini to ignore the FunctionResponse and `stalled_generation_replay_failed` increments. No call breakage. |
| T-01-03 | Denial of service | Listener side-effect raises and breaks the existing 61.2 mute pattern | mitigate | Side-effect wrapped in `try/except Exception: pass` mirroring the existing `saw_fresh_speaking[0] = False` handler. Listener cannot raise into the SDK event dispatcher. |

No new network surfaces, no auth changes, no third-party calls. Inherits 61.2 cascade-prevention threat model.
</threat_model>

<verification>
- All 5 existing tests in `test_tool_mute_invariants.py` pass after every task.
- `python -c "import ast; ast.parse(...)"` exits 0 for every file modified in this plan.
- `grep -c 'deps\["_last_tool_state"\]'` returns expected counts per file (see per-task acceptance criteria).
- Final cross-file check: `grep -rn '"_last_tool_state"' C:/Users/leheh/.Projects/livekit-agent/src/tools/ | wc -l` returns at least 26 lines (sum of writes across the 6 tool files).
- Final listener check: `grep -n '"_last_tool_call_id"' C:/Users/leheh/.Projects/livekit-agent/src/tools/_availability_lib.py` returns at least 1 line in the `_on_tools_executed` body.
</verification>

<success_criteria>
- 7 files modified (1 lib + 6 tool files); no behavior change to call path.
- `deps["_last_tool_state"]` populated on every reachable return in 6 data-fetch tools.
- `deps["_last_tool_call_id"]` and `deps["_last_tool_name"]` populated by listener side-effect in `_availability_lib.py`.
- 61.2 invariant suite (5 tests) green.
- AST parses for all 7 files.
- D-05 data prerequisite satisfied; Plan 03 can consume the deps keys.
- D-09 satisfied (no new tracking mechanism — `last_audio_frame_at` reuse decision honored; this plan does NOT touch audio tracking).
</success_criteria>

<output>
After completion, create `.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-01-SUMMARY.md`.
</output>
