---
phase: 61.3
plan: 04
type: execute
wave: 3
depends_on: [61.3-03]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
autonomous: false
requirements: []
addresses_decisions: [D-01, D-02, D-12]
repo: homeservice_agent
must_haves:
  truths:
    - "voice-call-architecture/SKILL.md gains a Phase 61.3 closeout subsection appended after the existing Phase 61.2 subsection"
    - "Subsection documents (a) stall-detection + replay mechanism, (b) mutable_chat_context gate correction (tool_results bypass), (c) listener-miss-was-not-a-miss correction, (d) cascade-failure-mode memory pointer update"
    - "User executes the D-12 live SIP UAT after Plan 03 lands on Railway and confirms recovery is observable"
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md"
      provides: "Phase 61.3 closeout subsection — cascade-recovery via tool-result replay"
      contains: "## Phase 61.3 — Cascade-Recovery via Tool-Result Replay"
      min_lines: 1620
  key_links:
    - from: "Phase 61.3 closeout subsection"
      to: "Phase 61.2 closeout subsection"
      via: "Subsection appended directly after the 61.2 verdict-gap section"
    - from: "61.3 closeout"
      to: "tests/test_cascade_recovery_invariants.py"
      via: "Cite test file in the test-invariants subsection"
---

<objective>
Append a Phase 61.3 closeout subsection to `.claude/skills/voice-call-architecture/SKILL.md` documenting the cascade-recovery mechanism that closes the slot-hallucination gap from Phase 61.2's D-12 UAT (call AJ_5NcSoiaZGZTJ). Subsection covers: the stall-detection + replay mechanism, the corrected understanding of the `mutable_chat_context` gate (tool_results bypass it), the listener-miss-was-not-a-miss correction (closing the false-trail finding from 61.2-VERIFICATION.md), and the updated cascade-failure-mode memory pointer. Then run the D-12 live SIP UAT (manual checkpoint) and capture the goodbye_race JSON evidence.

Purpose: D-12 mandates the SKILL closeout. D-01 + D-02 are validated by the UAT result. Project rule (CLAUDE.md): "Keep skills in sync — when making changes to any system covered by a skill, read the skill first, make the code changes, then update the skill." This plan executes the third step.

Output: Updated SKILL.md with a new ~80-120 line subsection appended after the existing Phase 61.2 closeout. D-12 UAT evidence captured in 61.3-UAT.md (created during the manual checkpoint).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-VALIDATION.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-VERIFICATION.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
@C:/Users/leheh/.Projects/homeservice_agent/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Phase 61.3 closeout subsection to voice-call-architecture/SKILL.md</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (read the LAST 250 lines — the existing Phase 61.2 closeout starts at line 1412 and ends near line 1610. Read offset 1400 to end-of-file to understand the closeout shape)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-CONTEXT.md (D-12: required topics)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-RESEARCH.md (§ "State of the Art" table — the corrected-understanding rows are the source of truth for the listener-miss / mutable_chat_context corrections)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-VERIFICATION.md (the gap report whose framing this plan corrects)
  </read_first>
  <action>
    APPEND (do not replace) a new subsection at the END of `voice-call-architecture/SKILL.md`, directly after the existing Phase 61.2 closeout subsection. The existing 61.2 subsection ends with the D-12 verdict-gap table and a forward-pointer to phase 61.3. Confirm the end-of-file position by reading the file before writing.

    Use this section verbatim, with the placeholder `{UAT_CALL_ID}` filled in from the D-12 UAT (Task 2 of this plan). If the UAT has not been executed yet, leave the placeholder as `{UAT_CALL_ID}` and update post-UAT in a follow-up commit. **DO NOT block the SKILL append on UAT completion** — write the subsection now, update the UAT-evidence sub-block after the UAT lands.

    Append this content (preserve markdown formatting exactly):

    ```markdown

    ---

    ## Phase 61.3 — Cascade-Recovery via Tool-Result Replay (2026-05-07)

    Phase 61.2 contained the cascade (cancellation rate -80%) but did not
    close the slot-hallucination failure mode. UAT call `AJ_5NcSoiaZGZTJ`
    showed `check_day` returning `STATE:day_has_slots count=9` followed by
    a Gemini-server stall + cancellation, then an agent regeneration that
    told the caller "we don't have anything open tomorrow" — direct
    inversion of the tool result. From the caller's perspective this is
    worse than the pre-61.2 deadlock (an authoritative wrong answer
    instead of dead air). Phase 61.3 closes this gap with one mechanism:
    stall-detection + tool-result replay via `update_chat_ctx`.

    ### Failure mode — regeneration without tool-result context

    When Gemini's server-side VAD cancels an in-flight generation that
    was responding to a tool call, the next regeneration runs from a
    chat context where the tool result has been consumed and discarded.
    The 61.2 mute pattern prevents the user from talking over the
    response (preventing CASCADE) but does not put the tool result
    BACK into the chat context for the regeneration to see (preventing
    INVERSION). The Gemini server provides no auto-replay mechanism
    (verified at `realtime_api.py:1302-1308` — `_handle_tool_call_cancellation`
    only logs and marks the generation done).

    ### The mechanism — stall-detection + synthetic FunctionCallOutput replay

    Located in `livekit-agent/src/tools/_availability_lib.py`. New
    `_attempt_tool_result_replay` async helper invoked from
    `_unmute_logic()`'s `TimeoutError` branch BEFORE the listener
    cleanup AND BEFORE `set_audio_enabled(True)`. Steps:

    1. **Stall confirmation (D-04):** compare
       `deps["_diag_record"][0]["last_audio_frame_at"]` against
       `mute_set_at_ms` (captured the moment input was muted). If no
       audio frames advanced during the speaking window, stall is
       confirmed. (Reuses Phase 60.3 Fix C's `last_audio_frame_at`
       — D-09: no parallel tracking mechanism.)
    2. **Data lookup:** read `deps["_last_tool_state"]` (set by tools
       in their return path), `deps["_last_tool_call_id"]` and
       `deps["_last_tool_name"]` (set by an extension to the existing
       `_on_tools_executed` listener that captures
       `FunctionToolsExecutedEvent.function_calls[-1]`). Null guard
       short-circuits with no replay if any are missing.
    3. **Replay construction (D-05):** build a synthetic
       `livekit.agents.llm.FunctionCallOutput` with the captured
       `call_id`, `name`, and STATE+DIRECTIVE string as `output`.
       Append to a copy of `rt_session.chat_ctx.items`, then
       `await rt_session.update_chat_ctx(chat_ctx)`.
    4. **Order (D-06):** replay completes (or fails) BEFORE
       `set_audio_enabled(True)`. VAD cannot fire on muted input, so
       user speech cannot race the recovery.
    5. **Best-effort (D-07):** the entire replay is wrapped in
       `try/except Exception` — any failure logs and increments a
       counter; `_unmute_logic` continues to the unmute path. Replay
       failure does not block the call.

    ### The unlock — tool_results send is unconditional

    The replay relies on a SDK invariant the original Phase 61.2
    research mis-characterized:

    | Old understanding | Corrected understanding (verified) |
    |-------------------|-------------------------------------|
    | "`mutable_chat_context=False` blocks all context updates for Gemini 3.1" | The gate at `realtime_api.py:628` only blocks the `turns` append (LiveClientContent). The `tool_results` send at `realtime_api.py:637-638` is OUTSIDE the gate and fires unconditionally — including for `gemini-3.1-flash-live-preview`. (Verified against installed livekit-plugins-google 1.5.7.) |
    | "Recovery requires `generate_reply()`" | `generate_reply()` is gated for 3.1 (raises `RealtimeError` on 1.5.7); `update_chat_ctx` with a synthetic `FunctionCallOutput` triggers Gemini-server-side regeneration without it. |

    The `FunctionResponse` lands on the Gemini server, which then
    auto-generates a new agent turn that has the tool result in its
    context — closing the slot-inversion failure mode.

    ### Listener-miss correction (closes 61.2-VERIFICATION.md gap #2)

    Phase 61.2-VERIFICATION.md filed gap #2 as "the
    `function_tools_executed` listener does not fire for the
    `check_day` path." This framing is **incorrect** and is closed
    here:

    - The listener fires on the correct emitter
      (`session.on("function_tools_executed", ...)` — `session` is
      the `AgentSession` and the event is emitted at
      `agent_activity.py:3372`).
    - What the 61.2 UAT actually showed was that the unmute came via
      the 25s safety fallback rather than via the listener-driven
      path. That happened because Gemini's server-side TTS stalled —
      the agent never transitioned `listening → speaking → listening`
      cleanly. The listener can only fire on a clean tool-execution
      cycle; if the cycle never completes, the SDK has no signal to
      emit. **The listener works as designed.**
    - 61.3 ADDS recovery as a complementary structural pattern: when
      the 25s fallback fires (the "listener didn't get a clean
      cycle" signal), the replay attempts to recover. The listener
      does not need to be rebuilt or rebound.

    Future phases should NOT chase the listener-miss thread — that
    investigation is closed.

    ### Telemetry (D-08)

    Two new counters extend Phase 61.2's `_ServerCancelHandler` shape
    via the same conditional-emit pattern (`dict.get(key, 0) + 1`):

    - `stalled_generation_recoveries` — number of times the replay
      helper attempted recovery (i.e. confirmed stall + had data to
      replay).
    - `stalled_generation_replay_failed` — number of times the
      `update_chat_ctx` call raised or `rt_session` was unavailable.

    Like the 61.2 counters, these keys are OMITTED from `[goodbye_race]`
    JSON / Sentry breadcrumb on healthy calls (count == 0). Mere field
    presence signals "stall fired on this call."

    Counter values are written from `_attempt_tool_result_replay` via
    `deps["_diag_record"][0]` — the same list reference shared with
    `agent.py`'s `_GoodbyeDiagHandler` and `_ServerCancelHandler`.
    Updates from the async helper flow into `_flush_goodbye_diag`
    automatically.

    ### Test invariants (`livekit-agent/tests/test_cascade_recovery_invariants.py`)

    Six substring-grep invariants (same shape as 61.2's
    `test_tool_mute_invariants.py` — pure structural guards, no SDK
    imports, no mocks, no fixtures):

    | Test | Guards |
    |------|--------|
    | `test_replay_path_in_fallback` | `_attempt_tool_result_replay` defined and awaited inside `_unmute_logic` |
    | `test_replay_uses_update_chat_ctx` | Helper calls `update_chat_ctx` and constructs `FunctionCallOutput` |
    | `test_replay_not_generate_reply` | NO `generate_reply` substring in `_availability_lib.py` (gated for 3.1) |
    | `test_replay_before_set_audio_enabled` | Source-order: replay invocation appears BEFORE `set_audio_enabled(True)` |
    | `test_stall_recovery_counters_present` | Both new counter names present |
    | `test_stall_counters_conditional_emit` | Both counters use `.get(key, 0) + 1` pattern |

    Behavioral testing of the replay was deferred (D-11): mocking
    `RealtimeSession.update_chat_ctx` doesn't faithfully exercise the
    cascade. The D-12 live SIP UAT is the behavioral gate, matching the
    61.2 deferral pattern.

    ### D-12 post-fix UAT verdict — call `{UAT_CALL_ID}` (UPDATE POST-UAT)

    {UAT_VERDICT_TABLE_PLACEHOLDER — populate from 61.3-UAT.md after the live call. Expected
    fields: server_tool_cancellations, orphaned_server_content,
    stalled_generation_recoveries, stalled_generation_replay_failed,
    booking_outcome, transcript inversion check.}

    Pre-fix baseline for cascade-recovery verdict is the 61.2 UAT call
    `AJ_5NcSoiaZGZTJ` — that call had `server_tool_cancellations=1`
    AND a slot-inverted regeneration ("we don't have anything open
    tomorrow" against `STATE:day_has_slots count=9`). 61.3 ships when
    a similar cascade event produces `stalled_generation_recoveries >=
    1` AND the agent does NOT invert the tool result.

    ### Memory pointer update

    The user-memory entry `project_phase_61_cascade_failure_mode`
    documents the structural mute pattern as the only known prevention.
    61.3 ADDS the structural replay pattern as a complementary
    recovery. The memory entry should be updated post-UAT to note both
    layers:

    - Prevention (61.2): mute caller input during tool execution +
      booking-section response window; raises fallback to 25s; adds
      `function_tools_executed` re-anchor listener; installs
      `_ServerCancelHandler` telemetry.
    - Recovery (61.3): on 25s fallback fire + confirmed stall, replay
      last tool's STATE+DIRECTIVE as a synthetic `FunctionCallOutput`
      via `update_chat_ctx` BEFORE unmuting.

    Together these close the slot-hallucination cascade. Future phases
    that observe new failure modes after both fixes are in place should
    re-measure the cascade rate against the 61.3 baseline rather than
    the pre-61.2 baseline (`AJ_vV4DM5AG9t7W`).

    ### Deferred items (closed via re-measurement plan, not separate fixes)

    - **Transcript fragmenting** (gap #3 from 61.2-VERIFICATION.md) —
      symptom of the cancel-and-regenerate cycle. Re-measure after
      61.3's recovery lands; if still present, surface as a new phase.
    - **Greeting playout 10s timeout** (gap #4 from
      61.2-VERIFICATION.md) — orthogonal to the cascade (different
      code path: `_unmute_after_greeting`). Future polish phase.
    - **Lowering `_TOOL_MUTE_FALLBACK_S`** — the 25s window IS the
      design; lowering it would expose more calls to the same race.
      Don't touch.
    ```

    Notes for the executor:
    - Place `---` separator BEFORE the new heading (matches the existing pattern between 61.1 and 61.2 subsections).
    - Preserve all markdown table formatting exactly.
    - Do NOT modify the existing Phase 61.2 subsection. Append-only.
    - Replace `{UAT_CALL_ID}` and `{UAT_VERDICT_TABLE_PLACEHOLDER}` AFTER Task 2 completes — they remain as placeholders during this task.
  </action>
  <acceptance_criteria>
    - `grep -c '^## Phase 61.3' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns 1.
    - `grep -c '_attempt_tool_result_replay' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 2 (mechanism description + test table).
    - `grep -c 'tool_results send is unconditional' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (the mutable_chat_context gate correction).
    - `grep -c 'Listener-miss correction' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns 1 (the false-trail correction).
    - `grep -c 'stalled_generation_recoveries' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 2.
    - `grep -c 'project_phase_61_cascade_failure_mode' C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns at least 1 (memory pointer reference).
    - The existing Phase 61.2 subsection (`## Phase 61.2 — Gemini-Server Cancellation Cascade Mitigation`) is still present and unchanged: `grep -c '^## Phase 61.2 — Gemini-Server Cancellation Cascade Mitigation' SKILL.md` still returns 1.
    - File is valid markdown (no broken table separators): `python -c "open(r'C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md', encoding='utf-8').read()"` does not raise.
    - File length increased by at least 80 lines compared to pre-task (the appended subsection).
  </acceptance_criteria>
  <verify>
    <automated>python -c "t=open(r'C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md', encoding='utf-8').read(); assert '## Phase 61.3' in t; assert '_attempt_tool_result_replay' in t; assert 'tool_results send is unconditional' in t; assert 'Listener-miss correction' in t; assert '## Phase 61.2 — Gemini-Server Cancellation Cascade Mitigation' in t; print('ok')"</automated>
  </verify>
  <done>SKILL.md has the new Phase 61.3 closeout subsection appended; existing 61.2 subsection intact; all required topics covered (mechanism, gate correction, listener correction, memory pointer); UAT placeholder remains for Task 2 to fill in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-12 live SIP UAT — execute and capture goodbye_race evidence</name>
  <what-built>
    Plan 03 has shipped to the livekit-agent Railway deployment. The cascade-recovery mechanism is live:
    `_attempt_tool_result_replay` invoked from `_unmute_logic`'s TimeoutError branch with stall-detection
    + synthetic `FunctionCallOutput` replay via `update_chat_ctx`. New diag counters
    (`stalled_generation_recoveries`, `stalled_generation_replay_failed`) appear in `[goodbye_race]` JSON
    only when > 0. All 6 cascade-recovery invariants + 5 61.2 invariants are GREEN locally.
  </what-built>
  <how-to-verify>
    Manual D-12 live SIP UAT (per 61.3-VALIDATION.md "Manual-Only Verifications" row 1):

    1. Confirm the livekit-agent Railway redeploy has picked up Plan 03's commit. Check Railway deployment
       log for the worker matching the latest livekit-agent commit hash. Worker registration log line
       should show livekit-agents 1.5.7.

    2. Place a live SIP test call against the same test tenant used for `AJ_5NcSoiaZGZTJ`
       (tenant `24141cd0...`).

    3. Drive the agent into `check_day` territory (ask about Thursday or any future-day availability —
       same path as the failed 61.2 UAT). Wait for the tool to fire and produce a STATE:day_has_slots
       result.

    4. If a Gemini-server stall + cancellation occurs naturally (the 25s fallback fires), the recovery
       should engage. If the call completes without a stall on the first attempt, retry the call up to
       3 times — stalls are non-deterministic but observed at non-trivial rate in the test tenant.

    5. After the call ends, fetch the Railway log line `[goodbye_race]` for the call. Confirm:
       (a) `stalled_generation_recoveries >= 1` IF a stall occurred (key absent if no stall — that's
       acceptable but inconclusive; retry up to 3 times to catch a stall).
       (b) `stalled_generation_replay_failed` is either absent (success) or > 0 (replay attempted but
       failed — note the warning log line for diagnosis).

    6. Pull the call's `transcript_structured` and confirm the agent did NOT invert any tool result.
       Specifically: if `check_day` returned `STATE:day_has_slots`, the agent should NOT say "we don't
       have anything open" or equivalent. The agent SHOULD acknowledge the day is open and ask for a
       concrete hour (matches the DIRECTIVE).

    7. Capture all evidence into a new file:
       `.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-UAT.md`.
       Include: call_id, room_name, timestamp, full `[goodbye_race]` JSON line,
       `transcript_structured` excerpt covering the cascade window (10s before through 30s after the
       cancellation), Railway worker version, and a verdict line:
       - `verdict: shipped` if `stalled_generation_recoveries >= 1` AND no inversion.
       - `verdict: gap` if a cascade fired but the agent still inverted (re-open phase 61.x).
       - `verdict: inconclusive` if no cascade fired in 3 retries (acceptable — recovery code path is
         present but unexercised; ship anyway based on invariant tests + structural review).

    8. After 61.3-UAT.md is created, replace the `{UAT_CALL_ID}` and
       `{UAT_VERDICT_TABLE_PLACEHOLDER}` placeholders in SKILL.md (Task 1 output) with the actual
       call_id and a verdict table sourced from 61.3-UAT.md.
  </how-to-verify>
  <resume-signal>
    Type "approved" with a short verdict note (`approved — verdict: shipped, recovery counter=N, no inversion`),
    or describe issues if a gap was reproduced.
  </resume-signal>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-UAT.md, C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>
  <action>Manual checkpoint — see <how-to-verify> for the 8-step UAT protocol. After UAT, write 61.3-UAT.md and replace SKILL.md placeholders.</action>
  <verify><automated>test -f C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-UAT.md && grep -q "verdict:" C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-UAT.md</automated></verify>
  <done>61.3-UAT.md captured with verdict line + [goodbye_race] JSON; SKILL.md placeholders replaced with actual call_id + verdict table; user has resumed with "approved".</done>
</task>

</tasks>

<threat_model>
No new threats introduced; this plan modifies a documentation file in the homeservice_agent repo only. Inherits 61.3 implementation threat model from Plan 03. The D-12 UAT itself does not introduce new threats — it is a verification of already-shipped code.
</threat_model>

<verification>
- `grep -c '^## Phase 61.3' SKILL.md` returns 1 (closeout subsection appended).
- All four D-12 required topics present in the subsection: stall-detection + replay mechanism, mutable_chat_context gate correction, listener-miss correction, memory pointer update.
- The existing Phase 61.2 subsection is intact and unchanged (regression guard).
- `61.3-UAT.md` exists with verdict line and `[goodbye_race]` JSON evidence.
- SKILL.md placeholders replaced with actual UAT call_id and verdict table after the manual checkpoint.
</verification>

<success_criteria>
- D-01 satisfied: single-fix scope confirmed by UAT — recovery counter increments on stall, no parallel scope additions.
- D-02 satisfied: D-12 live SIP UAT executed; verdict captured in 61.3-UAT.md.
- D-12 satisfied: SKILL.md Phase 61.3 closeout subsection in place documenting the four required topics.
- Project rule honored (CLAUDE.md "Keep skills in sync"): SKILL.md updated after the code change.
- Phase 61.3 ready for `/gsd-verify-work` once `verdict: shipped` (or `verdict: inconclusive` with reviewer ack) is recorded.
</success_criteria>

<output>
After completion:
- Create `.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-UAT.md` (Task 2 output)
- Create `.planning/phases/61.3-phase-61-production-regressions-round-3-cascade-recovery-slo/61.3-04-SUMMARY.md`
</output>
