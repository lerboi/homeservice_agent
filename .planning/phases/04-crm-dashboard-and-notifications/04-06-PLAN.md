---
phase: 04-crm-dashboard-and-notifications
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - src/app/dashboard/page.js
  - src/components/dashboard/DashboardHomeStats.jsx
  - src/components/dashboard/RecentActivityFeed.jsx
  - src/app/dashboard/analytics/page.js
  - src/components/dashboard/AnalyticsCharts.jsx
  - src/app/dashboard/leads/page.js
  - src/app/dashboard/settings/page.js
autonomous: false
requirements: [CRM-04, CRM-05]

must_haves:
  truths:
    - "Owner sees today's summary on dashboard home: new leads count, upcoming appointments, calls today, conversion rate"
    - "Owner sees recent activity feed on dashboard home"
    - "Owner can see revenue charts and conversion funnel on analytics page"
    - "New leads appear in the lead list in real-time via Supabase Realtime"
    - "Lead status changes reflect live across open tabs"
  artifacts:
    - path: "src/app/dashboard/page.js"
      provides: "Dashboard home page with stats and activity"
    - path: "src/components/dashboard/DashboardHomeStats.jsx"
      provides: "4 stat widgets with counter animation"
    - path: "src/components/dashboard/RecentActivityFeed.jsx"
      provides: "Chronological activity list"
    - path: "src/app/dashboard/analytics/page.js"
      provides: "Analytics page with revenue and funnel charts"
    - path: "src/components/dashboard/AnalyticsCharts.jsx"
      provides: "Recharts line, bar, and donut charts"
  key_links:
    - from: "src/app/dashboard/leads/page.js"
      to: "supabase Realtime"
      via: "supabase.channel('leads-realtime').on('postgres_changes')"
      pattern: "postgres_changes"
    - from: "src/app/dashboard/page.js"
      to: "/api/leads"
      via: "fetch for stat aggregation"
      pattern: "fetch.*api"
    - from: "src/app/dashboard/analytics/page.js"
      to: "/api/leads"
      via: "fetch for chart data"
      pattern: "fetch.*api"
---

<objective>
Build the dashboard home page (stats + activity feed), analytics page (revenue charts + conversion funnel), wire Supabase Realtime into the leads page, install recharts, and add settings placeholder.

Purpose: This completes the dashboard experience — the home page gives an at-a-glance summary, analytics provides revenue insights, and Realtime makes the lead list feel alive.
Output: Dashboard home, analytics page, Realtime wiring, settings stub.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-crm-dashboard-and-notifications/04-CONTEXT.md
@.planning/phases/04-crm-dashboard-and-notifications/04-RESEARCH.md
@.planning/phases/04-crm-dashboard-and-notifications/04-UI-SPEC.md
@.planning/phases/04-crm-dashboard-and-notifications/04-04-SUMMARY.md
@.planning/phases/04-crm-dashboard-and-notifications/04-05-SUMMARY.md

<interfaces>
From src/lib/supabase-browser.js (confirmed exists via RESEARCH.md):
```javascript
import { supabase } from '@/lib/supabase-browser';
// Used for client-side Realtime subscriptions
```

From 04-UI-SPEC.md - Dashboard Home:
- 2-column stats row (lg+) / single column (mobile)
- Stat widget: white card, 28px semibold number text-[#0F172A], 12px label text-[#475569], delta arrow
- 4 stats: New Leads Today, Upcoming Appointments, Calls Today, Conversion Rate
- Counter animation: 0 to value over 600ms, skip if prefers-reduced-motion
- Activity feed: icon + description + relative time, last 20 events, Skeleton loading

From 04-UI-SPEC.md - Realtime:
- New card: @keyframes slide-in-from-top 200ms ease-out
- Only on Realtime INSERT, not initial page load

From 04-RESEARCH.md - Supabase Realtime pattern:
```javascript
const channel = supabase
  .channel('leads-realtime')
  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'leads', filter: `tenant_id=eq.${tenantId}` }, callback)
  .on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'leads', filter: `tenant_id=eq.${tenantId}` }, callback)
  .subscribe();
```

From 04-RESEARCH.md - Recharts API (verified 3.x):
```javascript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';
import { BarChart, Bar } from 'recharts';
import { PieChart, Pie, Cell } from 'recharts';
```

From 04-UI-SPEC.md - Analytics empty state:
- "Not enough data yet" heading
- "Revenue and conversion charts appear once you have at least 5 completed leads."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard home page with stats and activity feed</name>
  <files>src/app/dashboard/page.js, src/components/dashboard/DashboardHomeStats.jsx, src/components/dashboard/RecentActivityFeed.jsx</files>
  <read_first>
    src/app/dashboard/layout.js
    src/lib/supabase-server.js
    src/components/ui/card.jsx
    src/components/ui/skeleton.jsx
    src/lib/design-tokens.js
  </read_first>
  <action>
**Create `src/components/dashboard/DashboardHomeStats.jsx`:**
'use client' component. Props: `{ stats }` where stats is `{ newLeadsToday, upcomingAppointments, callsToday, conversionRate }`

4 stat widgets in a responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`

Each widget card:
- White card: `bg-white rounded-xl border border-stone-200/60 shadow-[0_1px_3px_0_rgba(0,0,0,0.04)] p-5`
- Large number: 28px semibold `text-[#0F172A]` (`text-[28px] font-semibold leading-[1.15]`)
- Label below: 12px semibold uppercase tracking-wider `text-[#475569]` (`text-xs font-semibold uppercase tracking-wider`)
- Icon in top-right corner: 16px muted lucide icon
- Counter animation: useEffect with requestAnimationFrame, count from 0 to value over 600ms. Use `const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches` — if true, show final value immediately.
- Stat numbers: `<p>` not heading (per UI-SPEC accessibility)
- Conversion rate shows as percentage with one decimal

Icons per stat:
- New Leads Today: `UserPlus` from lucide
- Upcoming Appointments: `CalendarCheck` from lucide
- Calls Today: `Phone` from lucide
- Conversion Rate: `TrendingUp` from lucide

**Create `src/components/dashboard/RecentActivityFeed.jsx`:**
'use client' component. Props: `{ activities }` where each activity is `{ id, event_type, metadata, created_at }`

- Chronological list, max 20 items
- Each item: 16px lucide icon (based on event_type) + description text + relative time (formatDistanceToNow)
- Event type icons: lead_created -> `UserPlus`, status_changed -> `ArrowRight`, notification_sent -> `Bell`, booking_created -> `CalendarCheck`
- Description text constructed from metadata: "New lead from [name]", "[name] moved to [Status]", "SMS sent to [number]", "Booking confirmed for [name]"
- Empty state: "No recent activity" with muted text
- Skeleton loading: 5 rows of Skeleton (height 40px)

**Create `src/app/dashboard/page.js`:**
'use client' page component.
1. Fetch stats from two endpoints on mount:
   - `/api/leads?status=new&date_from=${today}` -> count for new leads today
   - `/api/leads` -> total count for conversion rate (completed+paid / total)
   - Upcoming appointments: fetch from `/api/appointments?date=${today}&days=1` (existing endpoint from Phase 3)
2. Fetch recent activity: new API needed — add inline fetch from activity_log table via supabase-browser
   - `supabase.from('activity_log').select('*').eq('tenant_id', tenantId).order('created_at', { ascending: false }).limit(20)`
3. Render:
   - DashboardHomeStats with computed values
   - Section separator
   - "Recent Activity" heading (20px semibold)
   - RecentActivityFeed
4. Loading: Skeleton state for both sections
5. Get tenantId from auth session (supabase-browser getUser)
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests && grep -c "DashboardHomeStats" src/app/dashboard/page.js && grep -c "RecentActivityFeed" src/app/dashboard/page.js</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/page.js renders DashboardHomeStats and RecentActivityFeed
    - DashboardHomeStats renders 4 stat widgets in a responsive grid
    - Stat numbers are 28px semibold text-[#0F172A], labels are 12px semibold uppercase tracking-wider text-[#475569]
    - Counter animation uses requestAnimationFrame over 600ms, respects prefers-reduced-motion
    - Stat numbers use `<p>` not heading elements
    - RecentActivityFeed renders up to 20 activity items with icon + description + relative time
    - Activity icons match event_type (UserPlus for lead_created, etc.)
    - Both components show Skeleton loading states
    - Page fetches data from /api/leads and activity_log
  </acceptance_criteria>
  <done>Dashboard home shows at-a-glance stats with animated counters and chronological activity feed, providing the owner with an instant overview on login.</done>
</task>

<task type="auto">
  <name>Task 2: Analytics page + Supabase Realtime + settings stub</name>
  <files>src/app/dashboard/analytics/page.js, src/components/dashboard/AnalyticsCharts.jsx, src/app/dashboard/leads/page.js, src/app/dashboard/settings/page.js</files>
  <read_first>
    src/app/dashboard/leads/page.js
    src/lib/supabase-browser.js
    src/lib/design-tokens.js
    package.json
  </read_first>
  <action>
**Install recharts:**
```bash
npm install recharts
```

**Create `src/components/dashboard/AnalyticsCharts.jsx`:**
'use client' component. Props: `{ leads }` (array of all leads for this tenant)

Three charts:

1. **Revenue Over Time (LineChart):**
   - Group leads with revenue_amount by month (created_at)
   - X: month label, Y: cumulative revenue
   - Line color: #C2410C, strokeWidth 2, dot false
   - CartesianGrid strokeDasharray="3 3" stroke="#e5e7eb"
   - Tooltip formatter: `$${value}`
   - YAxis tickFormatter: `$${value}`

2. **Conversion Funnel (BarChart):**
   - 4 bars: New, Booked, Completed, Paid (count of leads in each status)
   - Bar colors: match pipeline status colors from UI-SPEC
     - New: #C2410C, Booked: #1d4ed8, Completed: #57534e, Paid: #166534
   - Horizontal BarChart (layout="vertical") for funnel feel

3. **Pipeline Breakdown (PieChart/Donut):**
   - 5 slices: New, Booked, Completed, Paid, Lost
   - Colors match pipeline status colors
   - innerRadius={60} outerRadius={80} for donut
   - Legend visible below chart

All charts wrapped in `<ResponsiveContainer width="100%" height={300}>`.
Each chart in a white card with heading (20px semibold) and card.base styling.

Empty state: When leads.length < 5, show "Not enough data yet" / "Revenue and conversion charts appear once you have at least 5 completed leads."

Skeleton loading for charts: 3 Skeleton rectangles (300px height).

**Create `src/app/dashboard/analytics/page.js`:**
'use client' page. Fetches all leads from `/api/leads` (no filters — full dataset for charts), passes to AnalyticsCharts.

**Update `src/app/dashboard/leads/page.js` to add Supabase Realtime:**

1. Import supabase from `@/lib/supabase-browser`
2. Get tenantId from auth session on mount
3. Add useEffect for Realtime subscription:
```javascript
useEffect(() => {
  if (!tenantId) return;
  const channel = supabase
    .channel('leads-realtime')
    .on('postgres_changes', {
      event: 'INSERT',
      schema: 'public',
      table: 'leads',
      filter: `tenant_id=eq.${tenantId}`,
    }, (payload) => {
      // Only animate for Realtime inserts, not initial load
      setLeads(prev => [{ ...payload.new, _isNew: true }, ...prev]);
    })
    .on('postgres_changes', {
      event: 'UPDATE',
      schema: 'public',
      table: 'leads',
      filter: `tenant_id=eq.${tenantId}`,
    }, (payload) => {
      setLeads(prev => prev.map(l => l.id === payload.new.id ? { ...payload.new, _isNew: false } : l));
    })
    .subscribe();

  return () => supabase.removeChannel(channel);
}, [tenantId]);
```

4. In LeadCard rendering, add a CSS animation class when `lead._isNew`:
   `className={lead._isNew ? 'animate-slide-in-from-top' : ''}`

5. Add the keyframe animation in the page or a shared CSS module:
   `@keyframes slide-in-from-top { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: translateY(0); } }`
   Duration: 200ms ease-out.

6. Also wire the LeadFlyout (from Plan 05) into the leads page:
   - Import LeadFlyout
   - State: selectedLeadId, flyoutOpen
   - Pass onView to LeadCard that sets selectedLeadId and opens flyout
   - Pass onStatusChange callback that refreshes the leads list

7. Wire KanbanBoard (from Plan 05) into the kanban viewMode:
   - When viewMode is 'kanban', render KanbanBoard instead of card list
   - Pass leads and onViewLead callback

**Create `src/app/dashboard/settings/page.js`:**
Simple placeholder page:
- Heading: "Settings" (20px semibold)
- Body: "Account and notification settings coming soon." in muted text
- This is a stub — full settings UI is a future enhancement
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests && grep -c "recharts" package.json && grep -c "postgres_changes" src/app/dashboard/leads/page.js && grep -c "AnalyticsCharts" src/app/dashboard/analytics/page.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains recharts as dependency
    - src/components/dashboard/AnalyticsCharts.jsx renders LineChart (revenue), BarChart (funnel), PieChart (pipeline)
    - Charts use exact colors: LineChart stroke #C2410C, BarChart bars match pipeline status colors
    - Charts wrapped in ResponsiveContainer with height={300}
    - Analytics shows "Not enough data yet" when leads.length < 5
    - src/app/dashboard/analytics/page.js fetches leads and renders AnalyticsCharts
    - src/app/dashboard/leads/page.js subscribes to Supabase Realtime 'postgres_changes' on leads table
    - Realtime INSERT events add lead to top of list with _isNew flag
    - Realtime UPDATE events replace existing lead in list
    - New leads animate with slide-in-from-top (200ms ease-out)
    - Leads page imports and renders LeadFlyout from Plan 05
    - Leads page renders KanbanBoard when viewMode is 'kanban'
    - src/app/dashboard/settings/page.js exists with "Settings" heading and placeholder text
    - useEffect cleanup calls supabase.removeChannel
    - All existing tests pass
  </acceptance_criteria>
  <done>Analytics page with revenue, funnel, and pipeline charts. Leads page has live Realtime updates with animated card arrival. Kanban and flyout wired into leads page. Settings stub created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete CRM dashboard</name>
  <files>src/app/dashboard/page.js, src/app/dashboard/leads/page.js, src/app/dashboard/analytics/page.js</files>
  <action>Human visually verifies the complete CRM dashboard across all pages: home stats, lead list, flyout detail with audio/transcript, kanban view, analytics charts, sidebar navigation, and breadcrumbs.</action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests</automated>
  </verify>
  <done>User has approved the visual quality and functionality of all dashboard pages.</done>
  <what-built>Complete CRM dashboard: sidebar nav, lead list with filters, lead flyout with audio/transcript, kanban view, dashboard home with stats, analytics with charts, Supabase Realtime, notifications (SMS + email), caller recovery SMS cron</what-built>
  <how-to-verify>
    1. Navigate to /dashboard — verify stat widgets show (may be 0s if no test data), activity feed visible
    2. Navigate to /dashboard/leads — verify lead list or empty state displays, filter bar is interactive
    3. If leads exist: click a lead card -> verify flyout opens with call detail, audio player, transcript
    4. In flyout: change status to Booked -> verify toast appears, lead card updates
    5. Toggle to Kanban view -> verify 5 columns display
    6. Navigate to /dashboard/analytics -> verify charts or empty state
    7. Navigate to /dashboard/settings -> verify placeholder text
    8. Verify sidebar has all 6 nav items: Home, Leads, Analytics, Calendar, Services, Settings
    9. Verify breadcrumb shows correct labels on each page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All dashboard pages render without errors
- Sidebar navigation works correctly
- Lead list loads and filters work
- Lead flyout shows full detail with audio and transcript
- Analytics charts render with data (or empty state)
- Supabase Realtime subscription active (check browser WebSocket in devtools)
- Full test suite passes: `node --experimental-vm-modules node_modules/jest-cli/bin/jest.js --passWithNoTests`
</verification>

<success_criteria>
- Dashboard home provides at-a-glance stats and activity (CRM-04)
- Revenue charts and conversion funnel visible on analytics page (CRM-05)
- Leads update in real-time without page refresh (CRM-04)
- All dashboard pages accessible via sidebar navigation
- Kanban and list views both functional
- Visual checkpoint confirms design quality matches UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-crm-dashboard-and-notifications/04-06-SUMMARY.md`
</output>
