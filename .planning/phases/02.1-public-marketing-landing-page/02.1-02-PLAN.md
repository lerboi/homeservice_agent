---
phase: 02.1-public-marketing-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/app/page.js
  - src/app/components/landing/HeroSection.jsx
  - src/app/components/landing/HowItWorksSection.jsx
  - src/app/components/landing/FeaturesGrid.jsx
  - src/app/components/landing/SocialProofSection.jsx
  - src/app/components/landing/FinalCTASection.jsx
  - src/app/components/landing/LandingFooter.jsx
  - src/app/demo/page.js
autonomous: false
requirements: [LAND-01, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07, LAND-10]

must_haves:
  truths:
    - "Visiting / shows a full marketing landing page, not the placeholder"
    - "Hero section displays headline, subheadline, two CTA buttons, and a product mockup visual"
    - "How it Works section shows 3-step plumber burst pipe scenario"
    - "Features Grid shows 4 cards with trade-specific titles, body, and justification copy"
    - "Social Proof section shows 3 testimonial cards from plumber/HVAC/electrician personas"
    - "Final CTA section has Heritage Copper background with single CTA button"
    - "Footer shows logo, copyright, Terms and Privacy placeholder links"
    - "All primary CTAs link to /onboarding"
    - "Watch Demo links to /demo which shows a coming-soon page"
    - "Page is responsive across mobile, tablet, and desktop breakpoints"
  artifacts:
    - path: "src/app/page.js"
      provides: "Root page composing all landing sections"
      contains: "HeroSection"
    - path: "src/app/components/landing/HeroSection.jsx"
      provides: "Hero with headline, subhead, CTAs, mockup"
      contains: "Every Call You Miss"
    - path: "src/app/components/landing/HowItWorksSection.jsx"
      provides: "3-step plumber scenario"
      contains: "burst pipe"
    - path: "src/app/components/landing/FeaturesGrid.jsx"
      provides: "4 feature cards"
      contains: "The Night Shift, Sorted"
    - path: "src/app/components/landing/SocialProofSection.jsx"
      provides: "3 testimonial cards"
      contains: "Dave R."
    - path: "src/app/components/landing/FinalCTASection.jsx"
      provides: "Final CTA block"
      contains: "Your Next Emergency Call Is Tonight"
    - path: "src/app/components/landing/LandingFooter.jsx"
      provides: "Footer with logo + links"
      contains: "No call goes to voicemail"
    - path: "src/app/demo/page.js"
      provides: "Watch Demo placeholder route"
      contains: "Coming soon"
  key_links:
    - from: "src/app/page.js"
      to: "src/app/components/landing/HeroSection.jsx"
      via: "import { HeroSection }"
      pattern: "import.*HeroSection.*from"
    - from: "src/app/components/landing/HeroSection.jsx"
      to: "/onboarding"
      via: "Link href"
      pattern: "href=\"/onboarding\""
    - from: "src/app/components/landing/FeaturesGrid.jsx"
      to: "@/components/ui/card"
      via: "import { Card }"
      pattern: "from '@/components/ui/card'"
    - from: "src/app/components/landing/HeroSection.jsx"
      to: "src/app/components/landing/AnimatedSection.jsx"
      via: "import { AnimatedSection }"
      pattern: "from.*AnimatedSection"
---

<objective>
Build all 7 content sections of the landing page and wire them into page.js. Create the /demo placeholder route. End with a visual checkpoint where the user verifies the full page in a browser.

Purpose: This is the core deliverable of Phase 02.1 — the actual marketing landing page that visitors see. All foundation work (colors, animation wrapper, nav) was done in Plan 01.

Output: Complete marketing landing page at `/` with all sections, copy, animations, and responsive layout. Plus `/demo` coming-soon placeholder.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-CONTEXT.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-RESEARCH.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-UI-SPEC.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — client components created in foundation -->
From src/app/components/landing/AnimatedSection.jsx:
```jsx
'use client';
// Wraps children with framer-motion whileInView fade+slide animation
// Props: children, className, delay (number, default 0)
export function AnimatedSection({ children, className, delay = 0 })
```

From src/app/components/landing/LandingNav.jsx:
```jsx
'use client';
// Sticky fixed nav: transparent -> solid on scroll, logo left, CTA right
export function LandingNav()
```

<!-- Existing shadcn components -->
From src/components/ui/button.jsx:
```jsx
// variant: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
// size: "default" (h-9) | "xs" (h-6) | "sm" (h-8) | "lg" (h-10)
// asChild: boolean — renders Slot.Root for Link wrapping
export { Button, buttonVariants }
```

From src/components/ui/card.jsx:
```jsx
// Card with nested slots: CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardAction
// Card base: flex flex-col gap-6 rounded-xl border bg-card py-6 shadow-sm
// CardContent base: px-6
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```

<!-- Color tokens available (from Plan 01 globals.css) -->
Tailwind utility classes available:
- bg-landing-surface (#F5F5F4 — Soft Stone, warm background)
- bg-landing-dark (#0F172A — Midnight Slate, dark sections)
- bg-landing-accent (#C2410C — Heritage Copper, CTA buttons)
- text-landing-muted (#475569 — Muted Zinc, secondary text)
- text-landing-success (#166534 — Evergreen, positive indicators)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all landing page sections and wire into page.js</name>
  <files>
    src/app/page.js,
    src/app/components/landing/HeroSection.jsx,
    src/app/components/landing/HowItWorksSection.jsx,
    src/app/components/landing/FeaturesGrid.jsx,
    src/app/components/landing/SocialProofSection.jsx,
    src/app/components/landing/FinalCTASection.jsx,
    src/app/components/landing/LandingFooter.jsx,
    src/app/demo/page.js
  </files>
  <read_first>
    - src/app/page.js
    - src/app/globals.css
    - src/app/layout.js
    - src/app/components/landing/AnimatedSection.jsx
    - src/app/components/landing/LandingNav.jsx
    - src/components/ui/button.jsx
    - src/components/ui/card.jsx
    - .planning/phases/02.1-public-marketing-landing-page/02.1-UI-SPEC.md
    - .planning/phases/02.1-public-marketing-landing-page/02.1-CONTEXT.md
    - .planning/phases/02.1-public-marketing-landing-page/02.1-RESEARCH.md
  </read_first>
  <action>
Create 7 section components and rewrite page.js. All section components are SERVER components (no `"use client"`) except where noted. They import `AnimatedSection` (client component from Plan 01) to wrap animated content.

**CRITICAL RULES for all components:**
- Do NOT add `"use client"` to section components — they are Server Components that render static HTML
- Use `AnimatedSection` wrapper from `./AnimatedSection` for any element that needs scroll animation
- Use `Link` from `next/link` for all internal routes (never `<a href>` for `/onboarding`, `/demo`, `/`)
- Use `Button` from `@/components/ui/button` with `asChild` wrapping `Link` for CTA buttons
- Heritage Copper CTA: `className="bg-landing-accent text-white hover:bg-landing-accent/90 min-h-[44px]"`
- Outline CTA (Watch Demo): `className="border-white text-white hover:bg-white/10 min-h-[44px]"` with `variant="outline"`
- All copy MUST match the Copywriting Contract in UI-SPEC exactly (copy verbatim from this action, do not paraphrase)

---

**1. src/app/components/landing/HeroSection.jsx** (Server Component)

Background: `bg-landing-dark` (Midnight Slate). Padding: `pt-24 md:pt-32 pb-16 md:pb-24 px-6` (top padding clears 64px nav + breathing room).

Layout: `max-w-6xl mx-auto` container. Two-column on desktop (`md:grid md:grid-cols-2 md:gap-12 md:items-center`), stacked on mobile.

Left column (text):
- Headline (Display role): `text-4xl md:text-5xl font-semibold text-white leading-tight` — Copy: **"Every Call You Miss Is a Job Your Competitor Just Won"**
- Subheadline: `text-lg text-white/80 mt-4 mb-8` — Copy: **"HomeService AI answers every call, triages the emergency, and books the slot — while you're on the job."**
- CTA row: `flex flex-col sm:flex-row gap-4`
  - Primary CTA: `Button asChild size="lg" className="bg-landing-accent text-white hover:bg-landing-accent/90 min-h-[44px]"` wrapping `Link href="/onboarding"` — Copy: **"Start My 5-Minute Setup"**
  - Secondary CTA: `Button asChild variant="outline" size="lg" className="border-white text-white hover:bg-white/10 min-h-[44px]"` wrapping `Link href="/demo"` — Copy: **"Watch Demo"**

Right column (mockup): Use `next/image` with `src="/images/dashboard-mockup.png"`, `alt="HomeService AI dashboard showing live call triage and booking"`, `width={640}`, `height={400}`, `priority` (LCP element), `className="rounded-xl shadow-2xl"`. On mobile this stacks below text with `mt-12 md:mt-0`.

Wrap the entire content in `AnimatedSection`.

---

**2. src/app/components/landing/HowItWorksSection.jsx** (Server Component)

Background: `bg-landing-surface` (Soft Stone). Section id: `id="how-it-works"`.

Heading: `text-3xl font-semibold text-center mb-12 text-landing-dark` — Copy: **"Here's How It Works"**

3 steps in a row: `grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto`. Each step:
- Step number circle: `w-10 h-10 rounded-full bg-landing-accent text-white flex items-center justify-center font-semibold mb-4` containing "1", "2", "3"
- Step text (16px body):
  - Step 1: **"A homeowner calls at 11 PM about a burst pipe."**
  - Step 2: **"Your AI answers in one second, triages it as an emergency."**
  - Step 3: **"The job is booked for first thing tomorrow. You get a text. You sleep."**

Each step wrapped in `AnimatedSection` with stagger delay: step 1 delay=0, step 2 delay=0.08, step 3 delay=0.16.

After the steps grid, add a CTA: centered `Button asChild size="lg" className="bg-landing-accent text-white hover:bg-landing-accent/90 min-h-[44px] mt-12"` wrapping `Link href="/onboarding"` — Copy: **"Start My 5-Minute Setup"**

---

**3. src/app/components/landing/FeaturesGrid.jsx** (Server Component)

Background: `bg-landing-surface` (Soft Stone, continues from How it Works). Section id: `id="features"`.

Heading: `text-3xl font-semibold text-center mb-12 text-landing-dark` — Copy: **"Four Reasons This Pays for Itself"**

Grid: `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto`.

4 cards using shadcn `Card` + `CardContent`. Each card wrapped in `AnimatedSection` with stagger (delay: 0, 0.08, 0.16, 0.24).

Icons from lucide-react: `Moon`, `Filter`, `Calendar`, `Bell`.

Card layout — override Card default styles: `className="p-6 border-0 shadow-md"` on Card, `className="p-0"` on CardContent.

Each card contains:
- Icon: `className="size-6 mb-3 text-landing-accent"` with `aria-hidden="true"`
- Title: `h3 className="text-xl font-semibold mb-2 text-landing-dark"`
- Body: `p className="text-landing-muted mb-3"`
- Justification: `p className="text-sm text-landing-success font-medium"`

Card data (EXACT copy from UI-SPEC Copywriting Contract):

Card 1: icon=Moon, title="The Night Shift, Sorted", body="24/7 AI answering. No voicemail. No missed leads. No excuses.", justification="One $3,000 emergency booking at 2 AM covers a month."

Card 2: icon=Filter, title="The Golden Lead Filter", body="Every call is triaged — burst pipe or quote for next month. You know before you call back.", justification="Stop wasting call-back time on tire-kickers."

Card 3: icon=Calendar, title="Money in the Calendar", body="Emergency calls lock a slot while the caller is still on the line. No back-and-forth.", justification="Booked means committed. Leads don't cool off."

Card 4: icon=Bell, title="Instant Emergency SMS", body="When a burst pipe or gas smell comes in, you get a text in seconds — not the morning.", justification="Be first. Not fastest to call back."

---

**4. src/app/components/landing/SocialProofSection.jsx** (Server Component)

Background: `bg-landing-dark` (Midnight Slate). Section id: `id="testimonials"`.

Heading: `text-3xl font-semibold text-center mb-12 text-white` — Copy: **"What Trades Owners Are Saying"**

Grid: `grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto`.

3 testimonial cards. Each wrapped in `AnimatedSection` with stagger (delay: 0, 0.08, 0.16). Use shadcn `Card` with override: `className="bg-white/10 border-white/20 text-white"`.

Each card contains:
- Quote: `p className="text-base leading-relaxed mb-4"` (wrapped in typographic quotes)
- Attribution: `p className="text-sm text-white/60 font-medium"`

Card data (EXACT copy from UI-SPEC):

Card 1: quote="I was losing $4k a month in missed calls. First week with HomeService AI, I booked two emergency jobs at midnight.", attribution="— Dave R., Master Plumber, Auckland"

Card 2: quote="The triage alone is worth it. I know before I pick up whether it's a burst pipe or someone wanting a quote for three months from now.", attribution="— James K., HVAC Owner, Brisbane"

Card 3: quote="Setup took four minutes. I heard my AI answer my own test call and nearly fell off my chair.", attribution="— Mark T., Electrician, Singapore"

---

**5. src/app/components/landing/FinalCTASection.jsx** (Server Component)

Background: `bg-landing-accent` (Heritage Copper). Section id: `id="cta"`.

Centered layout: `max-w-3xl mx-auto text-center py-16 md:py-24 px-6`.

- Heading: `text-3xl md:text-4xl font-semibold text-white mb-4` — Copy: **"Your Next Emergency Call Is Tonight"**
- Subtext: `text-lg text-white/90 mb-8` — Copy: **"Set up your AI in 5 minutes. No tech skills needed."**
- CTA: `Button asChild size="lg" className="bg-landing-dark text-white hover:bg-landing-dark/90 min-h-[44px] text-base px-8"` wrapping `Link href="/onboarding"` — Copy: **"Start My 5-Minute Setup"**

Note: This CTA is inverted — dark button on copper background (not copper button on dark). This is intentional for contrast.

Wrap content in `AnimatedSection`.

---

**6. src/app/components/landing/LandingFooter.jsx** (Server Component)

Background: `bg-landing-dark` (Midnight Slate).

Layout: `max-w-5xl mx-auto py-8 px-6 flex flex-col md:flex-row items-center justify-between gap-4`.

- Left: `text-white/60 text-sm` — Copy: **"HomeService AI — No call goes to voicemail."**
- Right: `flex gap-6 text-white/40 text-sm`
  - `Link href="/terms" className="hover:text-white/60"` — Copy: **"Terms"**
  - `Link href="/privacy" className="hover:text-white/60"` — Copy: **"Privacy"**
- Bottom center: `text-white/30 text-xs mt-4 md:mt-0` — Copyright: use current year dynamically with `{new Date().getFullYear()}`

---

**7. Rewrite src/app/page.js** (Server Component — NO `"use client"`)

Replace the entire current content. The new page.js:
- Does NOT import `getTranslations` from `next-intl/server` (no i18n needed for static marketing copy)
- Does NOT use `async` (no server-side data fetching)
- Imports `LandingNav` and all 6 section components from `./components/landing/`
- Renders them in order: LandingNav, then `<main>` containing HeroSection, HowItWorksSection, FeaturesGrid, SocialProofSection, FinalCTASection, then LandingFooter outside `<main>`

```jsx
import { LandingNav } from './components/landing/LandingNav';
import { HeroSection } from './components/landing/HeroSection';
import { HowItWorksSection } from './components/landing/HowItWorksSection';
import { FeaturesGrid } from './components/landing/FeaturesGrid';
import { SocialProofSection } from './components/landing/SocialProofSection';
import { FinalCTASection } from './components/landing/FinalCTASection';
import { LandingFooter } from './components/landing/LandingFooter';

export default function HomePage() {
  return (
    <>
      <LandingNav />
      <main>
        <HeroSection />
        <HowItWorksSection />
        <FeaturesGrid />
        <SocialProofSection />
        <FinalCTASection />
      </main>
      <LandingFooter />
    </>
  );
}
```

---

**8. Create src/app/demo/page.js** (Server Component)

A minimal coming-soon placeholder for the "Watch Demo" CTA destination.

```jsx
import Link from 'next/link';
import { Button } from '@/components/ui/button';

export default function DemoPage() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-landing-surface px-6">
      <h1 className="text-3xl font-semibold text-landing-dark mb-4">Demo Coming Soon</h1>
      <p className="text-landing-muted mb-8 text-center max-w-md">
        We're putting together a walkthrough of HomeService AI in action. In the meantime, the fastest way to see it is to try it yourself.
      </p>
      <Button asChild size="lg" className="bg-landing-accent text-white hover:bg-landing-accent/90 min-h-[44px]">
        <Link href="/onboarding">Start My 5-Minute Setup</Link>
      </Button>
    </div>
  );
}
```

  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && node node_modules/jest-cli/bin/jest.js --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.js does NOT contain `getTranslations`
    - src/app/page.js does NOT contain `"use client"`
    - src/app/page.js does NOT contain `async`
    - src/app/page.js contains `import { HeroSection }`
    - src/app/page.js contains `import { LandingNav }`
    - src/app/page.js contains `import { LandingFooter }`
    - src/app/components/landing/HeroSection.jsx contains `Every Call You Miss Is a Job Your Competitor Just Won`
    - src/app/components/landing/HeroSection.jsx contains `href="/onboarding"`
    - src/app/components/landing/HeroSection.jsx contains `href="/demo"`
    - src/app/components/landing/HeroSection.jsx contains `import Image from 'next/image'`
    - src/app/components/landing/HeroSection.jsx contains `priority`
    - src/app/components/landing/HowItWorksSection.jsx contains `burst pipe`
    - src/app/components/landing/HowItWorksSection.jsx contains `id="how-it-works"`
    - src/app/components/landing/FeaturesGrid.jsx contains `The Night Shift, Sorted`
    - src/app/components/landing/FeaturesGrid.jsx contains `The Golden Lead Filter`
    - src/app/components/landing/FeaturesGrid.jsx contains `Money in the Calendar`
    - src/app/components/landing/FeaturesGrid.jsx contains `Instant Emergency SMS`
    - src/app/components/landing/FeaturesGrid.jsx contains `import { Card`
    - src/app/components/landing/FeaturesGrid.jsx contains `id="features"`
    - src/app/components/landing/SocialProofSection.jsx contains `Dave R., Master Plumber`
    - src/app/components/landing/SocialProofSection.jsx contains `James K., HVAC Owner`
    - src/app/components/landing/SocialProofSection.jsx contains `Mark T., Electrician`
    - src/app/components/landing/SocialProofSection.jsx contains `id="testimonials"`
    - src/app/components/landing/FinalCTASection.jsx contains `Your Next Emergency Call Is Tonight`
    - src/app/components/landing/FinalCTASection.jsx contains `id="cta"`
    - src/app/components/landing/LandingFooter.jsx contains `No call goes to voicemail`
    - src/app/demo/page.js contains `Coming Soon`
    - src/app/demo/page.js contains `href="/onboarding"`
    - `npx next build` succeeds (exit code 0)
    - `node node_modules/jest-cli/bin/jest.js --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>All 7 landing sections render correct copy per UI-SPEC Copywriting Contract. page.js is a Server Component composing all sections. All primary CTAs link to /onboarding. Watch Demo links to /demo. Feature grid uses shadcn Card with Lucide icons. Testimonials show 3 trade personas. Build succeeds. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of landing page</name>
  <files>src/app/page.js</files>
  <action>
Human visual verification of the complete landing page. Start the dev server with `npm run dev` and open http://localhost:3000.

Verification steps:
1. Verify hero: headline "Every Call You Miss Is a Job Your Competitor Just Won" visible, two CTA buttons present
2. Scroll down — verify nav background transitions from transparent to dark
3. Verify sections fade/slide in as you scroll (scroll animations)
4. Verify How it Works: 3 numbered steps, plumber burst pipe scenario
5. Verify Features Grid: 4 cards with icons, titles, body text, green justification lines
6. Verify Social Proof: 3 testimonial cards on dark background
7. Verify Final CTA: Heritage Copper background with "Your Next Emergency Call Is Tonight"
8. Verify Footer: tagline + Terms/Privacy links
9. Click "Start My 5-Minute Setup" — should navigate to /onboarding
10. Click "Watch Demo" — should navigate to /demo with coming-soon page
11. Resize browser to mobile width (375px) — verify single-column layout, no horizontal overflow
12. Check color palette: Midnight Slate (#0F172A) dark sections, Heritage Copper (#C2410C) CTA buttons, Soft Stone (#F5F5F4) light sections
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User has visually approved the landing page layout, colors, copy, animations, CTAs, and responsive behavior</done>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` succeeds — all imports resolve, no CSS errors, no Server/Client Component violations
- `node node_modules/jest-cli/bin/jest.js --passWithNoTests` — existing 110 tests pass, no regressions
- Visual inspection: page renders all 7 sections with correct copy, colors, and layout
- Navigation: all CTA buttons work (link to /onboarding and /demo)
- Responsive: page is usable on mobile, tablet, and desktop viewports
</verification>

<success_criteria>
- Visiting `/` shows a complete marketing landing page (not the placeholder)
- All copy matches the UI-SPEC Copywriting Contract exactly
- 3 primary CTA placements all link to `/onboarding` with Heritage Copper styling
- "Watch Demo" links to `/demo` with a coming-soon page
- Feature grid shows 4 cards with Lucide icons, trade-specific copy, and green justification lines
- Social proof shows 3 testimonials from plumber, HVAC, and electrician personas
- Scroll animations trigger on viewport entry with fade+slide effect
- Sticky nav transitions from transparent to solid on scroll
- Page is responsive across mobile (< 768px), tablet (768-1023px), and desktop (>= 1024px)
- `next build` succeeds, all existing tests pass
- User has visually approved the page
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-public-marketing-landing-page/02.1-02-SUMMARY.md`
</output>
