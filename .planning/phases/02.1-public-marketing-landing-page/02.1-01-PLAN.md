---
phase: 02.1-public-marketing-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/components/landing/AnimatedSection.jsx
  - src/app/components/landing/LandingNav.jsx
  - public/images/dashboard-mockup.png
autonomous: true
requirements: [LAND-02, LAND-08, LAND-09]

must_haves:
  truths:
    - "Landing color palette tokens are available as Tailwind v4 utility classes"
    - "AnimatedSection wrapper triggers fade+slide animation on scroll into view"
    - "LandingNav is sticky, shows logo left and CTA right, background changes on scroll"
    - "A placeholder hero image exists at public/images/dashboard-mockup.png"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Landing palette CSS custom properties + Tailwind v4 theme registration"
      contains: "--color-landing-accent: #C2410C"
    - path: "src/app/components/landing/AnimatedSection.jsx"
      provides: "Framer Motion whileInView wrapper"
      contains: "use client"
    - path: "src/app/components/landing/LandingNav.jsx"
      provides: "Sticky nav with scroll-triggered background"
      contains: "use client"
    - path: "public/images/dashboard-mockup.png"
      provides: "Hero section visual placeholder"
  key_links:
    - from: "src/app/components/landing/AnimatedSection.jsx"
      to: "framer-motion"
      via: "import { motion, useReducedMotion } from 'framer-motion'"
      pattern: "from 'framer-motion'"
    - from: "src/app/components/landing/LandingNav.jsx"
      to: "@/components/ui/button"
      via: "import { Button }"
      pattern: "from '@/components/ui/button'"
---

<objective>
Set up the landing page foundation: install framer-motion, register trade-inspired color palette in Tailwind v4, create the two client components (AnimatedSection scroll wrapper, LandingNav sticky nav), add smooth-scroll CSS, and create a placeholder hero mockup image.

Purpose: Plan 02 builds all content sections on top of these foundation pieces. Without color tokens registered in globals.css, no landing-specific Tailwind classes work. Without AnimatedSection, no scroll animations. Without LandingNav, no sticky nav.

Output: framer-motion installed, globals.css extended, 2 client components created, placeholder image in public/images/.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-CONTEXT.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-RESEARCH.md
@.planning/phases/02.1-public-marketing-landing-page/02.1-UI-SPEC.md

<interfaces>
<!-- Existing shadcn Button API used by LandingNav -->
From src/components/ui/button.jsx:
```jsx
// cva variants: default, destructive, outline, secondary, ghost, link
// sizes: default (h-9), xs (h-6), sm (h-8), lg (h-10), icon (size-9)
// asChild prop renders Slot.Root instead of button
export { Button, buttonVariants }
```

From src/app/globals.css:
```css
/* Tailwind v4 pattern: @theme inline registers CSS vars as utility classes */
@theme inline {
  --color-background: var(--background);
  /* ... existing tokens ... */
}
:root {
  /* ... existing color values ... */
}
@layer base {
  * { @apply border-border outline-ring/50; }
  body { @apply bg-background text-foreground; font-family: var(--font-inter), system-ui, sans-serif; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install framer-motion and register landing color palette in globals.css</name>
  <files>src/app/globals.css, package.json</files>
  <read_first>
    - src/app/globals.css
    - package.json
    - .planning/phases/02.1-public-marketing-landing-page/02.1-UI-SPEC.md
    - .planning/phases/02.1-public-marketing-landing-page/02.1-RESEARCH.md
  </read_first>
  <action>
1. Install framer-motion:
```bash
npm install framer-motion
```

2. Edit `src/app/globals.css` — add landing palette tokens in TWO places:

Inside `@theme inline { ... }` block, ADD these lines (after the existing entries, before the closing `}`):
```css
  --color-landing-surface: var(--color-landing-surface);
  --color-landing-dark: var(--color-landing-dark);
  --color-landing-accent: var(--color-landing-accent);
  --color-landing-muted: var(--color-landing-muted);
  --color-landing-success: var(--color-landing-success);
```

Inside `:root { ... }` block, ADD these lines (after the existing entries, before the closing `}`):
```css
  --color-landing-surface: #F5F5F4;
  --color-landing-dark: #0F172A;
  --color-landing-accent: #C2410C;
  --color-landing-muted: #475569;
  --color-landing-success: #166534;
```

3. Inside `@layer base { ... }`, ADD a new `html` rule:
```css
  html {
    scroll-padding-top: 4rem;
    scroll-behavior: smooth;
  }
```

This makes anchor links (e.g., `href="#features"`) scroll to the correct position offset by the 64px fixed nav height, and enables native smooth scrolling without JS.

DO NOT:
- Remove any existing CSS from globals.css
- Add `"use client"` to globals.css (it's a CSS file)
- Use oklch for the landing colors (hex values are intentional — they match the UI-SPEC exactly)
  </action>
  <verify>
    <automated>grep -c "color-landing-accent" src/app/globals.css | grep -q "2" && grep -q "framer-motion" package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css `@theme inline` block contains `--color-landing-surface: var(--color-landing-surface);`
    - globals.css `@theme inline` block contains `--color-landing-dark: var(--color-landing-dark);`
    - globals.css `@theme inline` block contains `--color-landing-accent: var(--color-landing-accent);`
    - globals.css `@theme inline` block contains `--color-landing-muted: var(--color-landing-muted);`
    - globals.css `@theme inline` block contains `--color-landing-success: var(--color-landing-success);`
    - globals.css `:root` block contains `--color-landing-surface: #F5F5F4;`
    - globals.css `:root` block contains `--color-landing-dark: #0F172A;`
    - globals.css `:root` block contains `--color-landing-accent: #C2410C;`
    - globals.css `:root` block contains `--color-landing-muted: #475569;`
    - globals.css `:root` block contains `--color-landing-success: #166534;`
    - globals.css `@layer base` contains `scroll-padding-top: 4rem;`
    - globals.css `@layer base` contains `scroll-behavior: smooth;`
    - package.json contains `"framer-motion"` in dependencies
    - `node node_modules/jest-cli/bin/jest.js --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>framer-motion is installed, all 5 landing color tokens registered in both @theme inline and :root, scroll-padding-top and scroll-behavior set, existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create AnimatedSection + LandingNav client components and hero placeholder image</name>
  <files>src/app/components/landing/AnimatedSection.jsx, src/app/components/landing/LandingNav.jsx, public/images/dashboard-mockup.png</files>
  <read_first>
    - src/app/globals.css
    - src/components/ui/button.jsx
    - .planning/phases/02.1-public-marketing-landing-page/02.1-RESEARCH.md
    - .planning/phases/02.1-public-marketing-landing-page/02.1-UI-SPEC.md
  </read_first>
  <action>
1. Create directory `src/app/components/landing/` if it doesn't exist.

2. Create `src/app/components/landing/AnimatedSection.jsx`:
```jsx
'use client';
import { motion, useReducedMotion } from 'framer-motion';

export function AnimatedSection({ children, className, delay = 0 }) {
  const prefersReducedMotion = useReducedMotion();

  return (
    <motion.div
      className={className}
      initial={prefersReducedMotion ? false : { opacity: 0, y: 24 }}
      whileInView={prefersReducedMotion ? {} : { opacity: 1, y: 0 }}
      viewport={{ once: true, margin: '-50px' }}
      transition={{ duration: 0.4, ease: 'easeOut', delay }}
    >
      {children}
    </motion.div>
  );
}
```

Key details:
- `'use client'` directive on line 1 — Framer Motion uses browser APIs
- `useReducedMotion()` hook respects `prefers-reduced-motion` OS setting (Framer Motion v12 requirement)
- `initial={prefersReducedMotion ? false : { opacity: 0, y: 24 }}` — `false` skips initial state entirely when reduced motion preferred
- `viewport={{ once: true, margin: '-50px' }}` — animation fires once (no re-trigger on scroll-back), triggers 50px before element reaches viewport edge
- `transition={{ duration: 0.4, ease: 'easeOut', delay }}` — 400ms ease-out per UI-SPEC, delay prop enables stagger (e.g., 80ms between feature cards)

3. Create `src/app/components/landing/LandingNav.jsx`:
```jsx
'use client';
import { useState, useEffect } from 'react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';

export function LandingNav() {
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll, { passive: true });
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <nav
      className={`fixed top-0 left-0 right-0 z-50 h-16 flex items-center justify-between px-6 transition-all duration-300 ${
        scrolled
          ? 'bg-landing-dark/95 backdrop-blur-md shadow-md'
          : 'bg-transparent'
      }`}
    >
      <Link href="/" className="text-white font-semibold text-lg">
        HomeService AI
      </Link>
      <Button
        asChild
        size="sm"
        className="bg-landing-accent text-white hover:bg-landing-accent/90 min-h-[44px] px-4"
      >
        <Link href="/onboarding">Start My 5-Minute Setup</Link>
      </Button>
    </nav>
  );
}
```

Key details:
- `'use client'` on line 1 — reads `window.scrollY`
- `{ passive: true }` on scroll listener — required for mobile performance (prevents paint blocking)
- Nav is `fixed` with `z-50` and `h-16` (64px)
- Background transitions from transparent (over dark hero) to `bg-landing-dark/95` with `backdrop-blur-md` after 20px scroll
- Logo links to `/` via `Link` (not `<a>`)
- CTA button uses shadcn `Button` with `asChild` wrapping `Link` for client-side navigation to `/onboarding`
- `min-h-[44px]` ensures WCAG 2.5.5 touch target minimum
- Uses Tailwind v4 utility classes `bg-landing-dark`, `bg-landing-accent` (registered in Task 1)

4. Create `public/images/` directory and add a placeholder PNG for the hero mockup.
   - Generate a 640x400 placeholder image: a simple gradient rectangle with "Dashboard Preview" text
   - If image generation isn't possible, create a 1x1 transparent PNG as a minimal placeholder (the hero section will use a CSS gradient fallback if image fails to load)
   - Place at `public/images/dashboard-mockup.png`

DO NOT:
- Add `"use client"` to any file that doesn't use browser APIs or React hooks
- Use `<a href>` instead of `<Link>` for internal routes
- Use `window.scrollTo()` for smooth scrolling (CSS `scroll-behavior: smooth` handles it)
- Define components inline inside other components (violates rerender-no-inline-components rule)
- Use `viewport={{ once: false }}` (causes re-trigger on scroll-back)
  </action>
  <verify>
    <automated>test -f src/app/components/landing/AnimatedSection.jsx && test -f src/app/components/landing/LandingNav.jsx && test -d public/images && grep -q "use client" src/app/components/landing/AnimatedSection.jsx && grep -q "use client" src/app/components/landing/LandingNav.jsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/components/landing/AnimatedSection.jsx exists and starts with `'use client';`
    - AnimatedSection.jsx contains `import { motion, useReducedMotion } from 'framer-motion';`
    - AnimatedSection.jsx contains `export function AnimatedSection(`
    - AnimatedSection.jsx contains `viewport={{ once: true`
    - AnimatedSection.jsx contains `useReducedMotion()`
    - src/app/components/landing/LandingNav.jsx exists and starts with `'use client';`
    - LandingNav.jsx contains `import { Button } from '@/components/ui/button';`
    - LandingNav.jsx contains `import Link from 'next/link';`
    - LandingNav.jsx contains `{ passive: true }`
    - LandingNav.jsx contains `min-h-[44px]`
    - LandingNav.jsx contains `href="/onboarding"`
    - LandingNav.jsx contains `export function LandingNav(`
    - public/images/ directory exists
    - `node node_modules/jest-cli/bin/jest.js --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>AnimatedSection wraps children with framer-motion whileInView fade+slide animation with reduced-motion support. LandingNav is a sticky fixed nav with transparent-to-solid scroll transition, logo left, Heritage Copper CTA right linking to /onboarding. Placeholder hero image exists.</done>
</task>

</tasks>

<verification>
- `npm ls framer-motion` shows installed version
- `grep "color-landing-accent" src/app/globals.css` returns 2 matches (one in @theme inline, one in :root)
- `grep "scroll-padding-top" src/app/globals.css` returns 1 match
- All files in `src/app/components/landing/` contain appropriate `'use client'` directives
- `node node_modules/jest-cli/bin/jest.js --passWithNoTests` — all existing tests pass (no regressions)
</verification>

<success_criteria>
- framer-motion is in package.json dependencies
- 5 landing color tokens registered in both `@theme inline` and `:root` in globals.css
- `scroll-padding-top: 4rem` and `scroll-behavior: smooth` set in `@layer base`
- AnimatedSection.jsx is a client component using framer-motion whileInView with useReducedMotion
- LandingNav.jsx is a client component with sticky behavior, scroll detection, logo, and CTA button
- public/images/dashboard-mockup.png exists
- Existing test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-public-marketing-landing-page/02.1-01-SUMMARY.md`
</output>
