---
phase: 02.1-public-marketing-landing-page
plan: 01
subsystem: ui
tags: [framer-motion, tailwind-v4, landing-page, css-tokens, react-components]

# Dependency graph
requires:
  - phase: 02-onboarding-and-triage
    provides: shadcn Button component at @/components/ui/button used by LandingNav
  - phase: 02-onboarding-and-triage
    provides: globals.css @theme inline pattern for registering Tailwind v4 color tokens
provides:
  - framer-motion installed as runtime dependency
  - 5 landing color tokens registered in globals.css @theme inline and :root
  - scroll-padding-top and scroll-behavior set in @layer base
  - AnimatedSection client component with whileInView fade+slide and useReducedMotion support
  - LandingNav client component with sticky scroll-triggered transparent-to-dark transition
  - public/images/dashboard-mockup.png placeholder for hero section
affects:
  - 02.1-02 (builds all content sections on top of these foundation pieces)

# Tech tracking
tech-stack:
  added:
    - framer-motion@12.38.0 (scroll-triggered animations)
  patterns:
    - Tailwind v4 color token registration: CSS var in @theme inline + value in :root
    - Client component with useReducedMotion for accessibility-safe animations
    - Passive scroll listener pattern for mobile performance in sticky nav

key-files:
  created:
    - src/app/components/landing/AnimatedSection.jsx
    - src/app/components/landing/LandingNav.jsx
    - public/images/dashboard-mockup.png
  modified:
    - src/app/globals.css
    - package.json

key-decisions:
  - "Tailwind v4 landing tokens use hex values (not oklch) — matches UI-SPEC exactly: #F5F5F4, #0F172A, #C2410C, #475569, #166534"
  - "useReducedMotion with initial={false} skips initial state entirely when reduced motion preferred — Framer Motion v12 requirement"
  - "Placeholder PNG is a 640x400 gradient (dark navy) — CSS gradient fallback in hero section handles any load failure"
  - "LandingNav uses passive scroll listener and window.scrollY > 20 threshold for scroll detection"

patterns-established:
  - "Landing color tokens registered via CSS custom property double-registration: @theme inline maps token to var(), :root provides value"
  - "AnimatedSection accepts delay prop for stagger orchestration by parent (80ms between feature cards)"
  - "LandingNav transparent-to-dark: bg-transparent -> bg-landing-dark/95 backdrop-blur-md after 20px scroll"

requirements-completed: [LAND-02, LAND-08, LAND-09]

# Metrics
duration: 8min
completed: 2026-03-20
---

# Phase 02.1 Plan 01: Landing Page Foundation Summary

**framer-motion installed, 5 trade-palette color tokens registered in Tailwind v4, AnimatedSection whileInView wrapper and LandingNav sticky component created**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-20T03:10:00Z
- **Completed:** 2026-03-20T03:18:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Installed framer-motion v12 and registered all 5 landing color tokens in globals.css (both @theme inline for Tailwind utility class generation and :root for CSS variable values)
- Added smooth scroll CSS to @layer base (scroll-padding-top: 4rem, scroll-behavior: smooth) for anchor link offset from sticky nav
- Created AnimatedSection client component with whileInView fade+slide, useReducedMotion hook for accessibility, and delay prop for stagger orchestration
- Created LandingNav sticky client component with passive scroll listener, transparent-to-dark transition, shadcn Button CTA linking to /onboarding, and WCAG 2.5.5 min-h-[44px] touch target
- Created 640x400 gradient placeholder PNG at public/images/dashboard-mockup.png

## Task Commits

Each task was committed atomically:

1. **Task 1: Install framer-motion and register landing color palette** - `b506fe1` (feat)
2. **Task 2: Create AnimatedSection + LandingNav client components and hero placeholder image** - `c0c1144` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/app/globals.css` - Added 5 landing color tokens in @theme inline + :root, html scroll rules in @layer base
- `package.json` - Added framer-motion as runtime dependency
- `src/app/components/landing/AnimatedSection.jsx` - Framer Motion whileInView fade+slide wrapper with useReducedMotion and delay prop
- `src/app/components/landing/LandingNav.jsx` - Sticky nav with scroll state, shadcn Button CTA, logo link
- `public/images/dashboard-mockup.png` - 640x400 dark gradient placeholder for hero section

## Decisions Made
- Hex values used for landing tokens (not oklch) to match UI-SPEC exactly: Heritage Copper `#C2410C`, Midnight Slate `#0F172A`, Soft Stone `#F5F5F4`, Muted Zinc `#475569`, Evergreen `#166534`
- `initial={prefersReducedMotion ? false : ...}` — `false` value skips initial state entirely per Framer Motion v12 recommendation, not just opacity 1 / y 0

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. All 110 existing tests passed without regression after both tasks.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Foundation complete: color tokens, scroll CSS, AnimatedSection, LandingNav, and placeholder image are all in place
- Plan 02 can build all content sections (Hero, How it Works, Features Grid, Social Proof, Final CTA, Footer) using bg-landing-surface, bg-landing-dark, bg-landing-accent utility classes and AnimatedSection/LandingNav imports
- No blockers

---
*Phase: 02.1-public-marketing-landing-page*
*Completed: 2026-03-20*
