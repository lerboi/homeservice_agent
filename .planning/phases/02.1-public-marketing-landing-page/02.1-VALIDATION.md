---
phase: 02.1
slug: public-marketing-landing-page
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-20
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.x (existing) |
| **Config file** | jest.config.js |
| **Quick run command** | `node node_modules/jest-cli/bin/jest.js --passWithNoTests` |
| **Full suite command** | `node node_modules/jest-cli/bin/jest.js` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `node node_modules/jest-cli/bin/jest.js --passWithNoTests`
- **After every plan wave:** Run `node node_modules/jest-cli/bin/jest.js`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | Landing page renders | build | `npm run build` | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `npm install framer-motion` — animation library for scroll animations
- [ ] CSS custom properties for landing palette in `globals.css`

*Existing test infrastructure covers build verification.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual appearance matches UI-SPEC palette | CONTEXT.md color decisions | Visual verification requires human eye | Open `/` in browser, verify Midnight Slate nav, Heritage Copper CTAs, Soft Stone background |
| Scroll animations trigger on viewport entry | CONTEXT.md motion decisions | Animation timing requires visual inspection | Scroll through page, verify sections fade/slide in |
| Responsive layout on mobile | CONTEXT.md responsive decisions | Layout verification requires device/viewport | Resize browser to 375px width, verify all sections readable |
| Sticky nav stays visible on scroll | CONTEXT.md nav decisions | Scroll behavior requires interaction | Scroll down page, verify nav remains fixed at top |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
