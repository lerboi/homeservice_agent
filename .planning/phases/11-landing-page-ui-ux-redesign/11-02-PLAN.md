---
phase: 11-landing-page-ui-ux-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/landing/HeroSection.jsx
  - src/app/components/landing/SocialProofSection.jsx
  - src/app/components/landing/FinalCTASection.jsx
  - src/app/globals.css
autonomous: true
requirements:
  - REDESIGN-HERO
  - REDESIGN-SOCIAL
  - REDESIGN-CTA

must_haves:
  truths:
    - "Hero section uses the new Spline community model URL (not the old prod.spline.design URL)"
    - "Social Proof cards lift on hover with translate-y transform"
    - "Social Proof metric badges show subtle green glow on hover"
    - "Final CTA has a third radial gradient layer and a slow CSS animation"
    - "Final CTA animation is disabled when prefers-reduced-motion is active"
  artifacts:
    - path: "src/app/components/landing/HeroSection.jsx"
      provides: "Updated Spline model URL"
      contains: "SPLINE_SCENE_URL"
    - path: "src/app/components/landing/SocialProofSection.jsx"
      provides: "Upgraded hover effects on testimonial cards"
      contains: "hover:-translate-y-1"
    - path: "src/app/components/landing/FinalCTASection.jsx"
      provides: "Third gradient layer and animated overlay"
      contains: "cta-glow-shift"
    - path: "src/app/globals.css"
      provides: "CTA glow shift keyframe and reduced-motion guard"
      contains: "cta-glow-shift"
  key_links:
    - from: "src/app/components/landing/FinalCTASection.jsx"
      to: "src/app/globals.css"
      via: "CSS class referencing cta-glow-shift keyframe"
      pattern: "cta-glow-shift"
---

<objective>
Polish Hero section (Spline model URL update), Social Proof section (hover lift + metric glow), and Final CTA section (gradient depth + CSS animation). All low-to-medium complexity changes on separate files from Plan 01.

Purpose: These are the visual polish items (D-03, D-16, D-18) that elevate the landing page from functional to handcrafted quality. The Spline model update brings the cursor-reactive 3D scene, and the CTA animation adds premium depth.

Output: HeroSection.jsx (updated URL), SocialProofSection.jsx (hover polish), FinalCTASection.jsx (gradient + animation class), globals.css (keyframe)
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-CONTEXT.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-RESEARCH.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-UI-SPEC.md

<interfaces>
<!-- Existing components being updated — executor must read current state -->

From src/app/components/landing/HeroSection.jsx:
```jsx
const SPLINE_SCENE_URL = 'https://prod.spline.design/PyzDhpQ9E5f1E3MT/scene.splinecode';
export function HeroSection()  // Named export, Server Component (no 'use client')
```

From src/app/components/landing/SocialProofSection.jsx:
```jsx
// Current card hover: hover:bg-white/[0.05] hover:border-white/[0.1]
// No translate-y lift currently
export function SocialProofSection()  // Named export
```

From src/app/components/landing/FinalCTASection.jsx:
```jsx
// Currently 2 gradient layers: bottom-right dark, top-left light
// No CSS animation
export function FinalCTASection()  // Named export, Server Component
```

From src/app/globals.css:
```css
/* Existing keyframes: accordion-down, accordion-up, draw-in, radial-pulse */
/* Landing tokens defined in @theme inline block */
/* @media (prefers-reduced-motion: no-preference) block exists for draw-in and radial-pulse animations */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero Spline model URL and polish Social Proof hover effects</name>
  <files>src/app/components/landing/HeroSection.jsx, src/app/components/landing/SocialProofSection.jsx</files>
  <read_first>
    - src/app/components/landing/HeroSection.jsx (current implementation — find SPLINE_SCENE_URL)
    - src/app/components/landing/SocialProofSection.jsx (current hover styles)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-CONTEXT.md (D-03 Spline model, D-16 social proof polish)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-RESEARCH.md (Pitfall 1: Spline URL format)
  </read_first>
  <action>
**Update `src/app/components/landing/HeroSection.jsx`:**

1. The Spline community model from D-03 (`https://app.spline.design/community/file/2ce6351a-d7a5-4c4e-bf13-75bc9f841891`) needs its `.splinecode` production URL. Per RESEARCH.md Pitfall 1, the community browser URL is NOT the scene URL — the Spline runtime needs `https://prod.spline.design/[ID]/scene.splinecode`.

2. IMPORTANT: The executor must determine the correct prod.spline.design URL for this model. Options:
   - Visit the community URL and extract the embed/export URL
   - If the prod URL cannot be determined, keep the existing `SPLINE_SCENE_URL` (`https://prod.spline.design/PyzDhpQ9E5f1E3MT/scene.splinecode`) and add a `// TODO: Update to D-03 community model once prod URL is extracted` comment

3. Update the `SPLINE_SCENE_URL` constant with the new URL.

4. All other Hero section content is locked and must remain unchanged per D-04 through D-07: RotatingText headline, avatar stack, CTA pair, mobile fallback, eyebrow, layout grid.

**Update `src/app/components/landing/SocialProofSection.jsx`:**

1. Add `hover:-translate-y-1` to the testimonial card `<div>` (the `group h-full flex flex-col rounded-2xl...` div). This adds a 4px lift on hover per UI-SPEC. The updated className for the card div should be:
   ```
   group h-full flex flex-col rounded-2xl bg-white/[0.03] border border-white/[0.06] backdrop-blur-sm transition-all duration-300 hover:bg-white/[0.05] hover:border-white/[0.1] hover:-translate-y-1
   ```

2. Add hover glow to the metric badge. Update the metric badge `<div>` (the `inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-[#166534]/10 border border-[#166534]/20` div) to add a transition and hover shadow via the parent card group:
   ```
   inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-[#166534]/10 border border-[#166534]/20 mb-4 transition-shadow duration-300 group-hover:shadow-[0_0_16px_rgba(22,101,52,0.2)]
   ```

3. All testimonial content (quotes, names, roles, metrics) must remain unchanged per D-17.
  </action>
  <verify>
    <automated>cd F:/homeservice-agent && node -e "const fs = require('fs'); const hero = fs.readFileSync('src/app/components/landing/HeroSection.jsx','utf8'); const social = fs.readFileSync('src/app/components/landing/SocialProofSection.jsx','utf8'); const checks = [hero.includes('SPLINE_SCENE_URL'), social.includes('hover:-translate-y-1'), social.includes('group-hover:shadow'), social.includes('Dave R.'), social.includes('James K.'), social.includes('Mark T.')]; console.log('All checks pass:', checks.every(Boolean)); checks.forEach((c,i) => { if(!c) console.log('FAIL check', i); });"</automated>
  </verify>
  <acceptance_criteria>
    - HeroSection.jsx contains SPLINE_SCENE_URL constant (either updated URL or existing URL with TODO comment)
    - HeroSection.jsx preserves RotatingText with texts={['Competitor', 'Revenue', 'Customer']}
    - HeroSection.jsx preserves AuthAwareCTA variant="hero"
    - HeroSection.jsx preserves mobile fallback Image with priority attribute
    - SocialProofSection.jsx card div contains `hover:-translate-y-1`
    - SocialProofSection.jsx metric badge div contains `group-hover:shadow-[0_0_16px_rgba(22,101,52,0.2)]`
    - SocialProofSection.jsx preserves all 3 testimonials (Dave R., James K., Mark T.)
    - SocialProofSection.jsx preserves StarRating component
  </acceptance_criteria>
  <done>Hero section has updated Spline model URL (or TODO for URL extraction). Social Proof cards lift 4px on hover with smooth transition, and metric badges show a subtle green glow on card hover.</done>
</task>

<task type="auto">
  <name>Task 2: Add Final CTA gradient depth and CSS keyframe animation</name>
  <files>src/app/components/landing/FinalCTASection.jsx, src/app/globals.css</files>
  <read_first>
    - src/app/components/landing/FinalCTASection.jsx (current 2-gradient implementation)
    - src/app/globals.css (existing keyframes and @media blocks)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-UI-SPEC.md (Section 5 Final CTA implementation notes)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-RESEARCH.md (Pattern 4: CTA Background Animation, Pitfall 7)
  </read_first>
  <action>
**Update `src/app/globals.css`:**

1. Add the `cta-glow-shift` keyframe AFTER the existing `radial-pulse` keyframe block (around line 82):
   ```css
   @keyframes cta-glow-shift {
     0%, 100% { opacity: 0.6; transform: scale(1); }
     50% { opacity: 1; transform: scale(1.05); }
   }
   ```

2. Add the animation class inside the existing `@media (prefers-reduced-motion: no-preference)` block (after the radial-pulse entries, before the closing `}`):
   ```css
   .animate-cta-glow {
     animation: cta-glow-shift 10s ease-in-out infinite;
   }
   ```

This approach uses CSS-only `@media (prefers-reduced-motion)` — when the user has reduced motion enabled, the `.animate-cta-glow` class simply has no animation rule applied. No JavaScript `useReducedMotion` hook needed, so FinalCTASection stays a Server Component per Pitfall 7.

**Update `src/app/components/landing/FinalCTASection.jsx`:**

1. Add a third gradient layer (center ellipse) after the existing 2 gradient overlays:
   ```jsx
   <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(255,255,255,0.05),transparent_40%)]" />
   ```

2. Add the animated overlay div after the third gradient layer:
   ```jsx
   <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(255,255,255,0.12),transparent_50%)] animate-cta-glow" />
   ```

3. The full gradient stack in order should be:
   - Layer 1: `bg-[#C2410C]` (base, existing)
   - Layer 2: `bg-[radial-gradient(ellipse_at_bottom_right,rgba(0,0,0,0.15),transparent_60%)]` (existing)
   - Layer 3: `bg-[radial-gradient(ellipse_at_top_left,rgba(255,255,255,0.08),transparent_50%)]` (existing)
   - Layer 4: `bg-[radial-gradient(ellipse_at_center,rgba(255,255,255,0.05),transparent_40%)]` (NEW static)
   - Layer 5: `bg-[radial-gradient(ellipse_at_center,rgba(255,255,255,0.12),transparent_50%)] animate-cta-glow` (NEW animated)

4. Do NOT add `'use client'` to this file. It must remain a Server Component.

5. All CTA copy and AuthAwareCTA usage must remain unchanged per D-19.
  </action>
  <verify>
    <automated>cd F:/homeservice-agent && node -e "const fs = require('fs'); const cta = fs.readFileSync('src/app/components/landing/FinalCTASection.jsx','utf8'); const css = fs.readFileSync('src/app/globals.css','utf8'); const checks = [cta.includes('animate-cta-glow'), cta.includes('rgba(255,255,255,0.05)'), cta.includes('rgba(255,255,255,0.12)'), !cta.includes(\"'use client'\"), css.includes('cta-glow-shift'), css.includes('animate-cta-glow'), css.includes('10s ease-in-out infinite'), cta.includes('AuthAwareCTA')]; console.log('All checks pass:', checks.every(Boolean)); checks.forEach((c,i) => { if(!c) console.log('FAIL check', i); });"</automated>
  </verify>
  <acceptance_criteria>
    - FinalCTASection.jsx does NOT contain `'use client'`
    - FinalCTASection.jsx contains `animate-cta-glow` class on a gradient overlay div
    - FinalCTASection.jsx contains 5 absolute overlay divs (base + 4 gradient layers)
    - FinalCTASection.jsx contains `rgba(255,255,255,0.05)` (third static gradient)
    - FinalCTASection.jsx contains `rgba(255,255,255,0.12)` (animated gradient)
    - FinalCTASection.jsx preserves AuthAwareCTA with variant="cta"
    - FinalCTASection.jsx preserves all existing copy per D-19
    - globals.css contains `@keyframes cta-glow-shift` with `opacity: 0.6` and `opacity: 1` and `scale(1.05)`
    - globals.css contains `.animate-cta-glow` inside `@media (prefers-reduced-motion: no-preference)` block
    - globals.css `.animate-cta-glow` uses `10s ease-in-out infinite`
  </acceptance_criteria>
  <done>Final CTA section has 3 additional gradient layers (2 static + 1 animated) creating premium depth. The animation is CSS-only with automatic reduced-motion disable. FinalCTASection remains a Server Component.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Load http://localhost:3000 and scroll to Social Proof — hover cards to see 4px lift and green badge glow
- Load http://localhost:3000 and scroll to Final CTA — observe slow gradient pulse animation
- Enable OS reduced-motion setting — CTA animation stops, no other animations play
- Hero section 3D scene loads and responds to cursor movement (desktop only)
</verification>

<success_criteria>
- Hero Spline model URL is updated (or has clear TODO if URL extraction needed)
- Social Proof cards lift on hover and metric badges glow green
- Final CTA has 3 gradient layers and a 10s breathing animation
- FinalCTASection remains a Server Component (no 'use client')
- All animations respect prefers-reduced-motion via CSS media query
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page-ui-ux-redesign/11-02-SUMMARY.md`
</output>
