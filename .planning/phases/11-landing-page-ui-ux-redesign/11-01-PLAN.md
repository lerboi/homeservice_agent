---
phase: 11-landing-page-ui-ux-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/landing/HowItWorksTabs.jsx
  - src/app/components/landing/HowItWorksSection.jsx
  - src/app/components/landing/FeaturesGrid.jsx
  - src/app/(public)/page.js
autonomous: true
requirements:
  - REDESIGN-HIW
  - REDESIGN-FEAT

must_haves:
  truths:
    - "How It Works section displays 3 tabs ([01] [02] [03]) that switch panel content on click"
    - "Tab transitions animate with fade + vertical translate (250ms ease-out)"
    - "Tabs are keyboard navigable with role=tablist, role=tab, aria-selected, aria-controls"
    - "Features section displays 5 bento cards including 'Speaks Their Language' as the 5th card"
    - "Features H2 reads 'Five features' not 'Four features'"
    - "page.js loading skeletons match new component dimensions (tab panel + 5-card grid)"
  artifacts:
    - path: "src/app/components/landing/HowItWorksTabs.jsx"
      provides: "Client-side tabbed panel with AnimatePresence transitions"
      contains: "'use client'"
    - path: "src/app/components/landing/HowItWorksSection.jsx"
      provides: "Server Component section shell with dynamic-imported tabs"
      contains: "HowItWorksTabs"
    - path: "src/app/components/landing/FeaturesGrid.jsx"
      provides: "5-card bento grid with Globe icon 5th card"
      contains: "Speaks Their Language"
    - path: "src/app/(public)/page.js"
      provides: "Updated loading skeletons for HowItWorks and FeaturesGrid"
      contains: "md:col-span-2"
  key_links:
    - from: "src/app/components/landing/HowItWorksSection.jsx"
      to: "src/app/components/landing/HowItWorksTabs.jsx"
      via: "dynamic import"
      pattern: "dynamic.*HowItWorksTabs"
    - from: "src/app/components/landing/HowItWorksTabs.jsx"
      to: "framer-motion"
      via: "AnimatePresence"
      pattern: "AnimatePresence.*mode.*wait"
---

<objective>
Rebuild How It Works section from stacked cards to an accessible tabbed panel interface, add the 5th feature card to the bento grid, and update page.js loading skeletons.

Purpose: The How It Works tab redesign (D-08 through D-12) is the highest-complexity change in Phase 11 — a new client component with animated transitions and full ARIA accessibility. The 5th bento card (D-14) completes the feature set with the multi-language differentiator.

Output: HowItWorksTabs.jsx (new), HowItWorksSection.jsx (rebuilt), FeaturesGrid.jsx (updated), page.js (skeleton updates)
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-CONTEXT.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-RESEARCH.md
@.planning/phases/11-landing-page-ui-ux-redesign/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/components/landing/AnimatedSection.jsx:
```jsx
// KEEP AS-IS — do not modify
export function AnimatedSection({ children, className, delay = 0, direction = 'up' })
export function AnimatedStagger({ children, className })
export function AnimatedItem({ children, className })
```

From src/app/components/landing/HowItWorksSection.jsx (current — will be rebuilt):
```jsx
// Current steps data array — reuse in HowItWorksTabs
const steps = [
  { number: '01', icon: Phone, title: 'Call comes in', description: '...', detail: '...', accent: 'from-amber-500/20 to-amber-500/5', iconColor: 'text-amber-600' },
  { number: '02', icon: Brain, title: 'AI triages instantly', description: '...', detail: '...', accent: 'from-sky-500/20 to-sky-500/5', iconColor: 'text-sky-600' },
  { number: '03', icon: CalendarCheck, title: 'Job is booked', description: '...', detail: '...', accent: 'from-emerald-500/20 to-emerald-500/5', iconColor: 'text-emerald-600' },
];
export function HowItWorksSection() // Named export
```

From src/app/components/landing/FeaturesGrid.jsx (current — will be updated):
```jsx
'use client';
const features = [ /* 4 cards currently */ ];
function BentoCard({ feature }) // Internal component
export function FeaturesGrid() // Named export
```

From src/app/(public)/page.js:
```jsx
import { HeroSection } from '@/app/components/landing/HeroSection';
import dynamic from 'next/dynamic';
const HowItWorksSection = dynamic(() => import('...').then(m => m.HowItWorksSection), { loading: () => (...) });
const FeaturesGrid = dynamic(() => import('...').then(m => m.FeaturesGrid), { loading: () => (...) });
// Named exports pattern — dynamic imports use .then(m => m.ComponentName)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HowItWorksTabs client component and rebuild HowItWorksSection</name>
  <files>src/app/components/landing/HowItWorksTabs.jsx, src/app/components/landing/HowItWorksSection.jsx</files>
  <read_first>
    - src/app/components/landing/HowItWorksSection.jsx (current implementation to extract step data and copy)
    - src/app/components/landing/AnimatedSection.jsx (reuse pattern, do NOT modify)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-UI-SPEC.md (interaction contracts for tab behavior)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-RESEARCH.md (Pattern 1: HowItWorksTabs code example)
  </read_first>
  <action>
**Create `src/app/components/landing/HowItWorksTabs.jsx`** — new file, `'use client'` directive at top:

1. Import `useState` from react, `AnimatePresence`, `motion`, `useReducedMotion` from `framer-motion`, and `Phone`, `Brain`, `CalendarCheck` from `lucide-react`.

2. Define `steps` array inside the component file (moved from HowItWorksSection):
   ```
   steps[0]: { number: '01', icon: Phone, title: 'Call comes in', description: 'A homeowner calls at 11 PM about a burst pipe. Your AI picks up in under a second.', detail: 'No voicemail. No hold music. No missed revenue.', accent: 'from-amber-500/20 to-amber-500/5', iconColor: 'text-amber-600' }
   steps[1]: { number: '02', icon: Brain, title: 'AI triages instantly', description: 'The call is classified as an emergency. Your AI shifts tone — faster, more direct.', detail: '"I understand this is urgent. Let me get you scheduled right away."', accent: 'from-sky-500/20 to-sky-500/5', iconColor: 'text-sky-600' }
   steps[2]: { number: '03', icon: CalendarCheck, title: 'Job is booked', description: 'First available morning slot is locked while the caller is still on the line.', detail: 'You get a text. The homeowner gets confirmation. You sleep.', accent: 'from-emerald-500/20 to-emerald-500/5', iconColor: 'text-emerald-600' }
   ```

3. Export named function `HowItWorksTabs`:
   - `const [active, setActive] = useState(0);`
   - `const prefersReducedMotion = useReducedMotion();`

4. Tab bar markup — `<div role="tablist" className="flex gap-2 justify-center mb-8">`:
   - 3 buttons, each with:
     - `role="tab"`
     - `aria-selected={active === i}`
     - `aria-controls={`panel-${step.number}`}`
     - `id={`tab-${step.number}`}`
     - `tabIndex={active === i ? 0 : -1}` (roving tabindex)
     - `onClick={() => setActive(i)}`
     - `onKeyDown` handler: ArrowRight goes to next tab (wraps), ArrowLeft goes to previous (wraps), Home goes to first, End goes to last — per WAI-ARIA Tabs pattern
     - Active state styling: `bg-[#C2410C]/10 text-[#C2410C] border-b-2 border-[#C2410C]`
     - Inactive state: `text-[#475569]/60 hover:text-[#475569]`
     - Focus ring: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#C2410C] focus-visible:ring-offset-2`
     - Each button label: `step.number` (e.g., "01") — text-sm font-medium
     - Button min-h-[48px] px-5 rounded-lg transition-colors duration-200

5. Panel markup — `<AnimatePresence mode="wait">`:
   - Single `<motion.div>` child with `key={active}`:
     - `id={`panel-${steps[active].number}`}`
     - `role="tabpanel"`
     - `aria-labelledby={`tab-${steps[active].number}`}`
     - `initial={prefersReducedMotion ? false : { opacity: 0, y: 8 }}`
     - `animate={{ opacity: 1, y: 0 }}`
     - `exit={prefersReducedMotion ? {} : { opacity: 0, y: -8 }}`
     - `transition={{ duration: 0.25, ease: 'easeOut' }}`
   - Panel container: `rounded-2xl border border-black/[0.04] bg-[#F5F5F4]/50 p-6 md:p-8 min-h-[280px]`
   - Panel content layout: `md:flex md:items-start md:gap-8`
     - Left: icon container (`size-14 rounded-2xl bg-gradient-to-br ${step.accent} flex items-center justify-center border border-black/[0.04]`) with `<Icon className={`size-6 ${step.iconColor}`} strokeWidth={1.5} />`
     - Center: title (`text-lg font-semibold text-[#0F172A] mb-1.5`), description (`text-[15px] text-[#475569] leading-relaxed`), detail (`text-sm text-[#0F172A]/50 mt-2 italic`)
     - Right (desktop only): large decorative step number (`hidden md:block text-7xl font-semibold text-black/[0.03] leading-none select-none`)

**Rebuild `src/app/components/landing/HowItWorksSection.jsx`** — Server Component (NO 'use client'):

1. Remove all existing content. Remove `AnimatedStagger`, `AnimatedItem` imports. Keep `AnimatedSection` import from `'./AnimatedSection'`. Import `dynamic` from `next/dynamic`. Import `Link` from `next/link`. Import `Button` from `@/components/ui/button`. Import `ArrowRight` from `lucide-react`.

2. Dynamic import HowItWorksTabs with loading skeleton:
   ```jsx
   const HowItWorksTabs = dynamic(
     () => import('./HowItWorksTabs').then((m) => m.HowItWorksTabs),
     {
       loading: () => (
         <div className="space-y-4">
           <div className="flex gap-2 justify-center mb-8">
             {[1, 2, 3].map((i) => (
               <div key={i} className="h-12 w-16 rounded-lg bg-black/[0.04]" />
             ))}
           </div>
           <div className="h-[280px] rounded-2xl bg-[#F5F5F4]/50 border border-black/[0.04]" />
         </div>
       ),
     }
   );
   ```

3. Section structure (same as before but with HowItWorksTabs replacing the stagger):
   - `<section id="how-it-works" className="relative bg-white py-20 md:py-28 px-6 overflow-hidden">`
   - Grid texture div (keep existing pattern): `bg-[linear-gradient(rgba(0,0,0,0.015)_1px,transparent_1px),linear-gradient(90deg,rgba(0,0,0,0.015)_1px,transparent_1px)] bg-[size:48px_48px]`
   - `<div className="relative max-w-5xl mx-auto">`
   - AnimatedSection wrapping heading (same H2, same eyebrow as current)
   - `<HowItWorksTabs />` (no AnimatedStagger wrapper — the dynamic import handles loading)
   - AnimatedSection wrapping CTA button (same as current — "Start My 5-Minute Setup" accent button with ArrowRight icon)
  </action>
  <verify>
    <automated>cd F:/homeservice-agent && node -e "const fs = require('fs'); const tabs = fs.readFileSync('src/app/components/landing/HowItWorksTabs.jsx','utf8'); const section = fs.readFileSync('src/app/components/landing/HowItWorksSection.jsx','utf8'); const checks = [tabs.includes(\"'use client'\"), tabs.includes('role=\"tablist\"'), tabs.includes('role=\"tab\"'), tabs.includes('aria-selected'), tabs.includes('aria-controls'), tabs.includes('AnimatePresence'), tabs.includes('mode=\"wait\"'), tabs.includes('useReducedMotion'), tabs.includes('key={active}'), !section.includes(\"'use client'\"), section.includes('HowItWorksTabs'), section.includes('dynamic')]; console.log('All checks pass:', checks.every(Boolean)); checks.forEach((c,i) => { if(!c) console.log('FAIL check', i); });"</automated>
  </verify>
  <acceptance_criteria>
    - HowItWorksTabs.jsx starts with `'use client'`
    - HowItWorksTabs.jsx contains `role="tablist"` and `role="tab"` and `role="tabpanel"`
    - HowItWorksTabs.jsx contains `aria-selected` and `aria-controls` and `aria-labelledby`
    - HowItWorksTabs.jsx contains `AnimatePresence` with `mode="wait"`
    - HowItWorksTabs.jsx contains `useReducedMotion`
    - HowItWorksTabs.jsx contains `key={active}` on the motion.div
    - HowItWorksTabs.jsx contains onKeyDown handler with ArrowRight/ArrowLeft
    - HowItWorksTabs.jsx contains `min-h-[280px]` on panel container
    - HowItWorksSection.jsx does NOT contain `'use client'`
    - HowItWorksSection.jsx contains `dynamic` import of `HowItWorksTabs`
    - HowItWorksSection.jsx contains loading skeleton with `h-[280px]`
    - HowItWorksSection.jsx preserves same H2 heading text "From missed call to booked job."
    - HowItWorksSection.jsx preserves "Start My 5-Minute Setup" CTA button
  </acceptance_criteria>
  <done>HowItWorksTabs renders 3 tabs with animated panel transitions, full ARIA tab pattern, keyboard navigation, and reduced-motion support. HowItWorksSection is a Server Component that dynamically imports HowItWorksTabs with a height-matched skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Add 5th feature card and update page.js loading skeletons</name>
  <files>src/app/components/landing/FeaturesGrid.jsx, src/app/(public)/page.js</files>
  <read_first>
    - src/app/components/landing/FeaturesGrid.jsx (current 4-card implementation)
    - src/app/(public)/page.js (current loading skeletons)
    - .planning/phases/11-landing-page-ui-ux-redesign/11-UI-SPEC.md (bento grid spans, 5th card copy, skeleton requirements)
  </read_first>
  <action>
**Update `src/app/components/landing/FeaturesGrid.jsx`:**

1. Add `Globe` to the lucide-react import: `import { Moon, Filter, Calendar, Bell, Globe } from 'lucide-react';`

2. Add 5th feature to the `features` array after the "Instant Emergency SMS" entry:
   ```
   {
     icon: Globe,
     title: 'Speaks Their Language',
     body: "Your AI receptionist answers in Spanish, Singlish, or English — switching language the moment it hears the caller. No frustrated hang-ups, no lost leads.",
     justification: 'Every caller heard. Every job captured.',
     span: 'md:col-span-2',
     variant: 'default',
   }
   ```

3. Update the `<h2>` text from "Four features. One question:" to "Five features. One question:"

4. The grid already uses `grid grid-cols-1 md:grid-cols-6 gap-3` — the 5th card (`md:col-span-2`) will flow naturally into the 3rd row alongside card 4 (`md:col-span-4`). Row math: cards 4+5 fill 6 columns in row 3. No grid container changes needed.

5. Card 4 ("Instant Emergency SMS") currently has `span: 'md:col-span-4'`. Card 5 gets `span: 'md:col-span-2'`. Together they fill 6 columns in the 3rd row. Verify this visually — if row-span issues appear, add `md:grid-rows-3` to the grid container.

**Update `src/app/(public)/page.js`:**

1. Update the HowItWorksSection loading skeleton to match the new tab layout:
   Replace the current skeleton (3 stacked `h-32` cards in `space-y-3`) with:
   ```jsx
   <section className="bg-white py-20 md:py-28 px-6" aria-hidden="true">
     <div className="max-w-5xl mx-auto">
       <div className="text-center mb-16">
         <div className="h-4 w-24 bg-black/10 rounded mx-auto mb-3" />
         <div className="h-10 w-80 bg-black/10 rounded mx-auto" />
       </div>
       <div className="space-y-4">
         <div className="flex gap-2 justify-center mb-8">
           {[1, 2, 3].map((i) => (
             <div key={i} className="h-12 w-16 rounded-lg bg-black/[0.04]" />
           ))}
         </div>
         <div className="h-[280px] rounded-2xl bg-[#F5F5F4]/50 border border-black/[0.04]" />
       </div>
     </div>
   </section>
   ```

2. Update the FeaturesGrid loading skeleton to include the 5th card:
   Add this line after the `md:col-span-4 h-40` skeleton div:
   ```jsx
   <div className="md:col-span-2 h-40 rounded-2xl bg-white" />
   ```
   So the full grid skeleton becomes 5 divs: col-span-4 row-span-2, col-span-2, col-span-2, col-span-4, col-span-2.
  </action>
  <verify>
    <automated>cd F:/homeservice-agent && node -e "const fs = require('fs'); const feat = fs.readFileSync('src/app/components/landing/FeaturesGrid.jsx','utf8'); const page = fs.readFileSync('src/app/(public)/page.js','utf8'); const checks = [feat.includes('Globe'), feat.includes('Speaks Their Language'), feat.includes('Five features'), feat.includes('Every caller heard'), page.includes('h-[280px]'), (page.match(/md:col-span-2/g) || []).length >= 3]; console.log('All checks pass:', checks.every(Boolean)); checks.forEach((c,i) => { if(!c) console.log('FAIL check', i); });"</automated>
  </verify>
  <acceptance_criteria>
    - FeaturesGrid.jsx imports `Globe` from lucide-react
    - FeaturesGrid.jsx contains "Speaks Their Language" as a feature title
    - FeaturesGrid.jsx contains "Five features. One question:" in the H2
    - FeaturesGrid.jsx contains "Every caller heard. Every job captured." as justification
    - FeaturesGrid.jsx contains 5 entries in the features array
    - FeaturesGrid.jsx 5th card has `span: 'md:col-span-2'` and `variant: 'default'`
    - page.js HowItWorks skeleton contains `h-[280px]` panel placeholder
    - page.js HowItWorks skeleton contains 3 tab placeholder divs
    - page.js FeaturesGrid skeleton contains 5 card placeholders (including new `md:col-span-2`)
  </acceptance_criteria>
  <done>Features section displays 5 bento cards with "Speaks Their Language" as the 5th card. H2 reads "Five features." Page.js loading skeletons match the new tab panel layout and 5-card grid dimensions for zero CLS.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors (or `npx next lint` if faster)
- Load http://localhost:3000 and verify How It Works section shows tabs instead of stacked cards
- Load http://localhost:3000 and verify Features section shows 5 cards in the bento grid
- Click each tab in How It Works and confirm panel content transitions
- Tab to How It Works tabs using keyboard and confirm Arrow key navigation works
</verification>

<success_criteria>
- How It Works section uses tabbed interface with 3 tabs, animated transitions, and full ARIA accessibility
- Features section displays 5 bento cards with the "Speaks Their Language" 5th card
- Loading skeletons in page.js match new component dimensions
- HowItWorksTabs is a client component; HowItWorksSection remains a Server Component
- All animations respect prefers-reduced-motion
</success_criteria>

<output>
After completion, create `.planning/phases/11-landing-page-ui-ux-redesign/11-01-SUMMARY.md`
</output>
