---
phase: 61.1
plan: 03
type: execute
wave: 2
depends_on:
  - "61.1-01"
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md
autonomous: true
gap_closure: true
requirements:
  - PHASE-61.1-WR-03
tags:
  - documentation
  - skill-sync
  - bug-fix
  - WR-03

must_haves:
  truths:
    - "The voice-call-architecture SKILL accurately documents the Phase 61 success-path tool-return shape (BOOKED/LEAD CAPTURED label form, NOT STATE:... | DIRECTIVE:... form)."
    - "The integrations-jobber-xero SKILL is corrected (it claims tool returns are 'STATE+DIRECTIVE-shaped and carry the verdict token verbatim' which is only true for the failure paths)."
    - "Verbatim sample success-path strings are documented for both book_appointment and capture_lead so future readers can grep for them."
    - "The brittleness risk is explicitly called out — a future prompt rev that keys on the `STATE:` prefix would silently drop these success paths."
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md"
      provides: "Updated 'Tool returns (D-E2)' subsection accurately documenting BOTH shapes (label-form on success, STATE+DIRECTIVE on failure) plus the brittleness watch-item."
      contains: "Tool returns use TWO shapes"
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md"
      provides: "Corrected statement about tool-return shapes."
      contains: "BOOKED [verdict="
  key_links:
    - from: ".claude/skills/voice-call-architecture/SKILL.md Phase 61 subsection"
      to: "src/tools/book_appointment.py + src/tools/capture_lead.py success-return code paths"
      via: "verbatim sample strings + line refs"
      pattern: "verdict=validated"
---

<objective>
Close WR-03 from `61-REVIEW.md` by updating the SKILL docs to accurately describe the Phase 61 success-path tool-return shape. Per 61-REVIEW.md WR-03 (lines 121-148):

> The skill `integrations-jobber-xero` says tool returns are "STATE+DIRECTIVE-shaped and carry the verdict token verbatim" — but only the failure paths use the `STATE:... | DIRECTIVE:...` shape. The new success-path returns use a different shape: `BOOKED [verdict=validated]: <directive>` (no `STATE:` prefix, no `|` separator, no `DIRECTIVE:` keyword).

61-REVIEW.md's recommendation: **update the doc to reflect the actual two shapes** rather than churn the success-path code (the `BOOKED` / `LEAD CAPTURED` labels exist pre-Phase-61 and are battle-tested). This plan follows that recommendation.

This plan changes ZERO code in the livekit-agent repo. It only updates two SKILL.md files in the homeservice_agent repo. This avoids the file-overlap risk with Plan 01 (which touches book_appointment.py + capture_lead.py); since this plan does NOT touch those code files, we could in principle run it in Wave 1, but it depends on Plan 01's UAT confirming the production fix landed (otherwise we'd be documenting code we may need to revisit). Set wave=2 to be safe.

Purpose: Eliminate the documentation/code drift that 61-REVIEW.md flagged as a brittleness risk (a future prompt revision keying on the `STATE:` prefix would silently drop these success paths).

Output: Two skill-doc edits, one homeservice_agent commit.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-REVIEW.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-INCIDENT.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md

<homeservice_agent_repo>
The two files this plan modifies are in the homeservice_agent repo (NOT the livekit-agent sibling). Commit lands on homeservice_agent's main branch.
</homeservice_agent_repo>

<interfaces>
Verbatim success-path strings produced by the current code (read these from the files in `<read_first>` to confirm):

book_appointment.py (lines 595-614):
- `BOOKED [verdict=validated]: relay normalized address [{formatted_address}] and time [{slot_speech}] as confirmed; ask if anything else is needed`
- `BOOKED [verdict=validated_with_corrections]: relay normalized address [{formatted_address}] as the final form, explicitly invite caller confirmation before closing; if caller corrects, accept correction and re-read full address`
- `BOOKED [verdict=unvalidated]: relay address as caller spoke it; do NOT claim "validated", "confirmed against records", or "looked up your address"`

capture_lead.py (lines 138-158):
- `LEAD CAPTURED [verdict=validated]: relay normalized address [{formatted_address}] as confirmed; ask if anything else is needed`
- `LEAD CAPTURED [verdict=validated_with_corrections]: relay normalized address [{formatted_address}] as the final form, explicitly invite caller confirmation; if caller corrects, accept correction and re-read full address`
- `LEAD CAPTURED [verdict=unvalidated]: relay address as caller spoke it; do NOT claim "validated", "confirmed against records", or "looked up your address"`

Failure paths use the documented STATE+DIRECTIVE shape verbatim:
- `STATE:lead_capture_failed reason=no_tenant_id | DIRECTIVE:apologize briefly; ...`
- `STATE:lead_capture_failed reason=call_not_ready | DIRECTIVE:...`
- `STATE:lead_capture_failed reason=db_error | DIRECTIVE:...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update voice-call-architecture SKILL.md to document both return shapes accurately</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-REVIEW.md WR-03 section (lines 121-148)
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md lines 1130-1210 (the Phase 61 subsection — current state, especially the "Tool returns (D-E2)" bullet on lines 1151-1156)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/book_appointment.py lines 590-614 (verbatim success-path return strings)
    - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py lines 135-166 (verbatim success-path return strings + failure-path STATE+DIRECTIVE strings)
  </read_first>

  <action>
Step 1 — Locate the "Tool returns (D-E2)" bullet in `voice-call-architecture/SKILL.md`. It currently sits at lines 1151-1156 and reads:

```
- **Tool returns (D-E2):** STATE+DIRECTIVE shape extended with verdict
  tokens — `BOOKED [verdict=validated]:`,
  `BOOKED [verdict=validated_with_corrections]:`,
  `BOOKED [verdict=unvalidated]:` (and `LEAD CAPTURED` equivalents).
  Strings are NEVER spoken aloud; the agent reads them and decides how
  to phrase the readback.
```

Replace those 6 lines verbatim with:

```
- **Tool returns (D-E2):** Tool returns use TWO shapes — the success
  path uses a label form `BOOKED [verdict=...]: <directive>` /
  `LEAD CAPTURED [verdict=...]: <directive>` (battle-tested pre-Phase-61
  label convention extended with the verdict token), and the failure
  path uses the documented `STATE:... | DIRECTIVE:...` shape
  (e.g. `STATE:lead_capture_failed reason=db_error | DIRECTIVE:...`).
  Phase 61 (D-E2) extended the success label form with verdict tokens —
  `BOOKED [verdict=validated]:`, `BOOKED [verdict=validated_with_corrections]:`,
  `BOOKED [verdict=unvalidated]:` (and `LEAD CAPTURED` equivalents).
  The prompt CRITICAL RULE (`_build_address_validation_section`)
  matches via SUBSTRING on the verdict tokens, so both shapes work.
  Strings are NEVER spoken aloud; the agent reads them and decides how
  to phrase the readback.

  **Brittleness watch (WR-03 follow-up):** A future prompt revision
  that keys on the `STATE:` prefix or the `|` separator would silently
  drop the success paths. If you change how the prompt parses these
  returns, audit `book_appointment.py` and `capture_lead.py` success
  branches for symmetric updates. Verbatim success-path samples:
    - `BOOKED [verdict=validated]: relay normalized address [...] and time [...] as confirmed; ask if anything else is needed`
    - `BOOKED [verdict=validated_with_corrections]: relay normalized address [...] as the final form, explicitly invite caller confirmation...`
    - `BOOKED [verdict=unvalidated]: relay address as caller spoke it; do NOT claim "validated"...`
    - `LEAD CAPTURED [verdict=validated]: relay normalized address [...] as confirmed; ask if anything else is needed`
    - `LEAD CAPTURED [verdict=validated_with_corrections]: relay normalized address [...] as the final form, explicitly invite caller confirmation...`
    - `LEAD CAPTURED [verdict=unvalidated]: relay address as caller spoke it; do NOT claim "validated"...`
```

Step 2 — Bump the `**Last updated**` line at the top of the file (currently line 11). Replace the existing date and note. Find the current line:

```
**Last updated**: 2026-05-03 (Phase 61 — ...)
```

Append `+ Phase 61.1 WR-03 — clarified success-path return shape (label-form, not STATE+DIRECTIVE; brittleness watch added)` to the parenthetical, OR if the line is too long, change the date to `2026-05-05` and rewrite the parenthetical to summarize Phase 61 + 61.1. Use your judgement; both forms acceptable.
  </action>

  <verify>
    <automated>grep -q "Tool returns use TWO shapes" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && grep -q "Brittleness watch (WR-03 follow-up)" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` contains the literal string `Tool returns use TWO shapes`
    - File contains the literal string `Brittleness watch (WR-03 follow-up)`
    - File contains all 6 verbatim sample strings (`BOOKED [verdict=validated]:`, `BOOKED [verdict=validated_with_corrections]:`, `BOOKED [verdict=unvalidated]:`, `LEAD CAPTURED [verdict=validated]:`, `LEAD CAPTURED [verdict=validated_with_corrections]:`, `LEAD CAPTURED [verdict=unvalidated]:`)
    - File contains the literal string `Phase 61.1` somewhere in the Last updated header line
    - File does NOT contain the OLD bullet text `STATE+DIRECTIVE shape extended with verdict tokens` (replaced by the new prose)
  </acceptance_criteria>

  <done>
    voice-call-architecture/SKILL.md accurately documents both tool-return shapes with verbatim samples and an explicit brittleness watch-item.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the integrations-jobber-xero SKILL claim about tool-return shapes</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61-google-maps-address-validation-and-structured-address-storage/61-REVIEW.md WR-03 section (lines 121-148, especially the quote on line 124-127)
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md (FULL FILE — find any line stating tool returns are "STATE+DIRECTIVE-shaped and carry the verdict token verbatim")
  </read_first>

  <action>
Step 1 — Use grep to find the inaccurate claim:

```bash
grep -n "STATE+DIRECTIVE-shaped" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md"
grep -n "STATE+DIRECTIVE" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md"
grep -n "verdict token verbatim" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md"
```

Step 2 — For each line found that claims tool returns are STATE+DIRECTIVE-shaped: replace the inaccurate prose with the corrected language. The exact replacement depends on the surrounding context (prose may be in a paragraph or a bullet), but the corrected statement MUST satisfy ALL of the following:

(a) Mentions that there are TWO shapes — label form (`BOOKED [verdict=...]:` / `LEAD CAPTURED [verdict=...]:`) on the success path, STATE+DIRECTIVE on the failure path.
(b) Mentions that the verdict token (`verdict=validated`, `verdict=validated_with_corrections`, `verdict=unvalidated`) appears in BOTH shapes via substring matching by the prompt CRITICAL RULE.
(c) References the source of truth: `livekit-agent/src/tools/book_appointment.py` + `livekit-agent/src/tools/capture_lead.py`.

A suitable replacement template (adapt to surrounding context):

```
Tool-return shapes after Phase 61 fall into two patterns: the success path
uses a label form (`BOOKED [verdict=validated]: <directive>`,
`LEAD CAPTURED [verdict=validated_with_corrections]: <directive>`, etc.)
and the failure path uses the STATE+DIRECTIVE form
(`STATE:<reason> | DIRECTIVE:<action>`). Both carry the verdict token
verbatim; the prompt's `_build_address_validation_section` substring-matches
on `verdict=validated` / `verdict=validated_with_corrections` /
`verdict=unvalidated` and is shape-agnostic. Source of truth:
`livekit-agent/src/tools/book_appointment.py` and `livekit-agent/src/tools/capture_lead.py`.
```

Step 3 — If grep finds NO occurrences (perhaps the original phrasing was different or the WR-03 reviewer paraphrased), then:
- Add a new bullet under the existing Phase 61 cross-link section that documents the two-shape reality. Find the section heading `## Phase 61 cross-link` (or similar — the SUMMARY mentions this section was added in Plan 04). Append a new bullet at the end of that section with the same template above.

Step 4 — Bump the `**Last updated**` line for this file with `+ Phase 61.1 WR-03 — corrected tool-return shape claim`.
  </action>

  <verify>
    <automated>grep -q "BOOKED \[verdict=" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md" && grep -q "Phase 61.1" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/integrations-jobber-xero/SKILL.md` contains the literal string `BOOKED [verdict=` (verbatim sample of the label form)
    - File contains the literal string `STATE:<reason>` OR equivalent showing the failure-path STATE+DIRECTIVE shape
    - File contains the literal string `Phase 61.1` (Last updated note OR section reference)
    - File does NOT contain the original inaccurate claim wording (verify via grep that any "STATE+DIRECTIVE-shaped and carry the verdict token verbatim" string is gone, replaced by corrected two-shape language)
  </acceptance_criteria>

  <done>
    integrations-jobber-xero/SKILL.md no longer makes the inaccurate single-shape claim. The corrected two-shape reality is documented with verbatim samples and a source-of-truth pointer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit skill doc updates to homeservice_agent</name>
  <files>(commits only)</files>

  <read_first>
    (none — pure git operation following the prior tasks)
  </read_first>

  <action>
Step 1 — Verify both files are dirty:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git status --short .claude/skills/voice-call-architecture/SKILL.md .claude/skills/integrations-jobber-xero/SKILL.md
```
Both should show as modified (` M`).

Step 2 — Stage and commit:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git add .claude/skills/voice-call-architecture/SKILL.md .claude/skills/integrations-jobber-xero/SKILL.md && git commit -m "docs(61.1): WR-03 - document both tool-return shapes (label-form + STATE+DIRECTIVE)"
```

Step 3 — Verify commit landed:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1
```
First line should show the new commit subject.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1 | grep -q "WR-03" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - homeservice_agent repo has a new commit with subject containing `WR-03` and `tool-return shapes`
    - The commit modifies exactly 2 files: `.claude/skills/voice-call-architecture/SKILL.md` and `.claude/skills/integrations-jobber-xero/SKILL.md`
    - `git status` is clean for those two files post-commit
  </acceptance_criteria>

  <done>
    WR-03 closed via documentation update; one commit landed on homeservice_agent main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill doc → Claude code-loading session | Inaccurate doc misleads future agents about the actual code shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-09 | Information Disclosure (incorrect mental model) | skill docs | mitigate | Both edits replace inaccurate prose with verbatim sample strings + source-of-truth pointers; future readers can grep the actual code. |
| T-61.1-10 | Tampering (silent drift) | success-path tool returns | mitigate | The brittleness watch-item explicitly warns against future prompt revs that key on the `STATE:` prefix; codified in voice-call-architecture/SKILL.md. |
</threat_model>

<verification>
- Both skill files contain verbatim sample strings of both shapes.
- Both skill files contain a `Phase 61.1` reference.
- Original inaccurate claim wording is gone.
- One commit on homeservice_agent main.
</verification>

<success_criteria>
- WR-03 closed via doc update (per 61-REVIEW.md recommendation).
- The two-shape reality is documented in both relevant skills.
- Future readers searching for `BOOKED [verdict=` or `STATE:` get accurate hits in either skill.
</success_criteria>

<output>
After completion, create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-03-SUMMARY.md` documenting:
- The grep findings from Task 2 Step 1 (which line(s) contained the inaccurate claim).
- The exact replacement prose.
- The commit SHA on homeservice_agent.
- Any deviations.
</output>
</content>
</invoke>