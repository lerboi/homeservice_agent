---
phase: 61.1
plan: 03
subsystem: docs / skills
tags: [skill-sync, wr-03, tool-return-shapes, doc-drift]
dependency-graph:
  requires:
    - "61-REVIEW.md WR-03 finding (lines 121-148)"
    - "Phase 61 Plan 04 success-path return shapes shipped to book_appointment + capture_lead"
  provides:
    - "voice-call-architecture/SKILL.md accurate two-shape documentation"
    - "integrations-jobber-xero/SKILL.md corrected single-shape claim"
  affects:
    - "Future prompt revs that key on STATE: prefix — now have explicit brittleness watch"
key-files:
  created: []
  modified:
    - ".claude/skills/voice-call-architecture/SKILL.md"
    - ".claude/skills/integrations-jobber-xero/SKILL.md"
decisions:
  - "Per 61-REVIEW.md WR-03 recommendation: aligned docs to code, not code to docs. Existing label-form returns (BOOKED [verdict=...]:, LEAD CAPTURED [verdict=...]:) are functionally fine; the brittleness was purely documentation drift."
  - "Documented BOTH return shapes verbatim with 6 sample strings (BOOKED + LEAD CAPTURED × {validated, validated_with_corrections, unvalidated})."
  - "Added explicit Brittleness watch (WR-03 follow-up) note for future prompt revs that might assume STATE: prefix."
metrics:
  duration: "~6 min"
  commits: 1
  completed_date: "2026-05-05"
---

# Phase 61.1 Plan 03: WR-03 Doc-Drift Fix Summary

Closed the WR-03 advisory finding from `61-REVIEW.md` by updating both relevant SKILL docs to accurately describe the actual two-shape Phase 61 tool-return contract.

## What Was Built

### Task 1: voice-call-architecture/SKILL.md — Tool returns (D-E2) bullet expanded

Replaced the inaccurate "STATE+DIRECTIVE shape extended with verdict tokens" claim with a 26-line section documenting both shapes:

- **Success path (label-form):** `BOOKED [verdict=...]: <prose>` and `LEAD CAPTURED [verdict=...]: <prose>` with 6 verbatim sample strings (validated, validated_with_corrections, unvalidated × {BOOKED, LEAD CAPTURED}).
- **Failure path (STATE+DIRECTIVE):** `STATE:<reason> | DIRECTIVE:<what to say>` for slot-collision, validation-error, and capture-failure cases.

Added "Brittleness watch (WR-03 follow-up)" note flagging that future prompt revs keying on `STATE:` prefix would need to handle the success path's different shape.

Last updated line appended with Phase 61.1 note.

### Task 2: integrations-jobber-xero/SKILL.md — Inaccurate claim corrected

The doc previously claimed "tool returns are STATE+DIRECTIVE-shaped and carry the verdict token verbatim" (line 170 pre-fix) — applying only to the failure path. Replaced with two-shape language pointing to the source-of-truth files (`book_appointment.py:594-614`, `capture_lead.py:138-158`) for canonical shape.

Last updated line appended with Phase 61.1 note.

### Task 3: Single commit

Commit `4c60129` — `docs(61.1): WR-03 - document both tool-return shapes (label-form + STATE+DIRECTIVE)` — modifies exactly 2 files.

## Acceptance Criteria

All grep checks passed:

- voice-call-architecture/SKILL.md contains "Tool returns use TWO shapes" (1 occurrence)
- voice-call-architecture/SKILL.md contains "Brittleness watch (WR-03 follow-up)" (1 occurrence)
- All 6 verbatim sample strings present
- "Phase 61.1" present in Last updated line
- Old text "STATE+DIRECTIVE shape extended with verdict tokens" absent
- integrations-jobber-xero/SKILL.md contains `BOOKED [verdict=` literal (2 occurrences)
- integrations-jobber-xero/SKILL.md contains `STATE:<reason>` failure-path sample
- Commit `4c60129` modifies exactly the 2 SKILL.md files

## Deviations

None.

## Self-Check: PASSED
