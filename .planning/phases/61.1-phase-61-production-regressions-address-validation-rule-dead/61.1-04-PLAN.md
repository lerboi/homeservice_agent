---
phase: 61.1
plan: 04
type: execute
wave: 3
depends_on:
  - "61.1-01"
  - "61.1-02"
  - "61.1-03"
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
  - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md
  - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md
autonomous: true
gap_closure: true
requirements:
  - PHASE-61.1-SKILL-SYNC
  - PHASE-61.1-LESSON-LEARNED
tags:
  - documentation
  - skill-sync
  - memory
  - lesson-learned

must_haves:
  truths:
    - "voice-call-architecture/SKILL.md reflects the post-61.1 reality (rule scoped to post-tool, no silence escape, WR-01/02 fixes documented)."
    - "User memory captures the lesson: directive prompts with explicit silence licenses cause deadlocks under Gemini 3.1 Flash Live."
    - "MEMORY.md index references the new feedback file."
    - "Phase 61.1 has a final closeout reflecting all 4 plans shipped."
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md"
      provides: "Phase 61.1 follow-up subsection or amendments to the Phase 61 subsection."
      contains: "Phase 61.1"
    - path: "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md"
      provides: "New feedback memory documenting the deadlock pattern + remediation."
      contains: "Silence escape hatch"
    - path: "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md"
      provides: "Index entry pointing to the new feedback file."
      contains: "feedback_directive_prompt_silence_deadlock.md"
  key_links:
    - from: ".claude/skills/voice-call-architecture/SKILL.md Phase 61.1 follow-up"
      to: "test_prompt_address_validation_rule.py::test_both_locales_pre_tool_readback_explicit"
      via: "test invariant lock list"
      pattern: "test_both_locales_pre_tool_readback_explicit"
    - from: "MEMORY.md Feedback section"
      to: "feedback_directive_prompt_silence_deadlock.md"
      via: "index entry"
      pattern: "feedback_directive_prompt_silence_deadlock"
---

<objective>
Close out Phase 61.1 by:

1. **Skill sync** — Update `voice-call-architecture/SKILL.md` to reflect the post-61.1 reality of the `_build_address_validation_section` rule, the WR-01/WR-02 fixes in `google_maps.py`, and the test-invariant additions. (Plan 03 already touched this file for WR-03; Plan 04 adds the Plan-01/02 amendments.)

2. **Memory capture** — Write a new feedback memory that codifies the lesson: **directive prompts with explicit silence licenses cause deadlocks under Gemini 3.1 Flash Live**. This is a new pattern beyond the existing `feedback_livekit_prompt_philosophy.md` (which is the general non-directive philosophy); the new memory is specifically about the silence-as-escape-hatch trap and how to detect it pre-ship.

3. **MEMORY.md index** — Add the new feedback file to the index.

This plan depends on Plans 1+2+3 because (a) we want to document the final code state, and (b) we want the memory entry to reflect the resolution path, not a hypothetical fix.

Purpose: Future Claude sessions reading `voice-call-architecture/SKILL.md` get the accurate post-61.1 picture; future prompt-design work avoids the silence-deadlock anti-pattern.

Output: One homeservice_agent commit + one user-memory write (no commit — memory dir is outside the repo).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-INCIDENT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-01-PLAN.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-02-PLAN.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-03-PLAN.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md

<existing_memory_context>
The user's auto-memory MEMORY.md already includes:
- `feedback_livekit_prompt_philosophy.md` — the general "outcome-based, SDK-matched, non-directive" philosophy.

The new feedback memory created here is COMPLEMENTARY and more specific: it covers the particular failure mode (silence escape hatch + directive scope confusion) that Phase 61 P04 hit despite the philosophy memory existing. The new memory cross-links to the philosophy memory.
</existing_memory_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 61.1 follow-up subsection to voice-call-architecture/SKILL.md</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (FULL FILE — find the existing "## Phase 61 — Google Maps Address Validation Integration" section starting at line 1130; Plan 03 may have modified the "Tool returns (D-E2)" bullet inside it — verify the current state before adding new content)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-01-PLAN.md "must_haves" + "tasks/Task 1/action" for the prompt reframe details
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-02-PLAN.md "must_haves" + "tasks" for the WR-01/WR-02 fix details
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py lines 168-246 (POST-Plan-01 reframed prompt — confirm it landed before writing about it)
    - C:/Users/leheh/.Projects/livekit-agent/src/integrations/google_maps.py lines 270-300 (WR-02 short-circuit position) and lines 510-545 (WR-01 tenant_id guard)
  </read_first>

  <action>
Step 1 — Open `C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` and find the end of the existing `## Phase 61 — Google Maps Address Validation Integration` section. The section currently ends just before `## Related skills` (or wherever the next top-level `##` heading begins after the Phase 61 content; verify by reading).

Step 2 — Insert a NEW subsection IMMEDIATELY AFTER the existing Phase 61 section (and AFTER any Plan 03 WR-03 amendments inside it). Use this verbatim heading and content:

```markdown
## Phase 61.1 — Address-Validation Rule Deadlock Fix + WR-01/02/03 Closeout

Phase 61 shipped to production on 2026-05-03 (Plan 04 GREEN, sibling commit
`590669f`). The first production call after ship (call AJ_ZhhHTywMieAi,
2026-05-05 07:09 UTC, tenant "make it ai", caller +6587528516) deadlocked
the agent for 44 seconds after the caller spoke their postal code, ending
in CLIENT_INITIATED disconnect with `outcome=not_attempted`. Diagnosis:
the new `_build_address_validation_section` CRITICAL RULE plus its
explicit "Silence is always acceptable" escape hatch caused a
chicken-and-egg deadlock — the agent had to read back the address to get
acknowledgment before invoking the tool, but the rule flagged any
address-related utterance without a verdict as the worst possible failure,
and silence was the only explicitly-licensed safe fallback. Phase 61.1
fixed this regression and closed the three advisory warnings from
`61-REVIEW.md` (WR-01, WR-02, WR-03).

### What changed in Phase 61.1

- **Prompt reframe (Plan 01):** `_build_address_validation_section`
  EN+ES rewritten so the CRITICAL RULE governs ONLY post-tool speech.
  Pre-tool readback is now explicitly licensed via the substrings
  `After book_appointment or capture_lead returns` (EN) /
  `Después de que book_appointment o capture_lead retorne` (ES),
  paired with `read back what the caller said` (EN) /
  `repita lo que el llamante dijo` (ES). The "Silence or a neutral
  readback is always acceptable" line was REMOVED — the model was
  selecting it as the safe path and breaking the conversation. The
  "worst failure mode in this section" framing was qualified to apply
  only after a tool return. Anti-hallucination guarantee for post-tool
  speech (the original D-E3 invariant) is preserved.

- **Tool descriptions (Plan 01):** `book_appointment.py` description
  changed from `the caller has acknowledged the name+address readback`
  to `the caller has acknowledged the address you heard back from them
  — that pre-tool readback is the ordinary 'I heard you say X, is that
  right?' exchange, not a 'validated' claim`. `capture_lead.py`
  description changed symmetrically. Both stay under the 1024-char
  Pitfall A6 budget.

- **WR-01 fix (Plan 02):** `validate_address_bounded` now skips the
  `gmaps_validate_events` insert with an explicit warn log when
  `tenant_id` is falsy, instead of swallowing the NOT NULL constraint
  violation in a bare except. Restores D-C2' observability semantics
  for early/anonymous calls.

- **WR-02 fix (Plan 02):** `validate_address` short-circuits empty /
  whitespace-only `address_lines` to `verdict=error` BEFORE the HTTP
  call, preventing the 400 INVALID_ARGUMENT misclassification as
  `unsupported_region`. The bounded wrapper's Sentry-on-error gate
  (D-A3) now correctly fires for "we never captured an address" — was
  silently masked as a region-coverage problem.

- **WR-03 closeout (Plan 03):** `voice-call-architecture/SKILL.md` and
  `integrations-jobber-xero/SKILL.md` updated to accurately document
  both tool-return shapes (label form on success, STATE+DIRECTIVE on
  failure). Brittleness watch-item added (a future prompt rev keying
  on the `STATE:` prefix would silently drop the success paths).

### New / updated test invariants

- `tests/test_prompt_address_validation_rule.py::test_both_locales_pre_tool_readback_explicit`
  (replaces `test_both_locales_silence_acceptable`) locks all 8
  reframe substrings (4 must-be-present, 4 must-be-absent) at the
  test layer.
- `tests/test_google_maps.py::test_telemetry_skipped_when_tenant_id_none`
  + `test_telemetry_skipped_when_tenant_id_empty_string` lock WR-01.
- `tests/test_google_maps.py::test_empty_address_lines_short_circuits_to_error`
  + `test_whitespace_only_address_lines_short_circuits_to_error`
  + `test_empty_address_lines_triggers_sentry_via_wrapper` lock WR-02.

### Lesson learned

A directive CRITICAL RULE that explicitly licenses silence as a "safe
fallback" can deadlock the model under Gemini 3.1 Flash Live. The model
takes the safe path. The fix is two-pronged: (a) scope every CRITICAL
RULE explicitly to the conversational moment it governs (NOT the entire
turn, NOT the entire conversation), and (b) NEVER include "silence is
acceptable" as an escape hatch in a section that governs an
information-gathering loop — silence is the failure mode, not the
remedy. See user memory `feedback_directive_prompt_silence_deadlock.md`.
```

Step 3 — Bump the `**Last updated**` line at the top of the file to add `+ Phase 61.1 — address-validation rule deadlock fix; WR-01/02 google_maps.py defects closed`.

Step 4 — Verify Plan 03's WR-03 amendments are NOT clobbered by this edit. Re-grep:
```bash
grep -q "Tool returns use TWO shapes" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && echo "Plan 03 WR-03 still present"
grep -q "Phase 61.1 — Address-Validation Rule Deadlock Fix" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && echo "Plan 04 follow-up added"
```
Both must echo their messages.
  </action>

  <verify>
    <automated>grep -q "Phase 61.1 — Address-Validation Rule Deadlock Fix" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && grep -q "test_both_locales_pre_tool_readback_explicit" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && grep -q "Tool returns use TWO shapes" "C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` contains the literal heading `## Phase 61.1 — Address-Validation Rule Deadlock Fix + WR-01/02/03 Closeout`
    - File contains the literal string `After book_appointment or capture_lead returns`
    - File contains the literal string `test_both_locales_pre_tool_readback_explicit`
    - File contains the literal string `WR-01 fix (Plan 02)`
    - File contains the literal string `WR-02 fix (Plan 02)`
    - File contains the literal string `WR-03 closeout (Plan 03)`
    - File still contains Plan 03's `Tool returns use TWO shapes` (not clobbered)
    - File `**Last updated**` line contains `Phase 61.1`
  </acceptance_criteria>

  <done>
    Phase 61.1 follow-up subsection added without clobbering Plan 03's WR-03 amendments. Test invariant references documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the new feedback memory + add to MEMORY.md index</name>
  <files>C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md, C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md</files>

  <read_first>
    - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md (FULL FILE — confirm the existing Feedback section structure so the new entry slots in cleanly)
    - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_livekit_prompt_philosophy.md (FULL FILE — the general philosophy memory the new one cross-links to; confirm style/length)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-INCIDENT.md (the incident facts to summarize in the lesson)
  </read_first>

  <action>
Step 1 — Create the new feedback file at `C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md` with this verbatim content:

```markdown
# Feedback: Directive Prompt + Silence License Causes Deadlock

**When designing prompt CRITICAL RULE sections for LiveKit + Gemini 3.1
Flash Live (and likely other realtime models), do NOT include "silence
is always acceptable" as an escape hatch in a section that governs an
information-gathering loop. The model selects silence as the safe path
and the conversation deadlocks.**

## The trap (Phase 61 production regression, 2026-05-05)

Phase 61 Plan 04 shipped a `_build_address_validation_section` CRITICAL
RULE that:

1. Prohibited 6 verbatim phrases unless the immediately-preceding tool
   return contained `verdict=validated` or `verdict=validated_with_corrections`.
2. Framed violation as "the worst failure mode in this section."
3. Explicitly licensed silence: *"Silence or a neutral readback is
   always acceptable; false confirmation is not."*

Meanwhile the `book_appointment` tool description told the model to
"call this after the caller has acknowledged the name+address readback."

Result: chicken-and-egg deadlock during info-gathering.
- Agent must read back the address before invoking the tool.
- Rule flags any address-related utterance without a verdict as the
  worst failure.
- Validation only happens INSIDE the tool — there is no verdict yet.
- Silence is the only explicitly-licensed safe fallback.

The model takes the silence path. No tool invocation. No utterance.
First production call: 44 seconds of silence after caller's postal
code, CLIENT_INITIATED disconnect, `outcome=not_attempted`.

## The remediation (Phase 61.1)

1. **Scope the rule explicitly to the moment it governs.** Phase 61.1
   reframed the section to begin with "After book_appointment or
   capture_lead returns, this rule governs how you speak back what
   the tool found." Pre-tool readback is now explicitly licensed as
   the ordinary "I heard you say X, is that right?" exchange — NOT
   subject to the verdict prohibition.
2. **Remove the silence escape hatch.** The line was deleted entirely
   from both EN and ES. Silence in an info-gathering loop is the
   failure mode, not the remedy.
3. **Soften "worst failure mode" framing.** Now reads "after a tool
   return without the verdict to back them up is a serious failure"
   — qualified to the post-tool moment.
4. **Tool description disambiguation.** Both `book_appointment` and
   `capture_lead` descriptions now say the pre-tool readback is the
   ordinary acknowledgment exchange, not a "validated" claim.

## Pre-ship detection heuristics

When reviewing any prompt CRITICAL RULE block before ship, ask:

1. **Does this rule have an explicit silence license?** If yes — does
   the surrounding conversational moment require speech to make
   forward progress (e.g. info-gathering, readback, acknowledgment)?
   If yes — the silence license will deadlock. Remove it.
2. **What conversational moment does this rule govern?** If the
   answer is "the entire turn" or "the entire conversation," the
   scope is too broad. Re-scope to a specific moment (e.g. "after
   tool X returns," "before tool Y is invoked," "during the
   greeting"). The model needs to know WHEN to apply the rule.
3. **Does the rule create a chicken-and-egg with another section
   or a tool description?** Trace every other section AND every
   tool description for state preconditions on the same surface.
   In Phase 61, the verdict prohibition (rule) and the readback
   acknowledgment (tool description) deadlocked at the seam.
4. **Test the rule with a placed UAT call BEFORE shipping** — pure
   prompt-test invariants are necessary but NOT sufficient. The
   Phase 61 Plan 04 had 14 GREEN tests and still deadlocked
   production. The deadlock was a behavioral interaction not
   captured by any single-section invariant.

## Cross-references

- See `feedback_livekit_prompt_philosophy.md` — general philosophy
  (outcome-based, SDK-matched, non-directive).
- See `feedback_deep_verify_before_fix.md` — the trace-every-writer/
  reader rule that catches cross-section deadlocks before ship.
- See `reference_livekit_generate_reply_gemini31.md` — orthogonal
  silence-trap (SDK bug, not prompt-induced).
- Source: `.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-INCIDENT.md`.
```

Step 2 — Edit `C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md`. Find the Feedback section (currently has 4 entries: feedback_proxy_not_middleware, feedback_deep_verify_before_fix, feedback_livekit_prompt_philosophy, feedback_gsd_worktree_stale_base_windows). Add a new line at the end of the Feedback section (preserving alphabetical/insertion order — append, since the existing list is insertion-ordered):

```
- [feedback_directive_prompt_silence_deadlock.md](feedback_directive_prompt_silence_deadlock.md) — Directive prompt + silence license = deadlock under Gemini 3.1 Flash Live; scope rules to the moment, never license silence in info-gathering loops
```

Step 3 — Verify both files:
```bash
ls -la "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md"
grep -q "feedback_directive_prompt_silence_deadlock" "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md" && echo "MEMORY index updated"
```

Note: the user memory directory is OUTSIDE the homeservice_agent repo. Do NOT attempt to git commit these files — they are not version-controlled in homeservice_agent.
  </action>

  <verify>
    <automated>test -f "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md" && grep -q "feedback_directive_prompt_silence_deadlock" "C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md` exists
    - That file contains the literal string `Silence or a neutral readback is always acceptable`
    - That file contains the literal string `Pre-ship detection heuristics`
    - That file contains a cross-reference to `feedback_livekit_prompt_philosophy.md`
    - File `C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/MEMORY.md` contains the literal string `feedback_directive_prompt_silence_deadlock.md`
    - The MEMORY.md index entry is in the Feedback section (verify by reading line context)
  </acceptance_criteria>

  <done>
    New feedback memory captures the deadlock pattern + remediation + pre-ship detection heuristics. MEMORY.md index references it. Future Claude sessions loading user memory will see the lesson.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit homeservice_agent skill update</name>
  <files>(commits only — homeservice_agent repo)</files>

  <read_first>
    (none — pure git operation)
  </read_first>

  <action>
Step 1 — Verify the skill file is dirty:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git status --short .claude/skills/voice-call-architecture/SKILL.md
```
Should show ` M` for the file.

Step 2 — Stage and commit:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git add .claude/skills/voice-call-architecture/SKILL.md && git commit -m "docs(61.1): close Phase 61.1 - skill sync for prompt reframe + WR-01/02/03"
```

Step 3 — Verify commit:
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1
```

Note: the memory files (Task 2) are NOT in homeservice_agent and are NOT committed here. They are part of the user's persistent auto-memory.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1 | grep -q "61.1" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - homeservice_agent repo has a new commit with subject containing `61.1` and `skill sync`
    - The commit modifies exactly 1 file: `.claude/skills/voice-call-architecture/SKILL.md`
    - `git status` is clean for that file post-commit
  </acceptance_criteria>

  <done>
    Phase 61.1 fully closed at the documentation layer. Skill reflects post-fix reality; user memory captures the lesson.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill doc → Claude code-loading session | Stale doc misleads future agents about post-61.1 reality |
| user memory → Claude session bootstrapping | Captured lessons inform future prompt-design work across all phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-11 | Information Disclosure (stale mental model) | voice-call-architecture/SKILL.md | mitigate | Phase 61.1 follow-up subsection captures every fix from Plans 01/02/03 with explicit cross-references to test files. |
| T-61.1-12 | Repeated mistake (no learning) | future prompt design | mitigate | New feedback memory codifies the deadlock pattern + 4 pre-ship detection heuristics; cross-linked from MEMORY.md index. |
</threat_model>

<verification>
- voice-call-architecture/SKILL.md contains the new Phase 61.1 follow-up subsection.
- Plan 03's WR-03 amendments are still present (not clobbered).
- New feedback memory file exists at the expected path.
- MEMORY.md index references the new file.
- One commit lands on homeservice_agent main.
</verification>

<success_criteria>
- All 3 closeout artifacts shipped: SKILL update, feedback memory, MEMORY index.
- One homeservice_agent commit.
- No livekit-agent code touched (deps on Plans 01+02+03 already settled).
</success_criteria>

<output>
After completion, create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.1-phase-61-production-regressions-address-validation-rule-dead/61.1-04-SUMMARY.md` documenting:
- The skill commit SHA on homeservice_agent.
- The path of the new feedback memory file.
- The MEMORY.md line where the new index entry was inserted.
- A final phase-closeout summary table linking each plan to its commit(s) and outcome.
- Any deviations.
</output>
</content>
</invoke>