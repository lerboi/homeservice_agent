---
phase: 19-codebase-skill-files
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - .claude/skills/auth-database-multitenancy/SKILL.md
  - .claude/skills/public-site-i18n/SKILL.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Reading the auth-database-multitenancy skill gives enough context to modify RLS policies, add migrations, or change auth guards without reading every source file"
    - "Reading the public-site-i18n skill gives enough context to modify any public page, landing section, or i18n configuration without reading every source file"
    - "CLAUDE.md lists all 6 skill names explicitly with maintenance obligation"
    - "All 6 skills together cover every major system in the codebase"
  artifacts:
    - path: ".claude/skills/auth-database-multitenancy/SKILL.md"
      provides: "Complete architectural reference for auth, database, and multi-tenancy"
      contains: "name: auth-database-multitenancy"
    - path: ".claude/skills/public-site-i18n/SKILL.md"
      provides: "Complete architectural reference for public site and i18n"
      contains: "name: public-site-i18n"
    - path: "CLAUDE.md"
      provides: "Updated skill maintenance directive covering all 6 skills"
      contains: "scheduling-calendar-system"
  key_links:
    - from: ".claude/skills/auth-database-multitenancy/SKILL.md"
      to: "src/lib/get-tenant-id.js"
      via: "File Map entry"
      pattern: "get-tenant-id"
    - from: ".claude/skills/auth-database-multitenancy/SKILL.md"
      to: "src/middleware.js"
      via: "File Map entry"
      pattern: "middleware"
    - from: ".claude/skills/public-site-i18n/SKILL.md"
      to: "src/i18n/routing.js"
      via: "File Map entry"
      pattern: "routing\\.js"
    - from: "CLAUDE.md"
      to: ".claude/skills/"
      via: "Skill maintenance directive"
      pattern: "scheduling-calendar-system.*dashboard-crm-system.*onboarding-flow.*auth-database-multitenancy.*public-site-i18n"
---

<objective>
Create two skill files (`auth-database-multitenancy` and `public-site-i18n`) and update CLAUDE.md to enforce skill file maintenance across all 6 skills.

Purpose: Complete the skill file coverage for the entire codebase and ensure the maintenance directive is explicit about all skills.
Output: Two SKILL.md files (~500 lines each) + updated CLAUDE.md
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-codebase-skill-files/19-RESEARCH.md
@.planning/phases/19-codebase-skill-files/19-01-SUMMARY.md
@.planning/phases/19-codebase-skill-files/19-02-SUMMARY.md

<interfaces>
<!-- Template to follow exactly -->
From .claude/skills/voice-call-architecture/SKILL.md:
- YAML frontmatter: name + description (description encodes WHAT + WHEN to trigger)
- Sections: Architecture Overview (table + ASCII diagram), File Map (table), numbered component deep dives, Database Tables, Environment Variables, Key Design Decisions, Maintenance reminder
- ~527 lines, progressive disclosure

<!-- CLAUDE.md current state (to be updated, not replaced) -->
From CLAUDE.md:
```markdown
# Project Instructions

## Skill-Driven Architecture Changes

When asked to read a skill file and make changes to a specific architecture or system:
1. Read the relevant skill file first to understand the full system
2. Make the requested code changes
3. **After changes are made, update the skill file** to reflect the new version of the code — keep it accurate and in sync with the actual codebase

This applies to all architecture skill files (e.g., `voice-call-architecture`). The skill files are living documents that must always reflect the current state of the code.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth-database-multitenancy SKILL.md</name>
  <files>.claude/skills/auth-database-multitenancy/SKILL.md</files>
  <read_first>
    .claude/skills/voice-call-architecture/SKILL.md
    src/middleware.js
    src/lib/supabase.js
    src/lib/supabase-server.js
    src/lib/supabase-browser.js
    src/lib/get-tenant-id.js
    supabase/migrations/001_initial_schema.sql
    supabase/migrations/002_onboarding_triage.sql
    supabase/migrations/003_scheduling.sql
    supabase/migrations/004_leads_crm.sql
    supabase/migrations/005_setup_checklist.sql
    supabase/migrations/006_escalation_contacts.sql
    supabase/migrations/007_outlook_calendar.sql
    supabase/migrations/008_call_outcomes.sql
    .planning/STATE.md
  </read_first>
  <action>
Create `.claude/skills/auth-database-multitenancy/SKILL.md` following voice-call-architecture structure.

**YAML frontmatter:**
```yaml
---
name: auth-database-multitenancy
description: "Complete architectural reference for authentication, database schema, and multi-tenant isolation — Supabase Auth, middleware auth guards, three Supabase client types, RLS policies, all 8 migrations with table definitions, getTenantId pattern, and tenant data isolation. Use this skill whenever making changes to auth middleware, RLS policies, database migrations, Supabase client usage, tenant isolation, or adding new tables. Also use when the user asks about how auth works, wants to add a new migration, or needs to debug RLS or tenant access issues."
---
```

**Required sections (in order):**

1. **Architecture Overview** — Table of auth layers (middleware, three Supabase clients, RLS, getTenantId) + ASCII diagram: request -> middleware (cookie-based auth check) -> if authenticated, route renders -> server component uses createSupabaseServer() -> API routes use getTenantId() -> RLS enforces tenant isolation at DB level. Also show webhook path: Retell webhook -> supabase service role client (bypasses RLS).

2. **File Map** — Table with EVERY source file from read_first (except SKILL.md/STATE.md).

3. **Three Supabase Clients** deep dive:
   - `src/lib/supabase.js`: Service role client — bypasses RLS, used by webhook handlers for cross-tenant writes. Extract the exact createClient() call and env vars used.
   - `src/lib/supabase-server.js`: `createSupabaseServer()` — SSR cookie-based client for server components and middleware. Extract the factory function signature.
   - `src/lib/supabase-browser.js`: Browser client with anon key — for client components. Extract the createBrowserClient() call.

4. **getTenantId Pattern** deep dive — File path, exact function signature, resolution flow: getUser() -> user.id (owner_id) -> query tenants WHERE owner_id -> return tenant.id. Note: does NOT use user_metadata. Show the exact query pattern.

5. **Middleware** deep dive — File path, AUTH_REQUIRED_PATHS array (exact paths), matcher config, auth check flow (uses anon key not service_role), onboarding_complete redirect logic (only on /onboarding paths). Note: middleware.js is shared with onboarding-flow skill — onboarding details are there, auth guard details are here.

6. **RLS Policy Patterns** — Two patterns with exact SQL examples:
   - Direct owner: `owner_id = auth.uid()` (used on tenants table)
   - Tenant child: `tenant_id IN (SELECT id FROM tenants WHERE owner_id = auth.uid())` (used on all child tables: calls, appointments, services, leads, etc.)
   - Service role bypass: `role() = 'service_role'` policy on every table
   - WITH CHECK on insert/update: requires tenant_id (why PATCH reorder must include tenant_id)

7. **Migration Trail** deep dive — For EACH of the 8 migrations, document:
   - 001_initial_schema.sql: tenants table (columns: id, owner_id, business_name, default_locale, phone_number, onboarding_complete, etc.), calls table, RLS policies, service_role_bypass
   - 002_onboarding_triage.sql: services table, working_hours jsonb, triage columns on calls
   - 003_scheduling.sql: appointments, service_zones, zone_travel_buffers, calendar_credentials, calendar_events, book_appointment_atomic RPC function
   - 004_leads_crm.sql: leads, lead_calls, activity_log, REPLICA IDENTITY FULL, supabase_realtime publication
   - 005_setup_checklist.sql: setup_checklist_dismissed column on tenants
   - 006_escalation_contacts.sql: escalation_contacts table, services.sort_order column
   - 007_outlook_calendar.sql: calendar_credentials.is_primary, appointments.external_event_id/external_event_provider
   - 008_call_outcomes.sql: calls.booking_outcome, exception_reason, notification_priority columns

   For each: list tables created/altered, key columns, constraints, RLS policies added.

8. **Complete Table Reference** — Summary table of ALL tables across all migrations with: table name, migration #, purpose, key columns, RLS pattern used.

9. **Environment Variables** — NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY. Which client uses which.

10. **Key Design Decisions** — Mine from STATE.md. Must include:
    - Three clients with distinct purposes (and WHY each exists)
    - getTenantId does NOT use user_metadata
    - RLS two-pattern design (owner vs tenant child)
    - Service role bypass for webhooks
    - REPLICA IDENTITY FULL for Realtime
    - Migration ordering matters (FK dependencies)
    - Middleware uses anon key for cookie-based auth (not service_role)
    - Multi-tenant isolation: ALL data tables carry tenant_id FK

11. **Maintenance reminder** — "When adding new migrations or modifying RLS policies, update the Migration Trail and RLS Policy Patterns sections..."

**Critical rules:**
- Read each migration file BEFORE writing its section — extract actual CREATE TABLE statements, column types, constraint names
- Read each Supabase client file and extract exact createClient() calls
- Target ~500 lines
- Cross-domain: "see onboarding-flow skill" for middleware onboarding redirect details, "see scheduling-calendar-system skill" for booking RPC details, "see dashboard-crm-system skill" for Realtime setup
  </action>
  <verify>
    <automated>test -f ".claude/skills/auth-database-multitenancy/SKILL.md" && head -3 ".claude/skills/auth-database-multitenancy/SKILL.md" | grep -q "name: auth-database-multitenancy" && wc -l ".claude/skills/auth-database-multitenancy/SKILL.md" | awk '{if ($1 >= 300) print "OK: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/auth-database-multitenancy/SKILL.md exists
    - Line 2 contains `name: auth-database-multitenancy`
    - Line 3 contains `description:` with trigger conditions
    - File contains `## Architecture Overview`
    - File contains `## File Map`
    - File contains `getTenantId`
    - File contains `createSupabaseServer`
    - File contains `service_role`
    - File contains `auth.uid()`
    - File contains `001_initial_schema`
    - File contains `008_call_outcomes`
    - File contains `REPLICA IDENTITY FULL`
    - File contains `## Key Design Decisions`
    - File contains `Keeping This Document Updated`
    - File is at least 300 lines
  </acceptance_criteria>
  <done>auth-database-multitenancy SKILL.md exists with all 11 sections, covers all 8 migrations and 5 auth/client files, contains actual SQL patterns and function signatures, and is 300+ lines</done>
</task>

<task type="auto">
  <name>Task 2: Create public-site-i18n SKILL.md and update CLAUDE.md</name>
  <files>.claude/skills/public-site-i18n/SKILL.md, CLAUDE.md</files>
  <read_first>
    .claude/skills/voice-call-architecture/SKILL.md
    CLAUDE.md
    src/app/(public)/layout.js
    src/app/(public)/page.js
    src/app/(public)/pricing/page.js
    src/app/(public)/pricing/pricingData.js
    src/app/(public)/pricing/PricingTiers.jsx
    src/app/(public)/pricing/ComparisonTable.jsx
    src/app/(public)/pricing/FAQSection.jsx
    src/app/(public)/about/page.js
    src/app/(public)/contact/page.js
    src/app/(public)/contact/ContactForm.jsx
    src/app/api/contact/route.js
    src/app/components/landing/LandingNav.jsx
    src/app/components/landing/LandingFooter.jsx
    src/app/components/landing/HeroSection.jsx
    src/app/components/landing/FeaturesGrid.jsx
    src/app/components/landing/SocialProofSection.jsx
    src/app/components/landing/HowItWorksSection.jsx
    src/app/components/landing/HowItWorksTabs.jsx
    src/app/components/landing/HowItWorksSticky.jsx
    src/app/components/landing/FinalCTASection.jsx
    src/app/components/landing/AnimatedSection.jsx
    src/components/landing/AuthAwareCTA.js
    src/emails/NewLeadEmail.jsx
    src/i18n/routing.js
    messages/en.json
    messages/es.json
    .planning/STATE.md
  </read_first>
  <action>
**Part A: Create `.claude/skills/public-site-i18n/SKILL.md`**

Follow voice-call-architecture structure.

**YAML frontmatter:**
```yaml
---
name: public-site-i18n
description: "Complete architectural reference for the public marketing site and internationalization — landing page sections, pricing page, about page, contact form with Resend email, navigation, footer, animation system, AuthAwareCTA, next-intl configuration, and translation files. Use this skill whenever making changes to public-facing pages, landing sections, pricing tiers, contact form, navigation, footer, animations, i18n configuration, or translation files. Also use when the user asks about how the marketing site works, wants to modify page design, or needs to debug i18n or animation issues."
---
```

**Required sections (in order):**

1. **Architecture Overview** — Table of layers (route group, pages, landing sections, shared components, i18n, email) + ASCII diagram: (public) route group -> layout.js (LandingNav + LandingFooter) -> page routes (/, /pricing, /about, /contact). Show how AuthAwareCTA routes authenticated vs unauthenticated users.

2. **File Map** — Table with EVERY source file from read_first (except SKILL.md/STATE.md/CLAUDE.md).

3. **Route Group and Layout** deep dive — (public) route group pattern, layout.js wrapping all public pages with LandingNav + LandingFooter (NOT in page components).

4. **Landing Page Sections** deep dive — For each section component: HeroSection (Spline TODO, design tokens), FeaturesGrid (bento grid), SocialProofSection, HowItWorksSection (Server Component + dynamic import of HowItWorksTabs for bundle splitting, mobile fallback with inline mobileSteps), HowItWorksTabs (roving tabindex WAI-ARIA Tabs, AnimatePresence mode=wait), HowItWorksSticky (sticky scroll variant), FinalCTASection (CSS-only prefers-reduced-motion, Server Component).

5. **Animation System** deep dive — AnimatedSection.jsx: direction prop (up/left/right), stagger/item variants, prefers-reduced-motion with initial={false} (Framer Motion v12 pattern). How landing sections use it.

6. **Navigation and Footer** — LandingNav: isRoot pattern for anchor links (sub-pages prefix / to navigate back-and-scroll), backdrop-blur-[12px]. LandingFooter: use client for back-to-top scrollTo, newsletter form display-only (no API wired).

7. **AuthAwareCTA** — src/components/landing/AuthAwareCTA.js: authenticated -> /dashboard, unauthenticated -> /onboarding.

8. **Pricing Page** deep dive — page.js, pricingData.js (tier data structure: 4 tiers, monthly/annual), PricingTiers.jsx (tier cards, "Most Popular" badge on Growth), ComparisonTable.jsx (feature comparison), FAQSection.jsx (Radix accordion, height: var(--radix-accordion-content-height) for smooth transition).

9. **About and Contact Pages** — about/page.js (mission, founding story). contact/page.js + ContactForm.jsx (named export, honeypot field returns 200 silently, validation). Contact API route: Resend instantiated per-request (serverless-safe), honeypot check, email dispatch.

10. **Email Templates** — NewLeadEmail.jsx: React Email template for owner notifications.

11. **Internationalization (i18n)** deep dive — routing.js: locales ['en', 'es'], defaultLocale 'en'. Cookie-based locale without URL prefix routing (API-first multi-tenant constraint). next-intl for client-side translation. Agent prompt uses direct JSON import (runs outside Next.js context). messages/en.json and messages/es.json structure (agent section for voice, UI section for frontend). Language barrier detection uses locales from routing.js as single source of truth.

12. **Design Tokens (Landing)** — Landing-specific tokens in CSS/Tailwind (NOT design-tokens.js which is dashboard): hero/footer #050505, accent #F97316, light #F5F5F4, muted #475569. Tailwind v4: @import 'tailwindcss' + @tailwindcss/postcss (no tailwind.config.js).

13. **Key Design Decisions** — Mine from STATE.md Phase 02.1, 06, 11, 13. Must include:
    - (public) route group for layout injection
    - isRoot pattern for anchor links
    - Cookie-based locale without URL prefix
    - next-intl for client, direct JSON import for agent
    - Language barrier uses routing.js locales as source of truth
    - ContactForm named export, honeypot 200, Resend per-request
    - AnimatedSection initial={false} for prefers-reduced-motion
    - HowItWorksSection Server Component + dynamic import
    - FinalCTASection CSS-only motion guard
    - Tailwind v4 uses @import not config file
    - Auth page three conditional branches (signup/signin/OTP)

14. **Maintenance reminder**

**Part B: Update CLAUDE.md**

Replace the entire content of CLAUDE.md with:

```markdown
# Project Instructions

## Skill-Driven Architecture Changes

When asked to read a skill file and make changes to a specific architecture or system:
1. Read the relevant skill file first to understand the full system
2. Make the requested code changes
3. **After changes are made, update the skill file** to reflect the new version of the code — keep it accurate and in sync with the actual codebase

**Architecture skill files (all must be kept in sync):**
- `voice-call-architecture` — WebSocket LLM server, Retell webhooks, call-processor, triage, whisper messages
- `scheduling-calendar-system` — Slot calculator, booking, Google + Outlook OAuth/sync/webhooks, cron jobs
- `dashboard-crm-system` — Dashboard pages, lead lifecycle, Kanban, analytics, escalation chain, settings, design tokens
- `onboarding-flow` — 7-step wizard, onboarding API routes, phone provisioning, SMS verify, test call
- `auth-database-multitenancy` — Supabase Auth, middleware, RLS, all migrations, getTenantId
- `public-site-i18n` — Landing, pricing, about, contact, Resend email, next-intl (en/es)
```

This replaces the vague "This applies to all architecture skill files (e.g., `voice-call-architecture`)" with an explicit list. The 3-step process and header remain identical.
  </action>
  <verify>
    <automated>test -f ".claude/skills/public-site-i18n/SKILL.md" && head -3 ".claude/skills/public-site-i18n/SKILL.md" | grep -q "name: public-site-i18n" && grep -q "scheduling-calendar-system" CLAUDE.md && grep -q "dashboard-crm-system" CLAUDE.md && grep -q "auth-database-multitenancy" CLAUDE.md && grep -q "public-site-i18n" CLAUDE.md && echo "ALL OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/public-site-i18n/SKILL.md exists
    - Line 2 contains `name: public-site-i18n`
    - Line 3 contains `description:` with trigger conditions
    - File contains `## Architecture Overview`
    - File contains `## File Map`
    - File contains `AnimatedSection`
    - File contains `isRoot`
    - File contains `AuthAwareCTA`
    - File contains `next-intl`
    - File contains `routing.js`
    - File contains `honeypot`
    - File contains `## Key Design Decisions`
    - File contains `Keeping This Document Updated`
    - File is at least 300 lines
    - CLAUDE.md contains `voice-call-architecture`
    - CLAUDE.md contains `scheduling-calendar-system`
    - CLAUDE.md contains `dashboard-crm-system`
    - CLAUDE.md contains `onboarding-flow`
    - CLAUDE.md contains `auth-database-multitenancy`
    - CLAUDE.md contains `public-site-i18n`
    - CLAUDE.md contains `Architecture skill files (all must be kept in sync)`
    - CLAUDE.md does NOT contain `(e.g., \`voice-call-architecture\`)` (old vague text removed)
  </acceptance_criteria>
  <done>public-site-i18n SKILL.md exists with all 14 sections covering 25+ source files, CLAUDE.md updated with explicit 6-skill maintenance list, old vague directive replaced</done>
</task>

</tasks>

<verification>
- Both skill files exist at their respective paths
- YAML frontmatter correct on both
- CLAUDE.md lists all 6 skills explicitly
- All 6 skills together (existing voice-call-architecture + 5 new) cover every major system
- voice-call-architecture/SKILL.md is unchanged
- No source code files were modified (documentation-only phase)
</verification>

<success_criteria>
- auth-database-multitenancy skill created with 300+ lines covering all 8 migrations and auth patterns
- public-site-i18n skill created with 300+ lines covering all public pages and i18n
- CLAUDE.md explicitly lists all 6 skill file names with maintenance obligation
- All 6 skills together provide complete codebase coverage
</success_criteria>

<output>
After completion, create `.planning/phases/19-codebase-skill-files/19-03-SUMMARY.md`
</output>
