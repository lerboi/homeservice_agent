---
phase: 64
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified: []  # No code files; deploy-only plan. Branch pushed to origin.
autonomous: false
requirements: []
covers_decisions: [D-01, D-08, D-11]
user_setup:
  - service: Railway
    why: "Preview deploy of phase-64-pipeline-migration branch required for live UAT (Plan 05)"
    dashboard_config:
      - task: "Confirm the Railway service has a preview-deploy hook bound to the livekit-agent repo. Phase 63 / 63.1 already exercised this pattern — if preview is auto-triggered on branch push to origin, Claude's role is to verify the deploy SUCCESS signal. If preview is manual, user triggers it from Railway dashboard."
        location: "Railway dashboard → livekit-agent service → Deployments"

must_haves:
  truths:
    - "Branch phase-64-pipeline-migration exists on origin (GitHub)"
    - "Railway preview deploy for that branch returns SUCCESS status"
    - "Railway preview logs show the worker registered (log line: 'registered worker' or equivalent)"
    - "Railway preview env has GOOGLE_APPLICATION_CREDENTIALS scoped for Cloud Speech v2 (pre-UAT verification)"
    - "No boot-time Python ImportError / ModuleNotFoundError (silero/onnxruntime dependency check — Pitfall 7)"
    - "No boot-time TypeError on plugin construction"
  artifacts:
    - path: "origin/phase-64-pipeline-migration"
      provides: "Remote branch ready for Railway preview consumption"
    - path: "Railway preview deployment"
      provides: "Running livekit-agent instance on preview SIP URL (or preview Railway URL) for Plan 05 UAT"
  key_links:
    - from: "local phase-64-pipeline-migration branch"
      to: "origin/phase-64-pipeline-migration"
      via: "git push -u origin phase-64-pipeline-migration"
      pattern: "remote: phase-64"
    - from: "origin/phase-64-pipeline-migration HEAD"
      to: "Railway preview deploy"
      via: "auto-trigger or manual trigger on Railway"
      pattern: "SUCCESS"
---

<objective>
Push the feature branch to GitHub and drive the Railway preview deployment to a SUCCESS state with a registered worker. Verify that the boot sequence completes without ImportError (Silero onnxruntime dependency — Pitfall 7) or TypeError (plugin constructor validation — Pitfall 3). This plan is NOT a merge plan — main stays untouched. Plan 05 (UAT) runs against the preview URL; Plan 06 (phase close) does the `--no-ff` merge.

Purpose: Preview-deploy gate before live SIP UAT. The big-bang D-01 rollout argues for pre-merge validation on a real Railway environment that mirrors production config. Boot-success is a weak signal (green boot + first-call TypeError is the Pitfall 3 failure mode); the actual validation is Plan 05's live call. This plan surfaces the boot-time signal only.

Output: Branch on GitHub + Railway preview green + boot log verification committed to `.planning/phases/64-livekit-pipeline-agent-migration/64-04-SUMMARY.md` with the preview URL for Plan 05 consumption.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-livekit-pipeline-agent-migration/64-CONTEXT.md
@.planning/phases/64-livekit-pipeline-agent-migration/64-RESEARCH.md
@.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-04-PLAN.md
# ^ The 63.1-04 plan used the same Railway preview gate pattern; reuse the post-push
# verification approach. If that file does not exist, fall back to the 63.1-SUMMARY.md
# Railway-deploy notes.
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push branch to origin and verify pre-flight test suite</name>
  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/.github/workflows/ (any CI hooks that run on push — confirm we're not tripping pre-commit hooks we've been bypassing)
    - .planning/phases/64-livekit-pipeline-agent-migration/64-02-PLAN.md (branch name confirmed)
    - .planning/phases/64-livekit-pipeline-agent-migration/64-03-PLAN.md (commit history expected on branch)
  </read_first>
  <files>none (commands only)</files>
  <action>
Working directory: `C:/Users/leheh/.Projects/livekit-agent`.

**Step 1: Confirm branch state.**
```bash
cd /c/Users/leheh/.Projects/livekit-agent
git status
git branch --show-current   # must return: phase-64-pipeline-migration
git log --oneline main..phase-64-pipeline-migration   # lists Plan 01-03 commits
```

Expected commit log (on top of `main`):
1. `test(64): wave 0 RED — session invariants + D-03c/D-03d prompt contracts` (Plan 01)
2. `feat(64): add _build_pipeline_plugins helper + silero import` (Plan 02 Task 1)
3. `feat(64): swap RealtimeModel → STT+LLM+TTS+VAD pipeline session` (Plan 02 Task 2)
4. `feat(64): re-frame _build_greeting_section per D-03c (pipeline)` (Plan 03 Task 1)
5. `feat(64): compress NO DOUBLE-BOOKING to one-liner per D-03d (pipeline)` (Plan 03 Task 2)

5 commits minimum. If Plans 01/02/03 split into more commits (e.g., Task 1 of Plan 01 committed separately from Tasks 2/3), that's fine — enforcement is on "branch has the full Phase 64 changeset," not on exact commit count.

**Step 2: Run full test suite locally as a last pre-push gate.**
```bash
cd /c/Users/leheh/.Projects/livekit-agent && pytest tests/ 2>&1 | tail -10
```

Expected: `<N>+ passed, 1 failed` where the 1 failed is the pre-existing deferred VIP test (`tests/webhook/test_routes.py::test_incoming_call_vip_lead`) documented in STATE.md. If any other test fails, STOP — resolve before pushing.

**Step 3: Push branch.**
```bash
git push --no-verify -u origin phase-64-pipeline-migration
```

`--no-verify` per D-08 convention. `-u` sets upstream so future pushes on this branch don't need to respecify.

**Step 4: Confirm push.**
```bash
git ls-remote origin phase-64-pipeline-migration
# Must return a SHA — non-empty output = branch exists on origin
```
  </action>
  <verify>
    <automated>cd /c/Users/leheh/.Projects/livekit-agent && git ls-remote origin phase-64-pipeline-migration 2>&1 | grep -c "refs/heads/phase-64-pipeline-migration"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` returns `phase-64-pipeline-migration` (in the livekit-agent repo)
    - `git log --oneline main..phase-64-pipeline-migration | wc -l` returns ≥ 5
    - `pytest tests/ 2>&1 | tail -5` shows "N+ passed, 1 failed" (the pre-existing deferred VIP test) OR "N+ passed" if the VIP test got fixed upstream
    - `git ls-remote origin phase-64-pipeline-migration` returns a non-empty line (remote branch exists)
    - `git log origin/phase-64-pipeline-migration --oneline -1` returns the same SHA as local `git log phase-64-pipeline-migration --oneline -1`
  </acceptance_criteria>
  <done>Branch pushed to origin with Phase 64 changeset; local suite green except pre-existing deferred VIP test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Railway preview deploy + boot health</name>
  <action>This is a human-verify checkpoint. Claude’s role: verify preconditions (per how-to-verify below), wait for the human signal, and record the outcome. See `<how-to-verify>` for the protocol and `<resume-signal>` for the expected response format.</action>
  <what-built>
    Phase 63 / 63.1 used Railway preview auto-deploys on branch push. Assuming the same hook fires on `phase-64-pipeline-migration`, Railway will:
    - Install the livekit-agent dependency graph including `livekit-plugins-silero==1.5.6` (already pinned from Phase 63)
    - Boot the worker and register it with the LiveKit cloud service
    - Listen on the preview SIP URL (or call test hook exposed by the preview service)

    Claude has pushed the branch; the deploy is either already in progress or requires user to trigger it from Railway dashboard.
  </what-built>
  <how-to-verify>
    1. Open Railway dashboard → livekit-agent service → Deployments.
    2. Find the deployment tied to `phase-64-pipeline-migration` HEAD SHA (compare against `git log -1 phase-64-pipeline-migration --format=%H`).
    3. Wait for status = SUCCESS (typical ~2-3 min).
    4. Open the deploy's logs pane.
    5. Confirm the following boot-time signals:
       - `INFO livekit.agents.cli:registered worker` (or equivalent "worker registered" log line — exact phrasing depends on 1.5.6 version)
       - NO `ImportError: No module named 'onnxruntime'` (Pitfall 7 — should not happen since Phase 63 pinned silero, but verify)
       - NO `FileNotFoundError: silero model not found` (Pitfall 7)
       - NO `TypeError` referencing `STT.__init__` or `LLM.__init__` or `VAD.load` (Pitfall 3 — would indicate a kwarg mismatch that Plan 01's mock-based tests didn't catch against real constructors)
       - NO `RuntimeError` or other unhandled exceptions in the first 60s of logs
    6. Verify `GOOGLE_APPLICATION_CREDENTIALS` env var is set AND has Cloud Speech v2 scope:
       - Railway dashboard → livekit-agent service → Variables → confirm `GOOGLE_APPLICATION_CREDENTIALS` is populated (actual value hidden; confirm presence only)
       - In Google Cloud Console → IAM & Admin → find the service account identified by that credential → confirm role is `roles/speech.admin` OR `roles/speech.client` OR a custom role including `speech.v2.*` permissions (per Plan 02 threat register T-64-02-02)
    7. Capture the preview URL / preview SIP address for Plan 05 UAT.
    8. Paste the preview URL + deploy SUCCESS SHA + boot log snippet confirming "worker registered" into `64-04-SUMMARY.md`.

    **If deploy fails**: capture the failure log (ImportError? TypeError? env-var missing?) and report it. Do NOT attempt to push a fix from this plan — this is a checkpoint, not a fixer. If a fix is needed, create a new commit on the branch (gap-closure plan) and re-push.

    **If Railway does not auto-deploy**: manually trigger from Railway dashboard (Deployments → New Deployment → choose `phase-64-pipeline-migration` branch).

    **If `GOOGLE_APPLICATION_CREDENTIALS` scope is insufficient for Cloud Speech v2**: this is a BLOCKER for Plan 05 UAT. Either (a) user adds the missing IAM role to the Railway service account, or (b) user rotates to a service account that has the scope. Record the resolution in `64-04-SUMMARY.md`.
  </how-to-verify>
  <acceptance_criteria>
    - Railway deploy status = SUCCESS for the `phase-64-pipeline-migration` branch at its current HEAD SHA
    - Boot log contains "worker registered" (or equivalent)
    - Boot log does NOT contain any of: `ImportError`, `ModuleNotFoundError`, `TypeError`, `FileNotFoundError` in the first 60s
    - `GOOGLE_APPLICATION_CREDENTIALS` present in Railway env and scoped for Cloud Speech v2 (confirmed via IAM role check)
    - Preview URL / SIP address captured and recorded in `64-04-SUMMARY.md`
  </acceptance_criteria>
  <resume-signal>Type "preview green" with the preview URL, OR describe the deploy failure (boot log excerpt + root cause).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- New boundary surfaced (not new attack surface): the Google Cloud Speech v2 API call from the Railway service → Cloud Speech endpoint. Credentials = Railway service account (ADC). No key in code.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04-01 | Information Disclosure | Google Cloud Speech credentials exposure | mitigate | Task 2 explicitly verifies `GOOGLE_APPLICATION_CREDENTIALS` is a service-account ADC (not a user key) AND scoped to `roles/speech.client` minimum. No wildcard `roles/owner` or `roles/editor`. |
| T-64-04-02 | Denial of Service | Cloud Speech API rate-limit / quota exhaustion on UAT | accept | UAT is a single call. Quota standard is 180 requests/min per project — well above a single test call's budget. |
</threat_model>

<verification>
- Branch on origin with correct HEAD SHA
- Railway deploy SUCCESS at that SHA
- Boot logs show worker registered without import/type/runtime errors
- Preview URL captured
- Cloud Speech v2 IAM scope verified
</verification>

<success_criteria>
- Checkpoint resolved: user types "preview green <URL>"
- `64-04-SUMMARY.md` written with deploy SHA, preview URL, and boot-log snippet
- Plan 05 unblocked
</success_criteria>

<output>
After checkpoint clears, create `.planning/phases/64-livekit-pipeline-agent-migration/64-04-SUMMARY.md` with:
- Remote branch SHA
- Railway deploy SHA + status
- Preview URL / SIP address for Plan 05 UAT
- Boot log snippet confirming worker registration and absence of errors
- Cloud Speech v2 IAM role confirmed (e.g., "roles/speech.client")
- Any deploy failures encountered and their resolution
</output>
