---
phase: 61.2-phase-61-production-regressions-round-2-slot-hallucination-a
plan: 05
subsystem: voice-agent-test-invariants
tags: [livekit, pytest, static-invariants, regression-guard, substring-grep, no-mocking]

requires:
  - phase: 61.2-02
    provides: "Fix A — mute_input_during_tool wired into 3 data-fetch tools (check_caller_history, check_customer_account, capture_lead)"
  - phase: 61.2-03
    provides: "Fix B — _TOOL_MUTE_FALLBACK_S raised 15→25s + function_tools_executed listener in _availability_lib.py"
  - phase: 61.2-04
    provides: "Fix C — _ServerCancelHandler installed on google plugin loggers; server_tool_cancellations + orphaned_server_content counters on _diag_record"
  - phase: 63.1
    provides: "test_no_generate_reply_in_src.py — substring-grep test pattern that this plan mirrors"
provides:
  - "tests/test_tool_mute_invariants.py — 5 substring-grep tests locking Fixes A/B/C against silent regression"
  - "Wave 2 close — Wave 1 fixes are now CI-protected"
affects: [61.2-06, 61.2-07]

tech-stack:
  added: []
  patterns:
    - "Substring-grep test pattern — open source files as text via Path.read_text(encoding='utf-8'), assert/refute keyword presence; no SDK imports, no mocks, no fixtures (mirrors Phase 63.1 test_no_generate_reply_in_src.py)"
    - "FLOOR-not-EXACT numeric guard — regex-extract _TOOL_MUTE_FALLBACK_S, assert value >= 25.0 to tolerate future raises (e.g. 30s) without test churn"
    - "Negative invariant for terminal tools — assert mute_input_during_tool NOT in transfer_call.py + end_call.py (teardown intent must NOT be muted)"

key-files:
  created:
    - "C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py — 5 invariant tests (101 lines)"
    - "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-…/deferred-items.md — pre-existing pytest failures discovered, all out of scope"
  modified: []

key-decisions:
  - "Removed book_appointment.py from DATA_FETCH_TOOLS list (Rule 1 auto-fix). The plan as-written listed it among 'already-existing mute users', but verification showed book_appointment.py never called mute_input_during_tool (confirmed by grep + by 61.2-02-SUMMARY which scoped Fix A to 3 tools, not 4). Including it would cause test_data_fetch_tools_mute to fail with a false-positive. The list now contains exactly 6 tools: 3 Plan-02 additions (check_caller_history, check_customer_account, capture_lead) + 3 pre-existing availability tools (check_day, check_slot, next_available_days). book_appointment performs synchronous DB writes via atomic_book_slot, not the Gemini-Live BLOCKING-wait pattern the mute helper protects against."
  - "Documented exclusion rationale inline in the test file comment block so future readers don't re-add book_appointment.py without first re-verifying."
  - "Did NOT modify the plan file. Per scope-boundary rule, the plan-spec mismatch is a fix-during-execution deviation tracked in the SUMMARY, not a re-plan."
  - "Kept FLOOR-not-EXACT regex guard for _TOOL_MUTE_FALLBACK_S. If a future phase needs to raise the fallback to 30s (after, say, longer Jobber/Xero context fetches), the test stays green."

patterns-established:
  - "When a plan-spec test list contradicts the actual code state, verify with grep and SUMMARY cross-reference before writing the test; trust the code + canonical SUMMARY over the plan body."
  - "Inline-comment exclusion rationale (book_appointment.py NOT in list because…) is more durable than a SUMMARY-only deviation note — the comment travels with the test forever."

requirements-completed: [D-10-rev]

# Metrics
duration: 6min
completed: 2026-05-06
---

# Phase 61.2 Plan 05: Static Invariants Locking Fix A/B/C Summary

**5 substring-grep pytest invariants in `tests/test_tool_mute_invariants.py` lock the Fix A/B/C structural-mute pattern against silent regression — pattern mirrors `tests/test_no_generate_reply_in_src.py` from Phase 63.1; no mocks, no fixtures, no SDK imports.**

## Performance

- **Duration:** ~6 min
- **Tasks:** 4 (1 file write + 2 pytest runs + 1 commit)
- **Files created:** 1 in livekit-agent + 1 deferred-items.md in homeservice_agent

## Accomplishments

- Created `tests/test_tool_mute_invariants.py` (101 lines, 5 test functions) on livekit-agent main.
- All 5 new tests PASS against current livekit-agent state (post-Wave-1 tree at `1b636bc`):
  - `test_data_fetch_tools_mute` — D-A-01: 6 tools call `mute_input_during_tool`.
  - `test_terminal_tools_do_not_mute` — D-A-01 negative: `transfer_call.py` + `end_call.py` do NOT call it.
  - `test_unmute_fallback_at_least_25s` — D-B-01: regex extracts `_TOOL_MUTE_FALLBACK_S = 25.0`, asserts `>= 25.0` floor.
  - `test_function_tools_executed_listener` — D-B-01: `_availability_lib.py` contains the substring `function_tools_executed`.
  - `test_server_cancel_handler_installed` — D-C-01: `agent.py` contains 4 substrings — `class _ServerCancelHandler`, `livekit.plugins.google.realtime`, `server_tool_cancellations`, `orphaned_server_content`.
- Suite delta: pre-Plan-05 = 282 passed / 11 failed; post-Plan-05 = 287 passed / 11 failed. **+5 passed, ±0 new failures.**
- Wave 2 closed; Wave 3 (D-12 live UAT, planned in Plan 06) is unblocked.

## pytest output

```
$ cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_tool_mute_invariants.py -v
============================= test session starts =============================
platform win32 -- Python 3.13.3, pytest-9.0.3, pluggy-1.6.0
collected 5 items

tests/test_tool_mute_invariants.py::test_data_fetch_tools_mute PASSED    [ 20%]
tests/test_tool_mute_invariants.py::test_terminal_tools_do_not_mute PASSED [ 40%]
tests/test_tool_mute_invariants.py::test_unmute_fallback_at_least_25s PASSED [ 60%]
tests/test_tool_mute_invariants.py::test_function_tools_executed_listener PASSED [ 80%]
tests/test_tool_mute_invariants.py::test_server_cancel_handler_installed PASSED [100%]

============================== 5 passed in 0.05s ==============================
```

Full suite (excluding 2 pre-existing collection-error files):

```
$ pytest tests/ --ignore=tests/test_check_availability_slot_cache.py --ignore=tests/test_slot_token_handoff.py
================= 11 failed, 287 passed, 2 warnings in 22.15s =================
```

Pre-Plan-05 baseline (verified by stashing the new test file and re-running):

```
================= 11 failed, 282 passed, 2 warnings in 21.28s =================
```

Delta: **+5 passed, 0 new failures.** The 11 pre-existing failures are documented in `deferred-items.md` as out-of-scope (drift from Phase 60.3/60.4/63.1 prompt + greeting refactors plus the long-tolerated VIP test from Phase 60.3 P01). All flagged as not-caused-by-Plan-05 with reproduction at HEAD `1b636bc`.

## Commits

| Phase    | Repo            | SHA       | Subject                                                          |
|----------|-----------------|-----------|------------------------------------------------------------------|
| 61.2-05  | livekit-agent   | c66e435   | `test(61.2): static invariants locking Fix A/B/C against regression` |

(homeservice_agent docs commit follows separately as the Plan 05 final-bundle commit.)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Removed `book_appointment.py` from DATA_FETCH_TOOLS list**
- **Found during:** Task 1 — pre-write verification grep.
- **Issue:** The plan's DATA_FETCH_TOOLS list named `book_appointment.py` as an "already-existing mute user" alongside the 3 availability tools. Verification showed: `grep mute_input_during_tool src/tools/book_appointment.py` returns nothing; `61.2-02-SUMMARY.md` scopes Fix A to exactly 3 tools (`check_caller_history`, `check_customer_account`, `capture_lead`); the only "already-existing" mute users are the 3 availability tools (`check_day`, `check_slot`, `next_available_days`). Including `book_appointment.py` would have caused `test_data_fetch_tools_mute` to fail incorrectly.
- **Fix:** Removed `book_appointment.py` from the list. Final list contains 6 tools (3 Plan-02 additions + 3 pre-existing availability tools). Inline comment in the test file documents the exclusion rationale.
- **Files modified:** `C:/Users/leheh/.Projects/livekit-agent/tests/test_tool_mute_invariants.py`
- **Commit:** c66e435

### Discovered Out-of-Scope Issues (deferred — not fixed)

- 2 pre-existing pytest collection errors (`tests/test_check_availability_slot_cache.py`, `tests/test_slot_token_handoff.py`) — both reference `src.tools.check_availability` which was renamed/split in a prior phase. Tracked in `deferred-items.md`.
- 11 pre-existing pytest failures across `test_goodbye_diag`, `test_prompt_booking`, `test_prompt_outcome_words`, `test_prompt_tool_narration`, `test_tenant_timezone_fallback`, `webhook/test_routes` — all reproduced at HEAD `1b636bc` BEFORE Plan 05's test file was added. Likely drift from Phase 60.3 / 60.4 / 63.1. Out of scope for cascade-mitigation Plan 05.

### Authentication Gates

None.

## Self-Check

```
$ git log --oneline -1 c66e435
c66e435 test(61.2): static invariants locking Fix A/B/C against regression
```

- FOUND: livekit-agent commit c66e435 on origin/main
- FOUND: tests/test_tool_mute_invariants.py (101 lines, parses, 5 tests pass)
- FOUND: deferred-items.md in phase 61.2 dir
- FOUND: 5/5 new tests passing; 0 new failures introduced

## Self-Check: PASSED
