---
phase: 61.2
plan: 02
subsystem: voice-call-architecture
tags: [livekit-agent, gemini-live, cancellation-cascade, mute-input, structural-fix]
requires:
  - 61.2-01 (Wave 0 PRE-FIX-UAT evidence — call AJ_vV4DM5AG9t7W)
provides:
  - Mute pattern extended from 4 availability/booking tools to 3 data-fetch tools
  - Cancellation surface coverage on check_caller_history (dominant cancellation point per pre-fix UAT)
affects:
  - livekit-agent/src/tools/check_caller_history.py
  - livekit-agent/src/tools/check_customer_account.py
  - livekit-agent/src/tools/capture_lead.py
tech_stack_added: []
tech_stack_patterns:
  - "Reuse of existing mute_input_during_tool pattern (no new abstractions)"
  - "Phase 61.2 inline comments anchor each call site to RESEARCH § 4 fix A"
key_files_created: []
key_files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py
  - C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py
decisions:
  - "Comment placement: RESEARCH § 4 fix A reference inlined at every mute call site to keep the cascade-mitigation rationale traceable from source to evidence"
  - "Import ordering: ._availability_lib import placed adjacent to existing relative imports in each file (between livekit.agents and the file's other ..-relative imports) for consistency with check_day.py reference shape"
metrics:
  duration: ~1min
  completed: 2026-05-06T07:37:17Z
  tasks_completed: 5
  files_modified: 3
  lines_added: 20
  commits: 1
commit_sha: 93dd4b5c1f6fd08851e5f90fda06b6ed875d0c78
file_blob_shas:
  capture_lead.py: d26446f498a9016944c5d8ff4f4ef2cf29233b1c
  check_caller_history.py: 8d7e45ee111e14c3bfec0cd7e59157e8c492d2c1
  check_customer_account.py: 3ee167d4178c7d6e171fcfe696aada1cf424e9a9
threats_addressed:
  - T-61.2-CASCADE-A
---

# Phase 61.2 Plan 02: Fix A — Mute Pattern Extension to Data-Fetch Tools — Summary

**One-liner:** Extended `mute_input_during_tool` from the 4 availability/booking tools to the 3 always-on data-fetch tools (`check_caller_history`, `check_customer_account`, `capture_lead`) so caller input audio is detached during their BLOCKING Gemini-Live tool waits, eliminating the dominant Gemini-server VAD cancellation surface identified in the D-09 pre-fix UAT.

## What was built

| File | Change | Lines added |
|------|--------|-------------|
| `livekit-agent/src/tools/check_caller_history.py` | Added `from ._availability_lib import mute_input_during_tool` import; added `mute_input_during_tool(deps)` as the first statement of the `check_caller_history` async fn body (BEFORE the existing `tenant_id = deps.get("tenant_id")` line) | +6 |
| `livekit-agent/src/tools/check_customer_account.py` | Added the same import; added `mute_input_during_tool(deps)` as the FIRST statement inside `async def check_customer_account` (before `ctx = deps.get("customer_context")`) | +7 |
| `livekit-agent/src/tools/capture_lead.py` | Added the same import; added `mute_input_during_tool(deps)` as the FIRST statement inside `async def capture_lead` (before `tenant_id = deps.get("tenant_id")` and before the address-validation HTTP call + Supabase writes) | +7 |

Total: **3 files, +20 lines, 1 commit on livekit-agent main.**

## Tasks completed (5/5)

| # | Task | Status | Verification |
|---|------|--------|--------------|
| 1 | Add mute pattern to `check_caller_history.py` | ✅ | `grep mute_input_during_tool` matches at L13 (import) + L31 (call); `python -m ast.parse` PASS |
| 2 | Add mute pattern to `check_customer_account.py` | ✅ | `grep` matches at L21 (import) + L121 (call, FIRST statement of fn body); AST PASS |
| 3 | Add mute pattern to `capture_lead.py` | ✅ | `grep` matches at L12 (import) + L55 (call, FIRST statement of fn body); AST PASS |
| 4 | Verify negative — terminal tools NOT modified | ✅ | `! grep mute_input_during_tool transfer_call.py end_call.py` PASS — neither contains the call |
| 5 | Commit Plan 02 to livekit-agent main | ✅ | Commit `93dd4b5` `fix(61.2-A): mute input during BLOCKING data-fetch tools` — 3 files, +20 lines, working tree clean post-commit |

## Acceptance criteria — D-A-01

All four invariants from `61.2-CONTEXT.md` D-A-01 satisfied:

- ✅ `mute_input_during_tool(deps)` added to `check_caller_history.py`, `check_customer_account.py`, `capture_lead.py`
- ✅ Each call is the first I/O-touching statement (before `deps.get(...)` reads in CCH/CL; before `deps.get("customer_context")` in CCA — first statement of fn body)
- ✅ `transfer_call.py` and `end_call.py` NOT modified (terminal-teardown intent preserved per D-A-01 reasoning: "Muting during teardown could mask caller cancellation signal")
- ✅ All 3 files parse as valid Python (`ast.parse`)

## Pattern alignment

The mute call shape mirrors `check_day.py:61` (the reference shape per the plan's `read_first` directive):

```python
# Prevent caller VAD from interrupting Gemini's BLOCKING tool wait
# (see _availability_lib.mute_input_during_tool module header).
mute_input_during_tool(deps)
```

Each new call site carries a Phase 61.2-anchored comment (vs. check_day's pre-existing comment) so the cascade-mitigation rationale traces back to `61.2-RESEARCH.md` § 4 fix A from the source.

## What this does NOT do

- **No `_availability_lib.py` edits.** Plan 03 owns Fix B (raise `_TOOL_MUTE_FALLBACK_S` 15→25s + add `function_tools_executed` listener for unmute re-anchor).
- **No `agent.py` edits.** Plan 04 owns Fix C (`_ServerCancelHandler` telemetry).
- **No prompt edits.** Per D-09 pre-fix UAT verdict: prompt is not the load-bearing layer for this regression class.
- **No tool-signature changes.** Tool runtime semantics unchanged — the only behavioral effect is that caller input audio is detached during the tool's BLOCKING wait.
- **No tests added.** Plan 05 owns the static invariant tests (`test_data_fetch_tools_mute`, `test_terminal_tools_do_not_mute`).

## Deviations from plan

**None.** Plan 02 executed exactly as written:
- Verified Assumption #1 from RESEARCH.md § Assumptions Log ("`check_customer_account.py` and `capture_lead.py` do not currently call `mute_input_during_tool`") via direct file read before editing — confirmed both files lacked the call.
- All 5 task verification commands returned PASS.
- Single commit, single repo (livekit-agent only — homeservice_agent docs commit happens in this Plan 02 close as part of standard GSD bundle, not within Task 5's scope).

No Rule 1/2/3/4 deviations triggered.

## Wave 1 unblocking

**Plan 03 (Fix B unmute lifecycle) and Plan 04 (Fix C telemetry) are now unblocked** — they touch different files (`_availability_lib.py` and `agent.py` respectively) so no merge conflict surface.

D-12 mandatory live UAT remains the ship gate (planned in Plan 06 or 07 per replan): cascade rate (Fix C telemetry) must be < 5 cancellations / 230s vs. AJ_vV4DM5AG9t7W baseline of 5/230s, AND no fragmenting agent turns, AND tool flow completes (book OR capture_lead).

## Threat model coverage

| Threat ID | Disposition | Status after Plan 02 |
|-----------|-------------|----------------------|
| T-61.2-CASCADE-A | mitigate | Partially mitigated — Fix A landed (3 dominant data-fetch cancellation surfaces detached). Full mitigation requires Plans 03 (Fix B) and 04 (Fix C telemetry to MEASURE cascade rate). D-12 live UAT confirms end-to-end. |

## Self-Check: PASSED

Verification:
- ✅ `C:/Users/leheh/.Projects/livekit-agent/src/tools/check_caller_history.py` exists, contains both `from ._availability_lib import mute_input_during_tool` (L13) and `mute_input_during_tool(deps)` (L31), parses as valid Python.
- ✅ `C:/Users/leheh/.Projects/livekit-agent/src/tools/check_customer_account.py` exists, contains both at L21 + L121, parses.
- ✅ `C:/Users/leheh/.Projects/livekit-agent/src/tools/capture_lead.py` exists, contains both at L12 + L55, parses.
- ✅ Commit `93dd4b5c1f6fd08851e5f90fda06b6ed875d0c78` exists on livekit-agent main (`git log --oneline -1` matches subject `fix(61.2-A): mute input during BLOCKING data-fetch tools`).
- ✅ `transfer_call.py` and `end_call.py` do NOT contain `mute_input_during_tool` (negative invariant).
- ✅ livekit-agent working tree clean post-commit (no orphan untracked files from this plan).
