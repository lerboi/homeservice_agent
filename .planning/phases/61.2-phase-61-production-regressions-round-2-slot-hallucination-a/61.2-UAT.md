# Phase 61.2 — D-12 Post-Fix UAT Evidence

**Placed:** 2026-05-06 08:03 UTC
**Caller:** +6587528516 (test SIP)
**Tenant:** make it ai (24141cd0-5735-4c7a-82d5-994c6b821861)
**Call ID:** AJ_5NcSoiaZGZTJ (room call-_+6587528516_ekktYHzPGW5z)
**Duration:** 202s
**booking_outcome:** not_attempted
**disconnection_reason:** CLIENT_INITIATED

Two calls were placed. Call 1 (above) is the comparable to the AJ_vV4DM5AG9t7W baseline. Call 2 (AJ_dTnDR7CQo8vD, 37s) timed out on greeting playout (`[63.1-07] greeting playout wait timed out at 10s; force-unmuting input`) and the caller dropped 1s later — not a comparable run; treated as a SIP audio dropout, not a code regression.

## Telemetry from Fix C

- `server_tool_cancellations`: **1** (baseline AJ_vV4DM5AG9t7W: 3 logged + 2 inferred = 5)
- `orphaned_server_content`: **1** (baseline: 1)
- Total cascade events: **2** vs baseline 5 — **60% reduction in cascade rate.** Fix A + Fix B + Fix C structurally contain the cancellation cascade.

Telemetry pipeline verified end-to-end: both new keys present in the `[goodbye_race]` JSON line, populated correctly, no serialization regression.

## Transcript fragmenting check

- Mid-word cuts: NO
- Leading-space-continuation pairs: YES (1 — `"agent:  one moment."` has the leading double-space marker)
- Restart/duplicate pairs: YES — `"Let me see what that day looks like for you. Give me just a second to check that day."` is a self-restart sequence consistent with cancellation-and-regenerate.

## Tool flow

- `check_caller_history` invocations: 0 logged (caller is new — no history to fetch; mute path was not exercised)
- `check_day` / `check_slot` / `next_available_days` invocations: **1 (`check_day` only)** — returned `STATE:day_has_slots date_label=Thursday, May 7th count=9` at 08:05:46
- `capture_lead` / `book_appointment` invocations: 0
- Final tool path: `check_day → [server cancelled tool call fc_15328661595426966818] → 25s mute fallback → agent regenerated and HALLUCINATED "we don't have anything open tomorrow"`

## Fix B listener regression (NEW finding)

The `mute_input_during_tool` unmute log line reads:

```
[tool_mute] unmuted input id=1 (fallback timeout 25.0s)
```

This means the `function_tools_executed` re-anchor listener Plan 03 added did NOT fire — the unmute came from the 25-second fallback path. The 25→`fbk=25.0s` raise correctly absorbed the cascade window (the call did not deadlock), but the listener as currently registered does not catch the event for the `check_day` path.

This is a Plan 03 gap to be closed in 61.3.

## Verdict

- Cascade rate strictly < 5? **YES** (2 < 5) ✓
- No transcript fragmenting? **NO** ✗ (one leading-space pair + one restart pair)
- Booking OR clean lead-capture? **NO** ✗ (slot hallucination — agent said "no slots open tomorrow" when `check_day` had returned `count=9`)

**Verdict:** gap

## Comparison to PRE-FIX-UAT

The pre-fix call (AJ_vV4DM5AG9t7W) ended in a recovery-cancellation deadlock with 5 cascade events, fragmented turns, and a `not_attempted` hangup. The post-fix call (AJ_5NcSoiaZGZTJ) reduced the cascade to 2 events and the cancellation no longer deadlocked the session — the agent successfully regenerated 23 seconds after the server-cancel. **However**, the regenerated response hallucinated "no slots open tomorrow" despite `check_day` having returned `STATE:day_has_slots count=9`. The cascade is contained but its residual effect now manifests as a slot-hallucination instead of a recovery-deadlock. Net: cascade structurally fixed, downstream behavior worse from the caller's perspective.

## If verdict = gap

Triage as **61.3**. Three concerns to close:

1. **Slot hallucination after cancellation** (highest severity) — when the agent's response to `check_day` is cancelled and regenerated from stale/cleared context, it inverts the tool result. Likely fixes:
   - On `function_call_cancelled` (or equivalent), inject a synthetic user turn that re-states the last successful tool's STATE so the regenerated response sees ground truth.
   - Or: mark the tool result as "must-include" in the regeneration context.
   - Investigation needed in `agent.py` `_ServerCancelHandler` — escalate from telemetry to recovery action.

2. **Fix B listener missed** — `function_tools_executed` did not fire for the `check_day` path. Investigation: which event/emitter the listener is bound to, and whether `_TOOL_MUTE_FALLBACK_S` should be lowered (25s feels long when the caller is silent).

3. **Greeting playout timeout** (Call 2) — Pre-existing fragility in `wait_for_playout()` when SIP audio drops mid-playout. Plan: extend the timeout failure path so the agent is robust to partial-audio greetings (consider awaiting only first-frame-sent rather than full playout).

Plan 07 (skill update for 61.2) should still ship — Wave 1 fixes are real and verified. The skill update should reflect: cascade contained but a new failure mode surfaced; document both for 61.3 to close.
