---
phase: 61.2
plan: 04
type: execute
wave: 3
depends_on:
  - "61.2-03"
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md
autonomous: false
requirements:
  - D-12
threats_addressed:
  - T-61.2-01
  - T-61.2-02
  - T-61.2-03

must_haves:
  truths:
    - "A live LiveKit SIP UAT call has been placed against the test tenant AFTER Plan 02's prompt edits and Plan 03's tests have shipped (and after the user has pushed both commits to remote so Railway has redeployed)."
    - "The UAT verdict is recorded in 61.2-UAT.md with concrete evidence: call ID, transcript excerpt covering the moment after the four trigger fields were given, and the three D-12 success criteria evaluated explicitly."
    - "Phase 61.2 is NOT declared verified unless all three D-12 criteria pass: (a) tools were invoked when info was gathered, (b) NO slot time was proposed that the caller did not say, (c) full booking flow completed OR a clean lead-capture happened."
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md"
      provides: "D-12 mandatory-gate evidence record. Pass/fail verdict per criterion."
      contains: "D-12"
  key_links:
    - from: "61.2-UAT.md"
      to: "61.2-PRE-FIX-UAT.md (Plan 01)"
      via: "comparison field"
      pattern: "Compared to pre-fix"
    - from: "61.2-UAT.md verdict"
      to: "61.2-VALIDATION.md D-12 row"
      via: "evidence-link"
      pattern: "D-12"
---

<objective>
Wave 3 — D-12 MANDATORY GATE. Place a live LiveKit SIP UAT call against the test tenant AFTER the prompt edits (Plan 02) and tests (Plan 03) have been shipped to production (Railway-deployed via push of livekit-agent main).

This is the actual behavioral validation gate. Phase 61.1 lesson is binding: "14 GREEN invariant tests still deadlocked production." Only a real call exercises the Gemini 3.1 Flash Live model's behavior; only a real call can confirm Fix 1 + Fix 2 are effective.

**Three D-12 success criteria** (all must pass — no partial verification accepted):
1. **Tools invoked when info gathered** — `check_slot` / `check_day` / `next_available_days` is invoked once the four trigger preconditions are met (street + postal + day + urgency).
2. **No slot hallucination** — the agent does NOT propose a specific clock time that the caller did not say, including as a confirmation question. The incident `AJ_W7woUYGhG5CV` failure mode (*"so just to confirm, you're available on Thursday at 10 AM?"* without the caller saying "10 AM") MUST be absent.
3. **Booking or lead-capture completes** — full booking flow lands a confirmed appointment, OR a clean `capture_lead` invocation lands a lead row. Anything else (silence, hangup, repeated info-gathering loop) = FAIL.

Purpose: Confirm Phase 61.2 actually fixed production behavior, not just test invariants.

Output: One file `61.2-UAT.md` with concrete evidence per criterion. One git commit. If verdict = PASS, Plan 05 (closeout) is unblocked. If verdict = FAIL, do NOT proceed — return to discuss-phase / planner with new evidence.

**No code changes in this plan.** This plan is gate-only.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-INCIDENT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-VALIDATION.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-PRE-FIX-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: D-12 — Live LiveKit SIP UAT call (post-fix mandatory gate)</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md</files>

  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md (D-12 decision text — the three success criteria)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-INCIDENT.md (the failure mode this UAT must demonstrate is absent)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-VALIDATION.md (D-12 row in Manual-Only Verifications table — evidence-recording requirement)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-PRE-FIX-UAT.md (the pre-fix baseline from Plan 01 — UAT verdict will reference this for delta)
  </read_first>

  <action>
**What was just built (parent-task context):**

- **Plan 02 prompt edits** are committed on livekit-agent (new SLOT TIMES section + URGENCY trigger paragraph + OUTCOME WORDS cross-reference, all EN+ES).
- **Plan 03 invariant tests** are committed on livekit-agent (15 new tests; full prompt suite GREEN).
- **The user has pushed livekit-agent main to remote** so Railway has redeployed the new prompt to production. (If this hasn't happened, the UAT will exercise the OLD prompt — STOP and push first.)

The agent is now running with Phase 61.2's prompt in production. Time to verify behavior with a real call.


**Manual verification protocol:**

**Why manual:** D-12 tests model behavior, not prompt content. Phase 61.1 lesson: 14 GREEN invariant tests still deadlocked production. Only a live call answers "does the model actually invoke the tool / refrain from fabricating times?"

**Pre-flight checks (do these BEFORE placing the call):**

1. Confirm livekit-agent has Plan 02 + Plan 03 commits pushed to remote:
   ```bash
   cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline origin/main..HEAD
   ```
   Should be empty (HEAD == origin/main). If non-empty, push first: `git push origin main`.

2. Confirm Railway has redeployed (visit Railway dashboard or wait ~60s after push).

3. Confirm the test tenant is reachable. Tenant: "make it ai" (`24141cd0-5735-4c7a-82d5-994c6b821861`). The test caller number used in the incident is +6587528516.

**Place the UAT call:**

1. Place a live LiveKit SIP test call against the test tenant. Use the same canonical info-gathering flow as the incident:
   - "Yes, I need a plumber."
   - Provide a service address (street + unit, e.g. "123 Main Street, unit 04-12").
   - Provide the postal code (e.g. "560123" for SG, or US zip).
   - Provide a day preference (e.g. "Thursday").
   - Provide an urgency cue (e.g. "the toilet is leaking, it's not flooding but I want it fixed soon").

2. **CRITICAL: do NOT name a specific time** in your scripted utterances. The whole point of D-12 is to verify the agent does not fabricate one back at you. If you happen to say "Thursday at 10 AM," you've leaked the time into the agent's verbatim window and the slot-fabrication test is invalid. Stick to day-only. If the agent asks "what time works?", proceed with a coarse answer like "afternoon" or "morning" — never a specific clock hour.

3. **Observe and record:**
   - Did the agent invoke `check_slot` / `check_day` / `next_available_days` AFTER the four fields were collected? **YES / NO** — D-12 criterion 1.
   - If the agent proposed a specific time, did the caller actually say that time first (you, the test placer)? **YES / NO** — D-12 criterion 2 (NO is the FAIL state for fabrication).
   - Did the call complete with a confirmed booking OR a clean `capture_lead`? **YES / NO** — D-12 criterion 3.
   - Capture the call ID (`AJ_...`) from LiveKit dashboard or Supabase.
   - Capture the transcript via the SQL query (substitute the new call_id):
     ```sql
     SELECT call_id, duration_seconds, booking_outcome, disconnection_reason,
            transcript_text, transcript_structured
     FROM calls WHERE call_id = '<new_call_id>';
     ```

4. **Create the evidence file** at `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md` with this structure:

```markdown
# Phase 61.2 — D-12 Post-Fix UAT Evidence (MANDATORY GATE)

**Placed:** {YYYY-MM-DD HH:MM TZ}
**livekit-agent SHA at time of call:** {git SHA — must include Plan 02 + Plan 03 commits}
**Caller (test placer):** +{number}
**Tenant:** make it ai (24141cd0-5735-4c7a-82d5-994c6b821861)
**Call ID:** AJ_...
**Duration:** {N seconds}
**booking_outcome:** {booked|attempted|declined|not_attempted}
**disconnection_reason:** {value}

## D-12 Criteria

### Criterion 1 — Tools invoked when info gathered

- All four trigger fields collected before tool invocation? YES / NO
- Which tool was invoked? `check_slot` / `check_day` / `next_available_days` / NONE
- Approximate turn or timestamp of first tool invocation: {turn N or HH:MM:SS}
- Verdict: PASS / FAIL

### Criterion 2 — No slot hallucination

- Did the agent propose a specific clock time? YES / NO
- If YES, quote the agent's turn: "{verbatim agent line}"
- Did the caller (you) say that time first in any prior turn? YES / NO
- Verdict: PASS (no fabrication) / FAIL (agent fabricated)

### Criterion 3 — Booking or lead-capture completes

- Final outcome: BOOKED / LEAD_CAPTURED / NEITHER
- Booking ID or lead ID (from DB): {value}
- Verdict: PASS / FAIL

## Transcript excerpt (load-bearing turns)

{paste 6-12 turns covering: the moment caller gave the last trigger field,
the agent's response, the tool invocation (if any), the agent's post-tool turn,
and the final outcome turn}

## Compared to pre-fix UAT (Plan 01 / 61.2-PRE-FIX-UAT.md)

{One paragraph contrasting the post-fix call against the pre-fix baseline.
E.g., "Pre-fix: agent proposed Thursday 10 AM unprompted; tools never invoked.
Post-fix: agent invoked check_slot after gathering urgency; no time proposed
by agent; full booking flow completed."}

## Overall Verdict

**PASS** if all three D-12 criteria pass.
**FAIL** if any criterion fails.

Verdict: {PASS | FAIL}

## If FAIL — next-step recommendation

{If FAIL, specify which criterion(ia) failed and propose: re-plan via
/gsd:discuss-phase 61.2-followup, or escalate to Phase 64 (LiveKit pipeline
migration) as the canonical owner of the underlying SDK / model behavior.}
```

5. **Critical: do NOT mark this checkpoint resolved unless verdict is PASS or you explicitly accept FAIL with a documented next-step plan.**

**Resume signal:** Reply `pass` if all three D-12 criteria are PASS. Reply `fail: {brief reason}` if any criterion failed (Plan 05 will NOT execute — orchestrator routes to gap-closure or follow-up phase). Reply `partial: {brief reason}` if mixed verdict (planner discretion: may proceed to Plan 05 with explicit deferred-items note, or hold).
  </action>

  <verify>
    <automated>MANUAL — gate satisfied when the named evidence file contains all required sections per acceptance_criteria; reviewed by human prior to resume-signal</automated>
  </verify>

  <resume-signal>Reply `pass` only if all three D-12 criteria PASS in `61.2-UAT.md`. Reply `fail: {reason}` to halt and route to gap-closure. Reply `partial: {reason}` to flag mixed verdict for discretion. NO ship without an explicit verdict.</resume-signal>

  <acceptance_criteria>
    - File `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md` exists.
    - File contains a Call ID line matching `Call ID:.*AJ_`.
    - File contains the literal substring `## Criterion 1 — Tools invoked when info gathered` followed by an explicit `Verdict: PASS` or `Verdict: FAIL` (not the placeholder).
    - File contains the literal substring `## Criterion 2 — No slot hallucination` with an explicit Verdict line.
    - File contains the literal substring `## Criterion 3 — Booking or lead-capture completes` with an explicit Verdict line.
    - File contains an `## Overall Verdict` section with `Verdict: PASS` or `Verdict: FAIL` (or `PARTIAL` if explicitly accepted).
    - File contains a `## Compared to pre-fix UAT` section linking to Plan 01's evidence.
    - If verdict is FAIL, file contains a `## If FAIL — next-step recommendation` section with concrete next steps.
  </acceptance_criteria>

  <done>
    D-12 evidence is captured. Plan 05 proceeds only on PASS verdict. Phase 61.2 is verified (or explicitly NOT verified — in which case planner is re-engaged).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit D-12 UAT evidence</name>
  <files>(commit only — homeservice_agent repo)</files>

  <read_first>
    (none — pure git operation)
  </read_first>

  <action>
**Step 1 — Verify the UAT file exists and is dirty:**

```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git status --short .planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md
```

Should show `??` (untracked).

**Step 2 — Stage and commit. Use a commit subject that reflects the verdict** (read the verdict line from `61.2-UAT.md` first):

```bash
# If verdict was PASS:
cd C:/Users/leheh/.Projects/homeservice_agent && git add .planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md && git commit -m "docs(61.2): D-12 post-fix UAT — verdict PASS"

# If verdict was FAIL:
cd C:/Users/leheh/.Projects/homeservice_agent && git add .planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md && git commit -m "docs(61.2): D-12 post-fix UAT — verdict FAIL — re-plan required"

# If verdict was PARTIAL (planner accepted with deferred items):
cd C:/Users/leheh/.Projects/homeservice_agent && git add .planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md && git commit -m "docs(61.2): D-12 post-fix UAT — verdict PARTIAL — deferred items noted"
```

**Step 3 — Verify commit:**

```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1
```

Should show `61.2` and `D-12 post-fix UAT`.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1 | grep -q "61.2" && git log --oneline -1 | grep -qi "D-12\|UAT" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - homeservice_agent has a new commit with subject containing both `61.2` and `D-12` (or `UAT`).
    - The commit modifies exactly: `.planning/phases/61.2-…/61.2-UAT.md`.
    - The commit subject reflects the verdict (PASS / FAIL / PARTIAL).
  </acceptance_criteria>

  <done>
    D-12 evidence is committed. Phase 61.2's verification gate is documented in version control.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live LiveKit SIP path → production agent | The UAT exercises the live agent in production. Any failure here is a real production failure. |
| caller utterances → agent prompt window | The test placer must NOT leak a specific clock time into their utterances; doing so invalidates criterion 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.2-01 | DoS / Repudiation (silent regression) | live agent behavior | mitigate | D-12 is the explicit gate that catches "rule exists but model ignores it" — Phase 61.1 lesson is binding ("14 GREEN tests still deadlocked production"). All three criteria must pass before declaring Phase 61.2 verified. |
| T-61.2-02 | Information Disclosure (false-positive booking) | live agent behavior | mitigate | Criterion 2 explicitly tests for slot fabrication using the incident shape (caller gives day-only, watches whether agent fabricates a clock hour). |
| T-61.2-03 | DoS (silence-deadlock regression) | live agent behavior | mitigate | Criterion 1 implicitly tests for non-deadlock — if the agent reaches the trigger condition without invoking a tool AND without pushing the conversation forward, criterion 1 fails. |
</threat_model>

<verification>
- `61.2-UAT.md` exists with evidence per all three D-12 criteria.
- Verdict line is explicit (`PASS` / `FAIL` / `PARTIAL`).
- Pre-fix comparison documented.
- Single git commit on homeservice_agent main.
</verification>

<success_criteria>
- D-12 verdict is PASS (the only outcome that unblocks Plan 05 by default).
- If verdict is FAIL or PARTIAL, planner is re-engaged before Plan 05 runs (orchestrator routes accordingly).
</success_criteria>

<output>
The UAT file IS the deliverable. No separate SUMMARY required for this gate-only plan. Plan 05 (closeout) writes the phase-level SUMMARY referencing this UAT.

If the orchestrator requires a SUMMARY for parity: create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-04-SUMMARY.md` with:
- Verdict (PASS / FAIL / PARTIAL).
- Call ID.
- Pre-fix vs post-fix one-line delta.
- Commit SHA.
</output>
