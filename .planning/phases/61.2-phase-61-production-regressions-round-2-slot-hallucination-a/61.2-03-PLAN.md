---
phase: 61.2
plan: 03
type: execute
wave: 2
depends_on:
  - "61.2-02"
files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_slot_times_rule.py
  - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_info_gathering.py
  - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_outcome_words.py
autonomous: true
requirements:
  - D-10
  - D-11
threats_addressed:
  - T-61.2-02
  - T-61.2-03

must_haves:
  truths:
    - "A new test file `tests/test_prompt_slot_times_rule.py` exists in `livekit-agent/`, mirroring `test_prompt_address_validation_rule.py`, with the 12 invariant tests from RESEARCH.md § Code Examples."
    - "A new test function in `tests/test_prompt_info_gathering.py` locks the four-precondition trigger phrasing for both EN and ES."
    - "A new test function in `tests/test_prompt_outcome_words.py` locks the SLOT TIMES cross-reference for both EN and ES."
    - "The full `pytest tests/test_prompt*.py -x` suite is GREEN — including the existing Phase 61.1 invariant `test_both_locales_pre_tool_readback_explicit` and all Phase 60.3 prompt invariants."
    - "D-11 behavioral E2E test is explicitly DEFERRED per RESEARCH.md recommendation; this is documented in the plan summary, not implemented."
  artifacts:
    - path: "C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_slot_times_rule.py"
      provides: "Fix 2 invariant tests (12 tests) — lock the new section's wording, position, locale parity, and absence of silence license."
      contains: "test_en_slot_times_rule_present"
    - path: "C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_info_gathering.py"
      provides: "Fix 1 invariant tests appended to existing file — lock the four-precondition trigger phrasing in EN+ES."
      contains: "test_tool_invocation_trigger"
    - path: "C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_outcome_words.py"
      provides: "D-04 cross-reference test appended to existing file — lock the OUTCOME WORDS → SLOT TIMES pointer in EN+ES."
      contains: "test_slot_times_cross_reference"
  key_links:
    - from: "test_prompt_slot_times_rule.py"
      to: "_build_slot_times_section"
      via: "import + substring assertions"
      pattern: "from src.prompt import _build_slot_times_section"
    - from: "test_prompt_slot_times_rule.py::test_position_top_attention_zone_en"
      to: "build_system_prompt"
      via: "section-index ordering assertion"
      pattern: "addr_idx < slot_idx < outcome_idx"
---

<objective>
Wave 2 — lock Plan 02's three prompt edits with invariant tests. Three test-file edits:

1. **NEW file:** `tests/test_prompt_slot_times_rule.py` — mirrors `test_prompt_address_validation_rule.py`. 12 tests covering EN/ES headers, core prohibition, failure-mode example anchor, tool-name presence, silence-license absence, position in top-attention zone, and EN/ES parity.

2. **APPEND to existing:** `tests/test_prompt_info_gathering.py` — add `test_tool_invocation_trigger_en` + `test_tool_invocation_trigger_es` covering Fix 1 wording (four trigger fields, three tool names, non-directive phrasing, no silence license).

3. **APPEND to existing:** `tests/test_prompt_outcome_words.py` — add `test_slot_times_cross_reference` covering D-04 (one cross-reference sentence in EN, one in ES, OUTCOME WORDS body otherwise unchanged).

Each test file is GREEN against Plan 02's code edits (Plan 02 was a "code-first" plan; this is a "test-after" lock plan with no RED phase to execute — the smoke checks in Plan 02 already proved the substrings are present).

**D-11 behavioral E2E test is DEFERRED** per RESEARCH.md § "D-11 Behavioral E2E Test Feasibility" recommendation: no existing harness pattern in the repo, 4-8 hours of brittle harness code, D-12 UAT (Plan 04) is the real validation gate. The deferral is documented; not implemented.

Purpose: Lock the wording of the three edits so future prompt revs cannot silently re-introduce the regression.

Output: Three test files modified. One livekit-agent commit. Full `pytest tests/test_prompt*.py -x` GREEN.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-VALIDATION.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `tests/test_prompt_slot_times_rule.py` (Fix 2 invariants — 12 tests)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_slot_times_rule.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_address_validation_rule.py (FULL FILE — this is the model. Mirror its imports, naming convention, and assertion shape.)
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (confirm `_build_slot_times_section` exists post-Plan-02 — if not, Plan 02 has not landed; halt)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (jump to "## Code Examples" — the verbatim test source is there; copy it; do not modify any assertion string)
    - C:/Users/leheh/.Projects/livekit-agent/tests/conftest.py (verify the fixture set; the tests in this file do NOT need any fixture beyond stdlib)
  </read_first>

  <behavior>
    - Test 1: `_build_slot_times_section("en")` contains `"SLOT TIMES — CRITICAL RULE"`.
    - Test 2: EN core prohibition substrings present (`"NEVER propose a specific time the caller has not said"` + `"This includes proposing it as a confirmation question"`).
    - Test 3: EN failure-mode example anchored to incident (`"Thursday at 10 AM"` + `"WRONG"`).
    - Test 4: EN tool names present untranslated (`check_slot`, `check_day`, `next_available_days`).
    - Test 5: EN no silence license (`"Silence is acceptable"`/`"Silence is always acceptable"`/`"the worst failure mode"` all absent).
    - Test 6: ES header present (`"HORARIOS DE CITAS — REGLA CRÍTICA"`).
    - Test 7: ES core prohibition substrings present (`"NUNCA proponga una hora específica"` + `"Esto incluye proponerla como una pregunta de confirmación"`).
    - Test 8: ES tool names present untranslated.
    - Test 9: ES no silence license.
    - Test 10: Position invariant EN — SLOT TIMES sits between ADDRESS VALIDATION and OUTCOME WORDS, and before TOOL NARRATION.
    - Test 11: Position invariant ES — SLOT TIMES sits between ADDRESS VALIDATION and OUTCOME WORDS.
    - Test 12: EN/ES parity guard — strings are distinct AND ES contains `REGLA CRÍTICA`.
  </behavior>

  <action>
**Step 1 — Create the new file `C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_slot_times_rule.py` with this VERBATIM content** (copy-paste from RESEARCH.md § Code Examples; the assertion strings are locked):

```python
"""Phase 61.2 Fix 2 — _build_slot_times_section locale parity + position invariants.

Locks the new SLOT TIMES section. Mirrors test_prompt_address_validation_rule.py.

Invariants:
1. EN/ES headers present.
2. Core prohibition substrings present in both locales.
3. The exact failure-mode example from incident AJ_W7woUYGhG5CV is present (EN).
4. Tool name code identifiers are NOT translated.
5. The new section appears in the top-attention zone — between
   _build_address_validation_section and _build_outcome_words_section,
   and BEFORE _build_tool_narration_section.
6. EN and ES strings are distinct (parity guard against copy-paste error).
7. NO silence escape hatch; NO unqualified "worst failure mode" framing.
"""
from __future__ import annotations

from src.prompt import (
    _build_address_validation_section,
    _build_outcome_words_section,
    _build_slot_times_section,
    _build_tool_narration_section,
    build_system_prompt,
)


def test_en_slot_times_rule_present():
    section = _build_slot_times_section("en")
    assert "SLOT TIMES — CRITICAL RULE" in section


def test_en_core_prohibition():
    section = _build_slot_times_section("en")
    assert "NEVER propose a specific time the caller has not said" in section
    assert "This includes proposing it as a confirmation question" in section


def test_en_failure_mode_example_anchored_to_incident():
    section = _build_slot_times_section("en")
    # Anchored to incident AJ_W7woUYGhG5CV (Thursday at 10 AM hallucination)
    assert "Thursday at 10 AM" in section
    assert "WRONG" in section


def test_en_tool_names_present_untranslated():
    section = _build_slot_times_section("en")
    for tool in ("check_slot", "check_day", "next_available_days"):
        assert tool in section


def test_en_no_silence_license():
    section = _build_slot_times_section("en")
    assert "Silence is acceptable" not in section
    assert "Silence is always acceptable" not in section
    # Avoid unqualified worst-failure framing per Phase 61.1 lesson
    assert "the worst failure mode" not in section


def test_es_slot_times_rule_present():
    section = _build_slot_times_section("es")
    assert "HORARIOS DE CITAS — REGLA CRÍTICA" in section


def test_es_core_prohibition():
    section = _build_slot_times_section("es")
    assert "NUNCA proponga una hora específica" in section
    assert "Esto incluye proponerla como una pregunta de confirmación" in section


def test_es_tool_names_present_untranslated():
    section = _build_slot_times_section("es")
    for tool in ("check_slot", "check_day", "next_available_days"):
        assert tool in section


def test_es_no_silence_license():
    section = _build_slot_times_section("es")
    assert "El silencio es aceptable" not in section
    assert "El silencio siempre es aceptable" not in section
    assert "el peor modo de falla" not in section


def test_position_top_attention_zone_en():
    full = build_system_prompt(locale="en", business_name="Voco")
    addr = _build_address_validation_section("en")
    slot = _build_slot_times_section("en")
    outcome = _build_outcome_words_section("en")
    tool_narr = _build_tool_narration_section("en")
    addr_idx = full.find(addr)
    slot_idx = full.find(slot)
    outcome_idx = full.find(outcome)
    tool_idx = full.find(tool_narr)
    assert addr_idx >= 0 and slot_idx >= 0 and outcome_idx >= 0 and tool_idx >= 0
    assert addr_idx < slot_idx < outcome_idx, (
        f"SLOT TIMES must sit between address_validation and outcome_words "
        f"(addr={addr_idx}, slot={slot_idx}, outcome={outcome_idx})"
    )
    assert slot_idx < tool_idx, (
        f"SLOT TIMES must precede tool_narration (slot={slot_idx}, tool={tool_idx})"
    )


def test_position_top_attention_zone_es():
    full = build_system_prompt(locale="es", business_name="Voco")
    addr = _build_address_validation_section("es")
    slot = _build_slot_times_section("es")
    outcome = _build_outcome_words_section("es")
    addr_idx = full.find(addr)
    slot_idx = full.find(slot)
    outcome_idx = full.find(outcome)
    assert addr_idx < slot_idx < outcome_idx


def test_en_es_distinct():
    en = _build_slot_times_section("en")
    es = _build_slot_times_section("es")
    assert en != es
    assert "REGLA CRÍTICA" in es
```

**Step 2 — Run the file in isolation and confirm all 12 tests pass:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_slot_times_rule.py -v
```

Expected output: 12 tests collected, 12 passed.

**If `build_system_prompt(locale="en", business_name="Voco")` raises a `TypeError` due to missing required kwargs** (e.g., `tone_label`, `working_hours`, `tenant_timezone`), inspect the function signature and add the minimum required kwargs to satisfy it (the test_prompt_address_validation_rule.py model file shows the canonical kwargs to use). Do NOT change the assertion strings.

**Step 3 — Run the full prompt suite as a regression guard:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_address_validation_rule.py tests/test_prompt_slot_times_rule.py tests/test_prompt_outcome_words.py tests/test_prompt_info_gathering.py -x
```

All tests must pass. The Phase 61.1 invariant `test_both_locales_pre_tool_readback_explicit` MUST pass (regression guard for T-61.2-03).
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_slot_times_rule.py -v --no-header 2>&1 | tail -5 | grep -E "12 passed" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_slot_times_rule.py` exists.
    - File contains exactly 12 test functions matching the regex `^def test_`.
    - `pytest tests/test_prompt_slot_times_rule.py -v` reports `12 passed`.
    - File imports `_build_slot_times_section` from `src.prompt`.
    - File contains the literal assertion `"SLOT TIMES — CRITICAL RULE"`.
    - File contains the literal assertion `"HORARIOS DE CITAS — REGLA CRÍTICA"`.
    - File contains the literal assertion `"Thursday at 10 AM"` (incident anchor).
    - File contains negative-space assertions for all four silence-license substrings (`"Silence is acceptable"`, `"Silence is always acceptable"`, `"El silencio es aceptable"`, `"El silencio siempre es aceptable"`) AND `"the worst failure mode"` AND `"el peor modo de falla"`.
    - Existing `tests/test_prompt_address_validation_rule.py::test_both_locales_pre_tool_readback_explicit` still passes (regression guard).
  </acceptance_criteria>

  <done>
    Fix 2 is locked at the test layer. Any future prompt edit that removes the section, weakens the prohibition, or re-introduces a silence license will fail invariant tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append `test_tool_invocation_trigger_*` to `tests/test_prompt_info_gathering.py` (Fix 1 invariants)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_info_gathering.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_info_gathering.py (FULL FILE — find the existing test functions and the import section. Phase 60.3-10 set this up; new tests append to the existing pattern.)
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (confirm Fix 1 paragraph landed in `_build_info_gathering_section`)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (jump to "### Invariant substrings (D-10 test lock for Fix 1)" — must-be-present + must-be-absent lists are the source of truth)
  </read_first>

  <behavior>
    - Test EN: After calling `_build_info_gathering_section(t, postal_label, "en")`, assert all four trigger-field substrings (`street`, `postal code or zip code`, `day preference`, `urgency`) are present, all three tool names (`check_slot`, `check_day`, `next_available_days`) are present, the load-bearing trigger phrase (`"you have what you need to check availability"`) is present, the hard-imperative phrase (`"you must invoke"`) is absent, and the silence-license phrase (case-insensitive `"silence is acceptable"`) is absent.
    - Test ES: same shape, in Spanish — `"calle"`, `"código postal"`, `"preferencia de día"`, `"urgencia"`, all three tool names, `"tiene lo que necesita para verificar disponibilidad"` present, `"debe invocar"` absent, `"el silencio es aceptable"` absent (case-insensitive).
    - Phase 60.3-10 single-question framing must still pass (the existing test in this file). New tests do not break old tests.
  </behavior>

  <action>
**Step 1 — Read the current `tests/test_prompt_info_gathering.py`.**

Identify the import section at the top of the file (likely imports `_build_info_gathering_section` from `src.prompt`, plus any test fixtures like `MagicMock` for the `t` translation function).

**Step 2 — Append two new test functions at the end of the file.**

Use this VERBATIM content (the assertion strings are locked):

```python


def test_tool_invocation_trigger_en():
    """Phase 61.2 Fix 1 — EN URGENCY block ends with the four-precondition trigger paragraph.

    Locks: load-bearing trigger phrase, all four trigger fields, all three tool names,
    no hard-imperative directive, no silence license.
    """
    from unittest.mock import MagicMock
    from src.prompt import _build_info_gathering_section

    t = MagicMock()
    t.side_effect = lambda *a, **k: ""
    section = _build_info_gathering_section(t, "postal code", "en")

    # Load-bearing trigger phrase (D-02 / Fix 1)
    assert "you have what you need to check availability" in section, (
        "Fix 1 trigger phrase missing — Phase 61.2 D-02 broke"
    )

    # All four trigger fields (D-02 locked)
    for field in ("street", "postal code or zip code", "day preference", "urgency"):
        assert field in section, f"trigger field '{field}' missing from URGENCY block"

    # All three availability tool names (untranslated code identifiers)
    for tool in ("check_slot", "check_day", "next_available_days"):
        assert tool in section, f"tool name '{tool}' missing from URGENCY block"

    # Non-directive phrasing per feedback_livekit_prompt_philosophy.md
    assert "you must invoke" not in section, (
        "hard-imperative phrasing leaked — Fix 1 must be non-directive"
    )

    # Anti-deadlock per feedback_directive_prompt_silence_deadlock.md
    assert "silence is acceptable" not in section.lower(), (
        "silence license leaked — Phase 61.1 lesson violated"
    )


def test_tool_invocation_trigger_es():
    """Phase 61.2 Fix 1 — ES URGENCY block ends with the four-precondition trigger paragraph.

    Mirrors EN test. ES register USTED consistent with Phase 60.3-10.
    """
    from unittest.mock import MagicMock
    from src.prompt import _build_info_gathering_section

    t = MagicMock()
    t.side_effect = lambda *a, **k: ""
    section = _build_info_gathering_section(t, "código postal", "es")

    # Load-bearing trigger phrase (ES)
    assert "tiene lo que necesita para verificar disponibilidad" in section, (
        "ES Fix 1 trigger phrase missing"
    )

    # All four ES trigger fields
    for field in ("calle", "código postal", "preferencia de día", "urgencia"):
        assert field in section, f"ES trigger field '{field}' missing"

    # Tool names untranslated
    for tool in ("check_slot", "check_day", "next_available_days"):
        assert tool in section, f"tool name '{tool}' missing from ES URGENCY"

    # Non-directive ES
    assert "debe invocar" not in section, "ES hard-imperative leaked"

    # Anti-deadlock ES
    assert "el silencio es aceptable" not in section.lower(), "ES silence license leaked"
```

**Note on the `t` mock:** Phase 60.3-10 made `_build_info_gathering_section` a function of `(t, postal_label, locale)`. The `t` is the i18n translation callable (`next-intl`-style). For these tests it is mocked to return empty strings — the function still composes the section's static prose. Verify this matches the existing test pattern by reading the file first; if Phase 60.3-10 used a different fixture, mirror that fixture instead.

**Step 3 — Run the appended tests in isolation:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_en tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_es -v
```

Expected: 2 passed.

**Step 4 — Run the full file as a regression guard:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_info_gathering.py -x
```

All tests pass — including the Phase 60.3-10 single-question and locale-parity tests.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_en tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_es -v --no-header 2>&1 | tail -5 | grep -E "2 passed" && pytest tests/test_prompt_info_gathering.py -x --no-header 2>&1 | tail -3 | grep -E "passed" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `tests/test_prompt_info_gathering.py` contains a function named `test_tool_invocation_trigger_en`.
    - File contains a function named `test_tool_invocation_trigger_es`.
    - Both new tests pass via `pytest tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_en tests/test_prompt_info_gathering.py::test_tool_invocation_trigger_es -v` (output contains `2 passed`).
    - Full `pytest tests/test_prompt_info_gathering.py -x` passes (no Phase 60.3-10 regression).
    - The new EN test asserts the literal substring `"you have what you need to check availability"`.
    - The new ES test asserts the literal substring `"tiene lo que necesita para verificar disponibilidad"`.
    - The new EN test asserts the absence of `"you must invoke"`.
    - The new ES test asserts the absence of `"debe invocar"`.
  </acceptance_criteria>

  <done>
    Fix 1 is locked at the test layer in both locales. Future edits to URGENCY block must preserve the trigger paragraph wording.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append `test_slot_times_cross_reference` to `tests/test_prompt_outcome_words.py` (D-04)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_outcome_words.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_outcome_words.py (FULL FILE — Phase 60.3-09 set this up with EN+ES locale parity tests; verify the import section)
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (confirm D-04 cross-references landed in both locales)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (D-08 verdict on D-04 — single sentence appended, no body amendment)
  </read_first>

  <behavior>
    - Test EN: `_build_outcome_words_section("en")` contains the literal cross-reference sentence `"governed by SLOT TIMES above"` AND the existing OUTCOME WORDS body invariants are still present (Phase 60.3-09 regression guard).
    - Test ES: `_build_outcome_words_section("es")` contains the literal cross-reference sentence `"gobernadas por HORARIOS DE CITAS arriba"` AND the existing PALABRAS DE RESULTADO body invariants are still present.
  </behavior>

  <action>
**Step 1 — Read the current `tests/test_prompt_outcome_words.py`.**

Identify the import section and confirm `_build_outcome_words_section` is already imported (from Phase 60.3-09). If not, add the import.

**Step 2 — Append a single new test function at the end of the file.**

Use this VERBATIM content (the cross-reference strings are locked verbatim from Plan 02 Task 3):

```python


def test_slot_times_cross_reference():
    """Phase 61.2 D-04 — OUTCOME WORDS contains a one-line cross-reference to SLOT TIMES.

    Per RESEARCH.md § D-08 verdict: do NOT amend the OUTCOME WORDS body; ADD a single
    cross-reference sentence in EN+ES so a future reader (and the model) sees the
    slot-time-fabrication clause has its own home in SLOT TIMES.
    """
    from src.prompt import _build_outcome_words_section

    en = _build_outcome_words_section("en")
    es = _build_outcome_words_section("es")

    # Cross-reference sentence (verbatim from Plan 02)
    assert "governed by SLOT TIMES above" in en, (
        "EN OUTCOME WORDS missing SLOT TIMES cross-reference (D-04 broke)"
    )
    assert "gobernadas por HORARIOS DE CITAS arriba" in es, (
        "ES PALABRAS DE RESULTADO missing SLOT TIMES cross-reference (D-04 broke)"
    )

    # Phase 60.3-09 body invariants — body must NOT be amended (D-08 verdict)
    assert "OUTCOME WORDS" in en
    assert "PALABRAS DE RESULTADO" in es
    # Reserved tool refs from Phase 60.3-09 still present
    for section in (en, es):
        assert "check_slot" in section
        assert "book_appointment" in section
```

**Step 3 — Run the new test in isolation:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_outcome_words.py::test_slot_times_cross_reference -v
```

Expected: 1 passed.

**Step 4 — Run the full file as a regression guard:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_outcome_words.py -x
```

All Phase 60.3-09 tests pass + the new test passes.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_outcome_words.py::test_slot_times_cross_reference -v --no-header 2>&1 | tail -3 | grep -E "1 passed" && pytest tests/test_prompt_outcome_words.py -x --no-header 2>&1 | tail -3 | grep -E "passed" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - File `tests/test_prompt_outcome_words.py` contains a function named `test_slot_times_cross_reference`.
    - `pytest tests/test_prompt_outcome_words.py::test_slot_times_cross_reference -v` reports `1 passed`.
    - Full `pytest tests/test_prompt_outcome_words.py -x` passes (no Phase 60.3-09 regression).
    - The new test asserts the literal substring `"governed by SLOT TIMES above"`.
    - The new test asserts the literal substring `"gobernadas por HORARIOS DE CITAS arriba"`.
    - The new test asserts both `OUTCOME WORDS` and `PALABRAS DE RESULTADO` are still present (regression guard).
    - The new test asserts both `check_slot` and `book_appointment` are still referenced in both locales (Phase 60.3-09 invariant).
  </acceptance_criteria>

  <done>
    D-04 cross-reference is locked at the test layer. The OUTCOME WORDS body is regression-guarded — any body amendment that strips the tool references will fail the test.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run full prompt-test suite and commit livekit-agent test additions</name>
  <files>(commit only — livekit-agent repo)</files>

  <read_first>
    (none — pure git + test operation)
  </read_first>

  <action>
**Step 1 — Run the full prompt-test suite as the final regression guard.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_address_validation_rule.py tests/test_prompt_slot_times_rule.py tests/test_prompt_outcome_words.py tests/test_prompt_info_gathering.py tests/test_prompt.py tests/test_prompt_booking.py tests/test_prompt_call_duration.py tests/test_prompt_corrections.py tests/test_prompt_customer_context.py tests/test_prompt_greeting_directive.py tests/test_prompt_language.py tests/test_prompt_tail_sections.py tests/test_prompt_tool_narration.py tests/test_prompt_voice_behavior.py -x
```

All tests must pass. Any failure here is a regression in a Phase 60.3 / 61.1 invariant — investigate before committing.

**Step 2 — If preferred, run the entire suite once for full safety:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/ -x
```

Note: the pre-existing deferred VIP test (`test_incoming_call_vip_lead`) may fail or be deselected — that has been documented as a known-pre-existing-failure since Phase 60.3 Plan 01 (`deferred-items.md`). It is NOT a Phase 61.2 regression. If it fails, that is OK — the prompt suite is the load-bearing one for this phase.

**Step 3 — Confirm the three test files are dirty (or new):**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git status --short tests/
```

Should show:
- `??` (or `A`) for `tests/test_prompt_slot_times_rule.py` (new file)
- ` M` for `tests/test_prompt_info_gathering.py` (modified)
- ` M` for `tests/test_prompt_outcome_words.py` (modified)

No other files dirty in `tests/`.

**Step 4 — Stage and commit:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git add tests/test_prompt_slot_times_rule.py tests/test_prompt_info_gathering.py tests/test_prompt_outcome_words.py && git commit -m "test(prompt): lock SLOT TIMES + tool-invocation trigger + cross-ref invariants (Phase 61.2)"
```

**Step 5 — Verify commit:**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline -2
```

The most recent commit should be the test commit; the one before it should be Plan 02's prompt-edit commit. Both must be on the same branch (no branch divergence).

Note: This commit is on livekit-agent. Do NOT push to remote unless the user requests — Plan 04 (D-12 live UAT) needs Railway to redeploy, which happens on push to main; the orchestrator coordinates push timing.

**D-11 deferral note for the SUMMARY:** Per RESEARCH.md § "D-11 Behavioral E2E Test Feasibility" recommendation, no behavioral E2E test is implemented in Phase 61.2. The decision is documented in Plan 03 SUMMARY with the rationale: (a) no existing harness pattern in repo, (b) 4-8 hours of brittle harness code against changing `livekit-plugins-google` versions, (c) D-12 live UAT is the actual behavioral check. CONTEXT D-11 said "researcher must scope" — researcher recommended DEFER; planner adopts the recommendation; this plan honors it. If the user disagrees, this can be re-scoped as Phase 61.3 or a follow-up plan within 61.2.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && pytest tests/test_prompt_slot_times_rule.py tests/test_prompt_info_gathering.py tests/test_prompt_outcome_words.py tests/test_prompt_address_validation_rule.py -x --no-header 2>&1 | tail -3 | grep -E "passed" && git log --oneline -1 | grep -q "61.2" && git log --oneline -1 | grep -qi "test\|invariant\|lock" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_prompt_slot_times_rule.py tests/test_prompt_info_gathering.py tests/test_prompt_outcome_words.py tests/test_prompt_address_validation_rule.py -x` is GREEN.
    - livekit-agent has a new commit with subject containing `61.2`.
    - The commit modifies exactly 3 test files (no `src/` changes).
    - `git log --oneline -2` shows the test commit on top of the Plan 02 prompt-edit commit (no `--amend`, no rebase, no force-push).
    - The Phase 61.1 invariant `test_both_locales_pre_tool_readback_explicit` is in the GREEN set.
  </acceptance_criteria>

  <done>
    All three Phase 61.2 prompt-edit invariants are locked at the test layer. livekit-agent has two coherent commits (Plan 02 = code, Plan 03 = tests). Plan 04 (D-12 live UAT) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test invariants -> future prompt edits | Future Claude sessions may rewrite prompt sections. Tests catch silent regressions only if invariant strings are precise. |
| livekit-agent main branch | Tests must run GREEN before Plan 04 deploys to production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.2-02 | Information Disclosure (false-positive booking) | invariant tests | mitigate | 12-test file locks `_build_slot_times_section` wording, including the failure-mode example anchored to incident `AJ_W7woUYGhG5CV` (`"Thursday at 10 AM"` + `"WRONG"`); cross-reference test in OUTCOME WORDS prevents future authors from removing the pointer. |
| T-61.2-03 | DoS (silence-deadlock regression — Phase 61.1 lesson) | invariant tests | mitigate | All three new test additions explicitly assert ABSENCE of silence-license substrings (EN+ES) and unqualified worst-failure framing. The Phase 61.1 invariant `test_both_locales_pre_tool_readback_explicit` is in the regression-guard set. |
</threat_model>

<verification>
- `tests/test_prompt_slot_times_rule.py` exists with 12 tests, all GREEN.
- `tests/test_prompt_info_gathering.py` has 2 new tests (`test_tool_invocation_trigger_en` + `_es`), all GREEN.
- `tests/test_prompt_outcome_words.py` has 1 new test (`test_slot_times_cross_reference`), GREEN.
- All Phase 60.3 prompt invariants still GREEN.
- Phase 61.1 `test_both_locales_pre_tool_readback_explicit` still GREEN.
- livekit-agent has one new commit modifying exactly 3 test files.
- D-11 behavioral E2E test explicitly deferred per RESEARCH.md recommendation; documented in Task 4 action block.
</verification>

<success_criteria>
- All four tasks pass their `<verify><automated>` blocks.
- `pytest tests/test_prompt*.py -x` GREEN end-to-end.
- livekit-agent has exactly two commits since Plan 01 (Plan 02 code + Plan 03 tests).
- D-11 deferral documented (not implemented).
</success_criteria>

<output>
After completion, create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-03-SUMMARY.md` documenting:
- livekit-agent commit SHA.
- Test counts: 12 new in slot_times_rule, 2 new in info_gathering, 1 new in outcome_words = 15 new tests total.
- Full-suite test summary (X passed, Y deselected/pre-existing-failed).
- Explicit D-11 deferral note with rationale (no harness pattern; 4-8h estimated; D-12 is real validation).
- Any deviations.
</output>
