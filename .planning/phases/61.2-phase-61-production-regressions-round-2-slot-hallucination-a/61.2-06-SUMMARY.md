---
phase: 61.2
plan: 06
status: complete
verdict: gap
---

# Plan 06 Summary — D-12 Post-Fix UAT (Live SIP Call)

## Outcome

Verdict: **gap** (1 of 3 D-12-rev criteria pass).

Live LiveKit SIP UAT placed against test tenant after Plans 02-05 deployed to livekit-agent main and Railway redeployed (worker `AW_jUYX6EriSybE`, 1.5.7, registered 07:59:29 UTC).

## Calls placed

- **Call 1 — `AJ_5NcSoiaZGZTJ`** (room `call-_+6587528516_ekktYHzPGW5z`, 202s, outcome=not_attempted, CLIENT_INITIATED) — comparable to pre-fix baseline `AJ_vV4DM5AG9t7W`. Full evidence in `61.2-UAT.md`.
- **Call 2 — `AJ_dTnDR7CQo8vD`** (37s, greeting playout timed out at 10s, caller dropped 1s later) — treated as SIP audio dropout, not a code regression. Pre-existing fragility in `wait_for_playout()`.

## Cascade telemetry (Fix C verified end-to-end)

| Field | Pre-fix baseline (`AJ_vV4DM5AG9t7W`) | Post-fix Call 1 | Delta |
|-------|--------------------------------------|------------------|-------|
| `server_tool_cancellations` | 5 (3 logged + 2 inferred) | 1 | -80% |
| `orphaned_server_content` | 1 | 1 | flat |
| **Total cascade events** | **5** | **2** | **-60%** |

Both new keys present in `[goodbye_race]` JSON; serialization correct. Telemetry pipeline shipped successfully.

## D-12-rev verdict breakdown

- Cascade rate strictly < 5? **YES** (2 < 5) ✓
- No transcript fragmenting? **NO** ✗ — 1 leading-space pair + 1 restart pair (`"Let me see what that day looks like for you. Give me just a second to check that day."`)
- Booking OR clean lead-capture? **NO** ✗ — slot hallucination

## New failure mode discovered: slot hallucination after cancellation

`check_day` returned `STATE:day_has_slots date_label=Thursday, May 7th count=9` at 08:05:46. After the 1 server-cancellation at 08:06:14 (function_call_id `fc_15328661595426966818`) and the 25s mute fallback at 08:06:08, the regenerated agent response told the caller: **"it looks like we don't have anything open tomorrow"** — directly contradicting the tool result.

The cascade is structurally contained (no deadlock) but the residual cascade now produces a slot inversion instead of a deadlock. From the caller's perspective, this is worse than the pre-fix deadlock — they get an answer, but it's wrong.

## Plan 03 listener regression (NEW finding)

The unmute log line for the `check_day` tool path:

```
[tool_mute] unmuted input id=1 (fallback timeout 25.0s)
```

The unmute came via the 25s fallback timer, **not** via the `function_tools_executed` re-anchor listener Plan 03 added. The listener as currently registered does not catch the event for this tool invocation. The 25s fallback covered for it (no deadlock), but the listener wiring is a Plan 03 gap to close in 61.3.

## Files written

- `.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md` — full evidence, committed in `6c42a74`.

## Routing

Triage as **61.3** with three closure items (in severity order):

1. Slot-hallucination-after-cancellation — synthetic user turn re-stating last successful tool STATE before regeneration, or pin tool result as must-include in context.
2. Fix B listener wiring — investigate which emitter/event the listener should bind to so it doesn't fall through to the 25s fallback.
3. Greeting playout timeout robustness — Call 2's `wait_for_playout()` failure path.

Wave 1 fixes (commits `93dd4b5`, `1afde0e`, `1b636bc`, `c66e435`) stay on `livekit-agent/main` — they are net-positive structural progress.

## Self-Check: PASSED
