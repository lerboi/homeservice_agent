---
phase: 61.2
plan: 02
type: execute
wave: 1
depends_on:
  - "61.2-01"
files_modified:
  - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-08
  - D-14
threats_addressed:
  - T-61.2-02
  - T-61.2-03

must_haves:
  truths:
    - "A new function `_build_slot_times_section(locale)` exists in `src/prompt.py` returning the EN body for `locale='en'` and the ES body for `locale='es'`, mirroring the SHAPE of `_build_address_validation_section`."
    - "The `build_system_prompt` composition list at `src/prompt.py:1351-1367` calls `_build_slot_times_section(locale)` between `_build_address_validation_section(locale)` and `_build_outcome_words_section(locale)`."
    - "`_build_info_gathering_section` URGENCY block ends with the four-precondition trigger paragraph (EN+ES), naming `street`/`postal code or zip code`/`day preference`/`urgency` and `check_slot`/`check_day`/`next_available_days`."
    - "`_build_outcome_words_section` contains a one-line cross-reference to the new SLOT TIMES section (EN+ES)."
    - "No occurrence of `\"Silence is acceptable\"` / `\"Silence is always acceptable\"` / `\"El silencio es aceptable\"` / `\"El silencio siempre es aceptable\"` appears in the new `_build_slot_times_section`."
    - "Tool name code identifiers `check_slot`, `check_day`, `next_available_days` are present in the new section in BOTH locales (untranslated)."
  artifacts:
    - path: "C:/Users/leheh/.Projects/livekit-agent/src/prompt.py"
      provides: "New _build_slot_times_section function + composition wiring + URGENCY trigger paragraph + OUTCOME WORDS cross-reference."
      contains: "_build_slot_times_section"
  key_links:
    - from: "build_system_prompt sections list"
      to: "_build_slot_times_section"
      via: "function call between _build_address_validation_section and _build_outcome_words_section"
      pattern: "_build_slot_times_section\\(locale\\)"
    - from: "_build_outcome_words_section EN body"
      to: "_build_slot_times_section"
      via: "one-line cross-reference"
      pattern: "SLOT TIMES"
    - from: "_build_outcome_words_section ES body"
      to: "_build_slot_times_section"
      via: "one-line cross-reference"
      pattern: "HORARIOS DE CITAS"
---

<objective>
Wave 1 — ship the prompt code edits in the sibling repo `livekit-agent/`. Three concrete edits to ONE file (`src/prompt.py`):

1. **Fix 2 (D-03):** Add new function `_build_slot_times_section(locale: str = "en")` returning the EN body and ES body verbatim from RESEARCH.md § "Proposed `_build_slot_times_section(locale)` Wording (Fix 2)". Wire it into the `build_system_prompt` composition list at `src/prompt.py:1355-1356` (insertion point: between `_build_address_validation_section(locale)` and `_build_outcome_words_section(locale)`).

2. **Fix 1 (D-02):** Append the four-precondition trigger paragraph (EN+ES verbatim from RESEARCH.md § "Proposed Tool-Invocation Trigger Phrasing (Fix 1)") to the URGENCY block of `_build_info_gathering_section`.

3. **D-04 minor amendment:** Add a one-line cross-reference in `_build_outcome_words_section` (EN+ES) pointing to the new SLOT TIMES section.

Purpose: Restore tool-invocation pressure (Fix 1) and prevent slot-time fabrication (Fix 2), with a single coherent prompt rev. No silence escape hatch. EN+ES locale parity. Tool names not translated. Phase 61.1 invariants preserved.

Output: Three edits in `livekit-agent/src/prompt.py`. One livekit-agent commit. NO test changes (Plan 03 covers tests). NO test runs in this plan beyond a quick "imports cleanly" smoke check — that prevents this plan from accidentally implementing Plan 03's RED step.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-INCIDENT.md

<binding_memory>
@C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md
@C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_livekit_prompt_philosophy.md
@C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_deep_verify_before_fix.md
</binding_memory>

<interfaces>
<!-- Key prompt.py shape and composition order, extracted from RESEARCH.md -->
<!-- Composition function: build_system_prompt at src/prompt.py:1308 -->
<!-- Current sections list at src/prompt.py:1351-1367: -->

```python
sections = [
    _build_identity_section(business_name, tone_label, locale),
    _build_voice_behavior_section(locale),
    _build_corrections_section(locale),
    _build_address_validation_section(locale),  # Phase 61 P04 / 61.1 — index 3
    _build_outcome_words_section(locale),       # index 4 — INSERT BEFORE THIS
    _build_call_duration_section(t, locale),
    _build_tool_narration_section(locale),
    _build_working_hours_section(working_hours, tenant_timezone, locale),
    _build_greeting_section(locale, business_name, onboarding_complete, t),
    _build_language_section(t, locale),
    _build_repeat_caller_section(onboarding_complete),
    _build_customer_account_section(customer_context, locale),
    _build_info_gathering_section(t, postal_label, locale),
    _build_intake_questions_section(intake_questions, locale),
    _build_booking_section(business_name, onboarding_complete, postal_label, locale),
]
```

<!-- Post-edit sections list (target): -->
```python
sections = [
    _build_identity_section(business_name, tone_label, locale),
    _build_voice_behavior_section(locale),
    _build_corrections_section(locale),
    _build_address_validation_section(locale),
    _build_slot_times_section(locale),          # ← NEW (Phase 61.2 Fix 2)
    _build_outcome_words_section(locale),
    _build_call_duration_section(t, locale),
    _build_tool_narration_section(locale),
    # ... rest unchanged ...
]
```

<!-- _build_info_gathering_section signature (existing, do NOT change): -->
```python
def _build_info_gathering_section(t, postal_label, locale="en") -> str:
```
EN URGENCY block lives around line 894; ES URGENCY block lives around line 835.
The `postal_label` parameter is already plumbed in: it equals `"postal code"` for SG and `"zip code"` for US (set at `prompt.py:1349`). The Fix 1 paragraph references `postal code or zip code` to cover both branches in a single sentence (do NOT parameterize — the trigger paragraph is informational, not contextual).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new `_build_slot_times_section` and wire into `build_system_prompt` (Fix 2 / D-03)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/prompt.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (FULL FILE — confirm current state of `_build_address_validation_section`, `_build_outcome_words_section`, and the `build_system_prompt` sections list at 1351-1367; locate the function-definition zone where the new function will be inserted — by convention adjacent to `_build_address_validation_section`, around lines 168-251)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (jump to "## Proposed `_build_slot_times_section(locale)` Wording (Fix 2)" and "## `prompt.py` Composition Function + Insertion Point" — the verbatim Python source is there; copy it without modification)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md (D-03, D-08, D-14)
    - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_directive_prompt_silence_deadlock.md (binding: NO silence escape hatch; scope explicitly to the moment)
  </read_first>

  <action>
**Step 1 — Locate insertion point for the new function.**

Open `C:/Users/leheh/.Projects/livekit-agent/src/prompt.py`. Find `_build_address_validation_section`. Its function definition runs roughly lines 168-251 (verify in your read). Insert the new function **immediately after** `_build_address_validation_section` returns, before the next `def _build_*` (likely `_build_outcome_words_section` around line 260).

**Step 2 — Insert the new `_build_slot_times_section` function VERBATIM.**

Paste exactly this Python source. Do NOT modify any string literal — these are the locked invariant substrings (Plan 03's tests assert them character-for-character):

```python
def _build_slot_times_section(locale: str = "en") -> str:
    # Phase 61.2 Fix 2: dedicated CRITICAL RULE for slot-time hallucination.
    # Mirrors the SHAPE of _build_address_validation_section (Phase 61.1).
    # Scoped explicitly to the conversational moment (caller has not yet
    # given a specific time). NO silence license per
    # feedback_directive_prompt_silence_deadlock.md. EN+ES locale parity per
    # Phase 60.3 D-B-03. Tool names are code identifiers — NEVER translated.
    if locale == "es":
        return (
            "HORARIOS DE CITAS — REGLA CRÍTICA:\n"
            "Una hora específica de cita (un día y una hora del reloj, p. ej. "
            "\"jueves a las 10\") es un hecho que solo puede provenir de dos "
            "lugares: las palabras textuales del llamante en este turno, o el "
            "resultado de check_slot / check_day / next_available_days que "
            "acaba de regresar.\n"
            "\n"
            "NUNCA proponga una hora específica que el llamante no haya dicho. "
            "Esto incluye proponerla como una pregunta de confirmación. Si el "
            "llamante no ha nombrado una hora, su única opción es preguntarle "
            "qué hora le funciona — nunca sugerir una usted mismo.\n"
            "\n"
            "Modo de falla a evitar:\n"
            "Llamante: \"Necesito un fontanero el jueves.\" (sin mencionar "
            "ninguna hora)\n"
            "Usted: \"Entendido. Para confirmar, ¿está disponible el jueves a "
            "las 10 de la mañana?\" — INCORRECTO. El llamante nunca dijo \"10 "
            "de la mañana.\" Acaba de fabricar una hora específica como "
            "pregunta de confirmación.\n"
            "\n"
            "Ruta correcta:\n"
            "Llamante: \"Necesito un fontanero el jueves.\"\n"
            "Usted: \"Entendido, jueves. ¿Hay alguna hora del día que le "
            "funcione mejor — mañana, mediodía o tarde?\"\n"
            "\n"
            "Esta regla aplica antes de que se invoque cualquier herramienta "
            "de disponibilidad. Después de que check_slot regrese, las reglas "
            "de PALABRAS DE RESULTADO toman el relevo."
        )
    return (
        "SLOT TIMES — CRITICAL RULE:\n"
        "A specific appointment time (a day plus a clock hour, e.g. "
        "\"Thursday at 10 AM\") is a fact that can only come from two "
        "places: the caller's verbatim words in this turn, or a "
        "check_slot / check_day / next_available_days result that just "
        "returned.\n"
        "\n"
        "NEVER propose a specific time the caller has not said. This "
        "includes proposing it as a confirmation question. If the caller "
        "has not named an hour, your only option is to ask them what "
        "time works — never suggest one yourself.\n"
        "\n"
        "Failure mode to avoid:\n"
        "Caller: \"I need a plumber on Thursday.\" (no time mentioned)\n"
        "You: \"Got it. Just to confirm, you're available Thursday at 10 "
        "AM?\" — WRONG. The caller never said \"10 AM.\" You just "
        "fabricated a specific time as a confirmation question.\n"
        "\n"
        "Correct path:\n"
        "Caller: \"I need a plumber on Thursday.\"\n"
        "You: \"Got it, Thursday. Is there a time of day that works "
        "best for you — morning, midday, or afternoon?\"\n"
        "\n"
        "This rule applies before any availability tool is invoked. "
        "After check_slot returns, the OUTCOME WORDS rules take over."
    )
```

**Step 3 — Wire the new function into `build_system_prompt`.**

Find the `sections = [` list inside `build_system_prompt` (currently at `src/prompt.py:1351-1367`). Insert ONE new line between `_build_address_validation_section(locale),` and `_build_outcome_words_section(locale),`:

```python
sections = [
    _build_identity_section(business_name, tone_label, locale),
    _build_voice_behavior_section(locale),
    _build_corrections_section(locale),
    _build_address_validation_section(locale),
    _build_slot_times_section(locale),          # Phase 61.2 Fix 2
    _build_outcome_words_section(locale),
    _build_call_duration_section(t, locale),
    _build_tool_narration_section(locale),
    _build_working_hours_section(working_hours, tenant_timezone, locale),
    _build_greeting_section(locale, business_name, onboarding_complete, t),
    _build_language_section(t, locale),
    _build_repeat_caller_section(onboarding_complete),
    _build_customer_account_section(customer_context, locale),
    _build_info_gathering_section(t, postal_label, locale),
    _build_intake_questions_section(intake_questions, locale),
    _build_booking_section(business_name, onboarding_complete, postal_label, locale),
]
```

**Step 4 — Smoke-check the file imports and the function returns the right shape.**

Run a quick Python import + invocation check (do NOT run pytest yet — that's Plan 03's GREEN step):

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_slot_times_section, build_system_prompt; en = _build_slot_times_section('en'); es = _build_slot_times_section('es'); assert 'SLOT TIMES — CRITICAL RULE' in en; assert 'HORARIOS DE CITAS — REGLA CRÍTICA' in es; assert 'check_slot' in en; assert 'check_slot' in es; print('OK')"
```

Should print `OK`. If it errors with ImportError or AssertionError, fix before moving on.

**Step 5 — Verify no regression in the address-validation rule (Phase 61.1 invariant guard).**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_address_validation_section; en = _build_address_validation_section('en'); assert 'After book_appointment or capture_lead returns' in en; assert 'Silence or a neutral readback is always acceptable' not in en; print('OK')"
```

Should print `OK`. If not, the address-validation section was accidentally clobbered — restore.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_slot_times_section, _build_address_validation_section, build_system_prompt; en = _build_slot_times_section('en'); es = _build_slot_times_section('es'); assert 'SLOT TIMES — CRITICAL RULE' in en, 'EN header missing'; assert 'NEVER propose a specific time the caller has not said' in en, 'EN core prohibition missing'; assert 'This includes proposing it as a confirmation question' in en, 'EN confirmation-question clause missing'; assert 'Thursday at 10 AM' in en and 'WRONG' in en, 'EN failure-mode example missing'; assert 'check_slot' in en and 'check_day' in en and 'next_available_days' in en, 'EN tool names missing'; assert 'Silence is acceptable' not in en and 'Silence is always acceptable' not in en, 'EN silence license leaked'; assert 'the worst failure mode' not in en, 'EN unqualified worst-failure framing leaked'; assert 'HORARIOS DE CITAS — REGLA CRÍTICA' in es; assert 'NUNCA proponga una hora específica' in es; assert 'check_slot' in es and 'check_day' in es and 'next_available_days' in es; assert 'El silencio es aceptable' not in es and 'El silencio siempre es aceptable' not in es; assert en != es, 'EN/ES identical (parity guard failed)'; full = build_system_prompt(locale='en', business_name='Voco'); addr = _build_address_validation_section('en'); slot = _build_slot_times_section('en'); addr_idx = full.find(addr); slot_idx = full.find(slot); assert addr_idx >= 0 and slot_idx >= 0, 'sections not in assembled prompt'; assert addr_idx < slot_idx, f'SLOT TIMES must follow ADDRESS VALIDATION (addr={addr_idx}, slot={slot_idx})'; print('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from src.prompt import _build_slot_times_section"` succeeds (function exists and module imports cleanly).
    - `_build_slot_times_section("en")` contains literal `"SLOT TIMES — CRITICAL RULE"`.
    - `_build_slot_times_section("en")` contains literal `"NEVER propose a specific time the caller has not said"`.
    - `_build_slot_times_section("en")` contains literal `"This includes proposing it as a confirmation question"`.
    - `_build_slot_times_section("en")` contains literal `"Thursday at 10 AM"` and `"WRONG"`.
    - `_build_slot_times_section("en")` contains all of `"check_slot"`, `"check_day"`, `"next_available_days"`.
    - `_build_slot_times_section("en")` does NOT contain `"Silence is acceptable"`, `"Silence is always acceptable"`, or `"the worst failure mode"`.
    - `_build_slot_times_section("es")` contains literal `"HORARIOS DE CITAS — REGLA CRÍTICA"`.
    - `_build_slot_times_section("es")` contains literal `"NUNCA proponga una hora específica"`.
    - `_build_slot_times_section("es")` contains all three tool names untranslated.
    - `_build_slot_times_section("es")` does NOT contain `"El silencio es aceptable"` or `"El silencio siempre es aceptable"`.
    - `_build_slot_times_section("en") != _build_slot_times_section("es")` (parity guard).
    - In `build_system_prompt(locale="en", business_name="Voco")`, the index of `_build_slot_times_section("en")` falls AFTER the index of `_build_address_validation_section("en")` (composition wired correctly).
    - The Phase 61.1 invariant `"After book_appointment or capture_lead returns"` is still present in `_build_address_validation_section("en")` (not clobbered).
  </acceptance_criteria>

  <done>
    Fix 2 is live in `src/prompt.py`: the new function is defined, the composition order places it between address_validation and outcome_words, and a smoke check validates EN/ES bodies + position. Phase 61.1 invariant preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append four-precondition trigger paragraph to URGENCY block (Fix 1 / D-02)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/prompt.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (full file or at minimum lines 700-920 — find `_build_info_gathering_section`; locate the URGENCY block in BOTH the EN branch and the ES branch; per RESEARCH.md, EN URGENCY closing is around line 894 and ES URGENCY closing is around line 835. Verify exact line numbers in your read — do NOT trust the line numbers blindly; the function body is composed of local variables in Phase 60.3-10's restructure)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (jump to "## Proposed Tool-Invocation Trigger Phrasing (Fix 1)" — the verbatim EN and ES paragraphs are there)
    - C:/Users/leheh/.claude/projects/C--Users-leheh--Projects-homeservice-agent/memory/feedback_livekit_prompt_philosophy.md (binding: non-directive phrasing — "you have what you need to invoke", NOT "you must invoke")
  </read_first>

  <action>
**Step 1 — Read the function and locate the URGENCY block in both locales.**

Open `C:/Users/leheh/.Projects/livekit-agent/src/prompt.py` and read the full `_build_info_gathering_section` body. Per Phase 60.3 Plan 10, the function uses composable local variables: `preamble`, `name_use_block`, `service_address_block`, `phone_readback_block`, `name_required`, `urgency_block`. Locate the `urgency_block` for both `locale == "es"` and the default EN branch.

The URGENCY block (EN) ends with text describing how urgency is classified silently. The ES URGENCY block ends symmetrically. Identify the exact closing line of `urgency_block` in BOTH branches — that is your insertion point.

**Step 2 — Append the EN trigger paragraph at the end of the EN urgency_block.**

Append this VERBATIM paragraph (preserve newlines and spacing — these substrings are locked by Plan 03's tests):

```
Once you have the street, the postal code or zip code, the caller's day
preference (a specific date or weekday), and you've classified urgency
silently, you have what you need to check availability. The next step is
to invoke check_slot if the caller named both a date and a time, check_day
if they named a date without a time, or next_available_days if they're
vague about when. Speak the filler first, then invoke in the same turn —
the AVAILABILITY RULES below cover the rest.
```

When wiring into the existing string composition, you will likely append it as another `"..."` string concatenation segment within the EN branch's `urgency_block` assignment. Preserve the existing newline-separator pattern (typically `"\n\n"` between paragraphs within a block — match the surrounding style).

**Step 3 — Append the ES trigger paragraph at the end of the ES urgency_block.**

Append this VERBATIM paragraph in the ES branch:

```
Una vez que tenga la calle, el código postal, la preferencia de día del
llamante (una fecha específica o día de la semana), y haya clasificado
la urgencia en silencio, tiene lo que necesita para verificar
disponibilidad. El siguiente paso es invocar check_slot si el llamante
nombró fecha y hora, check_day si nombró una fecha sin hora, o
next_available_days si es vago sobre cuándo. Pronuncie el relleno primero,
luego invoque en el mismo turno — las REGLAS DE DISPONIBILIDAD de abajo
cubren el resto.
```

**Step 4 — Smoke-check.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_info_gathering_section, build_system_prompt; from unittest.mock import MagicMock; t = MagicMock(); t.side_effect = lambda *a, **k: ''; en = _build_info_gathering_section(t, 'postal code', 'en'); es = _build_info_gathering_section(t, 'postal code', 'es'); assert 'you have what you need to check availability' in en, 'EN trigger missing'; assert 'check_slot' in en and 'check_day' in en and 'next_available_days' in en, 'EN tool names missing'; assert 'street' in en and 'postal code or zip code' in en and 'day preference' in en and 'urgency' in en, 'EN four trigger fields missing'; assert 'you must invoke' not in en, 'EN hard imperative leaked'; assert 'silence is acceptable' not in en.lower(), 'EN silence license leaked'; assert 'tiene lo que necesita para verificar disponibilidad' in es, 'ES trigger missing'; assert 'calle' in es and 'código postal' in es and 'preferencia de día' in es and 'urgencia' in es, 'ES four trigger fields missing'; assert 'check_slot' in es and 'check_day' in es and 'next_available_days' in es; assert 'debe invocar' not in es, 'ES hard imperative leaked'; print('PASS')"
```

Should print `PASS`.

**Step 5 — Phase 60.3-10 invariant guard (info_gathering single-question framing).**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_info_gathering_section; from unittest.mock import MagicMock; t = MagicMock(); t.side_effect = lambda *a, **k: ''; en = _build_info_gathering_section(t, 'postal code', 'en').lower(); assert 'one' in en and 'at a time' in en, 'EN single-question framing missing'; print('PASS')"
```

Should print `PASS`. If not, the Phase 60.3-10 invariant was clobbered — restore.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_info_gathering_section; from unittest.mock import MagicMock; t = MagicMock(); t.side_effect = lambda *a, **k: ''; en = _build_info_gathering_section(t, 'postal code', 'en'); es = _build_info_gathering_section(t, 'postal code', 'es'); assert 'you have what you need to check availability' in en; assert all(x in en for x in ('check_slot', 'check_day', 'next_available_days')); assert all(x in en for x in ('street', 'postal code or zip code', 'day preference', 'urgency')); assert 'you must invoke' not in en; assert 'silence is acceptable' not in en.lower(); assert 'tiene lo que necesita para verificar disponibilidad' in es; assert all(x in es for x in ('calle', 'código postal', 'preferencia de día', 'urgencia')); assert all(x in es for x in ('check_slot', 'check_day', 'next_available_days')); assert 'debe invocar' not in es; en_low = en.lower(); assert 'one' in en_low and 'at a time' in en_low, 'Phase 60.3-10 single-question invariant clobbered'; print('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - `_build_info_gathering_section(t, 'postal code', 'en')` contains literal `"you have what you need to check availability"`.
    - EN result contains all three tool names: `check_slot`, `check_day`, `next_available_days`.
    - EN result contains all four trigger field names: `street`, `postal code or zip code`, `day preference`, `urgency`.
    - EN result does NOT contain `"you must invoke"`.
    - EN result does NOT contain `"silence is acceptable"` (case-insensitive).
    - `_build_info_gathering_section(t, 'postal code', 'es')` contains literal `"tiene lo que necesita para verificar disponibilidad"`.
    - ES result contains all four ES trigger field names: `calle`, `código postal`, `preferencia de día`, `urgencia`.
    - ES result contains all three tool names untranslated.
    - ES result does NOT contain `"debe invocar"`.
    - Phase 60.3-10 invariant preserved: EN result (lowercased) contains both `"one"` and `"at a time"`.
  </acceptance_criteria>

  <done>
    Fix 1 is live: the URGENCY block of `_build_info_gathering_section` ends with the four-precondition trigger paragraph in both EN and ES, with non-directive phrasing and no silence license. Phase 60.3-10 framing preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add SLOT TIMES cross-reference in `_build_outcome_words_section` (D-04)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent/src/prompt.py</files>

  <read_first>
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (read `_build_outcome_words_section` in full — both EN and ES branches; verify the current closing line of each branch's body so the cross-reference appends cleanly. The function landed in Phase 60.3-09 and has full EN+ES parity.)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-RESEARCH.md (jump to "### Combined verdict" under D-08 — the proposed cross-reference sentence is there; do NOT amend the OUTCOME WORDS body, ONLY append the cross-reference)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md (D-04 + D-08)
  </read_first>

  <action>
**Step 1 — Read both EN and ES branches of `_build_outcome_words_section` and identify the closing string segment of each.**

The EN body ends with a "correct path" coda (from Phase 60.3-09 SUMMARY: "correct-path coda"). The ES body ends symmetrically. The cross-reference is appended AFTER the existing coda — it is a SINGLE sentence; it does NOT modify any other line.

**Step 2 — Append the EN cross-reference sentence.**

At the end of the EN string body of `_build_outcome_words_section`, append this VERBATIM sentence as the final paragraph (separated by a `\n\n` from the previous paragraph in the existing concatenation pattern):

```
Specific clock times that the caller has not given you — including offered as confirmation questions — are governed by SLOT TIMES above.
```

**Step 3 — Append the ES cross-reference sentence.**

At the end of the ES string body, append this VERBATIM sentence:

```
Las horas específicas del reloj que el llamante no le ha dado — incluyendo las ofrecidas como preguntas de confirmación — están gobernadas por HORARIOS DE CITAS arriba.
```

**Step 4 — Verify the rest of OUTCOME WORDS body is untouched.**

Diff your edit and confirm: only ONE new paragraph added in EN and ONE in ES. The existing reserved-words enumeration, failure-mode example, and per-tool licensing language must be IDENTICAL to the pre-edit state. Per D-04 + D-08 verdict, we ADD a cross-reference; we do NOT amend the body.

**Step 5 — Smoke-check.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_outcome_words_section; en = _build_outcome_words_section('en'); es = _build_outcome_words_section('es'); assert 'governed by SLOT TIMES above' in en, 'EN cross-ref missing'; assert 'gobernadas por HORARIOS DE CITAS arriba' in es, 'ES cross-ref missing'; assert 'OUTCOME WORDS' in en and 'PALABRAS DE RESULTADO' in es, 'Existing OUTCOME WORDS body clobbered'; print('PASS')"
```

Should print `PASS`.

**Step 6 — Phase 60.3-09 OUTCOME WORDS invariant guard (regression check).**

The Phase 60.3-09 SUMMARY documented these invariants on the OUTCOME WORDS section: "verifiable facts require tool results; fabrication is worst failure mode" framing in BOTH locales. Confirm they survive:

```bash
cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_outcome_words_section; en = _build_outcome_words_section('en'); es = _build_outcome_words_section('es'); assert 'OUTCOME WORDS' in en; assert 'CRITICAL RULE' in en or 'REGLA CRÍTICA' in es; assert 'check_slot' in en and 'book_appointment' in en; assert 'check_slot' in es and 'book_appointment' in es; print('PASS')"
```

Should print `PASS`.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && python -c "from src.prompt import _build_outcome_words_section; en = _build_outcome_words_section('en'); es = _build_outcome_words_section('es'); assert 'governed by SLOT TIMES above' in en, 'EN SLOT TIMES cross-ref missing'; assert 'gobernadas por HORARIOS DE CITAS arriba' in es, 'ES SLOT TIMES cross-ref missing'; assert 'OUTCOME WORDS' in en and 'PALABRAS DE RESULTADO' in es, 'OUTCOME WORDS section bodies clobbered'; assert 'check_slot' in en and 'book_appointment' in en, 'EN tool refs in body missing (regression)'; assert 'check_slot' in es and 'book_appointment' in es, 'ES tool refs in body missing (regression)'; print('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - `_build_outcome_words_section("en")` contains literal `"governed by SLOT TIMES above"`.
    - `_build_outcome_words_section("es")` contains literal `"gobernadas por HORARIOS DE CITAS arriba"`.
    - EN body still contains `"OUTCOME WORDS"` (header preserved).
    - ES body still contains `"PALABRAS DE RESULTADO"` (header preserved).
    - EN body still references both `check_slot` and `book_appointment` (Phase 60.3-09 invariant).
    - ES body still references both `check_slot` and `book_appointment` (Phase 60.3-09 invariant).
    - The diff against pre-edit state shows only ONE new paragraph added in EN and ONE in ES (no other lines modified — checkable via `git diff src/prompt.py | grep ^+ | wc -l` should be small, ~4-6 lines including the additions and the surrounding string-concat punctuation).
  </acceptance_criteria>

  <done>
    D-04 cross-reference is live: OUTCOME WORDS now points readers (and the model) at SLOT TIMES for the specific-time-fabrication case. The OUTCOME WORDS body itself is unchanged per D-08 verdict.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit livekit-agent prompt edits</name>
  <files>(commits only — livekit-agent repo)</files>

  <read_first>
    (none — pure git operation)
  </read_first>

  <action>
**Step 1 — Verify only `src/prompt.py` is dirty in livekit-agent.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git status --short
```

Should show `M  src/prompt.py` and nothing else dirty (no test files yet — Plan 03 owns tests).

**Step 2 — Inspect the diff briefly to make sure it is the THREE intended edits (new function, composition wiring, URGENCY trigger paragraph EN+ES, OUTCOME WORDS cross-ref EN+ES).**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git diff src/prompt.py | head -200
```

Sanity-check that:
- A new `def _build_slot_times_section` definition is present.
- The `sections = [` list has a new line calling `_build_slot_times_section(locale)`.
- The URGENCY block in `_build_info_gathering_section` has a new EN paragraph + ES paragraph.
- The end of `_build_outcome_words_section` body has the new cross-reference (EN+ES).
- No other functions are modified.

**Step 3 — Stage and commit.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git add src/prompt.py && git commit -m "feat(prompt): add SLOT TIMES rule + tool-invocation trigger (Phase 61.2 Fix 1+2)"
```

**Step 4 — Verify commit.**

```bash
cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline -1
```

Should show subject containing `61.2` and `SLOT TIMES`.

Note: This commit is on livekit-agent. The homeservice_agent repo is NOT touched in this plan. Plan 05 (D-16) updates the homeservice_agent skill doc.

**Important:** Do NOT push to remote unless the user explicitly requests it. Plan 04 (D-12 live UAT) needs Railway to redeploy from this commit, so the user will likely push after Plan 03 (tests) lands GREEN. The execute-phase orchestrator handles deploy timing.
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline -1 | grep -q "61.2" && git log --oneline -1 | grep -q "SLOT TIMES" && git diff HEAD~1 HEAD --stat | grep -q "src/prompt.py" && [ "$(git diff HEAD~1 HEAD --name-only | wc -l)" = "1" ] && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - livekit-agent has a new commit with subject containing both `61.2` and `SLOT TIMES`.
    - The commit modifies exactly 1 file: `src/prompt.py`.
    - `git status` is clean post-commit.
    - The prior commit on livekit-agent (one before HEAD) is NOT clobbered (no `--amend` was used).
  </acceptance_criteria>

  <done>
    Plan 02 ships as a single commit on livekit-agent. Three coherent edits in one file. Plan 03 (tests) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt.py → live LiveKit + Gemini 3.1 Flash Live | Prompt edits change model behavior on every production call. A regression here = production failure. |
| section composition order | Order is load-bearing per D-08. Misplacing SLOT TIMES (e.g., after `_build_tool_narration_section`) loses the top-attention anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.2-02 | Information Disclosure (false-positive booking) | `_build_slot_times_section` + composition | mitigate | New CRITICAL RULE explicitly prohibits proposing a time the caller did not say, including as a confirmation question. The `"This includes proposing it as a confirmation question"` substring is the load-bearing clause that maps directly to the incident `AJ_W7woUYGhG5CV` failure-mode example ("Thursday at 10 AM"). |
| T-61.2-03 | DoS (silence-deadlock regression — Phase 61.1 lesson) | new section bodies | mitigate | New section explicitly omits any `"Silence is acceptable"` / `"el silencio es aceptable"` substring (locked at smoke-check + Plan 03 tests). Hard-imperative phrasing is also avoided in Fix 1 (`"you have what you need to invoke"` not `"you must invoke"`). The Phase 61.1 invariant `"After book_appointment or capture_lead returns"` in `_build_address_validation_section` is regression-checked in Task 1 Step 5. |
</threat_model>

<verification>
- `_build_slot_times_section` exists with EN + ES bodies matching RESEARCH.md verbatim wording.
- `build_system_prompt` composition list has SLOT TIMES inserted between ADDRESS VALIDATION and OUTCOME WORDS.
- `_build_info_gathering_section` URGENCY block ends with the four-precondition trigger paragraph in both locales.
- `_build_outcome_words_section` ends with the SLOT TIMES cross-reference in both locales.
- Phase 61.1 address-validation invariant preserved (`"After book_appointment or capture_lead returns"` still in EN).
- Phase 60.3-09 OUTCOME WORDS invariants preserved (header + tool refs).
- Phase 60.3-10 info_gathering single-question invariant preserved.
- One livekit-agent commit, one file, three logical edits.
</verification>

<success_criteria>
- All four tasks pass their `<verify><automated>` blocks.
- livekit-agent has exactly one new commit on top of the prior state.
- No homeservice_agent changes in this plan.
- No tests modified (Plan 03 owns tests).
</success_criteria>

<output>
After completion, create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-02-SUMMARY.md` documenting:
- livekit-agent commit SHA.
- Exact line numbers where each of the three edits landed (post-edit `prompt.py` line numbers).
- Confirmation that all three smoke checks (Tasks 1, 2, 3) passed.
- Any deviations.
</output>
