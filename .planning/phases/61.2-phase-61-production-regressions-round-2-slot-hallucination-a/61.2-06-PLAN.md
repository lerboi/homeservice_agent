---
phase: 61.2
plan: 06
type: execute
wave: 3
depends_on: [61.2-05]
files_modified:
  - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md
autonomous: false
requirements:
  - D-12-rev
threats_addressed:
  - T-61.2-CASCADE-UAT

must_haves:
  truths:
    - "A live LiveKit SIP UAT call is placed against the test tenant AFTER Plans 02-05 are merged to `livekit-agent/main` and Railway has redeployed."
    - "The new `[goodbye_race]` log line includes `server_tool_cancellations` and `orphaned_server_content` counters from Fix C — confirming the telemetry pipeline works end-to-end."
    - "Cascade rate is strictly less than the AJ_vV4DM5AG9t7W baseline of 5 cancellations / 230s."
    - "No agent turn in the new call's `transcript_structured` is fragmented (no leading-space continuation pairs, no mid-word cuts)."
    - "The call reaches a clean `check_day` or `check_slot` invocation and either books OR cleanly captures a lead — NOT a not_attempted hangup."
  artifacts:
    - path: "C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md"
      provides: "Post-fix UAT evidence with verdict"
      contains: "Cascade verdict"
  key_links:
    - from: "61.2-UAT.md"
      to: "61.2-PRE-FIX-UAT.md"
      via: "comparison-link"
      pattern: "AJ_vV4DM5AG9t7W"
---

<objective>
Wave 3 — D-12 mandatory ship gate. Live LiveKit SIP UAT call against the test tenant after Plans 02-05 are deployed. Compare cancellation count and transcript-fragmentation pattern against the pre-fix baseline (`61.2-PRE-FIX-UAT.md`). NO ship without this gate per `61.2-CONTEXT.md` D-12-rev. Phase 61.1 lesson: invariant tests are necessary, NOT sufficient — only a real call exercises Gemini-server VAD behavior.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-PRE-FIX-UAT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy livekit-agent branch + place D-12 UAT call</name>
  <files>C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md</files>

  <read_first>
    - 61.2-PRE-FIX-UAT.md (baseline to compare against)
    - 61.2-CONTEXT.md D-12-rev (success criteria)
  </read_first>

  <action>
**Why manual:** Behavior under test is Gemini-server-side VAD + cancellation cascade. Only a real call exercises it. No CLI/API simulates it faithfully. (Same posture as Phase 63 / 63.1 / 63.2 D-09 gate.)

**Step-by-step:**

1. **Merge `livekit-agent` feature branch to `main`.** Plans 02-05 should each have left one commit on the branch (`fix(61.2-A)`, `fix(61.2-B)`, `fix(61.2-C)`, `test(61.2)`). Push to origin and merge per the branch-discipline pattern from Phase 63 D-02.

2. **Wait for Railway redeploy.** Confirm new pod is live by checking the Railway log for the `registered worker` line containing the new agent_name + `version=1.5.7`.

3. **Place a live LiveKit SIP test call** against the test tenant (caller `+6587528516` → tenant "make it ai" / `24141cd0-5735-4c7a-82d5-994c6b821861`). Use the same SIP path as the pre-fix UAT.

4. **Walk a normal info-gathering script** mirroring the AJ_vV4DM5AG9t7W shape:
   - "Yes, I need a plumber, my toilet is leaking."
   - Provide service address (street + unit).
   - Provide postal code.
   - Provide a day preference ("today" or "tomorrow").
   - Confirm the readback if asked.
   - Allow the agent to call `check_day` or `check_slot` and propose a slot.
   - Either accept the slot (book) OR push back ("nothing works for me today, can someone call me back?") and let the agent fall through to capture_lead.

5. **Pull telemetry from Railway logs:**
   - Find the `[goodbye_race]` JSON log line for the new call.
   - Extract `server_tool_cancellations` and `orphaned_server_content` counters.
   - Note the call_id (LiveKit Job ID, `AJ_…`).

6. **Pull `transcript_structured` from Supabase** (`node .planning/queries/_fetch_call.cjs` — edit the script's `callRoom` constant to match the new call_id). Inspect for:
   - Any agent turn ending mid-word.
   - Any TWO consecutive agent turns where the second starts with a leading space (cancellation continuation pattern).
   - The path the agent took: did `check_day` / `check_slot` fire? Did the call book or capture a lead?

7. **Fill in `61.2-UAT.md`** at `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/61.2-…/61.2-UAT.md` using this template:

```markdown
# Phase 61.2 — D-12 Post-Fix UAT Evidence

**Placed:** {YYYY-MM-DD HH:MM TZ}
**Caller:** +6587528516 (test SIP)
**Tenant:** make it ai (24141cd0-5735-4c7a-82d5-994c6b821861)
**Call ID:** AJ_… (post-fix)
**Duration:** {N seconds}
**booking_outcome:** {booked|attempted|declined|not_attempted}
**disconnection_reason:** {value}

## Telemetry from Fix C

- `server_tool_cancellations`: **{N}** (baseline AJ_vV4DM5AG9t7W: 3 logged + 2 inferred = 5)
- `orphaned_server_content`: **{N}** (baseline: 1)
- Total cascade events: **{N}** vs baseline 5

## Transcript fragmenting check

- Mid-word cuts: YES / NO ({count} examples)
- Leading-space-continuation pairs: YES / NO ({count} examples)
- Sample fragmented turns (if any): {paste}

## Tool flow

- `check_caller_history` invocations: {count} (any cancelled? YES/NO)
- `check_day` / `check_slot` / `next_available_days` invocations: {count}
- Final tool path: {e.g. "check_day → check_slot → book_appointment ✓" OR "check_day → capture_lead ✓"}

## Verdict

- Cascade rate strictly < 5? **{YES / NO}**
- No transcript fragmenting? **{YES / NO}**
- Booking OR clean lead-capture? **{YES / NO}**

**Verdict:** {merge | gap}

## Comparison to PRE-FIX-UAT

{1-3 sentences contrasting AJ_vV4DM5AG9t7W (cascade-driven hangup) with this call's outcome.}

## If verdict = gap

{Describe what failed and recommend whether to triage as 61.3 or as additional 61.2 work.}
```

8. **Commit the UAT file** (Task 2 below).

**Resume signal:** Reply `merge` (verdict passed all 3 criteria) or `gap` (any criterion failed; planner triages).
  </action>

  <verify>
    <automated>MANUAL — gate satisfied when 61.2-UAT.md contains the four load-bearing fields (call ID, server_tool_cancellations count, fragmenting YES/NO, verdict)</automated>
  </verify>

  <resume-signal>Reply `merge` if all D-12-rev criteria passed, or `gap` if any failed. Either way, fill in 61.2-UAT.md before responding.</resume-signal>

  <acceptance_criteria>
    - File `61.2-UAT.md` exists.
    - File contains a Call ID matching `AJ_` (post-fix).
    - File contains a numeric `server_tool_cancellations` count (NOT a placeholder).
    - File contains a YES/NO answer for "Cascade rate strictly < 5".
    - File contains a YES/NO answer for "No transcript fragmenting".
    - File contains a final `**Verdict:**` line that is either `merge` or `gap`.
  </acceptance_criteria>

  <done>
    UAT placed. Verdict committed. Wave 4 unblocked (only if verdict = merge).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit UAT evidence</name>
  <files>(commit only — homeservice_agent repo)</files>

  <action>
```bash
cd C:/Users/leheh/.Projects/homeservice_agent && git add .planning/phases/61.2-phase-61-production-regressions-round-2-slot-hallucination-a/61.2-UAT.md && git commit -m "docs(61.2): D-12 post-fix UAT evidence — verdict={merge|gap}

Live UAT against test tenant after Plans 02-05 deployed. Telemetry +
transcript captured. Compared to AJ_vV4DM5AG9t7W baseline (5 cascade
events, fragmented turns, not_attempted hangup).
"
```
(Replace `{merge|gap}` with the actual verdict from Task 1.)
  </action>

  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && git log --oneline -1 | grep -q "61.2.*UAT" && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    - One commit on homeservice_agent main containing the new UAT file.
    - Commit message reflects the actual verdict.
  </acceptance_criteria>

  <done>
    Evidence committed.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-61.2-CASCADE-UAT | Repudiation (shipping fixes that don't work in production) | LiveKit agent + Gemini Live | mitigate | Mandatory live UAT against test tenant before declaring phase complete. Telemetry counters from Fix C give objective cascade-rate measurement. |
</threat_model>

<verification>
- 61.2-UAT.md exists with all required fields populated.
- Verdict is `merge` (success) or `gap` (failure) — never empty.
- One commit on homeservice_agent main.
- If verdict = gap, planner queues 61.3 (or additional 61.2 work) before any merge to livekit-agent main.
</verification>

<success_criteria>
- D-12-rev passes all three criteria (cascade rate < 5, no fragmenting, booking-or-lead-capture).
- UAT file is filled in completely.
- Evidence committed.
</success_criteria>

<output>
Write `61.2-06-SUMMARY.md` recording: post-fix call ID, cascade counts, verdict, comparison to baseline. Reference UAT file SHA.
</output>
