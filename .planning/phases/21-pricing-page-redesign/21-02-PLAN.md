---
phase: 21-pricing-page-redesign
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/app/(public)/pricing/ComparisonTable.jsx
  - src/app/(public)/pricing/page.js
  - src/app/(public)/pricing/FAQSection.jsx
  - src/app/(public)/contact/ContactForm.jsx
  - .claude/skills/public-site-i18n/SKILL.md
autonomous: false
requirements: [PR21-04, PR21-05, PR21-06, PR21-07, PR21-08]
must_haves:
  truths:
    - "Comparison table shows volume-based rows where all features are available on all paid tiers"
    - "Growth column in comparison table is visually highlighted"
    - "Testimonial section with 2 quotes appears between comparison table and FAQ"
    - "FAQ has 8 questions covering setup, AI quality, billing, and security on dark background"
    - "No money-back or no-credit-card messaging remains in FAQ"
    - "Enterprise CTA navigating to /contact?type=sales pre-selects sales inquiry type"
    - "Pricing page renders correctly on mobile at 375px"
  artifacts:
    - path: "src/app/(public)/pricing/ComparisonTable.jsx"
      provides: "Volume-based comparison with Growth highlight"
      contains: "ring-[#F97316]"
    - path: "src/app/(public)/pricing/FAQSection.jsx"
      provides: "8-question dark FAQ"
      contains: "How long does setup take"
    - path: "src/app/(public)/pricing/page.js"
      provides: "Testimonial section and dark FAQ wrapper"
      contains: "bg-[#1A1816]"
    - path: "src/app/(public)/contact/ContactForm.jsx"
      provides: "URL param pre-selection for inquiry type"
      contains: "useSearchParams"
  key_links:
    - from: "src/app/(public)/pricing/ComparisonTable.jsx"
      to: "src/app/(public)/pricing/pricingData.js"
      via: "import { COMPARISON_FEATURES, PRICING_TIERS }"
      pattern: "COMPARISON_FEATURES"
    - from: "src/app/(public)/contact/ContactForm.jsx"
      to: "next/navigation"
      via: "import { useSearchParams }"
      pattern: "useSearchParams"
---

<objective>
Update comparison table to volume-based structure with Growth column highlight, add testimonial section, rewrite FAQ to 8 dark-themed questions, add contact form query param pre-selection, and update the public-site-i18n skill file.

Purpose: Complete the pricing page transformation — testimonials add social proof, expanded FAQ addresses buyer objections, comparison table reflects honest volume-based tiers, and Enterprise CTA friction is reduced with pre-selected inquiry type.
Output: Updated ComparisonTable.jsx, FAQSection.jsx, page.js (testimonial + dark FAQ sections), ContactForm.jsx, and public-site-i18n SKILL.md.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-pricing-page-redesign/21-CONTEXT.md
@.planning/phases/21-pricing-page-redesign/21-UI-SPEC.md
@.planning/phases/21-pricing-page-redesign/21-RESEARCH.md
@.planning/phases/21-pricing-page-redesign/21-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — pricingData.js updated structure -->
From src/app/(public)/pricing/pricingData.js (after Plan 01):
```js
export const PRICING_TIERS = [
  { id: 'starter', name: 'Starter', highlighted: false, ... },
  { id: 'growth', name: 'Growth', highlighted: true, badge: 'Most Popular', ... },
  { id: 'scale', name: 'Scale', highlighted: false, ... },
  { id: 'enterprise', name: 'Enterprise', highlighted: false, ... },
];

export const COMPARISON_FEATURES = [
  { name: 'Monthly calls', starter: '40', growth: '120', scale: '400', enterprise: 'Unlimited' },
  // ... 13 rows total, volume-based, no feature gating except Custom integrations (enterprise only)
];
```

From src/app/components/landing/AnimatedSection.jsx:
```jsx
export function AnimatedSection({ children, direction, className }) { ... }
export function AnimatedStagger({ children, className }) { ... }
export function AnimatedItem({ children, className }) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ComparisonTable for volume-based rows with Growth column highlight</name>
  <files>src/app/(public)/pricing/ComparisonTable.jsx</files>
  <read_first>
    - src/app/(public)/pricing/ComparisonTable.jsx
    - src/app/(public)/pricing/pricingData.js
    - .planning/phases/21-pricing-page-redesign/21-UI-SPEC.md
  </read_first>
  <action>
Rewrite ComparisonTable.jsx to render the updated volume-based COMPARISON_FEATURES from pricingData.js. Per D-14, UI-SPEC Section 3.

Keep: Server component (no 'use client'), AnimatedSection wrapper, imports from pricingData.js, Check/lucide-react import, overall table structure with `overflow-x-auto` for mobile scroll.

**Changes to table header row:**
- Add Growth column visual highlight: when rendering tier headers, check if `tier.highlighted === true` (or `tier.id === 'growth'`). If so, apply `text-[#F97316] font-semibold` to that `<th>`. Other tier headers keep `text-[#0F172A] font-semibold text-sm`.

**Changes to table body cells:**
- For the Growth column cells, add a subtle background highlight: apply `bg-[#FFF7ED]` (very light orange tint) to the Growth `<td>` cells to create a vertical column highlight effect. This pairs with the `text-[#F97316]` header.
- Keep existing rendering logic: `true` = Check icon in `text-[#F97316]`, `false` = dash in `text-[#94A3B8]`, string = text in `text-[#0F172A] font-medium`.
- Keep alternating row backgrounds: even rows `bg-stone-50/60`, odd rows default.
- Keep border: `divide-y divide-stone-200/60` or `border-b border-stone-100`.

The column highlight approach: In the `<td>` render, check if `tierId === 'growth'` and conditionally add `bg-[#FFF7ED]`.

Do NOT change the table's `bg-white` container, the `rounded-2xl border border-stone-200/60` outer div, or the `min-w-[640px]` minimum width for mobile scroll.
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -q "text-\[#F97316\].*font-semibold" src/app/\(public\)/pricing/ComparisonTable.jsx && grep -q "growth" src/app/\(public\)/pricing/ComparisonTable.jsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonTable.jsx renders Growth column header with `text-[#F97316]`
    - ComparisonTable.jsx checks `tier.highlighted` or `tier.id === 'growth'` for column highlighting
    - ComparisonTable.jsx renders Check icon for `true` values, dash for `false`, string for text values
    - ComparisonTable.jsx preserves `overflow-x-auto` for mobile horizontal scroll
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Comparison table renders volume-based rows from updated pricingData.js with Growth column visually highlighted in #F97316</done>
</task>

<task type="auto">
  <name>Task 2: Add testimonial section to page.js, rewrite FAQ to 8 dark questions, update contact form pre-selection</name>
  <files>src/app/(public)/pricing/page.js, src/app/(public)/pricing/FAQSection.jsx, src/app/(public)/contact/ContactForm.jsx</files>
  <read_first>
    - src/app/(public)/pricing/page.js
    - src/app/(public)/pricing/FAQSection.jsx
    - src/app/(public)/contact/ContactForm.jsx
    - src/app/(public)/contact/page.js
    - .planning/phases/21-pricing-page-redesign/21-UI-SPEC.md
  </read_first>
  <action>
**page.js — Add testimonial section and convert FAQ section to dark (per D-08, D-09, UI-SPEC Section 4/5):**

Insert a new testimonial section BETWEEN the comparison table section and the FAQ section. The section order after changes: Hero -> Tier Cards -> Comparison Table -> **Testimonials** -> FAQ -> CTA Banner.

Testimonial section markup (inline in page.js — no new component file):
```jsx
{/* Testimonials (dark) */}
<section className="bg-[#1A1816] py-16">
  <div className="max-w-4xl mx-auto px-6">
    <AnimatedStagger className="grid md:grid-cols-2 gap-8">
      <AnimatedItem>
        <div className="bg-white/[0.04] border border-white/[0.06] rounded-xl p-8">
          <p className="text-6xl font-serif text-[#F97316]/30 leading-none mb-4">&ldquo;</p>
          <p className="text-xl text-white/80 italic leading-relaxed">
            Before Voco, I was losing 3-4 calls every weekend. Now my phone&apos;s booked Monday before I&apos;ve had coffee.
          </p>
          <p className="text-sm text-white/50 mt-4">&mdash; Mike R., HVAC contractor, Phoenix AZ</p>
        </div>
      </AnimatedItem>
      <AnimatedItem>
        <div className="bg-white/[0.04] border border-white/[0.06] rounded-xl p-8">
          <p className="text-6xl font-serif text-[#F97316]/30 leading-none mb-4">&ldquo;</p>
          <p className="text-xl text-white/80 italic leading-relaxed">
            Setup took 4 minutes. I heard my AI answer a call with my business name before I even finished my first cup.
          </p>
          <p className="text-sm text-white/50 mt-4">&mdash; Sandra T., Plumbing company owner, Austin TX</p>
        </div>
      </AnimatedItem>
    </AnimatedStagger>
  </div>
</section>
```

Add `AnimatedStagger, AnimatedItem` to the imports from `@/app/components/landing/AnimatedSection` in page.js (currently only imports `AnimatedSection`).

Change the FAQ section wrapper in page.js from:
```jsx
<section className="bg-[#F5F5F4] py-20 border-t border-stone-200/60">
```
to:
```jsx
<section className="bg-[#050505] py-20">
```

Change the FAQ heading from `text-[#0F172A]` to `text-white`. Change heading text from "Frequently Asked Questions" to "Questions from the field".

**FAQSection.jsx — Rewrite to 8 dark-themed FAQ items (per D-15 through D-21, UI-SPEC Section 5):**

Replace the entire FAQ_ITEMS array with these 8 questions (verbatim from UI-SPEC):

```js
const FAQ_ITEMS = [
  {
    q: 'How long does setup take?',
    a: "Under 5 minutes. You'll enter your business name, choose your trade, and hear your AI receptionist answer a real call — all before your first coffee break. No technical skills needed.",
  },
  {
    q: 'Do I need any technical skills to use Voco?',
    a: "None at all. If you can fill out a form on your phone, you can set up Voco. We handle the AI, the phone number, and the call routing — you just show up for the jobs.",
  },
  {
    q: "Can callers tell they're talking to an AI?",
    a: "Some might sense it, but most don't stop to ask. What matters is that your phone gets answered in one ring, every time, with your business name and a professional tone. Callers care more about being helped than who's helping.",
  },
  {
    q: 'What if the AI gets confused on a call?',
    a: "Voco is built for this. If a caller asks something outside the AI's scope, it captures their info and routes the call to you with a full summary. You never lose a lead — even on edge cases.",
  },
  {
    q: 'How does the 14-day trial work?',
    a: "Sign up, complete the 5-minute setup, and your AI receptionist starts answering calls immediately. You have 14 full days to evaluate Voco with real calls. No payment required to start.",
  },
  {
    q: 'Can I cancel anytime?',
    a: "Yes. No lock-in contracts. Cancel from your dashboard settings at any time, and your service stops at the end of the current billing period. No cancellation fees.",
  },
  {
    q: 'What happens if I go over my call limit?',
    a: "We don't cut you off mid-month. Calls beyond your plan limit are charged at a transparent per-call overage rate shown on your invoice. You can upgrade anytime to get a better per-call rate.",
  },
  {
    q: 'Where are call recordings stored? Who can access them?',
    a: "Recordings are stored on encrypted servers in the US. Only your account can access your recordings and transcripts — no one else sees your call data. Voco does not share or sell your data.",
  },
];
```

Convert accordion styling to dark treatment:
- Item border: change `border-[#0F172A]/10` to `border-white/[0.08]`
- Trigger text: change `text-[#0F172A]` to `text-white`
- Answer text: change `text-[#475569]` to `text-white/60`

Keep: Radix Accordion structure, `type="single" collapsible`, ChevronDown icon with `text-[#F97316]` and rotation, animation classes `data-[state=open]:animate-accordion-down data-[state=closed]:animate-accordion-up`, AnimatedSection wrapper.

**ContactForm.jsx — Add useSearchParams pre-selection (per D-01 Enterprise CTA):**

Add `import { useSearchParams } from 'next/navigation';` at the top.

Inside the `ContactForm` function, add before the return:
```jsx
const searchParams = useSearchParams();
const preselectedType = searchParams.get('type') || '';
```

Change the select element's `defaultValue=""` to `defaultValue={preselectedType}`.

That is the only change to ContactForm.jsx. If `npm run build` warns about a Suspense boundary, also update `src/app/(public)/contact/page.js` to wrap `<ContactForm />` in `<Suspense fallback={null}>` (importing Suspense from 'react').
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -q "Mike R" src/app/\(public\)/pricing/page.js && grep -q "Sandra T" src/app/\(public\)/pricing/page.js && grep -q "bg-\[#050505\]" src/app/\(public\)/pricing/page.js && grep -c "q:" src/app/\(public\)/pricing/FAQSection.jsx && grep -q "useSearchParams" src/app/\(public\)/contact/ContactForm.jsx && ! grep -qi "money.back" src/app/\(public\)/pricing/FAQSection.jsx && ! grep -qi "credit card" src/app/\(public\)/pricing/FAQSection.jsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.js contains testimonial section with `bg-[#1A1816]` between comparison and FAQ sections
    - page.js contains "Mike R" and "Sandra T" testimonial attributions
    - page.js imports AnimatedStagger and AnimatedItem
    - page.js FAQ section wrapper has `bg-[#050505]` (not `bg-[#F5F5F4]`)
    - page.js FAQ heading text is "Questions from the field" in `text-white`
    - FAQSection.jsx contains exactly 8 FAQ items (8 occurrences of `q:`)
    - FAQSection.jsx contains "How long does setup take?" as first question
    - FAQSection.jsx contains "Where are call recordings stored?" as last question
    - FAQSection.jsx does NOT contain "money-back" or "credit card" anywhere
    - FAQSection.jsx does NOT contain "refund policy" anywhere
    - FAQSection.jsx accordion items use `border-white/[0.08]` border
    - FAQSection.jsx trigger text uses `text-white`
    - FAQSection.jsx answer text uses `text-white/60`
    - ContactForm.jsx imports `useSearchParams` from `next/navigation`
    - ContactForm.jsx select defaultValue uses `preselectedType` variable
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Testimonial section with 2 quotes renders between comparison and FAQ, FAQ has 8 dark-themed questions with no prohibited messaging, Enterprise CTA pre-selects sales inquiry on contact page, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Update public-site-i18n skill file to reflect pricing page changes</name>
  <files>.claude/skills/public-site-i18n/SKILL.md</files>
  <read_first>
    - .claude/skills/public-site-i18n/SKILL.md
  </read_first>
  <action>
Per CLAUDE.md requirement: "After changes are made, update the skill file to reflect the new version of the code."

Find the section in SKILL.md that documents the pricing page (likely Section 7 or a "Pricing Page" heading). Update it to reflect:

1. **Page layout**: 6 sections — Hero (dark #050505 with dot-grid + blur orb), Tier Cards (dark, with trial banner above billing toggle), Comparison Table (light #F5F5F4), Testimonials (dark #1A1816, 2 quotes), FAQ (dark #050505, 8 questions), CTA Banner (dark warm #1C1412).

2. **Data structure**: pricingData.js has volume-based PRICING_TIERS (all features on all paid tiers, differentiated by call volume and support level only). COMPARISON_FEATURES has 13 rows. Enterprise CTA routes to `/contact?type=sales`.

3. **CTA labels**: All paid tiers use "Start Free Trial" linking to `/onboarding`. Enterprise uses "Contact Us" linking to `/contact?type=sales`.

4. **FAQ**: 8 questions in 4 topic areas (setup, AI quality, billing, security) on dark background. Radix accordion, dark treatment (white text, white/60 answers, white/08 borders).

5. **Testimonials**: Inline in page.js (not a separate component). Two-quote layout with AnimatedStagger.

6. **Dark card treatment**: `bg-[#1A1816]`, `border-white/[0.06]`, copper hover glow, text hierarchy (white, white/50, white/70, white/40).

7. **No money-back guarantee or no-credit-card-required messaging anywhere on pricing page.**

Only update the pricing-relevant section — do not change other sections of SKILL.md.
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -q "volume-based" .claude/skills/public-site-i18n/SKILL.md && grep -q "Start Free Trial" .claude/skills/public-site-i18n/SKILL.md && grep -q "contact?type=sales" .claude/skills/public-site-i18n/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md pricing section mentions "volume-based" tier structure
    - SKILL.md mentions "Start Free Trial" as CTA label
    - SKILL.md mentions `/contact?type=sales` for Enterprise
    - SKILL.md documents 6 page sections including testimonials
    - SKILL.md documents 8 FAQ questions
    - SKILL.md documents dark card treatment colors
  </acceptance_criteria>
  <done>public-site-i18n SKILL.md pricing section updated to reflect volume-based tiers, dark treatment, testimonials, expanded FAQ, and updated CTAs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of complete pricing page redesign</name>
  <files>src/app/(public)/pricing/page.js</files>
  <action>
Human verifies the complete pricing page redesign at localhost:3000/pricing. All automated tasks are complete — this checkpoint confirms visual correctness and interaction quality.
  </action>
  <what-built>Complete pricing page redesign: dark hero with dot-grid/blur-orb, trial banner, dark tier cards, volume-based comparison table with Growth highlight, 2-quote testimonial section, 8-question dark FAQ, updated CTA banner, Enterprise contact pre-selection</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/pricing
    2. Verify hero section has dark #050505 background with visible dot-grid texture and orange blur orb
    3. Verify eyebrow pill says "AI Receptionist for Trades" with pulsing orange dot
    4. Verify "14-Day Free Trial - Cancel Anytime" banner appears above the billing toggle
    5. Toggle between Monthly and Annual — prices should update on all 4 cards
    6. Verify all tier cards have dark backgrounds with copper glow on hover
    7. Verify Growth card has "Most Popular" badge and orange ring highlight
    8. Verify Starter/Growth/Scale CTAs say "Start Free Trial" and link to /onboarding
    9. Verify Enterprise CTA says "Contact Us"
    10. Click Enterprise "Contact Us" — verify it goes to /contact?type=sales and the inquiry dropdown is pre-selected to "Sales"
    11. Scroll to comparison table — verify it has light #F5F5F4 background and Growth column is highlighted in orange
    12. Verify comparison table shows all features available on all paid tiers (checkmarks across the board), with only call volume, annual calls, and support level differing
    13. Scroll to testimonials — verify 2 quotes from Mike R. and Sandra T. appear on dark background
    14. Scroll to FAQ — verify 8 questions on dark background, accordion opens/closes smoothly
    15. Verify NO mention of "money-back guarantee" or "no credit card required" anywhere
    16. Scroll to CTA banner — verify "Every missed call is a job your competitor booked." headline with "Start Free Trial" button
    17. Resize browser to 375px width — verify cards stack vertically, comparison table scrolls horizontally, all sections readable
  </how-to-verify>
  <verify>Human visual inspection at localhost:3000/pricing</verify>
  <done>User approves pricing page visual design, interactions, and mobile responsiveness</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes (no import/JSX errors)
- `grep -ri "money.back\|credit card required\|Get Started" src/app/(public)/pricing/` returns no matches
- `grep -c "q:" src/app/(public)/pricing/FAQSection.jsx` returns 8
- `grep "useSearchParams" src/app/(public)/contact/ContactForm.jsx` returns a match
- All 8 success criteria from ROADMAP verified at localhost:3000/pricing
</verification>

<success_criteria>
- Comparison table uses volume-based data with Growth column highlighted
- Testimonial section between comparison and FAQ with 2 quotes
- FAQ has exactly 8 questions on dark background, no prohibited messaging
- Enterprise CTA pre-selects sales inquiry on contact page
- Mobile viewport renders correctly
- Skill file updated to reflect all changes
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-pricing-page-redesign/21-02-SUMMARY.md`
</output>
