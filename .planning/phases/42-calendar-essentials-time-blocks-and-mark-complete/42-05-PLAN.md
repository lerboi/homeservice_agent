---
phase: 42-calendar-essentials-time-blocks-and-mark-complete
plan: 05
type: execute
wave: 3
depends_on: ["42-02", "42-03"]
files_modified: []
autonomous: false
requirements: [CAL-05, CAL-06, CAL-07, CAL-08, CAL-09]

must_haves:
  truths:
    - "Time blocks render as hatched full-width elements behind appointments"
    - "Mark Complete flow works end-to-end with undo"
    - "Completed appointments show at 40% opacity with checkmark"
    - "Show completed toggle hides/shows completed appointments"
---

<objective>
Visual verification checkpoint for all UI work in Phase 42.

Purpose: Confirm that time blocks and mark-complete features look and work correctly in the browser before considering the phase complete.

Output: User approval of visual and interactive behavior.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-calendar-essentials-time-blocks-and-mark-complete/42-UI-SPEC.md
@.planning/phases/42-calendar-essentials-time-blocks-and-mark-complete/42-02-SUMMARY.md
@.planning/phases/42-calendar-essentials-time-blocks-and-mark-complete/42-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and Interactive Verification of Time Blocks and Mark Complete</name>
  <files>src/app/dashboard/calendar/page.js</files>
  <action>
    Run `npm run dev` and verify all Phase 42 UI features in the browser at `/dashboard/calendar`. This is a human verification checkpoint — no code changes needed.
  </action>
  <what-built>
    Time block creation/editing/deletion via Sheet, time block hatched rendering on calendar, mark-complete flow with expandable notes and undo toast, completed appointment 40% opacity + checkmark badge, show completed toggle with localStorage persistence.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to the Calendar page at `/dashboard/calendar`
    2. **Time Block Create:** Click the "+" button in the toolbar. Verify the Sheet opens with Title, Date, Start time, End time, All-day toggle, and Note fields. Enter "Lunch break" as title, pick today's date, set 12:00-13:00. Click "Save Block". Verify the block appears on the calendar as a hatched/striped element.
    3. **Time Block Visual:** Verify the time block is full-width, has diagonal stripe pattern (slate-colored), and renders BEHIND any appointment blocks. Appointments should overlap on top.
    4. **Time Block Edit:** Click the time block on the calendar. Verify the Sheet reopens with fields pre-filled. Verify "Delete Block" button appears. Change the title and save.
    5. **Time Block Delete:** Open the block again, click "Delete Block". Verify instant deletion + "Time block deleted" toast with Undo button.
    6. **Mark Complete:** Click any confirmed appointment on the calendar. In the flyout, click "Mark Complete". Verify the notes textarea expands. Type a note like "Fixed pipe". Click "Confirm Complete". Verify flyout closes and "Job marked as complete" toast appears with Undo.
    7. **Completed Visual:** Verify the completed appointment now shows at reduced opacity (~40%) with a small green checkmark badge in the bottom-right corner. The urgency color tint should still be visible but muted.
    8. **Undo:** Quickly click "Undo" on the toast. Verify the appointment reverts to full opacity (confirmed state).
    9. **Show Completed Toggle:** Find the "Show completed jobs" toggle in the toolbar. Toggle it off. Verify completed appointments disappear. Toggle back on. Verify they reappear. Refresh the page. Verify the toggle remembers its state.
    10. **Completed in Flyout:** Click a completed appointment. Verify the flyout shows "Completed" badge and "Completed on [date]" text instead of action buttons.
  </how-to-verify>
  <verify>
    <automated>npm run dev -- --port 3099 &amp; sleep 3 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3099/dashboard/calendar</automated>
  </verify>
  <done>User confirms all time block and mark-complete features work as specified in the UI-SPEC.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all Phase 42 UI features.
</verification>

<success_criteria>
User confirms all time block and mark-complete features work as specified.
</success_criteria>

<output>
After completion, create `.planning/phases/42-calendar-essentials-time-blocks-and-mark-complete/42-05-SUMMARY.md`
</output>
