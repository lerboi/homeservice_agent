---
phase: 63.1
plan: 04
type: execute
wave: 3
depends_on: [63.1-03]
files_modified:
  - livekit-agent/  # Railway preview deploy + UAT call (no source edits)
  - .claude/skills/voice-call-architecture/SKILL.md
  - .planning/STATE.md
  - .planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md
  - .planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PHASE-63.1-GOAL]
tags: [uat, docs, phase-close, livekit, skill-sync]
must_haves:
  truths:
    - "Railway preview deploy of phase-63.1-generate-reply-fix branch reports SUCCESS + 'registered worker' in logs"
    - "One live UAT call completes with zero 'generate_reply is not compatible' log lines (D-09 #1)"
    - "Agent's FIRST transcript turn on the UAT call is the greeting, not the caller's utterance (D-09 #2)"
    - "If tenant has intake questions configured, at least one surfaces naturally in the UAT conversation"
    - "All Phase 60.4 + Phase 63 commits survive branch merge round-trip (D-08)"
    - "voice-call-architecture skill updated to document the Phase 63.1 prompt-directive greeting contract"
    - "Phase 60.4 is unblocked and can resume"
  artifacts:
    - path: ".planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md"
      provides: "UAT evidence — Railway log excerpt + transcript first-turn verdict + intake-question surfacing observation"
    - path: ".planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md"
      provides: "Phase rollup — per-plan commits, UAT verdict, D-09 success-criteria table, follow-ups"
    - path: ".claude/skills/voice-call-architecture/SKILL.md"
      provides: "Updated skill documenting prompt-directive greeting contract + intake pre-session hoist"
  key_links:
    - from: "UAT call transcript"
      to: "D-09 success criteria (zero generate_reply warnings + agent-first-turn)"
      via: "Railway log grep + transcript inspection"
      pattern: "generate_reply is not compatible"
---

<objective>
Close Phase 63.1 via: (a) Railway preview deploy of `phase-63.1-generate-reply-fix`, (b) one live UAT call per D-03/D-09, (c) merge branch to `livekit-agent/main` preserving all Phase 60.4 + Phase 63 commits (D-08), (d) `voice-call-architecture` skill sync, (e) STATE.md + ROADMAP.md + phase SUMMARY docs, and (f) unblock Phase 60.4.

Purpose: Ship the regression fix and document it. This plan contains the only checkpoints in Phase 63.1 — human verification of the UAT call is the gating signal for merge.

Output: Phase 63.1 closed; 60.4 unblocked; STATE.md + ROADMAP.md updated; skill synced; Railway deploy green; merge commit on `livekit-agent/main`.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-03-SUMMARY.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HANDOFF.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push branch, wait for Railway preview green, inspect deploy logs</name>
  <files>C:/Users/leheh/.Projects/livekit-agent (git push only — no source edits)</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-03-SUMMARY.md (confirm Plan 03 shipped)
  </read_first>
  <action>
    Working directory: `C:/Users/leheh/.Projects/livekit-agent/`.

    Step A — Confirm local branch state:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git status && git log --oneline -10
    ```
    Expect clean working tree; 4 fix(63.1) + test(63.1) commits on `phase-63.1-generate-reply-fix` branch on top of main HEAD `9ce12d6`.

    Step B — Push the branch:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git push --no-verify -u origin phase-63.1-generate-reply-fix
    ```

    Step C — Wait for Railway preview deploy to report SUCCESS. Inspect via Railway dashboard or CLI. Capture the preview URL and the "registered worker" log line for evidence.

    Step D — Record Railway deploy evidence in a staging file (will be folded into 63.1-UAT.md by Task 2):
    ```bash
    mkdir -p C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/
    ```
    Create `63.1-UAT.md` starter with Railway deploy SHA + worker-registered log timestamp + preview URL.
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && git ls-remote --heads origin phase-63.1-generate-reply-fix | grep -q phase-63.1-generate-reply-fix && echo "branch pushed"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin phase-63.1-generate-reply-fix` returns a SHA (branch on origin)
    - Railway preview deploy for that SHA reports SUCCESS (manual verification via dashboard)
    - Railway logs for the preview show `"registered worker"` line (manual grep)
    - `test -f C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md` returns 0 (starter file exists)
  </acceptance_criteria>
  <done>
    Branch pushed to origin; Railway preview deploy green and worker registered; UAT evidence file started.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Live UAT call on Railway preview (D-09 verification)</name>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-CONTEXT.md (D-09)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>
    Phase 63.1 fix complete on `phase-63.1-generate-reply-fix` branch, deployed to Railway preview:
    - Both `session.generate_reply(...)` calls deleted from `src/agent.py` (intake + greeting).
    - `intake_questions_text` fetched pre-`session.start()` and threaded through `build_system_prompt(intake_questions=...)`.
    - `_build_greeting_section` extended with outcome-shaped FIRST TURN / PRIMER TURNO directive (EN + ES).
    - Grep-guard pytest in place preventing `session.generate_reply(` regressions in `src/`.
    - Test suite green except 1 pre-existing deferred VIP test.
  </what-built>
  <how-to-verify>
    Place ONE live UAT call to the preview deployment (use your Singapore test number +6587528516 or equivalent tenant with intake questions configured). Then verify:

    1. **D-09 criterion #1 — zero `generate_reply` warnings:**
       Grep the Railway preview logs for the call window:
       ```
       grep "generate_reply is not compatible" <railway-log-for-uat-call>
       ```
       Expected: ZERO matches.

    2. **D-09 criterion #2 — agent speaks first:**
       Inspect the Railway `conversation_item_added` or transcript log lines in chronological order. The FIRST emitted turn must have `role="agent"` (or equivalent) BEFORE any `role="user"` turn. Agent must open with a branded greeting referencing the tenant's business name.

    3. **Intake questions surface (if tenant has them configured):**
       Listen to the call / read the transcript. At least one tenant-configured intake question must be asked naturally during the conversation flow (after understanding the main issue, before committing a booking).

    4. **Branded greeting content check:**
       Agent's opening line should be outcome-aligned (warm, branded, not robotic/verbatim). If transcript shows a literal `"Thank you for calling..."` script match, wording is too prescriptive — report back for Plan 03 revision.

    5. **No regression in barge-in / tool execution:**
       Confirm the caller can interrupt (barge-in still works) and that `check_availability` + `book_appointment` execute without hangs (Phase 60.2 Fix G + 60.4 Plans 01-02 behavior preserved).

    Capture all observations in `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md`:
    - Call ID (Railway log prefix `call-_+...`)
    - Timestamp + duration
    - D-09 #1 verdict (zero warnings — PASS/FAIL)
    - D-09 #2 verdict (agent-first-turn — PASS/FAIL)
    - Intake-question surfacing verdict (PASS/FAIL/N-A)
    - Transcript tail (first 5 turns)
    - Any unexpected warnings or behaviors
  </how-to-verify>
  <resume-signal>Type "approved" if UAT passes D-09 #1 + D-09 #2, OR describe the failure. On fail, Plan 03 task loops for wording refinement; on pass, proceed to Task 3 (merge).</resume-signal>
  <files>.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md</files>
  <action>Place ONE live UAT call to the Railway preview deployment per the <how-to-verify> instructions above. Record all 5 verification points in 63.1-UAT.md. Block merge on D-09 #1 (zero generate_reply warnings) + D-09 #2 (agent-first-turn).</action>
  <verify><automated>test -s C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md && grep -q "D-09" C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md</automated></verify>
  <done>63.1-UAT.md populated with call ID, timestamps, D-09 #1/#2/intake verdicts, and transcript tail; human approval received before Task 3 proceeds.</done>
</task>

<task type="auto">
  <name>Task 3: Merge to livekit-agent/main preserving Phase 60.4 + Phase 63 commits (D-08)</name>
  <files>C:/Users/leheh/.Projects/livekit-agent (git merge only)</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HANDOFF.md (§"Live commits — exact SHAs" — c2482f8, 1df5223, b46851b, 5e48273, e580f14, 68828d7, 87d6883)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-UAT.md (UAT verdict from Task 2)
  </read_first>
  <action>
    Working directory: `C:/Users/leheh/.Projects/livekit-agent/`.

    Step A — Verify main is unchanged since branch cut (HEAD still `9ce12d6`):
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git fetch origin && git log --oneline origin/main -5
    ```

    Step B — Merge with preservation (use merge-commit style, NOT fast-forward or rebase, to keep branch topology visible):
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git checkout main && git pull origin main && git merge --no-ff phase-63.1-generate-reply-fix -m "merge(63.1): generate_reply regression fix" && git push --no-verify origin main
    ```

    Step C — Verify ALL D-08 commits present on main:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && for sha in c2482f8 1df5223 b46851b 5e48273 e580f14 68828d7 87d6883 38352f2 9ce12d6; do git log --oneline main | grep -q "^$sha" && echo "$sha OK" || echo "$sha MISSING"; done
    ```
    All 9 must report OK.

    Step D — Verify Railway auto-deployed main; worker registered; no `generate_reply is not compatible` warnings on a 2nd smoke UAT call (optional but recommended).
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline origin/main | head -20 | grep -qE "(fix\(63.1\)|merge\(63.1\))" && echo "merge landed"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline origin/main | head -20` contains `merge(63.1)` AND ≥3 `fix(63.1):` / `test(63.1):` commits
    - All 9 D-08 SHAs (c2482f8 1df5223 b46851b 5e48273 e580f14 68828d7 87d6883 38352f2 9ce12d6) still present in `git log main`
    - `grep -cE '^[^#]*\bgenerate_reply\s*\(' C:/Users/leheh/.Projects/livekit-agent/src/agent.py` returns 0 on main
  </acceptance_criteria>
  <done>
    phase-63.1-generate-reply-fix merged to main via merge-commit; all Phase 60.4 + Phase 63 commits preserved per D-08; Railway auto-deployed main.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update voice-call-architecture skill + STATE.md + ROADMAP.md + Phase SUMMARY</name>
  <files>
    C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md;
    C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md;
    C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md;
    C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md (NEW)
  </files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md (current — look for the existing Phase 60.3 subsection to mirror the ADD-ONLY pattern)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md (Decisions + Session Continuity sections)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/ROADMAP.md (Phase 63.1 entry)
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-01-SUMMARY.md, 63.1-02-SUMMARY.md, 63.1-03-SUMMARY.md, 63.1-UAT.md
  </read_first>
  <action>
    Working directory: `C:/Users/leheh/.Projects/homeservice_agent/`.

    Step A — Write `63.1-SUMMARY.md` phase rollup:
    - Per-plan commits table (Plan 01 — 2 SHAs test(63.1), Plan 02 — 2 SHAs fix(63.1), Plan 03 — 1 SHA fix(63.1), Plan 04 — merge SHA)
    - D-09 success-criteria table (#1 zero generate_reply warnings — verdict, #2 agent speaks first — verdict, intake surfaced — verdict)
    - Lessons: document (1) Gemini 3.1 capability-guard design + why synthetic-user-turn is infeasible on 1.5.6; (2) prompt-directive + server VAD as the replacement pattern; (3) pre-session DB hoist pattern for any other 1.5.6 + 3.1 data-injection need.
    - Follow-ups:
      - 60.4 unblocked — resume via HANDOFF doc
      - Tool-call cancellation noop (still priority #1 for future hardening phase)
      - SegmentSynchronizer race (still byte-identical at 1.5.6)
      - Canary-call harness (deferred)

    Step B — Update `SKILL.md` ADD-ONLY with a new subsection "Phase 63.1 — generate_reply regression fix (Gemini 3.1 + plugins-google 1.5.6)". Mirror the Phase 60.3 subsection style:
    - What broke: capability-guarded generate_reply silently dropped on 3.1 models
    - Fix shape: two call sites deleted; intake hoisted pre-session; greeting delivered via `_build_greeting_section` FIRST TURN / PRIMER TURNO directive; Gemini server VAD fires on first caller audio
    - Regression guard: `tests/test_no_generate_reply_in_src.py` grep scan
    - DO NOT edit earlier subsections — preserve Phase 60.3 and earlier content verbatim

    Step C — Update `STATE.md`:
    - Append to `Accumulated Context → Decisions (v6.0 planning)` a new entry: `[v6.0 P63.1]: generate_reply regression shipped to livekit-agent main (merge SHA ...); UAT verdict ...; 60.4 unblocked.`
    - Update `progress.completed_plans` (+4), `progress.completed_phases` (+1 if counting 63.1 as a full phase per ROADMAP convention — confirm with ROADMAP before bumping), adjust `progress.percent`.
    - Update `stopped_at` → `"Phase 63.1 shipped; 60.4 ready to resume"`.
    - Update `last_updated` timestamp.
    - Update `Session Continuity → Resume file` → point to `.planning/phases/60.4-booking-timezone-fix-and-stt-language-pinning/60.4-HANDOFF.md` and remove the "60.4 paused at" line (or update it to "60.4 ready to resume").

    Step D — Update `ROADMAP.md` Phase 63.1 entry:
    - Change status to ✅ Complete (or milestone-convention equivalent)
    - Add SUMMARY link
    - Ensure Phase 60.4 entry's "blocked by 63" / "blocked by 63.1" note is removed or updated to "unblocked"

    Step E — Commit in homeservice_agent:
    ```bash
    cd C:/Users/leheh/.Projects/homeservice_agent && git add .claude/skills/voice-call-architecture/SKILL.md .planning/STATE.md .planning/ROADMAP.md .planning/phases/63.1-gemini-3-generate-reply-regression-fix/ && git commit -m "docs(63.1): phase close — SUMMARY + skill sync + STATE + ROADMAP"
    ```
    (No `--no-verify` in homeservice_agent per D-07 scope — livekit-agent only.)
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && test -f .planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md && grep -q "Phase 63.1" .claude/skills/voice-call-architecture/SKILL.md && grep -q "P63.1" .planning/STATE.md && echo "all docs updated"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-SUMMARY.md` returns 0
    - `grep -c "Phase 63.1" C:/Users/leheh/.Projects/homeservice_agent/.claude/skills/voice-call-architecture/SKILL.md` returns >= 1
    - `grep -c "P63.1" C:/Users/leheh/.Projects/homeservice_agent/.planning/STATE.md` returns >= 1
    - ROADMAP Phase 63.1 entry marked complete; Phase 60.4 unblock note present
    - `cd C:/Users/leheh/.Projects/homeservice_agent && git log -1 --format=%s` contains `"docs(63.1): phase close"`
    - Phase 60.3 and earlier SKILL.md subsections byte-identical before/after this edit (ADD-ONLY verified via diff)
  </acceptance_criteria>
  <done>
    63.1-SUMMARY.md written with per-plan commits + D-09 verdict table + lessons + follow-ups; voice-call-architecture SKILL.md extended with Phase 63.1 subsection (ADD-ONLY, earlier content preserved); STATE.md updated with P63.1 decision entry + progress bump + resume pointer to 60.4 HANDOFF; ROADMAP.md Phase 63.1 marked complete and Phase 60.4 unblocked; one commit on homeservice_agent main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. Plan 04 performs: git operations (push/merge), documentation writes in homeservice_agent, and a live UAT call that exercises existing production surfaces (Twilio SIP -> LiveKit -> Gemini).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1-04 | N/A | UAT + merge + docs | accept | No new untrusted input, no new auth/data boundary, no new external callouts. Merge strategy (--no-ff) explicitly preserves Phase 60.4 + Phase 63 commit SHAs (D-08). UAT call uses existing Twilio number + tenant. No threats introduced; no mitigations required. |
</threat_model>

<verification>
- Branch pushed; Railway preview green; worker registered log line captured.
- UAT call verdict recorded in 63.1-UAT.md: D-09 #1 zero warnings PASS, D-09 #2 agent-first-turn PASS, intake surfaced PASS/N-A.
- Merge landed on livekit-agent/main via --no-ff; all 9 D-08 SHAs verified present.
- 63.1-SUMMARY.md + SKILL.md subsection + STATE.md + ROADMAP.md updated; one docs(63.1) commit on homeservice_agent.
- Phase 60.4 HANDOFF doc reachable and unblocked.
</verification>

<success_criteria>
- Railway preview green; one live UAT call satisfies D-09 #1 + #2
- Branch merged to livekit-agent/main via --no-ff; D-08 SHAs preserved
- 63.1-SUMMARY.md, voice-call-architecture SKILL.md, STATE.md, ROADMAP.md all updated
- Phase 60.4 unblocked and ready to resume
- Two commits total this plan (homeservice_agent docs commit + the livekit-agent merge commit already made in Task 3)
</success_criteria>

<output>
Phase 63.1 closed. Next step: user runs `/gsd-execute-phase 60.4` to resume booking-timezone + STT language pinning work via HANDOFF doc.
</output>
