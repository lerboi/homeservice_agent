---
phase: 63.1
slug: gemini-3-generate-reply-regression-fix
status: approved
nyquist_compliant: true
wave_0_complete: false  # flips true when Plan 01 ships
created: 2026-04-24
---

# Phase 63.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from 63.1-RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (livekit-agent sibling repo) |
| **Config file** | `C:/Users/leheh/.Projects/livekit-agent/pyproject.toml` (pytest section) |
| **Quick run command** | `cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_prompt_greeting_directive.py tests/test_no_generate_reply_in_src.py -q` |
| **Full suite command** | `cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest -q` |
| **Estimated runtime** | ~5s quick / ~15s full (247 tests baseline from Phase 63) |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

Populated by planner on 2026-04-24.

| Plan-Task | Behavior | Test File | Wave |
|-----------|----------|-----------|------|
| 63.1-01 T1 | Greeting-directive RED tests (EN+ES+business_name+no-verbatim) + intake audit | `tests/test_prompt_greeting_directive.py` (new, 6 tests) + `tests/test_prompt_tail_sections.py` (+2 if gap) | 0 (RED) |
| 63.1-01 T2 | Grep-guard RED test for active generate_reply( in src/ | `tests/test_no_generate_reply_in_src.py` (new, 1 test) | 0 (RED) |
| 63.1-02 T1 | Intake hoist + pre-session fetch + build_system_prompt threading + _run_db_queries reindex | `tests/test_prompt.py` + `tests/test_check_availability_slot_cache.py` (existing, must stay green) + static grep | 1 |
| 63.1-02 T2 | Delete greeting-side generate_reply + session_ready.set/wait | `tests/test_no_generate_reply_in_src.py` flips RED→GREEN | 1 |
| 63.1-03 T1 | FIRST TURN / PRIMER TURNO directive in _build_greeting_section (EN+ES) | `tests/test_prompt_greeting_directive.py` flips all 6 RED→GREEN; `tests/test_prompt_tail_sections.py` parity guards stay green | 2 |
| 63.1-04 T1 | Railway preview deploy | Manual: dashboard SUCCESS + `registered worker` log | 3 |
| 63.1-04 T2 | **CHECKPOINT** — Live UAT call | Manual D-09 verdict in `63.1-UAT.md` | 3 |
| 63.1-04 T3 | Merge to main preserving D-08 SHAs | `git log --oneline main` for 9 required SHAs | 3 |
| 63.1-04 T4 | SKILL + STATE + ROADMAP + SUMMARY | `grep "Phase 63.1" SKILL.md`, `grep "P63.1" STATE.md`, test -f SUMMARY | 3 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Confirm existing `tests/test_prompt.py` covers `_build_intake_questions_section` rendering for EN + ES with non-empty and empty `all_questions` (locks hoist invariant)
- [ ] Create `tests/test_prompt_greeting_directive.py` with RED tests asserting the outcome-based greeting directive appears in `build_system_prompt()` output for locale=en-US and locale=es
- [ ] Create `tests/test_no_generate_reply_in_src.py` with RED test — will fail at Wave 0 because two call sites still exist; flips to GREEN after Plan 02 deletes them

*If none: N/A — Wave 0 is required to lock RED→GREEN transitions for the deletion.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Agent speaks greeting FIRST on SIP connect without caller prompting | D-09 success metric | Requires live Twilio SIP + LiveKit + Gemini 3.1 Flash Live call; VAD server-side timing cannot be unit-tested | 1. Merge to `livekit-agent/main`, wait for Railway preview SUCCESS + "registered worker" log. 2. Place a real SIP call from +65 test number. 3. Verify transcript's FIRST entry is agent greeting (not caller "hello"). 4. Grep Railway logs for `generate_reply is not compatible` — must be zero matches. 5. If tenant has intake questions configured, verify at least one surfaces naturally in conversation. |
| Phase 60.4 commits + Phase 63 merge commit survive branch round-trip | D-08 | Git history inspection post-merge | `cd C:/Users/leheh/.Projects/livekit-agent && git log --oneline main` — verify SHAs c2482f8, 1df5223, b46851b, 5e48273, e580f14, 68828d7, 87d6883, 38352f2, 9ce12d6 all present |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (greeting directive test, grep-guard test)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-24 — planner populated per-task map; Wave 0 RED tests defined in Plan 01.
