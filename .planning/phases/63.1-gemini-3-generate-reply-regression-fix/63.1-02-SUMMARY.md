---
phase: 63.1
plan: 02
subsystem: livekit-agent
tags: [livekit, agent, refactor, regression-fix, gemini-3.1]
dependency_graph:
  requires: [63.1-01]
  provides: [intake_questions_pre_session_hoist, generate_reply_deletion]
  affects: [livekit-agent/src/agent.py]
tech_stack:
  added: []
  patterns: [pre-session construction-time injection, named tuple unpacking for asyncio.gather]
key_files:
  created: []
  modified:
    - C:/Users/leheh/.Projects/livekit-agent/src/agent.py
decisions:
  - "Shape A (minimal hoist) chosen over Shape B (parallelize with customer_context): minimal diff; 100ms delta imperceptible per D-02"
  - "session_ready Event entirely eliminated (declaration + set) — dead code after generate_reply removal (Assumption A5 confirmed)"
  - "_run_db_queries gather result extraction refactored to named unpacking (Pitfall 2 mitigation); index-based access eliminated"
metrics:
  duration: "~8min"
  completed: "2026-04-24"
  commits: 2
requirements: [PHASE-63.1-GOAL]
---

# Phase 63.1 Plan 02: Delete broken generate_reply calls + hoist intake_questions Summary

**One-liner:** Both broken `session.generate_reply(...)` call sites deleted from src/agent.py; intake_questions fetch hoisted pre-session and threaded through `build_system_prompt(intake_questions=...)` so Gemini 3.1 receives tenant intake configuration in the initial `instructions=` payload.

## What Shipped

- Pre-session intake_questions Supabase fetch block inserted before `build_system_prompt(...)` call (mirrors existing customer_context pre-session pattern at L248-267)
- `build_system_prompt(intake_questions=intake_questions_text)` kwarg wired to the hoisted fetch result (was hardcoded `""`)
- `_run_db_queries` refactored:
  - `intake_task` removed from parallel gather
  - `asyncio.gather(...)` result extraction converted from index-based `results[N]` to named unpacking (`sub_res, call_res, appts_res, events_res, zones_res, buffers_res, blocks_res`) per Pitfall 2
  - Intake-side `session.generate_reply(...)` block (L702-717) + `session_ready.wait()` fully deleted
- `session_ready = asyncio.Event()` declaration deleted (dead code)
- Greeting-side `session.generate_reply(instructions="Greet the caller now.")` + `session_ready.set()` block (L754-758) deleted
- Explanatory comment inserted at the former greeting-trigger location pointing to RESEARCH doc Targets 1 & 3 + `_build_greeting_section` as the replacement mechanism

## `_run_db_queries` Reindex Map (Old → New)

| Before (index) | After (named) | Consumer |
|----------------|---------------|----------|
| `results[0]` (sub)     | `sub_res`     | Subscription block check (unchanged logic) |
| `results[1]` (intake)  | **DELETED**   | Hoisted pre-session (Step A) |
| `results[2]` (call)    | `call_res`    | `deps["call_uuid"]` population (unchanged logic) |
| `results[3]` (appts)   | `appts_res`   | `deps["_slot_cache"]["appointments"]` |
| `results[4]` (events)  | `events_res`  | `deps["_slot_cache"]["calendar_events"]` |
| `results[5]` (zones)   | `zones_res`   | `deps["_slot_cache"]["service_zones"]` |
| `results[6]` (buffers) | `buffers_res` | `deps["_slot_cache"]["zone_travel_buffers"]` |
| `results[7]` (blocks)  | `blocks_res`  | `deps["_slot_cache"]["calendar_blocks"]` |

All 7 downstream consumers updated atomically; no stale index references remain.

## Diff Size

`src/agent.py` — single file modified, 49 insertions / 48 deletions (~ net 0 lines — pure re-shape from post-session generate_reply injection to pre-session prompt-construction injection).

## Verification

### Static grep state (post-Task-2)

| Check | Command | Expected | Actual |
|-------|---------|----------|--------|
| Active `generate_reply(` in agent.py | `grep -cE '^[^#]*\bgenerate_reply\s*\(' src/agent.py` | 0 | 0 |
| `intake_questions_text` references | `grep -c 'intake_questions_text' src/agent.py` | ≥3 | 3 (declaration + assignment + kwarg) |
| `intake_questions=intake_questions_text` kwarg | `grep -c 'intake_questions=intake_questions_text' src/agent.py` | 1 | 1 |
| `intake_task` remaining | `grep -c 'intake_task' src/agent.py` | 0 | 0 |
| `session_ready` remaining (any) | `grep -c 'session_ready' src/agent.py` | 0 | 0 |

Only comment-embedded `generate_reply` mentions remain (4 total, all inside explanatory comments). Grep-guard test file is Plan 01's deliverable; the guard regex (`^[^#]*\bgenerate_reply\s*\(`) matches zero lines — guard will flip GREEN on its creation.

### Test suite (`python -m pytest tests/ -q`)

- **247 passed** (Phase 63 baseline preserved — slot_cache, prompt, tail_sections, goodbye_diag, etc. all green)
- **7 failed** (expected — matches D-09 gating):
  - 6 × `test_prompt_greeting_directive.py::*` — Plan 01 RED tests awaiting Plan 03 GREEN
  - 1 × `tests/webhook/test_routes.py::test_incoming_call_vip_lead` — pre-existing deferred VIP test (project memory `project_vip_caller_routing.md`)

Quick subset `python -m pytest tests/test_prompt.py tests/test_check_availability_slot_cache.py -q` → **10 passed**. Validates Pitfall 2 reindex correctness (slot_cache consumers of `appts_res`/`events_res`/`zones_res`/`buffers_res`/`blocks_res` produce identical cache shape to pre-fix).

### Grep-guard status

`tests/test_no_generate_reply_in_src.py` was not present on branch at Plan 02 execution time (Plan 01 Task 2 creates it in parallel execution wave). Our code-level guarantee (0 active `generate_reply(` calls in `src/`) ensures the guard test will be GREEN when Plan 01 lands. Explicit grep verified by the automated verify step embedded in Task 1 acceptance criteria.

## Commits

| SHA       | Subject |
|-----------|---------|
| `3e43bb3` | fix(63.1): hoist intake_questions fetch pre-session; delete intake-side generate_reply |
| `823aab3` | fix(63.1): delete greeting-side generate_reply + session_ready.set (grep-guard GREEN) |

Both commits on `phase-63.1-generate-reply-fix` with `--no-verify` per parallel-execution convention.

## Deviations from Plan

**Minor (not substantive):**
- **Comment wording in Step A of Task 2** — plan action block used the literal phrase `"session_ready.set()"` in the replacement comment. Strict grep-guard acceptance criterion (`grep -c 'session_ready' src/agent.py` returns 0) required zeroing ALL occurrences, including comment mentions. Comment rephrased to `"removed post-session greeting trigger (dead generate_reply + Event signaling)"` — preserves the historical-explanation intent while eliminating the literal token `session_ready` (0 matches confirmed). No logic impact.

Otherwise, plan executed exactly as written. No Rule-1/2/3 auto-fixes triggered; no authentication gates; no architectural deviations.

## Self-Check

- [x] `src/agent.py` exists and contains `intake_questions_text` (3 occurrences) — FOUND
- [x] `src/agent.py` contains `intake_questions=intake_questions_text` kwarg — FOUND
- [x] `src/agent.py` contains zero active `generate_reply(` calls — CONFIRMED via regex scan
- [x] `src/agent.py` contains zero `session_ready` references — CONFIRMED
- [x] Commit `3e43bb3` present on branch — FOUND (git log)
- [x] Commit `823aab3` present on branch — FOUND (git log)
- [x] Quick tests green (10/10) — CONFIRMED
- [x] Full suite matches expected RED profile (6 greeting-directive + 1 VIP) — CONFIRMED

## Self-Check: PASSED
