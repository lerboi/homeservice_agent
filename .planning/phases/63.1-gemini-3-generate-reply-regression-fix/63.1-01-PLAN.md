---
phase: 63.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - livekit-agent/tests/test_prompt_greeting_directive.py
  - livekit-agent/tests/test_no_generate_reply_in_src.py
autonomous: true
requirements: [PHASE-63.1-GOAL]
tags: [livekit, tdd, regression-guard, prompt]
must_haves:
  truths:
    - "Running pytest on test_prompt_greeting_directive.py RED-fails today (greeting directive text absent from prompt output)"
    - "Running pytest on test_no_generate_reply_in_src.py RED-fails today (two active session.generate_reply( call sites still in src/agent.py)"
    - "Existing _build_intake_questions_section EN + ES coverage is confirmed present OR gap-filled"
  artifacts:
    - path: "livekit-agent/tests/test_prompt_greeting_directive.py"
      provides: "RED tests for EN + ES greeting directive + business_name interpolation"
    - path: "livekit-agent/tests/test_no_generate_reply_in_src.py"
      provides: "RED grep-guard test flagging both active generate_reply( call sites"
  key_links:
    - from: "tests/test_no_generate_reply_in_src.py"
      to: "src/agent.py"
      via: "Path(__file__).parent.parent / 'src' rglob scan"
      pattern: "\\bgenerate_reply\\s*\\("
---

<objective>
Create Wave 0 RED tests that codify the Phase 63.1 contract before any production code is touched. These tests MUST fail today and serve as the GREEN gate for Plans 02 and 03.

Purpose: Lock the RED→GREEN transition per D-03/D-04. RED tests prove the regression exists; they flip GREEN only after Plan 02 deletes both call sites (D-04 guard) and Plan 03 adds the greeting directive to `_build_greeting_section` (EN + ES parity).

Output: Two new test files in `C:/Users/leheh/.Projects/livekit-agent/tests/`, plus confirmation that existing `tests/test_prompt.py` already covers `_build_intake_questions_section` rendering (or two new 3-line tests added if not).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-CONTEXT.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-RESEARCH.md
@C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-VALIDATION.md

<interfaces>
From livekit-agent/src/prompt.py:
```python
def _build_greeting_section(locale: str, business_name: str, onboarding_complete: bool, t) -> str: ...
def _build_intake_questions_section(intake_questions: str | None, locale: str = "en") -> str: ...
def build_system_prompt(
    locale: str, *,
    business_name: str = "Voco",
    onboarding_complete: bool = False,
    tone_preset: str = "professional",
    intake_questions: str = "",
    country: str = "US",
    working_hours: dict | None = None,
    tenant_timezone: str = "America/Chicago",
    customer_context: dict | None = None,
) -> str: ...
```

From livekit-agent/pyproject.toml [tool.pytest.ini_options]:
- testpaths = ["tests"]
- asyncio_mode = "auto"
- pythonpath = ["."]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm existing intake_questions prompt coverage + create greeting-directive RED tests</name>
  <files>livekit-agent/tests/test_prompt_greeting_directive.py (NEW); livekit-agent/tests/test_prompt.py or test_prompt_tail_sections.py (audit only, add 2 tests if missing)</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-CONTEXT.md
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-RESEARCH.md (especially §Primary Research Target 3 + Pitfall 4)
    - C:/Users/leheh/.Projects/livekit-agent/src/prompt.py (lines 462-518 for `_build_greeting_section`, lines 812-833 for `_build_intake_questions_section`, lines 1189-1258 for `build_system_prompt` signature + section list)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_tail_sections.py (Phase 60.3 Plan 12 pattern for locale-parity inverted-assertion tests)
    - C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt.py (grep for `_build_intake_questions_section` or `intake_questions=` — confirm A4 assumption from RESEARCH Assumptions Log)
  </read_first>
  <behavior>
    Inverted-substring assertions (lowercased), Phase 60.3 Plan 12 style. All tests RED today because the greeting directive text does not yet exist in `_build_greeting_section`.

    - test_greeting_directive_en_states_first_thing_contract: `build_system_prompt(locale="en-US", business_name="AcmeCorp")` output (lowercased) contains BOTH substrings `"first thing"` AND `"branded greeting"` (or equivalent outcome-shaped phrase — lock the exact phrase in Task 1 of Plan 03).
    - test_greeting_directive_es_states_first_thing_contract: `build_system_prompt(locale="es", business_name="AcmeCorp")` output (lowercased) contains BOTH substrings `"lo primero"` AND `"saludo"` AND `"con marca"` (or equivalent — must mirror EN outcome).
    - test_greeting_directive_interpolates_business_name_en: `"acmecorp"` (lowercased) present in EN output at least once in the greeting section region.
    - test_greeting_directive_interpolates_business_name_es: `"acmecorp"` (lowercased) present in ES output at least once in the greeting section region.
    - test_greeting_directive_no_verbatim_script_en: EN output does NOT contain the string `"thank you for calling"` wrapped as a verbatim quoted directive (guards Pitfall 3 — outcome-shaped, not a script).
    - test_greeting_directive_no_verbatim_script_es: ES output does NOT contain the string `"gracias por llamar"` wrapped as a verbatim quoted directive.

    Intake coverage audit (if `grep "_build_intake_questions_section\|intake_questions=" tests/test_prompt*.py` returns zero hits):
    - test_intake_questions_en_rendered_when_nonempty: `build_system_prompt(locale="en-US", intake_questions="Question 1\nQuestion 2")` output contains `"additional questions"` (lowercased) AND `"question 1"` (lowercased).
    - test_intake_questions_es_rendered_when_nonempty: `build_system_prompt(locale="es", intake_questions="Pregunta uno\nPregunta dos")` output contains `"preguntas adicionales"` (lowercased) AND `"pregunta uno"` (lowercased).
  </behavior>
  <action>
    Working directory: `C:/Users/leheh/.Projects/livekit-agent/` (sibling repo, branch `phase-63.1-generate-reply-fix` cut from main HEAD `9ce12d6`).

    Step A — audit existing intake coverage:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && grep -n "_build_intake_questions_section\|intake_questions=" tests/test_prompt*.py
    ```
    If grep returns zero hits OR no test asserts non-empty `intake_questions` rendering, add the two `test_intake_questions_*` tests at the bottom of `tests/test_prompt_tail_sections.py` (mirror existing section-presence tests in that file). If coverage already present, skip this sub-step and note in commit message.

    Step B — create `tests/test_prompt_greeting_directive.py`:
    Implement the 6 behaviors listed above. Use `from src.prompt import build_system_prompt` and the same `t(...)` stub / locale keyword pattern used in `tests/test_prompt_tail_sections.py`. Lowercased-substring inverted-assertions (NOT exact-string matches) so Plan 03's final wording has latitude within the outcome-shape. Keep each test ≤10 lines.

    Step C — run tests and CONFIRM RED:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_prompt_greeting_directive.py -v
    ```
    All 6 must fail today (greeting directive text is not yet in `_build_greeting_section`). If any pass RED-accidentally, tighten the assertion before commit.

    Step D — commit in livekit-agent:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git add tests/test_prompt_greeting_directive.py tests/test_prompt_tail_sections.py && git commit --no-verify -m "test(63.1): RED tests for greeting directive + intake audit"
    ```
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_prompt_greeting_directive.py --tb=no -q 2>&1 | grep -E "(6 failed|FAILED)" </automated>
  </verify>
  <acceptance_criteria>
    - `test -f C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_greeting_directive.py` returns 0
    - `grep -c "^def test_" C:/Users/leheh/.Projects/livekit-agent/tests/test_prompt_greeting_directive.py` returns 6
    - `cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_prompt_greeting_directive.py -q` exits non-zero AND stdout contains `"6 failed"` (all RED)
    - `cd C:/Users/leheh/.Projects/livekit-agent && git log -1 --format=%s` contains `"test(63.1): RED tests for greeting directive"`
    - Intake audit outcome recorded in commit body (either "existing coverage confirmed" OR "added 2 intake tests to test_prompt_tail_sections.py")
  </acceptance_criteria>
  <done>
    Greeting-directive RED test file exists with 6 tests, all fail on current prompt.py; intake coverage audited and gap-filled if needed; commit landed on phase-63.1-generate-reply-fix branch with --no-verify.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create grep-guard RED test for generate_reply( in src/</name>
  <files>livekit-agent/tests/test_no_generate_reply_in_src.py (NEW)</files>
  <read_first>
    - C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-RESEARCH.md (§Primary Research Target 4)
    - C:/Users/leheh/.Projects/livekit-agent/src/agent.py (lines 700-760 — confirm both active call sites still present today: L712 intake, L755 greeting)
    - C:/Users/leheh/.Projects/livekit-agent/tests/conftest.py (if exists — check for fixtures to reuse)
  </read_first>
  <behavior>
    Single test, pure stdlib, <50ms runtime. RED today — two offenders detected in `src/agent.py`. Flips GREEN after Plan 02 deletes both call sites.

    - test_no_active_generate_reply_calls_in_src: Walks `Path(__file__).parent.parent / "src"` rglob("*.py"), skips `__pycache__`, for each line checks the regex `^[^#]*\bgenerate_reply\s*\(` against the raw line (after `lstrip` — skip if first non-whitespace char is `#`). Asserts offender list is empty; failure message embeds file:lineno:line_content and explains why 1.5.6 + Gemini 3.1 drops it silently.
  </behavior>
  <action>
    Working directory: `C:/Users/leheh/.Projects/livekit-agent/`.

    Step A — write `tests/test_no_generate_reply_in_src.py` using the verbatim code from 63.1-RESEARCH.md §Primary Research Target 4 (lines 321-372 of that doc). Regex exactly: `re.compile(r"\bgenerate_reply\s*\(")`. Comment-skip heuristic: `if raw.lstrip().startswith("#"): continue`. Scope: `SRC_DIR = Path(__file__).parent.parent / "src"`.

    Step B — run and confirm RED with exactly 2 offenders:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_no_generate_reply_in_src.py -v
    ```
    Expected output: 1 test failed, assertion message lists `src/agent.py:712` AND `src/agent.py:755` (line numbers may drift ±2 depending on current file state — verify both are flagged).

    Step C — commit:
    ```bash
    cd C:/Users/leheh/.Projects/livekit-agent && git add tests/test_no_generate_reply_in_src.py && git commit --no-verify -m "test(63.1): RED grep-guard against session.generate_reply( in src/"
    ```
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_no_generate_reply_in_src.py -q 2>&1 | grep -E "(1 failed|FAILED).*generate_reply|src/agent.py:7"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f C:/Users/leheh/.Projects/livekit-agent/tests/test_no_generate_reply_in_src.py` returns 0
    - `grep -c "generate_reply" C:/Users/leheh/.Projects/livekit-agent/tests/test_no_generate_reply_in_src.py` returns ≥3 (docstring + regex + assertion message)
    - Running the test exits non-zero; stdout contains `"src/agent.py:"` with two distinct line numbers (the two active call sites)
    - `cd C:/Users/leheh/.Projects/livekit-agent && git log -1 --format=%s` contains `"test(63.1): RED grep-guard"`
  </acceptance_criteria>
  <done>
    Grep-guard test exists, fails RED with exactly both agent.py call sites flagged, commit landed on phase-63.1-generate-reply-fix branch with --no-verify.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 63.1 Plan 01 adds test code only. No trust boundary crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63.1-01 | N/A | Test-only files | accept | No new untrusted input, no new auth/data boundary, no new external callouts. Test files are developer-facing and scoped to the test runner. |
</threat_model>

<verification>
- Quick run: `cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest tests/test_prompt_greeting_directive.py tests/test_no_generate_reply_in_src.py -q` — expects 7 failures (6 greeting + 1 grep-guard) + optional 0-2 extra intake audit tests passing.
- Full suite (smoke): `cd C:/Users/leheh/.Projects/livekit-agent && .venv/Scripts/python -m pytest -q` — all pre-existing Phase-63-baseline 247 tests still green; only the 7 new tests fail (RED by design).
- Two commits on `phase-63.1-generate-reply-fix` branch, both `test(63.1):` prefix with `--no-verify`.
</verification>

<success_criteria>
- tests/test_prompt_greeting_directive.py exists with 6 RED tests
- tests/test_no_generate_reply_in_src.py exists with 1 RED test flagging exactly src/agent.py:712 + src/agent.py:755
- Existing test_prompt_*.py intake coverage audited; gap filled with 2 tests if missing
- Phase-63 baseline 247 tests remain GREEN
- Two commits on the phase branch with --no-verify per D-07
</success_criteria>

<output>
After completion, create `C:/Users/leheh/.Projects/homeservice_agent/.planning/phases/63.1-gemini-3-generate-reply-regression-fix/63.1-01-SUMMARY.md` documenting: files created, RED test counts, intake audit outcome, two commit SHAs on livekit-agent branch.
</output>
