---
phase: 36-landing-page-section-redesign-how-it-works-minimalism-and-features-carousel
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
files_modified:
  - src/app/(public)/page.js
autonomous: false
requirements:
  - HIW-01
  - HIW-02
  - FEAT-01
  - FEAT-02
  - INTEG-01
must_haves:
  truths:
    - "ScrollLinePath copper sine wave renders correctly spanning How It Works, Features, and Social Proof"
    - "Both new sections render correctly at 375px, 768px, and 1280px viewport widths"
    - "Build succeeds with zero errors"
  artifacts:
    - path: "src/app/components/landing/HowItWorksMinimal.jsx"
      provides: "Full-viewport scroll steps"
    - path: "src/app/components/landing/FeaturesCarousel.jsx"
      provides: "Horizontal carousel with icon nav"
  key_links:
    - from: "src/app/components/landing/ScrollLinePath.jsx"
      to: "HowItWorksSection + FeaturesCarousel + SocialProofSection"
      via: "getBoundingClientRect measurement"
      pattern: "getBoundingClientRect"
---

<objective>
Verify both new sections integrate correctly — build passes, ScrollLinePath renders properly with the new section heights, and both sections are visually correct at multiple breakpoints.

Purpose: Catch integration issues between the two independently-built sections and the existing ScrollLinePath copper sine wave.
Output: Verified build, visual confirmation at 3 breakpoints.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-landing-page-section-redesign-how-it-works-minimalism-and-features-carousel/36-01-SUMMARY.md
@.planning/phases/36-landing-page-section-redesign-how-it-works-minimalism-and-features-carousel/36-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and integration smoke test</name>
  <files>src/app/(public)/page.js</files>
  <read_first>
    - src/app/(public)/page.js (verify final composition)
    - src/app/components/landing/HowItWorksSection.jsx (verify import swap complete)
    - src/app/components/landing/HowItWorksMinimal.jsx (verify component exists and exports)
    - src/app/components/landing/FeaturesCarousel.jsx (verify component exists and exports)
    - src/app/components/landing/ScrollLinePath.jsx (understand measurement logic — confirm it uses getBoundingClientRect dynamically)
  </read_first>
  <action>
1. Run `npm run build` and verify zero errors.
2. Verify integration checklist via grep:
   - `id="how-it-works"` exists in HowItWorksSection.jsx
   - `id="features"` exists in FeaturesCarousel.jsx
   - `HowItWorksMinimal` is imported in HowItWorksSection.jsx (not HowItWorksSticky)
   - `FeaturesCarousel` is imported in page.js (not FeaturesGrid)
   - ScrollLinePath still wraps all three sections in page.js
3. If build fails, fix any import errors, missing exports, or syntax issues in the new components.
  </action>
  <verify>
    <automated>cd "C:\Users\leheh\.Projects\homeservice_agent" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript/JSX compilation errors in HowItWorksMinimal.jsx or FeaturesCarousel.jsx
    - grep confirms `id="how-it-works"` in HowItWorksSection.jsx
    - grep confirms `id="features"` in FeaturesCarousel.jsx
    - grep confirms `FeaturesCarousel` in page.js (not FeaturesGrid)
    - grep confirms `HowItWorksMinimal` in HowItWorksSection.jsx (not HowItWorksSticky)
  </acceptance_criteria>
  <done>Build passes cleanly, all section IDs preserved, all imports correctly swapped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of both redesigned sections</name>
  <action>Present the landing page to the user for visual verification of How It Works scroll steps, Features carousel, ScrollLinePath integration, and mobile responsiveness.</action>
  <what-built>
    Replaced the How It Works sticky folder-stack section with a full-viewport scroll step design (4 steps, each one screen height, staggered fade-in animations, parallax on icons). Replaced the Features 2-column grid with a horizontal carousel (2-3 visible cards on desktop, swipe + arrows + icon nav, auto-advance, micro visual animation gating).
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000 in browser

    **How It Works section (scroll past hero):**
    - [ ] 4 full-viewport steps visible, each roughly fills the screen
    - [ ] Steps alternate white/off-white backgrounds
    - [ ] Scrolling into a step triggers staggered fade: number -> icon -> title -> line -> description
    - [ ] Large faint step numbers visible behind content (01, 02, 03, 04)
    - [ ] Soft circular background shapes visible at low opacity
    - [ ] Copper/orange gradient accent line appears below each step title
    - [ ] Icons have subtle vertical parallax on desktop (icon moves slightly relative to text on scroll)
    - [ ] "See It In Action" CTA button appears after the 4 steps
    - [ ] Section heading reads "From first ring to full calendar — in one call."

    **Features carousel (scroll past How It Works):**
    - [ ] Horizontal carousel with 2-3 cards visible on desktop
    - [ ] Active/centered card is slightly larger with orange top border
    - [ ] Arrow buttons on either side work (advance/retreat)
    - [ ] Icon nav grid below carousel with 7 labeled icons
    - [ ] Clicking an icon scrolls carousel to that feature
    - [ ] Carousel auto-advances every ~5 seconds
    - [ ] Interacting (click/swipe) pauses auto-advance
    - [ ] Micro visual animations play only on the active card
    - [ ] Section heading reads "Every call handled. Every job captured."

    **ScrollLinePath:**
    - [ ] Copper sine wave renders correctly spanning How It Works through Features through Social Proof
    - [ ] Line does not appear cut off or misaligned at section boundaries

    **Mobile (resize to 375px or use device mode):**
    - [ ] How It Works steps fill viewport, reduced padding, smaller icons/numbers
    - [ ] Features carousel shows 1 card + small peek on right, swipeable
    - [ ] Arrow buttons hidden on mobile
    - [ ] Icon nav scrollable horizontally

    **Reduced motion (enable in OS accessibility settings):**
    - [ ] No fade/stagger animations on How It Works steps
    - [ ] No auto-advance on carousel
    - [ ] CSS micro visual keyframes paused
  </how-to-verify>
  <verify>User visually inspects the landing page and confirms both sections render correctly</verify>
  <done>User approves visual result of both redesigned sections at desktop and mobile viewports.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Build passes (`npm run build` exits 0)
2. All section IDs preserved
3. ScrollLinePath renders correctly
4. User approves visual result at desktop and mobile
</verification>

<success_criteria>
- Build passes cleanly
- User confirms both sections look correct visually
- ScrollLinePath copper line renders correctly with new section heights
- Mobile responsive behavior verified
</success_criteria>

<output>
After completion, create `.planning/phases/36-landing-page-section-redesign-how-it-works-minimalism-and-features-carousel/36-03-SUMMARY.md`
</output>
