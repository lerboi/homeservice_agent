---
phase: 35-invoice-integrations-and-ai
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/invoices/new/page.js
autonomous: true
gap_closure: true
requirements: [D-06]

must_haves:
  truths:
    - "Owner can edit an existing draft invoice by clicking Edit on the detail page"
    - "Owner sees the AI Describe button in the invoice editor when editing a saved invoice linked to a lead with call transcripts"
    - "Owner clicks AI Describe and receives generated descriptions from Gemini Flash"
  artifacts:
    - path: "src/app/dashboard/invoices/new/page.js"
      provides: "Edit mode handling via ?edit=id query param"
      contains: "searchParams.get('edit')"
  key_links:
    - from: "src/app/dashboard/invoices/new/page.js"
      to: "/api/invoices/[id]"
      via: "fetch GET when edit param present"
      pattern: "fetch.*api/invoices/"
    - from: "src/app/dashboard/invoices/new/page.js"
      to: "InvoiceEditor"
      via: "invoiceId, leadId, hasTranscript props"
      pattern: "invoiceId=|leadId=|hasTranscript="
---

<objective>
Fix the broken edit flow and unreachable AI Describe button in the invoice editor.

Purpose: The AI Describe button in InvoiceEditor is gated behind `{invoiceId && ...}` but no parent page ever passes `invoiceId`, `leadId`, or `hasTranscript` props. Additionally, the Edit button on the invoice detail page routes to `/dashboard/invoices/new?edit=${id}` but `new/page.js` ignores the `edit` query param entirely. This plan fixes both issues by making `new/page.js` handle edit mode.
Output: Updated `new/page.js` that supports both create and edit modes, passing all required props to InvoiceEditor.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-invoice-integrations-and-ai/35-CONTEXT.md
@.planning/phases/35-invoice-integrations-and-ai/35-VERIFICATION.md

<interfaces>
<!-- InvoiceEditor props (from src/components/dashboard/InvoiceEditor.jsx line 78): -->
```javascript
export default function InvoiceEditor({ initialData, settings, onSave, onSend, saving, invoiceId, leadId, hasTranscript })
```

<!-- GET /api/invoices/[id] response shape: -->
```javascript
{ invoice: { id, tenant_id, lead_id, customer_name, customer_phone, customer_email, customer_address, job_type, issued_date, due_date, notes, payment_terms, status, ... }, line_items: [{ id, item_type, description, quantity, unit_price, markup_pct, taxable, sort_order, line_total, ... }] }
```

<!-- PATCH /api/invoices/[id] accepts (for draft status): -->
```javascript
{ customer_name, customer_phone, customer_email, customer_address, job_type, issued_date, due_date, notes, payment_terms, line_items: [...] }
```

<!-- AI Describe button gate condition (InvoiceEditor.jsx line 462): -->
```jsx
{invoiceId && (
  <TooltipProvider>
    <Tooltip>
      <TooltipTrigger asChild>
        <span>
          <Button disabled={!leadId || !hasTranscript || aiGenerating} onClick={handleAiDescribe}>
```

<!-- lead_calls junction table: lead_id + call_id composite PK -->
<!-- calls table has transcript_text column -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edit mode to new/page.js — fetch invoice, resolve transcript, pass props to InvoiceEditor</name>
  <files>src/app/dashboard/invoices/new/page.js</files>
  <read_first>
    - src/app/dashboard/invoices/new/page.js (current state — the file being modified)
    - src/components/dashboard/InvoiceEditor.jsx (understand the props: invoiceId, leadId, hasTranscript)
    - src/app/api/invoices/[id]/route.js (GET response shape: { invoice, line_items })
    - src/app/dashboard/invoices/[id]/page.js (line 586 — Edit button routes to new?edit=${id})
  </read_first>
  <action>
Modify `src/app/dashboard/invoices/new/page.js` to support edit mode when `?edit=id` is present:

1. Read the `edit` query param: `const editId = searchParams.get('edit');`

2. In the `loadData` useEffect, add an edit-mode branch AFTER settings fetch:
   - If `editId` is truthy, fetch the existing invoice: `GET /api/invoices/${editId}`
   - From the response, extract `invoice` and `line_items`
   - Set `initialData` to include ALL existing invoice fields: `customer_name`, `customer_phone`, `customer_email`, `customer_address`, `job_type`, `issued_date`, `due_date`, `notes`, `payment_terms`, AND `line_items` (the array from the response), AND `lead_id` from `invoice.lead_id`
   - Store `editId` in a new state variable `editInvoiceId` (set to `editId` when fetch succeeds, null otherwise)
   - Store `invoice.lead_id` in a new state variable `editLeadId`
   - If `invoice.lead_id` exists, check for transcripts by querying: `GET /api/leads/${invoice.lead_id}` and then check if the lead has call transcripts. The simplest approach: fetch lead_calls via a small API call. Since there's no dedicated endpoint, use the Supabase approach via an inline check in the edit-load flow. Actually, the cleanest approach is to add a small fetch to the ai-describe endpoint's prerequisite check. But simpler: just query `/api/calls?lead_id=${invoice.lead_id}&limit=1` to see if calls with transcripts exist. BUT that endpoint may not support lead_id filter. Instead, use a pragmatic approach: set `hasTranscript` to `true` when `lead_id` exists (the AI Describe endpoint itself validates transcript availability and returns a clear error if none). This avoids adding a new API endpoint.
   - Actually, re-reading the ai-describe route: it fetches transcripts via `getTranscriptsForLead(supabase, leadId)` and returns error if empty. So setting `hasTranscript = !!invoice.lead_id` is acceptable as a first pass — the button will be enabled when a lead is linked, and the endpoint handles the "no transcript" case with a clear error toast. For a more accurate check, create a lightweight fetch. The BEST approach: fetch from the existing pattern. Check if lead_calls has any rows with transcript_text. Add a small API helper OR just set hasTranscript based on lead_id presence. Go with: `hasTranscript = !!invoice.lead_id` — the endpoint validates and returns user-friendly error.
   - Store `hasTranscript` in a new state variable `editHasTranscript`

3. If `editId` is present but NOT `leadId` param, skip the lead pre-fill fetch (edit mode takes precedence over lead_id param).

4. Change `handleSave` for edit mode: if `editInvoiceId` is set, use PATCH `/api/invoices/${editInvoiceId}` instead of POST `/api/invoices`. The PATCH body is the same shape as the create body (customer fields + line_items). On success, redirect to `/dashboard/invoices/${editInvoiceId}`.

5. Change `handleSend` for edit mode: if `editInvoiceId` is set, PATCH the invoice first (save changes), then POST to `/api/invoices/${editInvoiceId}/send`. Redirect to detail page.

6. Update the page header: when in edit mode, show "Edit Invoice" instead of "New Invoice".

7. Pass `invoiceId={editInvoiceId}`, `leadId={editLeadId}`, `hasTranscript={editHasTranscript}` to InvoiceEditor. These are null/false in create mode (existing behavior preserved).

8. In the InvoiceEditor initialData for edit mode, include `line_items` from the fetched invoice so the editor pre-populates existing line items. The InvoiceEditor already has a useEffect that applies initialData — but it does NOT handle line_items. So ALSO add `existingLineItems` handling: pass `initialLineItems` as a separate prop OR include line_items in initialData. Looking at InvoiceEditor line 91, `lineItems` state is initialized to `[emptyLineItem(0)]`. The useEffect at line 107 only handles customer fields. The cleanest fix: add a new useEffect in... wait, we should NOT modify InvoiceEditor in this task (keep scope tight). Instead, include line_items in initialData and note that InvoiceEditor needs a small update. Actually, looking more carefully: we CAN pass line_items in initialData and add a check in the existing useEffect. But the instruction says modify only new/page.js. Let me reconsider — InvoiceEditor.jsx WILL need a small change to handle initialData.line_items. Include that in this task since it's a 2-line addition.

9. In `InvoiceEditor.jsx`, add to the existing useEffect (line 107-117) after the customer field checks:
   ```
   if (initialData.line_items && initialData.line_items.length > 0) {
     setLineItems(initialData.line_items);
   }
   ```
   Also add initialData handling for `issued_date`, `due_date`, `notes`, `payment_terms`:
   ```
   if (initialData.issued_date) setIssuedDate(initialData.issued_date);
   if (initialData.payment_terms) setPaymentTerms(initialData.payment_terms);
   if (initialData.due_date) setDueDate(initialData.due_date);
   if (initialData.notes !== undefined) setNotes(initialData.notes);
   ```

10. Remove the stale comment on line 97 of new/page.js: `// Delivery not yet wired — silently ignore (Plan 07)` — delivery IS wired via sendSingleInvoice.
  </action>
  <verify>
    <automated>cd "C:/Users/leheh/.Projects/homeservice_agent" && node -e "const fs=require('fs'); const c=fs.readFileSync('src/app/dashboard/invoices/new/page.js','utf8'); const checks=['searchParams.get(\"edit\")','api/invoices/','invoiceId=','leadId=','hasTranscript=','Edit Invoice','PATCH','editInvoiceId']; const pass=checks.every(s=>c.includes(s)); console.log(pass?'PASS: all edit-mode patterns found':'FAIL: missing patterns'); checks.forEach(s=>{if(!c.includes(s))console.log('  MISSING:',s)}); const e=fs.readFileSync('src/components/dashboard/InvoiceEditor.jsx','utf8'); const eChecks=['initialData.line_items','setLineItems(initialData']; const ePass=eChecks.every(s=>e.includes(s)); console.log(ePass?'PASS: InvoiceEditor line_items hydration found':'FAIL: InvoiceEditor missing line_items hydration'); eChecks.forEach(s=>{if(!e.includes(s))console.log('  MISSING:',s)})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/invoices/new/page.js` contains `searchParams.get('edit')` or `searchParams.get("edit")`
    - `src/app/dashboard/invoices/new/page.js` contains a fetch to `/api/invoices/${editId}` (or equivalent) when edit param is present
    - `src/app/dashboard/invoices/new/page.js` passes `invoiceId={editInvoiceId}` to InvoiceEditor
    - `src/app/dashboard/invoices/new/page.js` passes `leadId={editLeadId}` to InvoiceEditor
    - `src/app/dashboard/invoices/new/page.js` passes `hasTranscript={editHasTranscript}` to InvoiceEditor
    - `src/app/dashboard/invoices/new/page.js` uses PATCH method when editInvoiceId is set (not POST)
    - `src/app/dashboard/invoices/new/page.js` shows "Edit Invoice" heading when in edit mode
    - `src/components/dashboard/InvoiceEditor.jsx` useEffect handles `initialData.line_items` — sets lineItems state from it
    - `src/components/dashboard/InvoiceEditor.jsx` useEffect handles `initialData.issued_date`, `initialData.payment_terms`, `initialData.due_date`, `initialData.notes`
    - The stale "Plan 07" comment is removed from new/page.js
  </acceptance_criteria>
  <done>
    - Navigating to /dashboard/invoices/new?edit={id} fetches the existing invoice and pre-populates InvoiceEditor with all fields including line items
    - The AI Describe button is visible when editing an invoice that has a linked lead
    - Saving in edit mode PATCHes the existing invoice (not creates a new one)
    - Sending in edit mode PATCHes then sends the existing invoice
    - Create mode (no edit param) works exactly as before — no regressions
  </done>
</task>

</tasks>

<verification>
1. Open `/dashboard/invoices/new` (no params) — should show blank "New Invoice" form, no AI Describe button visible (create mode, no invoiceId)
2. Create and save a draft invoice linked to a lead — navigate to its detail page — click Edit — should redirect to `/dashboard/invoices/new?edit={id}` — form should be pre-populated with all invoice data including line items
3. If the linked lead has call transcripts, the AI Describe button should be visible and clickable
4. Make changes in edit mode, click Save — should PATCH the invoice, not create a duplicate
5. Verify the invoice detail page shows the updated data after edit-save
</verification>

<success_criteria>
- The edit flow works end-to-end: detail page Edit button -> pre-populated editor -> save updates existing invoice
- AI Describe button is reachable when editing invoices with linked leads
- Create mode is unaffected (no regressions)
- D-06 (manual trigger via AI Describe button) is no longer blocked
</success_criteria>

<output>
After completion, create `.planning/phases/35-invoice-integrations-and-ai/35-06-SUMMARY.md`
</output>
