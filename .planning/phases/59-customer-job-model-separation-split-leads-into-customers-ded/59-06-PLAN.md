---
phase: 59-customer-job-model-separation
plan: 06
type: execute
wave: 3
depends_on: [04]
files_modified:
  - src/app/dashboard/jobs/page.js
  - src/app/dashboard/inquiries/page.js
  - src/components/dashboard/JobCard.jsx
  - src/components/dashboard/JobFilterBar.jsx
  - src/components/dashboard/JobStatusPills.jsx
  - src/components/dashboard/InquiryCard.jsx
  - src/components/dashboard/InquiryFilterBar.jsx
  - src/components/dashboard/InquiryStatusPills.jsx
  - src/components/dashboard/EmptyStateJobs.jsx
  - src/components/dashboard/EmptyStateInquiries.jsx
  - src/components/dashboard/HotJobsTile.jsx
  - src/components/layout/DashboardSidebar.jsx
  - src/components/layout/BottomTabBar.jsx
  - src/lib/chatbot-knowledge/customers.md
  - src/lib/chatbot-knowledge/jobs.md
  - src/lib/chatbot-knowledge/inquiries.md
  - tests/realtime/jobs.test.js
autonomous: true
requirements: [D-08, D-09, D-15]
tags: [phase-59, wave-3, dashboard, ui, realtime, chatbot-corpus]

must_haves:
  truths:
    - "/dashboard/jobs renders jobs list sourced from /api/jobs (not /api/leads)"
    - "/dashboard/inquiries renders inquiries list sourced from /api/inquiries"
    - "Jobs tab status pill strip shows: Scheduled → Completed → Paid ... (ml-2 gap) ... Cancelled, Lost (D-09 + Phase 52 Lost-gap)"
    - "Inquiries tab status pill strip shows: Open → Converted ... (ml-2 gap) ... Lost (D-09)"
    - "Sidebar and BottomTabBar have Inquiries nav item below existing Jobs (D-08)"
    - "Jobs tab Realtime subscribes to `jobs` table (not `leads`) filtered by tenant_id (D-15)"
    - "Inquiries tab Realtime subscribes to `inquiries` table filtered by tenant_id (D-15)"
    - "Jobs tab preserves Phase 49 pill palette, Phase 46 VIP badge, Phase 52 Lost-gap, filter bar controls, empty state, dark mode — UI-SPEC parity checklist all green"
    - "Chatbot knowledge corpus `leads.md` replaced by `customers.md`, `jobs.md`, `inquiries.md` with terminology matching UI-SPEC §Terminology"
  artifacts:
    - path: "src/app/dashboard/jobs/page.js"
      provides: "Rewritten Jobs tab"
    - path: "src/app/dashboard/inquiries/page.js"
      provides: "New Inquiries tab"
    - path: "src/components/dashboard/JobStatusPills.jsx"
      provides: "Renamed + updated status pill strip"
      contains: "scheduled"
    - path: "src/components/dashboard/InquiryStatusPills.jsx"
      provides: "New pill strip with open/converted/lost"
  key_links:
    - from: "src/app/dashboard/jobs/page.js"
      to: "/api/jobs"
      via: "fetch in useEffect or server-component"
      pattern: "/api/jobs"
    - from: "src/app/dashboard/jobs/page.js"
      to: "supabase.channel(...).on('postgres_changes', { table: 'jobs', filter: `tenant_id=eq.${id}` })"
      via: "Realtime subscription"
      pattern: "table: 'jobs'"
---

<objective>
Rewrite the Jobs tab to read from `/api/jobs` + subscribe to `jobs` Realtime channel (replacing leads). Add the new `/dashboard/inquiries` tab with its own pill set, flyout, and Realtime subscription. Rename lead-prefixed components to job-prefixed (where stated in UI-SPEC rename map) or clone to inquiry-prefixed. Update sidebar + bottom tab bar with the new Inquiries nav item. Rewrite chatbot knowledge corpus to use Customer/Job/Inquiry terminology.

Purpose: Implements D-08 (two top-level tabs), D-09 (distinct pill sets per tab with Phase 49 palette + Phase 52 Lost-gap preserved), D-15 (Realtime per tab). UI-SPEC §Component Inventory rename/clone map is the contract.

Output: Rewritten Jobs page + new Inquiries page + renamed/cloned components + nav updates + new chatbot corpus. Legacy `LeadFlyout.jsx` etc. preserved until Plan 07 (Customer detail + flyouts) replaces them, and `/api/leads/*` + `src/lib/leads.js` preserved until Plan 08 cleanup.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md
@.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-CONTEXT.md
@src/app/dashboard/jobs/page.js
@src/components/dashboard/LeadStatusPills.jsx
@src/components/dashboard/LeadCard.jsx
@src/components/dashboard/LeadFilterBar.jsx
@src/components/dashboard/EmptyStateLeads.jsx
@src/components/dashboard/HotLeadsTile.jsx
@src/components/layout/DashboardSidebar.jsx
@src/components/layout/BottomTabBar.jsx
@src/lib/chatbot-knowledge/leads.md
@src/lib/design-tokens.js
@.claude/skills/dashboard-crm-system/SKILL.md

<interfaces>
From Plan 04 — API response shapes:
- GET /api/jobs → `{jobs: [{id, status, urgency, revenue_amount, is_vip, created_at, customer: {...}, appointment: {...}, calls: [...]}]}`
- GET /api/inquiries → `{inquiries: [{id, status, urgency, job_type, service_address, created_at, customer: {...}}]}`

From UI-SPEC §Color — status pill classes to reuse VERBATIM:
- scheduled: `bg-blue-600 dark:bg-blue-500 text-white`
- completed: `bg-stone-700 dark:bg-stone-600 text-white`
- paid: `bg-[#166534] dark:bg-emerald-600 text-white`
- lost: `bg-red-600 dark:bg-red-500 text-white` (+ ml-2)
- cancelled: `bg-stone-500 dark:bg-stone-600 text-white` (+ ml-2)
- open (inquiry): `bg-[var(--brand-accent)] text-[var(--brand-accent-fg)]`
- converted (inquiry): `bg-[#166534] dark:bg-emerald-600 text-white`

From UI-SPEC §Navigation:
- Sidebar: Jobs stays; add Inquiries below with lucide `PhoneIncoming` icon
- BottomTabBar: add Inquiries — may require demoting the lowest-use tab into More overflow (Claude's discretion — UI-SPEC flags)

UI-SPEC §Parity checklist (lines 240-252) must all hold post-rewrite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Jobs tab + rename Lead* → Job* components + HotJobsTile</name>
  <read_first>
    - src/app/dashboard/jobs/page.js (current implementation — source for query shape, subscription wiring, filter handlers to preserve)
    - src/components/dashboard/LeadStatusPills.jsx (copy classes verbatim; add cancelled pill)
    - src/components/dashboard/LeadCard.jsx (JobCard is visually identical — only query source changes)
    - src/components/dashboard/LeadFilterBar.jsx (preserve filter controls)
    - src/components/dashboard/EmptyStateLeads.jsx (clone copy from UI-SPEC §Empty states)
    - src/components/dashboard/HotLeadsTile.jsx (query source shifts to jobs with urgency=emergency + status=scheduled)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md (§Component Inventory rename map, §Jobs tab parity checklist lines 240-252)
    - src/lib/design-tokens.js (card/focus/selected tokens)
  </read_first>
  <files>src/app/dashboard/jobs/page.js, src/components/dashboard/JobCard.jsx, src/components/dashboard/JobFilterBar.jsx, src/components/dashboard/JobStatusPills.jsx, src/components/dashboard/EmptyStateJobs.jsx, src/components/dashboard/HotJobsTile.jsx, tests/realtime/jobs.test.js</files>
  <behavior>
    After rewrite:
    - `/dashboard/jobs` fetches from `/api/jobs`, not `/api/leads`
    - Realtime subscription filter: `table: 'jobs', filter: 'tenant_id=eq.<uuid>'`
    - Visual parity with pre-rewrite Jobs tab (UI-SPEC §Parity checklist): status pill strip, filter bar, card rows, VIP badge, urgency badge, escalation indicators, empty state layout, dark mode variants — all preserved
    - NEW: cancelled status pill next to lost with `ml-2` gap (D-09 + Phase 52 layout)
    - NEW: JobCard.customer.name links to `/dashboard/customers/{customer_id}` with `hover:underline`
    - JobStatusPills status list is: `['scheduled', 'completed', 'paid', 'lost', 'cancelled']` with `lost` and `cancelled` each prefixed `ml-2` (two gaps total — Lost-gap from Phase 52 + cancelled-gap as spec'd in UI-SPEC §Color)
    - EmptyStateJobs uses UI-SPEC copy verbatim: heading "No jobs yet", body "When Voco books an appointment, it shows up here. Check call history to see recent inquiries."
    - HotJobsTile query: `jobs` where `urgency='emergency' AND status='scheduled'` (replaces prior leads-based query)
    - Realtime test flipped from skip to active: inserts a row into `jobs` via service-role, asserts subscription handler fires with matching payload
  </behavior>
  <action>
    1. Copy `LeadStatusPills.jsx` → `JobStatusPills.jsx`. Edit the statuses array: replace `new/booked/completed/paid/lost` with `scheduled/completed/paid/lost/cancelled`. Preserve ALL className strings verbatim (UI-SPEC explicitly requires). Add `cancelled` pill with `bg-stone-500 dark:bg-stone-600 text-white ml-2`. Keep `lost` pill `ml-2` (Phase 52 gap).
    2. Copy `LeadCard.jsx` → `JobCard.jsx`. Change prop signature from `lead` to `job`. Read fields from new shape: `job.customer.name`, `job.customer.phone_e164`, `job.appointment.start_time`, `job.urgency`, `job.status`, `job.is_vip`. Wrap `customer.name` in a `<Link href={`/dashboard/customers/${job.customer.id}`} className="hover:underline">` (UI-SPEC navigation rule).
    3. Copy `LeadFilterBar.jsx` → `JobFilterBar.jsx`. Preserve all controls; swap status enum options to jobs status list.
    4. Copy `EmptyStateLeads.jsx` → `EmptyStateJobs.jsx`. Use UI-SPEC copy:
       - No jobs yet (heading) + "When Voco books an appointment, it shows up here. Check call history to see recent inquiries."
       - Filter-zero variant: "No jobs match these filters" + "Try clearing a filter or widening the date range." with lucide `SearchX` icon.
    5. Copy `HotLeadsTile.jsx` → `HotJobsTile.jsx`. Swap query to `jobs` table with `urgency='emergency' AND status='scheduled'` filter.
    6. Rewrite `src/app/dashboard/jobs/page.js`:
       - Replace fetch from `/api/leads` → `/api/jobs`
       - Replace Realtime channel from `leads` table → `jobs` table
       - Replace all component imports (`LeadCard` → `JobCard`, `LeadFilterBar` → `JobFilterBar`, `LeadStatusPills` → `JobStatusPills`, `EmptyStateLeads` → `EmptyStateJobs`)
       - Wire HotJobsTile import if the dashboard-home tile imports from here
       - Preserve URL query param handling, sort order, pagination, scroll position
    7. Do NOT delete `LeadStatusPills.jsx`, `LeadCard.jsx`, `LeadFilterBar.jsx`, `EmptyStateLeads.jsx`, `HotLeadsTile.jsx` yet — they may still be referenced by legacy routes or dashboard-home until Plan 08 sweeps. Task 3 deletes them along with legacy corpus + /api/leads.
    8. Flip `tests/realtime/jobs.test.js` from skip to active: set up a Supabase channel subscription, INSERT via service-role, assert the subscriber handler fired with the new row payload within 3 seconds. Use existing Realtime test pattern if present; otherwise document fixture.
  </action>
  <verify>
    <automated>grep -q "/api/jobs" src/app/dashboard/jobs/page.js &amp;&amp; ! grep -q "/api/leads" src/app/dashboard/jobs/page.js &amp;&amp; grep -q "table: 'jobs'" src/app/dashboard/jobs/page.js &amp;&amp; grep -q "bg-stone-500" src/components/dashboard/JobStatusPills.jsx &amp;&amp; grep -q "ml-2" src/components/dashboard/JobStatusPills.jsx &amp;&amp; test -f src/components/dashboard/JobCard.jsx &amp;&amp; npx vitest run tests/realtime/jobs.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/jobs/page.js` has zero `/api/leads` refs and zero `table: 'leads'` refs
    - JobStatusPills has exactly 5 pills matching D-09 Jobs status list with cancelled+lost both carrying `ml-2`
    - JobCard.customer.name wrapped in Link with href `/dashboard/customers/${customer_id}`
    - HotJobsTile queries `jobs` table
    - Realtime test passes (no skip)
    - Zero breaking imports (build succeeds): `npm run build` — reviewer spot-checks
    - UI-SPEC parity checklist items (lines 240-252) all preserved
  </acceptance_criteria>
  <done>Jobs tab rewritten against new schema with full visual parity and Realtime wired to `jobs`.</done>
</task>

<task type="auto">
  <name>Task 2: New Inquiries tab + Inquiry components + sidebar/bottom-tab nav</name>
  <read_first>
    - src/components/dashboard/JobCard.jsx (Task 1 output — clone for InquiryCard)
    - src/components/dashboard/JobFilterBar.jsx (Task 1 — clone for InquiryFilterBar)
    - src/components/dashboard/JobStatusPills.jsx (Task 1 — reference pill classes for clone)
    - src/components/layout/DashboardSidebar.jsx (nav registry)
    - src/components/layout/BottomTabBar.jsx (mobile nav)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md (§Inquiries tab, §Navigation)
  </read_first>
  <files>src/app/dashboard/inquiries/page.js, src/components/dashboard/InquiryCard.jsx, src/components/dashboard/InquiryFilterBar.jsx, src/components/dashboard/InquiryStatusPills.jsx, src/components/dashboard/EmptyStateInquiries.jsx, src/components/layout/DashboardSidebar.jsx, src/components/layout/BottomTabBar.jsx</files>
  <behavior>
    - `/dashboard/inquiries` renders inquiry list from `/api/inquiries`
    - Page skeleton mirrors Jobs tab (same outer structure), different pills + filter bar + empty state
    - Row click opens InquiryFlyout (built in Plan 07 — Task 2 of this plan imports a shim component that returns null; Plan 07 replaces)
    - Realtime subscribes to `inquiries` table with `tenant_id=eq.<uuid>` filter
    - Sidebar has Inquiries nav item below Jobs, icon `PhoneIncoming`, active-state matches existing treatment
    - BottomTabBar includes Inquiries tab; if this exceeds 5 visible tabs, demote the lowest-use tab per Claude's discretion (UI-SPEC flags this decision — document choice in SUMMARY)
    - InquiryStatusPills has 3 pills: `open` / `converted` / `lost` with `lost` prefixed `ml-2` (Phase 52 gap preserved)
    - EmptyStateInquiries uses UI-SPEC copy: heading "No open inquiries", body "Callers who didn't book will land here. Nothing to chase right now.", icon lucide `PhoneIncoming`
  </behavior>
  <action>
    1. Create `InquiryStatusPills.jsx`: 3 pills using UI-SPEC classes:
       - `open`: `bg-[var(--brand-accent)] text-[var(--brand-accent-fg)]`
       - `converted`: `bg-[#166534] dark:bg-emerald-600 text-white`
       - `lost`: `bg-red-600 dark:bg-red-500 text-white ml-2`
    2. Create `InquiryCard.jsx` (clone of JobCard without appointment row; displays `job_type`, `service_address`, `urgency` badge, customer name link).
    3. Create `InquiryFilterBar.jsx` (clone of JobFilterBar with status options `open|converted|lost`).
    4. Create `EmptyStateInquiries.jsx` using UI-SPEC copy.
    5. Create `src/app/dashboard/inquiries/page.js`:
       - Same general shape as `src/app/dashboard/jobs/page.js`
       - Fetch `/api/inquiries`
       - Realtime subscribe to `inquiries` table
       - Default filter: `status=open`
       - Row onClick: stub handler logging `console.debug('Plan 07 InquiryFlyout open')` — Plan 07 replaces with real flyout
    6. Update `src/components/layout/DashboardSidebar.jsx`: add new nav entry below Jobs. Use existing entry shape (path, icon, label, badge).
    7. Update `src/components/layout/BottomTabBar.jsx`: add Inquiries tab. If current tab count is 5, demote the bottom-most tab into More-menu overflow; record the demoted tab ID + reason in SUMMARY.
    8. Breadcrumbs: add `Dashboard → Inquiries` mapping in whatever breadcrumb registry the dashboard uses (grep for existing registrations).
  </action>
  <verify>
    <automated>test -f src/app/dashboard/inquiries/page.js &amp;&amp; grep -q "/api/inquiries" src/app/dashboard/inquiries/page.js &amp;&amp; grep -q "table: 'inquiries'" src/app/dashboard/inquiries/page.js &amp;&amp; grep -q "PhoneIncoming" src/components/layout/DashboardSidebar.jsx &amp;&amp; grep -q "Inquiries" src/components/layout/BottomTabBar.jsx &amp;&amp; grep -q "ml-2" src/components/dashboard/InquiryStatusPills.jsx</automated>
  </verify>
  <acceptance_criteria>
    - 4 new component files + 1 new page file exist
    - Sidebar has Inquiries below Jobs using PhoneIncoming icon
    - BottomTabBar includes Inquiries (overflow decision documented)
    - InquiryStatusPills has exactly 3 pills; lost has `ml-2`
    - Realtime subscription uses table `inquiries` (not `leads`)
  </acceptance_criteria>
  <done>D-08 two-tab nav complete; D-09 Inquiries pills present; D-15 Realtime wired.</done>
</task>

<task type="auto">
  <name>Task 3: Chatbot knowledge corpus split + terminology update</name>
  <read_first>
    - src/lib/chatbot-knowledge/leads.md (current corpus content)
    - src/lib/chatbot-knowledge/ (ls — confirm other files/structure)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md (§Terminology hard rules)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-CONTEXT.md (D-08 two-tab model; mental model mapping)
  </read_first>
  <files>src/lib/chatbot-knowledge/customers.md, src/lib/chatbot-knowledge/jobs.md, src/lib/chatbot-knowledge/inquiries.md</files>
  <behavior>
    After split:
    - `customers.md` documents: what a Customer is, phone dedup rule, merge + undo flow, how to navigate to a Customer detail page
    - `jobs.md` documents: what a Job is (booked work with appointment), status lifecycle (Scheduled → Completed → Paid / Cancelled / Lost), how to filter + find jobs, Kanban-absent-by-design note
    - `inquiries.md` documents: what an Inquiry is (unbooked call), status lifecycle (Open → Converted / Lost), same-call auto-convert rule, manual Convert to Job flow
    - UI-SPEC §Terminology hard rules honored: use Customer/Job/Inquiry; never "Lead". Files themselves are authored after Phase 52 reframe, so "lead" is already absent from Phase 52's reframed corpus if it existed — verify by grep.
    - Legacy `leads.md` NOT deleted in this task — Plan 08 deletes it to avoid breaking any stale references; this task creates replacements.
  </behavior>
  <action>
    Create the three new markdown files with structure:
    ```
    # Customers (or Jobs / Inquiries)

    ## What this is
    <1-paragraph explanation in owner-friendly voice>

    ## Where it lives in the dashboard
    <path + navigation>

    ## Fields
    <bullet list of key fields>

    ## Lifecycle / status flow
    <diagram-as-text or bullets>

    ## Common tasks
    - <task 1>
    - <task 2>

    ## Related
    - Customers → Jobs (see jobs.md)
    - ...
    ```
    Follow existing `leads.md` structure if it has a different convention. Use verbs + tone matching UI-SPEC copywriting contract ("Call back", "Mark paid", "Voco"). Never "Lead".

    Verify grep after write: `grep -RIn "\\bLead\\b" src/lib/chatbot-knowledge/{customers,jobs,inquiries}.md` returns 0 matches (allowing "release", "lead-in" etc. only via word-boundary and context check).
  </action>
  <verify>
    <automated>test -f src/lib/chatbot-knowledge/customers.md &amp;&amp; test -f src/lib/chatbot-knowledge/jobs.md &amp;&amp; test -f src/lib/chatbot-knowledge/inquiries.md &amp;&amp; ! grep -RIw "Lead\\|Leads\\|lead record" src/lib/chatbot-knowledge/customers.md src/lib/chatbot-knowledge/jobs.md src/lib/chatbot-knowledge/inquiries.md</automated>
  </verify>
  <acceptance_criteria>
    - 3 new markdown files exist with structure described
    - Zero user-facing "Lead" word-boundary matches
    - Legacy leads.md untouched (Plan 08 deletes)
    - Each file documents its entity, location, fields, lifecycle, common tasks
  </acceptance_criteria>
  <done>Chatbot corpus reframed for new terminology; UI-SPEC hard rule honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard Realtime channel → browser | Supabase Realtime payload scoped by tenant_id filter; RLS on underlying table also enforces. |
| Chatbot corpus → model context | Read-only markdown files; no user-controlled input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06-01 | Information Disclosure | Realtime filter malformed → receives other tenants' rows (RESEARCH Pitfall 4) | mitigate | Explicit `filter: 'tenant_id=eq.<uuid>'` string template; test asserts cross-tenant INSERT does NOT fire handler. |
| T-59-06-02 | Tampering | Dashboard route pulls `/api/leads` by mistake (post-Plan 04) | mitigate | Plan 08 deletes `/api/leads` entirely; this plan grep-verifies no `/api/leads` in new page files. |
| T-59-06-03 | Information Disclosure | Chatbot corpus leaks internal schema/RPC names | accept | Corpus serves owner-facing documentation — schema terms like "customers table" are acceptable in dev-phase. |
| T-59-06-04 | XSS | InquiryCard renders customer.name/phone unescaped | accept | React auto-escapes interpolated text. Only attribute-injection vectors would bypass; none in use. |
</threat_model>

<verification>
- `/dashboard/jobs` fetches from `/api/jobs` (grep-verified)
- `/dashboard/inquiries` page exists and renders
- JobStatusPills has cancelled + lost with ml-2; InquiryStatusPills has 3 pills with lost ml-2
- Sidebar + BottomTabBar updated
- Realtime test green
- Chatbot corpus split into 3 files with no "Lead" word-boundary
</verification>

<success_criteria>
- D-08 nav + tabs complete
- D-09 pill sets per tab, Phase 49 palette + Phase 52 Lost-gap preserved
- D-15 Realtime subscriptions wired to new tables
- Jobs tab UI-SPEC parity checklist all green
</success_criteria>

<output>
After completion, create `.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-06-SUMMARY.md` — record BottomTabBar demotion decision if applicable.
</output>
