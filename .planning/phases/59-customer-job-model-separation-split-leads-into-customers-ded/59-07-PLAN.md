---
phase: 59-customer-job-model-separation
plan: 07
type: execute
wave: 3
depends_on: [04, 06]
files_modified:
  - src/app/dashboard/customers/[id]/page.js
  - src/components/dashboard/customers/CustomerDetailHeader.jsx
  - src/components/dashboard/customers/CustomerEditModal.jsx
  - src/components/dashboard/customers/CustomerMergeDialog.jsx
  - src/components/dashboard/customers/CustomerActivityTimeline.jsx
  - src/components/dashboard/customers/CustomerJobsList.jsx
  - src/components/dashboard/customers/CustomerInvoicesList.jsx
  - src/components/dashboard/customers/UnmergeBanner.jsx
  - src/components/dashboard/InquiryFlyout.jsx
  - src/components/dashboard/JobFlyout.jsx
autonomous: false
requirements: [D-10, D-17, D-18, D-19]
tags: [phase-59, wave-3, customer-detail, merge, flyouts, checkpoint]

must_haves:
  truths:
    - "/dashboard/customers/[id] renders sticky header + 3 tabs (Activity | Jobs | Invoices) per D-17"
    - "Header surfaces name, phone, default_address, lifetime_value, outstanding_balance, Jobber/Xero badges (when credentials present), VIP indicator"
    - "Edit Customer modal opens from header primary CTA, allows editing name/default_address/email/notes/tags, phone is read-only with help text (D-18)"
    - "Merge Customer dialog is 2-step: typeahead picker (Command) → preview counts + destructive confirm (AlertDialog) — D-19"
    - "UnmergeBanner appears for 7 days after merge on target customer with 'Undo merge' button"
    - "InquiryFlyout has 'Convert to Job' + 'Mark as Lost' buttons (D-10 offline path)"
    - "JobFlyout replaces LeadFlyout on the Jobs tab — preserves Phase 33-49 affordances (status change, audio, transcript, Create/View Invoice, VIP toggle)"
    - "Customer detail Realtime: 3 subscriptions (customers single-row, jobs by customer_id, inquiries by customer_id) per D-15"
  artifacts:
    - path: "src/app/dashboard/customers/[id]/page.js"
      provides: "Customer detail page"
    - path: "src/components/dashboard/customers/CustomerMergeDialog.jsx"
      provides: "Merge UX (D-19)"
      contains: "Merge Customer"
  key_links:
    - from: "CustomerMergeDialog.jsx"
      to: "POST /api/customers/:id/merge"
      via: "fetch on destructive confirm"
      pattern: "/merge"
    - from: "UnmergeBanner.jsx"
      to: "POST /api/customers/:id/unmerge"
      via: "fetch on 'Undo merge' click"
      pattern: "/unmerge"
    - from: "InquiryFlyout.jsx"
      to: "POST /api/inquiries/:id/convert"
      via: "Convert to Job button → QuickBook sheet → convert"
      pattern: "/convert"
---

<objective>
Build the Customer detail page (D-17 sticky header + 3 tabs), Edit modal (D-18), Merge + Unmerge UX (D-19 with 7-day undo), InquiryFlyout with offline Convert to Job button (D-10), and JobFlyout to replace LeadFlyout on the Jobs tab. All visual details follow UI-SPEC. This plan contains a human-verify checkpoint because the Customer detail page is a new surface and merge is destructive-with-undo — visual regression must be confirmed live.

Purpose: Close out the UI decisions D-10 (offline manual convert), D-17 (detail layout), D-18 (edit modal), D-19 (merge + undo).

Output: 9 new components + 1 new page + Sheet replacements for LeadFlyout. Legacy `LeadFlyout.jsx` preserved for one more plan — Plan 08 deletes after confirming no other referrers remain.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md
@.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-CONTEXT.md
@src/components/dashboard/LeadFlyout.jsx
@src/lib/design-tokens.js
@src/lib/phone/normalize.js
@src/app/dashboard/jobs/page.js
@.claude/skills/dashboard-crm-system/SKILL.md

<interfaces>
From Plan 04 APIs:
- GET /api/customers/[id] → `{customer, stats: {lifetime_value, outstanding_balance, jobs_count, open_inquiries_count}}`
- PATCH /api/customers/[id] body `{name, default_address, email, notes, tags}` → 200 updated
- GET /api/customers?search=<str> → `{customers: [...]}` (for Merge typeahead)
- POST /api/customers/[id]/merge body `{target_id}` → 200 `{moved_counts, target_id}`
- POST /api/customers/[id]/unmerge → 200 `{source_id, restored_from}` (410 if >7 days)
- POST /api/inquiries/[id]/convert body `{appointment_id}` → 200 `{job_id, inquiry_id}`
- GET /api/jobs?customer_id=<> → scoped jobs list
- GET /api/inquiries?customer_id=<> → scoped inquiries list

UI-SPEC components & copy:
- CustomerDetailHeader: lines 198
- CustomerEditModal: line 199 — phone read-only with help text "To change phone, use Merge"
- CustomerMergeDialog: line 200 — 2-step; final destructive confirm button "Merge Customer"
- UnmergeBanner: line 204 — "Undo merge" button; 7-day visible window
- InquiryFlyout: line 205 — Convert to Job + Mark as Lost
- JobFlyout: line 206 — preserves all LeadFlyout affordances

UI-SPEC copy table (lines 124-141) + empty states (lines 143-152) + destructive confirmations (lines 166-171) are contractual — use verbatim.

shadcn primitives (UI-SPEC §Registry Safety): `tabs`, `command`, `alert-dialog`, `dropdown-menu` — install if missing via `npx shadcn add`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer detail page + header + Edit modal + Activity timeline + scoped lists</name>
  <read_first>
    - src/components/dashboard/LeadFlyout.jsx (reference for Sheet patterns + affordances to preserve in JobFlyout)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md (§Component Inventory, §Interaction Contracts, §Copywriting Contract lines 124-171)
    - src/lib/design-tokens.js (card/focus/selected)
    - src/lib/phone/normalize.js (formatInternational for header display)
    - components.json (shadcn config — verify primitives)
  </read_first>
  <files>src/app/dashboard/customers/[id]/page.js, src/components/dashboard/customers/CustomerDetailHeader.jsx, src/components/dashboard/customers/CustomerEditModal.jsx, src/components/dashboard/customers/CustomerActivityTimeline.jsx, src/components/dashboard/customers/CustomerJobsList.jsx, src/components/dashboard/customers/CustomerInvoicesList.jsx</files>
  <behavior>
    Page layout (D-17):
    - Sticky header (pos-sticky top-<topbar-offset>) rendering CustomerDetailHeader
    - shadcn Tabs with triggers `Activity | Jobs | Invoices`, defaults to `activity`, URL `?tab=<value>` persists
    - Three tab panels render CustomerActivityTimeline, CustomerJobsList, CustomerInvoicesList respectively
    - 3 Realtime subscriptions: `customers` single-row, `jobs` filter=`customer_id=eq.<id>`, `inquiries` filter=`customer_id=eq.<id>` (D-15)
    - Loading: skeleton matching final layout
    - 404 state: UI-SPEC copy "Customer not found. They may have been merged into another record." + Back to Customers button

    CustomerDetailHeader:
    - Name in `text-2xl font-semibold tracking-tight`
    - Phone in `font-mono tabular-nums` formatted via `formatInternational()`, with copy-to-clipboard button (aria-label "Copy phone number")
    - Default address (muted)
    - Lifetime value + Outstanding balance stats (tabular-nums; outstanding > 0 → `text-red-600 dark:text-red-400 font-semibold`)
    - Jobber/Xero badges ONLY when `accounting_credentials` row exists for tenant+provider (query via existing BusinessIntegrationsClient data or a new GET) — gracefully absent if not connected (UI-SPEC lines 52-56)
    - VIP indicator if any related job/inquiry is_vip (violet-100/violet-700 Star badge per UI-SPEC line 116)
    - Primary CTA `Edit Customer` → opens CustomerEditModal
    - Overflow dropdown (DropdownMenu) with single item `Merge into another customer…` → opens CustomerMergeDialog

    CustomerEditModal:
    - shadcn Dialog
    - Fields: name (Input), default_address (Input), email (Input type=email), notes (Textarea), tags (free-form chip input — create on Enter, remove on backspace; no taxonomy per Deferred Ideas)
    - Phone (readOnly Input, muted treatment, help text "To change phone, use Merge" with lucide `Info` icon)
    - Save Changes button (brand accent); Cancel (secondary)
    - On submit: PATCH /api/customers/[id]; optimistic UI on success; inline error above form on failure
    - Close with unsaved changes: show shadcn AlertDialog "Discard changes?"

    CustomerActivityTimeline:
    - Fetches activity_log rows for customer_id (add light `GET /api/customers/[id]/activity` if not present via /api/customers/[id] response — either approach acceptable; document choice)
    - Renders chronological flat list most-recent-first with day separator when date changes
    - Icon per event type (call=Phone, booking=Calendar, invoice=FileText, note=StickyNote, inquiry=PhoneIncoming)
    - Empty state: UI-SPEC copy "No activity yet"

    CustomerJobsList:
    - Renders jobs list for this customer using the JobCard component (Plan 06)
    - Empty state: UI-SPEC "No jobs for this customer"

    CustomerInvoicesList:
    - Reuses existing invoice card
    - Gated: if `features_enabled.invoicing === false` → render "Invoicing is off" empty state + link to /dashboard/more/features (UI-SPEC line 151)
    - If ON but no invoices → UI-SPEC "No invoices yet" empty state
  </behavior>
  <action>
    1. Install any missing shadcn primitives: `npx shadcn add tabs command alert-dialog dropdown-menu` (skip if already present per `components.json` registry check).
    2. Create the 6 files with the structure described above.
    3. For CustomerEditModal, use the `updateCustomer` API from Plan 04. Forbidden field handling server-side returns 400 — surface as inline error.
    4. For CustomerActivityTimeline data fetching, decide between:
       (a) Extending `GET /api/customers/[id]` to include recent `activity_log` rows in response (small + simple)
       (b) Adding dedicated `GET /api/customers/[id]/activity` route
       Pick (a) if activity fits in ≤50 most-recent rows for Phase 59; pick (b) if pagination is needed. Default to (a); document choice in SUMMARY.
    5. Jobber/Xero badges: query `accounting_credentials` filtered by tenant (service-role proxied through a `/api/customers/[id]/integration-status` GET or reuse an existing helper). Gracefully render nothing if not connected — do not block page load.
    6. Follow UI-SPEC copywriting contract verbatim.
    7. Dark mode: use CSS-variable tokens + `dark:` variants — no hardcoded hex (UI-SPEC §Design System).
  </action>
  <verify>
    <automated>test -f src/app/dashboard/customers/[id]/page.js &amp;&amp; test -f src/components/dashboard/customers/CustomerDetailHeader.jsx &amp;&amp; test -f src/components/dashboard/customers/CustomerEditModal.jsx &amp;&amp; grep -q "Tabs" src/app/dashboard/customers/[id]/page.js &amp;&amp; grep -q "formatInternational" src/components/dashboard/customers/CustomerDetailHeader.jsx &amp;&amp; grep -q "To change phone, use Merge" src/components/dashboard/customers/CustomerEditModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Page + 5 component files exist
    - Tabs default to Activity; URL param `?tab=...` is wired
    - 3 Realtime subscriptions in page.js (customers, jobs, inquiries — grep for all three `table:` strings)
    - Edit modal phone field has readOnly + exact help text
    - CustomerInvoicesList correctly gates on `features_enabled.invoicing`
    - UI-SPEC copy for empty states used verbatim (grep a few exact phrases)
  </acceptance_criteria>
  <done>Customer detail page with header + 3 tabs + edit modal functional; D-17 + D-18 delivered.</done>
</task>

<task type="auto">
  <name>Task 2: CustomerMergeDialog + UnmergeBanner + InquiryFlyout + JobFlyout</name>
  <read_first>
    - src/components/dashboard/LeadFlyout.jsx (copy affordances into JobFlyout)
    - .planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-UI-SPEC.md (§Interaction Contracts Merge flow + Edit modal + InquiryFlyout sections)
    - Task 1 output (CustomerDetailHeader — Merge dialog triggers from overflow menu)
    - src/lib/design-tokens.js (destructive button variant reference)
  </read_first>
  <files>src/components/dashboard/customers/CustomerMergeDialog.jsx, src/components/dashboard/customers/UnmergeBanner.jsx, src/components/dashboard/InquiryFlyout.jsx, src/components/dashboard/JobFlyout.jsx</files>
  <behavior>
    CustomerMergeDialog (D-19):
    - Step 1 (Dialog with Command typeahead): searches customers by name/phone via GET /api/customers?search=<q>. Min 2 chars. Max 8 results. Disabled rows: self (id === current), already-merged (`merged_into IS NOT NULL`). `Continue` button advances to step 2 when target selected. Cancel closes.
    - Step 2 (AlertDialog, destructive): preview counts `{N} jobs, {M} inquiries, {K} invoices, {L} call recordings will move to {Target Name}` — numbers queried via a preflight `GET /api/customers/[source]/merge-preview?target_id=<target>` OR computed client-side via the customer detail stats (simpler; document choice). Shows UI-SPEC copy verbatim. Confirm button `Merge Customer` (destructive variant). Cancel returns to Step 1.
    - On destructive confirm: POST /api/customers/[id]/merge → on success, navigate to `/dashboard/customers/{target_id}` with `?recently_merged=1` so UnmergeBanner renders.

    UnmergeBanner (D-19):
    - Appears on target customer detail page when `?recently_merged=1` present OR when the target has `customers` rows where `merged_into = <target_id> AND merged_at > now() - interval '7 days'`
    - Copy: "Merge complete. This customer now includes records from {Source Name}. You can undo this for 7 days."
    - `Undo merge` button → POST /api/customers/{source_id}/unmerge → on 200 navigate back to source customer; on 410 show toast "The 7-day undo window has expired."
    - Dismissible via X button (sets a localStorage flag for this merge — banner doesn't reappear for this user/session)

    InquiryFlyout (D-10 offline manual convert):
    - shadcn Sheet (side=right)
    - Shows: caller name, phone (formatted), transcript link, call audio player, service_address, urgency
    - Primary button `Convert to Job` (brand accent) → opens QuickBookSheet (existing component) pre-filled with customer phone + service_address; on booking success, POST /api/inquiries/[id]/convert with the new appointment_id; on 200 flyout closes with success toast
    - Secondary destructive button `Mark as Lost` → PATCH /api/inquiries/[id] with `{status: 'lost'}`; toast with 5-second Undo action mirroring Phase 42 mark-complete pattern (sonner)

    JobFlyout (replaces LeadFlyout on Jobs tab):
    - Copy structure from `LeadFlyout.jsx` verbatim but scope to job shape (`job.customer`, `job.appointment`, `job.status`)
    - Preserve all Phase 33-49 affordances: status dropdown (scheduled/completed/paid/cancelled/lost), audio player, transcript view, Create Invoice button (when invoicing flag ON), View Invoice button (when invoice exists), VIP toggle (Phase 46), escalation indicators if any
    - Update Jobs tab page.js from Plan 06 to import `JobFlyout` instead of `LeadFlyout` (finalizes the swap)
  </behavior>
  <action>
    1. Create CustomerMergeDialog.jsx with 2-step flow:
       - Step 1: shadcn Dialog + Command typeahead bound to search-debounced fetch of /api/customers
       - Step 2: shadcn AlertDialog with preview + destructive confirm; button `destructive` variant
       - Use UI-SPEC copy verbatim including counts template: `"{N} jobs, {M} inquiries, {K} invoices, and {L} call recordings will move to {Target Name}. {Source Name} will be hidden. You can undo this for 7 days."`
       - On merge success: `router.push(`/dashboard/customers/${target_id}?recently_merged=1`)`
       - Decision needed: preview counts — use a preflight GET /api/customers/[id]/merge-preview?target_id=... OR client-side compute. Preferred: preflight to avoid overcounting (a cross-tenant edge can return wrong counts from client). Add this route to Plan 04 retroactively OR implement here as a thin route module-sibling; document choice in SUMMARY.
    2. Create UnmergeBanner.jsx — Alert-style with `border-l-4 border-[var(--brand-accent)]` per UI-SPEC line 204. Fetch list of merges targeting this customer in last 7 days to determine visibility. Wire POST /api/customers/[source_id]/unmerge. Handle 410 with toast.
    3. Create InquiryFlyout.jsx. Integrate with existing QuickBookSheet for booking flow. On booking success, convert inquiry via /api/inquiries/[id]/convert.
    4. Create JobFlyout.jsx by copying LeadFlyout.jsx and:
       - Renaming props `lead` → `job`
       - Updating field paths (job.customer.name, job.appointment.start_time, etc.)
       - Keeping ALL Phase 33-49 affordances — do not remove any button/action that exists in LeadFlyout
    5. Update Jobs tab page.js (from Plan 06 Task 1) to import and render `JobFlyout` on row click instead of `LeadFlyout`.
    6. Wire InquiryFlyout onto Inquiries tab page.js (replacing Plan 06 stub).
    7. Include the merge-preview route if Task 1/2 decision requires (slot into `src/app/api/customers/[id]/merge-preview/route.js`). If deferred, explicitly document the client-side counting approach.
  </action>
  <verify>
    <automated>test -f src/components/dashboard/customers/CustomerMergeDialog.jsx &amp;&amp; test -f src/components/dashboard/customers/UnmergeBanner.jsx &amp;&amp; test -f src/components/dashboard/InquiryFlyout.jsx &amp;&amp; test -f src/components/dashboard/JobFlyout.jsx &amp;&amp; grep -q "Merge Customer" src/components/dashboard/customers/CustomerMergeDialog.jsx &amp;&amp; grep -q "Convert to Job" src/components/dashboard/InquiryFlyout.jsx &amp;&amp; grep -q "Undo merge" src/components/dashboard/customers/UnmergeBanner.jsx &amp;&amp; grep -q "JobFlyout" src/app/dashboard/jobs/page.js</automated>
  </verify>
  <acceptance_criteria>
    - 4 new component files exist
    - Merge dialog uses 2-step flow (Dialog + AlertDialog)
    - UnmergeBanner handles 410 Gone with toast (7-day window expired)
    - InquiryFlyout has Convert to Job + Mark as Lost buttons
    - JobFlyout preserves all LeadFlyout affordances (status, audio, transcript, invoice buttons, VIP toggle)
    - Jobs tab page.js imports JobFlyout (not LeadFlyout)
    - Inquiries tab page.js wires InquiryFlyout
  </acceptance_criteria>
  <done>D-19 merge + undo UX complete; D-10 offline convert wired; JobFlyout replaces LeadFlyout functionally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Customer detail + Merge + Unmerge + Flyouts live smoke</name>
  <action>Execute the verification steps described in &lt;how-to-verify&gt; below (operator runs commands + reports result). This task is a blocking human gate — there is no automated action.</action>
  <what-built>Customer detail page with 3 tabs, Edit modal, Merge dialog, UnmergeBanner, InquiryFlyout, JobFlyout. All components automated and wired to Plan 04 APIs + Plan 03 RPCs. This checkpoint verifies the destructive Merge + 7-day undo + visual correctness of the new surface, which cannot be fully covered by automated snapshot tests.</what-built>
  <how-to-verify>
    Preconditions: Plans 01-06 deployed; dev tenant has at least 3 customers with mixed jobs/inquiries/invoices (seed if needed).

    1. Customer detail page layout:
       - Visit `/dashboard/customers/{any-id}`
       - Verify sticky header shows name, phone formatted, default address, lifetime value, outstanding balance (tabular-nums)
       - Verify 3 tabs render (Activity | Jobs | Invoices); switching tabs persists in URL
       - Verify dark mode toggle works (no hardcoded hex visible)
       - Verify responsive at 375px (stats stack vertically)

    2. Edit modal (D-18):
       - Click Edit Customer
       - Verify phone field is greyed-out / readonly with help text "To change phone, use Merge"
       - Change name → Save Changes → verify UI updates and refreshing still shows new name
       - Try closing modal with unsaved changes → verify Discard changes? AlertDialog appears

    3. Merge + Unmerge (D-19 — destructive):
       - From customer A overflow menu → Merge into another customer
       - Typeahead for customer B by name → select → Continue
       - Verify preview copy matches UI-SPEC: "{N} jobs, {M} inquiries, {K} invoices, and {L} call recordings will move to {B}. {A} will be hidden. You can undo this for 7 days."
       - Confirm Merge Customer → verify navigation to customer B's page
       - Verify UnmergeBanner appears with "Undo merge" button
       - Click Undo merge → verify customer A is restored and navigable
       - Second test: merge C into D, dismiss the banner via X → verify it stays dismissed
       - Third test (requires SQL): UPDATE customers SET merged_at = now() - interval '8 days' WHERE id = <test-source>. Visit target page. Click Undo → verify 410 toast "The 7-day undo window has expired."

    4. InquiryFlyout (D-10 offline convert):
       - Visit /dashboard/inquiries, click an Open inquiry
       - Verify flyout shows caller info + Convert to Job / Mark as Lost buttons
       - Click Convert to Job → QuickBookSheet prefills phone+address → complete booking → verify toast success + inquiry row disappears from Open filter; check /dashboard/jobs for new row
       - Click Mark as Lost on another inquiry → verify toast with 5s Undo action → click Undo within 5s → verify status back to open

    5. JobFlyout parity:
       - Visit /dashboard/jobs, click a job
       - Verify all Phase 33-49 affordances present: status dropdown, audio, transcript, Create/View Invoice button (if invoicing ON), VIP toggle
       - Change status → verify update
  </how-to-verify>
  <resume-signal>Type "approved" or list specific issues (component + step + observed behavior)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form submit → PATCH /api/customers/[id] | Input validated server-side (Plan 04); client-side gates for UX only. |
| Merge confirmation → destructive POST | 2-step UI (preview + confirm); double-confirmation human gate reduces accidental-merge risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07-01 | Tampering | Merge picks target from search results that includes cross-tenant rows (malicious network response) | mitigate | GET /api/customers search applies RLS + explicit tenant filter (Plan 04). Even if response were tampered, merge RPC re-validates both tenants match. |
| T-59-07-02 | Tampering | User clicks Merge accidentally | mitigate | 2-step flow + explicit counts preview + destructive variant red button per UI-SPEC. 7-day undo is safety net. |
| T-59-07-03 | Information Disclosure | Merge preview counts leak other-tenant data | mitigate | Preflight merge-preview route checks tenant ownership before querying child counts. |
| T-59-07-04 | Tampering | Client-side localStorage banner-dismissal tampering | accept | Merely hides a UX affordance; doesn't grant capabilities. Undo still works via direct API call. |
| T-59-07-05 | Information Disclosure | PII (phone, name) in console logs during dev | accept | Development console logs acceptable; production logger is server-side (already redacted). No new client-side telemetry added. |
| T-59-07-06 | Tampering | InquiryFlyout Convert-to-Job bypasses booking validation | mitigate | Convert route requires valid appointment_id in caller's tenant (Plan 04 Task 2). Flyout-UX just kicks off QuickBookSheet which does normal booking. |
</threat_model>

<verification>
- 10 new files created
- All grep acceptance criteria pass
- Human checkpoint returns "approved"
- 3 destructive flows (merge, unmerge, 410-gone) confirmed
- UI-SPEC §Parity checklist for Jobs tab still green (JobFlyout swap did not regress)
</verification>

<success_criteria>
- D-10 offline manual convert UX delivered
- D-17 Customer detail page with 3 tabs delivered
- D-18 Edit modal delivered with phone immutable
- D-19 Merge + 7-day undo UX delivered end-to-end
- JobFlyout replaces LeadFlyout on Jobs tab
</success_criteria>

<output>
After completion, create `.planning/phases/59-customer-job-model-separation-split-leads-into-customers-ded/59-07-SUMMARY.md` — record decisions on activity-timeline fetch strategy (inline vs dedicated route) and merge-preview approach (preflight vs client compute).
</output>
