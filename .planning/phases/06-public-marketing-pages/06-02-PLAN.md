---
phase: 06-public-marketing-pages
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(public)/pricing/page.js
  - src/app/(public)/pricing/PricingTiers.jsx
  - src/app/(public)/pricing/ComparisonTable.jsx
  - src/app/(public)/pricing/FAQSection.jsx
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05
  - PRICE-06
  - PRICE-07

must_haves:
  truths:
    - "Visitor sees 4 pricing tier cards (Starter $99, Growth $249, Scale $599, Enterprise custom) on /pricing"
    - "Growth tier card has visible 'Most Popular' badge and Heritage Copper ring highlight"
    - "Clicking Monthly/Annual toggle updates displayed prices without page reload (annual = 20% off)"
    - "Feature comparison table below cards shows features as rows, tiers as columns, with sticky header"
    - "FAQ accordion with 4 items (cancellation, overages, trial, refunds) expands/collapses one at a time"
    - "ROI-framed hero copy speaks in job revenue (not SaaS metrics)"
    - "Each tier CTA routes to /onboarding except Enterprise which routes to /contact"
    - "On mobile, Growth card appears first (reordered from desktop layout)"
  artifacts:
    - path: "src/app/(public)/pricing/page.js"
      provides: "Pricing page with hero, tiers, comparison, FAQ, CTA banner"
      contains: "PricingTiers"
    - path: "src/app/(public)/pricing/PricingTiers.jsx"
      provides: "Client component with billing toggle and tier cards"
      contains: "use client"
    - path: "src/app/(public)/pricing/ComparisonTable.jsx"
      provides: "Feature comparison table with sticky header"
      contains: "sticky top-16"
    - path: "src/app/(public)/pricing/FAQSection.jsx"
      provides: "Accordion FAQ with 4 items"
      contains: "use client"
  key_links:
    - from: "src/app/(public)/pricing/PricingTiers.jsx"
      to: "src/app/(public)/pricing/pricingData.js"
      via: "import PRICING_TIERS, getAnnualPrice"
      pattern: "import.*pricingData"
    - from: "src/app/(public)/pricing/ComparisonTable.jsx"
      to: "src/app/(public)/pricing/pricingData.js"
      via: "import COMPARISON_FEATURES"
      pattern: "import.*COMPARISON_FEATURES"
    - from: "src/app/(public)/pricing/PricingTiers.jsx"
      to: "/onboarding"
      via: "Link href on CTA buttons"
      pattern: "href=.*/onboarding"
---

<objective>
Build the complete pricing page with all 6 sections: ROI-framed hero, billing toggle with tier cards, feature comparison table, FAQ accordion, and bottom CTA banner.

Purpose: PRICE-01 through PRICE-07 -- the pricing page is the primary conversion page for prospective customers.
Output: Fully functional /pricing page with interactive toggle, comparison table, FAQ, and CTAs linking to onboarding.
</objective>

<execution_context>
@F:/homeservice-agent/.claude/get-shit-done/workflows/execute-plan.md
@F:/homeservice-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-public-marketing-pages/06-CONTEXT.md
@.planning/phases/06-public-marketing-pages/06-UI-SPEC.md
@.planning/phases/06-public-marketing-pages/06-RESEARCH.md
@.planning/phases/06-public-marketing-pages/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/app/(public)/pricing/pricingData.js:
```js
export const PRICING_TIERS = [
  { id: 'starter', name: 'Starter', monthlyPrice: 99, callLimit: 40, description: '...', cta: 'Get Started', ctaHref: '/onboarding', highlighted: false, features: [...] },
  { id: 'growth', name: 'Growth', monthlyPrice: 249, callLimit: 120, ..., highlighted: true, badge: 'Most Popular', ... },
  { id: 'scale', name: 'Scale', monthlyPrice: 599, callLimit: 400, ..., highlighted: false, ... },
  { id: 'enterprise', name: 'Enterprise', monthlyPrice: null, callLimit: null, ..., cta: 'Contact Us', ctaHref: '/contact', highlighted: false, ... },
];

export const COMPARISON_FEATURES = [
  { name: 'Monthly calls', starter: '40', growth: '120', scale: '400', enterprise: 'Unlimited' },
  // ... 14 rows total, values are true/false or strings
];

export function getAnnualPrice(monthlyPrice) {
  if (monthlyPrice === null) return null;
  return Math.round(monthlyPrice * 0.8);
}
```

From src/app/components/landing/AnimatedSection.jsx:
```jsx
export function AnimatedSection({ children, className, delay, direction }) { ... }
export function AnimatedStagger({ children, className }) { ... }
export function AnimatedItem({ children, className }) { ... }
```

Existing color tokens (globals.css):
- --color-landing-surface: #F5F5F4
- --color-landing-dark: #0F172A
- --color-landing-accent: #C2410C
- --color-landing-muted: #475569
- --color-landing-success: #166534
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PricingTiers client component (billing toggle + tier cards) and FAQSection</name>
  <files>
    src/app/(public)/pricing/PricingTiers.jsx
    src/app/(public)/pricing/FAQSection.jsx
  </files>
  <read_first>
    src/app/(public)/pricing/pricingData.js
    src/app/components/landing/AnimatedSection.jsx
    src/app/components/landing/HeroSection.jsx
    src/components/ui/card.jsx
    src/components/ui/badge.jsx
    src/components/ui/button.jsx
    .planning/phases/06-public-marketing-pages/06-UI-SPEC.md
    .planning/phases/06-public-marketing-pages/06-RESEARCH.md
  </read_first>
  <action>
    **PricingTiers.jsx (`'use client'`):**

    1. Imports: `useState` from react, `Link` from next/link, `PRICING_TIERS` and `getAnnualPrice` from `./pricingData`, `Card` and `CardContent` and `CardHeader` from `@/components/ui/card`, `Badge` from `@/components/ui/badge`, `Button` from `@/components/ui/button`, `Check` from `lucide-react`, `AnimatedStagger` and `AnimatedItem` from `@/app/components/landing/AnimatedSection`

    2. Billing toggle (centered pill):
       ```jsx
       <div className="flex items-center justify-center mb-12">
         <div className="flex items-center gap-0 rounded-full border border-white/[0.12] bg-white/[0.04] p-1">
           <button
             onClick={() => setBilling('monthly')}
             className={`px-5 py-2.5 rounded-full text-sm font-medium transition-all duration-150 min-h-[44px] ${
               billing === 'monthly'
                 ? 'bg-[#C2410C] text-white shadow-sm'
                 : 'text-white/50 hover:text-white/70'
             }`}
           >Monthly</button>
           <button
             onClick={() => setBilling('annual')}
             className={`px-5 py-2.5 rounded-full text-sm font-medium transition-all duration-150 min-h-[44px] flex items-center gap-2 ${
               billing === 'annual'
                 ? 'bg-[#C2410C] text-white shadow-sm'
                 : 'text-white/50 hover:text-white/70'
             }`}
           >
             Annual
             <span className="text-xs bg-[#166534] text-white px-2 py-0.5 rounded-full">Save 20%</span>
           </button>
         </div>
       </div>
       ```

    3. Tier cards grid:
       - Container: `grid grid-cols-1 md:grid-cols-4 gap-6 max-w-6xl mx-auto`
       - Each tier uses shadcn `<Card>`:
         - Growth card: `ring-2 ring-[#C2410C] shadow-lg` + `md:order-2` (desktop position 2)
           - Badge: `<Badge className="bg-[#C2410C] text-white text-xs">Most Popular</Badge>` positioned absolutely or inline above card title
         - Starter card: `md:order-1`
         - Scale card: `md:order-3`
         - Enterprise card: `md:order-4`
         - Growth card on mobile: `order-first` (appears first) -- combine with `md:order-2`
       - Card hover: `hover:-translate-y-0.5 transition-all duration-200 hover:shadow-lg`
       - Inside each card:
         - Tier name: `text-xl font-semibold text-[#0F172A]`
         - Price: `<span className="text-4xl font-semibold text-[#0F172A]">${price}</span><span className="text-sm text-[#475569]">/mo</span>` (Enterprise shows "Custom")
         - If billing==='annual' and monthlyPrice!==null: show `<span className="text-xs text-[#475569] line-through">$${tier.monthlyPrice}/mo</span>` for strikethrough
         - Description: `text-sm text-[#475569]`
         - Feature list: each feature with `<Check className="size-4 text-[#166534] shrink-0" />` + text
         - CTA button at bottom: Heritage Copper for highlighted tier (`bg-[#C2410C] text-white hover:bg-[#C2410C]/90 shadow-[0_4px_16px_0_rgba(194,65,12,0.4)]`), outline for others (`border border-[#0F172A] text-[#0F172A] hover:bg-[#0F172A]/5`)
         - All CTAs: `<Button asChild className="w-full min-h-[44px] ..."><Link href={tier.ctaHref}>{tier.cta}</Link></Button>`
       - Wrap cards in `<AnimatedStagger>` with each card in `<AnimatedItem>`

    **FAQSection.jsx (`'use client'`):**

    1. Use Radix Accordion via `import * as Accordion from '@radix-ui/react-accordion'` (available via radix-ui dep). If import fails at build time, fall back to a custom accordion with useState.

    2. FAQ data (hardcoded in component -- only 4 items, not worth extracting):
       ```js
       const FAQ_ITEMS = [
         { q: 'Can I cancel anytime?', a: 'Absolutely. There are no lock-in contracts. Cancel from your dashboard settings at any time, and your service stops at the end of the current billing period. No cancellation fees, no hoops to jump through.' },
         { q: 'What happens if I exceed my call limit?', a: 'We don\'t cut you off mid-month. Calls beyond your plan limit are billed at a transparent per-call overage rate shown on your invoice. You can upgrade your plan anytime to get a better per-call rate.' },
         { q: 'Is there a free trial?', a: 'Yes. Every plan comes with a 14-day free trial -- no credit card required. You\'ll have your AI receptionist answering calls within 5 minutes of signing up.' },
         { q: 'What is your refund policy?', a: 'We offer a 30-day money-back guarantee. If HomeService AI doesn\'t pay for itself within the first month, we\'ll refund your subscription -- no questions asked.' },
       ];
       ```

    3. Accordion structure:
       - `<Accordion.Root type="single" collapsible className="space-y-3 max-w-3xl mx-auto">`
       - Each item: `<Accordion.Item>` with border-bottom styling
       - Trigger: `<Accordion.Trigger className="flex items-center justify-between w-full py-5 text-left text-[#0F172A] font-semibold text-lg group min-h-[44px]">` with ChevronDown icon that rotates: `<ChevronDown className="size-5 text-[#475569] transition-transform duration-200 group-data-[state=open]:rotate-180" />`
       - Content: `<Accordion.Content className="overflow-hidden data-[state=open]:animate-accordion-down data-[state=closed]:animate-accordion-up">` with answer text in `text-[#475569] text-[15px] leading-relaxed pb-5`

    4. Import `ChevronDown` from `lucide-react`

    5. Wrap in `<AnimatedSection direction="up">`
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest-cli/bin/jest.js tests/pricing/ --passWithNoTests 2>&1 | tail -5</automated>
    Verify PricingTiers.jsx contains 'use client' and imports pricingData.
    Verify FAQSection.jsx contains 4 FAQ items and accordion pattern.
  </verify>
  <acceptance_criteria>
    - src/app/(public)/pricing/PricingTiers.jsx starts with `'use client'`
    - PricingTiers.jsx contains `import { PRICING_TIERS, getAnnualPrice } from './pricingData'`
    - PricingTiers.jsx contains `useState('monthly')` for billing toggle
    - PricingTiers.jsx contains `bg-[#C2410C]` for active toggle pill and Growth tier CTA
    - PricingTiers.jsx contains `Save 20%` badge text with `bg-[#166534]`
    - PricingTiers.jsx contains `ring-2 ring-[#C2410C]` for Growth card highlight
    - PricingTiers.jsx contains `order-first` for Growth card mobile reorder
    - PricingTiers.jsx contains `md:order-1` and `md:order-2` and `md:order-3` and `md:order-4` for desktop card order
    - PricingTiers.jsx contains `min-h-[44px]` on toggle buttons and CTA buttons
    - PricingTiers.jsx contains `href={tier.ctaHref}` linking to onboarding/contact
    - FAQSection.jsx starts with `'use client'`
    - FAQSection.jsx contains all 4 FAQ questions: 'cancel', 'exceed', 'free trial', 'refund'
    - FAQSection.jsx contains `ChevronDown` and `rotate-180` for accordion animation
    - FAQSection.jsx contains `type="single"` and `collapsible` for single-open accordion
  </acceptance_criteria>
  <done>
    PricingTiers renders 4 tier cards with billing toggle. Growth card is highlighted with Most Popular badge and Heritage Copper ring. Toggle switches between monthly and annual pricing. FAQSection renders 4 accordion items that expand/collapse one at a time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build pricing page (hero + comparison table + page assembly)</name>
  <files>
    src/app/(public)/pricing/page.js
    src/app/(public)/pricing/ComparisonTable.jsx
  </files>
  <read_first>
    src/app/(public)/pricing/PricingTiers.jsx
    src/app/(public)/pricing/FAQSection.jsx
    src/app/(public)/pricing/pricingData.js
    src/app/components/landing/HeroSection.jsx
    src/app/components/landing/FinalCTASection.jsx
    src/app/components/landing/AnimatedSection.jsx
    .planning/phases/06-public-marketing-pages/06-UI-SPEC.md
  </read_first>
  <action>
    **ComparisonTable.jsx (Server Component -- no 'use client'):**

    1. Import `COMPARISON_FEATURES`, `PRICING_TIERS` from `./pricingData`
    2. Import `Check` from `lucide-react`
    3. Import `AnimatedSection` from `@/app/components/landing/AnimatedSection`

    4. Render:
       ```jsx
       <AnimatedSection direction="up">
         <div className="overflow-x-auto -mx-6 px-6">
           <table className="w-full min-w-[640px]">
             <thead className="sticky top-16 z-10 bg-[#F5F5F4]">
               <tr className="border-b border-[#0F172A]/10">
                 <th scope="col" className="py-4 text-left text-sm font-medium text-[#475569] w-[200px]">Feature</th>
                 {PRICING_TIERS.map(tier => (
                   <th key={tier.id} scope="col" className="py-4 text-center text-sm font-semibold text-[#0F172A]">{tier.name}</th>
                 ))}
               </tr>
             </thead>
             <tbody>
               {COMPARISON_FEATURES.map((feature, i) => (
                 <tr key={feature.name} className="border-b border-[#0F172A]/5 hover:bg-[#F5F5F4]/60">
                   <th scope="row" className="py-4 text-left text-sm text-[#0F172A] font-medium">{feature.name}</th>
                   {['starter', 'growth', 'scale', 'enterprise'].map(tierId => {
                     const val = feature[tierId];
                     return (
                       <td key={tierId} className="py-4 text-center text-sm">
                         {val === true ? <Check className="size-4 text-[#166534] mx-auto" /> :
                          val === false ? <span className="text-[#475569]/40">&mdash;</span> :
                          <span className="text-[#0F172A] font-medium">{val}</span>}
                       </td>
                     );
                   })}
                 </tr>
               ))}
             </tbody>
           </table>
         </div>
       </AnimatedSection>
       ```

    5. Accessibility: `<th scope="col">` for tier headers, `<th scope="row">` for feature names (per UI-SPEC accessibility checklist)

    **Pricing page.js (Server Component):**

    1. Import PricingTiers, ComparisonTable, FAQSection from local files
    2. Import AnimatedSection from `@/app/components/landing/AnimatedSection`
    3. Import Button from `@/components/ui/button`, Link from `next/link`

    4. Page structure (6 sections per UI-SPEC):

       **Section 1 - PricingHero (dark section):**
       ```jsx
       <section className="relative bg-[#0F172A] pt-36 pb-20 overflow-hidden">
         {/* Radial copper glow (matches HeroSection pattern) */}
         <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_rgba(194,65,12,0.08)_0%,_transparent_70%)]" />
         <div className="relative max-w-4xl mx-auto px-6 text-center">
           <AnimatedSection>
             <h1 className="text-[2.25rem] md:text-[3rem] lg:text-[3.75rem] font-semibold text-white tracking-tight leading-[1.1]">
               Stop Losing <span className="text-[#C2410C]">$1,000 Jobs</span> to Voicemail
             </h1>
             <p className="mt-6 text-lg text-white/60 max-w-2xl mx-auto">
               Every tier pays for itself after one booked job. Pick the call volume that matches your crew.
             </p>
             <p className="mt-4 text-sm text-white/40">No credit card required</p>
           </AnimatedSection>
           {/* Billing toggle + tier cards (client component) */}
           <div className="mt-12">
             <PricingTiers />
           </div>
         </div>
       </section>
       ```
       NOTE: The dark hero extends to include the tier cards so the toggle visually lives in the hero. But PricingTiers renders the cards on white backgrounds -- the cards themselves are white `<Card>` components on the dark section. This creates the "cards floating on dark" effect.

       Actually, re-reading UI-SPEC: PricingHero is dark, PricingTiers is a separate section. The toggle is inside PricingTiers but visually in the hero. Better approach: extend dark section to include PricingTiers, with cards having `bg-white` backgrounds that stand out against dark.

       Alternative layout (simpler, matching UI-SPEC section list):
       - Hero section (dark): h1, subheading, "no credit card" -- ends here
       - Tiers section (still dark background extending down, or transition to light): PricingTiers with toggle
       - Comparison section (light): ComparisonTable
       - FAQ section (light): FAQSection
       - CTA banner (dark): Final CTA

       Use a single dark section for hero + tiers, then light for comparison + FAQ, then dark CTA:

       ```jsx
       {/* Hero + Tiers (dark) */}
       <section className="relative bg-[#0F172A] pt-36 pb-24 overflow-hidden">
         <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_rgba(194,65,12,0.08)_0%,_transparent_70%)]" />
         <div className="relative max-w-6xl mx-auto px-6">
           <div className="text-center mb-16">
             <AnimatedSection>
               <h1>...</h1>
               <p>...</p>
               <p>No credit card required</p>
             </AnimatedSection>
           </div>
           <PricingTiers />
         </div>
       </section>

       {/* Comparison Table (light) */}
       <section className="bg-[#F5F5F4] py-20">
         <div className="max-w-6xl mx-auto px-6">
           <AnimatedSection>
             <h2 className="text-2xl font-semibold text-[#0F172A] text-center mb-12 tracking-tight leading-[1.3]">
               Compare Plans
             </h2>
           </AnimatedSection>
           <ComparisonTable />
         </div>
       </section>

       {/* FAQ (light) */}
       <section className="bg-[#F5F5F4] py-20 border-t border-[#0F172A]/5">
         <div className="max-w-6xl mx-auto px-6">
           <AnimatedSection>
             <h2 className="text-2xl font-semibold text-[#0F172A] text-center mb-12 tracking-tight leading-[1.3]">
               Frequently Asked Questions
             </h2>
           </AnimatedSection>
           <FAQSection />
         </div>
       </section>

       {/* CTA Banner (dark) */}
       <section className="bg-[#0F172A] py-20">
         <div className="max-w-4xl mx-auto px-6 text-center">
           <AnimatedSection>
             <h2 className="text-2xl md:text-3xl font-semibold text-white tracking-tight leading-[1.3]">
               Ready to stop losing jobs to voicemail?
             </h2>
             <p className="mt-4 text-white/60">
               Your AI receptionist is 5 minutes away from answering every call.
             </p>
             <div className="mt-8">
               <Button asChild className="bg-[#C2410C] text-white hover:bg-[#C2410C]/90 shadow-[0_4px_16px_0_rgba(194,65,12,0.4)] min-h-[44px] px-8 text-base font-medium rounded-lg">
                 <Link href="/onboarding">Get Started Free</Link>
               </Button>
             </div>
           </AnimatedSection>
         </div>
       </section>
       ```

    5. Export `metadata` for page title: `export const metadata = { title: 'Pricing - HomeService AI', description: 'Simple, transparent pricing for AI receptionist service.' }`
  </action>
  <verify>
    <automated>cd F:/homeservice-agent && npx next build 2>&1 | tail -15</automated>
    Verify pricing page builds without errors.
    Verify ComparisonTable has sticky header and scope attributes.
  </verify>
  <acceptance_criteria>
    - src/app/(public)/pricing/page.js does NOT contain `'use client'` (Server Component)
    - page.js imports PricingTiers, ComparisonTable, FAQSection from local files
    - page.js contains h1 with "Stop Losing" and "$1,000 Jobs" and "Voicemail"
    - page.js contains "Every tier pays for itself after one booked job"
    - page.js contains "No credit card required"
    - page.js contains "Compare Plans" heading for comparison section
    - page.js contains "Frequently Asked Questions" heading
    - page.js contains CTA banner with "Ready to stop losing jobs to voicemail?" and Link to /onboarding
    - page.js contains `export const metadata` with title containing "Pricing"
    - ComparisonTable.jsx does NOT contain `'use client'` (Server Component)
    - ComparisonTable.jsx contains `scope="col"` and `scope="row"` for accessibility
    - ComparisonTable.jsx contains `sticky top-16` on thead
    - ComparisonTable.jsx contains `min-w-[640px]` for horizontal scroll on mobile
    - ComparisonTable.jsx imports COMPARISON_FEATURES and PRICING_TIERS from pricingData
    - ComparisonTable.jsx renders Check icon for true values and mdash for false values
    - Next.js build succeeds with no errors
  </acceptance_criteria>
  <done>
    /pricing page renders complete: ROI-framed hero with accent word, billing toggle with 4 tier cards (Growth highlighted), feature comparison table with sticky header, 4-item FAQ accordion, and bottom CTA banner linking to onboarding.
  </done>
</task>

</tasks>

<verification>
- /pricing page loads with dark hero, tier cards, comparison table, FAQ, CTA
- Monthly/annual toggle updates prices (annual = 20% off)
- Growth card has "Most Popular" badge and copper ring
- Enterprise card shows "Custom" price and "Contact Us" CTA
- Comparison table header sticks below nav (top-16)
- FAQ items expand/collapse one at a time
- All CTAs link to /onboarding (Enterprise to /contact)
- Mobile: Growth card appears first, table scrolls horizontally
- `node --experimental-vm-modules node_modules/jest-cli/bin/jest.js tests/pricing/ -x` passes
</verification>

<success_criteria>
- All 7 PRICE requirements satisfied visually on /pricing page
- Interactive toggle works without page reload
- Responsive layout: 4-col desktop, stacked mobile with Growth first
- Accessible: table headers have scope, buttons have min-h-[44px], FAQ has ARIA states
</success_criteria>

<output>
After completion, create `.planning/phases/06-public-marketing-pages/06-02-SUMMARY.md`
</output>
