---
phase: 38-programmatic-seo-content-engine
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(public)/for/page.js
  - src/app/(public)/for/[persona]/page.js
  - src/app/(public)/compare/page.js
  - src/app/(public)/compare/[comparison]/page.js
autonomous: true
requirements:
  - SEO-05
  - SEO-06
  - SEO-10

must_haves:
  truths:
    - "/for renders a hub listing of all trade personas from PERSONAS data array"
    - "/for/plumber renders a mini-landing page with hero, pain points, features, testimonial, and CTA"
    - "/compare renders a hub listing of all comparisons from COMPARISONS data array"
    - "/compare/vs-voicemail renders a side-by-side feature table with Voco vs competitor, verdict section, and CTA"
    - "Every page uses generateMetadata() with await params, title template, canonical URL, and OG image"
    - "JSON-LD WebPage schema renders on persona and comparison detail pages"
  artifacts:
    - path: "src/app/(public)/for/page.js"
      provides: "Persona hub listing page"
      contains: "PERSONAS"
    - path: "src/app/(public)/for/[persona]/page.js"
      provides: "Persona detail mini-landing page"
      contains: "generateStaticParams"
    - path: "src/app/(public)/compare/page.js"
      provides: "Comparison hub listing page"
      contains: "COMPARISONS"
    - path: "src/app/(public)/compare/[comparison]/page.js"
      provides: "Comparison detail page with feature table"
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/(public)/for/[persona]/page.js"
      to: "src/data/personas.js"
      via: "import PERSONAS for generateStaticParams"
      pattern: "import.*PERSONAS.*from.*@/data/personas"
    - from: "src/app/(public)/compare/[comparison]/page.js"
      to: "src/components/ui/table.jsx"
      via: "shadcn Table for feature comparison"
      pattern: "import.*Table.*from.*@/components/ui/table"
---

<objective>
Build the Persona and Comparison page templates — hub listings and dynamic detail pages. Persona pages are mini-landing pages with trade-specific messaging. Comparison pages use side-by-side feature tables.

Purpose: Delivers two more page template types (persona + comparison) in parallel with Plan 02 (blog + glossary), maximizing Wave 2 throughput.
Output: 4 page files (persona hub, persona detail, comparison hub, comparison detail).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-programmatic-seo-content-engine/38-CONTEXT.md
@.planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
@.planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
@.planning/phases/38-programmatic-seo-content-engine/38-01-SUMMARY.md

<interfaces>
<!-- Data layer contracts from Plan 01 -->

From src/data/personas.js:
```javascript
export const PERSONAS = [
  {
    slug: string,        // 'plumber'
    trade: string,       // 'Plumber'
    headline: string,    // H1 text
    subheadline: string, // Subhead text
    painPoints: [{ icon: string, title: string, body: string }],  // 3 items, icon is Lucide name
    features: [{ title: string, description: string, checks: string[] }],  // feature highlights with check items
    testimonial: { quote: string, author: string },
    relatedSlugs: string[],
  },
];
```

From src/data/comparisons.js:
```javascript
export const COMPARISONS = [
  {
    slug: string,            // 'vs-voicemail'
    title: string,           // Page title
    subheadline: string,
    competitorName: string,  // 'Voicemail'
    features: [{ name: string, voco: boolean, competitor: boolean }],  // Feature rows
    verdictHeading: string,
    verdictBody: string,
    relatedSlugs: string[],
  },
];
```

From src/components/SchemaMarkup.jsx:
```javascript
export function SchemaMarkup({ schema })
```

From src/components/landing/AuthAwareCTA.js:
```javascript
export function AuthAwareCTA({ variant = 'hero' })
```

From src/app/components/landing/AnimatedSection.jsx:
```javascript
<AnimatedSection direction="up">...</AnimatedSection>
```

From src/components/ui/table.jsx:
```javascript
// shadcn Table, TableHeader, TableBody, TableHead, TableRow, TableCell
import { Table, TableHeader, TableBody, TableHead, TableRow, TableCell } from '@/components/ui/table';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Persona hub listing and Persona detail pages</name>
  <files>
    src/app/(public)/for/page.js,
    src/app/(public)/for/[persona]/page.js
  </files>
  <read_first>
    src/data/personas.js,
    src/components/SchemaMarkup.jsx,
    src/components/landing/AuthAwareCTA.js,
    src/app/components/landing/AnimatedSection.jsx,
    src/components/ui/card.jsx,
    src/lib/design-tokens.js,
    src/app/(public)/about/page.js,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
  </read_first>
  <action>
**src/app/(public)/for/page.js** — Persona Hub (static route). Per UI-SPEC "Hub Listing Pages Contract":

- Static metadata: `{ title: 'AI Receptionist for Your Trade | Voco', description: 'See how Voco works for plumbers, HVAC techs, electricians, and more.', alternates: { canonical: 'https://voco.live/for' } }`
- Page structure:
  - Header: `bg-[#F5F5F4] py-16 md:py-20`, `max-w-6xl mx-auto px-6`
  - H1: "Built for Your Trade" at `text-4xl md:text-5xl font-semibold text-[#0F172A]`
  - Subtitle: "See how Voco works for your specific trade." at `text-lg text-[#475569] mt-4`
  - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`, `max-w-6xl mx-auto px-6 py-16`
  - Each card: shadcn Card with `card.base` design token classes. Contains:
    - Page-type badge: `bg-[#F97316]/10 text-[#F97316] text-xs font-medium rounded-full px-2.5 py-0.5` with text "PERSONA"
    - Trade name: `text-base font-semibold text-[#0F172A]` (e.g., "Plumber")
    - Headline preview: `text-sm text-[#475569] line-clamp-2`
    - "See how it works" link at `text-sm text-[#F97316] font-medium`
  - Card wrapped in `<Link href={'/for/' + persona.slug}>`
- Import from `@/data/personas`

**src/app/(public)/for/[persona]/page.js** — Persona Detail (dynamic route, mini-landing page). Per UI-SPEC "Persona Pages" contract and D-03:

- `generateStaticParams()`: Return `PERSONAS.map(p => ({ persona: p.slug }))`
- `generateMetadata({ params })`: MUST `await params`. Title: `AI Receptionist for ${item.trade}s | Voco`. Description: `${item.subheadline} ${item.headline}`. Canonical: `https://voco.live/for/${personaSlug}`. OG: `/og?title=${encodeURIComponent('AI Receptionist for ' + item.trade + 's')}&type=PERSONA`. If not found, return `{ title: 'Not Found | Voco' }`.
- Page component: `export default async function PersonaDetailPage({ params })`. MUST `const { persona: personaSlug } = await params;`. Find item. If not found, `notFound()`.

**Section 1 — Hero** (white bg):
- Container: `max-w-6xl mx-auto px-6 pt-24 md:pt-32 pb-16`
- H1: `text-4xl md:text-5xl lg:text-6xl font-semibold text-[#0F172A] leading-tight` rendering `item.headline`
- Subheadline: `text-lg md:text-xl text-[#475569] mt-6 max-w-2xl` rendering `item.subheadline`
- CTA row: `<AuthAwareCTA variant="hero" />` + secondary link "See how it works" at `text-sm text-[#F97316] font-medium ml-4` linking to `/#how-it-works`
- Below CTA: `text-xs text-[#64748B] mt-3` "14-day free trial — no credit card required" per copywriting contract

**Section 2 — Pain Points** (`bg-[#F5F5F4]`):
- Container: `py-16 md:py-24`
- Inner: `max-w-6xl mx-auto px-6`
- H2: `text-2xl md:text-3xl font-semibold text-[#0F172A] text-center mb-12`
- 3-column grid on md+: `grid grid-cols-1 md:grid-cols-3 gap-8`
- Each pain point:
  - Icon container: `w-10 h-10 rounded-xl bg-[#F97316]/10 flex items-center justify-center mb-4`. Import Lucide icon dynamically by name from `item.painPoints[].icon` — use a simple icon map: `{ Phone, Clock, DollarSign }` from `lucide-react`.
  - Lucide icon: `className="w-5 h-5 text-[#F97316]"`
  - Title: `text-lg font-semibold text-[#0F172A] mb-2`
  - Body: `text-sm text-[#475569] leading-relaxed`

**Section 3 — Feature Highlights** (white bg):
- Container: `py-16 md:py-24 max-w-6xl mx-auto px-6`
- 2-col grid: `grid grid-cols-1 md:grid-cols-2 gap-8`
- Each feature card: `bg-[#F5F5F4] rounded-2xl p-8`
  - Title: `text-xl font-semibold text-[#0F172A] mb-3`
  - Description: `text-sm text-[#475569] leading-relaxed mb-4`
  - Check list: `item.features[].checks.map(check => ...)` each as `flex items-center gap-2 text-sm text-[#475569]` with Lucide `Check` icon at `w-4 h-4 text-[#F97316]`

**Section 4 — Testimonial** (`bg-[#F5F5F4]`):
- Container: `py-16 md:py-24 max-w-3xl mx-auto px-6`
- Blockquote: `border-l-[3px] border-[#F97316] pl-6`
- Quote: `text-xl md:text-2xl text-[#0F172A] italic leading-relaxed`
- Attribution: `text-sm text-[#64748B] mt-4 not-italic`

**Section 5 — CTA** (dark bg, reuse about page CTA pattern):
- Full-width section with `bg-[#1C1412]` background
- `bg-[radial-gradient(ellipse_at_center,rgba(249,115,22,0.15),transparent_70%)]` overlay
- Container: `py-16 md:py-24 max-w-3xl mx-auto px-6 text-center`
- H2: `text-2xl md:text-3xl font-semibold text-white mb-6`
- `<AuthAwareCTA variant="cta" />`

- JSON-LD: `<SchemaMarkup schema={{ '@context': 'https://schema.org', '@type': 'WebPage', name: 'AI Receptionist for ' + item.trade + 's', description: item.subheadline, url: 'https://voco.live/for/' + personaSlug }} />`
- Wrap each section in `<AnimatedSection direction="up">`
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(for|persona|error|Error)" | head -20; echo "---"; ls src/app/\(public\)/for/page.js src/app/\(public\)/for/\[persona\]/page.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/for/page.js contains `export const metadata` with title containing 'Voco'
    - src/app/(public)/for/page.js imports PERSONAS from '@/data/personas'
    - src/app/(public)/for/[persona]/page.js contains `export async function generateStaticParams()`
    - src/app/(public)/for/[persona]/page.js contains `await params` (not synchronous params access)
    - src/app/(public)/for/[persona]/page.js contains AuthAwareCTA component (at least 2 instances — hero + CTA section)
    - src/app/(public)/for/[persona]/page.js contains SchemaMarkup with '@type': 'WebPage'
    - src/app/(public)/for/[persona]/page.js renders pain points with Lucide icons (Phone, Clock, DollarSign)
    - src/app/(public)/for/[persona]/page.js renders feature highlights with Check icon list items
    - src/app/(public)/for/[persona]/page.js renders testimonial blockquote with border-[#F97316]
    - src/app/(public)/for/[persona]/page.js has dark CTA section with bg-[#1C1412]
    - Persona detail metadata has canonical URL containing 'https://voco.live/for/'
  </acceptance_criteria>
  <done>Persona hub shows trade cards, persona detail renders full mini-landing page with hero, pain points, features, testimonial, and CTA</done>
</task>

<task type="auto">
  <name>Task 2: Create Comparison hub listing and Comparison detail pages</name>
  <files>
    src/app/(public)/compare/page.js,
    src/app/(public)/compare/[comparison]/page.js
  </files>
  <read_first>
    src/data/comparisons.js,
    src/components/SchemaMarkup.jsx,
    src/components/landing/AuthAwareCTA.js,
    src/components/ui/table.jsx,
    src/components/ui/card.jsx,
    src/app/components/landing/AnimatedSection.jsx,
    src/lib/design-tokens.js,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
  </read_first>
  <action>
**src/app/(public)/compare/page.js** — Comparison Hub (static route). Per UI-SPEC "Hub Listing Pages Contract":

- Static metadata: `{ title: 'Compare Voco to Alternatives | Voco', description: 'See how Voco stacks up against voicemail, answering services, and other options.', alternates: { canonical: 'https://voco.live/compare' } }`
- Page structure matches hub pattern:
  - Header: `bg-[#F5F5F4] py-16 md:py-20`, H1 "Compare" Display size, subtitle "See how Voco stacks up against the alternatives."
  - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`, `max-w-6xl mx-auto px-6 py-16`
  - Each card: shadcn Card with `card.base`. Contains:
    - Badge: "COMPARE" in `bg-[#F97316]/10 text-[#F97316] text-xs font-medium rounded-full px-2.5 py-0.5`
    - Title: `text-base font-semibold text-[#0F172A]`
    - Subheadline preview: `text-sm text-[#475569] line-clamp-2`
    - "See comparison" link at `text-sm text-[#F97316] font-medium`
  - Card wrapped in `<Link href={'/compare/' + c.slug}>`
- Import from `@/data/comparisons`

**src/app/(public)/compare/[comparison]/page.js** — Comparison Detail (dynamic route). Per UI-SPEC "Comparison Pages" contract and D-04:

- `generateStaticParams()`: Return `COMPARISONS.map(c => ({ comparison: c.slug }))`
- `generateMetadata({ params })`: MUST `await params`. Title: `${item.title} | Voco`. Description: `${item.subheadline}`. Canonical: `https://voco.live/compare/${comparisonSlug}`. OG: `/og?title=${encodeURIComponent(item.title)}&type=COMPARE`. If not found, `{ title: 'Not Found | Voco' }`.
- Page component: `export default async function ComparisonDetailPage({ params })`. MUST `const { comparison: comparisonSlug } = await params;`. Find item. If not found, `notFound()`.

**Hero section** (white bg):
- Container: `max-w-5xl mx-auto px-6 pt-24 md:pt-32 pb-12`
- Back-to-hub: `<Link href="/compare">` with "All comparisons" text
- H1: `text-4xl md:text-5xl font-semibold text-[#0F172A] leading-tight mt-4` rendering `item.title`
- Subheadline: `text-lg text-[#475569] mt-4`

**Comparison table section** (white bg):
- Container: `max-w-5xl mx-auto px-6 pb-16`
- Table wrapper: `bg-white rounded-2xl border border-stone-200/60 overflow-hidden`
- Use shadcn Table component: `import { Table, TableHeader, TableBody, TableHead, TableRow, TableCell } from '@/components/ui/table'`
- TableHeader:
  - First column header: `Feature` at default styling
  - Second column header: "Voco" with `bg-[#F97316]/5 font-semibold text-[#F97316]`
  - Third column header: `item.competitorName` at default styling
- TableBody: map `item.features` to rows:
  - First cell: `text-sm font-medium text-[#0F172A]` with feature name (use `<th scope="row">` pattern)
  - Second cell (Voco): if `feature.voco === true`, render Lucide `Check` icon at `w-4 h-4 text-[#F97316]` + visually hidden `<span className="sr-only">Supported</span>`. If false, render Lucide `X` icon at `w-4 h-4 text-slate-400` + `<span className="sr-only">Not supported</span>`.
  - Third cell (Competitor): same boolean icon pattern but checking `feature.competitor`

**Verdict section**:
- Container: `max-w-5xl mx-auto px-6 py-16`
- Verdict card: `bg-[#F5F5F4] rounded-2xl p-8 md:p-12`
- H2: `text-2xl md:text-3xl font-semibold text-[#0F172A] mb-4` rendering `item.verdictHeading`
- Body: `text-base text-[#475569] leading-relaxed mb-8` rendering `item.verdictBody`
- CTA: `<AuthAwareCTA variant="cta" />`

- JSON-LD: `<SchemaMarkup schema={{ '@context': 'https://schema.org', '@type': 'WebPage', name: item.title, description: item.subheadline, url: 'https://voco.live/compare/' + comparisonSlug }} />`
- Wrap sections in AnimatedSection
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(compare|comparison|error|Error)" | head -20; echo "---"; ls src/app/\(public\)/compare/page.js src/app/\(public\)/compare/\[comparison\]/page.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/compare/page.js contains `export const metadata` with title containing 'Compare'
    - src/app/(public)/compare/page.js imports COMPARISONS from '@/data/comparisons'
    - src/app/(public)/compare/[comparison]/page.js contains `export async function generateStaticParams()`
    - src/app/(public)/compare/[comparison]/page.js contains `await params` (not synchronous)
    - src/app/(public)/compare/[comparison]/page.js imports Table components from '@/components/ui/table'
    - src/app/(public)/compare/[comparison]/page.js renders Check icon for voco=true features and X icon for false
    - src/app/(public)/compare/[comparison]/page.js contains `sr-only` text for accessibility (Supported / Not supported)
    - src/app/(public)/compare/[comparison]/page.js contains verdict section with bg-[#F5F5F4]
    - src/app/(public)/compare/[comparison]/page.js contains AuthAwareCTA component
    - src/app/(public)/compare/[comparison]/page.js contains SchemaMarkup with '@type': 'WebPage'
    - src/app/(public)/compare/[comparison]/page.js contains link back to /compare hub
    - Comparison detail metadata has canonical URL containing 'https://voco.live/compare/'
  </acceptance_criteria>
  <done>Comparison hub shows comparison cards, detail renders side-by-side feature table with Check/X icons, verdict section, and CTA</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors for /for and /compare routes
- Persona hub at /for renders trade cards
- Persona detail at /for/plumber renders full mini-landing page
- Comparison hub at /compare renders comparison cards
- Comparison detail at /compare/vs-voicemail renders feature table and verdict
- All pages have correct generateMetadata with await params
- `npm test -- --testPathPattern=tests/unit/seo` still passes (no regressions)
</verification>

<success_criteria>
- Persona and comparison hub + detail pages render correctly with seed data
- All dynamic pages use generateStaticParams + generateMetadata with await params
- Persona pages have all 5 sections (hero, pain points, features, testimonial, CTA)
- Comparison pages have feature table with accessibility (sr-only, scope attributes)
- JSON-LD WebPage schema on both persona and comparison detail pages
- Internal linking: back-to-hub links, AuthAwareCTA on every page
</success_criteria>

<output>
After completion, create `.planning/phases/38-programmatic-seo-content-engine/38-03-SUMMARY.md`
</output>
