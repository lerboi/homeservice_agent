---
phase: 38-programmatic-seo-content-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blog.js
  - src/data/personas.js
  - src/data/comparisons.js
  - src/data/integrations.js
  - src/data/glossary.js
  - src/components/SchemaMarkup.jsx
  - src/app/(public)/og/route.jsx
  - src/app/sitemap.js
  - src/app/robots.js
  - tests/unit/seo-data-layer.test.js
  - tests/unit/seo-schema-markup.test.js
  - tests/unit/seo-sitemap.test.js
  - tests/unit/seo-robots.test.js
  - tests/unit/seo-metadata.test.js
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-09

must_haves:
  truths:
    - "/sitemap.xml serves all static and dynamic routes including blog, persona, comparison, integration, and glossary slugs"
    - "/robots.txt allows all crawling and points to https://voco.live/sitemap.xml"
    - "/og?title=Test&type=BLOG returns a 1200x630 image with dark-to-copper gradient, title text, and page-type badge"
    - "SchemaMarkup component renders a script tag with valid JSON-LD for any schema type"
    - "Five data layer arrays exist in src/data/ with at least 1 seed item each containing slug and title fields"
  artifacts:
    - path: "src/data/blog.js"
      provides: "Blog posts data array"
      exports: ["BLOG_POSTS"]
    - path: "src/data/personas.js"
      provides: "Personas data array"
      exports: ["PERSONAS"]
    - path: "src/data/comparisons.js"
      provides: "Comparisons data array"
      exports: ["COMPARISONS"]
    - path: "src/data/integrations.js"
      provides: "Integrations data array"
      exports: ["INTEGRATIONS"]
    - path: "src/data/glossary.js"
      provides: "Glossary terms data array"
      exports: ["GLOSSARY_TERMS"]
    - path: "src/components/SchemaMarkup.jsx"
      provides: "Reusable JSON-LD renderer"
      exports: ["SchemaMarkup"]
    - path: "src/app/(public)/og/route.jsx"
      provides: "OG image generation endpoint"
      exports: ["GET"]
    - path: "src/app/sitemap.js"
      provides: "Sitemap metadata convention"
    - path: "src/app/robots.js"
      provides: "Robots metadata convention"
  key_links:
    - from: "src/app/sitemap.js"
      to: "src/data/*.js"
      via: "import data arrays to generate dynamic URLs"
      pattern: "import.*from.*@/data/"
    - from: "src/app/(public)/og/route.jsx"
      to: "next/og"
      via: "ImageResponse constructor"
      pattern: "import.*ImageResponse.*from.*next/og"
---

<objective>
Build the SEO foundation layer: five data arrays (blog, personas, comparisons, integrations, glossary), the SchemaMarkup JSON-LD component, the OG image route handler, sitemap.js, robots.js, and all Wave 0 test scaffolds.

Purpose: Every subsequent plan depends on these data arrays and shared components. This plan creates the contracts that all five page template plans consume.
Output: 5 data files in src/data/, SchemaMarkup.jsx, OG route, sitemap.js, robots.js, 5 test files.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-programmatic-seo-content-engine/38-CONTEXT.md
@.planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
@.planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
@.planning/phases/38-programmatic-seo-content-engine/38-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/design-tokens.js:
```javascript
export const card = {
  base: 'bg-white rounded-2xl shadow-[0_1px_3px_0_rgba(0,0,0,0.04),0_1px_2px_-1px_rgba(0,0,0,0.03)] border border-stone-200/60',
  hover: 'hover:shadow-[0_4px_12px_0_rgba(0,0,0,0.06)] hover:-translate-y-0.5 transition-all duration-200',
};
```

From src/app/(public)/layout.js:
```javascript
// All routes under (public) automatically get LandingNav + LandingFooter
export default function PublicLayout({ children }) {
  return (
    <>
      <LandingNav />
      <main className="relative">{children}</main>
      <LandingFooter />
      <Toaster richColors position="top-center" />
      <PublicChatButton />
    </>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data layer arrays and SchemaMarkup component</name>
  <files>
    src/data/blog.js,
    src/data/personas.js,
    src/data/comparisons.js,
    src/data/integrations.js,
    src/data/glossary.js,
    src/components/SchemaMarkup.jsx
  </files>
  <read_first>
    src/app/(public)/pricing/pricingData.js,
    src/app/(public)/pricing/FAQSection.jsx,
    .planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md,
    .planning/phases/38-programmatic-seo-content-engine/38-CONTEXT.md,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
  </read_first>
  <action>
Create `src/data/` directory and five data files per D-11 (arrays exported as named constants).

**src/data/blog.js** — export `BLOG_POSTS` array with 1 seed post per D-08/D-09/D-10:
```javascript
export const BLOG_POSTS = [
  {
    slug: 'ai-receptionist-for-plumbers',
    title: 'Why Every Plumber Needs an AI Receptionist in 2025',
    excerpt: "You can't answer the phone while you're under a sink. Here's how AI changes that.",
    publishedAt: '2025-11-01',
    featuredImage: '/images/blog/ai-receptionist-plumber.jpg',
    content: `...FULL SEO-OPTIMIZED ARTICLE HERE (1000-1500 words)...`,
    relatedSlugs: [],
  },
];
```
The `content` field must be a REAL, publishable, high-quality SEO article — NOT placeholder text. Write actual long-form content (1000-1500 words) about why plumbers need AI receptionists, weaving in Voco's value props. Reference the `abm-programmatic-seo` skill for SEO best practices (keyword density, heading structure, internal linking mentions). Article should have H2/H3 sections rendered via markdown-style string that the blog detail page will parse. Use a raw string with section markers like `## Section Title\n\nParagraph text...` that can be split on `##` for rendering.

**src/data/personas.js** — export `PERSONAS` array with 1 seed persona (plumber) per D-09:
```javascript
export const PERSONAS = [
  {
    slug: 'plumber',
    trade: 'Plumber',
    headline: 'Stop Losing Jobs to Voicemail. Let Voco Answer Every Plumbing Call.',
    subheadline: "Built for plumbers who are under a sink when the phone rings.",
    painPoints: [
      { icon: 'Phone', title: 'Missed calls = missed revenue', body: 'The average emergency plumbing job is worth $650. Every call that goes to voicemail is money walking to your competitor.' },
      { icon: 'Clock', title: "Can't answer mid-job", body: "You're elbow-deep in a pipe repair. Your phone rings. By the time you clean up and call back, they've already called someone else." },
      { icon: 'DollarSign', title: 'Answering services cost $300+/mo', body: "And they still put callers on hold. Voco answers instantly, books the job, and costs less than a single emergency call-out." },
    ],
    features: [
      { title: 'Instant call answering', description: 'Voco picks up in under 1 second — before the second ring. Your business name, professional greeting, every time.', checks: ['24/7 coverage', 'Your business name', 'Professional tone'] },
      { title: 'Automatic booking', description: 'Voco checks your calendar, finds the next open slot, and books the appointment while the caller is still on the line.', checks: ['Calendar-aware', 'Travel time buffers', 'Conflict prevention'] },
      { title: 'Emergency detection', description: 'Burst pipes and gas leaks get flagged instantly. Voco books emergency calls into same-day slots and sends you a priority alert.', checks: ['Smart triage', 'Priority SMS alerts', 'Same-day booking'] },
    ],
    testimonial: { quote: "I was losing 3-4 calls a day while on jobs. First week with Voco, I booked $4,200 in work that would've gone to voicemail.", author: 'Mike D., Licensed Plumber — Austin, TX' },
    relatedSlugs: [],
  },
];
```

**src/data/comparisons.js** — export `COMPARISONS` array with 1 seed comparison (vs voicemail) per D-09:
```javascript
export const COMPARISONS = [
  {
    slug: 'vs-voicemail',
    title: 'Voco vs Voicemail: Why Voicemail Costs You $650 Per Missed Call',
    subheadline: 'Head-to-head: the real cost of letting calls go to voicemail.',
    competitorName: 'Voicemail',
    features: [
      { name: 'Answers calls 24/7', voco: true, competitor: false },
      { name: 'Books appointments automatically', voco: true, competitor: false },
      { name: 'Detects emergencies', voco: true, competitor: false },
      { name: 'Sends SMS confirmation to caller', voco: true, competitor: false },
      { name: 'Speaks 70+ languages', voco: true, competitor: false },
      { name: 'Notifies you instantly', voco: true, competitor: false },
      { name: 'Works while you are on a job', voco: true, competitor: true },
      { name: 'Zero monthly cost', voco: false, competitor: true },
      { name: 'Captures caller details', voco: true, competitor: false },
      { name: 'Syncs with Google Calendar', voco: true, competitor: false },
    ],
    verdictHeading: 'Why 500+ contractors choose Voco over voicemail',
    verdictBody: "Voicemail is free — but it costs you every caller who hangs up without leaving a message. Studies show 80% of callers won't leave a voicemail. That's 4 out of 5 potential jobs lost. Voco answers every call, books the appointment, and sends you a summary — all before you finish the job you're on.",
    relatedSlugs: [],
  },
];
```

**src/data/integrations.js** — export `INTEGRATIONS` array with 1 seed integration (google-calendar) per D-09:
```javascript
export const INTEGRATIONS = [
  {
    slug: 'google-calendar',
    toolName: 'Google Calendar',
    description: "Voco syncs bidirectionally with Google Calendar so your AI receptionist always knows your real availability. When a caller requests an appointment, Voco checks your calendar in real time, finds the next open slot with travel time buffers, and books it — all while the caller is on the line.",
    useCases: [
      { icon: 'Calendar', title: 'Auto-sync appointments', body: 'Every booking Voco makes appears in your Google Calendar within seconds. No double-entry, no missed appointments.' },
      { icon: 'Clock', title: 'Real-time availability', body: 'Block time in Google Calendar and Voco instantly knows. Personal appointments, lunch breaks, days off — all respected automatically.' },
      { icon: 'Bell', title: 'Conflict prevention', body: 'Voco never double-books. If a slot is taken in Google Calendar, it offers the next available time with built-in travel buffers between jobs.' },
      { icon: 'RefreshCw', title: 'Bidirectional sync', body: 'Changes flow both ways. Edit an appointment in Google Calendar and Voco updates. Reschedule in Voco and Google Calendar reflects it.' },
    ],
    ctaHeading: 'Connect Google Calendar to Voco in 5 minutes',
    relatedSlugs: [],
  },
];
```

**src/data/glossary.js** — export `GLOSSARY_TERMS` array with 1 seed term (ai-receptionist) per D-09:
```javascript
export const GLOSSARY_TERMS = [
  {
    slug: 'ai-receptionist',
    term: 'AI Receptionist',
    definition: "An AI receptionist is an artificial intelligence system that answers phone calls on behalf of a business — greeting callers, understanding their needs, and taking action like booking appointments or capturing lead information. Unlike traditional voicemail or hold music, an AI receptionist engages in natural conversation, available 24/7 without breaks, sick days, or hold times.",
    faqItems: [
      { q: 'How does an AI receptionist work?', a: 'An AI receptionist uses speech recognition to understand what a caller says, natural language processing to determine their intent, and text-to-speech to respond conversationally. Modern AI receptionists like Voco can book appointments, detect emergencies, and speak 70+ languages — all in real time during the call.' },
      { q: 'Can an AI receptionist replace a human receptionist?', a: 'For most home service businesses, yes. An AI receptionist handles the core job — answering calls, booking appointments, and capturing caller details — at a fraction of the cost. For complex situations, it can transfer to a human with full context of the conversation.' },
      { q: 'What industries use AI receptionists?', a: 'AI receptionists are most popular in home services (plumbing, HVAC, electrical, roofing), medical offices, legal firms, and real estate. Any business that relies on inbound phone calls and cannot afford to miss them benefits from AI reception.' },
      { q: 'How much does an AI receptionist cost?', a: 'AI receptionist services typically range from $99 to $599 per month depending on call volume. This is significantly less than a human receptionist ($2,500-4,000/mo) or a traditional answering service ($300-800/mo with hold times and per-minute charges).' },
    ],
    relatedSlugs: [],
  },
];
```

**src/components/SchemaMarkup.jsx** — NO `'use client'` directive. Pure Server Component per D-20 and Pitfall 6 from RESEARCH.md:
```javascript
export function SchemaMarkup({ schema }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
    />
  );
}
```
  </action>
  <verify>
    <automated>node -e "const b = require('./src/data/blog.js'); const p = require('./src/data/personas.js'); const c = require('./src/data/comparisons.js'); const i = require('./src/data/integrations.js'); const g = require('./src/data/glossary.js'); console.log('blog:', b.BLOG_POSTS.length, 'personas:', p.PERSONAS.length, 'comparisons:', c.COMPARISONS.length, 'integrations:', i.INTEGRATIONS.length, 'glossary:', g.GLOSSARY_TERMS.length); if (b.BLOG_POSTS.length < 1 || p.PERSONAS.length < 1 || c.COMPARISONS.length < 1 || i.INTEGRATIONS.length < 1 || g.GLOSSARY_TERMS.length < 1) { process.exit(1); }" || echo "FAIL: data layer check"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/blog.js exports BLOG_POSTS array with at least 1 item containing slug, title, excerpt, publishedAt, content fields
    - src/data/personas.js exports PERSONAS array with at least 1 item containing slug, trade, headline, painPoints, features, testimonial fields
    - src/data/comparisons.js exports COMPARISONS array with at least 1 item containing slug, title, competitorName, features (array of objects with name, voco, competitor), verdictHeading, verdictBody
    - src/data/integrations.js exports INTEGRATIONS array with at least 1 item containing slug, toolName, description, useCases, ctaHeading
    - src/data/glossary.js exports GLOSSARY_TERMS array with at least 1 item containing slug, term, definition, faqItems (array with q and a)
    - src/components/SchemaMarkup.jsx exports SchemaMarkup function, does NOT contain 'use client'
    - Blog seed content is 1000+ words of real SEO content (not lorem ipsum or placeholder)
  </acceptance_criteria>
  <done>Five data layer files with real seed content and SchemaMarkup component exist, all exports importable</done>
</task>

<task type="auto">
  <name>Task 2: Create OG image route, sitemap.js, robots.js, and Wave 0 tests</name>
  <files>
    src/app/(public)/og/route.jsx,
    src/app/sitemap.js,
    src/app/robots.js,
    tests/unit/seo-data-layer.test.js,
    tests/unit/seo-schema-markup.test.js,
    tests/unit/seo-sitemap.test.js,
    tests/unit/seo-robots.test.js,
    tests/unit/seo-metadata.test.js
  </files>
  <read_first>
    src/data/blog.js,
    src/data/personas.js,
    src/data/comparisons.js,
    src/data/integrations.js,
    src/data/glossary.js,
    src/components/SchemaMarkup.jsx,
    .planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md,
    .planning/phases/38-programmatic-seo-content-engine/38-VALIDATION.md,
    jest.config.js
  </read_first>
  <action>
**src/app/(public)/og/route.jsx** — Route Handler (NOT page.js) per D-12/D-13. Bypasses layout. Uses `ImageResponse` from `next/og`:
```javascript
import { ImageResponse } from 'next/og';

export const runtime = 'edge';

export async function GET(request) {
  const { searchParams } = new URL(request.url);
  const title = searchParams.get('title') ?? 'Voco AI Receptionist';
  const type = searchParams.get('type') ?? 'PAGE';

  return new ImageResponse(
    (
      <div style={{
        width: '1200px', height: '630px', display: 'flex', flexDirection: 'column',
        alignItems: 'flex-start', justifyContent: 'center',
        background: 'linear-gradient(135deg, #0F172A 0%, #1C1412 60%, #C2410C 100%)',
        padding: '80px', fontFamily: 'Inter, sans-serif',
      }}>
        {/* Page type badge */}
        <div style={{
          background: '#F97316', color: 'white', fontSize: '14px', fontWeight: '600',
          padding: '6px 16px', borderRadius: '999px', marginBottom: '32px',
          textTransform: 'uppercase', letterSpacing: '0.05em',
        }}>
          {type}
        </div>
        {/* Title */}
        <div style={{
          fontSize: '56px', fontWeight: '700', color: 'white',
          lineHeight: 1.15, maxWidth: '900px',
        }}>
          {title}
        </div>
        {/* Voco branding */}
        <div style={{
          position: 'absolute', bottom: '60px', left: '80px',
          fontSize: '24px', fontWeight: '600', color: 'rgba(255,255,255,0.6)',
        }}>
          voco.live
        </div>
      </div>
    ),
    { width: 1200, height: 630 }
  );
}
```

**src/app/sitemap.js** — at app root (NOT inside route group). Imports all 5 data arrays per D-18. Use exact pattern from RESEARCH.md Pattern 1:
- Static routes: `/` (priority 1.0), `/pricing` (0.9), `/about` (0.7), `/contact` (0.6), `/blog` (0.8), `/for` (0.8), `/compare` (0.8), `/integrations` (0.8), `/glossary` (0.8)
- Dynamic routes: map each data array to `{ url: 'https://voco.live/{type}/{slug}', lastModified, changeFrequency: 'monthly', priority: 0.7 }`
- Base URL: `https://voco.live` per D-15

**src/app/robots.js** — at app root per D-19:
```javascript
export default function robots() {
  return {
    rules: [{ userAgent: '*', allow: '/' }],
    sitemap: 'https://voco.live/sitemap.xml',
  };
}
```

**Wave 0 Test Files** — Create all 5 test scaffolds per VALIDATION.md:

1. `tests/unit/seo-data-layer.test.js` — Import all 5 data arrays. Assert each has >= 1 item. Assert each item has `slug` (string) and `title` or `term` (string). Assert blog items have `content`, `excerpt`, `publishedAt`. Assert persona items have `painPoints`, `features`, `testimonial`. Assert comparison items have `features` (array), `competitorName`. Assert integration items have `useCases`, `ctaHeading`. Assert glossary items have `faqItems` (array with q and a).

2. `tests/unit/seo-schema-markup.test.js` — Import SchemaMarkup. Use React Testing Library (`@testing-library/react`) `render()`. Assert it renders a `<script>` tag with `type="application/ld+json"`. Assert the JSON inside is valid and contains `@type` matching the passed schema type (e.g., FAQPage). Test with FAQPage and WebPage schema types.

3. `tests/unit/seo-sitemap.test.js` — Import the default export from `src/app/sitemap.js`. Call it. Assert return is an array. Assert it contains objects with `url`, `lastModified`, `changeFrequency`, `priority`. Assert it includes the static route `https://voco.live`. Assert it includes at least one dynamic blog route matching `https://voco.live/blog/`. Assert total count >= 14 (9 static + 5 dynamic seeds).

4. `tests/unit/seo-robots.test.js` — Import the default export from `src/app/robots.js`. Call it. Assert return has `rules` array with `{ userAgent: '*', allow: '/' }`. Assert return has `sitemap` equal to `'https://voco.live/sitemap.xml'`.

5. `tests/unit/seo-metadata.test.js` — This is a HELPER test that validates the metadata generation pattern. Create a test helper function `buildMetadata(title, slug, type, excerpt)` that mirrors what `generateMetadata` does in dynamic pages. Assert it returns `title` matching `{title} | Voco` format. Assert it returns `alternates.canonical` matching `https://voco.live/{type}/{slug}`. Assert it returns `openGraph.images[0].url` containing `/og?title=`. This test validates the pattern that all 5 page types will use.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=tests/unit/seo</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/og/route.jsx exports GET function and contains `import { ImageResponse } from 'next/og'`
    - src/app/(public)/og/route.jsx contains `export const runtime = 'edge'`
    - src/app/sitemap.js is at app root (not inside route group), imports from @/data/blog, @/data/personas, @/data/comparisons, @/data/integrations, @/data/glossary
    - src/app/sitemap.js default export returns array containing url 'https://voco.live' with priority 1.0
    - src/app/robots.js default export returns object with sitemap 'https://voco.live/sitemap.xml'
    - tests/unit/seo-data-layer.test.js exists and passes
    - tests/unit/seo-schema-markup.test.js exists and passes
    - tests/unit/seo-sitemap.test.js exists and passes
    - tests/unit/seo-robots.test.js exists and passes
    - tests/unit/seo-metadata.test.js exists and passes
    - `npm test -- --testPathPattern=tests/unit/seo` exits with code 0
  </acceptance_criteria>
  <done>OG route returns image, sitemap lists all routes, robots allows crawling, all 5 Wave 0 tests pass</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=tests/unit/seo` — all 5 test files pass
- `node -e "const s = require('./src/app/sitemap.js'); console.log(s.default().length)"` returns >= 14
- `node -e "const r = require('./src/app/robots.js'); console.log(JSON.stringify(r.default()))"` shows rules and sitemap
- All data files importable without errors
- SchemaMarkup.jsx has no 'use client' directive
</verification>

<success_criteria>
- 5 data layer files exist in src/data/ with real seed content (not placeholder)
- SchemaMarkup component renders valid JSON-LD script tags
- OG route at /og accepts title and type query params
- sitemap.js at app root returns all static + dynamic routes
- robots.js at app root allows crawling and points to sitemap
- All 5 Wave 0 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-programmatic-seo-content-engine/38-01-SUMMARY.md`
</output>
