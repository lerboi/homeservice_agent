---
phase: 38-programmatic-seo-content-engine
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(public)/blog/page.js
  - src/app/(public)/blog/[slug]/page.js
  - src/app/(public)/glossary/page.js
  - src/app/(public)/glossary/[term]/page.js
autonomous: true
requirements:
  - SEO-04
  - SEO-08
  - SEO-10

must_haves:
  truths:
    - "/blog renders a card grid listing all blog posts from BLOG_POSTS data array"
    - "/blog/ai-receptionist-for-plumbers renders the full article with headings, prose, featured image, and CTA"
    - "/glossary renders a listing of all glossary terms from GLOSSARY_TERMS data array"
    - "/glossary/ai-receptionist renders definition, FAQ accordion with FAQPage schema, and related terms"
    - "Every page uses generateMetadata() with await params, title template '{Title} | Voco', canonical URL, OG image"
    - "JSON-LD FAQPage schema renders on glossary detail pages"
  artifacts:
    - path: "src/app/(public)/blog/page.js"
      provides: "Blog hub listing page"
      contains: "BLOG_POSTS"
    - path: "src/app/(public)/blog/[slug]/page.js"
      provides: "Blog detail page with generateStaticParams and generateMetadata"
      contains: "generateStaticParams"
    - path: "src/app/(public)/glossary/page.js"
      provides: "Glossary hub listing page"
      contains: "GLOSSARY_TERMS"
    - path: "src/app/(public)/glossary/[term]/page.js"
      provides: "Glossary detail page with FAQ accordion and FAQPage schema"
      contains: "FAQPage"
  key_links:
    - from: "src/app/(public)/blog/[slug]/page.js"
      to: "src/data/blog.js"
      via: "import BLOG_POSTS for generateStaticParams and page render"
      pattern: "import.*BLOG_POSTS.*from.*@/data/blog"
    - from: "src/app/(public)/glossary/[term]/page.js"
      to: "src/components/SchemaMarkup.jsx"
      via: "renders FAQPage JSON-LD"
      pattern: "SchemaMarkup.*FAQPage"
    - from: "src/app/(public)/blog/[slug]/page.js"
      to: "src/app/(public)/blog/page.js"
      via: "back-to-hub link"
      pattern: "href.*blog"
---

<objective>
Build the Blog and Glossary page templates — both hub listing pages and dynamic detail pages. Blog uses a card grid listing + long-form article detail. Glossary uses a definition + FAQ accordion with FAQPage schema markup.

Purpose: Delivers two of five page template types with full SEO metadata, JSON-LD, and internal linking patterns that the remaining templates will follow.
Output: 4 page files (blog hub, blog detail, glossary hub, glossary detail).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-programmatic-seo-content-engine/38-CONTEXT.md
@.planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
@.planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
@.planning/phases/38-programmatic-seo-content-engine/38-01-SUMMARY.md

<interfaces>
<!-- Data layer contracts from Plan 01 -->

From src/data/blog.js:
```javascript
export const BLOG_POSTS = [
  {
    slug: string,           // URL segment, e.g. 'ai-receptionist-for-plumbers'
    title: string,          // Display title
    excerpt: string,        // 1-2 sentence summary for cards
    publishedAt: string,    // ISO date string
    featuredImage: string,  // Path to image
    content: string,        // Full article with ## headings
    relatedSlugs: string[], // Sibling post slugs
  },
];
```

From src/data/glossary.js:
```javascript
export const GLOSSARY_TERMS = [
  {
    slug: string,       // URL segment
    term: string,       // Display term name
    definition: string, // Full definition paragraph
    faqItems: [{ q: string, a: string }],  // FAQ accordion items
    relatedSlugs: string[], // Related term slugs
  },
];
```

From src/components/SchemaMarkup.jsx:
```javascript
export function SchemaMarkup({ schema }) // renders <script type="application/ld+json">
```

From src/components/landing/AuthAwareCTA.js:
```javascript
export function AuthAwareCTA({ variant = 'hero' })
// variant='hero': smaller CTA, variant='cta': larger CTA
// Logged-in: "Go to Dashboard", Logged-out: "Start My 5-Minute Setup"
```

From src/app/components/landing/AnimatedSection.jsx:
```javascript
// Fade-up scroll animation wrapper
<AnimatedSection direction="up">...</AnimatedSection>
```

From src/app/(public)/pricing/FAQSection.jsx:
```javascript
// Radix Accordion pattern — 'use client', dark styling with border-white/[0.08]
// Glossary MUST adapt to light surface: border-stone-200, text-[#0F172A], text-[#475569]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Blog hub listing and Blog detail pages</name>
  <files>
    src/app/(public)/blog/page.js,
    src/app/(public)/blog/[slug]/page.js
  </files>
  <read_first>
    src/data/blog.js,
    src/components/SchemaMarkup.jsx,
    src/components/landing/AuthAwareCTA.js,
    src/app/components/landing/AnimatedSection.jsx,
    src/components/ui/card.jsx,
    src/lib/design-tokens.js,
    src/app/(public)/about/page.js,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md,
    .planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
  </read_first>
  <action>
**src/app/(public)/blog/page.js** — Blog Hub (static route, no generateStaticParams needed). Per UI-SPEC "Hub Listing Pages Contract":

- Static metadata: `{ title: 'Blog | Voco', description: 'AI receptionist tips, guides, and news for home service contractors.', alternates: { canonical: 'https://voco.live/blog' } }`
- Page structure:
  - Header section: `bg-[#F5F5F4]` background, `py-16 md:py-20`, `max-w-6xl mx-auto px-6`
  - H1 Display: "Blog" at `text-4xl md:text-5xl font-semibold text-[#0F172A]`
  - Subtitle: "Tips, guides, and insights for home service contractors." at `text-lg text-[#475569] mt-4`
  - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` on white `bg-white` section, `max-w-6xl mx-auto px-6 py-16`
  - Each card: shadcn `Card` component with `card.base` token classes. Contains:
    - Featured image placeholder div: `aspect-[16/9] bg-[#F5F5F4] rounded-t-2xl` (actual image if available, placeholder if not)
    - Page-type badge inside card: `<span className="inline-flex items-center bg-[#F97316]/10 text-[#F97316] text-xs font-medium rounded-full px-2.5 py-0.5">BLOG</span>`
    - Title: `text-base font-semibold text-[#0F172A]`
    - Excerpt: `text-sm text-[#475569] line-clamp-2`
    - Date: `text-xs text-[#64748B]` formatted with `new Date(post.publishedAt).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })`
    - "Read article" link with ArrowRight icon at `text-sm text-[#F97316] font-medium`
  - Card wrapped in `<Link href={'/blog/' + post.slug}>` (next/link)
  - Cards wrapped in AnimatedStagger/AnimatedItem if those components exist, otherwise AnimatedSection
- Import from `@/data/blog` and `@/components/ui/card`

**src/app/(public)/blog/[slug]/page.js** — Blog Detail (dynamic route). Per UI-SPEC "Blog Detail" contract and RESEARCH.md Pattern 3:

- `generateStaticParams()`: Return `BLOG_POSTS.map(post => ({ slug: post.slug }))`
- `generateMetadata({ params })`: MUST `await params` per D-17. Build title as `${post.title} | Voco`, description as `post.excerpt`, canonical as `https://voco.live/blog/${slug}`, OG image as `https://voco.live/og?title=${encodeURIComponent(post.title)}&type=BLOG`. If post not found, return `{ title: 'Not Found | Voco' }`.
- Page component: `export default async function BlogDetailPage({ params })`. MUST `const { slug } = await params;`. If post not found, call `notFound()` from `next/navigation`.
- Layout: single-column prose, `max-w-2xl mx-auto px-6 py-16 md:py-24`
- Featured image at top: `aspect-[16/9] w-full rounded-xl overflow-hidden bg-[#F5F5F4]` (show Image component if available, bg placeholder if not)
- Article header: title at `text-4xl md:text-5xl font-semibold text-[#0F172A] mt-8`, date at `text-sm text-[#64748B] mt-3`
- Content rendering: split `post.content` on `## ` markers. For each section, render an H2 heading at `text-2xl md:text-3xl font-semibold text-[#0F172A] mt-12 mb-4` and paragraph text at `text-base text-[#475569] leading-relaxed`. Prose max-width `max-w-[65ch]`.
- Back-to-hub link: `<Link href="/blog">` with "All articles" text and ArrowLeft icon, positioned above the title
- JSON-LD: Render `<SchemaMarkup schema={{ '@context': 'https://schema.org', '@type': 'Article', headline: post.title, datePublished: post.publishedAt, author: { '@type': 'Organization', name: 'Voco' }, publisher: { '@type': 'Organization', name: 'Voco' } }} />`
- CTA at bottom: full-width section with `<AuthAwareCTA variant="cta" />` per UI-SPEC copywriting contract. Wrap in AnimatedSection.
- Related posts section if `post.relatedSlugs.length > 0`: show cards linking to siblings
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(blog|error|Error)" | head -20; echo "---"; ls src/app/\(public\)/blog/page.js src/app/\(public\)/blog/\[slug\]/page.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/blog/page.js contains `export const metadata` with title 'Blog | Voco'
    - src/app/(public)/blog/page.js imports BLOG_POSTS from '@/data/blog'
    - src/app/(public)/blog/page.js contains Card component usage from '@/components/ui/card'
    - src/app/(public)/blog/[slug]/page.js contains `export async function generateStaticParams()`
    - src/app/(public)/blog/[slug]/page.js contains `export async function generateMetadata({ params })`
    - src/app/(public)/blog/[slug]/page.js contains `const { slug } = await params` (NOT `const { slug } = params`)
    - src/app/(public)/blog/[slug]/page.js contains `notFound()` call for missing slugs
    - src/app/(public)/blog/[slug]/page.js contains SchemaMarkup with '@type': 'Article'
    - src/app/(public)/blog/[slug]/page.js contains AuthAwareCTA component
    - src/app/(public)/blog/[slug]/page.js contains link back to /blog hub
    - Blog detail metadata has canonical URL containing 'https://voco.live/blog/'
    - Blog detail metadata has openGraph.images containing '/og?title='
  </acceptance_criteria>
  <done>Blog hub shows card grid of posts, blog detail renders full article with SEO metadata, JSON-LD, back-to-hub link, and CTA</done>
</task>

<task type="auto">
  <name>Task 2: Create Glossary hub listing and Glossary detail pages</name>
  <files>
    src/app/(public)/glossary/page.js,
    src/app/(public)/glossary/[term]/page.js
  </files>
  <read_first>
    src/data/glossary.js,
    src/components/SchemaMarkup.jsx,
    src/components/landing/AuthAwareCTA.js,
    src/app/(public)/pricing/FAQSection.jsx,
    src/app/components/landing/AnimatedSection.jsx,
    src/lib/design-tokens.js,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md,
    .planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
  </read_first>
  <action>
**src/app/(public)/glossary/page.js** — Glossary Hub (static route). Per UI-SPEC "Hub Listing Pages Contract":

- Static metadata: `{ title: 'Glossary | Voco', description: 'Home service and AI terminology explained — from AI receptionist to voice triage.', alternates: { canonical: 'https://voco.live/glossary' } }`
- Page structure matches blog hub pattern:
  - Header: `bg-[#F5F5F4]` background, H1 "Glossary" Display size, subtitle "AI and home service terminology explained."
  - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`, `max-w-6xl mx-auto px-6 py-16`
  - Each card: shadcn Card with `card.base` token. Contains:
    - Page-type badge: `bg-[#F97316]/10 text-[#F97316] text-xs font-medium rounded-full px-2.5 py-0.5` with text "GLOSSARY"
    - Term name: `text-base font-semibold text-[#0F172A]`
    - Definition preview: `text-sm text-[#475569] line-clamp-2` (first 120 chars of definition)
    - "Read definition" link at `text-sm text-[#F97316] font-medium`
  - Card wrapped in `<Link href={'/glossary/' + term.slug}>`
- Import from `@/data/glossary`

**src/app/(public)/glossary/[term]/page.js** — Glossary Detail (dynamic route). Per UI-SPEC "Glossary Pages" contract:

- `generateStaticParams()`: Return `GLOSSARY_TERMS.map(t => ({ term: t.slug }))`
- `generateMetadata({ params })`: MUST `await params`. Title: `${item.term} | Voco`. Description: first 160 chars of `item.definition`. Canonical: `https://voco.live/glossary/${termSlug}`. OG: `/og?title=${encodeURIComponent(item.term)}&type=GLOSSARY`. If not found, return `{ title: 'Not Found | Voco' }`.
- Page component: `export default async function GlossaryDetailPage({ params })`. MUST `const { term: termSlug } = await params;`. Find item by slug. If not found, `notFound()`.
- Layout: `max-w-3xl mx-auto px-6 py-16 md:py-24` per UI-SPEC
- Definition section: Term as H1 Display (`text-4xl md:text-5xl font-semibold text-[#0F172A]`), definition as Body at `text-lg text-[#475569] leading-relaxed mt-6`
- FAQ Accordion: Create a NEW client component or adapt FAQSection pattern for LIGHT surface. This is a 'use client' component. Use Radix Accordion (same as FAQSection.jsx but with light-surface colors per UI-SPEC):
  - Accordion.Root: `type="single" collapsible`
  - Accordion.Item: `border-b border-stone-200` (NOT `border-white/[0.08]`)
  - Accordion.Trigger: `text-[#0F172A] font-semibold text-base min-h-[44px]` with ChevronDown icon `text-[#F97316]`
  - Accordion.Content: `text-[#475569] text-[15px] leading-relaxed`
  - Section heading above accordion: `text-2xl font-semibold text-[#0F172A] mt-12 mb-6` "Frequently Asked Questions"
  - Create inline or as `src/app/(public)/glossary/GlossaryFAQ.jsx` — 'use client' component accepting `items` prop
- Related terms: section below accordion with heading "Related terms" at `text-lg font-semibold text-[#0F172A] mt-12 mb-4`. Row of pill links: `rounded-full border border-stone-200 px-3 py-1 text-sm text-[#475569] hover:border-[#F97316] hover:text-[#F97316] transition-colors`. Each links to `/glossary/{relatedSlug}`. Only render if `relatedSlugs.length > 0`.
- Back-to-hub link: `<Link href="/glossary">` with "All terms" text above title
- JSON-LD: `<SchemaMarkup schema={{ '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: item.faqItems.map(faq => ({ '@type': 'Question', name: faq.q, acceptedAnswer: { '@type': 'Answer', text: faq.a } })) }} />`
- CTA at bottom: `<AuthAwareCTA variant="cta" />` wrapped in AnimatedSection
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(glossary|error|Error)" | head -20; echo "---"; ls src/app/\(public\)/glossary/page.js src/app/\(public\)/glossary/\[term\]/page.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/glossary/page.js contains `export const metadata` with title 'Glossary | Voco'
    - src/app/(public)/glossary/page.js imports GLOSSARY_TERMS from '@/data/glossary'
    - src/app/(public)/glossary/[term]/page.js contains `export async function generateStaticParams()`
    - src/app/(public)/glossary/[term]/page.js contains `const { term: termSlug } = await params` or equivalent await pattern
    - src/app/(public)/glossary/[term]/page.js contains SchemaMarkup with '@type': 'FAQPage'
    - src/app/(public)/glossary/[term]/page.js or GlossaryFAQ.jsx contains Radix Accordion with `border-stone-200` (light surface, NOT border-white)
    - src/app/(public)/glossary/[term]/page.js contains AuthAwareCTA component
    - src/app/(public)/glossary/[term]/page.js contains link back to /glossary hub
    - Glossary detail metadata has canonical URL containing 'https://voco.live/glossary/'
    - Glossary detail metadata has openGraph.images containing '/og?title='
  </acceptance_criteria>
  <done>Glossary hub shows term cards, glossary detail renders definition, FAQ accordion with FAQPage JSON-LD, related terms, and CTA</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors for blog and glossary routes
- Blog hub at /blog renders card grid
- Blog detail at /blog/ai-receptionist-for-plumbers renders article content
- Glossary hub at /glossary renders term cards
- Glossary detail at /glossary/ai-receptionist renders definition and FAQ accordion
- All pages have correct generateMetadata with await params pattern
- `npm test -- --testPathPattern=tests/unit/seo` still passes (no regressions)
</verification>

<success_criteria>
- Blog hub and detail pages render correctly with seed data
- Glossary hub and detail pages render with FAQ accordion on light surface
- All dynamic pages use generateStaticParams + generateMetadata with await params
- JSON-LD Article schema on blog detail, FAQPage schema on glossary detail
- Internal linking: back-to-hub links, AuthAwareCTA on every page
</success_criteria>

<output>
After completion, create `.planning/phases/38-programmatic-seo-content-engine/38-02-SUMMARY.md`
</output>
