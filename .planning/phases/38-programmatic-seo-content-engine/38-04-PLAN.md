---
phase: 38-programmatic-seo-content-engine
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/app/(public)/integrations/page.js
  - src/app/(public)/integrations/[tool]/page.js
autonomous: true
requirements:
  - SEO-07
  - SEO-10

must_haves:
  truths:
    - "/integrations renders a hub listing of all integrations from INTEGRATIONS data array"
    - "/integrations/google-calendar renders tool description, use case cards, and CTA"
    - "Every page uses generateMetadata() with await params, title template, canonical URL, and OG image"
    - "JSON-LD SoftwareApplication schema renders on integration detail pages"
  artifacts:
    - path: "src/app/(public)/integrations/page.js"
      provides: "Integrations hub listing page"
      contains: "INTEGRATIONS"
    - path: "src/app/(public)/integrations/[tool]/page.js"
      provides: "Integration detail page with use cases"
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/(public)/integrations/[tool]/page.js"
      to: "src/data/integrations.js"
      via: "import INTEGRATIONS for generateStaticParams"
      pattern: "import.*INTEGRATIONS.*from.*@/data/integrations"
    - from: "src/app/(public)/integrations/[tool]/page.js"
      to: "src/components/SchemaMarkup.jsx"
      via: "renders SoftwareApplication JSON-LD"
      pattern: "SoftwareApplication"
---

<objective>
Build the Integration page templates — hub listing and dynamic detail page. Integration pages are marketing-focused with tool description, use case cards, and CTA.

Purpose: Completes the fourth of five page template types. Runs parallel with Plans 02 and 03 in Wave 2.
Output: 2 page files (integrations hub, integration detail).
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-programmatic-seo-content-engine/38-CONTEXT.md
@.planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
@.planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
@.planning/phases/38-programmatic-seo-content-engine/38-01-SUMMARY.md

<interfaces>
<!-- Data layer contracts from Plan 01 -->

From src/data/integrations.js:
```javascript
export const INTEGRATIONS = [
  {
    slug: string,        // 'google-calendar'
    toolName: string,    // 'Google Calendar'
    description: string, // Full description paragraph
    useCases: [{ icon: string, title: string, body: string }],  // 4 items, icon is Lucide name
    ctaHeading: string,  // 'Connect Google Calendar to Voco in 5 minutes'
    relatedSlugs: string[],
  },
];
```

From src/components/SchemaMarkup.jsx:
```javascript
export function SchemaMarkup({ schema })
```

From src/components/landing/AuthAwareCTA.js:
```javascript
export function AuthAwareCTA({ variant = 'hero' })
```

From src/app/components/landing/AnimatedSection.jsx:
```javascript
<AnimatedSection direction="up">...</AnimatedSection>
```

From src/components/ui/card.jsx:
```javascript
import { Card, CardHeader, CardContent } from '@/components/ui/card';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Integrations hub listing page</name>
  <files>src/app/(public)/integrations/page.js</files>
  <read_first>
    src/data/integrations.js,
    src/components/ui/card.jsx,
    src/lib/design-tokens.js,
    src/app/components/landing/AnimatedSection.jsx,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md
  </read_first>
  <action>
**src/app/(public)/integrations/page.js** — Integrations Hub (static route). Per UI-SPEC "Hub Listing Pages Contract":

- Static metadata: `{ title: 'Integrations | Voco', description: 'Connect Voco to the tools you already use — Google Calendar, Outlook, and more.', alternates: { canonical: 'https://voco.live/integrations' } }`
- Page structure matches hub pattern:
  - Header section: `bg-[#F5F5F4] py-16 md:py-20`
  - Inner: `max-w-6xl mx-auto px-6`
  - H1: "Integrations" at `text-4xl md:text-5xl font-semibold text-[#0F172A]`
  - Subtitle: "Connect Voco to the tools you already use." at `text-lg text-[#475569] mt-4`
  - Card grid section: white bg, `max-w-6xl mx-auto px-6 py-16`
  - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
  - Each card: shadcn Card with `card.base` + `card.hover` design token classes. Contains:
    - Badge: "INTEGRATION" in `bg-[#F97316]/10 text-[#F97316] text-xs font-medium rounded-full px-2.5 py-0.5`
    - Tool name: `text-base font-semibold text-[#0F172A]` rendering `integration.toolName`
    - Description preview: `text-sm text-[#475569] line-clamp-2`
    - "Learn more" link at `text-sm text-[#F97316] font-medium`
  - Card wrapped in `<Link href={'/integrations/' + integration.slug}>`
- Import `INTEGRATIONS` from `@/data/integrations`
- Import Card from `@/components/ui/card`
- Import AnimatedSection for fade-in
- Import Link from `next/link`
  </action>
  <verify>
    <automated>ls src/app/\(public\)/integrations/page.js 2>/dev/null && grep -c "INTEGRATIONS" src/app/\(public\)/integrations/page.js</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/integrations/page.js exists
    - File contains `export const metadata` with title 'Integrations | Voco'
    - File imports INTEGRATIONS from '@/data/integrations'
    - File imports Card from '@/components/ui/card'
    - File contains Link components wrapping cards pointing to '/integrations/' + slug
    - File contains page-type badge text "INTEGRATION"
    - Metadata has alternates.canonical 'https://voco.live/integrations'
  </acceptance_criteria>
  <done>Integrations hub shows card grid listing all integration tools</done>
</task>

<task type="auto">
  <name>Task 2: Create Integration detail page with use cases and SoftwareApplication schema</name>
  <files>src/app/(public)/integrations/[tool]/page.js</files>
  <read_first>
    src/data/integrations.js,
    src/components/SchemaMarkup.jsx,
    src/components/landing/AuthAwareCTA.js,
    src/components/ui/card.jsx,
    src/app/components/landing/AnimatedSection.jsx,
    src/lib/design-tokens.js,
    .planning/phases/38-programmatic-seo-content-engine/38-UI-SPEC.md,
    .planning/phases/38-programmatic-seo-content-engine/38-RESEARCH.md
  </read_first>
  <action>
**src/app/(public)/integrations/[tool]/page.js** — Integration Detail (dynamic route). Per UI-SPEC "Integration Pages" contract and D-06:

- `generateStaticParams()`: Return `INTEGRATIONS.map(i => ({ tool: i.slug }))`
- `generateMetadata({ params })`: MUST `await params`. Title: `${item.toolName} Integration | Voco`. Description: first 160 chars of `item.description`. Canonical: `https://voco.live/integrations/${toolSlug}`. OG: `/og?title=${encodeURIComponent(item.toolName + ' + Voco')}&type=INTEGRATION`. If not found, `{ title: 'Not Found | Voco' }`.
- Page component: `export default async function IntegrationDetailPage({ params })`. MUST `const { tool: toolSlug } = await params;`. Find item from `INTEGRATIONS.find(i => i.slug === toolSlug)`. If not found, `notFound()`.

**Hero section** (white bg):
- Container: `max-w-5xl mx-auto px-6 pt-24 md:pt-32 pb-12`
- Back-to-hub: `<Link href="/integrations">` with "All integrations" text and ArrowLeft icon
- Logo connector row: flex row with "Voco" text logo + `<span className="mx-4 text-2xl text-[#64748B]">+</span>` + tool name text. Both at `text-xl font-semibold text-[#0F172A]`. This is a simple text connector (no actual logos per D-06 marketing focus).
- H1: `text-4xl md:text-5xl font-semibold text-[#0F172A] leading-tight mt-8` rendering `Voco + ${item.toolName}`
- Subheadline: `text-lg text-[#475569] mt-4 max-w-2xl`

**Description section** (white bg):
- Container: `max-w-5xl mx-auto px-6 pb-12`
- Prose: `max-w-2xl text-base text-[#475569] leading-relaxed` rendering `item.description`

**Use cases section** (bg-[#F5F5F4]):
- Container: `py-16 md:py-24`
- Inner: `max-w-5xl mx-auto px-6`
- H2: `text-2xl md:text-3xl font-semibold text-[#0F172A] text-center mb-12` "What you can do"
- 2x2 grid: `grid grid-cols-1 md:grid-cols-2 gap-6`
- Each use case card: shadcn Card with white bg, `border border-stone-200/60 p-6`:
  - Icon: Lucide icon from `useCase.icon` name, mapped via icon lookup object `{ Calendar, Clock, Bell, RefreshCw }` imported from `lucide-react`. Rendered at `w-8 h-8 text-[#F97316] mb-4`.
  - Title: `text-lg font-semibold text-[#0F172A] mb-2` rendering `useCase.title`
  - Body: `text-sm text-[#475569] leading-relaxed` rendering `useCase.body`

**CTA section** (bg-[#F5F5F4] card):
- Container: `max-w-5xl mx-auto px-6 py-16`
- CTA card: `bg-[#F5F5F4] rounded-2xl p-8 md:p-12 text-center`
- H2: `text-2xl md:text-3xl font-semibold text-[#0F172A] mb-6` rendering `item.ctaHeading`
- CTA: `<AuthAwareCTA variant="cta" />`

- JSON-LD: `<SchemaMarkup schema={{ '@context': 'https://schema.org', '@type': 'SoftwareApplication', name: 'Voco', applicationCategory: 'BusinessApplication', operatingSystem: 'Web', description: 'AI receptionist for home service businesses — integrates with ' + item.toolName }} />`

- Related integrations: if `item.relatedSlugs.length > 0`, render "Related integrations" section with cards linking to siblings. Otherwise omit.
- Wrap sections in `<AnimatedSection direction="up">`
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(integrations|tool|error|Error)" | head -20; echo "---"; ls src/app/\(public\)/integrations/\[tool\]/page.js 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/integrations/[tool]/page.js contains `export async function generateStaticParams()`
    - File contains `export async function generateMetadata({ params })` with `await params`
    - File contains `const { tool: toolSlug } = await params` or equivalent await pattern
    - File contains `notFound()` from 'next/navigation' for missing slugs
    - File imports INTEGRATIONS from '@/data/integrations'
    - File contains SchemaMarkup with '@type': 'SoftwareApplication'
    - File imports and renders Lucide icons (Calendar, Clock, Bell, RefreshCw)
    - File contains AuthAwareCTA component
    - File contains link back to /integrations hub ("All integrations")
    - File renders use case cards in a 2-column grid
    - Integration detail metadata has canonical URL containing 'https://voco.live/integrations/'
    - Integration detail metadata has openGraph.images containing '/og?title='
  </acceptance_criteria>
  <done>Integration detail renders tool description, 2x2 use case card grid, SoftwareApplication schema, and CTA</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors for /integrations routes
- Integrations hub at /integrations renders card grid
- Integration detail at /integrations/google-calendar renders description and use cases
- generateMetadata uses await params pattern on dynamic route
- `npm test -- --testPathPattern=tests/unit/seo` still passes (no regressions)
</verification>

<success_criteria>
- Integration hub and detail pages render correctly with seed data
- Dynamic page uses generateStaticParams + generateMetadata with await params
- Use case cards render with correct Lucide icons
- JSON-LD SoftwareApplication schema on detail page
- Internal linking: back-to-hub link, AuthAwareCTA
</success_criteria>

<output>
After completion, create `.planning/phases/38-programmatic-seo-content-engine/38-04-SUMMARY.md`
</output>
