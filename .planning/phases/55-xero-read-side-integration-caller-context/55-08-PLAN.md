---
phase: 55
plan: 08
type: execute
wave: 3
depends_on: [55-03, 55-04, 55-05, 55-07]
files_modified:
  - .claude/skills/voice-call-architecture/SKILL.md
  - .claude/skills/auth-database-multitenancy/SKILL.md
  - .claude/skills/dashboard-crm-system/SKILL.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - XERO-01
  - XERO-02
  - XERO-03
  - XERO-04

must_haves:
  truths:
    - "voice-call-architecture skill documents the new check_customer_account tool, the customer_context prompt block, the 4th parallel task in _run_db_queries, and the integrations/xero.py module — so future agents touching the call path know about Xero context"
    - "voice-call-architecture skill explicitly notes the intentional cross-runtime camelCase/snake_case divergence between Next.js fetchCustomerByPhone (Plan 02) and Python fetch_xero_customer_by_phone (Plan 06) so future maintainers don't 'fix' the inconsistency"
    - "auth-database-multitenancy skill documents migration 053 (error_state column) and the Python service-role write-back pattern (Pitfall 5)"
    - "dashboard-crm-system skill documents the BusinessIntegrationsClient new states (Reconnect banner, last-synced timestamp), the connect_xero checklist item, and the XeroReconnectEmail template"
    - "ROADMAP.md Phase 55 entry shows all plans complete with their actual outcome notes"
    - "STATE.md current position advances to Phase 56 (Jobber read-side) ready-to-plan; pending todo 'register Xero dev account' marked complete"
    - "REQUIREMENTS.md traceability table marks XERO-01..XERO-04 → Phase 55 → Complete"
  artifacts:
    - path: .claude/skills/voice-call-architecture/SKILL.md
      provides: "Updated to reflect P55 changes including cross-runtime casing note"
    - path: .claude/skills/auth-database-multitenancy/SKILL.md
      provides: "Migration 053 + Python write-back pattern documented"
    - path: .claude/skills/dashboard-crm-system/SKILL.md
      provides: "BusinessIntegrationsClient states + connect_xero checklist + Resend email pattern documented"
    - path: .planning/ROADMAP.md
      provides: "Phase 55 closed; plan checklist all marked done"
    - path: .planning/STATE.md
      provides: "Position advanced to Phase 56"
    - path: .planning/REQUIREMENTS.md
      provides: "XERO-01..04 traceability marked complete"
  key_links:
    - from: ".claude/skills/voice-call-architecture/SKILL.md"
      to: "src/integrations/xero.py + tools/check_customer_account.py + prompt.py customer_context block"
      via: "skill index + rule references"
      pattern: "check_customer_account|customer_context|integrations/xero"
---

<objective>
Close Phase 55 by syncing all four documentation surfaces required by `./CLAUDE.md`'s skill-sync rule:

1. **`voice-call-architecture` skill** — document the Python livekit-agent additions (integrations/xero.py, tools/check_customer_account.py, prompt.py customer_context block, 4th parallel task in _run_db_queries with 800ms timeout) AND the intentional cross-runtime casing divergence between Next.js (camelCase) and Python (snake_case) customer_context shapes
2. **`auth-database-multitenancy` skill** — document migration 053 (error_state column) and the Python-side service-role write-back pattern that prevents stale-token races (Pitfall 5)
3. **`dashboard-crm-system` skill** — document BusinessIntegrationsClient's new card states (Reconnect banner, Last-synced timestamp), the connect_xero checklist item, and the XeroReconnectEmail Resend template
4. **`.planning/ROADMAP.md`** — mark Phase 55 plans complete; update Phase 55 entry with actual deliverables and any deferred items
5. **`.planning/STATE.md`** — advance position to Phase 56; mark "register Xero dev account" pending todo complete; record Phase 55 milestone progress
6. **`.planning/REQUIREMENTS.md`** — mark XERO-01, XERO-02, XERO-03, XERO-04 → Phase 55 → Complete in the traceability table

Per `./CLAUDE.md` skill rules: "Read the relevant skill before changes, update it after." This plan is the AFTER step.

Purpose: Future agents (and the user) need accurate skill files to navigate the codebase. Without this, the next time someone touches the agent prompt or the integrations card, they won't know about the Xero changes and may break them.
Output: 6 docs in sync with the new code surface.
</objective>

<execution_context>
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leroyngzz/Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/skills/voice-call-architecture/SKILL.md
@.claude/skills/auth-database-multitenancy/SKILL.md
@.claude/skills/dashboard-crm-system/SKILL.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-01-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-02-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-03-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-04-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-05-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-06-SUMMARY.md
@.planning/phases/55-xero-read-side-integration-caller-context/55-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update voice-call-architecture skill</name>
  <read_first>
    - .claude/skills/voice-call-architecture/SKILL.md (full file — find existing sections on tools, prompt, _run_db_queries)
    - .claude/skills/voice-call-architecture/rules/*.md (any rule files that touch tools or prompt — likely need a new rule for Xero context)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-02-SUMMARY.md (Plan 02 outcomes — Next.js fetchCustomerByPhone camelCase shape)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-06-SUMMARY.md (Plan 06 outcomes — Python fetch_xero_customer_by_phone snake_case shape)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-07-SUMMARY.md (Plan 07 outcomes)
  </read_first>
  <files>.claude/skills/voice-call-architecture/SKILL.md</files>
  <action>
In `.claude/skills/voice-call-architecture/SKILL.md`, locate the index sections covering tools, prompt construction, and the _run_db_queries pattern. Add or update entries:

**Tools section (or equivalent):**
Append a `check_customer_account` row to the tools table with:
- File: `livekit-agent/src/tools/check_customer_account.py` (cross-repo, separate Python repo)
- Purpose: Re-serves cached Xero customer-context as STATE+DIRECTIVE string when caller explicitly asks about their account
- Source: cached data from `_run_db_queries` (no live re-fetch)
- Privacy: silent-awareness; never volunteers data
- Phase: 55

**Prompt section:**
Add a note that `build_system_prompt` accepts a `customer_context` kwarg (added P55) which injects a CRITICAL RULE-framed STATE+DIRECTIVE block immediately after the anti-hallucination section. When None, block omitted (D-11). Reference `format_customer_context_state()` in `tools/check_customer_account.py` as the shared formatter.

**_run_db_queries section:**
Update the parallel-task list from 3 to 4:
1. subscription
2. intake
3. call_record
4. **xero_context** (P55 — bounded by `asyncio.wait_for(..., timeout=0.8)`; on timeout/exception sets `customer_context=None` and Sentry-captures with hashed phone tag)

**Integrations section (NEW):**
Add a subsection documenting `livekit-agent/src/integrations/xero.py`:
- Module purpose: refresh-aware Xero REST fetch via raw httpx
- Service-role Supabase reads `accounting_credentials` row
- Refresh write-back persists access_token + refresh_token + expiry_date (Pitfall 5)
- On refresh failure: persists `error_state='token_refresh_failed'` (Next.js dashboard surfaces banner + email; Python NEVER sends email)
- Returns the standard `{contact, outstanding_balance, last_invoices, last_payment_date}` shape OR None

**Cross-runtime customer_context shape — CASING NOTE (NEW, mandatory):**
Add a clearly-flagged note to the integrations or prompt section that the customer_context payload returned by `fetchCustomerByPhone` and `fetch_xero_customer_by_phone` is the SAME LOGICAL SHAPE in both runtimes but uses LANGUAGE-IDIOMATIC CASING. The skill MUST contain prose substantively equivalent to:

> "fetchCustomerByPhone returns the same logical shape in both runtimes — Next.js uses camelCase ({contact, outstandingBalance, lastInvoices, lastPaymentDate}); Python uses snake_case ({contact, outstanding_balance, last_invoices, last_payment_date}). Intentional language-idiomatic divergence — DO NOT 'unify' the casing."

Place this note adjacent to the Integrations subsection so future maintainers reading either side see it. Use the literal strings `outstandingBalance` (camelCase) and `outstanding_balance` (snake_case) and the phrase `language-idiomatic divergence` so the grep checks pass. The rationale: each runtime serializes per its ecosystem convention (Next.js JSON-style, Python PEP 8 dicts); the agent prompt formatter (`format_customer_context_state` in tools/check_customer_account.py) reads the snake_case shape; the dashboard reads the camelCase shape. They never cross runtime boundaries.

**Update Phase Reference:**
Add a row at the bottom of any "phase reference table" with Phase 55 → Xero read-side caller-context.

Keep the SKILL.md lightweight (~130 lines per the skill convention). If detail bloats over 150 lines, extract a new rule file `.claude/skills/voice-call-architecture/rules/xero-context.md` and reference it from SKILL.md. The casing note can live in the rule file too — but the SKILL.md itself MUST contain the literal grep-checkable strings (`outstandingBalance`, `outstanding_balance`, `language-idiomatic divergence` or `intentional`) so the acceptance grep does not need to traverse rules/.
  </action>
  <verify>
    <automated>grep -n "check_customer_account\|customer_context\|integrations/xero" .claude/skills/voice-call-architecture/SKILL.md && grep -q "outstandingBalance" .claude/skills/voice-call-architecture/SKILL.md && grep -q "outstanding_balance" .claude/skills/voice-call-architecture/SKILL.md && grep -qE "language-idiomatic divergence|intentional" .claude/skills/voice-call-architecture/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md mentions `check_customer_account` (tool)
    - SKILL.md mentions `customer_context` (prompt block)
    - SKILL.md mentions `integrations/xero` (module)
    - SKILL.md mentions the 800ms timeout in _run_db_queries
    - SKILL.md mentions the silent-awareness privacy rule (D-10)
    - `grep -q "outstandingBalance" .claude/skills/voice-call-architecture/SKILL.md` succeeds (Next.js camelCase form documented)
    - `grep -q "outstanding_balance" .claude/skills/voice-call-architecture/SKILL.md` succeeds (Python snake_case form documented)
    - `grep -qE "language-idiomatic divergence|intentional" .claude/skills/voice-call-architecture/SKILL.md` succeeds (rationale documented)
    - File line count stays reasonable (<200 lines or extract to rules/, but the casing strings remain in SKILL.md)
  </acceptance_criteria>
  <done>Skill reflects current state of voice call architecture post-P55, including the explicit cross-runtime casing divergence note that prevents future "consistency" refactors from breaking either side.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update auth-database-multitenancy skill</name>
  <read_first>
    - .claude/skills/auth-database-multitenancy/SKILL.md (full file — find migrations index, accounting_credentials section, Python service-role section if present)
    - supabase/migrations/053_xero_error_state.sql (Plan 01 deliverable)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-01-SUMMARY.md
  </read_first>
  <files>.claude/skills/auth-database-multitenancy/SKILL.md</files>
  <action>
In `.claude/skills/auth-database-multitenancy/SKILL.md`:

**Migrations index:**
Add row for `053_xero_error_state.sql` — adds `accounting_credentials.error_state TEXT NULL` + partial index `idx_accounting_credentials_error_state ON (tenant_id, provider) WHERE error_state IS NOT NULL`. Used by Phase 55 to surface Xero token-refresh failures in the BusinessIntegrationsClient banner + Resend email.

**accounting_credentials table reference:**
Update the table schema entry to include `error_state TEXT NULL` column with semantics:
- NULL = healthy
- 'token_refresh_failed' = refresh failed; dashboard renders banner + sends one Resend email
- Cleared on: successful OAuth re-callback, successful token refresh
- Cleared by: row deletion (Disconnect)

**Python service-role pattern:**
Add a note (or new rule file `.claude/skills/auth-database-multitenancy/rules/python-service-role-writeback.md`) documenting the cross-repo livekit-agent write-back pattern:
- The Python agent refreshes Xero tokens during `fetch_xero_customer_by_phone`
- Per Pitfall 5: write-back is mandatory (access_token + refresh_token + expiry_date together) to prevent Next.js / Python stale-token race
- Same pattern will apply to Phase 56 Jobber read-side
- Refresh-failure write of `error_state='token_refresh_failed'` lets the dashboard surface the issue (Python never sends email — that's dashboard-only per RESEARCH known-pitfall)

Keep the file index-style; if the Python pattern grows complex, extract to `rules/python-service-role-writeback.md` (~50 lines).
  </action>
  <verify>
    <automated>grep -n "053_xero_error_state\|error_state\|service-role.*writeback\|write-back" .claude/skills/auth-database-multitenancy/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md indexes migration 053
    - SKILL.md describes the error_state column semantics
    - SKILL.md (or rule file) documents the Python write-back pattern
  </acceptance_criteria>
  <done>Skill reflects schema + cross-repo write-back pattern.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update dashboard-crm-system skill</name>
  <read_first>
    - .claude/skills/dashboard-crm-system/SKILL.md (full file — find sections on integrations card, setup checklist, design tokens)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-05-SUMMARY.md
    - .planning/phases/55-xero-read-side-integration-caller-context/55-UI-SPEC.md
  </read_first>
  <files>.claude/skills/dashboard-crm-system/SKILL.md</files>
  <action>
In `.claude/skills/dashboard-crm-system/SKILL.md`:

**BusinessIntegrationsClient section:**
Update the card states inventory to list 4 states (was 3 in P54):
1. Disconnected — P54
2. Connected — P54 + P55 (added muted "Last synced X ago" timestamp from `last_context_fetch_at`)
3. **Reconnect needed** (NEW P55) — amber Alert banner with AlertTriangle icon when `error_state='token_refresh_failed'`; primary "Reconnect Xero" button + secondary ghost "Disconnect" link
4. Loading skeleton — P54

Reference UI-SPEC.md at `.planning/phases/55-xero-read-side-integration-caller-context/55-UI-SPEC.md` for canonical copy + colors.

**Setup checklist section:**
Add `connect_xero` to the items table:
- Theme: `voice`
- Auto-completion: presence of `accounting_credentials WHERE provider='xero'`
- Target URL: `/dashboard/more/integrations`

**Resend email templates section:**
Add `XeroReconnectEmail` (`src/emails/XeroReconnectEmail.jsx`) with:
- Trigger: `notifyXeroRefreshFailure(tenantId, ownerEmail)` from `src/lib/notifications.js`
- Subject: "Your Xero connection needs attention"
- Body: locked plain-language excerpt per UI-SPEC §Copywriting Contract
- CTA URL: `/dashboard/more/integrations`
- Caller scope: dashboard read paths only — NEVER from the Python call path

**Notify helper:**
Document `notifyXeroRefreshFailure(tenantId, ownerEmail)` in the notifications section: writes `error_state` + revalidates integration-status tag + sends email. Default `notifyOnFailure: false` on `refreshTokenIfNeeded` to keep the call path silent.
  </action>
  <verify>
    <automated>grep -n "Reconnect needed\|XeroReconnectEmail\|connect_xero\|notifyXeroRefreshFailure\|Last synced" .claude/skills/dashboard-crm-system/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md lists all 4 card states including Reconnect-needed
    - SKILL.md indexes connect_xero checklist item
    - SKILL.md documents XeroReconnectEmail + notifyXeroRefreshFailure
    - SKILL.md references the UI-SPEC.md path for canonical copy
  </acceptance_criteria>
  <done>Dashboard skill reflects all P55 owner-facing UX additions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update ROADMAP.md, STATE.md, REQUIREMENTS.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 55 section ~line 246-256)
    - .planning/STATE.md (Current Position, Pending Todos, Decisions sections)
    - .planning/REQUIREMENTS.md (traceability table at bottom — find or create v6.0 section)
    - .planning/phases/55-xero-read-side-integration-caller-context/55-0{1..7}-SUMMARY.md (all plan summaries for actual deliverable notes)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>
**ROADMAP.md (~line 246-256):**

Replace the `Plans: TBD` section with the actual 8 plans:

```
**Plans:** 8 plans

Plans:
- [x] 55-01-PLAN.md — Migration 053 error_state column + .env.example + schema push
- [x] 55-02-PLAN.md — Next.js XeroAdapter.fetchCustomerByPhone + 'use cache' + two-tier cacheTag
- [x] 55-03-PLAN.md — OAuth wire (auth/callback heal error_state + xero-context revalidateTag) + disconnect (revoke + xero-context invalidate)
- [x] 55-04-PLAN.md — /api/webhooks/xero (HMAC + intent-verify + invoice→phone resolution + per-phone revalidateTag)
- [x] 55-05-PLAN.md — BusinessIntegrationsClient states (Reconnect banner + last-synced timestamp) + setup checklist connect_xero + XeroReconnectEmail + notifyXeroRefreshFailure
- [x] 55-06-PLAN.md — [CROSS-REPO] livekit-agent integrations/xero.py + agent.py 4th parallel task with 800ms timeout
- [x] 55-07-PLAN.md — [CROSS-REPO] livekit-agent prompt.py customer_context block + check_customer_account tool
- [x] 55-08-PLAN.md — Skills sync (voice-call-architecture, auth-database-multitenancy, dashboard-crm-system) + ROADMAP/STATE/REQUIREMENTS update
```

Mark the Phase 55 header `### Phase 55:` as completed if your roadmap convention uses checkboxes; add a "Closed" date.

**STATE.md updates:**

```yaml
status: completed-phase-55-ready-for-56
stopped_at: Phase 55 complete (UAT pending)
last_updated: <today's date>
last_activity: <today's date>
progress:
  total_phases: 12
  completed_phases: 12
  total_plans: 61   # was 53; +8 from P55
  completed_plans: 61
  percent: 100
```

In `## Current Position`:
- Update Phase to "55 CLOSED → next Phase 56 (Jobber read-side)"
- Status: "Ready to plan Phase 56"

In `## Accumulated Context > Decisions (v6.0 planning)`, append:
- `[v6.0 P55]: Xero read-side shipped — XeroAdapter.fetchCustomerByPhone with 'use cache' two-tier cacheTag, /api/webhooks/xero handler with intent-verify, BusinessIntegrationsClient gets 4 states with Reconnect banner + last-synced timestamp, livekit-agent injects customer_context as STATE+DIRECTIVE block, check_customer_account tool re-serves cached data, error_state column + Resend email surface refresh failures`

In `## Accumulated Context > Pending Todos`:
- Mark `User to register Xero dev account at developer.xero.com` as DONE / strike through (presumably done since OAuth flow worked end-to-end in UAT)
- Keep Jobber dev account todo (still pending for P56)

**REQUIREMENTS.md traceability table:**

Find or create the v6.0 traceability section. Add rows:
```
| XERO-01 | Phase 55 | Complete |
| XERO-02 | Phase 55 | Complete |
| XERO-03 | Phase 55 | Complete |
| XERO-04 | Phase 55 | Complete |
```

Update v6.0 coverage notes at the bottom (or add if not present):
- v6.0 requirements: ~30 total (RENAME, TOGGLE, INTFOUND, XERO, JOBBER, JOBSCHED, CHECKLIST, CTX, POLISH)
- Mapped to phases: include XERO-01..04 → Phase 55
  </action>
  <verify>
    <automated>grep -n "55-01-PLAN\|55-08-PLAN" .planning/ROADMAP.md && grep -n "Phase 55 CLOSED\|completed-phase-55" .planning/STATE.md && grep -n "XERO-01.*Phase 55.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md lists all 8 plans with [x] checkboxes
    - STATE.md current position shows Phase 55 closed
    - STATE.md decisions section appends a P55 summary entry
    - STATE.md pending todos: Xero dev account marked done
    - REQUIREMENTS.md traceability table includes 4 new rows for XERO-01..04
  </acceptance_criteria>
  <done>All planning docs in sync with shipped state. Project ready for `/gsd-plan-phase 56` (Jobber read-side).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill files → future agents | Skills inform what code patterns are safe to assume |
| ROADMAP/STATE/REQUIREMENTS → planning agents | These drive what gets planned next |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-08-01 | Tampering | Stale skill files mislead future agents (e.g. they don't know about check_customer_account and remove it) | mitigate | This entire plan exists to prevent this. The CLAUDE.md skill-sync rule enforces. |
| T-55-08-02 | Information Disclosure | Skill files leak secrets (e.g. paste a real refresh_token into an example) | mitigate | All examples in skill updates use placeholder values; never real tokens. |
| T-55-08-03 | Repudiation | ROADMAP/STATE updates lose track of what shipped | accept | Git history captures everything; the planning docs are convenience, not source-of-truth. |
| T-55-08-04 | Tampering | Future "consistency refactor" unifies camelCase/snake_case across runtimes and breaks one side | mitigate | Task 1 documents the divergence as INTENTIONAL with grep-checkable strings; CLAUDE.md skill-sync rule ensures any cross-runtime work consults voice-call-architecture skill. |
</threat_model>

<verification>
- All 4 grep checks return matches (Task 1's grep includes 3 sub-checks for the casing note)
- A manual read of each updated SKILL.md confirms the index reads naturally
</verification>

<success_criteria>
The next agent invoked by the user (whether for P56 Jobber or any maintenance work touching the call path or integrations card) reads the updated skills and immediately understands the P55 surface area, including that camelCase (Next.js) and snake_case (Python) shapes are intentionally distinct. STATE.md correctly advertises Phase 56 as the next ready-to-plan target.
</success_criteria>

<output>
After completion, create `.planning/phases/55-xero-read-side-integration-caller-context/55-08-SUMMARY.md` AND `.planning/phases/55-xero-read-side-integration-caller-context/55-SUMMARY.md` (overall phase summary aggregating the 8 plan summaries).
</output>
