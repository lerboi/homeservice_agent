---
phase: 13-frontend-public-pages-redesign
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/app/(public)/page.js
  - src/app/components/landing/HowItWorksSection.jsx
  - src/app/components/landing/SocialProofSection.jsx
  - src/app/components/landing/FeaturesGrid.jsx
  - src/app/components/landing/FinalCTASection.jsx
  - src/app/(public)/pricing/page.js
  - src/app/(public)/pricing/PricingTiers.jsx
  - src/app/(public)/pricing/FAQSection.jsx
  - src/app/(public)/pricing/ComparisonTable.jsx
  - src/app/(public)/about/page.js
  - src/app/(public)/contact/page.js
  - src/app/(public)/contact/ContactForm.jsx
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-13, D-14, D-15, D-16, D-17, D-18, D-21, D-24, D-25, D-26, D-27, D-28, D-29]

must_haves:
  truths:
    - "Home page section flow is: dark hero -> charcoal HIW -> stone Features -> dark Social Proof -> copper CTA"
    - "HowItWorks loading skeleton has dark charcoal background, not white"
    - "Pricing tier cards are dark (#1E293B) with copper glow hover effect"
    - "Pricing comparison table sits in a stone (#F5F5F4) section"
    - "FAQ section is dark with copper chevron accent"
    - "About page has dark hero, stone values section, dark closing CTA"
    - "Contact page has dark hero and dark form card with copper focus glow on inputs"
    - "All cards on dark backgrounds have the copper hover glow treatment"
    - "One stone (#F5F5F4) breath section exists per page"
  artifacts:
    - path: "src/app/(public)/page.js"
      provides: "Updated HowItWorks skeleton bg to #1E293B"
      contains: "bg-[#1E293B]"
    - path: "src/app/(public)/pricing/PricingTiers.jsx"
      provides: "Dark tier cards with copper hover"
      contains: "hover:border-[#C2410C]/40"
    - path: "src/app/(public)/pricing/FAQSection.jsx"
      provides: "Dark FAQ with copper chevron"
      contains: "text-[#C2410C]"
    - path: "src/app/(public)/about/page.js"
      provides: "Dark hero + stone values + dark CTA"
      contains: "bg-[#F5F5F4]"
    - path: "src/app/(public)/contact/ContactForm.jsx"
      provides: "Dark form card with copper focus"
      contains: "rgba(194,65,12"
  key_links:
    - from: "src/app/(public)/page.js"
      to: "HowItWorksSection.jsx"
      via: "dynamic import skeleton bg must match section bg"
      pattern: "bg-\\[#1E293B\\]"
    - from: "PricingTiers.jsx"
      to: "pricing/page.js"
      via: "tier cards rendered within dark hero section"
      pattern: "bg-\\[#0F172A\\]"
---

<objective>
Apply the Premium Dark SaaS reskin to all public page content: Home page section backgrounds, Pricing page (tier cards, comparison table, FAQ, CTA), About page, and Contact page.

Purpose: Transform all public-facing content from the mixed light/dark palette to the consistent dark-first design with stone breath sections and copper hover accents.

Output: All 12 page/section component files updated with dark palette, copper hover treatments, and stone breath sections.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-frontend-public-pages-redesign/13-CONTEXT.md
@.planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md
@.planning/phases/13-frontend-public-pages-redesign/13-RESEARCH.md

@.planning/phases/13-frontend-public-pages-redesign/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page section backgrounds + skeleton update + section dark reskins</name>
  <files>src/app/(public)/page.js, src/app/components/landing/HowItWorksSection.jsx, src/app/components/landing/SocialProofSection.jsx, src/app/components/landing/FeaturesGrid.jsx, src/app/components/landing/FinalCTASection.jsx</files>
  <read_first>
    - src/app/(public)/page.js (dynamic import skeletons — especially HowItWorks skeleton bg-white on line 12)
    - src/app/components/landing/HowItWorksSection.jsx (full file — current bg class)
    - src/app/components/landing/SocialProofSection.jsx (full file — current bg and card classes)
    - src/app/components/landing/FeaturesGrid.jsx (full file — current bg and card classes, bento grid structure)
    - src/app/components/landing/FinalCTASection.jsx (full file — current bg class)
    - .planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md (Home Page Section Flow contract)
  </read_first>
  <action>
**Home page section flow per D-18:**
1. Hero — `bg-[#0F172A]` (already correct, no change needed per D-21)
2. How It Works — `bg-[#1E293B]` (charcoal)
3. Features — `bg-[#F5F5F4]` (stone — this page's breath section, likely already correct)
4. Social Proof — `bg-[#0F172A]` (likely already correct)
5. Final CTA — `bg-[#C2410C]` copper gradient (likely already correct)

**page.js — HowItWorks skeleton update (Pitfall 2 from RESEARCH.md):**
- Change HowItWorks loading skeleton `bg-white` to `bg-[#1E293B]` on the section wrapper (line 12)
- Change skeleton shimmer elements from `bg-black/10` and `bg-black/[0.04]` to `bg-white/10` and `bg-white/[0.04]` (dark surface needs light shimmer)
- Change `bg-[#F5F5F4]/50` to `bg-white/[0.03]` for the main content placeholder (dark card-like placeholder)
- Change `border-black/[0.04]` to `border-white/[0.06]` on the placeholder border
- Other skeleton sections (FeaturesGrid, SocialProofSection, FinalCTASection) likely already have correct backgrounds — verify and update only if they don't match the flow above

**HowItWorksSection.jsx:**
- Change section background from whatever current value to `bg-[#1E293B]`
- Update any text colors to use light text: headings `text-[#F1F5F9]`, body text `text-[#94A3B8]`
- Update any eyebrow/label text to use `text-[#C2410C]` copper accent (if there's an eyebrow like "HOW IT WORKS")
- If tab buttons exist within HowItWorksTabs (imported dynamically), those are handled by HowItWorksTabs itself — only change the section wrapper and direct children
- For mobile fallback per D-26: Inside HowItWorksSection, if HowItWorksTabs is rendered, wrap it with `hidden md:block`. Add a mobile-only stacked steps version using `block md:hidden` — simple numbered list of the same steps content, no JS tabs, no animation complexity

**SocialProofSection.jsx:**
- Verify background is already `bg-[#0F172A]` — update if not
- Apply dark card hover treatment to testimonial cards per D-05/D-06:
  - Rest: `bg-[#1E293B] border border-white/[0.06]`
  - Hover: `hover:border-[#C2410C]/40 hover:shadow-[0_0_20px_rgba(194,65,12,0.15)] hover:-translate-y-0.5`
  - Add `transition-all duration-200` for smooth hover
- Update text colors: names/titles `text-[#F1F5F9]`, quote text `text-[#94A3B8]`
- Eyebrow text: `text-[#C2410C]` copper accent

**FeaturesGrid.jsx:**
- Verify section background is `bg-[#F5F5F4]` (stone breath section) — this page's designated breath section
- Feature bento cards should use stone-section card style: `bg-white border border-stone-200/60` (light cards on stone background, per UI-SPEC "Stone section cards" contract)
- Text on stone section: headings `text-[#0F172A]`, body `text-[#475569]` (landing-muted)
- Eyebrow text: `text-[#C2410C]` copper accent
- Mobile fallback per D-25: Change bento grid from `grid-cols-6` to `grid-cols-1 md:grid-cols-6` so mobile gets single-column stacked full-width cards

**FinalCTASection.jsx:**
- Verify background is copper gradient `bg-[#C2410C]` or `bg-gradient-to-br from-[#C2410C] to-[#9A3412]` — update if needed
- Button should be inverted: dark bg on copper background, e.g. `bg-[#0F172A] text-white hover:bg-[#0F172A]/90`
- Text: white on copper background
  </action>
  <verify>
    <automated>grep -n "bg-\[#1E293B\]" src/app/\(public\)/page.js && grep -n "bg-\[#1E293B\]" src/app/components/landing/HowItWorksSection.jsx && grep -n "hover:border-\[#C2410C\]" src/app/components/landing/SocialProofSection.jsx && grep -n "grid-cols-1" src/app/components/landing/FeaturesGrid.jsx</automated>
  </verify>
  <acceptance_criteria>
    - page.js HowItWorks skeleton has `bg-[#1E293B]` (not bg-white)
    - page.js HowItWorks skeleton shimmer uses `bg-white/10` (not bg-black/10)
    - HowItWorksSection.jsx section wrapper has `bg-[#1E293B]`
    - HowItWorksSection.jsx has mobile fallback: `block md:hidden` stacked steps + `hidden md:block` HowItWorksTabs
    - SocialProofSection.jsx cards have `bg-[#1E293B] border border-white/[0.06]`
    - SocialProofSection.jsx cards have `hover:border-[#C2410C]/40` and `hover:shadow-[0_0_20px_rgba(194,65,12,0.15)]`
    - FeaturesGrid.jsx section has `bg-[#F5F5F4]`
    - FeaturesGrid.jsx grid has `grid-cols-1 md:grid-cols-6` (mobile single-column)
    - FinalCTASection.jsx has copper gradient background
  </acceptance_criteria>
  <done>Home page sections follow the dark flow: dark hero, charcoal HIW, stone Features, dark Social Proof, copper CTA. Skeletons match. Mobile fallbacks in place.</done>
</task>

<task type="auto">
  <name>Task 2: Pricing page dark reskin — tier cards, comparison table, FAQ, CTA</name>
  <files>src/app/(public)/pricing/page.js, src/app/(public)/pricing/PricingTiers.jsx, src/app/(public)/pricing/ComparisonTable.jsx, src/app/(public)/pricing/FAQSection.jsx</files>
  <read_first>
    - src/app/(public)/pricing/page.js (full file — section wrapper backgrounds, component composition)
    - src/app/(public)/pricing/PricingTiers.jsx (full file — card structure, isHighlighted logic, toggle state)
    - src/app/(public)/pricing/ComparisonTable.jsx (full file — table structure, row styling)
    - src/app/(public)/pricing/FAQSection.jsx (full file — Radix Accordion structure, chevron)
    - src/app/(public)/pricing/pricingData.js (data structure — tier names, features, prices)
    - .planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md (Pricing Page contract)
  </read_first>
  <action>
**pricing/page.js — Section backgrounds per D-13, D-14, D-15:**
- Hero/headline section: `bg-[#0F172A]` with `text-[#F1F5F9]` headings and `text-[#94A3B8]` body text
- Tier cards section: `bg-[#0F172A]` (dark, cards float on dark bg)
- Comparison table section: `bg-[#F5F5F4]` (stone — this page's breath section). Text within: dark colors (`text-[#0F172A]` headings, `text-[#475569]` body)
- FAQ section: `bg-[#0F172A]` dark
- CTA banner section at bottom: `bg-gradient-to-r from-[#C2410C] to-[#9A3412]` copper gradient. White text. Dark CTA button (`bg-[#0F172A] text-white`).
- Eyebrow text above hero headline: `text-[#C2410C] text-sm font-semibold uppercase tracking-[0.15em]`

**PricingTiers.jsx — Dark tier cards per D-05, D-06, D-13:**
- Each tier card: replace any `bg-white` or light card bg with:
  ```
  bg-[#1E293B] border border-white/[0.06] rounded-xl
  transition-all duration-200
  hover:border-[#C2410C]/40
  hover:shadow-[0_0_20px_rgba(194,65,12,0.15)]
  hover:-translate-y-0.5
  ```
- "Most Popular" badge (isHighlighted tier): `bg-[#C2410C] text-white text-xs font-semibold px-3 py-1 rounded-full`
- Highlighted tier ring: `ring-2 ring-[#C2410C]/60` (adds copper ring to the most popular card)
- Monthly/Annual toggle: Active pill `bg-[#C2410C] text-white`, inactive `text-white/50 bg-transparent`. Toggle container: `bg-[#0F172A] rounded-full p-1 border border-white/[0.06]`
- Tier card text: tier name `text-[#F1F5F9] font-semibold`, price `text-[#F1F5F9]`, description `text-[#94A3B8]`, feature list items `text-[#94A3B8]`
- Feature checkmarks: `text-[#C2410C]` copper (not green)
- CTA button per tier: `bg-[#C2410C] text-white hover:bg-[#C2410C]/90` for Most Popular; `bg-white/[0.06] text-[#F1F5F9] hover:bg-white/[0.1]` for others

**ComparisonTable.jsx — Stone section per D-14:**
- Table wrapper: no explicit bg needed (inherits from stone section in page.js)
- Header row: `text-[#0F172A] font-semibold`
- Data rows: alternating `bg-white` and `bg-stone-50` row tints for scanning
- Cell text: `text-[#475569]` (landing-muted on light)
- Check/cross icons: check = `text-[#C2410C]`, cross = `text-[#94A3B8]`

**FAQSection.jsx — Dark reskin per D-15:**
- Section bg inherited from page.js (`bg-[#0F172A]`)
- Accordion item border: `border-b border-white/[0.08]`
- Trigger text: `text-[#F1F5F9] font-semibold` with `min-h-[44px]` touch target
- ChevronDown icon: `text-[#C2410C]` (copper accent on expand icon per D-15)
- Content text: `text-[#94A3B8]`
- Preserve existing Radix Accordion structure and animation keyframes (200ms ease-out accordion-down/up already defined in globals.css)
  </action>
  <verify>
    <automated>grep -n "bg-\[#1E293B\]" src/app/\(public\)/pricing/PricingTiers.jsx && grep -n "hover:border-\[#C2410C\]" src/app/\(public\)/pricing/PricingTiers.jsx && grep -n "text-\[#C2410C\]" src/app/\(public\)/pricing/FAQSection.jsx && grep -n "bg-\[#F5F5F4\]" src/app/\(public\)/pricing/page.js</automated>
  </verify>
  <acceptance_criteria>
    - pricing/page.js hero section has `bg-[#0F172A]`
    - pricing/page.js comparison table section has `bg-[#F5F5F4]`
    - pricing/page.js FAQ section has `bg-[#0F172A]`
    - pricing/page.js CTA banner has `bg-gradient-to-r from-[#C2410C] to-[#9A3412]`
    - PricingTiers.jsx cards have `bg-[#1E293B] border border-white/[0.06]`
    - PricingTiers.jsx cards have `hover:border-[#C2410C]/40` and `hover:shadow-[0_0_20px_rgba(194,65,12,0.15)]`
    - PricingTiers.jsx toggle active state uses `bg-[#C2410C]`
    - PricingTiers.jsx Most Popular badge uses `bg-[#C2410C]`
    - FAQSection.jsx trigger text is `text-[#F1F5F9]`
    - FAQSection.jsx ChevronDown has `text-[#C2410C]`
    - FAQSection.jsx content text is `text-[#94A3B8]`
    - ComparisonTable.jsx has alternating rows (bg-white and bg-stone-50)
  </acceptance_criteria>
  <done>Pricing page fully dark: dark hero, dark tier cards with copper hover, stone comparison table, dark FAQ with copper chevron, copper CTA banner.</done>
</task>

<task type="auto">
  <name>Task 3: About page + Contact page dark reskin</name>
  <files>src/app/(public)/about/page.js, src/app/(public)/contact/page.js, src/app/(public)/contact/ContactForm.jsx</files>
  <read_first>
    - src/app/(public)/about/page.js (full file — section structure, value cards)
    - src/app/(public)/contact/page.js (full file — section structure, form placement)
    - src/app/(public)/contact/ContactForm.jsx (full file — form inputs, submit button, validation, honeypot)
    - .planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md (About Page and Contact Page contracts)
  </read_first>
  <action>
**About page per D-16:**

The about page should have 3 sections:

1. **Dark hero section:** `bg-[#0F172A]` — mission headline in `text-[#F1F5F9]`, body in `text-[#94A3B8]`, eyebrow in `text-[#C2410C] text-sm font-semibold uppercase tracking-[0.15em]`

2. **Stone values/principles section:** `bg-[#F5F5F4]` (this page's breath section) — value cards use light card style: `bg-white border border-stone-200/60 rounded-xl p-6`. Headings `text-[#0F172A]`, body `text-[#475569]`. Icons on value cards: `text-[#C2410C]` copper accent.

3. **Dark closing CTA section:** `bg-[#0F172A]` — headline `text-[#F1F5F9]`, body `text-[#94A3B8]`, CTA button `bg-[#C2410C] text-white hover:bg-[#C2410C]/90`.

Update any existing text colors that use dark-on-light pattern to the appropriate light-on-dark or dark-on-stone pattern depending on section.

**Contact page per D-17:**

The contact page has 2 visual sections:

1. **Dark hero:** `bg-[#0F172A]` — headline `text-[#F1F5F9]`, subtitle `text-[#94A3B8]`

2. **Stone section with dark form card:** The main content area should be `bg-[#F5F5F4]` (stone — this page's breath section per UI-SPEC). Inside this stone section, the form sits in a dark card: `bg-[#1E293B] rounded-2xl p-8 md:p-10`.

**ContactForm.jsx:**

- Form card wrapper (if ContactForm renders its own card): `bg-[#1E293B] rounded-2xl`
- Input fields: `bg-[#0F172A] border border-white/[0.12] text-[#F1F5F9] placeholder:text-[#94A3B8]`
- Input focus: `focus:border-[#C2410C] focus:ring-1 focus:ring-[#C2410C]/20` — copper glow focus ring. Remove any existing focus:ring-offset.
- Alternatively express focus glow as: `focus:outline-none focus:border-[#C2410C]` + inline style or arbitrary value for box-shadow: `focus:shadow-[0_0_0_3px_rgba(194,65,12,0.2)]`
- Label text: `text-[#F1F5F9] text-sm font-medium`
- Select/dropdown (inquiry type): same dark treatment as inputs
- Submit button: `bg-[#C2410C] text-white hover:bg-[#C2410C]/90 min-h-[44px]` full-width copper
- Response time SLA text: `text-[#94A3B8] text-sm` — "We respond within 1 business day."
- Error messages: keep existing `text-destructive` pattern (already oklch red)
- Honeypot field: keep hidden, no visual change needed
- Preserve all existing form validation logic, honeypot, loading states, success/error handling
  </action>
  <verify>
    <automated>grep -n "bg-\[#0F172A\]" src/app/\(public\)/about/page.js && grep -n "bg-\[#F5F5F4\]" src/app/\(public\)/about/page.js && grep -n "bg-\[#F5F5F4\]" src/app/\(public\)/contact/page.js && grep -n "border-\[#C2410C\]" src/app/\(public\)/contact/ContactForm.jsx</automated>
  </verify>
  <acceptance_criteria>
    - about/page.js has dark hero section with `bg-[#0F172A]`
    - about/page.js has stone values section with `bg-[#F5F5F4]`
    - about/page.js has dark closing CTA section with `bg-[#0F172A]`
    - about/page.js value card icons use `text-[#C2410C]`
    - contact/page.js has dark hero with `bg-[#0F172A]`
    - contact/page.js has stone section with `bg-[#F5F5F4]` containing dark form card
    - ContactForm.jsx inputs have `bg-[#0F172A] border border-white/[0.12]`
    - ContactForm.jsx inputs have copper focus: `focus:border-[#C2410C]`
    - ContactForm.jsx submit button uses `bg-[#C2410C]`
    - ContactForm.jsx labels use `text-[#F1F5F9]`
    - ContactForm.jsx preserves honeypot field, all validation logic, loading states
    - Response time SLA text visible as `text-[#94A3B8]`
  </acceptance_criteria>
  <done>About page: dark hero, stone values, dark CTA. Contact page: dark hero, stone section with dark form card, copper focus inputs, copper submit button.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Home page section flow: verify bg classes in sequence (#0F172A, #1E293B, #F5F5F4, #0F172A, #C2410C)
3. Pricing page: dark cards with copper hover, stone comparison table, dark FAQ
4. About page: 3 sections with correct backgrounds
5. Contact page: dark hero, stone form area, copper focus states
6. All pages have exactly one stone (#F5F5F4) breath section
</verification>

<success_criteria>
- Every public page follows the dark-first palette: 70% dark surfaces, one stone breath section per page
- All cards on dark backgrounds have copper hover glow (border + shadow + translateY)
- HowItWorks skeleton matches section background (no CLS flash)
- Mobile fallbacks in place (single-column bento, stacked steps for HIW)
- All existing page content, links, and functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-public-pages-redesign/13-02-SUMMARY.md`
</output>
