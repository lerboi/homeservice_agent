---
phase: 13-frontend-public-pages-redesign
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/app/auth/signin/page.js
  - src/components/onboarding/OtpInput.js
autonomous: true
requirements: [D-30, D-31, D-32, D-33, D-34, D-35, D-36, D-22]

must_haves:
  truths:
    - "Signup view shows a split layout: left form card + right brand panel on lg+ screens"
    - "Signin view shows a compact centered card with no selling points"
    - "OTP verification view shows a centered dark card with copper accents"
    - "Toggling between signup and signin uses text links, not a tab switcher"
    - "All existing auth functionality works: Google OAuth, email+password, OTP, cooldown timer"
    - "Mobile auth shows form only (no right brand panel)"
  artifacts:
    - path: "src/app/auth/signin/page.js"
      provides: "Differentiated signup/signin/OTP layouts"
      contains: "mode === 'signin'"
    - path: "src/components/onboarding/OtpInput.js"
      provides: "Dark-restyled OTP input boxes"
      contains: "bg-[#0F172A]"
  key_links:
    - from: "src/app/auth/signin/page.js"
      to: "src/components/onboarding/OtpInput.js"
      via: "import { OtpInput }"
      pattern: "OtpInput"
    - from: "src/app/auth/signin/page.js"
      to: "src/lib/supabase-browser"
      via: "supabase auth methods"
      pattern: "supabase.auth"
---

<objective>
Redesign the auth page with differentiated layouts for signup (split), signin (compact centered), and OTP verification (centered dark card) — preserving all existing auth functionality while applying the Premium Dark SaaS palette.

Purpose: Signup should feel like an invitation ("welcome, here's why") with brand storytelling. Signin should feel like a quick doorway ("welcome back, get in"). Currently both are identical tab-toggled views.

Output: Fully restyled auth/signin/page.js with three conditional layout branches + dark-styled OtpInput.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-frontend-public-pages-redesign/13-CONTEXT.md
@.planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md
@.planning/phases/13-frontend-public-pages-redesign/13-RESEARCH.md

@.planning/phases/13-frontend-public-pages-redesign/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle OtpInput to dark palette</name>
  <files>src/components/onboarding/OtpInput.js</files>
  <read_first>
    - src/components/onboarding/OtpInput.js (full file — current styling, focus ring, bg colors, paste/backspace handlers)
    - .planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md (Auth Page — OTP Verification section)
    - .planning/phases/13-frontend-public-pages-redesign/13-RESEARCH.md (Pitfall 7: OtpInput focus ring)
  </read_first>
  <action>
**OtpInput dark reskin per D-35 and UI-SPEC Auth OTP contract:**

Update each digit input box styling:
- Background: Change from `bg-white` to `bg-[#0F172A]`
- Text color: `text-[#F1F5F9]` (light text on dark)
- Border: `border border-white/[0.12]` at rest
- Focus state: Remove any existing `focus:ring-offset-*` classes. Replace with `focus:outline-none focus:border-[#C2410C] focus:ring-1 focus:ring-[#C2410C]/30` (copper focus border + subtle ring, no ring offset).
- Font: keep existing size and weight
- Rounded corners: keep existing `rounded-lg` or similar
- Size: keep existing dimensions for the digit boxes

**Pitfall 7 avoidance:** The `focus:ring-offset-1` uses `--tw-ring-offset-color` which defaults to white — this creates a white ring gap on dark backgrounds. By removing ring-offset entirely and using direct `focus:border-[#C2410C]` + `focus:ring-1 focus:ring-[#C2410C]/30`, the focus indicator is copper-colored with no white gap.

**Preserve ALL existing behavior:** Focus management between digits, paste handling (multi-digit paste), backspace navigation to previous digit, onChange callback, digit count. Only visual classes change.
  </action>
  <verify>
    <automated>grep -n "bg-\[#0F172A\]" src/components/onboarding/OtpInput.js && grep -n "border-\[#C2410C\]" src/components/onboarding/OtpInput.js && grep -n "text-\[#F1F5F9\]" src/components/onboarding/OtpInput.js</automated>
  </verify>
  <acceptance_criteria>
    - OtpInput.js digit boxes have `bg-[#0F172A]` (not bg-white)
    - OtpInput.js digit boxes have `text-[#F1F5F9]`
    - OtpInput.js digit boxes have `border border-white/[0.12]` at rest
    - OtpInput.js focus state contains `focus:border-[#C2410C]`
    - OtpInput.js does NOT contain `focus:ring-offset` (removed)
    - OtpInput.js preserves useRef, onChange, paste handler, backspace handler, focus navigation
  </acceptance_criteria>
  <done>OTP digit boxes are dark with copper focus border. All input behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Auth page — differentiated signup, signin, and OTP layouts</name>
  <files>src/app/auth/signin/page.js</files>
  <read_first>
    - src/app/auth/signin/page.js (FULL file — all 478 lines: mode state, handleGoogleOAuth, handleEmailAuth, handleSignin, handleResendOtp, handleVerifyOtp, switchMode, cooldown, SELLING_POINTS, GoogleIcon, entire JSX render)
    - src/components/onboarding/OtpInput.js (updated in Task 1 — dark styling now applied)
    - .planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md (Auth Page — Signup View, Signin View, OTP Verification sections)
    - .planning/phases/13-frontend-public-pages-redesign/13-RESEARCH.md (Pattern 5: Auth Page Mode Switch, Pitfall 4: Auth Page State Sharing)
  </read_first>
  <action>
**CRITICAL: Read the ENTIRE existing auth/signin/page.js first. Understand all state, handlers, and JSX before making changes. The auth logic is complex (478 lines) and MUST be preserved exactly per D-36.**

**Structural change — conditional layout branches per D-30:**

Replace the existing single-layout render with three conditional branches based on `mode` state:

```jsx
if (mode === 'otp') return <OtpView />;
if (mode === 'signin') return <SigninView />;
return <SignupView />; // default
```

Each "view" can be a local function component within the file or inline JSX. Share all state (email, password, error, loading, verifying, cooldown, mode setter, handlers) via props or by keeping them in the parent scope. Do NOT split into separate files — the route stays `/auth/signin`.

**Signup view (D-31) — split layout:**
```
Page bg: bg-[#0F172A]
Outer container: min-h-screen flex items-center justify-center px-4 py-12
Card container: w-full max-w-[960px] rounded-2xl overflow-hidden flex flex-col lg:flex-row

Left panel (form): flex-1 bg-[#334155] p-8 sm:p-10 lg:p-12
  - Heading: "Create your account" in text-[#F1F5F9] text-2xl font-semibold
  - Subtitle: "Start your 5-minute setup" in text-[#94A3B8]
  - Google OAuth button: full-width, outlined border border-white/[0.12], bg-transparent, text-[#F1F5F9], GoogleIcon on left, hover:bg-white/[0.04]. Text: "Continue with Google"
  - Divider: "or" in text-[#94A3B8] with lines border-t border-white/[0.08]
  - Email input: bg-[#1E293B] border border-white/[0.12] text-[#F1F5F9] placeholder:text-[#94A3B8]
  - Password input: same styling
  - Focus on all inputs: focus:border-[#C2410C] focus:outline-none focus:shadow-[0_0_0_3px_rgba(194,65,12,0.2)]
  - Labels: text-[#F1F5F9] text-sm font-medium
  - Submit button: bg-[#C2410C] text-white hover:bg-[#C2410C]/90 w-full min-h-[44px] rounded-lg font-medium. Text: "Create account"
  - Error alert: keep existing Alert component, ensure it's visible on dark bg
  - Toggle link at bottom: "Already have an account? " + <button onClick={() => switchMode('signin')} className="text-[#C2410C] font-semibold hover:underline">Sign in</button>

Right panel (brand): hidden lg:flex lg:w-[380px] xl:w-[420px] bg-[#0F172A] flex-col justify-center p-8 xl:p-10
  - Copper radial glow blobs: 2-3 absolutely positioned divs with bg-[#C2410C]/10, rounded-full, blur-3xl (decorative, subtle)
  - Reuse existing SELLING_POINTS array: render each as a chip/badge with icon
    - Each chip: flex items-center gap-3, icon in text-[#C2410C], text in text-[#F1F5F9] text-sm
  - Social proof strip: "Trusted by 500+ home service businesses" or similar in text-[#94A3B8] text-sm at bottom
  - Brand accent: a subtle copper line or decorative element

Mobile (below lg): Right panel hidden. Left panel takes full width. Form is single-column, max-w-[480px] centered.

**Signin view (D-32) — compact centered card:**
```
Page bg: bg-[#0F172A]
Outer: min-h-screen flex items-center justify-center px-4 py-12
Card: w-full max-w-[400px] bg-[#1E293B] rounded-2xl p-8 sm:p-10

Content (minimal):
  - Logo: Voco logo (reuse the SVG from LandingFooter or inline — small copper gradient icon + "Voco" text), centered
  - Heading: "Welcome back" in text-[#F1F5F9] text-2xl font-semibold text-center
  - Google OAuth button: same style as signup
  - Divider: "or" same as signup
  - Email input: bg-[#0F172A] border border-white/[0.12] text-[#F1F5F9] (slightly darker than card bg)
  - Password input: same
  - Focus: copper focus glow same as signup
  - Labels: text-[#F1F5F9] text-sm
  - Submit button: bg-[#C2410C] text-white min-h-[44px] w-full. Text: "Sign in"
  - Error alert: same as signup
  - Toggle link: "Don't have an account? " + <button onClick={() => switchMode('signup')} className="text-[#C2410C] font-semibold hover:underline">Get started</button>

NO selling points, NO brand panel, NO split layout. Just: logo, heading, form, submit, toggle.
```

**OTP view (D-35) — centered card:**
```
Page bg: bg-[#0F172A]
Outer: min-h-screen flex items-center justify-center px-4 py-12
Card: w-full max-w-[400px] bg-[#1E293B] rounded-2xl p-8 sm:p-10 text-center

Content:
  - Copper email icon: Mail icon from lucide-react in text-[#C2410C] size-8, centered
  - Heading: "Check your email" in text-[#F1F5F9] text-2xl font-semibold
  - Body: "We sent a 6-digit code to {email}" in text-[#94A3B8] text-sm
  - OtpInput component: rendered centered (now dark-styled from Task 1)
  - Verify button: bg-[#C2410C] text-white min-h-[44px] w-full
  - Resend link: "Didn't receive it? " + resend button in text-[#C2410C] (with cooldown timer if active)
  - Back link: "Back to {signup|signin}" in text-[#94A3B8] hover:text-[#F1F5F9]
  - Error display: same Alert pattern
```

**State management (Pitfall 4 from RESEARCH.md):**
- Keep all existing state variables: mode, email, password, error, loading, verifying, cooldown
- Keep all existing handlers: handleGoogleOAuth, handleEmailAuth, handleSignin, handleResendOtp, handleVerifyOtp
- Keep existing `switchMode` function — it already clears error on mode switch. Email persists across mode switch (good UX), password does NOT need clearing (existing behavior is fine)
- Keep SELLING_POINTS constant and GoogleIcon component exactly as-is

**Mode toggle (D-33):**
- Remove any existing pill/tab toggle UI
- Replace with text links at the bottom of each view:
  - Signup shows: "Already have an account? Sign in" (Sign in is clickable, calls switchMode('signin'))
  - Signin shows: "Don't have an account? Get started" (Get started is clickable, calls switchMode('signup'))
- No animation between mode switches per D-22 — instant layout swap

**Auth mode toggle transition (D-22):** No animation. Instant layout swap. Do NOT add AnimatePresence or Framer Motion transitions between signup/signin/otp views.

**Preserve ALL existing auth functionality per D-36:**
- Google OAuth flow (signInWithOAuth redirect to /auth/callback)
- Email+password signup (signUp with redirectTo)
- Email+password signin (signInWithPassword)
- OTP verification (verifyOtp)
- OTP resend with cooldown timer
- Error handling and display
- Loading states (Loader2 spinner on buttons)
- All form validation
  </action>
  <verify>
    <automated>grep -n "mode === 'signin'" src/app/auth/signin/page.js && grep -n "mode === 'otp'" src/app/auth/signin/page.js && grep -n "bg-\[#334155\]" src/app/auth/signin/page.js && grep -n "max-w-\[400px\]" src/app/auth/signin/page.js && grep -n "Already have an account" src/app/auth/signin/page.js && grep -n "handleGoogleOAuth" src/app/auth/signin/page.js && grep -n "handleVerifyOtp" src/app/auth/signin/page.js</automated>
  </verify>
  <acceptance_criteria>
    - auth/signin/page.js has three conditional render branches: mode === 'otp', mode === 'signin', default signup
    - Signup view: contains `bg-[#334155]` left panel and `lg:w-[380px]` right panel
    - Signup view: right panel has `hidden lg:flex` (hidden on mobile)
    - Signin view: contains `max-w-[400px]` centered card with `bg-[#1E293B]`
    - Signin view: does NOT contain SELLING_POINTS rendering or brand panel
    - OTP view: contains `max-w-[400px]` centered card with OtpInput component
    - Page background on all views: `bg-[#0F172A]`
    - All inputs have `border-[#C2410C]` in focus state
    - Submit buttons use `bg-[#C2410C]`
    - Toggle links: "Already have an account? Sign in" and "Don't have an account? Get started"
    - No tab/pill toggle UI remains
    - All handlers preserved: handleGoogleOAuth, handleEmailAuth, handleSignin, handleResendOtp, handleVerifyOtp
    - supabase import preserved
    - OtpInput import preserved
    - SELLING_POINTS and GoogleIcon preserved (used in signup right panel)
    - No AnimatePresence or Framer Motion used for mode transitions
  </acceptance_criteria>
  <done>Auth page shows differentiated layouts: signup (split with brand panel), signin (compact centered), OTP (centered card). All auth functionality preserved. Text link toggles replace tab switcher.</done>
</task>

</tasks>

<verification>
1. `grep "handleGoogleOAuth\|handleEmailAuth\|handleSignin\|handleVerifyOtp\|handleResendOtp" src/app/auth/signin/page.js | wc -l` returns >= 5 (all handlers present)
2. `grep "supabase.auth" src/app/auth/signin/page.js | wc -l` returns >= 3 (auth calls preserved)
3. `grep "mode === " src/app/auth/signin/page.js` shows all three modes
4. `grep "bg-\[#0F172A\]" src/components/onboarding/OtpInput.js` returns match
5. `npm run build` completes without errors
</verification>

<success_criteria>
- Signup page: split layout with form on left, brand panel on right (hidden on mobile)
- Signin page: compact centered dark card, no brand panel
- OTP page: centered dark card with copper accents, dark digit boxes
- Mode toggle via text links (no tab/pill switcher)
- All auth flows work: Google OAuth, email+password, OTP with cooldown
- No visual regression on auth functionality
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-public-pages-redesign/13-03-SUMMARY.md`
</output>
