---
phase: 13-frontend-public-pages-redesign
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/auth/signin/page.js
  - src/components/onboarding/OtpInput.js
autonomous: true
gap_closure: true
requirements: [D-30, D-31, D-32, D-33, D-34, D-35, D-36]

must_haves:
  truths:
    - "Auth signup left panel is WHITE (#FFFFFF), not slate-700 (#334155)"
    - "Auth page overall background is lighter — not pure #0F172A darkness everywhere"
    - "The color transition between left white panel and right dark panel is clean, not 'horrendous'"
    - "Signup, signin, and OTP views remain three distinct layouts"
    - "All auth functionality works: Google OAuth, email/password, OTP, cooldown, error handling"
  artifacts:
    - path: "src/app/auth/signin/page.js"
      provides: "Auth page with WHITE left panel and clean dark/light contrast"
      contains: "bg-white"
    - path: "src/components/onboarding/OtpInput.js"
      provides: "OTP input with dark styling"
      contains: "bg-[#0F172A]"
  key_links:
    - from: "src/app/auth/signin/page.js"
      to: "@/lib/supabase-browser"
      via: "supabase auth methods"
      pattern: "supabase\\.auth"
    - from: "src/app/auth/signin/page.js"
      to: "@/components/onboarding/OtpInput"
      via: "OtpInput import"
      pattern: "import.*OtpInput"
---

<objective>
Complete redo of the auth page color scheme to fix the "horrendous" color blend. The user's specific feedback: if the right side is dark, the LEFT side should be WHITE (not #334155 slate-700 — that's still dark). The background should be lighter overall.

Purpose: The current implementation has a dark-on-dark split that creates no visual contrast. The user wants a clean light/dark split where the form panel is white and the brand panel is dark, creating a sharp, intentional contrast rather than a murky dark-on-slightly-less-dark blend.

Output: Auth page with white left panel, dark right panel, lighter page background, and clean visual separation.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-frontend-public-pages-redesign/13-CONTEXT.md
@.planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md
@.planning/phases/13-frontend-public-pages-redesign/13-03-SUMMARY.md
@.planning/phases/13-frontend-public-pages-redesign/13-04-SUMMARY.md

<interfaces>
From src/app/auth/signin/page.js (current state — Plan 03 output):
- 'use client' component with useState for mode ('signup'|'signin'|'otp'), email, password, error, loading, verifying, cooldown
- GoogleIcon SVG component (inline)
- SELLING_POINTS array: [{icon, text}] — 4 items
- VocoLogo local component with textColor prop
- Auth handlers: handleGoogleOAuth, handleEmailAuth, handleSignin, handleVerifyOtp, handleResendOtp, switchMode
- Three conditional render branches: mode==='otp' (OTP view), mode==='signin' (signin view), default (signup split view)

From src/components/onboarding/OtpInput.js:
- Named export OtpInput with onComplete and disabled props
- 6-digit input boxes with useRef array, handleChange, handleKeyDown, handlePaste
- Currently styled: bg-[#0F172A], border-white/[0.12], focus:border-[#C2410C], focus:ring-1 focus:ring-[#C2410C]/30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete redo of auth page — WHITE left panel, lighter background, clean contrast</name>
  <files>src/app/auth/signin/page.js</files>
  <read_first>
    - src/app/auth/signin/page.js
    - src/components/onboarding/OtpInput.js
  </read_first>
  <action>
**USER OVERRIDE of D-31:** The user explicitly said the left panel should be WHITE, not #334155. D-31 specified `bg-[#334155]` but user rejected this as "horrendous." Change to `bg-white`.

**SIGNUP VIEW (default) — complete color redo:**

1. Page background: change from `bg-[#0F172A]` to `bg-[#F1F5F9]` (light gray — the user said "background should be lighter"). This is the overall page bg visible around the card container.

2. Card container: add `shadow-2xl` to the `rounded-2xl overflow-hidden flex` div for a strong elevated card effect on the light bg.

3. **Left panel (form) — WHITE:**
   - Change from `bg-[#334155]` to `bg-white`
   - VocoLogo: pass `textColor="text-[#0F172A]"` so logo text is dark on white
   - Heading "Create your account": change from `text-[#F1F5F9]` to `text-[#0F172A]`
   - Subtext "Start your 5-minute setup": change from `text-[#94A3B8]` to `text-[#475569]`
   - Google OAuth button: change from `bg-transparent hover:bg-white/[0.04] text-[#F1F5F9] border border-white/[0.12]` to `bg-white hover:bg-stone-50 text-[#0F172A] border border-stone-200 shadow-sm`
   - Divider: change from `border-white/[0.08]` to `border-stone-200`, "or" text from `text-[#94A3B8]` to `text-[#475569]`
   - Labels: change from `text-[#F1F5F9]` to `text-[#0F172A]`
   - Inputs: change from `bg-[#1E293B] border border-white/[0.12] text-[#F1F5F9] placeholder:text-[#94A3B8]` to `bg-white border border-stone-300 text-[#0F172A] placeholder:text-stone-400`
   - Input focus: change from `focus:border-[#C2410C] focus:shadow-[0_0_0_3px_rgba(194,65,12,0.2)]` to `focus:border-[#C2410C] focus:ring-2 focus:ring-[#C2410C]/20 focus:outline-none`
   - Submit button: KEEP `bg-[#C2410C]` copper — this is the brand accent, works on white
   - Toggle link text: change from `text-[#94A3B8]` to `text-[#475569]`, keep `text-[#C2410C]` for the clickable part
   - Error Alert: keep `variant="destructive"` — works on both light and dark

4. **Right panel (brand) — KEEP DARK:**
   - Keep `bg-[#0F172A]` — this creates the intentional light/dark contrast
   - Keep all existing right panel content (copper glow blobs, SELLING_POINTS, social proof) — these are designed for dark bg
   - Keep all text colors on right panel (they're already correct for dark)

**SIGNIN VIEW — lighter treatment to match:**
1. Page background: change from `bg-[#0F172A]` to `bg-[#F1F5F9]` (matching signup page bg)
2. Card: change from `bg-[#1E293B]` to `bg-white` with `shadow-2xl`
3. Heading "Welcome back": from `text-[#F1F5F9]` to `text-[#0F172A]`
4. Google OAuth button: same light treatment as signup left panel
5. Divider: `border-stone-200`, text `text-[#475569]`
6. Labels: `text-[#0F172A]`
7. Inputs: `bg-white border border-stone-300 text-[#0F172A] placeholder:text-stone-400 focus:border-[#C2410C] focus:ring-2 focus:ring-[#C2410C]/20 focus:outline-none`
8. Submit button: KEEP copper `bg-[#C2410C]`
9. Toggle link: `text-[#475569]` with `text-[#C2410C]` for clickable
10. VocoLogo: `textColor="text-[#0F172A]"`

**OTP VIEW — keep dark (it's a verification step, dark feels intentional):**
1. Page background: change from `bg-[#0F172A]` to `bg-[#F1F5F9]` for consistency
2. Card: change from `bg-[#1E293B]` to `bg-white` with `shadow-2xl`
3. Heading "Check your email": from `text-[#F1F5F9]` to `text-[#0F172A]`
4. Body text: from `text-[#94A3B8]` to `text-[#475569]`
5. Email display span: from `text-[#F1F5F9]` to `text-[#0F172A] font-semibold`
6. Copper Mail icon: KEEP `text-[#C2410C]`
7. Resend text: from `text-[#94A3B8]` to `text-[#475569]`
8. Back link: from `text-[#94A3B8] hover:text-[#F1F5F9]` to `text-[#475569] hover:text-[#0F172A]`
9. Loader spinner: KEEP `text-[#C2410C]`

**CRITICAL — Preserve ALL auth functionality (D-36):**
- handleGoogleOAuth (redirectTo URL construction)
- handleEmailAuth (signUp + signInWithOtp)
- handleSignin (signInWithPassword)
- handleVerifyOtp (verifyOtp)
- handleResendOtp (signInWithOtp with shouldCreateUser: true)
- switchMode state management
- cooldown timer useEffect
- All error handling paths
- All loading/verifying/disabled states
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -c "bg-white" src/app/auth/signin/page.js && grep "bg-\[#334155\]" src/app/auth/signin/page.js | wc -l && grep -c "bg-\[#F1F5F9\]" src/app/auth/signin/page.js && grep -c "supabase.auth" src/app/auth/signin/page.js && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/app/auth/signin/page.js contains `bg-white` for signup left panel (grep returns >= 3 — signup panel, signin card, OTP card)
    - src/app/auth/signin/page.js does NOT contain `bg-[#334155]` anywhere (grep returns 0 lines)
    - src/app/auth/signin/page.js contains `bg-[#F1F5F9]` for page backgrounds (grep returns >= 3 — one per view)
    - src/app/auth/signin/page.js contains `shadow-2xl` on card containers
    - src/app/auth/signin/page.js signup left panel has `text-[#0F172A]` for heading
    - src/app/auth/signin/page.js signin card has `text-[#0F172A]` for heading
    - src/app/auth/signin/page.js right brand panel still has `bg-[#0F172A]` (dark panel preserved)
    - src/app/auth/signin/page.js contains `supabase.auth` calls (all 5 auth handlers preserved)
    - src/app/auth/signin/page.js contains `handleGoogleOAuth` function
    - src/app/auth/signin/page.js contains `handleEmailAuth` function
    - src/app/auth/signin/page.js contains `handleVerifyOtp` function
    - src/app/auth/signin/page.js contains `setCooldown` (cooldown timer preserved)
    - src/app/auth/signin/page.js input borders use `border-stone-300` (light treatment)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Auth page has white left panel, #F1F5F9 page background, dark right brand panel (preserved), clean light/dark contrast. All three views (signup, signin, OTP) use white cards on light bg. All auth functionality (Google OAuth, email/password, OTP, cooldown, error handling) preserved exactly. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update OtpInput styling for light card context</name>
  <files>src/components/onboarding/OtpInput.js</files>
  <read_first>
    - src/components/onboarding/OtpInput.js
  </read_first>
  <action>
Since the OTP card is now `bg-white` (not `bg-[#1E293B]`), the OtpInput digit boxes need to work on a WHITE background.

**OtpInput.js styling update:**
1. Input box background: change from `bg-[#0F172A]` to `bg-stone-50` (very light gray — subtle contrast on white card)
2. Input box text: change from `text-[#F1F5F9]` to `text-[#0F172A]` (dark text on light)
3. Input box border at rest: change from `border border-white/[0.12]` to `border border-stone-300`
4. Input box focus border: KEEP `focus:border-[#C2410C]` — copper focus works on both light and dark
5. Input box focus ring: change from `focus:ring-1 focus:ring-[#C2410C]/30` to `focus:ring-2 focus:ring-[#C2410C]/20` — slightly wider, softer glow on light

**Preserve ALL behavior:**
- useRef array for 6 digit inputs
- handleChange, handleKeyDown, handlePaste
- Focus navigation between inputs
- onComplete callback
- disabled prop handling
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -c "bg-stone-50" src/components/onboarding/OtpInput.js && grep -c "text-\[#0F172A\]" src/components/onboarding/OtpInput.js && grep -c "border-stone-300" src/components/onboarding/OtpInput.js && grep -c "focus:border-\[#C2410C\]" src/components/onboarding/OtpInput.js && grep -c "handlePaste" src/components/onboarding/OtpInput.js && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - OtpInput.js contains `bg-stone-50` for digit box background (not `bg-[#0F172A]`)
    - OtpInput.js contains `text-[#0F172A]` for digit text color
    - OtpInput.js contains `border-stone-300` for rest border
    - OtpInput.js contains `focus:border-[#C2410C]` for focus border (copper preserved)
    - OtpInput.js contains `handlePaste` function (behavior preserved)
    - OtpInput.js contains `handleKeyDown` function (behavior preserved)
    - OtpInput.js does NOT contain `bg-[#0F172A]` (old dark styling removed)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>OTP digit boxes styled for white card context: light bg, dark text, stone border, copper focus. All input behavior (paste, keyboard nav, focus management) preserved.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Auth signup has white left panel (not #334155)
3. Auth page backgrounds are #F1F5F9 (lighter, not #0F172A)
4. Auth right brand panel remains dark #0F172A for intentional contrast
5. OTP inputs work on white card background
6. All auth flows functional: Google OAuth, email/password signup, signin, OTP verify, resend with cooldown
</verification>

<success_criteria>
- Auth page color scheme is clean light/dark contrast, not "horrendous" dark-on-dark
- White left panel + dark right panel creates intentional, premium split
- All three auth views (signup, signin, OTP) use consistent lighter treatment
- Every auth handler and error path preserved (D-36)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-public-pages-redesign/13-06-SUMMARY.md`
</output>
