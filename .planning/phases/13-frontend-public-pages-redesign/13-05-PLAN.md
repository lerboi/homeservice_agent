---
phase: 13-frontend-public-pages-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/page.js
  - src/app/components/landing/HowItWorksSection.jsx
  - src/app/components/landing/FeaturesGrid.jsx
  - src/app/components/landing/LandingFooter.jsx
  - src/app/components/landing/SocialProofSection.jsx
autonomous: true
gap_closure: true
requirements: [D-03, D-04, D-05, D-06, D-10, D-11, D-12, D-18, D-19, D-20, D-23, D-25]

must_haves:
  truths:
    - "How It Works section has a LIGHT (#F5F5F4) background, not dark charcoal"
    - "No large vertical gap between CTA button and third step in How It Works"
    - "Features section looks dramatically different with dark card treatment, copper hover glows, and refined typography"
    - "Footer has visually distinct copper gradient border, larger logo, better spacing, and a newsletter section — but retains 3-column link grid per D-10"
    - "Page feels cohesive with clear visual rhythm: dark-light-dark-light-copper"
  artifacts:
    - path: "src/app/components/landing/HowItWorksSection.jsx"
      provides: "Light background How It Works with tightened layout"
      contains: "bg-[#F5F5F4]"
    - path: "src/app/components/landing/FeaturesGrid.jsx"
      provides: "Dramatically redesigned features with dark cards on dark bg"
      contains: "bg-[#0F172A]"
    - path: "src/app/components/landing/LandingFooter.jsx"
      provides: "Visually upgraded footer with newsletter CTA and social links in bottom bar"
      contains: "newsletter"
    - path: "src/app/(public)/page.js"
      provides: "Updated loading skeletons matching new section backgrounds"
      contains: "bg-[#F5F5F4]"
  key_links:
    - from: "src/app/(public)/page.js"
      to: "HowItWorksSection"
      via: "dynamic import skeleton bg must match component bg"
      pattern: "bg-\\[#F5F5F4\\].*py-20"
---

<objective>
Dramatically transform the Home page sections and Footer to address the user's rejection feedback: "very little actually changed visually."

Purpose: The prior Plans 01-03 only swapped background colors and added hover effects. The user wants a DRAMATIC visual transformation — structural changes to components, not just color value changes. This plan rebuilds HowItWorks (light bg per user feedback, tightened layout), Features (dark bg with completely reworked card system), and Footer (upgraded with newsletter section, social links, and stronger visual hierarchy).

Output: 4 rebuilt components + updated page.js skeletons that deliver an unmistakably different visual experience.
</objective>

<execution_context>
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leheh/.Projects/homeservice_agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-public-pages-redesign/13-CONTEXT.md
@.planning/phases/13-frontend-public-pages-redesign/13-UI-SPEC.md
@.planning/phases/13-frontend-public-pages-redesign/13-02-SUMMARY.md
@.planning/phases/13-frontend-public-pages-redesign/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild HowItWorksSection with LIGHT background + tighten layout, and rebuild FeaturesGrid with dark dramatic treatment</name>
  <files>src/app/components/landing/HowItWorksSection.jsx, src/app/components/landing/FeaturesGrid.jsx, src/app/(public)/page.js</files>
  <read_first>
    - src/app/components/landing/HowItWorksSection.jsx
    - src/app/components/landing/FeaturesGrid.jsx
    - src/app/(public)/page.js
    - src/app/components/landing/AnimatedSection.jsx
  </read_first>
  <action>
**USER OVERRIDE of D-18:** The user explicitly said How It Works should be a LIGHT background, not dark charcoal. Override D-18's `#1E293B` with `#F5F5F4` (stone). This changes the section flow to: Dark hero -> LIGHT HIW (#F5F5F4) -> DARK Features (#0F172A) -> Dark Social Proof (#0F172A) -> Copper CTA.

**HowItWorksSection.jsx — LIGHT background rebuild:**
1. Change section background from `bg-[#1E293B]` to `bg-[#F5F5F4]`
2. Remove the subtle grid texture overlay (it was for dark bg, not needed on light)
3. Change heading text from `text-[#F1F5F9]` to `text-[#0F172A]` (dark on light)
4. Change subheading from `text-[#94A3B8]` to `text-[#475569]` (muted on light)
5. Keep eyebrow `text-[#C2410C]` (copper works on both light and dark)
6. Mobile step cards: change from `bg-[#0F172A]` to `bg-white` with `border border-stone-200/60` and `shadow-sm`. Step card heading to `text-[#0F172A]`, description to `text-[#475569]`, detail to `text-[#475569]/60`
7. Desktop HowItWorksSticky loading skeleton: change from `bg-white/[0.03] border border-white/[0.06]` to `bg-white border border-stone-200/60 shadow-sm`
8. **FIX THE GAP**: Move the CTA button (`AnimatedSection delay={0.3}` with "Start My 5-Minute Setup") from `mt-16` to `mt-8` to close the huge gap between the third step and the CTA
9. CTA button: keep `bg-[#C2410C]` copper — it works on light background

**FeaturesGrid.jsx — DRAMATIC dark treatment rebuild:**
The user said this section "looks virtually unchanged." It currently has a stone `#F5F5F4` bg with white cards. Since HIW is now light, Features becomes the DARK section.

1. Change section background from `bg-[#F5F5F4]` to `bg-[#0F172A]`
2. Change heading from `text-[#0F172A]` to `text-[#F1F5F9]`
3. Change subheading span from `text-[#475569]` to `text-[#94A3B8]`
4. Change eyebrow from `text-[#C2410C]` (keep — works on dark)

5. **BentoCard complete rework for ALL cards (not just hero):**
   - ALL cards (hero, default, wide) get dark treatment: `bg-[#1E293B]` background
   - ALL cards get copper hover glow: `border border-white/[0.06] hover:border-[#C2410C]/40 hover:shadow-[0_0_20px_rgba(194,65,12,0.15)] hover:-translate-y-0.5 transition-all duration-300`
   - Hero card: `bg-[#1E293B]` (was `bg-[#0F172A]`, now same as other cards for cohesion on `#0F172A` bg) — OR use a lighter shade. Actually: hero card should be `bg-[#1E293B]` with a subtle radial copper gradient overlay to stand out. Keep the existing `radial-gradient(circle,rgba(194,65,12,0.12),transparent_70%)` but make it larger.
   - Default/wide cards: `bg-[#1E293B]` with `border border-white/[0.06]`
   - Text for non-hero cards: title `text-[#F1F5F9]`, body `text-[#94A3B8]` (was `text-[#475569]` — totally invisible on dark)
   - Icon containers for non-hero: `bg-white/[0.06] border border-white/[0.08]` (was `bg-[#0F172A]/[0.04]` — invisible on dark)
   - Icon color for non-hero: `text-[#C2410C]` (was `text-[#0F172A]/70` — invisible on dark)
   - Justification dot: keep `bg-[#166534]`, text `text-[#22C55E]` (green on dark is more visible than `text-[#166534]`)
   - Justification border: `border-t border-white/[0.06]` for ALL cards (was `border-black/[0.04]` for non-hero — invisible on dark)
   - Wide card MetricBar container: keep `bg-[#0F172A]` — it creates nice depth on `#1E293B` card
   - Remove the hover glow overlay divs (`.absolute.inset-0.opacity-0.group-hover:opacity-100`) — the border+shadow hover from D-05/D-06 is cleaner

6. Add a subtle grid texture to the section (like SocialProofSection has): `bg-[linear-gradient(rgba(255,255,255,0.015)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.015)_1px,transparent_1px)] bg-[size:48px_48px]` as an absolute overlay div

**page.js skeleton updates:**
1. HowItWorksSection skeleton: change `bg-[#1E293B]` to `bg-[#F5F5F4]`, shimmer placeholders from `bg-white/10` to `bg-black/10`, inner cards from `bg-white/[0.03] border border-white/[0.06]` to `bg-white border border-stone-200/60`
2. FeaturesGrid skeleton: change `bg-[#F5F5F4]` to `bg-[#0F172A]`, placeholders from `bg-black/10` to `bg-white/10`, card placeholders from `bg-white` to `bg-white/[0.03] border border-white/[0.06]`
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -c "bg-\[#F5F5F4\]" src/app/components/landing/HowItWorksSection.jsx && grep -c "bg-\[#0F172A\]" src/app/components/landing/FeaturesGrid.jsx && grep -c "bg-\[#1E293B\]" src/app/components/landing/FeaturesGrid.jsx && grep "mt-8" src/app/components/landing/HowItWorksSection.jsx | head -1 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - HowItWorksSection.jsx contains `bg-[#F5F5F4]` as section background (grep returns >= 1)
    - HowItWorksSection.jsx does NOT contain `bg-[#1E293B]` as section background
    - HowItWorksSection.jsx mobile step cards contain `bg-white` (not `bg-[#0F172A]`)
    - HowItWorksSection.jsx CTA wrapper has `mt-8` (not `mt-16`)
    - HowItWorksSection.jsx headings use `text-[#0F172A]` (dark text on light bg)
    - FeaturesGrid.jsx section background is `bg-[#0F172A]`
    - FeaturesGrid.jsx ALL BentoCard variants use `bg-[#1E293B]`
    - FeaturesGrid.jsx non-hero card text uses `text-[#F1F5F9]` for titles
    - FeaturesGrid.jsx non-hero card body uses `text-[#94A3B8]`
    - FeaturesGrid.jsx non-hero icon uses `text-[#C2410C]`
    - FeaturesGrid.jsx ALL cards have `hover:border-[#C2410C]/40`
    - page.js HowItWorksSection skeleton has `bg-[#F5F5F4]`
    - page.js FeaturesGrid skeleton has `bg-[#0F172A]`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>HowItWorks is on light #F5F5F4 background with dark text and white mobile cards. Features is on dark #0F172A with ALL cards in #1E293B with copper hover glow. CTA gap reduced from mt-16 to mt-8. Page.js skeletons match. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Dramatically upgrade LandingFooter with newsletter section, social links in bottom bar, and visual hierarchy (per D-10: 3-column grid preserved)</name>
  <files>src/app/components/landing/LandingFooter.jsx</files>
  <read_first>
    - src/app/components/landing/LandingFooter.jsx
    - src/app/components/landing/LandingNav.jsx
  </read_first>
  <action>
The user said the Footer "looks virtually unchanged." The current footer is minimal: copper gradient top border, 3-column links, copyright. It needs visual additions and better hierarchy, but per D-10 the 3-column structure (Product | Company | Legal) MUST be preserved — no 4th column.

**Complete LandingFooter.jsx rebuild with these additions:**

1. **Newsletter CTA section at top of footer** — Add a prominent section above the columns:
   - Full-width container within footer with `border-b border-white/[0.06]` separator below it
   - Left side: heading "Stay in the loop" in `text-xl font-semibold text-[#F1F5F9]` + subtext "Product updates and tips for growing your business." in `text-sm text-[#94A3B8]`
   - Right side: inline email input + subscribe button. Input: `bg-[#1E293B] border border-white/[0.12] text-[#F1F5F9] placeholder:text-[#94A3B8] rounded-l-xl h-11 px-4 text-sm focus:border-[#C2410C] focus:outline-none focus:shadow-[0_0_0_3px_rgba(194,65,12,0.2)]`. Button: `bg-[#C2410C] hover:bg-[#C2410C]/90 text-white font-semibold px-5 h-11 rounded-r-xl text-sm transition-colors`
   - The newsletter form is display-only (no API wired) — just visual. The input and button are purely presentational for now.
   - Layout: `flex flex-col md:flex-row md:items-center md:justify-between gap-6 pb-10 mb-10`

2. **Keep 3-column grid per D-10** — NO 4th column. Preserve existing Product | Company | Legal structure:
   - Column headings: upgrade from `text-xs font-semibold text-white/30` to `text-sm font-semibold text-[#F1F5F9]` for better visibility
   - Grid stays: `grid-cols-2 md:grid-cols-3` (unchanged structure)
   - All existing links preserved

3. **Bottom bar with logo, social links, copyright, and back-to-top:**
   - After the 3-column grid, add a horizontal divider `border-t border-white/[0.06]`
   - Bottom bar layout: `flex flex-col md:flex-row items-center justify-between pt-8 gap-4`
   - Left: logo (increase from `size-8` to `size-9`) + tagline text (increase from `text-sm text-white/40` to `text-base text-[#94A3B8]`)
   - Center: social links row — Twitter/X, LinkedIn, GitHub as small icon-style text links: `text-sm text-[#94A3B8] hover:text-[#C2410C] transition-colors` with `flex items-center gap-6`
   - Right: copyright text + "Back to top" link

4. **Keep existing copper gradient top border** (already correct)

5. **Better vertical spacing:**
   - Increase outer padding from `py-16` to `py-20`
   - Column link spacing stays at `space-y-4`

6. **"Back to top" link** at bottom-right: `text-sm text-[#94A3B8] hover:text-[#C2410C] transition-colors` with an ArrowUp icon from lucide-react. onClick scrolls `window.scrollTo({ top: 0, behavior: 'smooth' })`. This requires adding `'use client'` directive since it uses onClick.
  </action>
  <verify>
    <automated>cd C:/Users/leheh/.Projects/homeservice_agent && grep -c "newsletter\|Newsletter\|Stay in the loop" src/app/components/landing/LandingFooter.jsx && grep -c "md:grid-cols-3" src/app/components/landing/LandingFooter.jsx && grep -c "size-9" src/app/components/landing/LandingFooter.jsx && grep -c "scrollTo" src/app/components/landing/LandingFooter.jsx && ! grep -q "md:grid-cols-4" src/app/components/landing/LandingFooter.jsx && echo "NO_4_COL_GRID_OK" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - LandingFooter.jsx contains "Stay in the loop" newsletter heading text
    - LandingFooter.jsx contains an email input element with `placeholder` attribute
    - LandingFooter.jsx uses `md:grid-cols-3` (3-column grid preserved per D-10 — NOT md:grid-cols-4)
    - LandingFooter.jsx does NOT contain `md:grid-cols-4` (D-10 compliance: no structural change to column layout)
    - LandingFooter.jsx bottom bar contains social links (Twitter/X, LinkedIn, GitHub)
    - LandingFooter.jsx logo uses `size-9` (was size-8, originally size-7)
    - LandingFooter.jsx contains `'use client'` directive (for back-to-top scroll)
    - LandingFooter.jsx contains `scrollTo` for back-to-top functionality
    - LandingFooter.jsx retains the copper gradient top border (`linear-gradient.*#C2410C`)
    - LandingFooter.jsx retains all existing links (Features, Pricing, How it works, About, Contact, Terms, Privacy)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Footer is dramatically upgraded with newsletter CTA section above columns, 3-column layout preserved per D-10, social links in bottom bar next to copyright, larger logo, back-to-top link, and improved typography hierarchy. Structurally enhanced without violating column layout constraint.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. HowItWorksSection has LIGHT background (#F5F5F4), not dark charcoal
3. FeaturesGrid has DARK background (#0F172A) with all dark cards
4. Footer has newsletter section, 3-column grid (per D-10), social links in bottom bar
5. Footer does NOT have md:grid-cols-4 (D-10 compliance check)
6. No large gap between HIW CTA and third step
7. Visual rhythm: Dark hero -> Light HIW -> Dark Features -> Dark Social -> Copper CTA
</verification>

<success_criteria>
- Home page has dramatically more visual change than just background swaps
- How It Works is on light background with tightened layout (gap fixed)
- Features section has completely different visual treatment (dark cards on dark bg)
- Footer is visually enhanced with newsletter and social sections while preserving 3-column structure per D-10
- Build passes, no runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-public-pages-redesign/13-05-SUMMARY.md`
</output>
